import ml from "maplibre-gl";
const defaultMarker$1 = "data:image/png;base64,";
let geomanVersion;
try {
  geomanVersion = "free";
} catch {
  geomanVersion = process.env.VITE_GEOMAN_VERSION || null;
}
const GM_PREFIX = "gm";
const GM_SYSTEM_PREFIX = `_${GM_PREFIX}`;
const IS_PRO = geomanVersion === "pro";
const systemControls = {
  draw: {
    marker: {
      type: "draw",
      eventType: "toggle",
      targetMode: "marker",
      settings: {
        exclusive: true
      }
    },
    circle_marker: {
      type: "draw",
      eventType: "toggle",
      targetMode: "circle_marker",
      settings: {
        exclusive: true
      }
    },
    text_marker: {
      type: "draw",
      eventType: "toggle",
      targetMode: "text_marker",
      settings: {
        exclusive: true
      }
    },
    circle: {
      type: "draw",
      eventType: "toggle",
      targetMode: "circle",
      settings: {
        exclusive: true
      }
    },
    ellipse: {
      type: "draw",
      eventType: "toggle",
      targetMode: "ellipse",
      settings: {
        exclusive: true
      }
    },
    line: {
      type: "draw",
      eventType: "toggle",
      targetMode: "line",
      settings: {
        exclusive: true
      }
    },
    rectangle: {
      type: "draw",
      eventType: "toggle",
      targetMode: "rectangle",
      settings: {
        exclusive: true
      }
    },
    polygon: {
      type: "draw",
      eventType: "toggle",
      targetMode: "polygon",
      settings: {
        exclusive: true
      }
    },
    freehand: {
      type: "draw",
      eventType: "toggle",
      targetMode: "freehand",
      settings: {
        exclusive: true
      }
    },
    custom_shape: {
      type: "draw",
      eventType: "toggle",
      targetMode: "custom_shape",
      settings: {
        exclusive: true
      }
    }
  },
  edit: {
    drag: {
      type: "edit",
      eventType: "toggle",
      targetMode: "drag",
      settings: {
        exclusive: true
      }
    },
    change: {
      type: "edit",
      eventType: "toggle",
      targetMode: "change",
      settings: {
        exclusive: true
      }
    },
    rotate: {
      type: "edit",
      eventType: "toggle",
      targetMode: "rotate",
      settings: {
        exclusive: true
      }
    },
    scale: {
      type: "edit",
      eventType: "toggle",
      targetMode: "scale",
      settings: {
        exclusive: true
      }
    },
    copy: {
      type: "edit",
      eventType: "toggle",
      targetMode: "copy",
      settings: {
        exclusive: true
      }
    },
    cut: {
      type: "edit",
      eventType: "toggle",
      targetMode: "cut",
      settings: {
        exclusive: true
      }
    },
    split: {
      type: "edit",
      eventType: "toggle",
      targetMode: "split",
      settings: {
        exclusive: true
      }
    },
    union: {
      type: "edit",
      eventType: "toggle",
      targetMode: "union",
      settings: {
        exclusive: true
      }
    },
    difference: {
      type: "edit",
      eventType: "toggle",
      targetMode: "difference",
      settings: {
        exclusive: true
      }
    },
    line_simplification: {
      type: "edit",
      eventType: "toggle",
      targetMode: "line_simplification",
      settings: {
        exclusive: true
      }
    },
    lasso: {
      type: "edit",
      eventType: "toggle",
      targetMode: "lasso",
      settings: {
        exclusive: true
      }
    },
    delete: {
      type: "edit",
      eventType: "toggle",
      targetMode: "delete",
      settings: {
        exclusive: true
      }
    }
  },
  helper: {
    shape_markers: {
      type: "helper",
      eventType: "toggle",
      targetMode: "shape_markers",
      settings: {
        exclusive: false,
        enabledBy: ["drag", "change", "rotate", "scale", "line_simplification"]
      }
    },
    snapping: {
      type: "helper",
      eventType: "toggle",
      targetMode: "snapping",
      settings: {
        exclusive: false
      }
    },
    pin: {
      type: "helper",
      eventType: "toggle",
      targetMode: "pin",
      settings: {
        exclusive: false
      }
    },
    snap_guides: {
      type: "helper",
      eventType: "toggle",
      targetMode: "snap_guides",
      settings: {
        exclusive: false
      }
    },
    measurements: {
      type: "helper",
      eventType: "toggle",
      targetMode: "measurements",
      settings: {
        exclusive: false
      }
    },
    auto_trace: {
      type: "helper",
      eventType: "toggle",
      targetMode: "auto_trace",
      settings: {
        exclusive: false
      }
    },
    geofencing: {
      type: "helper",
      eventType: "toggle",
      targetMode: "geofencing",
      settings: {
        exclusive: false
      }
    },
    zoom_to_features: {
      type: "helper",
      eventType: "click",
      targetMode: "zoom_to_features",
      settings: {
        exclusive: false
      }
    },
    click_to_edit: {
      type: "helper",
      eventType: "toggle",
      targetMode: "click_to_edit",
      settings: {
        exclusive: false
      }
    }
  }
};
const change = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="m22.775 29.562 6.75-6.75-5.625-5.625-3.6 3.6-3.15-3.15 3.6-3.6-5.626-5.55-6.75 6.75zm25.95 26.101 6.75-6.825-5.624-5.625-3.6 3.6-3.15-3.15 3.6-3.6-5.55-5.55-6.75 6.75zm.6-46.126 5.25 5.25ZM16.7 59.039H5v-11.7l14.55-14.551L2 15.237l13.05-13.2 17.7 17.624L46.1 6.312q.676-.676 1.5-.976.826-.3 1.65-.3.826 0 1.65.3.826.3 1.5.976l5.326 5.325q.675.675.975 1.5.3.825.3 1.65t-.3 1.65q-.3.825-.975 1.5l-13.35 13.35L62 48.913l-13.125 13.05-17.551-17.55Zm-7.2-4.5h5.25l29.476-29.552-5.25-5.25L9.5 49.288Zm32.1-32.178-2.625-2.625 5.25 5.25Z"/>\n</svg>';
const circleMarker = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="none" d="M0 0h24v24H0Z"/>\n    <circle cx="32" cy="32" r="26" fill="currentColor" fill-opacity=".15" stroke="currentColor" stroke-width="7"/>\n    <circle cx="32" cy="32" r="5" fill="currentColor"/>\n</svg>';
const circle$1 = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="none" d="M0 0h24v24H0Z"/>\n    <circle cx="32" cy="32" r="26" fill="currentColor" fill-opacity=".15" stroke="currentColor" stroke-width="7"/>\n    <path stroke="currentColor" stroke-dasharray="3" stroke-width="4" d="M6 32h52M32 6v52"/>\n</svg>';
const ellipse$1 = '<?xml version="1.0" encoding="UTF-8" standalone="no"?>\n<svg\n   viewBox="0 0 64 64"\n   version="1.1"\n   id="svg2"\n   xmlns="http://www.w3.org/2000/svg"\n   xmlns:svg="http://www.w3.org/2000/svg">\n  <defs\n     id="defs2" />\n  <path\n     fill="none"\n     d="M 0,0 H 24 V 24 H 0 Z"\n     id="path1"\n     style="display:inline" />\n  <ellipse\n     cx="32"\n     cy="32"\n     fill="currentColor"\n     fill-opacity="0.15"\n     stroke="currentColor"\n     stroke-width="7"\n     id="circle1"\n     rx="26.787416"\n     ry="15.158148" />\n  <path\n     stroke="currentColor"\n     stroke-dasharray="3"\n     stroke-width="4"\n     d="M 6.4966994,32 H 58 M 32,18.552705 v 30.15073"\n     id="path2"\n     style="stroke-width:3;stroke-dasharray:3;stroke-dashoffset:0" />\n</svg>\n';
const drag = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path\n            fill="currentColor"\n            d="M32 61.4 19.85 49.42l3.28-3.235 6.634 6.542V34.205H10.98l6.26 6.174-3.279 3.234L2.185 32l11.852-11.686 3.28 3.234-6.336 6.247h18.783V11.273l-6.261 6.174-3.28-3.234L32 2.6l11.777 11.613-3.28 3.234-6.26-6.174v18.522h18.782l-6.26-6.174 3.279-3.234L61.815 32 50.038 43.613l-3.28-3.234 6.261-6.174H34.236v18.522l6.634-6.542 3.28 3.234z"\n    />\n</svg>\n';
const line = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <line x1="8" y1="50" x2="24" y2="20" stroke="currentColor" stroke-width="8"/>\n    <line x1="24" y1="20" x2="40" y2="54" stroke="currentColor" stroke-width="8"/>\n    <line x1="40" y1="54" x2="54" y2="10" stroke="currentColor" stroke-width="8"/>\n    <circle cx="8" cy="50" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="24" cy="20" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="40" cy="54" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="54" cy="10" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n</svg>\n';
const marker = '<svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 64 64">\n    <path d="M32 62.81s23.107-21.898 23.107-38.512a23.107 23.107 0 0 0-46.214 0C8.893 40.912 32 62.809 32 62.809m0-26.958a11.554 11.554 0 1 1 0-23.107 11.554 11.554 0 0 1 0 23.107"/>\n</svg>\n';
const polygon$1 = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <!-- Polygon -->\n    <polygon points="8,28 40,6 55,35 51,55 18,54" fill="none" stroke="currentColor" stroke-width="8"/>\n\n    <!-- Vertex Circles -->\n    <circle cx="9" cy="28" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="40" cy="8" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="55" cy="35" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="50" cy="55" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="18" cy="54" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n</svg>\n';
const rectangle = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <!-- Rectangle -->\n    <polygon points="10,10 54,10 54,54 10,54" fill="none" stroke="currentColor" stroke-width="8"/>\n\n    <!-- Vertex Circles -->\n    <circle cx="10" cy="10" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="54" cy="10" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="54" cy="54" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n    <circle cx="10" cy="54" r="6" fill="white" stroke="currentColor" stroke-width="4.5"/>\n</svg>\n';
const remove = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M39.063 42.14 22.288 28.12 7.96 44.603l11.338 9.854h9.059zm-3.498 12.317h25.941v5.465h-44.25L4.377 48.726a5.465 5.465 0 0 1-.542-7.711L34.311 5.958a5.465 5.465 0 0 1 7.712-.538l16.496 14.343a5.465 5.465 0 0 1 .541 7.709z"/>\n</svg>';
const rotate = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M58.709 17.724c-.3-.6-.601-.901-.902-1.502l-.3-.601c-.301-.601-.602-.902-.902-1.503l-.3-.601c-.301-.601-.602-.902-1.203-1.503l-.6-.901 8.414-9.317-13.825.3-13.824.301 1.502 13.524 1.503 13.524 9.617-10.819v.3l.3.602c.301.3.301.6.602.6l.3.602c0 .6 0 .901.3 1.202.903 2.104 1.804 4.208 2.105 6.311.6 4.208 0 8.716-1.804 12.623l-.6 1.202-.301.3-.902 1.503-.3.601c-.601.601-1.202 1.503-1.804 2.104-1.502 1.503-3.305 3.005-5.109 3.907-1.803 1.202-3.907 1.803-6.311 2.404-2.104.301-4.508.601-6.612.301-2.404-.3-4.508-.902-6.612-1.503-2.103-.901-3.907-2.104-5.71-3.606l-1.202-1.202-.601-.902-.601-.601c-.3-.3-.601-.601-.601-.902l-.902-1.202-.3-.3-.602-1.203c0-.3-.3-.3-.3-.6l-.902-1.503-.3-.601-.902-2.705q-.901-3.607-.901-7.213V27.04c0-.601 0-.901.3-1.202l.3-1.803.602-1.803c.901-2.405 2.103-4.509 3.606-6.612q2.254-3.156 5.41-5.41c.6-.3.901-1.202.3-1.803-.3-.3-.6-.601-.901-.601h-.601l-.3.3c-2.706 1.202-4.81 3.006-6.913 4.809-2.104 2.104-3.607 4.508-5.11 7.213-2.704 5.41-3.606 11.42-2.404 17.431.3 1.503.601 2.705.902 4.207l.902 2.405c.3.3.6.6.6 1.202l.602 1.202c0 .3.3.601.3.601l.902 1.503c.3.601.6.901.901 1.503l.301.6c.3.301.601.902.902 1.203l2.103 1.803c2.104 2.104 4.809 3.907 7.514 5.41q4.057 2.254 9.016 2.705c1.503.3 3.005.3 4.508.3 1.803 0 3.306 0 4.809-.3 3.005-.602 6.311-1.503 9.016-3.006l.6-.3c.602-.3.903-.601 1.504-.902l2.103-1.202c.601-.3.902-.601 1.203-.902l.6-.6a3.622 3.622 0 0 0 1.503-1.504l.3-.3c.902-.902 2.105-2.104 2.706-3.005l1.803-2.705 1.202-1.804c3.005-5.71 4.208-12.322 3.306-18.633-.601-3.306-1.503-6.612-3.005-9.317z"/>\n</svg>\n';
const snapGuides = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path stroke="currentColor"\n          stroke-width="3.8"\n          d="m2.155 32.345 42.35-29.82M18.963 60.919l42.35-29.82M31.443 2.332l29.82 42.35M2.207 19.515l29.82 42.35"/>\n</svg>\n';
const textMarker = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="none" d="M0 0h24v24H0Z"/>\n    <path fill="currentColor"\n          d="M6.962 3.385h50.076a3.577 3.577 0 0 1 3.577 3.577v50.076a3.577 3.577 0 0 1-3.577 3.577H6.962a3.577 3.577 0 0 1-3.577-3.577V6.962a3.577 3.577 0 0 1 3.577-3.577Zm3.577 7.154v42.922h42.922V10.539Zm7.154 7.154h28.614v7.153H17.693Zm0 14.307h28.614v7.154H17.693Z"/>\n</svg>\n';
const autoTrace = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M16.862 2.13 7.297 21.17a2.091 2.091 0 0 0-.193 1.288l4.107 24.166-7.618 13.15 3.619 2.096 7.997-13.804a2.091 2.091 0 0 0 .252-1.397l-4.119-24.235 9.256-18.428Zm40.876.255-1.25 2.49 2.668 1.341 1.25-2.49zm-2.593 5.16-2.68 5.337 2.668 1.341 2.682-5.338zm-4.022 8.007-2.682 5.338 2.67 1.34 2.681-5.337zm-18.67 5.83.055 6.984-11.118.087.056 7.242 11.12-.088.054 6.985 10.521-10.688zm18.927 2.817-2.945.5 1.001 5.89 2.945-.5zm1.502 8.835-2.945.5 1.001 5.89 2.945-.5zm1.502 8.834-2.945.5.74 4.355-.24.413 2.585 1.499.51-.881a1.494 1.494 0 0 0 .18-1zm-3.943 7.854-2.994 5.17 2.584 1.496 2.994-5.169zm-4.492 7.754-1.497 2.584 2.584 1.497 1.498-2.584z"/>\n</svg>\n';
const copy = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M41.687.926a2.173 2.173 0 0 0-.433.045c-4.075.232-7.358 3.618-7.358 7.747 0 .084.01.164.012.246l-12.987 4.261a7.842 7.842 0 0 0-6.126-3.563 2.173 2.173 0 0 0-.424-.043 2.173 2.173 0 0 0-.435.044c-4.073.232-7.358 3.618-7.358 7.747 0 3.143 1.901 5.869 4.604 7.097L8.377 35.11a2.173 2.173 0 0 0-.028.007C4.274 35.35.99 38.736.99 42.865c0 4.277 3.514 7.791 7.791 7.791 2.497 0 4.727-1.201 6.157-3.048l12.887 6.235a7.67 7.67 0 0 0-.139 1.44c0 4.277 3.515 7.791 7.792 7.791 4.278 0 7.793-3.514 7.793-7.792a7.67 7.67 0 0 0-1.885-5.027l.508-1.05-3.263-1.579 4.505-29.969.888-.29-.368-1.122a7.78 7.78 0 0 0 2.28-1.018l.447 1.364 4.72-1.547-.774-2.359-2.522.826a7.73 7.73 0 0 0 1.672-4.793c0-4.132-3.288-7.52-7.368-7.748a2.173 2.173 0 0 0-.424-.044zm0 4.346a3.414 3.414 0 0 1 3.447 3.446 3.415 3.415 0 0 1-3.446 3.447 3.414 3.414 0 0 1-3.446-3.447 3.414 3.414 0 0 1 3.445-3.446zm20.076 3.725a1.242 1.242 0 0 0-.382.062l-1.612.528.578 1.76-.293 1.94 2.456.37.486-3.234a1.242 1.242 0 0 0-1.233-1.426Zm-4.354 1.364-4.72 1.548.775 2.36 4.719-1.548-.774-2.36zm-22.153 2.732a7.877 7.877 0 0 0 3.735 2.922l-.062.408-.397.13.26.793-4.255 28.298-3.818-1.847-1.082 2.235 3.718 1.798a7.841 7.841 0 0 0-3.543 2.147l-13.272-6.42c.02-.23.035-.458.035-.692 0-2.888-1.609-5.408-3.969-6.745l2.912-11.012c3.74-.562 6.645-3.81 6.645-7.698l-.001-.02 13.094-4.297zm-20.885.87a3.414 3.414 0 0 1 3.445 3.447 3.414 3.414 0 0 1-3.445 3.447 3.414 3.414 0 0 1-3.447-3.447 3.414 3.414 0 0 1 3.446-3.446zm45.314 1.78-.738 4.912 2.456.37.738-4.912zM36.172 17.33l-1.793.588a1.242 1.242 0 0 0-.813.862l-.788 2.978 2.401.634.615-2.325 1.15-.377-.773-2.36zm22.405 5.781-.737 4.913 2.455.368.739-4.91zm-26.432 1.048-1.27 4.802 2.401.634 1.27-4.802zm25.325 6.32-.739 4.913 2.457.368.738-4.911-2.456-.37zm-27.23.882-1.27 4.802 2.402.634 1.27-4.801zm26.124 6.486-.739 4.91 2.455.37.74-4.912-2.457-.368zm-28.027.717-1.27 4.803 2.4.634 1.27-4.802-2.401-.634zm-19.555.856a3.414 3.414 0 0 1 3.446 3.446 3.414 3.414 0 0 1-3.446 3.447 3.414 3.414 0 0 1-3.445-3.447 3.414 3.414 0 0 1 3.445-3.446zm46.473 5.795-.739 4.91 2.456.37.74-4.911zm-11.124 5.072-1.082 2.236 4.47 2.164 1.083-2.237zm-8.651 1.55a3.414 3.414 0 0 1 3.445 3.446 3.414 3.414 0 0 1-3.445 3.447 3.414 3.414 0 0 1-3.447-3.447 3.414 3.414 0 0 1 3.446-3.446zm18.669.745-.358 2.378-2.953-1.428-1.083 2.236 4.472 2.163.541-1.117 1.228.183.608-4.046-2.455-.37z"/>\n</svg>\n';
const customShape = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <circle cx="49.754" cy="50.219" r="10.098" fill="none" stroke="currentColor" stroke-width="5.1"/>\n    <path fill="none" stroke="currentColor" stroke-linejoin="round" stroke-width="4.491"\n          d="M44.516 26.657H17.842l6.669-11.55 6.668-11.55 6.669 11.55Z"/>\n    <path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-miterlimit="6"\n          stroke-width="4.683" d="M4.481 40.605h20.761v19.093H4.481z"/>\n</svg>\n';
const cut = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path stroke="currentColor" fill="currentColor" stroke-width="2"\n          d="M44.095 2.247a2.285 2.285 0 0 0-.135.04L12.025 12.762a2.285 2.285 0 0 0-1.497 1.588l-7.772 29.4a2.285 2.285 0 0 0 1.213 2.641L35.397 61.6a2.285 2.285 0 0 0 2.873-.754l22.64-32.582a2.285 2.285 0 0 0 .05-2.534L46.6 3.23a2.285 2.285 0 0 0-2.504-.982ZM43.7 7.182l12.203 19.12-5.167 1.76 1.052 3.09 2.031-.693L38.07 53.122l.01-1.375-3.262-.023-.034 4.5-27.13-13.127 6.975-26.38Zm3.946 21.932-6.18 2.105 1.052 3.09 6.18-2.105zm-9.27 3.157-2.329.793a1.632 1.632 0 0 0-1.105 1.534l-.03 4.068 3.265.024.02-2.91 1.232-.419zm-3.488 9.66-.047 6.529 3.264.023.048-6.528z"/>\n</svg>\n';
const difference$1 = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="m41.478 7.967-.033 3.069c1.887.02 3.772.337 5.561.933l.97-2.913a21.337 21.337 0 0 0-6.498-1.09Zm-3.294.215a21.567 21.567 0 0 0-6.298 1.915l1.332 2.766a18.518 18.518 0 0 1 5.401-1.641zm12.836 2.155-1.402 2.73a18.497 18.497 0 0 1 4.572 3.312l2.16-2.179a21.548 21.548 0 0 0-5.331-3.863ZM22.783 13.06c-11.848 0-21.486 9.638-21.486 21.486 0 11.849 9.638 21.486 21.486 21.486a21.37 21.37 0 0 0 14.288-5.466c.003 0 .006 0 .009.002.04-.036.078-.075.117-.11l.077-.072a21.285 21.285 0 0 0 1.51-1.532 22.128 22.128 0 0 0 .867-1.025 19.557 19.557 0 0 1-.88-.095c-.196-.025-.389-.058-.582-.09-.097-.015-.194-.028-.29-.045a18.378 18.378 0 0 1-1.576-.359l-.118-.03c-7.753-2.172-13.422-9.271-13.422-17.728a18.34 18.34 0 0 1 5.867-13.478c.1-.093.199-.185.3-.276.153-.136.31-.268.467-.4.097-.08.193-.163.291-.242.255-.203.514-.402.78-.592a21.534 21.534 0 0 0-1.76-.592l-.018-.005c-.176-.05-.355-.095-.533-.141-.132-.035-.263-.073-.397-.105-.213-.051-.43-.095-.646-.14-.077-.016-.153-.035-.231-.05H26.9a21.37 21.37 0 0 0-4.118-.4Zm35.728 3.64-2.472 1.82a18.424 18.424 0 0 1 2.61 5.004l2.907-.986a21.474 21.474 0 0 0-3.045-5.839Zm3.864 9.034-3.022.534c.328 1.86.37 3.772.118 5.642l3.042.409a21.448 21.448 0 0 0-.138-6.585zm-3.496 8.943a18.539 18.539 0 0 1-2.416 5.104l2.543 1.72a21.586 21.586 0 0 0 2.815-5.952zM54.703 42a18.288 18.288 0 0 1-4.428 3.492l1.505 2.675a21.348 21.348 0 0 0 5.173-4.08zm-6.995 4.69a18.512 18.512 0 0 1-5.526 1.158l.163 3.065a21.56 21.56 0 0 0 6.444-1.35z"/>\n</svg>\n';
const freehand = '<svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 64 64">\n    <path d="M44.698 2.661a1.644 1.644 0 0 1 2.328 0l9.866 9.866a1.644 1.644 0 0 1 0 2.329L24.004 47.744a1.644 1.644 0 0 1-.552.362L7.008 54.683a1.644 1.644 0 0 1-2.138-2.138l6.578-16.444a1.644 1.644 0 0 1 .362-.552zm-3.089 7.742 7.542 7.541 4.252-4.252-7.541-7.541zm5.216 9.866-7.54-7.54-21.378 21.376v.964h1.644a1.644 1.644 0 0 1 1.645 1.644v1.645h1.644a1.644 1.644 0 0 1 1.645 1.644v1.644h.963zM14.724 37.29l-.35.348L9.35 50.204l12.566-5.025.349-.35a1.644 1.644 0 0 1-1.07-1.538v-1.645h-1.644a1.644 1.644 0 0 1-1.644-1.644v-1.644h-1.644a1.644 1.644 0 0 1-1.54-1.07"/>\n    <path stroke="currentColor" stroke-width="5.107"\n          d="M6.298 59.53c3.44-1.692 6.88-3.384 10.558-4.319s7.594-1.111 11.719-.298c4.125.814 8.459 2.617 13.483 2.655 5.023.038 10.737-1.689 16.45-3.415"/>\n</svg>\n';
const geoFencing = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M31.226 17.87c-4.69 0-8.532 3.832-8.532 8.508 0 1.812.58 3.495 1.558 4.88l5.932 10.255c.831 1.085 1.383.879 2.074-.058l6.543-11.135c.132-.24.236-.493.327-.753a8.396 8.396 0 0 0 .628-3.19c0-4.675-3.841-8.506-8.53-8.506zm0 3.987a4.5 4.5 0 0 1 4.532 4.52 4.5 4.5 0 0 1-4.532 4.521 4.5 4.5 0 0 1-4.534-4.52 4.5 4.5 0 0 1 4.533-4.52z"/>\n    <path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-miterlimit="6"\n          stroke-width="5" d="M8.97 53.509 4.423 19.145 35.698 4.199l23.88 25.127L43.06 59.801Z"/>\n</svg>\n';
const lasso = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <ellipse cx="4.907" cy="-32.051" fill="none" stroke="currentColor" stroke-dasharray="13.7543, 6.87718"\n             stroke-width="5.489" rx="20.4" ry="13.713" transform="matrix(-.71127 .70292 -.86047 -.5095 0 0)"/>\n    <path fill="currentColor"\n          d="M34.597 25.275a1.934 1.934 0 0 0-2.04 2.165l2.922 31.37c.188 1.523 1.989 2.235 3.165 1.249l6.169-5.004 2.765 4.79c1.59 2.756 4.115 3.431 6.87 1.841 2.756-1.59 3.433-4.114 1.841-6.87l-2.755-4.773 7.31-2.803c1.443-.525 1.728-2.44.503-3.365L35.64 25.66a1.93 1.93 0 0 0-1.042-.383z"/>\n</svg>\n';
const lineSimplification = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M1.947 25.554c-2.176 3.77-.867 8.656 2.903 10.832 2.126 1.229 4.607 1.346 6.757.539l8.252 12.152a7.79 7.79 0 0 0-.701 1.005c-2.176 3.77-.867 8.656 2.903 10.833 3.77 2.177 8.655.867 10.832-2.903 2.1-3.64.93-8.294-2.543-10.572a2.211 2.211 0 0 0-.36-.26 2.211 2.211 0 0 0-.404-.181 7.955 7.955 0 0 0-6.086-.432l-8.348-12.292c.19-.252.369-.512.53-.79 1.457-2.526 1.339-5.537-.03-7.907l8.202-8.256c3.494 1.335 7.562.025 9.575-3.229l13.899 2.913c.266 2.435 1.634 4.721 3.899 6.03 3.77 2.176 8.655.866 10.832-2.904 2.101-3.64.93-8.294-2.543-10.572a2.211 2.211 0 0 0-.36-.26 2.211 2.211 0 0 0-.403-.181c-3.71-1.87-8.328-.556-10.429 3.083-.084.147-.155.297-.228.446l-13.5-2.83c-.053-2.55-1.329-5.023-3.58-6.499a2.211 2.211 0 0 0-.358-.259 2.211 2.211 0 0 0-.405-.181c-3.708-1.87-8.326-.556-10.428 3.083-1.61 2.79-1.307 6.19.486 8.652l-7.84 7.892a2.211 2.211 0 0 0-.094-.036c-3.71-1.87-8.328-.556-10.429 3.084zm3.83 2.211a3.473 3.473 0 0 1 4.79-1.282 3.472 3.472 0 0 1 1.284 4.789 3.473 3.473 0 0 1-4.79 1.284 3.475 3.475 0 0 1-1.283-4.79ZM23.656 8.173a3.474 3.474 0 0 1 4.79-1.283 3.472 3.472 0 0 1 1.283 4.79 3.473 3.473 0 0 1-4.789 1.284 3.475 3.475 0 0 1-1.284-4.791zm-.666 44.12a3.474 3.474 0 0 1 4.79-1.283 3.472 3.472 0 0 1 1.283 4.79 3.473 3.473 0 0 1-4.79 1.284 3.475 3.475 0 0 1-1.283-4.791zm29.166-37.88a3.474 3.474 0 0 1 4.79-1.283 3.472 3.472 0 0 1 1.283 4.79 3.473 3.473 0 0 1-4.79 1.284 3.475 3.475 0 0 1-1.283-4.791z"/>\n    <path fill="currentColor"\n          d="M12.9 13.516c-.63.177-.685.37-.88.95l-1.069 3.18 2.995 1.006.844-2.506 2.074-.882-1.236-2.908zm19.209 5.25 4.378 4.554 2.277-2.19-4.379-4.554zm8.132 3.106 1.441 2.812 5.623-2.882-1.441-2.812zM5.448 46.082l3.144.304.608-6.29-3.145-.303zm4.623 3.728 5.762 2.592 1.297-2.88-5.762-2.593z"/>\n</svg>\n';
const measurements = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M1.717 38.645a2.675 2.668 79.7 0 0-.307 3.769L16.754 60.55a2.675 2.668 79.7 0 0 3.76.312l41.769-35.508a2.675 2.668 79.7 0 0 .307-3.769L47.246 3.45a2.675 2.668 79.7 0 0-3.76-.312zm5.486 2.352 37.702-32.05 11.892 14.056-2.717 2.31-4.458-5.269-2.033 1.729 4.457 5.269-3.11 2.643-4.457-5.268-2.034 1.728 4.458 5.27-3.106 2.64-4.458-5.27-2.033 1.73 4.457 5.268-3.11 2.644-6.194-7.32-2.033 1.728 6.193 7.32-3.11 2.645-4.457-5.269-2.034 1.729 4.458 5.268-3.106 2.64-4.458-5.268-2.033 1.73 4.457 5.268-3.11 2.643-4.457-5.268-2.034 1.729 4.458 5.268-2.098 1.783z"/>\n</svg>\n';
const mouse = '<svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 64 64">\n    <path d="M7.839 34.833c-7.104-9.763-5.332-23.33 4.2-30.266C21.572-2.369 35.03.123 42.13 9.882L56.166 29.17c7.098 9.755 5.329 23.325-4.204 30.262-9.533 6.936-22.989 4.444-30.09-5.315L7.841 34.836Zm5.063-25.476c-6.079 5.766-7.006 15.598-1.634 22.98l.778 1.07 12.002-8.732zm3.43-2.495L27.476 22.18l12.002-8.733-.781-1.073c-5.37-7.38-15.01-9.522-22.367-5.512m25.643 10.014L14.54 36.836l10.762 14.79c5.818 7.997 16.653 9.842 24.163 4.378 7.51-5.465 9.088-16.342 3.267-24.342z"/>\n</svg>\n';
const pin = '\uFEFF<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M42.012 2.106a4.092 4.092 0 0 0-5.741.735 4.092 4.092 0 0 0 .572 5.607L23.964 23.45a8.185 8.185 0 0 0-10.458 2.142l5.18 4.005 5.454 4.218L7.87 59.22a1.124 7.304 37.712 0 0 1.54 1.19l20.494-22.138 5.454 4.217 5.18 4.005a8.185 8.185 0 0 0-.558-10.66l11.277-16.24a4.092 4.092 0 0 0 5.572-.858 4.092 4.092 0 0 0-.735-5.74z"/>\n</svg>\n';
const scale = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <g fill="currentColor" stroke="currentColor">\n        <path d="m37.62 5.028-.337.284-4.039 4.068 3.547 3.523L39.875 9.8l2.846.596 1.025-4.893-4.176-.877Zm9.995 6.393 9.787 2.053 1.026-4.895-9.787-2.05zm14.68 3.078 9.787 2.05 1.025-4.892-9.787-2.053zm-39.623 5.522 3.547 3.523 7.049-7.094-3.547-3.523zm-10.57 10.642 3.546 3.524 7.047-7.094-3.547-3.524zM1.529 41.306l3.55 3.523 7.046-7.096-3.547-3.523Zm70.977 4.027 3.91 3.115 3.44-4.387-1.118-6.55zM.947 48.319l5.617 8.274 4.137-2.809-5.617-8.273Zm62.211 8.744 3.91 3.116 6.233-7.82-3.91-3.116zM9.373 60.73l5.62 8.272 4.134-2.809-5.617-8.271zm44.438 8.067 3.91 3.115 6.232-7.82-3.91-3.117zM17.8 73.138l5.617 8.273 4.137-2.808-5.617-8.274zm26.662 7.388 3.91 3.116 6.232-7.82-3.91-3.116zm-18.236 5.022 5.617 8.273 4.136-2.81-5.617-8.272zm8.888 6.709 3.91 3.117 6.233-7.822-3.91-3.115z"\n              stroke-width="3"\n              transform="translate(2.517 1.586) scale(.60827)"/>\n        <path d="M22.672 20.02a3.5 3.5 0 0 0-2.506 1.034L-.514 41.872a3.5 3.5 0 0 0-.412 4.434L18 74.176a3.5 3.5 0 0 0 5.633.216l33.494-42.038a3.5 3.5 0 0 0-2.018-5.605l-31.742-6.654a3.5 3.5 0 0 0-.695-.074zm1.121 7.317 24.32 5.098-26.992 33.877L6.484 44.759Z"\n              stroke-width="3"\n              transform="translate(2.517 1.586) scale(.60827)"/>\n        <path d="m78.734 7.668 2.61 11.396-18.145 4.154 2.705 11.816L84.05 30.88l2.609 11.396L100 21.011Z"\n              stroke-width="3"\n              transform="translate(1.586 1.586) scale(.60827)"/>\n    </g>\n</svg>\n';
const snapping = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor" fill-rule="evenodd"\n          d="M19.435 3.417c-4.478 0-8.96 1.693-12.343 5.076-6.766 6.766-6.766 17.913 0 24.679h.006c2.754 2.749 5.563 5.561 8.357 8.357 1.857 1.859 3.696 3.696 5.553 5.553l.949.949 1.892 1.898 9.496-9.49-1.898-1.898-6.502-6.502c-2.796-2.797-5.6-5.606-8.363-8.362-1.65-1.653-1.652-4.042 0-5.694 1.653-1.653 4.047-1.653 5.7 0l8.357 8.362 6.502 6.502 1.898 1.893 9.49-9.49-1.893-1.893-6.507-6.502-8.357-8.362c-3.384-3.383-7.86-5.076-12.337-5.076Zm0 2.68c3.789 0 7.577 1.432 10.439 4.294l8.357 8.362-5.694 5.694-8.357-8.362a6.7 6.7 0 0 0-9.496 0 6.697 6.697 0 0 0 0 9.49c2.76 2.755 5.566 5.564 8.363 8.362l-5.694 5.694c-2.795-2.796-5.601-5.607-8.357-8.357H8.99c-5.724-5.724-5.724-15.16 0-20.883 2.861-2.862 6.655-4.294 10.444-4.294ZM57.709 29.03a4.266 4.266 0 0 0-4.268 4.268c0 1.594.881 2.973 2.176 3.707-.714 4.997-1.424 10.107-2.176 15.037-.934 0-1.792.309-2.495.818l-7.115-5.07a4.25 4.25 0 0 0 .115-.965 4.27 4.27 0 1 0-4.268 4.268 4.23 4.23 0 0 0 2.732-1.012l6.973 4.965a4.22 4.22 0 0 0-.215 1.269 4.27 4.27 0 0 0 4.273 4.268 4.266 4.266 0 0 0 4.268-4.268 4.247 4.247 0 0 0-1.657-3.356l2.208-15.446c2.095-.273 3.722-2.045 3.722-4.215a4.27 4.27 0 0 0-4.273-4.268z"\n    />\n</svg>\n';
const split = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <g fill="currentColor">\n        <path d="m4.879 2.684-2.832 2.8L7.65 11.15l2.832-2.801Zm8.403 8.495-2.832 2.802 5.603 5.662 2.831-2.802zm8.404 8.494-2.832 2.8 5.602 5.664 2.832-2.802z"\n        />\n        <path d="M27.814 7.408a3.576 3.576 0 0 0-1.161-.176 3.2 3.2 0 0 0-.878.138l7.963 26.068L7.18 27.3c-.543 2.352 1.67 6.055 4.962 6.816l23.054 5.297 2.326 6.42c-2.27-.175-4.279 1.912-4.908 3.805-.535 1.698-.512 3.656.085 5.61.597 1.954 1.671 3.59 3.064 4.7 1.393 1.11 3.238 1.71 5.023 1.164 8.064-3.039 2.135-15.793.467-20.58 5.917 1.023 18.77 5.88 20.578-1.979.42-1.819-.306-3.616-1.509-4.929-1.203-1.313-2.909-2.27-4.9-2.73-1.99-.46-3.866-.547-5.602.303-1.737.849-3.768 2.642-3.468 4.73a6.17 6.17 0 0 0 .012.43l-6.563-1.794-6.88-22.632c-.702-2.297-3.445-3.99-5.108-4.523Zm26.824 26.88c1.36.315 2.472.99 3.116 1.692.644.703.795 1.307.684 1.79-.112.482-.511.956-1.399 1.305-.887.348-2.182.467-3.542.152-1.361-.314-2.472-.99-3.116-1.693-.644-.703-.797-1.304-.686-1.787.112-.482.513-.956 1.4-1.305a5.198 5.198 0 0 1 1.594-.327 6.98 6.98 0 0 1 1.949.173zM37.846 49.174c.333.069.71.25 1.13.585.746.594 1.497 1.654 1.905 2.99.408 1.335.379 2.635.092 3.545-.287.91-.731 1.34-1.205 1.484-.473.145-1.085.036-1.831-.558-.746-.594-1.496-1.655-1.904-2.99-.407-1.336-.378-2.637-.092-3.546.287-.91.732-1.342 1.206-1.486.118-.036.245-.057.38-.058.102 0 .207.011.319.034z"\n        />\n    </g>\n</svg>\n';
const union$1 = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M41.233 7.917c-5.5 0-10.519 2.077-14.328 5.482a21.443 21.443 0 0 0-4.138-.404c-11.88 0-21.544 9.664-21.544 21.544s9.664 21.544 21.544 21.544c5.5 0 10.519-2.077 14.328-5.482 1.34.263 2.722.404 4.138.404 11.88 0 21.544-9.664 21.544-21.544S53.113 7.917 41.233 7.917zm-9.969 6.791a21.61 21.61 0 0 1 5.598 3.499l-2.01 2.33a18.557 18.557 0 0 0-4.8-2.999Zm-7.927 2.765 2.557 1.714a18.558 18.558 0 0 0-2.396 5.128l-2.955-.86a21.614 21.614 0 0 1 2.794-5.982zm15.86 3.08a21.413 21.413 0 0 1 3.46 5.628l-2.839 1.188a18.357 18.357 0 0 0-2.963-4.817zm-19.334 6.142 3.053.399a18.348 18.348 0 0 0 .144 5.654l-3.027.552a21.407 21.407 0 0 1-.17-6.605Zm23.834 2.63a21.81 21.81 0 0 1 .548 3.263c.1 1.1.115 2.207.044 3.31l-3.07-.195c.12-1.888-.056-3.8-.511-5.64zm-19.918 6.172a18.37 18.37 0 0 0 2.64 5.003l-2.466 1.84a21.427 21.427 0 0 1-3.083-5.84Zm17.042 3.016 3.004.666a21.486 21.486 0 0 1-2.404 6.15l-2.66-1.547a18.434 18.434 0 0 0 2.06-5.27zm-12.535 4.124a18.554 18.554 0 0 0 4.6 3.302l-1.392 2.744a21.611 21.611 0 0 1-5.363-3.85Z"/>\n</svg>\n';
const zoomToFeatures = '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">\n    <path fill="currentColor"\n          d="M1.585 1.585V13.75h5.17v1.52H8.58v-1.52h5.17V1.585Zm3.041 3.041h6.083v6.083H4.626Zm9.429 2.13V8.58h3.65V6.755Zm5.475 0V8.58h3.65V6.755Zm5.474 0V8.58h3.65V6.755Zm5.475 0V8.58h3.65V6.755Zm5.475 0V8.58h3.65V6.755Zm5.475 0v1.597a1.521 1.521 0 0 0-.325.152l-15.686 9.758a1.52 1.52 0 0 0-.199 2.437l23.512 20.553a1.52 1.52 0 0 0 2.477-.78l4.212-17.037v.352h1.825v-3.65H55.42v1.524a1.52 1.52 0 0 0-.222-.296L43.002 8.741a1.521 1.521 0 0 0-.182-.16h2.259V6.754Zm5.475 0V8.58h3.65V6.755Zm5.474 0V8.58h3.65V6.755Zm3.042 2.432v3.65h1.825v-3.65Zm-13.752 2.549 10.757 11.137-3.57 14.446-20.087-17.559Zm13.752 2.926v3.65h1.825v-3.65ZM6.755 17.096v3.65H8.58v-3.65zm0 5.475v3.65h1.628a1.52 1.52 0 0 0 1.02.909l12.683 3.406 7.905 12.694c.168.27.417.481.712.602l13.39 4.576a1.52 1.52 0 0 0 1.157-2.814l-12.936-4.389L24.35 28.42a1.52 1.52 0 0 0-.896-.664L10.19 24.193a1.52 1.52 0 0 0-1.61.557v-2.178zm48.665 3.042v3.65h1.825v-3.65ZM6.755 28.046v3.65H8.58v-3.65zm48.665 3.042v3.65h1.825v-3.65ZM6.755 33.52v3.65H8.58v-3.65zm48.665 3.041v3.65h1.825v-3.65ZM6.755 38.996v3.65H8.58v-3.65zm48.665 3.041v3.65h1.825v-3.65zm-35.382.913c-3.454 0-6.287 2.833-6.287 6.287 0 3.076 2.248 5.654 5.179 6.183h-1.225v1.825h3.65V55.42h-.206c2.712-.5 4.777-2.796 5.055-5.59l.002-.002a1.52 1.52 0 0 0 0-1.18c-.314-3.168-2.922-5.698-6.168-5.698zM6.755 44.47v3.65H8.58v-3.65zm13.283 1.521a3.223 3.223 0 0 1 3.246 3.246 3.223 3.223 0 0 1-3.246 3.246 3.223 3.223 0 0 1-3.246-3.246 3.223 3.223 0 0 1 3.246-3.246zm35.382 1.52v2.738h-5.17v12.166h12.165V50.25h-5.17v-2.737ZM6.755 49.946v3.65H8.58v-3.65zm46.536 3.346h6.083v6.083H53.29ZM6.755 55.42v1.825h3.65V55.42H7.668Zm5.475 0v1.825h3.65V55.42zm10.95 0v1.825h3.65V55.42zm5.474 0v1.825h3.65V55.42zm5.475 0v1.825h3.65V55.42zm5.475 0v1.825h3.65V55.42zm5.475 0v1.825h3.65V55.42z"/>\n</svg>\n';
const controlIcons = {
  marker,
  circle: circle$1,
  ellipse: ellipse$1,
  circle_marker: circleMarker,
  text_marker: textMarker,
  line,
  rectangle,
  polygon: polygon$1,
  freehand,
  custom_shape: customShape,
  drag,
  change,
  rotate,
  scale,
  copy,
  cut,
  split,
  delete: remove,
  union: union$1,
  difference: difference$1,
  line_simplification: lineSimplification,
  lasso,
  shape_markers: null,
  snapping,
  pin,
  snap_guides: snapGuides,
  measurements,
  auto_trace: autoTrace,
  geofencing: geoFencing,
  zoom_to_features: zoomToFeatures,
  click_to_edit: mouse
};
const getCircleMarkerStyles = (styleVariables) => {
  return [
    {
      type: "circle",
      paint: {
        "circle-radius": styleVariables.circleMarkerRadius,
        "circle-color": styleVariables.fillColor,
        "circle-opacity": styleVariables.fillOpacity,
        "circle-stroke-color": styleVariables.lineColor,
        "circle-stroke-width": styleVariables.lineWidth,
        "circle-stroke-opacity": styleVariables.lineOpacity
      }
    }
  ];
};
const getControlMarkerStyles = (styleVariables) => {
  return [
    {
      type: "circle",
      paint: {
        "circle-radius": 7,
        "circle-color": "#ffffff",
        "circle-opacity": 1,
        "circle-stroke-color": styleVariables.lineColor,
        "circle-stroke-width": 2,
        "circle-stroke-opacity": 1
      }
    }
  ];
};
const getLineStyles = (styleVariables) => {
  return [
    {
      type: "line",
      paint: {
        "line-color": styleVariables.lineColor,
        "line-opacity": styleVariables.lineOpacity,
        "line-width": styleVariables.lineWidth
      }
    }
  ];
};
const getMarkerStyles = () => {
  return [
    {
      type: "symbol",
      layout: {
        "icon-image": "default-marker",
        "icon-size": 0.18,
        "icon-allow-overlap": true,
        "icon-anchor": "bottom"
      }
    }
  ];
};
const getPolygonStyles = (styleVariables) => {
  return [
    {
      type: "fill",
      paint: {
        "fill-color": styleVariables.fillColor,
        "fill-opacity": styleVariables.fillOpacity
      }
    },
    {
      type: "line",
      paint: {
        "line-color": styleVariables.lineColor,
        "line-opacity": styleVariables.lineOpacity,
        "line-width": styleVariables.lineWidth
      }
    }
  ];
};
const getSecondaryControlMarkerStyles = (styleVariables) => {
  return [
    {
      type: "circle",
      paint: {
        "circle-radius": 6,
        "circle-color": "#ffffff",
        "circle-opacity": 0.6,
        "circle-stroke-color": styleVariables.lineColor,
        "circle-stroke-width": 2,
        "circle-stroke-opacity": 1
      }
    }
  ];
};
const getSnapGuideStyles = () => {
  return [
    {
      type: "line",
      paint: {
        "line-color": "#00979f",
        "line-width": 1.8,
        "line-dasharray": [2, 1]
      }
    }
  ];
};
const FEATURE_PROPERTY_PREFIX = `__${GM_PREFIX}_`;
const FEATURE_ID_PROPERTY = `${FEATURE_PROPERTY_PREFIX}id`;
const LOAD_TIMEOUT = 6e4;
const SOURCES = {
  // order matters here, layers order will be aligned according to these items
  ...IS_PRO && { standby: `${GM_PREFIX}_standby` },
  // available only in the pro version
  main: `${GM_PREFIX}_main`,
  temporary: `${GM_PREFIX}_temporary`,
  internal: `${GM_PREFIX}_internal`
};
const getTextMarkerStyles = () => {
  return [
    {
      type: "symbol",
      layout: {
        "text-field": ["get", `${FEATURE_PROPERTY_PREFIX}text`],
        "text-justify": "center"
      },
      paint: {
        "text-color": "black",
        "text-halo-color": "#fff",
        "text-halo-width": 2
      }
    }
  ];
};
const sourceStyles = {
  [SOURCES.main]: {
    lineColor: "#278cda",
    lineOpacity: 0.8,
    lineWidth: 3,
    fillColor: "#4fb3ff",
    fillOpacity: 0.4,
    circleMarkerRadius: 10
  },
  [SOURCES.temporary]: {
    lineColor: "#ff5600",
    lineOpacity: 0.8,
    lineWidth: 3,
    fillColor: "#4fb3ff",
    fillOpacity: 0.4,
    circleMarkerRadius: 10
  },
  [SOURCES.internal]: {
    lineColor: "#278cda",
    lineOpacity: 0.8,
    lineWidth: 3,
    fillColor: "#4fb3ff",
    fillOpacity: 0.4,
    circleMarkerRadius: 10
  },
  ...IS_PRO && {
    [SOURCES.standby]: {
      lineColor: "#787878",
      lineOpacity: 0.8,
      lineWidth: 3,
      fillColor: "#a5a5a5",
      fillOpacity: 0.4,
      circleMarkerRadius: 10
    }
  }
};
const styles = {
  // order matters here, layers order will be aligned according to these items
  polygon: {
    [SOURCES.main]: getPolygonStyles(sourceStyles[SOURCES.main]),
    [SOURCES.temporary]: getPolygonStyles(sourceStyles[SOURCES.temporary]),
    ...IS_PRO && { [SOURCES.standby]: getPolygonStyles(sourceStyles[SOURCES.standby]) }
  },
  ellipse: {
    [SOURCES.main]: getPolygonStyles(sourceStyles[SOURCES.main]),
    [SOURCES.temporary]: getPolygonStyles(sourceStyles[SOURCES.temporary]),
    ...IS_PRO && { [SOURCES.standby]: getPolygonStyles(sourceStyles[SOURCES.standby]) }
  },
  rectangle: {
    [SOURCES.main]: getPolygonStyles(sourceStyles[SOURCES.main]),
    [SOURCES.temporary]: getPolygonStyles(sourceStyles[SOURCES.temporary]),
    ...IS_PRO && { [SOURCES.standby]: getPolygonStyles(sourceStyles[SOURCES.standby]) }
  },
  circle: {
    [SOURCES.main]: getPolygonStyles(sourceStyles[SOURCES.main]),
    [SOURCES.temporary]: getPolygonStyles(sourceStyles[SOURCES.temporary]),
    ...IS_PRO && { [SOURCES.standby]: getPolygonStyles(sourceStyles[SOURCES.standby]) }
  },
  circle_marker: {
    [SOURCES.main]: getCircleMarkerStyles(sourceStyles[SOURCES.main]),
    [SOURCES.temporary]: getCircleMarkerStyles(sourceStyles[SOURCES.temporary]),
    ...IS_PRO && { [SOURCES.standby]: getCircleMarkerStyles(sourceStyles[SOURCES.standby]) }
  },
  line: {
    [SOURCES.main]: getLineStyles(sourceStyles[SOURCES.main]),
    [SOURCES.temporary]: getLineStyles(sourceStyles[SOURCES.temporary]),
    ...IS_PRO && { [SOURCES.standby]: getLineStyles(sourceStyles[SOURCES.standby]) }
  },
  marker: {
    [SOURCES.temporary]: getMarkerStyles(),
    [SOURCES.main]: getMarkerStyles(),
    ...IS_PRO && { [SOURCES.standby]: getMarkerStyles() }
  },
  text_marker: {
    [SOURCES.main]: getTextMarkerStyles(),
    [SOURCES.temporary]: getTextMarkerStyles(),
    ...IS_PRO && { [SOURCES.standby]: getTextMarkerStyles() }
  },
  dom_marker: {
    // not a geojson source, layers aren't required
    [SOURCES.main]: [],
    [SOURCES.temporary]: [],
    [SOURCES.internal]: [],
    ...IS_PRO && { [SOURCES.standby]: [] }
  },
  center_marker: {
    [SOURCES.temporary]: getControlMarkerStyles(sourceStyles[SOURCES.temporary]),
    [SOURCES.internal]: getControlMarkerStyles(sourceStyles[SOURCES.internal]),
    ...IS_PRO && { [SOURCES.standby]: getControlMarkerStyles(sourceStyles[SOURCES.standby]) }
  },
  vertex_marker: {
    [SOURCES.temporary]: getControlMarkerStyles(sourceStyles[SOURCES.temporary]),
    [SOURCES.internal]: getControlMarkerStyles(sourceStyles[SOURCES.internal]),
    ...IS_PRO && { [SOURCES.standby]: getControlMarkerStyles(sourceStyles[SOURCES.standby]) }
  },
  edge_marker: {
    [SOURCES.temporary]: getSecondaryControlMarkerStyles(sourceStyles[SOURCES.temporary]),
    [SOURCES.internal]: getSecondaryControlMarkerStyles(sourceStyles[SOURCES.internal]),
    ...IS_PRO && {
      [SOURCES.standby]: getSecondaryControlMarkerStyles(sourceStyles[SOURCES.standby])
    }
  },
  snap_guide: {
    // todo: check which sources can't display snap guides (and other shapes) and remove layers
    [SOURCES.main]: getSnapGuideStyles(),
    [SOURCES.temporary]: getSnapGuideStyles(),
    [SOURCES.internal]: getSnapGuideStyles(),
    ...IS_PRO && { [SOURCES.standby]: getSnapGuideStyles() }
  }
};
const defaultMarker = '<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="#0A95F0" class="bi bi-geo-alt-fill"\n     viewBox="0 0 16 16">\n    <path d="M8 16s6-5.686 6-10A6 6 0 0 0 2 6c0 4.314 6 10 6 10m0-7a3 3 0 1 1 0-6 3 3 0 0 1 0 6"/>\n</svg>\n';
const defaultShapeMarker = '<svg xmlns="http://www.w3.org/2000/svg" width="21" height="21">\n    <circle cx="10.5" cy="10.5" r="9.213" fill="#fff" stroke="#278cda" stroke-width="2.303"/>\n</svg>\n';
const defaultOptions = {
  settings: {
    throttlingDelay: 10,
    awaitDataUpdatesOnEvents: true,
    useDefaultLayers: true,
    controlsPosition: "top-left",
    controlsUiEnabledByDefault: true,
    controlsCollapsible: false,
    controlsStyles: {
      controlGroupClass: "maplibregl-ctrl maplibregl-ctrl-group",
      controlContainerClass: "gm-control-container",
      controlButtonClass: "gm-control-button"
    },
    idGenerator: null,
    markerIcons: {
      default: defaultMarker,
      control: defaultShapeMarker
    }
  },
  layerStyles: styles,
  controls: {
    draw: {
      marker: {
        title: "Marker",
        icon: controlIcons.marker,
        uiEnabled: true,
        active: false
      },
      circle_marker: {
        title: "Circle Marker",
        icon: controlIcons.circle_marker,
        uiEnabled: true,
        active: false
      },
      text_marker: {
        title: "Text Marker",
        icon: controlIcons.text_marker,
        uiEnabled: true,
        active: false
      },
      circle: {
        title: "Circle",
        icon: controlIcons.circle,
        uiEnabled: true,
        active: false
      },
      ellipse: {
        title: "Ellipse",
        icon: controlIcons.ellipse,
        uiEnabled: true,
        active: false
      },
      line: {
        title: "Line",
        icon: controlIcons.line,
        uiEnabled: true,
        active: false
      },
      rectangle: {
        title: "Rectangle",
        icon: controlIcons.rectangle,
        uiEnabled: true,
        active: false
      },
      polygon: {
        title: "Polygon",
        icon: controlIcons.polygon,
        uiEnabled: true,
        active: false
      }
    },
    edit: {
      drag: {
        title: "Drag",
        icon: controlIcons.drag,
        uiEnabled: true,
        active: false
      },
      change: {
        title: "Change",
        icon: controlIcons.change,
        uiEnabled: true,
        active: false
      },
      rotate: {
        title: "Rotate",
        icon: controlIcons.rotate,
        uiEnabled: true,
        active: false
      },
      cut: {
        title: "Cut",
        icon: controlIcons.cut,
        uiEnabled: true,
        active: false
      },
      delete: {
        title: "Delete",
        icon: controlIcons.delete,
        uiEnabled: true,
        active: false
      }
    },
    helper: {
      shape_markers: {
        title: "Shape markers",
        icon: null,
        uiEnabled: false,
        active: false
      },
      snapping: {
        title: "Snapping",
        icon: controlIcons.snapping,
        uiEnabled: true,
        active: false
      },
      zoom_to_features: {
        title: "Zoom to features",
        icon: controlIcons.zoom_to_features,
        uiEnabled: true,
        active: false
      }
    }
  }
};
var freeGlobal = typeof global == "object" && global && global.Object === Object && global;
var freeSelf = typeof self == "object" && self && self.Object === Object && self;
var root$1 = freeGlobal || freeSelf || Function("return this")();
var Symbol$1 = root$1.Symbol;
var objectProto$g = Object.prototype;
var hasOwnProperty$d = objectProto$g.hasOwnProperty;
var nativeObjectToString$1 = objectProto$g.toString;
var symToStringTag$1 = Symbol$1 ? Symbol$1.toStringTag : void 0;
function getRawTag(value) {
  var isOwn = hasOwnProperty$d.call(value, symToStringTag$1), tag = value[symToStringTag$1];
  try {
    value[symToStringTag$1] = void 0;
    var unmasked = true;
  } catch (e) {
  }
  var result = nativeObjectToString$1.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag$1] = tag;
    } else {
      delete value[symToStringTag$1];
    }
  }
  return result;
}
var objectProto$f = Object.prototype;
var nativeObjectToString = objectProto$f.toString;
function objectToString(value) {
  return nativeObjectToString.call(value);
}
var nullTag = "[object Null]", undefinedTag = "[object Undefined]";
var symToStringTag = Symbol$1 ? Symbol$1.toStringTag : void 0;
function baseGetTag(value) {
  if (value == null) {
    return value === void 0 ? undefinedTag : nullTag;
  }
  return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
}
function isObjectLike(value) {
  return value != null && typeof value == "object";
}
var symbolTag$3 = "[object Symbol]";
function isSymbol(value) {
  return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag$3;
}
function arrayMap(array, iteratee) {
  var index2 = -1, length2 = array == null ? 0 : array.length, result = Array(length2);
  while (++index2 < length2) {
    result[index2] = iteratee(array[index2], index2, array);
  }
  return result;
}
var isArray = Array.isArray;
var symbolProto$2 = Symbol$1 ? Symbol$1.prototype : void 0, symbolToString = symbolProto$2 ? symbolProto$2.toString : void 0;
function baseToString(value) {
  if (typeof value == "string") {
    return value;
  }
  if (isArray(value)) {
    return arrayMap(value, baseToString) + "";
  }
  if (isSymbol(value)) {
    return symbolToString ? symbolToString.call(value) : "";
  }
  var result = value + "";
  return result == "0" && 1 / value == -Infinity ? "-0" : result;
}
var reWhitespace = /\s/;
function trimmedEndIndex(string) {
  var index2 = string.length;
  while (index2-- && reWhitespace.test(string.charAt(index2))) {
  }
  return index2;
}
var reTrimStart = /^\s+/;
function baseTrim(string) {
  return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, "") : string;
}
function isObject$1(value) {
  var type = typeof value;
  return value != null && (type == "object" || type == "function");
}
var NAN = 0 / 0;
var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
var reIsBinary = /^0b[01]+$/i;
var reIsOctal = /^0o[0-7]+$/i;
var freeParseInt = parseInt;
function toNumber(value) {
  if (typeof value == "number") {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject$1(value)) {
    var other = typeof value.valueOf == "function" ? value.valueOf() : value;
    value = isObject$1(other) ? other + "" : other;
  }
  if (typeof value != "string") {
    return value === 0 ? value : +value;
  }
  value = baseTrim(value);
  var isBinary = reIsBinary.test(value);
  return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
}
function identity$1(value) {
  return value;
}
var asyncTag = "[object AsyncFunction]", funcTag$2 = "[object Function]", genTag$1 = "[object GeneratorFunction]", proxyTag = "[object Proxy]";
function isFunction(value) {
  if (!isObject$1(value)) {
    return false;
  }
  var tag = baseGetTag(value);
  return tag == funcTag$2 || tag == genTag$1 || tag == asyncTag || tag == proxyTag;
}
var coreJsData = root$1["__core-js_shared__"];
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
  return uid ? "Symbol(src)_1." + uid : "";
})();
function isMasked(func) {
  return !!maskSrcKey && maskSrcKey in func;
}
var funcProto$2 = Function.prototype;
var funcToString$2 = funcProto$2.toString;
function toSource(func) {
  if (func != null) {
    try {
      return funcToString$2.call(func);
    } catch (e) {
    }
    try {
      return func + "";
    } catch (e) {
    }
  }
  return "";
}
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
var reIsHostCtor = /^\[object .+?Constructor\]$/;
var funcProto$1 = Function.prototype, objectProto$e = Object.prototype;
var funcToString$1 = funcProto$1.toString;
var hasOwnProperty$c = objectProto$e.hasOwnProperty;
var reIsNative = RegExp(
  "^" + funcToString$1.call(hasOwnProperty$c).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
);
function baseIsNative(value) {
  if (!isObject$1(value) || isMasked(value)) {
    return false;
  }
  var pattern = isFunction(value) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}
function getValue(object, key) {
  return object == null ? void 0 : object[key];
}
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : void 0;
}
var WeakMap$1 = getNative(root$1, "WeakMap");
var objectCreate = Object.create;
var baseCreate = /* @__PURE__ */ (function() {
  function object() {
  }
  return function(proto) {
    if (!isObject$1(proto)) {
      return {};
    }
    if (objectCreate) {
      return objectCreate(proto);
    }
    object.prototype = proto;
    var result = new object();
    object.prototype = void 0;
    return result;
  };
})();
function apply$1(func, thisArg, args) {
  switch (args.length) {
    case 0:
      return func.call(thisArg);
    case 1:
      return func.call(thisArg, args[0]);
    case 2:
      return func.call(thisArg, args[0], args[1]);
    case 3:
      return func.call(thisArg, args[0], args[1], args[2]);
  }
  return func.apply(thisArg, args);
}
function noop$2() {
}
function copyArray(source2, array) {
  var index2 = -1, length2 = source2.length;
  array || (array = Array(length2));
  while (++index2 < length2) {
    array[index2] = source2[index2];
  }
  return array;
}
var HOT_COUNT = 800, HOT_SPAN = 16;
var nativeNow = Date.now;
function shortOut(func) {
  var count = 0, lastCalled = 0;
  return function() {
    var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
    lastCalled = stamp;
    if (remaining > 0) {
      if (++count >= HOT_COUNT) {
        return arguments[0];
      }
    } else {
      count = 0;
    }
    return func.apply(void 0, arguments);
  };
}
function constant(value) {
  return function() {
    return value;
  };
}
var defineProperty = (function() {
  try {
    var func = getNative(Object, "defineProperty");
    func({}, "", {});
    return func;
  } catch (e) {
  }
})();
var baseSetToString = !defineProperty ? identity$1 : function(func, string) {
  return defineProperty(func, "toString", {
    "configurable": true,
    "enumerable": false,
    "value": constant(string),
    "writable": true
  });
};
var setToString = shortOut(baseSetToString);
function arrayEach(array, iteratee) {
  var index2 = -1, length2 = array == null ? 0 : array.length;
  while (++index2 < length2) {
    if (iteratee(array[index2], index2, array) === false) {
      break;
    }
  }
  return array;
}
function baseFindIndex(array, predicate, fromIndex, fromRight) {
  var length2 = array.length, index2 = fromIndex + -1;
  while (++index2 < length2) {
    if (predicate(array[index2], index2, array)) {
      return index2;
    }
  }
  return -1;
}
function baseIsNaN(value) {
  return value !== value;
}
function strictIndexOf(array, value, fromIndex) {
  var index2 = fromIndex - 1, length2 = array.length;
  while (++index2 < length2) {
    if (array[index2] === value) {
      return index2;
    }
  }
  return -1;
}
function baseIndexOf(array, value, fromIndex) {
  return value === value ? strictIndexOf(array, value, fromIndex) : baseFindIndex(array, baseIsNaN, fromIndex);
}
function arrayIncludes(array, value) {
  var length2 = array == null ? 0 : array.length;
  return !!length2 && baseIndexOf(array, value, 0) > -1;
}
var MAX_SAFE_INTEGER$2 = 9007199254740991;
var reIsUint = /^(?:0|[1-9]\d*)$/;
function isIndex(value, length2) {
  var type = typeof value;
  length2 = length2 == null ? MAX_SAFE_INTEGER$2 : length2;
  return !!length2 && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length2);
}
function baseAssignValue(object, key, value) {
  if (key == "__proto__" && defineProperty) {
    defineProperty(object, key, {
      "configurable": true,
      "enumerable": true,
      "value": value,
      "writable": true
    });
  } else {
    object[key] = value;
  }
}
function eq(value, other) {
  return value === other || value !== value && other !== other;
}
var objectProto$d = Object.prototype;
var hasOwnProperty$b = objectProto$d.hasOwnProperty;
function assignValue(object, key, value) {
  var objValue = object[key];
  if (!(hasOwnProperty$b.call(object, key) && eq(objValue, value)) || value === void 0 && !(key in object)) {
    baseAssignValue(object, key, value);
  }
}
function copyObject(source2, props, object, customizer) {
  var isNew = !object;
  object || (object = {});
  var index2 = -1, length2 = props.length;
  while (++index2 < length2) {
    var key = props[index2];
    var newValue = void 0;
    if (newValue === void 0) {
      newValue = source2[key];
    }
    if (isNew) {
      baseAssignValue(object, key, newValue);
    } else {
      assignValue(object, key, newValue);
    }
  }
  return object;
}
var nativeMax$1 = Math.max;
function overRest(func, start, transform) {
  start = nativeMax$1(start === void 0 ? func.length - 1 : start, 0);
  return function() {
    var args = arguments, index2 = -1, length2 = nativeMax$1(args.length - start, 0), array = Array(length2);
    while (++index2 < length2) {
      array[index2] = args[start + index2];
    }
    index2 = -1;
    var otherArgs = Array(start + 1);
    while (++index2 < start) {
      otherArgs[index2] = args[index2];
    }
    otherArgs[start] = transform(array);
    return apply$1(func, this, otherArgs);
  };
}
function baseRest(func, start) {
  return setToString(overRest(func, start, identity$1), func + "");
}
var MAX_SAFE_INTEGER$1 = 9007199254740991;
function isLength(value) {
  return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER$1;
}
function isArrayLike(value) {
  return value != null && isLength(value.length) && !isFunction(value);
}
function isIterateeCall(value, index2, object) {
  if (!isObject$1(object)) {
    return false;
  }
  var type = typeof index2;
  if (type == "number" ? isArrayLike(object) && isIndex(index2, object.length) : type == "string" && index2 in object) {
    return eq(object[index2], value);
  }
  return false;
}
function createAssigner(assigner) {
  return baseRest(function(object, sources) {
    var index2 = -1, length2 = sources.length, customizer = length2 > 1 ? sources[length2 - 1] : void 0, guard = length2 > 2 ? sources[2] : void 0;
    customizer = assigner.length > 3 && typeof customizer == "function" ? (length2--, customizer) : void 0;
    if (guard && isIterateeCall(sources[0], sources[1], guard)) {
      customizer = length2 < 3 ? void 0 : customizer;
      length2 = 1;
    }
    object = Object(object);
    while (++index2 < length2) {
      var source2 = sources[index2];
      if (source2) {
        assigner(object, source2, index2, customizer);
      }
    }
    return object;
  });
}
var objectProto$c = Object.prototype;
function isPrototype(value) {
  var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto$c;
  return value === proto;
}
function baseTimes(n, iteratee) {
  var index2 = -1, result = Array(n);
  while (++index2 < n) {
    result[index2] = iteratee(index2);
  }
  return result;
}
var argsTag$3 = "[object Arguments]";
function baseIsArguments(value) {
  return isObjectLike(value) && baseGetTag(value) == argsTag$3;
}
var objectProto$b = Object.prototype;
var hasOwnProperty$a = objectProto$b.hasOwnProperty;
var propertyIsEnumerable$1 = objectProto$b.propertyIsEnumerable;
var isArguments = baseIsArguments(/* @__PURE__ */ (function() {
  return arguments;
})()) ? baseIsArguments : function(value) {
  return isObjectLike(value) && hasOwnProperty$a.call(value, "callee") && !propertyIsEnumerable$1.call(value, "callee");
};
function stubFalse() {
  return false;
}
var freeExports$2 = typeof exports == "object" && exports && !exports.nodeType && exports;
var freeModule$2 = freeExports$2 && typeof module == "object" && module && !module.nodeType && module;
var moduleExports$2 = freeModule$2 && freeModule$2.exports === freeExports$2;
var Buffer$1 = moduleExports$2 ? root$1.Buffer : void 0;
var nativeIsBuffer = Buffer$1 ? Buffer$1.isBuffer : void 0;
var isBuffer = nativeIsBuffer || stubFalse;
var argsTag$2 = "[object Arguments]", arrayTag$2 = "[object Array]", boolTag$3 = "[object Boolean]", dateTag$3 = "[object Date]", errorTag$2 = "[object Error]", funcTag$1 = "[object Function]", mapTag$5 = "[object Map]", numberTag$3 = "[object Number]", objectTag$4 = "[object Object]", regexpTag$3 = "[object RegExp]", setTag$5 = "[object Set]", stringTag$3 = "[object String]", weakMapTag$2 = "[object WeakMap]";
var arrayBufferTag$3 = "[object ArrayBuffer]", dataViewTag$4 = "[object DataView]", float32Tag$2 = "[object Float32Array]", float64Tag$2 = "[object Float64Array]", int8Tag$2 = "[object Int8Array]", int16Tag$2 = "[object Int16Array]", int32Tag$2 = "[object Int32Array]", uint8Tag$2 = "[object Uint8Array]", uint8ClampedTag$2 = "[object Uint8ClampedArray]", uint16Tag$2 = "[object Uint16Array]", uint32Tag$2 = "[object Uint32Array]";
var typedArrayTags = {};
typedArrayTags[float32Tag$2] = typedArrayTags[float64Tag$2] = typedArrayTags[int8Tag$2] = typedArrayTags[int16Tag$2] = typedArrayTags[int32Tag$2] = typedArrayTags[uint8Tag$2] = typedArrayTags[uint8ClampedTag$2] = typedArrayTags[uint16Tag$2] = typedArrayTags[uint32Tag$2] = true;
typedArrayTags[argsTag$2] = typedArrayTags[arrayTag$2] = typedArrayTags[arrayBufferTag$3] = typedArrayTags[boolTag$3] = typedArrayTags[dataViewTag$4] = typedArrayTags[dateTag$3] = typedArrayTags[errorTag$2] = typedArrayTags[funcTag$1] = typedArrayTags[mapTag$5] = typedArrayTags[numberTag$3] = typedArrayTags[objectTag$4] = typedArrayTags[regexpTag$3] = typedArrayTags[setTag$5] = typedArrayTags[stringTag$3] = typedArrayTags[weakMapTag$2] = false;
function baseIsTypedArray(value) {
  return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
}
function baseUnary(func) {
  return function(value) {
    return func(value);
  };
}
var freeExports$1 = typeof exports == "object" && exports && !exports.nodeType && exports;
var freeModule$1 = freeExports$1 && typeof module == "object" && module && !module.nodeType && module;
var moduleExports$1 = freeModule$1 && freeModule$1.exports === freeExports$1;
var freeProcess = moduleExports$1 && freeGlobal.process;
var nodeUtil = (function() {
  try {
    var types = freeModule$1 && freeModule$1.require && freeModule$1.require("util").types;
    if (types) {
      return types;
    }
    return freeProcess && freeProcess.binding && freeProcess.binding("util");
  } catch (e) {
  }
})();
var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
var objectProto$a = Object.prototype;
var hasOwnProperty$9 = objectProto$a.hasOwnProperty;
function arrayLikeKeys(value, inherited) {
  var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length2 = result.length;
  for (var key in value) {
    if ((inherited || hasOwnProperty$9.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
    (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
    isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
    isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
    isIndex(key, length2)))) {
      result.push(key);
    }
  }
  return result;
}
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}
var nativeKeys = overArg(Object.keys, Object);
var objectProto$9 = Object.prototype;
var hasOwnProperty$8 = objectProto$9.hasOwnProperty;
function baseKeys(object) {
  if (!isPrototype(object)) {
    return nativeKeys(object);
  }
  var result = [];
  for (var key in Object(object)) {
    if (hasOwnProperty$8.call(object, key) && key != "constructor") {
      result.push(key);
    }
  }
  return result;
}
function keys(object) {
  return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
}
function nativeKeysIn(object) {
  var result = [];
  if (object != null) {
    for (var key in Object(object)) {
      result.push(key);
    }
  }
  return result;
}
var objectProto$8 = Object.prototype;
var hasOwnProperty$7 = objectProto$8.hasOwnProperty;
function baseKeysIn(object) {
  if (!isObject$1(object)) {
    return nativeKeysIn(object);
  }
  var isProto = isPrototype(object), result = [];
  for (var key in object) {
    if (!(key == "constructor" && (isProto || !hasOwnProperty$7.call(object, key)))) {
      result.push(key);
    }
  }
  return result;
}
function keysIn(object) {
  return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
}
var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, reIsPlainProp = /^\w*$/;
function isKey(value, object) {
  if (isArray(value)) {
    return false;
  }
  var type = typeof value;
  if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
    return true;
  }
  return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
}
var nativeCreate = getNative(Object, "create");
function hashClear() {
  this.__data__ = nativeCreate ? nativeCreate(null) : {};
  this.size = 0;
}
function hashDelete(key) {
  var result = this.has(key) && delete this.__data__[key];
  this.size -= result ? 1 : 0;
  return result;
}
var HASH_UNDEFINED$2 = "__lodash_hash_undefined__";
var objectProto$7 = Object.prototype;
var hasOwnProperty$6 = objectProto$7.hasOwnProperty;
function hashGet(key) {
  var data = this.__data__;
  if (nativeCreate) {
    var result = data[key];
    return result === HASH_UNDEFINED$2 ? void 0 : result;
  }
  return hasOwnProperty$6.call(data, key) ? data[key] : void 0;
}
var objectProto$6 = Object.prototype;
var hasOwnProperty$5 = objectProto$6.hasOwnProperty;
function hashHas(key) {
  var data = this.__data__;
  return nativeCreate ? data[key] !== void 0 : hasOwnProperty$5.call(data, key);
}
var HASH_UNDEFINED$1 = "__lodash_hash_undefined__";
function hashSet(key, value) {
  var data = this.__data__;
  this.size += this.has(key) ? 0 : 1;
  data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED$1 : value;
  return this;
}
function Hash(entries2) {
  var index2 = -1, length2 = entries2 == null ? 0 : entries2.length;
  this.clear();
  while (++index2 < length2) {
    var entry = entries2[index2];
    this.set(entry[0], entry[1]);
  }
}
Hash.prototype.clear = hashClear;
Hash.prototype["delete"] = hashDelete;
Hash.prototype.get = hashGet;
Hash.prototype.has = hashHas;
Hash.prototype.set = hashSet;
function listCacheClear() {
  this.__data__ = [];
  this.size = 0;
}
function assocIndexOf(array, key) {
  var length2 = array.length;
  while (length2--) {
    if (eq(array[length2][0], key)) {
      return length2;
    }
  }
  return -1;
}
var arrayProto = Array.prototype;
var splice = arrayProto.splice;
function listCacheDelete(key) {
  var data = this.__data__, index2 = assocIndexOf(data, key);
  if (index2 < 0) {
    return false;
  }
  var lastIndex = data.length - 1;
  if (index2 == lastIndex) {
    data.pop();
  } else {
    splice.call(data, index2, 1);
  }
  --this.size;
  return true;
}
function listCacheGet(key) {
  var data = this.__data__, index2 = assocIndexOf(data, key);
  return index2 < 0 ? void 0 : data[index2][1];
}
function listCacheHas(key) {
  return assocIndexOf(this.__data__, key) > -1;
}
function listCacheSet(key, value) {
  var data = this.__data__, index2 = assocIndexOf(data, key);
  if (index2 < 0) {
    ++this.size;
    data.push([key, value]);
  } else {
    data[index2][1] = value;
  }
  return this;
}
function ListCache(entries2) {
  var index2 = -1, length2 = entries2 == null ? 0 : entries2.length;
  this.clear();
  while (++index2 < length2) {
    var entry = entries2[index2];
    this.set(entry[0], entry[1]);
  }
}
ListCache.prototype.clear = listCacheClear;
ListCache.prototype["delete"] = listCacheDelete;
ListCache.prototype.get = listCacheGet;
ListCache.prototype.has = listCacheHas;
ListCache.prototype.set = listCacheSet;
var Map$1 = getNative(root$1, "Map");
function mapCacheClear() {
  this.size = 0;
  this.__data__ = {
    "hash": new Hash(),
    "map": new (Map$1 || ListCache)(),
    "string": new Hash()
  };
}
function isKeyable(value) {
  var type = typeof value;
  return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
}
function getMapData(map, key) {
  var data = map.__data__;
  return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
}
function mapCacheDelete(key) {
  var result = getMapData(this, key)["delete"](key);
  this.size -= result ? 1 : 0;
  return result;
}
function mapCacheGet(key) {
  return getMapData(this, key).get(key);
}
function mapCacheHas(key) {
  return getMapData(this, key).has(key);
}
function mapCacheSet(key, value) {
  var data = getMapData(this, key), size = data.size;
  data.set(key, value);
  this.size += data.size == size ? 0 : 1;
  return this;
}
function MapCache(entries2) {
  var index2 = -1, length2 = entries2 == null ? 0 : entries2.length;
  this.clear();
  while (++index2 < length2) {
    var entry = entries2[index2];
    this.set(entry[0], entry[1]);
  }
}
MapCache.prototype.clear = mapCacheClear;
MapCache.prototype["delete"] = mapCacheDelete;
MapCache.prototype.get = mapCacheGet;
MapCache.prototype.has = mapCacheHas;
MapCache.prototype.set = mapCacheSet;
var FUNC_ERROR_TEXT$2 = "Expected a function";
function memoize(func, resolver) {
  if (typeof func != "function" || resolver != null && typeof resolver != "function") {
    throw new TypeError(FUNC_ERROR_TEXT$2);
  }
  var memoized = function() {
    var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
    if (cache.has(key)) {
      return cache.get(key);
    }
    var result = func.apply(this, args);
    memoized.cache = cache.set(key, result) || cache;
    return result;
  };
  memoized.cache = new (memoize.Cache || MapCache)();
  return memoized;
}
memoize.Cache = MapCache;
var MAX_MEMOIZE_SIZE = 500;
function memoizeCapped(func) {
  var result = memoize(func, function(key) {
    if (cache.size === MAX_MEMOIZE_SIZE) {
      cache.clear();
    }
    return key;
  });
  var cache = result.cache;
  return result;
}
var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
var reEscapeChar = /\\(\\)?/g;
var stringToPath = memoizeCapped(function(string) {
  var result = [];
  if (string.charCodeAt(0) === 46) {
    result.push("");
  }
  string.replace(rePropName, function(match, number, quote, subString) {
    result.push(quote ? subString.replace(reEscapeChar, "$1") : number || match);
  });
  return result;
});
function toString(value) {
  return value == null ? "" : baseToString(value);
}
function castPath(value, object) {
  if (isArray(value)) {
    return value;
  }
  return isKey(value, object) ? [value] : stringToPath(toString(value));
}
function toKey(value) {
  if (typeof value == "string" || isSymbol(value)) {
    return value;
  }
  var result = value + "";
  return result == "0" && 1 / value == -Infinity ? "-0" : result;
}
function baseGet(object, path) {
  path = castPath(path, object);
  var index2 = 0, length2 = path.length;
  while (object != null && index2 < length2) {
    object = object[toKey(path[index2++])];
  }
  return index2 && index2 == length2 ? object : void 0;
}
function get$2(object, path, defaultValue) {
  var result = object == null ? void 0 : baseGet(object, path);
  return result === void 0 ? defaultValue : result;
}
function arrayPush$1(array, values2) {
  var index2 = -1, length2 = values2.length, offset = array.length;
  while (++index2 < length2) {
    array[offset + index2] = values2[index2];
  }
  return array;
}
var spreadableSymbol = Symbol$1 ? Symbol$1.isConcatSpreadable : void 0;
function isFlattenable(value) {
  return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
}
function baseFlatten(array, depth, predicate, isStrict, result) {
  var index2 = -1, length2 = array.length;
  predicate || (predicate = isFlattenable);
  result || (result = []);
  while (++index2 < length2) {
    var value = array[index2];
    if (predicate(value)) {
      {
        arrayPush$1(result, value);
      }
    } else {
      result[result.length] = value;
    }
  }
  return result;
}
var getPrototype = overArg(Object.getPrototypeOf, Object);
var objectTag$3 = "[object Object]";
var funcProto = Function.prototype, objectProto$5 = Object.prototype;
var funcToString = funcProto.toString;
var hasOwnProperty$4 = objectProto$5.hasOwnProperty;
var objectCtorString = funcToString.call(Object);
function isPlainObject(value) {
  if (!isObjectLike(value) || baseGetTag(value) != objectTag$3) {
    return false;
  }
  var proto = getPrototype(value);
  if (proto === null) {
    return true;
  }
  var Ctor = hasOwnProperty$4.call(proto, "constructor") && proto.constructor;
  return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
}
function stackClear() {
  this.__data__ = new ListCache();
  this.size = 0;
}
function stackDelete(key) {
  var data = this.__data__, result = data["delete"](key);
  this.size = data.size;
  return result;
}
function stackGet(key) {
  return this.__data__.get(key);
}
function stackHas(key) {
  return this.__data__.has(key);
}
var LARGE_ARRAY_SIZE$1 = 200;
function stackSet(key, value) {
  var data = this.__data__;
  if (data instanceof ListCache) {
    var pairs = data.__data__;
    if (!Map$1 || pairs.length < LARGE_ARRAY_SIZE$1 - 1) {
      pairs.push([key, value]);
      this.size = ++data.size;
      return this;
    }
    data = this.__data__ = new MapCache(pairs);
  }
  data.set(key, value);
  this.size = data.size;
  return this;
}
function Stack(entries2) {
  var data = this.__data__ = new ListCache(entries2);
  this.size = data.size;
}
Stack.prototype.clear = stackClear;
Stack.prototype["delete"] = stackDelete;
Stack.prototype.get = stackGet;
Stack.prototype.has = stackHas;
Stack.prototype.set = stackSet;
var freeExports = typeof exports == "object" && exports && !exports.nodeType && exports;
var freeModule = freeExports && typeof module == "object" && module && !module.nodeType && module;
var moduleExports = freeModule && freeModule.exports === freeExports;
var Buffer = moduleExports ? root$1.Buffer : void 0, allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
function cloneBuffer(buffer2, isDeep) {
  if (isDeep) {
    return buffer2.slice();
  }
  var length2 = buffer2.length, result = allocUnsafe ? allocUnsafe(length2) : new buffer2.constructor(length2);
  buffer2.copy(result);
  return result;
}
function arrayFilter(array, predicate) {
  var index2 = -1, length2 = array == null ? 0 : array.length, resIndex = 0, result = [];
  while (++index2 < length2) {
    var value = array[index2];
    if (predicate(value, index2, array)) {
      result[resIndex++] = value;
    }
  }
  return result;
}
function stubArray() {
  return [];
}
var objectProto$4 = Object.prototype;
var propertyIsEnumerable = objectProto$4.propertyIsEnumerable;
var nativeGetSymbols = Object.getOwnPropertySymbols;
var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
  if (object == null) {
    return [];
  }
  object = Object(object);
  return arrayFilter(nativeGetSymbols(object), function(symbol) {
    return propertyIsEnumerable.call(object, symbol);
  });
};
function baseGetAllKeys(object, keysFunc, symbolsFunc) {
  var result = keysFunc(object);
  return isArray(object) ? result : arrayPush$1(result, symbolsFunc(object));
}
function getAllKeys(object) {
  return baseGetAllKeys(object, keys, getSymbols);
}
var DataView = getNative(root$1, "DataView");
var Promise$1 = getNative(root$1, "Promise");
var Set$1 = getNative(root$1, "Set");
var mapTag$4 = "[object Map]", objectTag$2 = "[object Object]", promiseTag = "[object Promise]", setTag$4 = "[object Set]", weakMapTag$1 = "[object WeakMap]";
var dataViewTag$3 = "[object DataView]";
var dataViewCtorString = toSource(DataView), mapCtorString = toSource(Map$1), promiseCtorString = toSource(Promise$1), setCtorString = toSource(Set$1), weakMapCtorString = toSource(WeakMap$1);
var getTag = baseGetTag;
if (DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag$3 || Map$1 && getTag(new Map$1()) != mapTag$4 || Promise$1 && getTag(Promise$1.resolve()) != promiseTag || Set$1 && getTag(new Set$1()) != setTag$4 || WeakMap$1 && getTag(new WeakMap$1()) != weakMapTag$1) {
  getTag = function(value) {
    var result = baseGetTag(value), Ctor = result == objectTag$2 ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : "";
    if (ctorString) {
      switch (ctorString) {
        case dataViewCtorString:
          return dataViewTag$3;
        case mapCtorString:
          return mapTag$4;
        case promiseCtorString:
          return promiseTag;
        case setCtorString:
          return setTag$4;
        case weakMapCtorString:
          return weakMapTag$1;
      }
    }
    return result;
  };
}
var objectProto$3 = Object.prototype;
var hasOwnProperty$3 = objectProto$3.hasOwnProperty;
function initCloneArray(array) {
  var length2 = array.length, result = new array.constructor(length2);
  if (length2 && typeof array[0] == "string" && hasOwnProperty$3.call(array, "index")) {
    result.index = array.index;
    result.input = array.input;
  }
  return result;
}
var Uint8Array = root$1.Uint8Array;
function cloneArrayBuffer(arrayBuffer) {
  var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
  new Uint8Array(result).set(new Uint8Array(arrayBuffer));
  return result;
}
function cloneDataView(dataView, isDeep) {
  var buffer2 = cloneArrayBuffer(dataView.buffer);
  return new dataView.constructor(buffer2, dataView.byteOffset, dataView.byteLength);
}
var reFlags = /\w*$/;
function cloneRegExp(regexp) {
  var result = new regexp.constructor(regexp.source, reFlags.exec(regexp));
  result.lastIndex = regexp.lastIndex;
  return result;
}
var symbolProto$1 = Symbol$1 ? Symbol$1.prototype : void 0, symbolValueOf$1 = symbolProto$1 ? symbolProto$1.valueOf : void 0;
function cloneSymbol(symbol) {
  return symbolValueOf$1 ? Object(symbolValueOf$1.call(symbol)) : {};
}
function cloneTypedArray(typedArray, isDeep) {
  var buffer2 = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
  return new typedArray.constructor(buffer2, typedArray.byteOffset, typedArray.length);
}
var boolTag$2 = "[object Boolean]", dateTag$2 = "[object Date]", mapTag$3 = "[object Map]", numberTag$2 = "[object Number]", regexpTag$2 = "[object RegExp]", setTag$3 = "[object Set]", stringTag$2 = "[object String]", symbolTag$2 = "[object Symbol]";
var arrayBufferTag$2 = "[object ArrayBuffer]", dataViewTag$2 = "[object DataView]", float32Tag$1 = "[object Float32Array]", float64Tag$1 = "[object Float64Array]", int8Tag$1 = "[object Int8Array]", int16Tag$1 = "[object Int16Array]", int32Tag$1 = "[object Int32Array]", uint8Tag$1 = "[object Uint8Array]", uint8ClampedTag$1 = "[object Uint8ClampedArray]", uint16Tag$1 = "[object Uint16Array]", uint32Tag$1 = "[object Uint32Array]";
function initCloneByTag(object, tag, isDeep) {
  var Ctor = object.constructor;
  switch (tag) {
    case arrayBufferTag$2:
      return cloneArrayBuffer(object);
    case boolTag$2:
    case dateTag$2:
      return new Ctor(+object);
    case dataViewTag$2:
      return cloneDataView(object);
    case float32Tag$1:
    case float64Tag$1:
    case int8Tag$1:
    case int16Tag$1:
    case int32Tag$1:
    case uint8Tag$1:
    case uint8ClampedTag$1:
    case uint16Tag$1:
    case uint32Tag$1:
      return cloneTypedArray(object, isDeep);
    case mapTag$3:
      return new Ctor();
    case numberTag$2:
    case stringTag$2:
      return new Ctor(object);
    case regexpTag$2:
      return cloneRegExp(object);
    case setTag$3:
      return new Ctor();
    case symbolTag$2:
      return cloneSymbol(object);
  }
}
function initCloneObject(object) {
  return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
}
var mapTag$2 = "[object Map]";
function baseIsMap(value) {
  return isObjectLike(value) && getTag(value) == mapTag$2;
}
var nodeIsMap = nodeUtil && nodeUtil.isMap;
var isMap = nodeIsMap ? baseUnary(nodeIsMap) : baseIsMap;
var setTag$2 = "[object Set]";
function baseIsSet(value) {
  return isObjectLike(value) && getTag(value) == setTag$2;
}
var nodeIsSet = nodeUtil && nodeUtil.isSet;
var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;
var CLONE_DEEP_FLAG$1 = 1;
var argsTag$1 = "[object Arguments]", arrayTag$1 = "[object Array]", boolTag$1 = "[object Boolean]", dateTag$1 = "[object Date]", errorTag$1 = "[object Error]", funcTag = "[object Function]", genTag = "[object GeneratorFunction]", mapTag$1 = "[object Map]", numberTag$1 = "[object Number]", objectTag$1 = "[object Object]", regexpTag$1 = "[object RegExp]", setTag$1 = "[object Set]", stringTag$1 = "[object String]", symbolTag$1 = "[object Symbol]", weakMapTag = "[object WeakMap]";
var arrayBufferTag$1 = "[object ArrayBuffer]", dataViewTag$1 = "[object DataView]", float32Tag = "[object Float32Array]", float64Tag = "[object Float64Array]", int8Tag = "[object Int8Array]", int16Tag = "[object Int16Array]", int32Tag = "[object Int32Array]", uint8Tag = "[object Uint8Array]", uint8ClampedTag = "[object Uint8ClampedArray]", uint16Tag = "[object Uint16Array]", uint32Tag = "[object Uint32Array]";
var cloneableTags = {};
cloneableTags[argsTag$1] = cloneableTags[arrayTag$1] = cloneableTags[arrayBufferTag$1] = cloneableTags[dataViewTag$1] = cloneableTags[boolTag$1] = cloneableTags[dateTag$1] = cloneableTags[float32Tag] = cloneableTags[float64Tag] = cloneableTags[int8Tag] = cloneableTags[int16Tag] = cloneableTags[int32Tag] = cloneableTags[mapTag$1] = cloneableTags[numberTag$1] = cloneableTags[objectTag$1] = cloneableTags[regexpTag$1] = cloneableTags[setTag$1] = cloneableTags[stringTag$1] = cloneableTags[symbolTag$1] = cloneableTags[uint8Tag] = cloneableTags[uint8ClampedTag] = cloneableTags[uint16Tag] = cloneableTags[uint32Tag] = true;
cloneableTags[errorTag$1] = cloneableTags[funcTag] = cloneableTags[weakMapTag] = false;
function baseClone(value, bitmask, customizer, key, object, stack) {
  var result, isDeep = bitmask & CLONE_DEEP_FLAG$1;
  if (result !== void 0) {
    return result;
  }
  if (!isObject$1(value)) {
    return value;
  }
  var isArr = isArray(value);
  if (isArr) {
    result = initCloneArray(value);
  } else {
    var tag = getTag(value), isFunc = tag == funcTag || tag == genTag;
    if (isBuffer(value)) {
      return cloneBuffer(value, isDeep);
    }
    if (tag == objectTag$1 || tag == argsTag$1 || isFunc && !object) {
      result = isFunc ? {} : initCloneObject(value);
    } else {
      if (!cloneableTags[tag]) {
        return object ? value : {};
      }
      result = initCloneByTag(value, tag, isDeep);
    }
  }
  stack || (stack = new Stack());
  var stacked = stack.get(value);
  if (stacked) {
    return stacked;
  }
  stack.set(value, result);
  if (isSet(value)) {
    value.forEach(function(subValue) {
      result.add(baseClone(subValue, bitmask, customizer, subValue, value, stack));
    });
  } else if (isMap(value)) {
    value.forEach(function(subValue, key2) {
      result.set(key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
    });
  }
  var keysFunc = getAllKeys;
  var props = isArr ? void 0 : keysFunc(value);
  arrayEach(props || value, function(subValue, key2) {
    if (props) {
      key2 = subValue;
      subValue = value[key2];
    }
    assignValue(result, key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
  });
  return result;
}
var CLONE_DEEP_FLAG = 1, CLONE_SYMBOLS_FLAG = 4;
function cloneDeep(value) {
  return baseClone(value, CLONE_DEEP_FLAG | CLONE_SYMBOLS_FLAG);
}
var HASH_UNDEFINED = "__lodash_hash_undefined__";
function setCacheAdd(value) {
  this.__data__.set(value, HASH_UNDEFINED);
  return this;
}
function setCacheHas(value) {
  return this.__data__.has(value);
}
function SetCache(values2) {
  var index2 = -1, length2 = values2 == null ? 0 : values2.length;
  this.__data__ = new MapCache();
  while (++index2 < length2) {
    this.add(values2[index2]);
  }
}
SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
SetCache.prototype.has = setCacheHas;
function arraySome(array, predicate) {
  var index2 = -1, length2 = array == null ? 0 : array.length;
  while (++index2 < length2) {
    if (predicate(array[index2], index2, array)) {
      return true;
    }
  }
  return false;
}
function cacheHas(cache, key) {
  return cache.has(key);
}
var COMPARE_PARTIAL_FLAG$5 = 1, COMPARE_UNORDERED_FLAG$3 = 2;
function equalArrays$1(array, other, bitmask, customizer, equalFunc, stack) {
  var isPartial = bitmask & COMPARE_PARTIAL_FLAG$5, arrLength = array.length, othLength = other.length;
  if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
    return false;
  }
  var arrStacked = stack.get(array);
  var othStacked = stack.get(other);
  if (arrStacked && othStacked) {
    return arrStacked == other && othStacked == array;
  }
  var index2 = -1, result = true, seen = bitmask & COMPARE_UNORDERED_FLAG$3 ? new SetCache() : void 0;
  stack.set(array, other);
  stack.set(other, array);
  while (++index2 < arrLength) {
    var arrValue = array[index2], othValue = other[index2];
    if (customizer) {
      var compared = isPartial ? customizer(othValue, arrValue, index2, other, array, stack) : customizer(arrValue, othValue, index2, array, other, stack);
    }
    if (compared !== void 0) {
      if (compared) {
        continue;
      }
      result = false;
      break;
    }
    if (seen) {
      if (!arraySome(other, function(othValue2, othIndex) {
        if (!cacheHas(seen, othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, bitmask, customizer, stack))) {
          return seen.push(othIndex);
        }
      })) {
        result = false;
        break;
      }
    } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
      result = false;
      break;
    }
  }
  stack["delete"](array);
  stack["delete"](other);
  return result;
}
function mapToArray(map) {
  var index2 = -1, result = Array(map.size);
  map.forEach(function(value, key) {
    result[++index2] = [key, value];
  });
  return result;
}
function setToArray(set2) {
  var index2 = -1, result = Array(set2.size);
  set2.forEach(function(value) {
    result[++index2] = value;
  });
  return result;
}
var COMPARE_PARTIAL_FLAG$4 = 1, COMPARE_UNORDERED_FLAG$2 = 2;
var boolTag = "[object Boolean]", dateTag = "[object Date]", errorTag = "[object Error]", mapTag = "[object Map]", numberTag = "[object Number]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", symbolTag = "[object Symbol]";
var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]";
var symbolProto = Symbol$1 ? Symbol$1.prototype : void 0, symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
  switch (tag) {
    case dataViewTag:
      if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
        return false;
      }
      object = object.buffer;
      other = other.buffer;
    case arrayBufferTag:
      if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
        return false;
      }
      return true;
    case boolTag:
    case dateTag:
    case numberTag:
      return eq(+object, +other);
    case errorTag:
      return object.name == other.name && object.message == other.message;
    case regexpTag:
    case stringTag:
      return object == other + "";
    case mapTag:
      var convert = mapToArray;
    case setTag:
      var isPartial = bitmask & COMPARE_PARTIAL_FLAG$4;
      convert || (convert = setToArray);
      if (object.size != other.size && !isPartial) {
        return false;
      }
      var stacked = stack.get(object);
      if (stacked) {
        return stacked == other;
      }
      bitmask |= COMPARE_UNORDERED_FLAG$2;
      stack.set(object, other);
      var result = equalArrays$1(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
      stack["delete"](object);
      return result;
    case symbolTag:
      if (symbolValueOf) {
        return symbolValueOf.call(object) == symbolValueOf.call(other);
      }
  }
  return false;
}
var COMPARE_PARTIAL_FLAG$3 = 1;
var objectProto$2 = Object.prototype;
var hasOwnProperty$2 = objectProto$2.hasOwnProperty;
function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
  var isPartial = bitmask & COMPARE_PARTIAL_FLAG$3, objProps = getAllKeys(object), objLength = objProps.length, othProps = getAllKeys(other), othLength = othProps.length;
  if (objLength != othLength && !isPartial) {
    return false;
  }
  var index2 = objLength;
  while (index2--) {
    var key = objProps[index2];
    if (!(isPartial ? key in other : hasOwnProperty$2.call(other, key))) {
      return false;
    }
  }
  var objStacked = stack.get(object);
  var othStacked = stack.get(other);
  if (objStacked && othStacked) {
    return objStacked == other && othStacked == object;
  }
  var result = true;
  stack.set(object, other);
  stack.set(other, object);
  var skipCtor = isPartial;
  while (++index2 < objLength) {
    key = objProps[index2];
    var objValue = object[key], othValue = other[key];
    if (customizer) {
      var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
    }
    if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
      result = false;
      break;
    }
    skipCtor || (skipCtor = key == "constructor");
  }
  if (result && !skipCtor) {
    var objCtor = object.constructor, othCtor = other.constructor;
    if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
      result = false;
    }
  }
  stack["delete"](object);
  stack["delete"](other);
  return result;
}
var COMPARE_PARTIAL_FLAG$2 = 1;
var argsTag = "[object Arguments]", arrayTag = "[object Array]", objectTag = "[object Object]";
var objectProto$1 = Object.prototype;
var hasOwnProperty$1 = objectProto$1.hasOwnProperty;
function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
  var objIsArr = isArray(object), othIsArr = isArray(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
  objTag = objTag == argsTag ? objectTag : objTag;
  othTag = othTag == argsTag ? objectTag : othTag;
  var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
  if (isSameTag && isBuffer(object)) {
    if (!isBuffer(other)) {
      return false;
    }
    objIsArr = true;
    objIsObj = false;
  }
  if (isSameTag && !objIsObj) {
    stack || (stack = new Stack());
    return objIsArr || isTypedArray(object) ? equalArrays$1(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
  }
  if (!(bitmask & COMPARE_PARTIAL_FLAG$2)) {
    var objIsWrapped = objIsObj && hasOwnProperty$1.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty$1.call(other, "__wrapped__");
    if (objIsWrapped || othIsWrapped) {
      var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
      stack || (stack = new Stack());
      return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
    }
  }
  if (!isSameTag) {
    return false;
  }
  stack || (stack = new Stack());
  return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
}
function baseIsEqual(value, other, bitmask, customizer, stack) {
  if (value === other) {
    return true;
  }
  if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
    return value !== value && other !== other;
  }
  return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
}
var COMPARE_PARTIAL_FLAG$1 = 1, COMPARE_UNORDERED_FLAG$1 = 2;
function baseIsMatch(object, source2, matchData, customizer) {
  var index2 = matchData.length, length2 = index2;
  if (object == null) {
    return !length2;
  }
  object = Object(object);
  while (index2--) {
    var data = matchData[index2];
    if (data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
      return false;
    }
  }
  while (++index2 < length2) {
    data = matchData[index2];
    var key = data[0], objValue = object[key], srcValue = data[1];
    if (data[2]) {
      if (objValue === void 0 && !(key in object)) {
        return false;
      }
    } else {
      var stack = new Stack();
      var result;
      if (!(result === void 0 ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG$1 | COMPARE_UNORDERED_FLAG$1, customizer, stack) : result)) {
        return false;
      }
    }
  }
  return true;
}
function isStrictComparable(value) {
  return value === value && !isObject$1(value);
}
function getMatchData(object) {
  var result = keys(object), length2 = result.length;
  while (length2--) {
    var key = result[length2], value = object[key];
    result[length2] = [key, value, isStrictComparable(value)];
  }
  return result;
}
function matchesStrictComparable(key, srcValue) {
  return function(object) {
    if (object == null) {
      return false;
    }
    return object[key] === srcValue && (srcValue !== void 0 || key in Object(object));
  };
}
function baseMatches(source2) {
  var matchData = getMatchData(source2);
  if (matchData.length == 1 && matchData[0][2]) {
    return matchesStrictComparable(matchData[0][0], matchData[0][1]);
  }
  return function(object) {
    return object === source2 || baseIsMatch(object, source2, matchData);
  };
}
function baseHasIn(object, key) {
  return object != null && key in Object(object);
}
function hasPath(object, path, hasFunc) {
  path = castPath(path, object);
  var index2 = -1, length2 = path.length, result = false;
  while (++index2 < length2) {
    var key = toKey(path[index2]);
    if (!(result = object != null && hasFunc(object, key))) {
      break;
    }
    object = object[key];
  }
  if (result || ++index2 != length2) {
    return result;
  }
  length2 = object == null ? 0 : object.length;
  return !!length2 && isLength(length2) && isIndex(key, length2) && (isArray(object) || isArguments(object));
}
function hasIn(object, path) {
  return object != null && hasPath(object, path, baseHasIn);
}
var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
function baseMatchesProperty(path, srcValue) {
  if (isKey(path) && isStrictComparable(srcValue)) {
    return matchesStrictComparable(toKey(path), srcValue);
  }
  return function(object) {
    var objValue = get$2(object, path);
    return objValue === void 0 && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
  };
}
function baseProperty(key) {
  return function(object) {
    return object == null ? void 0 : object[key];
  };
}
function basePropertyDeep(path) {
  return function(object) {
    return baseGet(object, path);
  };
}
function property(path) {
  return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
}
function baseIteratee(value) {
  if (typeof value == "function") {
    return value;
  }
  if (value == null) {
    return identity$1;
  }
  if (typeof value == "object") {
    return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
  }
  return property(value);
}
function arrayAggregator(array, setter, iteratee, accumulator) {
  var index2 = -1, length2 = array == null ? 0 : array.length;
  while (++index2 < length2) {
    var value = array[index2];
    setter(accumulator, value, iteratee(value), array);
  }
  return accumulator;
}
function createBaseFor(fromRight) {
  return function(object, iteratee, keysFunc) {
    var index2 = -1, iterable = Object(object), props = keysFunc(object), length2 = props.length;
    while (length2--) {
      var key = props[++index2];
      if (iteratee(iterable[key], key, iterable) === false) {
        break;
      }
    }
    return object;
  };
}
var baseFor = createBaseFor();
function baseForOwn(object, iteratee) {
  return object && baseFor(object, iteratee, keys);
}
function createBaseEach(eachFunc, fromRight) {
  return function(collection, iteratee) {
    if (collection == null) {
      return collection;
    }
    if (!isArrayLike(collection)) {
      return eachFunc(collection, iteratee);
    }
    var length2 = collection.length, index2 = -1, iterable = Object(collection);
    while (++index2 < length2) {
      if (iteratee(iterable[index2], index2, iterable) === false) {
        break;
      }
    }
    return collection;
  };
}
var baseEach = createBaseEach(baseForOwn);
function baseAggregator(collection, setter, iteratee, accumulator) {
  baseEach(collection, function(value, key, collection2) {
    setter(accumulator, value, iteratee(value), collection2);
  });
  return accumulator;
}
function createAggregator(setter, initializer) {
  return function(collection, iteratee) {
    var func = isArray(collection) ? arrayAggregator : baseAggregator, accumulator = initializer ? initializer() : {};
    return func(collection, setter, baseIteratee(iteratee), accumulator);
  };
}
var objectProto = Object.prototype;
var hasOwnProperty = objectProto.hasOwnProperty;
var countBy = createAggregator(function(result, value, key) {
  if (hasOwnProperty.call(result, key)) {
    ++result[key];
  } else {
    baseAssignValue(result, key, 1);
  }
});
var now$1 = function() {
  return root$1.Date.now();
};
var FUNC_ERROR_TEXT$1 = "Expected a function";
var nativeMax = Math.max, nativeMin$1 = Math.min;
function debounce(func, wait, options) {
  var lastArgs, lastThis, maxWait, result, timerId, lastCallTime, lastInvokeTime = 0, leading = false, maxing = false, trailing = true;
  if (typeof func != "function") {
    throw new TypeError(FUNC_ERROR_TEXT$1);
  }
  wait = toNumber(wait) || 0;
  if (isObject$1(options)) {
    leading = !!options.leading;
    maxing = "maxWait" in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = "trailing" in options ? !!options.trailing : trailing;
  }
  function invokeFunc(time) {
    var args = lastArgs, thisArg = lastThis;
    lastArgs = lastThis = void 0;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }
  function leadingEdge(time) {
    lastInvokeTime = time;
    timerId = setTimeout(timerExpired, wait);
    return leading ? invokeFunc(time) : result;
  }
  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime, timeWaiting = wait - timeSinceLastCall;
    return maxing ? nativeMin$1(timeWaiting, maxWait - timeSinceLastInvoke) : timeWaiting;
  }
  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime, timeSinceLastInvoke = time - lastInvokeTime;
    return lastCallTime === void 0 || timeSinceLastCall >= wait || timeSinceLastCall < 0 || maxing && timeSinceLastInvoke >= maxWait;
  }
  function timerExpired() {
    var time = now$1();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    timerId = setTimeout(timerExpired, remainingWait(time));
  }
  function trailingEdge(time) {
    timerId = void 0;
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = void 0;
    return result;
  }
  function cancel() {
    if (timerId !== void 0) {
      clearTimeout(timerId);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timerId = void 0;
  }
  function flush() {
    return timerId === void 0 ? result : trailingEdge(now$1());
  }
  function debounced() {
    var time = now$1(), isInvoking = shouldInvoke(time);
    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;
    if (isInvoking) {
      if (timerId === void 0) {
        return leadingEdge(lastCallTime);
      }
      if (maxing) {
        clearTimeout(timerId);
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    if (timerId === void 0) {
      timerId = setTimeout(timerExpired, wait);
    }
    return result;
  }
  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}
function assignMergeValue(object, key, value) {
  if (value !== void 0 && !eq(object[key], value) || value === void 0 && !(key in object)) {
    baseAssignValue(object, key, value);
  }
}
function isArrayLikeObject(value) {
  return isObjectLike(value) && isArrayLike(value);
}
function safeGet(object, key) {
  if (key === "constructor" && typeof object[key] === "function") {
    return;
  }
  if (key == "__proto__") {
    return;
  }
  return object[key];
}
function toPlainObject(value) {
  return copyObject(value, keysIn(value));
}
function baseMergeDeep(object, source2, key, srcIndex, mergeFunc, customizer, stack) {
  var objValue = safeGet(object, key), srcValue = safeGet(source2, key), stacked = stack.get(srcValue);
  if (stacked) {
    assignMergeValue(object, key, stacked);
    return;
  }
  var newValue = customizer ? customizer(objValue, srcValue, key + "", object, source2, stack) : void 0;
  var isCommon = newValue === void 0;
  if (isCommon) {
    var isArr = isArray(srcValue), isBuff = !isArr && isBuffer(srcValue), isTyped = !isArr && !isBuff && isTypedArray(srcValue);
    newValue = srcValue;
    if (isArr || isBuff || isTyped) {
      if (isArray(objValue)) {
        newValue = objValue;
      } else if (isArrayLikeObject(objValue)) {
        newValue = copyArray(objValue);
      } else if (isBuff) {
        isCommon = false;
        newValue = cloneBuffer(srcValue, true);
      } else if (isTyped) {
        isCommon = false;
        newValue = cloneTypedArray(srcValue, true);
      } else {
        newValue = [];
      }
    } else if (isPlainObject(srcValue) || isArguments(srcValue)) {
      newValue = objValue;
      if (isArguments(objValue)) {
        newValue = toPlainObject(objValue);
      } else if (!isObject$1(objValue) || isFunction(objValue)) {
        newValue = initCloneObject(srcValue);
      }
    } else {
      isCommon = false;
    }
  }
  if (isCommon) {
    stack.set(srcValue, newValue);
    mergeFunc(newValue, srcValue, srcIndex, customizer, stack);
    stack["delete"](srcValue);
  }
  assignMergeValue(object, key, newValue);
}
function baseMerge(object, source2, srcIndex, customizer, stack) {
  if (object === source2) {
    return;
  }
  baseFor(source2, function(srcValue, key) {
    stack || (stack = new Stack());
    if (isObject$1(srcValue)) {
      baseMergeDeep(object, source2, key, srcIndex, baseMerge, customizer, stack);
    } else {
      var newValue = customizer ? customizer(safeGet(object, key), srcValue, key + "", object, source2, stack) : void 0;
      if (newValue === void 0) {
        newValue = srcValue;
      }
      assignMergeValue(object, key, newValue);
    }
  }, keysIn);
}
var mergeWith = createAssigner(function(object, source2, srcIndex, customizer) {
  baseMerge(object, source2, srcIndex, customizer);
});
function arrayIncludesWith(array, value, comparator) {
  var index2 = -1, length2 = array == null ? 0 : array.length;
  while (++index2 < length2) {
    if (comparator(value, array[index2])) {
      return true;
    }
  }
  return false;
}
function baseMap(collection, iteratee) {
  var index2 = -1, result = isArrayLike(collection) ? Array(collection.length) : [];
  baseEach(collection, function(value, key, collection2) {
    result[++index2] = iteratee(value, key, collection2);
  });
  return result;
}
function baseValues(object, props) {
  return arrayMap(props, function(key) {
    return object[key];
  });
}
function values(object) {
  return object == null ? [] : baseValues(object, keys(object));
}
var nativeMin = Math.min;
function baseIntersection(arrays, iteratee, comparator) {
  var includes = arrayIncludes, length2 = arrays[0].length, othLength = arrays.length, othIndex = othLength, caches = Array(othLength), maxLength = Infinity, result = [];
  while (othIndex--) {
    var array = arrays[othIndex];
    maxLength = nativeMin(array.length, maxLength);
    caches[othIndex] = length2 >= 120 && array.length >= 120 ? new SetCache(othIndex && array) : void 0;
  }
  array = arrays[0];
  var index2 = -1, seen = caches[0];
  outer:
    while (++index2 < length2 && result.length < maxLength) {
      var value = array[index2], computed = value;
      value = value !== 0 ? value : 0;
      if (!(seen ? cacheHas(seen, computed) : includes(result, computed))) {
        othIndex = othLength;
        while (--othIndex) {
          var cache = caches[othIndex];
          if (!(cache ? cacheHas(cache, computed) : includes(arrays[othIndex], computed))) {
            continue outer;
          }
        }
        if (seen) {
          seen.push(computed);
        }
        result.push(value);
      }
    }
  return result;
}
function castArrayLikeObject(value) {
  return isArrayLikeObject(value) ? value : [];
}
var intersection$1 = baseRest(function(arrays) {
  var mapped = arrayMap(arrays, castArrayLikeObject);
  return mapped.length && mapped[0] === arrays[0] ? baseIntersection(mapped) : [];
});
function isEqual(value, other) {
  return baseIsEqual(value, other);
}
var keyBy = createAggregator(function(result, value, key) {
  baseAssignValue(result, key, value);
});
var merge$1 = createAssigner(function(object, source2, srcIndex) {
  baseMerge(object, source2, srcIndex);
});
function baseSet(object, path, value, customizer) {
  if (!isObject$1(object)) {
    return object;
  }
  path = castPath(path, object);
  var index2 = -1, length2 = path.length, lastIndex = length2 - 1, nested = object;
  while (nested != null && ++index2 < length2) {
    var key = toKey(path[index2]), newValue = value;
    if (key === "__proto__" || key === "constructor" || key === "prototype") {
      return object;
    }
    if (index2 != lastIndex) {
      var objValue = nested[key];
      newValue = void 0;
      if (newValue === void 0) {
        newValue = isObject$1(objValue) ? objValue : isIndex(path[index2 + 1]) ? [] : {};
      }
    }
    assignValue(nested, key, newValue);
    nested = nested[key];
  }
  return object;
}
function baseSortBy(array, comparer) {
  var length2 = array.length;
  array.sort(comparer);
  while (length2--) {
    array[length2] = array[length2].value;
  }
  return array;
}
function compareAscending(value, other) {
  if (value !== other) {
    var valIsDefined = value !== void 0, valIsNull = value === null, valIsReflexive = value === value, valIsSymbol = isSymbol(value);
    var othIsDefined = other !== void 0, othIsNull = other === null, othIsReflexive = other === other, othIsSymbol = isSymbol(other);
    if (!othIsNull && !othIsSymbol && !valIsSymbol && value > other || valIsSymbol && othIsDefined && othIsReflexive && !othIsNull && !othIsSymbol || valIsNull && othIsDefined && othIsReflexive || !valIsDefined && othIsReflexive || !valIsReflexive) {
      return 1;
    }
    if (!valIsNull && !valIsSymbol && !othIsSymbol && value < other || othIsSymbol && valIsDefined && valIsReflexive && !valIsNull && !valIsSymbol || othIsNull && valIsDefined && valIsReflexive || !othIsDefined && valIsReflexive || !othIsReflexive) {
      return -1;
    }
  }
  return 0;
}
function compareMultiple(object, other, orders) {
  var index2 = -1, objCriteria = object.criteria, othCriteria = other.criteria, length2 = objCriteria.length, ordersLength = orders.length;
  while (++index2 < length2) {
    var result = compareAscending(objCriteria[index2], othCriteria[index2]);
    if (result) {
      if (index2 >= ordersLength) {
        return result;
      }
      var order = orders[index2];
      return result * (order == "desc" ? -1 : 1);
    }
  }
  return object.index - other.index;
}
function baseOrderBy(collection, iteratees, orders) {
  if (iteratees.length) {
    iteratees = arrayMap(iteratees, function(iteratee) {
      if (isArray(iteratee)) {
        return function(value) {
          return baseGet(value, iteratee.length === 1 ? iteratee[0] : iteratee);
        };
      }
      return iteratee;
    });
  } else {
    iteratees = [identity$1];
  }
  var index2 = -1;
  iteratees = arrayMap(iteratees, baseUnary(baseIteratee));
  var result = baseMap(collection, function(value, key, collection2) {
    var criteria = arrayMap(iteratees, function(iteratee) {
      return iteratee(value);
    });
    return { "criteria": criteria, "index": ++index2, "value": value };
  });
  return baseSortBy(result, function(object, other) {
    return compareMultiple(object, other, orders);
  });
}
function set$2(object, path, value) {
  return object == null ? object : baseSet(object, path, value);
}
var sortBy = baseRest(function(collection, iteratees) {
  if (collection == null) {
    return [];
  }
  var length2 = iteratees.length;
  if (length2 > 1 && isIterateeCall(collection, iteratees[0], iteratees[1])) {
    iteratees = [];
  } else if (length2 > 2 && isIterateeCall(iteratees[0], iteratees[1], iteratees[2])) {
    iteratees = [iteratees[0]];
  }
  return baseOrderBy(collection, baseFlatten(iteratees), []);
});
var FUNC_ERROR_TEXT = "Expected a function";
function throttle(func, wait, options) {
  var leading = true, trailing = true;
  if (typeof func != "function") {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  if (isObject$1(options)) {
    leading = "leading" in options ? !!options.leading : leading;
    trailing = "trailing" in options ? !!options.trailing : trailing;
  }
  return debounce(func, wait, {
    "leading": leading,
    "maxWait": wait,
    "trailing": trailing
  });
}
var INFINITY = 1 / 0;
var createSet = !(Set$1 && 1 / setToArray(new Set$1([, -0]))[1] == INFINITY) ? noop$2 : function(values2) {
  return new Set$1(values2);
};
var LARGE_ARRAY_SIZE = 200;
function baseUniq(array, iteratee, comparator) {
  var index2 = -1, includes = arrayIncludes, length2 = array.length, isCommon = true, result = [], seen = result;
  if (comparator) {
    isCommon = false;
    includes = arrayIncludesWith;
  } else if (length2 >= LARGE_ARRAY_SIZE) {
    var set2 = createSet(array);
    if (set2) {
      return setToArray(set2);
    }
    isCommon = false;
    includes = cacheHas;
    seen = new SetCache();
  } else {
    seen = result;
  }
  outer:
    while (++index2 < length2) {
      var value = array[index2], computed = value;
      value = comparator || value !== 0 ? value : 0;
      if (isCommon && computed === computed) {
        var seenIndex = seen.length;
        while (seenIndex--) {
          if (seen[seenIndex] === computed) {
            continue outer;
          }
        }
        result.push(value);
      } else if (!includes(seen, computed, comparator)) {
        if (seen !== result) {
          seen.push(computed);
        }
        result.push(value);
      }
    }
  return result;
}
function uniqWith(array, comparator) {
  comparator = typeof comparator == "function" ? comparator : void 0;
  return array && array.length ? baseUniq(array, void 0, comparator) : [];
}
function getDefaultExportFromCjs(x) {
  return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, "default") ? x["default"] : x;
}
var loglevel$1 = { exports: {} };
var loglevel = loglevel$1.exports;
var hasRequiredLoglevel;
function requireLoglevel() {
  if (hasRequiredLoglevel) return loglevel$1.exports;
  hasRequiredLoglevel = 1;
  (function(module2) {
    (function(root2, definition) {
      if (module2.exports) {
        module2.exports = definition();
      } else {
        root2.log = definition();
      }
    })(loglevel, function() {
      var noop2 = function() {
      };
      var undefinedType = "undefined";
      var isIE = typeof window !== undefinedType && typeof window.navigator !== undefinedType && /Trident\/|MSIE /.test(window.navigator.userAgent);
      var logMethods = [
        "trace",
        "debug",
        "info",
        "warn",
        "error"
      ];
      var _loggersByName = {};
      var defaultLogger = null;
      function bindMethod(obj, methodName) {
        var method = obj[methodName];
        if (typeof method.bind === "function") {
          return method.bind(obj);
        } else {
          try {
            return Function.prototype.bind.call(method, obj);
          } catch (e) {
            return function() {
              return Function.prototype.apply.apply(method, [obj, arguments]);
            };
          }
        }
      }
      function traceForIE() {
        if (console.log) {
          if (console.log.apply) {
            console.log.apply(console, arguments);
          } else {
            Function.prototype.apply.apply(console.log, [console, arguments]);
          }
        }
        if (console.trace) console.trace();
      }
      function realMethod(methodName) {
        if (methodName === "debug") {
          methodName = "log";
        }
        if (typeof console === undefinedType) {
          return false;
        } else if (methodName === "trace" && isIE) {
          return traceForIE;
        } else if (console[methodName] !== void 0) {
          return bindMethod(console, methodName);
        } else if (console.log !== void 0) {
          return bindMethod(console, "log");
        } else {
          return noop2;
        }
      }
      function replaceLoggingMethods() {
        var level = this.getLevel();
        for (var i = 0; i < logMethods.length; i++) {
          var methodName = logMethods[i];
          this[methodName] = i < level ? noop2 : this.methodFactory(methodName, level, this.name);
        }
        this.log = this.debug;
        if (typeof console === undefinedType && level < this.levels.SILENT) {
          return "No console available for logging";
        }
      }
      function enableLoggingWhenConsoleArrives(methodName) {
        return function() {
          if (typeof console !== undefinedType) {
            replaceLoggingMethods.call(this);
            this[methodName].apply(this, arguments);
          }
        };
      }
      function defaultMethodFactory(methodName, _level, _loggerName) {
        return realMethod(methodName) || enableLoggingWhenConsoleArrives.apply(this, arguments);
      }
      function Logger(name, factory) {
        var self2 = this;
        var inheritedLevel;
        var defaultLevel;
        var userLevel;
        var storageKey = "loglevel";
        if (typeof name === "string") {
          storageKey += ":" + name;
        } else if (typeof name === "symbol") {
          storageKey = void 0;
        }
        function persistLevelIfPossible(levelNum) {
          var levelName = (logMethods[levelNum] || "silent").toUpperCase();
          if (typeof window === undefinedType || !storageKey) return;
          try {
            window.localStorage[storageKey] = levelName;
            return;
          } catch (ignore) {
          }
          try {
            window.document.cookie = encodeURIComponent(storageKey) + "=" + levelName + ";";
          } catch (ignore) {
          }
        }
        function getPersistedLevel() {
          var storedLevel;
          if (typeof window === undefinedType || !storageKey) return;
          try {
            storedLevel = window.localStorage[storageKey];
          } catch (ignore) {
          }
          if (typeof storedLevel === undefinedType) {
            try {
              var cookie = window.document.cookie;
              var cookieName = encodeURIComponent(storageKey);
              var location = cookie.indexOf(cookieName + "=");
              if (location !== -1) {
                storedLevel = /^([^;]+)/.exec(
                  cookie.slice(location + cookieName.length + 1)
                )[1];
              }
            } catch (ignore) {
            }
          }
          if (self2.levels[storedLevel] === void 0) {
            storedLevel = void 0;
          }
          return storedLevel;
        }
        function clearPersistedLevel() {
          if (typeof window === undefinedType || !storageKey) return;
          try {
            window.localStorage.removeItem(storageKey);
          } catch (ignore) {
          }
          try {
            window.document.cookie = encodeURIComponent(storageKey) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
          } catch (ignore) {
          }
        }
        function normalizeLevel(input) {
          var level = input;
          if (typeof level === "string" && self2.levels[level.toUpperCase()] !== void 0) {
            level = self2.levels[level.toUpperCase()];
          }
          if (typeof level === "number" && level >= 0 && level <= self2.levels.SILENT) {
            return level;
          } else {
            throw new TypeError("log.setLevel() called with invalid level: " + input);
          }
        }
        self2.name = name;
        self2.levels = {
          "TRACE": 0,
          "DEBUG": 1,
          "INFO": 2,
          "WARN": 3,
          "ERROR": 4,
          "SILENT": 5
        };
        self2.methodFactory = factory || defaultMethodFactory;
        self2.getLevel = function() {
          if (userLevel != null) {
            return userLevel;
          } else if (defaultLevel != null) {
            return defaultLevel;
          } else {
            return inheritedLevel;
          }
        };
        self2.setLevel = function(level, persist) {
          userLevel = normalizeLevel(level);
          if (persist !== false) {
            persistLevelIfPossible(userLevel);
          }
          return replaceLoggingMethods.call(self2);
        };
        self2.setDefaultLevel = function(level) {
          defaultLevel = normalizeLevel(level);
          if (!getPersistedLevel()) {
            self2.setLevel(level, false);
          }
        };
        self2.resetLevel = function() {
          userLevel = null;
          clearPersistedLevel();
          replaceLoggingMethods.call(self2);
        };
        self2.enableAll = function(persist) {
          self2.setLevel(self2.levels.TRACE, persist);
        };
        self2.disableAll = function(persist) {
          self2.setLevel(self2.levels.SILENT, persist);
        };
        self2.rebuild = function() {
          if (defaultLogger !== self2) {
            inheritedLevel = normalizeLevel(defaultLogger.getLevel());
          }
          replaceLoggingMethods.call(self2);
          if (defaultLogger === self2) {
            for (var childName in _loggersByName) {
              _loggersByName[childName].rebuild();
            }
          }
        };
        inheritedLevel = normalizeLevel(
          defaultLogger ? defaultLogger.getLevel() : "WARN"
        );
        var initialLevel = getPersistedLevel();
        if (initialLevel != null) {
          userLevel = normalizeLevel(initialLevel);
        }
        replaceLoggingMethods.call(self2);
      }
      defaultLogger = new Logger();
      defaultLogger.getLogger = function getLogger(name) {
        if (typeof name !== "symbol" && typeof name !== "string" || name === "") {
          throw new TypeError("You must supply a name when creating a logger.");
        }
        var logger = _loggersByName[name];
        if (!logger) {
          logger = _loggersByName[name] = new Logger(
            name,
            defaultLogger.methodFactory
          );
        }
        return logger;
      };
      var _log = typeof window !== undefinedType ? window.log : void 0;
      defaultLogger.noConflict = function() {
        if (typeof window !== undefinedType && window.log === defaultLogger) {
          window.log = _log;
        }
        return defaultLogger;
      };
      defaultLogger.getLoggers = function getLoggers() {
        return _loggersByName;
      };
      defaultLogger["default"] = defaultLogger;
      return defaultLogger;
    });
  })(loglevel$1);
  return loglevel$1.exports;
}
var loglevelExports = requireLoglevel();
const log = /* @__PURE__ */ getDefaultExportFromCjs(loglevelExports);
const findInCollection = (collection, callback) => {
  let targetItem = null;
  try {
    collection.forEach((item) => {
      if (callback(item)) {
        targetItem = item;
        throw new Error("found");
      }
    });
  } catch {
  }
  return targetItem;
};
function forEachDeep(item, iteratee, path = []) {
  iteratee(item, path);
  if (Array.isArray(item)) {
    item.forEach((value, index2) => forEachDeep(value, iteratee, [...path, index2]));
  } else if (isPlainObject(item)) {
    Object.entries(item).forEach(
      ([key, value]) => forEachDeep(value, iteratee, [...path, key])
    );
  } else if (item !== null && typeof item === "object") {
    log.warn(`forEachDeep: unknown collection type (${item}), path "${path.join(".")}"`);
  }
}
const dedupeById = (items) => {
  const seen = /* @__PURE__ */ new Map();
  items.forEach((item) => {
    if (!seen.has(item.id)) {
      seen.set(item.id, item);
    }
  });
  return Array.from(seen.values());
};
const trackDefaultUiEnabledState = (options) => {
  const EXCLUDED_ACTIONS = ["shape_markers"];
  const defaultUiEnabled = options.settings.controlsUiEnabledByDefault;
  forEachDeep(options, (_, path) => {
    const modeName = path.at(2);
    if (path.length === 4 && path.at(-1) === "uiEnabled") {
      if (EXCLUDED_ACTIONS.includes(modeName)) {
        return;
      }
      set$2(options, path, defaultUiEnabled);
    }
  });
};
const getDefaultOptions = () => {
  return cloneDeep(defaultOptions);
};
const DEV = false;
var is_array = Array.isArray;
var index_of = Array.prototype.indexOf;
var array_from = Array.from;
var define_property = Object.defineProperty;
var get_descriptor = Object.getOwnPropertyDescriptor;
var get_descriptors = Object.getOwnPropertyDescriptors;
var object_prototype = Object.prototype;
var array_prototype = Array.prototype;
var get_prototype_of = Object.getPrototypeOf;
var is_extensible = Object.isExtensible;
function is_function(thing) {
  return typeof thing === "function";
}
const noop$1 = () => {
};
function run_all(arr) {
  for (var i = 0; i < arr.length; i++) {
    arr[i]();
  }
}
function deferred() {
  var resolve;
  var reject;
  var promise = new Promise((res, rej) => {
    resolve = res;
    reject = rej;
  });
  return { promise, resolve, reject };
}
function to_array(value, n) {
  if (Array.isArray(value)) {
    return value;
  }
  if (!(Symbol.iterator in value)) {
    return Array.from(value);
  }
  const array = [];
  for (const element of value) {
    array.push(element);
    if (array.length === n) break;
  }
  return array;
}
const DERIVED = 1 << 1;
const EFFECT = 1 << 2;
const RENDER_EFFECT = 1 << 3;
const MANAGED_EFFECT = 1 << 24;
const BLOCK_EFFECT = 1 << 4;
const BRANCH_EFFECT = 1 << 5;
const ROOT_EFFECT = 1 << 6;
const BOUNDARY_EFFECT = 1 << 7;
const CONNECTED = 1 << 9;
const CLEAN = 1 << 10;
const DIRTY = 1 << 11;
const MAYBE_DIRTY = 1 << 12;
const INERT = 1 << 13;
const DESTROYED = 1 << 14;
const EFFECT_RAN = 1 << 15;
const EFFECT_TRANSPARENT = 1 << 16;
const EAGER_EFFECT = 1 << 17;
const HEAD_EFFECT = 1 << 18;
const EFFECT_PRESERVED = 1 << 19;
const USER_EFFECT = 1 << 20;
const EFFECT_OFFSCREEN = 1 << 25;
const WAS_MARKED = 1 << 15;
const REACTION_IS_UPDATING = 1 << 21;
const ASYNC = 1 << 22;
const ERROR_VALUE = 1 << 23;
const STATE_SYMBOL = Symbol("$state");
const LOADING_ATTR_SYMBOL = Symbol("");
const STALE_REACTION = new class StaleReactionError extends Error {
  name = "StaleReactionError";
  message = "The reaction that called `getAbortSignal()` was re-run or destroyed";
}();
function equals(value) {
  return value === this.v;
}
function safe_not_equal(a, b) {
  return a != a ? b == b : a !== b || a !== null && typeof a === "object" || typeof a === "function";
}
function safe_equals(value) {
  return !safe_not_equal(value, this.v);
}
function lifecycle_outside_component(name) {
  {
    throw new Error(`https://svelte.dev/e/lifecycle_outside_component`);
  }
}
function async_derived_orphan() {
  {
    throw new Error(`https://svelte.dev/e/async_derived_orphan`);
  }
}
function effect_in_teardown(rune) {
  {
    throw new Error(`https://svelte.dev/e/effect_in_teardown`);
  }
}
function effect_in_unowned_derived() {
  {
    throw new Error(`https://svelte.dev/e/effect_in_unowned_derived`);
  }
}
function effect_orphan(rune) {
  {
    throw new Error(`https://svelte.dev/e/effect_orphan`);
  }
}
function effect_update_depth_exceeded() {
  {
    throw new Error(`https://svelte.dev/e/effect_update_depth_exceeded`);
  }
}
function state_descriptors_fixed() {
  {
    throw new Error(`https://svelte.dev/e/state_descriptors_fixed`);
  }
}
function state_prototype_fixed() {
  {
    throw new Error(`https://svelte.dev/e/state_prototype_fixed`);
  }
}
function state_unsafe_mutation() {
  {
    throw new Error(`https://svelte.dev/e/state_unsafe_mutation`);
  }
}
function svelte_boundary_reset_onerror() {
  {
    throw new Error(`https://svelte.dev/e/svelte_boundary_reset_onerror`);
  }
}
let tracing_mode_flag = false;
const EACH_ITEM_REACTIVE = 1;
const EACH_INDEX_REACTIVE = 1 << 1;
const EACH_IS_CONTROLLED = 1 << 2;
const EACH_IS_ANIMATED = 1 << 3;
const EACH_ITEM_IMMUTABLE = 1 << 4;
const TRANSITION_IN = 1;
const TRANSITION_OUT = 1 << 1;
const TRANSITION_GLOBAL = 1 << 2;
const TEMPLATE_FRAGMENT = 1;
const TEMPLATE_USE_IMPORT_NODE = 1 << 1;
const UNINITIALIZED = Symbol();
const NAMESPACE_HTML = "http://www.w3.org/1999/xhtml";
let component_context = null;
function set_component_context(context) {
  component_context = context;
}
function getContext(key) {
  const context_map = get_or_init_context_map();
  const result = (
    /** @type {T} */
    context_map.get(key)
  );
  return result;
}
function push(props, runes = false, fn) {
  component_context = {
    p: component_context,
    i: false,
    c: null,
    e: null,
    s: props,
    x: null,
    l: null
  };
}
function pop(component) {
  var context = (
    /** @type {ComponentContext} */
    component_context
  );
  var effects = context.e;
  if (effects !== null) {
    context.e = null;
    for (var fn of effects) {
      create_user_effect(fn);
    }
  }
  context.i = true;
  component_context = context.p;
  return (
    /** @type {T} */
    {}
  );
}
function is_runes() {
  return true;
}
function get_or_init_context_map(name) {
  if (component_context === null) {
    lifecycle_outside_component();
  }
  return component_context.c ??= new Map(get_parent_context(component_context) || void 0);
}
function get_parent_context(component_context2) {
  let parent = component_context2.p;
  while (parent !== null) {
    const context_map = parent.c;
    if (context_map !== null) {
      return context_map;
    }
    parent = parent.p;
  }
  return null;
}
let micro_tasks = [];
function run_micro_tasks() {
  var tasks = micro_tasks;
  micro_tasks = [];
  run_all(tasks);
}
function queue_micro_task(fn) {
  if (micro_tasks.length === 0 && true) {
    var tasks = micro_tasks;
    queueMicrotask(() => {
      if (tasks === micro_tasks) run_micro_tasks();
    });
  }
  micro_tasks.push(fn);
}
function select_multiple_invalid_value() {
  {
    console.warn(`https://svelte.dev/e/select_multiple_invalid_value`);
  }
}
function svelte_boundary_reset_noop() {
  {
    console.warn(`https://svelte.dev/e/svelte_boundary_reset_noop`);
  }
}
function proxy(value) {
  if (typeof value !== "object" || value === null || STATE_SYMBOL in value) {
    return value;
  }
  const prototype = get_prototype_of(value);
  if (prototype !== object_prototype && prototype !== array_prototype) {
    return value;
  }
  var sources = /* @__PURE__ */ new Map();
  var is_proxied_array = is_array(value);
  var version = /* @__PURE__ */ state(0);
  var parent_version = update_version;
  var with_parent = (fn) => {
    if (update_version === parent_version) {
      return fn();
    }
    var reaction = active_reaction;
    var version2 = update_version;
    set_active_reaction(null);
    set_update_version(parent_version);
    var result = fn();
    set_active_reaction(reaction);
    set_update_version(version2);
    return result;
  };
  if (is_proxied_array) {
    sources.set("length", /* @__PURE__ */ state(
      /** @type {any[]} */
      value.length
    ));
  }
  return new Proxy(
    /** @type {any} */
    value,
    {
      defineProperty(_, prop, descriptor) {
        if (!("value" in descriptor) || descriptor.configurable === false || descriptor.enumerable === false || descriptor.writable === false) {
          state_descriptors_fixed();
        }
        var s = sources.get(prop);
        if (s === void 0) {
          s = with_parent(() => {
            var s2 = /* @__PURE__ */ state(descriptor.value);
            sources.set(prop, s2);
            return s2;
          });
        } else {
          set$1(s, descriptor.value, true);
        }
        return true;
      },
      deleteProperty(target, prop) {
        var s = sources.get(prop);
        if (s === void 0) {
          if (prop in target) {
            const s2 = with_parent(() => /* @__PURE__ */ state(UNINITIALIZED));
            sources.set(prop, s2);
            increment(version);
          }
        } else {
          set$1(s, UNINITIALIZED);
          increment(version);
        }
        return true;
      },
      get(target, prop, receiver) {
        if (prop === STATE_SYMBOL) {
          return value;
        }
        var s = sources.get(prop);
        var exists = prop in target;
        if (s === void 0 && (!exists || get_descriptor(target, prop)?.writable)) {
          s = with_parent(() => {
            var p = proxy(exists ? target[prop] : UNINITIALIZED);
            var s2 = /* @__PURE__ */ state(p);
            return s2;
          });
          sources.set(prop, s);
        }
        if (s !== void 0) {
          var v = get$1(s);
          return v === UNINITIALIZED ? void 0 : v;
        }
        return Reflect.get(target, prop, receiver);
      },
      getOwnPropertyDescriptor(target, prop) {
        var descriptor = Reflect.getOwnPropertyDescriptor(target, prop);
        if (descriptor && "value" in descriptor) {
          var s = sources.get(prop);
          if (s) descriptor.value = get$1(s);
        } else if (descriptor === void 0) {
          var source2 = sources.get(prop);
          var value2 = source2?.v;
          if (source2 !== void 0 && value2 !== UNINITIALIZED) {
            return {
              enumerable: true,
              configurable: true,
              value: value2,
              writable: true
            };
          }
        }
        return descriptor;
      },
      has(target, prop) {
        if (prop === STATE_SYMBOL) {
          return true;
        }
        var s = sources.get(prop);
        var has = s !== void 0 && s.v !== UNINITIALIZED || Reflect.has(target, prop);
        if (s !== void 0 || active_effect !== null && (!has || get_descriptor(target, prop)?.writable)) {
          if (s === void 0) {
            s = with_parent(() => {
              var p = has ? proxy(target[prop]) : UNINITIALIZED;
              var s2 = /* @__PURE__ */ state(p);
              return s2;
            });
            sources.set(prop, s);
          }
          var value2 = get$1(s);
          if (value2 === UNINITIALIZED) {
            return false;
          }
        }
        return has;
      },
      set(target, prop, value2, receiver) {
        var s = sources.get(prop);
        var has = prop in target;
        if (is_proxied_array && prop === "length") {
          for (var i = value2; i < /** @type {Source<number>} */
          s.v; i += 1) {
            var other_s = sources.get(i + "");
            if (other_s !== void 0) {
              set$1(other_s, UNINITIALIZED);
            } else if (i in target) {
              other_s = with_parent(() => /* @__PURE__ */ state(UNINITIALIZED));
              sources.set(i + "", other_s);
            }
          }
        }
        if (s === void 0) {
          if (!has || get_descriptor(target, prop)?.writable) {
            s = with_parent(() => /* @__PURE__ */ state(void 0));
            set$1(s, proxy(value2));
            sources.set(prop, s);
          }
        } else {
          has = s.v !== UNINITIALIZED;
          var p = with_parent(() => proxy(value2));
          set$1(s, p);
        }
        var descriptor = Reflect.getOwnPropertyDescriptor(target, prop);
        if (descriptor?.set) {
          descriptor.set.call(receiver, value2);
        }
        if (!has) {
          if (is_proxied_array && typeof prop === "string") {
            var ls = (
              /** @type {Source<number>} */
              sources.get("length")
            );
            var n = Number(prop);
            if (Number.isInteger(n) && n >= ls.v) {
              set$1(ls, n + 1);
            }
          }
          increment(version);
        }
        return true;
      },
      ownKeys(target) {
        get$1(version);
        var own_keys = Reflect.ownKeys(target).filter((key2) => {
          var source3 = sources.get(key2);
          return source3 === void 0 || source3.v !== UNINITIALIZED;
        });
        for (var [key, source2] of sources) {
          if (source2.v !== UNINITIALIZED && !(key in target)) {
            own_keys.push(key);
          }
        }
        return own_keys;
      },
      setPrototypeOf() {
        state_prototype_fixed();
      }
    }
  );
}
function get_proxied_value(value) {
  try {
    if (value !== null && typeof value === "object" && STATE_SYMBOL in value) {
      return value[STATE_SYMBOL];
    }
  } catch {
  }
  return value;
}
function is(a, b) {
  return Object.is(get_proxied_value(a), get_proxied_value(b));
}
var $window;
var is_firefox;
var first_child_getter;
var next_sibling_getter;
function init_operations() {
  if ($window !== void 0) {
    return;
  }
  $window = window;
  is_firefox = /Firefox/.test(navigator.userAgent);
  var element_prototype = Element.prototype;
  var node_prototype = Node.prototype;
  var text_prototype = Text.prototype;
  first_child_getter = get_descriptor(node_prototype, "firstChild").get;
  next_sibling_getter = get_descriptor(node_prototype, "nextSibling").get;
  if (is_extensible(element_prototype)) {
    element_prototype.__click = void 0;
    element_prototype.__className = void 0;
    element_prototype.__attributes = null;
    element_prototype.__style = void 0;
    element_prototype.__e = void 0;
  }
  if (is_extensible(text_prototype)) {
    text_prototype.__t = void 0;
  }
}
function create_text(value = "") {
  return document.createTextNode(value);
}
// @__NO_SIDE_EFFECTS__
function get_first_child(node) {
  return (
    /** @type {TemplateNode | null} */
    first_child_getter.call(node)
  );
}
// @__NO_SIDE_EFFECTS__
function get_next_sibling(node) {
  return (
    /** @type {TemplateNode | null} */
    next_sibling_getter.call(node)
  );
}
function child(node, is_text) {
  {
    return /* @__PURE__ */ get_first_child(node);
  }
}
function first_child(node, is_text = false) {
  {
    var first = /* @__PURE__ */ get_first_child(node);
    if (first instanceof Comment && first.data === "") return /* @__PURE__ */ get_next_sibling(first);
    return first;
  }
}
function sibling(node, count = 1, is_text = false) {
  let next_sibling = node;
  while (count--) {
    next_sibling = /** @type {TemplateNode} */
    /* @__PURE__ */ get_next_sibling(next_sibling);
  }
  {
    return next_sibling;
  }
}
function clear_text_content(node) {
  node.textContent = "";
}
function should_defer_append() {
  return false;
}
function handle_error(error) {
  var effect2 = active_effect;
  if (effect2 === null) {
    active_reaction.f |= ERROR_VALUE;
    return error;
  }
  if ((effect2.f & EFFECT_RAN) === 0) {
    if ((effect2.f & BOUNDARY_EFFECT) === 0) {
      throw error;
    }
    effect2.b.error(error);
  } else {
    invoke_error_boundary(error, effect2);
  }
}
function invoke_error_boundary(error, effect2) {
  while (effect2 !== null) {
    if ((effect2.f & BOUNDARY_EFFECT) !== 0) {
      try {
        effect2.b.error(error);
        return;
      } catch (e) {
        error = e;
      }
    }
    effect2 = effect2.parent;
  }
  throw error;
}
const batches = /* @__PURE__ */ new Set();
let current_batch = null;
let previous_batch = null;
let batch_values = null;
let queued_root_effects = [];
let last_scheduled_effect = null;
let is_flushing = false;
class Batch {
  committed = false;
  /**
   * The current values of any sources that are updated in this batch
   * They keys of this map are identical to `this.#previous`
   * @type {Map<Source, any>}
   */
  current = /* @__PURE__ */ new Map();
  /**
   * The values of any sources that are updated in this batch _before_ those updates took place.
   * They keys of this map are identical to `this.#current`
   * @type {Map<Source, any>}
   */
  previous = /* @__PURE__ */ new Map();
  /**
   * When the batch is committed (and the DOM is updated), we need to remove old branches
   * and append new ones by calling the functions added inside (if/each/key/etc) blocks
   * @type {Set<() => void>}
   */
  #commit_callbacks = /* @__PURE__ */ new Set();
  /**
   * If a fork is discarded, we need to destroy any effects that are no longer needed
   * @type {Set<(batch: Batch) => void>}
   */
  #discard_callbacks = /* @__PURE__ */ new Set();
  /**
   * The number of async effects that are currently in flight
   */
  #pending = 0;
  /**
   * The number of async effects that are currently in flight, _not_ inside a pending boundary
   */
  #blocking_pending = 0;
  /**
   * A deferred that resolves when the batch is committed, used with `settled()`
   * TODO replace with Promise.withResolvers once supported widely enough
   * @type {{ promise: Promise<void>, resolve: (value?: any) => void, reject: (reason: unknown) => void } | null}
   */
  #deferred = null;
  /**
   * Deferred effects (which run after async work has completed) that are DIRTY
   * @type {Set<Effect>}
   */
  #dirty_effects = /* @__PURE__ */ new Set();
  /**
   * Deferred effects that are MAYBE_DIRTY
   * @type {Set<Effect>}
   */
  #maybe_dirty_effects = /* @__PURE__ */ new Set();
  /**
   * A set of branches that still exist, but will be destroyed when this batch
   * is committed — we skip over these during `process`
   * @type {Set<Effect>}
   */
  skipped_effects = /* @__PURE__ */ new Set();
  is_fork = false;
  is_deferred() {
    return this.is_fork || this.#blocking_pending > 0;
  }
  /**
   *
   * @param {Effect[]} root_effects
   */
  process(root_effects) {
    queued_root_effects = [];
    previous_batch = null;
    this.apply();
    var target = {
      parent: null,
      effect: null,
      effects: [],
      render_effects: []
    };
    for (const root2 of root_effects) {
      this.#traverse_effect_tree(root2, target);
    }
    if (!this.is_fork) {
      this.#resolve();
    }
    if (this.is_deferred()) {
      this.#defer_effects(target.effects);
      this.#defer_effects(target.render_effects);
    } else {
      previous_batch = this;
      current_batch = null;
      flush_queued_effects(target.render_effects);
      flush_queued_effects(target.effects);
      previous_batch = null;
      this.#deferred?.resolve();
    }
    batch_values = null;
  }
  /**
   * Traverse the effect tree, executing effects or stashing
   * them for later execution as appropriate
   * @param {Effect} root
   * @param {EffectTarget} target
   */
  #traverse_effect_tree(root2, target) {
    root2.f ^= CLEAN;
    var effect2 = root2.first;
    while (effect2 !== null) {
      var flags2 = effect2.f;
      var is_branch = (flags2 & (BRANCH_EFFECT | ROOT_EFFECT)) !== 0;
      var is_skippable_branch = is_branch && (flags2 & CLEAN) !== 0;
      var skip = is_skippable_branch || (flags2 & INERT) !== 0 || this.skipped_effects.has(effect2);
      if ((effect2.f & BOUNDARY_EFFECT) !== 0 && effect2.b?.is_pending()) {
        target = {
          parent: target,
          effect: effect2,
          effects: [],
          render_effects: []
        };
      }
      if (!skip && effect2.fn !== null) {
        if (is_branch) {
          effect2.f ^= CLEAN;
        } else if ((flags2 & EFFECT) !== 0) {
          target.effects.push(effect2);
        } else if (is_dirty(effect2)) {
          if ((effect2.f & BLOCK_EFFECT) !== 0) this.#dirty_effects.add(effect2);
          update_effect(effect2);
        }
        var child2 = effect2.first;
        if (child2 !== null) {
          effect2 = child2;
          continue;
        }
      }
      var parent = effect2.parent;
      effect2 = effect2.next;
      while (effect2 === null && parent !== null) {
        if (parent === target.effect) {
          this.#defer_effects(target.effects);
          this.#defer_effects(target.render_effects);
          target = /** @type {EffectTarget} */
          target.parent;
        }
        effect2 = parent.next;
        parent = parent.parent;
      }
    }
  }
  /**
   * @param {Effect[]} effects
   */
  #defer_effects(effects) {
    for (const e of effects) {
      if ((e.f & DIRTY) !== 0) {
        this.#dirty_effects.add(e);
      } else if ((e.f & MAYBE_DIRTY) !== 0) {
        this.#maybe_dirty_effects.add(e);
      }
      this.#clear_marked(e.deps);
      set_signal_status(e, CLEAN);
    }
  }
  /**
   * @param {Value[] | null} deps
   */
  #clear_marked(deps) {
    if (deps === null) return;
    for (const dep of deps) {
      if ((dep.f & DERIVED) === 0 || (dep.f & WAS_MARKED) === 0) {
        continue;
      }
      dep.f ^= WAS_MARKED;
      this.#clear_marked(
        /** @type {Derived} */
        dep.deps
      );
    }
  }
  /**
   * Associate a change to a given source with the current
   * batch, noting its previous and current values
   * @param {Source} source
   * @param {any} value
   */
  capture(source2, value) {
    if (!this.previous.has(source2)) {
      this.previous.set(source2, value);
    }
    if ((source2.f & ERROR_VALUE) === 0) {
      this.current.set(source2, source2.v);
      batch_values?.set(source2, source2.v);
    }
  }
  activate() {
    current_batch = this;
    this.apply();
  }
  deactivate() {
    if (current_batch !== this) return;
    current_batch = null;
    batch_values = null;
  }
  flush() {
    this.activate();
    if (queued_root_effects.length > 0) {
      flush_effects();
      if (current_batch !== null && current_batch !== this) {
        return;
      }
    } else if (this.#pending === 0) {
      this.process([]);
    }
    this.deactivate();
  }
  discard() {
    for (const fn of this.#discard_callbacks) fn(this);
    this.#discard_callbacks.clear();
  }
  #resolve() {
    if (this.#blocking_pending === 0) {
      for (const fn of this.#commit_callbacks) fn();
      this.#commit_callbacks.clear();
    }
    if (this.#pending === 0) {
      this.#commit();
    }
  }
  #commit() {
    if (batches.size > 1) {
      this.previous.clear();
      var previous_batch_values = batch_values;
      var is_earlier = true;
      var dummy_target = {
        parent: null,
        effect: null,
        effects: [],
        render_effects: []
      };
      for (const batch of batches) {
        if (batch === this) {
          is_earlier = false;
          continue;
        }
        const sources = [];
        for (const [source2, value] of this.current) {
          if (batch.current.has(source2)) {
            if (is_earlier && value !== batch.current.get(source2)) {
              batch.current.set(source2, value);
            } else {
              continue;
            }
          }
          sources.push(source2);
        }
        if (sources.length === 0) {
          continue;
        }
        const others = [...batch.current.keys()].filter((s) => !this.current.has(s));
        if (others.length > 0) {
          var prev_queued_root_effects = queued_root_effects;
          queued_root_effects = [];
          const marked = /* @__PURE__ */ new Set();
          const checked = /* @__PURE__ */ new Map();
          for (const source2 of sources) {
            mark_effects(source2, others, marked, checked);
          }
          if (queued_root_effects.length > 0) {
            current_batch = batch;
            batch.apply();
            for (const root2 of queued_root_effects) {
              batch.#traverse_effect_tree(root2, dummy_target);
            }
            batch.deactivate();
          }
          queued_root_effects = prev_queued_root_effects;
        }
      }
      current_batch = null;
      batch_values = previous_batch_values;
    }
    this.committed = true;
    batches.delete(this);
  }
  /**
   *
   * @param {boolean} blocking
   */
  increment(blocking) {
    this.#pending += 1;
    if (blocking) this.#blocking_pending += 1;
  }
  /**
   *
   * @param {boolean} blocking
   */
  decrement(blocking) {
    this.#pending -= 1;
    if (blocking) this.#blocking_pending -= 1;
    this.revive();
  }
  revive() {
    for (const e of this.#dirty_effects) {
      this.#maybe_dirty_effects.delete(e);
      set_signal_status(e, DIRTY);
      schedule_effect(e);
    }
    for (const e of this.#maybe_dirty_effects) {
      set_signal_status(e, MAYBE_DIRTY);
      schedule_effect(e);
    }
    this.flush();
  }
  /** @param {() => void} fn */
  oncommit(fn) {
    this.#commit_callbacks.add(fn);
  }
  /** @param {(batch: Batch) => void} fn */
  ondiscard(fn) {
    this.#discard_callbacks.add(fn);
  }
  settled() {
    return (this.#deferred ??= deferred()).promise;
  }
  static ensure() {
    if (current_batch === null) {
      const batch = current_batch = new Batch();
      batches.add(current_batch);
      {
        Batch.enqueue(() => {
          if (current_batch !== batch) {
            return;
          }
          batch.flush();
        });
      }
    }
    return current_batch;
  }
  /** @param {() => void} task */
  static enqueue(task) {
    queue_micro_task(task);
  }
  apply() {
    return;
  }
}
function flush_effects() {
  var was_updating_effect = is_updating_effect;
  is_flushing = true;
  var source_stacks = null;
  try {
    var flush_count = 0;
    set_is_updating_effect(true);
    while (queued_root_effects.length > 0) {
      var batch = Batch.ensure();
      if (flush_count++ > 1e3) {
        var updates, entry;
        if (DEV) ;
        infinite_loop_guard();
      }
      batch.process(queued_root_effects);
      old_values.clear();
      if (DEV) ;
    }
  } finally {
    is_flushing = false;
    set_is_updating_effect(was_updating_effect);
    last_scheduled_effect = null;
  }
}
function infinite_loop_guard() {
  try {
    effect_update_depth_exceeded();
  } catch (error) {
    invoke_error_boundary(error, last_scheduled_effect);
  }
}
let eager_block_effects = null;
function flush_queued_effects(effects) {
  var length2 = effects.length;
  if (length2 === 0) return;
  var i = 0;
  while (i < length2) {
    var effect2 = effects[i++];
    if ((effect2.f & (DESTROYED | INERT)) === 0 && is_dirty(effect2)) {
      eager_block_effects = /* @__PURE__ */ new Set();
      update_effect(effect2);
      if (effect2.deps === null && effect2.first === null && effect2.nodes === null) {
        if (effect2.teardown === null && effect2.ac === null) {
          unlink_effect(effect2);
        } else {
          effect2.fn = null;
        }
      }
      if (eager_block_effects?.size > 0) {
        old_values.clear();
        for (const e of eager_block_effects) {
          if ((e.f & (DESTROYED | INERT)) !== 0) continue;
          const ordered_effects = [e];
          let ancestor = e.parent;
          while (ancestor !== null) {
            if (eager_block_effects.has(ancestor)) {
              eager_block_effects.delete(ancestor);
              ordered_effects.push(ancestor);
            }
            ancestor = ancestor.parent;
          }
          for (let j = ordered_effects.length - 1; j >= 0; j--) {
            const e2 = ordered_effects[j];
            if ((e2.f & (DESTROYED | INERT)) !== 0) continue;
            update_effect(e2);
          }
        }
        eager_block_effects.clear();
      }
    }
  }
  eager_block_effects = null;
}
function mark_effects(value, sources, marked, checked) {
  if (marked.has(value)) return;
  marked.add(value);
  if (value.reactions !== null) {
    for (const reaction of value.reactions) {
      const flags2 = reaction.f;
      if ((flags2 & DERIVED) !== 0) {
        mark_effects(
          /** @type {Derived} */
          reaction,
          sources,
          marked,
          checked
        );
      } else if ((flags2 & (ASYNC | BLOCK_EFFECT)) !== 0 && (flags2 & DIRTY) === 0 && depends_on(reaction, sources, checked)) {
        set_signal_status(reaction, DIRTY);
        schedule_effect(
          /** @type {Effect} */
          reaction
        );
      }
    }
  }
}
function depends_on(reaction, sources, checked) {
  const depends = checked.get(reaction);
  if (depends !== void 0) return depends;
  if (reaction.deps !== null) {
    for (const dep of reaction.deps) {
      if (sources.includes(dep)) {
        return true;
      }
      if ((dep.f & DERIVED) !== 0 && depends_on(
        /** @type {Derived} */
        dep,
        sources,
        checked
      )) {
        checked.set(
          /** @type {Derived} */
          dep,
          true
        );
        return true;
      }
    }
  }
  checked.set(reaction, false);
  return false;
}
function schedule_effect(signal) {
  var effect2 = last_scheduled_effect = signal;
  while (effect2.parent !== null) {
    effect2 = effect2.parent;
    var flags2 = effect2.f;
    if (is_flushing && effect2 === active_effect && (flags2 & BLOCK_EFFECT) !== 0 && (flags2 & HEAD_EFFECT) === 0) {
      return;
    }
    if ((flags2 & (ROOT_EFFECT | BRANCH_EFFECT)) !== 0) {
      if ((flags2 & CLEAN) === 0) return;
      effect2.f ^= CLEAN;
    }
  }
  queued_root_effects.push(effect2);
}
function createSubscriber(start) {
  let subscribers = 0;
  let version = source(0);
  let stop;
  return () => {
    if (effect_tracking()) {
      get$1(version);
      render_effect(() => {
        if (subscribers === 0) {
          stop = untrack(() => start(() => increment(version)));
        }
        subscribers += 1;
        return () => {
          queue_micro_task(() => {
            subscribers -= 1;
            if (subscribers === 0) {
              stop?.();
              stop = void 0;
              increment(version);
            }
          });
        };
      });
    }
  };
}
var flags = EFFECT_TRANSPARENT | EFFECT_PRESERVED | BOUNDARY_EFFECT;
function boundary(node, props, children) {
  new Boundary(node, props, children);
}
class Boundary {
  /** @type {Boundary | null} */
  parent;
  #pending = false;
  /** @type {TemplateNode} */
  #anchor;
  /** @type {TemplateNode | null} */
  #hydrate_open = null;
  /** @type {BoundaryProps} */
  #props;
  /** @type {((anchor: Node) => void)} */
  #children;
  /** @type {Effect} */
  #effect;
  /** @type {Effect | null} */
  #main_effect = null;
  /** @type {Effect | null} */
  #pending_effect = null;
  /** @type {Effect | null} */
  #failed_effect = null;
  /** @type {DocumentFragment | null} */
  #offscreen_fragment = null;
  /** @type {TemplateNode | null} */
  #pending_anchor = null;
  #local_pending_count = 0;
  #pending_count = 0;
  #is_creating_fallback = false;
  /**
   * A source containing the number of pending async deriveds/expressions.
   * Only created if `$effect.pending()` is used inside the boundary,
   * otherwise updating the source results in needless `Batch.ensure()`
   * calls followed by no-op flushes
   * @type {Source<number> | null}
   */
  #effect_pending = null;
  #effect_pending_subscriber = createSubscriber(() => {
    this.#effect_pending = source(this.#local_pending_count);
    return () => {
      this.#effect_pending = null;
    };
  });
  /**
   * @param {TemplateNode} node
   * @param {BoundaryProps} props
   * @param {((anchor: Node) => void)} children
   */
  constructor(node, props, children) {
    this.#anchor = node;
    this.#props = props;
    this.#children = children;
    this.parent = /** @type {Effect} */
    active_effect.b;
    this.#pending = !!this.#props.pending;
    this.#effect = block(() => {
      active_effect.b = this;
      {
        var anchor = this.#get_anchor();
        try {
          this.#main_effect = branch(() => children(anchor));
        } catch (error) {
          this.error(error);
        }
        if (this.#pending_count > 0) {
          this.#show_pending_snippet();
        } else {
          this.#pending = false;
        }
      }
      return () => {
        this.#pending_anchor?.remove();
      };
    }, flags);
  }
  #hydrate_resolved_content() {
    try {
      this.#main_effect = branch(() => this.#children(this.#anchor));
    } catch (error) {
      this.error(error);
    }
    this.#pending = false;
  }
  #hydrate_pending_content() {
    const pending = this.#props.pending;
    if (!pending) {
      return;
    }
    this.#pending_effect = branch(() => pending(this.#anchor));
    Batch.enqueue(() => {
      var anchor = this.#get_anchor();
      this.#main_effect = this.#run(() => {
        Batch.ensure();
        return branch(() => this.#children(anchor));
      });
      if (this.#pending_count > 0) {
        this.#show_pending_snippet();
      } else {
        pause_effect(
          /** @type {Effect} */
          this.#pending_effect,
          () => {
            this.#pending_effect = null;
          }
        );
        this.#pending = false;
      }
    });
  }
  #get_anchor() {
    var anchor = this.#anchor;
    if (this.#pending) {
      this.#pending_anchor = create_text();
      this.#anchor.before(this.#pending_anchor);
      anchor = this.#pending_anchor;
    }
    return anchor;
  }
  /**
   * Returns `true` if the effect exists inside a boundary whose pending snippet is shown
   * @returns {boolean}
   */
  is_pending() {
    return this.#pending || !!this.parent && this.parent.is_pending();
  }
  has_pending_snippet() {
    return !!this.#props.pending;
  }
  /**
   * @param {() => Effect | null} fn
   */
  #run(fn) {
    var previous_effect = active_effect;
    var previous_reaction = active_reaction;
    var previous_ctx = component_context;
    set_active_effect(this.#effect);
    set_active_reaction(this.#effect);
    set_component_context(this.#effect.ctx);
    try {
      return fn();
    } catch (e) {
      handle_error(e);
      return null;
    } finally {
      set_active_effect(previous_effect);
      set_active_reaction(previous_reaction);
      set_component_context(previous_ctx);
    }
  }
  #show_pending_snippet() {
    const pending = (
      /** @type {(anchor: Node) => void} */
      this.#props.pending
    );
    if (this.#main_effect !== null) {
      this.#offscreen_fragment = document.createDocumentFragment();
      this.#offscreen_fragment.append(
        /** @type {TemplateNode} */
        this.#pending_anchor
      );
      move_effect(this.#main_effect, this.#offscreen_fragment);
    }
    if (this.#pending_effect === null) {
      this.#pending_effect = branch(() => pending(this.#anchor));
    }
  }
  /**
   * Updates the pending count associated with the currently visible pending snippet,
   * if any, such that we can replace the snippet with content once work is done
   * @param {1 | -1} d
   */
  #update_pending_count(d) {
    if (!this.has_pending_snippet()) {
      if (this.parent) {
        this.parent.#update_pending_count(d);
      }
      return;
    }
    this.#pending_count += d;
    if (this.#pending_count === 0) {
      this.#pending = false;
      if (this.#pending_effect) {
        pause_effect(this.#pending_effect, () => {
          this.#pending_effect = null;
        });
      }
      if (this.#offscreen_fragment) {
        this.#anchor.before(this.#offscreen_fragment);
        this.#offscreen_fragment = null;
      }
    }
  }
  /**
   * Update the source that powers `$effect.pending()` inside this boundary,
   * and controls when the current `pending` snippet (if any) is removed.
   * Do not call from inside the class
   * @param {1 | -1} d
   */
  update_pending_count(d) {
    this.#update_pending_count(d);
    this.#local_pending_count += d;
    if (this.#effect_pending) {
      internal_set(this.#effect_pending, this.#local_pending_count);
    }
  }
  get_effect_pending() {
    this.#effect_pending_subscriber();
    return get$1(
      /** @type {Source<number>} */
      this.#effect_pending
    );
  }
  /** @param {unknown} error */
  error(error) {
    var onerror = this.#props.onerror;
    let failed = this.#props.failed;
    if (this.#is_creating_fallback || !onerror && !failed) {
      throw error;
    }
    if (this.#main_effect) {
      destroy_effect(this.#main_effect);
      this.#main_effect = null;
    }
    if (this.#pending_effect) {
      destroy_effect(this.#pending_effect);
      this.#pending_effect = null;
    }
    if (this.#failed_effect) {
      destroy_effect(this.#failed_effect);
      this.#failed_effect = null;
    }
    var did_reset = false;
    var calling_on_error = false;
    const reset = () => {
      if (did_reset) {
        svelte_boundary_reset_noop();
        return;
      }
      did_reset = true;
      if (calling_on_error) {
        svelte_boundary_reset_onerror();
      }
      Batch.ensure();
      this.#local_pending_count = 0;
      if (this.#failed_effect !== null) {
        pause_effect(this.#failed_effect, () => {
          this.#failed_effect = null;
        });
      }
      this.#pending = this.has_pending_snippet();
      this.#main_effect = this.#run(() => {
        this.#is_creating_fallback = false;
        return branch(() => this.#children(this.#anchor));
      });
      if (this.#pending_count > 0) {
        this.#show_pending_snippet();
      } else {
        this.#pending = false;
      }
    };
    var previous_reaction = active_reaction;
    try {
      set_active_reaction(null);
      calling_on_error = true;
      onerror?.(error, reset);
      calling_on_error = false;
    } catch (error2) {
      invoke_error_boundary(error2, this.#effect && this.#effect.parent);
    } finally {
      set_active_reaction(previous_reaction);
    }
    if (failed) {
      queue_micro_task(() => {
        this.#failed_effect = this.#run(() => {
          Batch.ensure();
          this.#is_creating_fallback = true;
          try {
            return branch(() => {
              failed(
                this.#anchor,
                () => error,
                () => reset
              );
            });
          } catch (error2) {
            invoke_error_boundary(
              error2,
              /** @type {Effect} */
              this.#effect.parent
            );
            return null;
          } finally {
            this.#is_creating_fallback = false;
          }
        });
      });
    }
  }
}
function flatten(blockers, sync, async, fn) {
  const d = derived;
  if (async.length === 0 && blockers.length === 0) {
    fn(sync.map(d));
    return;
  }
  var batch = current_batch;
  var parent = (
    /** @type {Effect} */
    active_effect
  );
  var restore = capture();
  function run() {
    Promise.all(async.map((expression) => /* @__PURE__ */ async_derived(expression))).then((result) => {
      restore();
      try {
        fn([...sync.map(d), ...result]);
      } catch (error) {
        if ((parent.f & DESTROYED) === 0) {
          invoke_error_boundary(error, parent);
        }
      }
      batch?.deactivate();
      unset_context();
    }).catch((error) => {
      invoke_error_boundary(error, parent);
    });
  }
  if (blockers.length > 0) {
    Promise.all(blockers).then(() => {
      restore();
      try {
        return run();
      } finally {
        batch?.deactivate();
        unset_context();
      }
    });
  } else {
    run();
  }
}
function capture() {
  var previous_effect = active_effect;
  var previous_reaction = active_reaction;
  var previous_component_context = component_context;
  var previous_batch2 = current_batch;
  return function restore(activate_batch = true) {
    set_active_effect(previous_effect);
    set_active_reaction(previous_reaction);
    set_component_context(previous_component_context);
    if (activate_batch) previous_batch2?.activate();
  };
}
function unset_context() {
  set_active_effect(null);
  set_active_reaction(null);
  set_component_context(null);
}
// @__NO_SIDE_EFFECTS__
function derived(fn) {
  var flags2 = DERIVED | DIRTY;
  var parent_derived = active_reaction !== null && (active_reaction.f & DERIVED) !== 0 ? (
    /** @type {Derived} */
    active_reaction
  ) : null;
  if (active_effect !== null) {
    active_effect.f |= EFFECT_PRESERVED;
  }
  const signal = {
    ctx: component_context,
    deps: null,
    effects: null,
    equals,
    f: flags2,
    fn,
    reactions: null,
    rv: 0,
    v: (
      /** @type {V} */
      UNINITIALIZED
    ),
    wv: 0,
    parent: parent_derived ?? active_effect,
    ac: null
  };
  return signal;
}
// @__NO_SIDE_EFFECTS__
function async_derived(fn, location) {
  let parent = (
    /** @type {Effect | null} */
    active_effect
  );
  if (parent === null) {
    async_derived_orphan();
  }
  var boundary2 = (
    /** @type {Boundary} */
    parent.b
  );
  var promise = (
    /** @type {Promise<V>} */
    /** @type {unknown} */
    void 0
  );
  var signal = source(
    /** @type {V} */
    UNINITIALIZED
  );
  var should_suspend = !active_reaction;
  var deferreds = /* @__PURE__ */ new Map();
  async_effect(() => {
    var d = deferred();
    promise = d.promise;
    try {
      Promise.resolve(fn()).then(d.resolve, d.reject).then(() => {
        if (batch === current_batch && batch.committed) {
          batch.deactivate();
        }
        unset_context();
      });
    } catch (error) {
      d.reject(error);
      unset_context();
    }
    var batch = (
      /** @type {Batch} */
      current_batch
    );
    if (should_suspend) {
      var blocking = !boundary2.is_pending();
      boundary2.update_pending_count(1);
      batch.increment(blocking);
      deferreds.get(batch)?.reject(STALE_REACTION);
      deferreds.delete(batch);
      deferreds.set(batch, d);
    }
    const handler = (value, error = void 0) => {
      batch.activate();
      if (error) {
        if (error !== STALE_REACTION) {
          signal.f |= ERROR_VALUE;
          internal_set(signal, error);
        }
      } else {
        if ((signal.f & ERROR_VALUE) !== 0) {
          signal.f ^= ERROR_VALUE;
        }
        internal_set(signal, value);
        for (const [b, d2] of deferreds) {
          deferreds.delete(b);
          if (b === batch) break;
          d2.reject(STALE_REACTION);
        }
      }
      if (should_suspend) {
        boundary2.update_pending_count(-1);
        batch.decrement(blocking);
      }
    };
    d.promise.then(handler, (e) => handler(null, e || "unknown"));
  });
  teardown(() => {
    for (const d of deferreds.values()) {
      d.reject(STALE_REACTION);
    }
  });
  return new Promise((fulfil) => {
    function next(p) {
      function go() {
        if (p === promise) {
          fulfil(signal);
        } else {
          next(promise);
        }
      }
      p.then(go, go);
    }
    next(promise);
  });
}
// @__NO_SIDE_EFFECTS__
function user_derived(fn) {
  const d = /* @__PURE__ */ derived(fn);
  push_reaction_value(d);
  return d;
}
// @__NO_SIDE_EFFECTS__
function derived_safe_equal(fn) {
  const signal = /* @__PURE__ */ derived(fn);
  signal.equals = safe_equals;
  return signal;
}
function destroy_derived_effects(derived2) {
  var effects = derived2.effects;
  if (effects !== null) {
    derived2.effects = null;
    for (var i = 0; i < effects.length; i += 1) {
      destroy_effect(
        /** @type {Effect} */
        effects[i]
      );
    }
  }
}
function get_derived_parent_effect(derived2) {
  var parent = derived2.parent;
  while (parent !== null) {
    if ((parent.f & DERIVED) === 0) {
      return (parent.f & DESTROYED) === 0 ? (
        /** @type {Effect} */
        parent
      ) : null;
    }
    parent = parent.parent;
  }
  return null;
}
function execute_derived(derived2) {
  var value;
  var prev_active_effect = active_effect;
  set_active_effect(get_derived_parent_effect(derived2));
  {
    try {
      derived2.f &= ~WAS_MARKED;
      destroy_derived_effects(derived2);
      value = update_reaction(derived2);
    } finally {
      set_active_effect(prev_active_effect);
    }
  }
  return value;
}
function update_derived(derived2) {
  var value = execute_derived(derived2);
  if (!derived2.equals(value)) {
    if (!current_batch?.is_fork) {
      derived2.v = value;
    }
    derived2.wv = increment_write_version();
  }
  if (is_destroying_effect) {
    return;
  }
  if (batch_values !== null) {
    if (effect_tracking() || current_batch?.is_fork) {
      batch_values.set(derived2, value);
    }
  } else {
    var status = (derived2.f & CONNECTED) === 0 ? MAYBE_DIRTY : CLEAN;
    set_signal_status(derived2, status);
  }
}
let eager_effects = /* @__PURE__ */ new Set();
const old_values = /* @__PURE__ */ new Map();
let eager_effects_deferred = false;
function source(v, stack) {
  var signal = {
    f: 0,
    // TODO ideally we could skip this altogether, but it causes type errors
    v,
    reactions: null,
    equals,
    rv: 0,
    wv: 0
  };
  return signal;
}
// @__NO_SIDE_EFFECTS__
function state(v, stack) {
  const s = source(v);
  push_reaction_value(s);
  return s;
}
// @__NO_SIDE_EFFECTS__
function mutable_source(initial_value, immutable = false, trackable = true) {
  const s = source(initial_value);
  if (!immutable) {
    s.equals = safe_equals;
  }
  return s;
}
function set$1(source2, value, should_proxy = false) {
  if (active_reaction !== null && // since we are untracking the function inside `$inspect.with` we need to add this check
  // to ensure we error if state is set inside an inspect effect
  (!untracking || (active_reaction.f & EAGER_EFFECT) !== 0) && is_runes() && (active_reaction.f & (DERIVED | BLOCK_EFFECT | ASYNC | EAGER_EFFECT)) !== 0 && !current_sources?.includes(source2)) {
    state_unsafe_mutation();
  }
  let new_value = should_proxy ? proxy(value) : value;
  return internal_set(source2, new_value);
}
function internal_set(source2, value) {
  if (!source2.equals(value)) {
    var old_value = source2.v;
    if (is_destroying_effect) {
      old_values.set(source2, value);
    } else {
      old_values.set(source2, old_value);
    }
    source2.v = value;
    var batch = Batch.ensure();
    batch.capture(source2, old_value);
    if ((source2.f & DERIVED) !== 0) {
      if ((source2.f & DIRTY) !== 0) {
        execute_derived(
          /** @type {Derived} */
          source2
        );
      }
      set_signal_status(source2, (source2.f & CONNECTED) !== 0 ? CLEAN : MAYBE_DIRTY);
    }
    source2.wv = increment_write_version();
    mark_reactions(source2, DIRTY);
    if (active_effect !== null && (active_effect.f & CLEAN) !== 0 && (active_effect.f & (BRANCH_EFFECT | ROOT_EFFECT)) === 0) {
      if (untracked_writes === null) {
        set_untracked_writes([source2]);
      } else {
        untracked_writes.push(source2);
      }
    }
    if (!batch.is_fork && eager_effects.size > 0 && !eager_effects_deferred) {
      flush_eager_effects();
    }
  }
  return value;
}
function flush_eager_effects() {
  eager_effects_deferred = false;
  var prev_is_updating_effect = is_updating_effect;
  set_is_updating_effect(true);
  const inspects = Array.from(eager_effects);
  try {
    for (const effect2 of inspects) {
      if ((effect2.f & CLEAN) !== 0) {
        set_signal_status(effect2, MAYBE_DIRTY);
      }
      if (is_dirty(effect2)) {
        update_effect(effect2);
      }
    }
  } finally {
    set_is_updating_effect(prev_is_updating_effect);
  }
  eager_effects.clear();
}
function increment(source2) {
  set$1(source2, source2.v + 1);
}
function mark_reactions(signal, status) {
  var reactions = signal.reactions;
  if (reactions === null) return;
  var length2 = reactions.length;
  for (var i = 0; i < length2; i++) {
    var reaction = reactions[i];
    var flags2 = reaction.f;
    var not_dirty = (flags2 & DIRTY) === 0;
    if (not_dirty) {
      set_signal_status(reaction, status);
    }
    if ((flags2 & DERIVED) !== 0) {
      var derived2 = (
        /** @type {Derived} */
        reaction
      );
      batch_values?.delete(derived2);
      if ((flags2 & WAS_MARKED) === 0) {
        if (flags2 & CONNECTED) {
          reaction.f |= WAS_MARKED;
        }
        mark_reactions(derived2, MAYBE_DIRTY);
      }
    } else if (not_dirty) {
      if ((flags2 & BLOCK_EFFECT) !== 0 && eager_block_effects !== null) {
        eager_block_effects.add(
          /** @type {Effect} */
          reaction
        );
      }
      schedule_effect(
        /** @type {Effect} */
        reaction
      );
    }
  }
}
let listening_to_form_reset = false;
function add_form_reset_listener() {
  if (!listening_to_form_reset) {
    listening_to_form_reset = true;
    document.addEventListener(
      "reset",
      (evt) => {
        Promise.resolve().then(() => {
          if (!evt.defaultPrevented) {
            for (
              const e of
              /**@type {HTMLFormElement} */
              evt.target.elements
            ) {
              e.__on_r?.();
            }
          }
        });
      },
      // In the capture phase to guarantee we get noticed of it (no possibility of stopPropagation)
      { capture: true }
    );
  }
}
function without_reactive_context(fn) {
  var previous_reaction = active_reaction;
  var previous_effect = active_effect;
  set_active_reaction(null);
  set_active_effect(null);
  try {
    return fn();
  } finally {
    set_active_reaction(previous_reaction);
    set_active_effect(previous_effect);
  }
}
function listen_to_event_and_reset_event(element, event, handler, on_reset = handler) {
  element.addEventListener(event, () => without_reactive_context(handler));
  const prev = element.__on_r;
  if (prev) {
    element.__on_r = () => {
      prev();
      on_reset(true);
    };
  } else {
    element.__on_r = () => on_reset(true);
  }
  add_form_reset_listener();
}
let is_updating_effect = false;
function set_is_updating_effect(value) {
  is_updating_effect = value;
}
let is_destroying_effect = false;
function set_is_destroying_effect(value) {
  is_destroying_effect = value;
}
let active_reaction = null;
let untracking = false;
function set_active_reaction(reaction) {
  active_reaction = reaction;
}
let active_effect = null;
function set_active_effect(effect2) {
  active_effect = effect2;
}
let current_sources = null;
function push_reaction_value(value) {
  if (active_reaction !== null && true) {
    if (current_sources === null) {
      current_sources = [value];
    } else {
      current_sources.push(value);
    }
  }
}
let new_deps = null;
let skipped_deps = 0;
let untracked_writes = null;
function set_untracked_writes(value) {
  untracked_writes = value;
}
let write_version = 1;
let read_version = 0;
let update_version = read_version;
function set_update_version(value) {
  update_version = value;
}
function increment_write_version() {
  return ++write_version;
}
function is_dirty(reaction) {
  var flags2 = reaction.f;
  if ((flags2 & DIRTY) !== 0) {
    return true;
  }
  if (flags2 & DERIVED) {
    reaction.f &= ~WAS_MARKED;
  }
  if ((flags2 & MAYBE_DIRTY) !== 0) {
    var dependencies = reaction.deps;
    if (dependencies !== null) {
      var length2 = dependencies.length;
      for (var i = 0; i < length2; i++) {
        var dependency = dependencies[i];
        if (is_dirty(
          /** @type {Derived} */
          dependency
        )) {
          update_derived(
            /** @type {Derived} */
            dependency
          );
        }
        if (dependency.wv > reaction.wv) {
          return true;
        }
      }
    }
    if ((flags2 & CONNECTED) !== 0 && // During time traveling we don't want to reset the status so that
    // traversal of the graph in the other batches still happens
    batch_values === null) {
      set_signal_status(reaction, CLEAN);
    }
  }
  return false;
}
function schedule_possible_effect_self_invalidation(signal, effect2, root2 = true) {
  var reactions = signal.reactions;
  if (reactions === null) return;
  if (current_sources?.includes(signal)) {
    return;
  }
  for (var i = 0; i < reactions.length; i++) {
    var reaction = reactions[i];
    if ((reaction.f & DERIVED) !== 0) {
      schedule_possible_effect_self_invalidation(
        /** @type {Derived} */
        reaction,
        effect2,
        false
      );
    } else if (effect2 === reaction) {
      if (root2) {
        set_signal_status(reaction, DIRTY);
      } else if ((reaction.f & CLEAN) !== 0) {
        set_signal_status(reaction, MAYBE_DIRTY);
      }
      schedule_effect(
        /** @type {Effect} */
        reaction
      );
    }
  }
}
function update_reaction(reaction) {
  var previous_deps = new_deps;
  var previous_skipped_deps = skipped_deps;
  var previous_untracked_writes = untracked_writes;
  var previous_reaction = active_reaction;
  var previous_sources = current_sources;
  var previous_component_context = component_context;
  var previous_untracking = untracking;
  var previous_update_version = update_version;
  var flags2 = reaction.f;
  new_deps = /** @type {null | Value[]} */
  null;
  skipped_deps = 0;
  untracked_writes = null;
  active_reaction = (flags2 & (BRANCH_EFFECT | ROOT_EFFECT)) === 0 ? reaction : null;
  current_sources = null;
  set_component_context(reaction.ctx);
  untracking = false;
  update_version = ++read_version;
  if (reaction.ac !== null) {
    without_reactive_context(() => {
      reaction.ac.abort(STALE_REACTION);
    });
    reaction.ac = null;
  }
  try {
    reaction.f |= REACTION_IS_UPDATING;
    var fn = (
      /** @type {Function} */
      reaction.fn
    );
    var result = fn();
    var deps = reaction.deps;
    if (new_deps !== null) {
      var i;
      remove_reactions(reaction, skipped_deps);
      if (deps !== null && skipped_deps > 0) {
        deps.length = skipped_deps + new_deps.length;
        for (i = 0; i < new_deps.length; i++) {
          deps[skipped_deps + i] = new_deps[i];
        }
      } else {
        reaction.deps = deps = new_deps;
      }
      if (effect_tracking() && (reaction.f & CONNECTED) !== 0) {
        for (i = skipped_deps; i < deps.length; i++) {
          (deps[i].reactions ??= []).push(reaction);
        }
      }
    } else if (deps !== null && skipped_deps < deps.length) {
      remove_reactions(reaction, skipped_deps);
      deps.length = skipped_deps;
    }
    if (is_runes() && untracked_writes !== null && !untracking && deps !== null && (reaction.f & (DERIVED | MAYBE_DIRTY | DIRTY)) === 0) {
      for (i = 0; i < /** @type {Source[]} */
      untracked_writes.length; i++) {
        schedule_possible_effect_self_invalidation(
          untracked_writes[i],
          /** @type {Effect} */
          reaction
        );
      }
    }
    if (previous_reaction !== null && previous_reaction !== reaction) {
      read_version++;
      if (untracked_writes !== null) {
        if (previous_untracked_writes === null) {
          previous_untracked_writes = untracked_writes;
        } else {
          previous_untracked_writes.push(.../** @type {Source[]} */
          untracked_writes);
        }
      }
    }
    if ((reaction.f & ERROR_VALUE) !== 0) {
      reaction.f ^= ERROR_VALUE;
    }
    return result;
  } catch (error) {
    return handle_error(error);
  } finally {
    reaction.f ^= REACTION_IS_UPDATING;
    new_deps = previous_deps;
    skipped_deps = previous_skipped_deps;
    untracked_writes = previous_untracked_writes;
    active_reaction = previous_reaction;
    current_sources = previous_sources;
    set_component_context(previous_component_context);
    untracking = previous_untracking;
    update_version = previous_update_version;
  }
}
function remove_reaction(signal, dependency) {
  let reactions = dependency.reactions;
  if (reactions !== null) {
    var index2 = index_of.call(reactions, signal);
    if (index2 !== -1) {
      var new_length = reactions.length - 1;
      if (new_length === 0) {
        reactions = dependency.reactions = null;
      } else {
        reactions[index2] = reactions[new_length];
        reactions.pop();
      }
    }
  }
  if (reactions === null && (dependency.f & DERIVED) !== 0 && // Destroying a child effect while updating a parent effect can cause a dependency to appear
  // to be unused, when in fact it is used by the currently-updating parent. Checking `new_deps`
  // allows us to skip the expensive work of disconnecting and immediately reconnecting it
  (new_deps === null || !new_deps.includes(dependency))) {
    set_signal_status(dependency, MAYBE_DIRTY);
    if ((dependency.f & CONNECTED) !== 0) {
      dependency.f ^= CONNECTED;
      dependency.f &= ~WAS_MARKED;
    }
    destroy_derived_effects(
      /** @type {Derived} **/
      dependency
    );
    remove_reactions(
      /** @type {Derived} **/
      dependency,
      0
    );
  }
}
function remove_reactions(signal, start_index) {
  var dependencies = signal.deps;
  if (dependencies === null) return;
  for (var i = start_index; i < dependencies.length; i++) {
    remove_reaction(signal, dependencies[i]);
  }
}
function update_effect(effect2) {
  var flags2 = effect2.f;
  if ((flags2 & DESTROYED) !== 0) {
    return;
  }
  set_signal_status(effect2, CLEAN);
  var previous_effect = active_effect;
  var was_updating_effect = is_updating_effect;
  active_effect = effect2;
  is_updating_effect = true;
  try {
    if ((flags2 & (BLOCK_EFFECT | MANAGED_EFFECT)) !== 0) {
      destroy_block_effect_children(effect2);
    } else {
      destroy_effect_children(effect2);
    }
    execute_effect_teardown(effect2);
    var teardown2 = update_reaction(effect2);
    effect2.teardown = typeof teardown2 === "function" ? teardown2 : null;
    effect2.wv = write_version;
    var dep;
    if (DEV && tracing_mode_flag && (effect2.f & DIRTY) !== 0 && effect2.deps !== null) ;
  } finally {
    is_updating_effect = was_updating_effect;
    active_effect = previous_effect;
  }
}
function get$1(signal) {
  var flags2 = signal.f;
  var is_derived = (flags2 & DERIVED) !== 0;
  if (active_reaction !== null && !untracking) {
    var destroyed = active_effect !== null && (active_effect.f & DESTROYED) !== 0;
    if (!destroyed && !current_sources?.includes(signal)) {
      var deps = active_reaction.deps;
      if ((active_reaction.f & REACTION_IS_UPDATING) !== 0) {
        if (signal.rv < read_version) {
          signal.rv = read_version;
          if (new_deps === null && deps !== null && deps[skipped_deps] === signal) {
            skipped_deps++;
          } else if (new_deps === null) {
            new_deps = [signal];
          } else if (!new_deps.includes(signal)) {
            new_deps.push(signal);
          }
        }
      } else {
        (active_reaction.deps ??= []).push(signal);
        var reactions = signal.reactions;
        if (reactions === null) {
          signal.reactions = [active_reaction];
        } else if (!reactions.includes(active_reaction)) {
          reactions.push(active_reaction);
        }
      }
    }
  }
  if (is_destroying_effect) {
    if (old_values.has(signal)) {
      return old_values.get(signal);
    }
    if (is_derived) {
      var derived2 = (
        /** @type {Derived} */
        signal
      );
      var value = derived2.v;
      if ((derived2.f & CLEAN) === 0 && derived2.reactions !== null || depends_on_old_values(derived2)) {
        value = execute_derived(derived2);
      }
      old_values.set(derived2, value);
      return value;
    }
  } else if (is_derived && (!batch_values?.has(signal) || current_batch?.is_fork && !effect_tracking())) {
    derived2 = /** @type {Derived} */
    signal;
    if (is_dirty(derived2)) {
      update_derived(derived2);
    }
    if (is_updating_effect && effect_tracking() && (derived2.f & CONNECTED) === 0) {
      reconnect(derived2);
    }
  }
  if (batch_values?.has(signal)) {
    return batch_values.get(signal);
  }
  if ((signal.f & ERROR_VALUE) !== 0) {
    throw signal.v;
  }
  return signal.v;
}
function reconnect(derived2) {
  if (derived2.deps === null) return;
  derived2.f ^= CONNECTED;
  for (const dep of derived2.deps) {
    (dep.reactions ??= []).push(derived2);
    if ((dep.f & DERIVED) !== 0 && (dep.f & CONNECTED) === 0) {
      reconnect(
        /** @type {Derived} */
        dep
      );
    }
  }
}
function depends_on_old_values(derived2) {
  if (derived2.v === UNINITIALIZED) return true;
  if (derived2.deps === null) return false;
  for (const dep of derived2.deps) {
    if (old_values.has(dep)) {
      return true;
    }
    if ((dep.f & DERIVED) !== 0 && depends_on_old_values(
      /** @type {Derived} */
      dep
    )) {
      return true;
    }
  }
  return false;
}
function untrack(fn) {
  var previous_untracking = untracking;
  try {
    untracking = true;
    return fn();
  } finally {
    untracking = previous_untracking;
  }
}
const STATUS_MASK = -7169;
function set_signal_status(signal, status) {
  signal.f = signal.f & STATUS_MASK | status;
}
function validate_effect(rune) {
  if (active_effect === null) {
    if (active_reaction === null) {
      effect_orphan();
    }
    effect_in_unowned_derived();
  }
  if (is_destroying_effect) {
    effect_in_teardown();
  }
}
function push_effect(effect2, parent_effect) {
  var parent_last = parent_effect.last;
  if (parent_last === null) {
    parent_effect.last = parent_effect.first = effect2;
  } else {
    parent_last.next = effect2;
    effect2.prev = parent_last;
    parent_effect.last = effect2;
  }
}
function create_effect(type, fn, sync) {
  var parent = active_effect;
  if (parent !== null && (parent.f & INERT) !== 0) {
    type |= INERT;
  }
  var effect2 = {
    ctx: component_context,
    deps: null,
    nodes: null,
    f: type | DIRTY | CONNECTED,
    first: null,
    fn,
    last: null,
    next: null,
    parent,
    b: parent && parent.b,
    prev: null,
    teardown: null,
    wv: 0,
    ac: null
  };
  if (sync) {
    try {
      update_effect(effect2);
      effect2.f |= EFFECT_RAN;
    } catch (e2) {
      destroy_effect(effect2);
      throw e2;
    }
  } else if (fn !== null) {
    schedule_effect(effect2);
  }
  var e = effect2;
  if (sync && e.deps === null && e.teardown === null && e.nodes === null && e.first === e.last && // either `null`, or a singular child
  (e.f & EFFECT_PRESERVED) === 0) {
    e = e.first;
    if ((type & BLOCK_EFFECT) !== 0 && (type & EFFECT_TRANSPARENT) !== 0 && e !== null) {
      e.f |= EFFECT_TRANSPARENT;
    }
  }
  if (e !== null) {
    e.parent = parent;
    if (parent !== null) {
      push_effect(e, parent);
    }
    if (active_reaction !== null && (active_reaction.f & DERIVED) !== 0 && (type & ROOT_EFFECT) === 0) {
      var derived2 = (
        /** @type {Derived} */
        active_reaction
      );
      (derived2.effects ??= []).push(e);
    }
  }
  return effect2;
}
function effect_tracking() {
  return active_reaction !== null && !untracking;
}
function teardown(fn) {
  const effect2 = create_effect(RENDER_EFFECT, null, false);
  set_signal_status(effect2, CLEAN);
  effect2.teardown = fn;
  return effect2;
}
function user_effect(fn) {
  validate_effect();
  var flags2 = (
    /** @type {Effect} */
    active_effect.f
  );
  var defer = !active_reaction && (flags2 & BRANCH_EFFECT) !== 0 && (flags2 & EFFECT_RAN) === 0;
  if (defer) {
    var context = (
      /** @type {ComponentContext} */
      component_context
    );
    (context.e ??= []).push(fn);
  } else {
    return create_user_effect(fn);
  }
}
function create_user_effect(fn) {
  return create_effect(EFFECT | USER_EFFECT, fn, false);
}
function component_root(fn) {
  Batch.ensure();
  const effect2 = create_effect(ROOT_EFFECT | EFFECT_PRESERVED, fn, true);
  return (options = {}) => {
    return new Promise((fulfil) => {
      if (options.outro) {
        pause_effect(effect2, () => {
          destroy_effect(effect2);
          fulfil(void 0);
        });
      } else {
        destroy_effect(effect2);
        fulfil(void 0);
      }
    });
  };
}
function effect(fn) {
  return create_effect(EFFECT, fn, false);
}
function async_effect(fn) {
  return create_effect(ASYNC | EFFECT_PRESERVED, fn, true);
}
function render_effect(fn, flags2 = 0) {
  return create_effect(RENDER_EFFECT | flags2, fn, true);
}
function template_effect(fn, sync = [], async = [], blockers = []) {
  flatten(blockers, sync, async, (values2) => {
    create_effect(RENDER_EFFECT, () => fn(...values2.map(get$1)), true);
  });
}
function block(fn, flags2 = 0) {
  var effect2 = create_effect(BLOCK_EFFECT | flags2, fn, true);
  return effect2;
}
function branch(fn) {
  return create_effect(BRANCH_EFFECT | EFFECT_PRESERVED, fn, true);
}
function execute_effect_teardown(effect2) {
  var teardown2 = effect2.teardown;
  if (teardown2 !== null) {
    const previously_destroying_effect = is_destroying_effect;
    const previous_reaction = active_reaction;
    set_is_destroying_effect(true);
    set_active_reaction(null);
    try {
      teardown2.call(null);
    } finally {
      set_is_destroying_effect(previously_destroying_effect);
      set_active_reaction(previous_reaction);
    }
  }
}
function destroy_effect_children(signal, remove_dom = false) {
  var effect2 = signal.first;
  signal.first = signal.last = null;
  while (effect2 !== null) {
    const controller = effect2.ac;
    if (controller !== null) {
      without_reactive_context(() => {
        controller.abort(STALE_REACTION);
      });
    }
    var next = effect2.next;
    if ((effect2.f & ROOT_EFFECT) !== 0) {
      effect2.parent = null;
    } else {
      destroy_effect(effect2, remove_dom);
    }
    effect2 = next;
  }
}
function destroy_block_effect_children(signal) {
  var effect2 = signal.first;
  while (effect2 !== null) {
    var next = effect2.next;
    if ((effect2.f & BRANCH_EFFECT) === 0) {
      destroy_effect(effect2);
    }
    effect2 = next;
  }
}
function destroy_effect(effect2, remove_dom = true) {
  var removed = false;
  if ((remove_dom || (effect2.f & HEAD_EFFECT) !== 0) && effect2.nodes !== null && effect2.nodes.end !== null) {
    remove_effect_dom(
      effect2.nodes.start,
      /** @type {TemplateNode} */
      effect2.nodes.end
    );
    removed = true;
  }
  destroy_effect_children(effect2, remove_dom && !removed);
  remove_reactions(effect2, 0);
  set_signal_status(effect2, DESTROYED);
  var transitions = effect2.nodes && effect2.nodes.t;
  if (transitions !== null) {
    for (const transition2 of transitions) {
      transition2.stop();
    }
  }
  execute_effect_teardown(effect2);
  var parent = effect2.parent;
  if (parent !== null && parent.first !== null) {
    unlink_effect(effect2);
  }
  effect2.next = effect2.prev = effect2.teardown = effect2.ctx = effect2.deps = effect2.fn = effect2.nodes = effect2.ac = null;
}
function remove_effect_dom(node, end) {
  while (node !== null) {
    var next = node === end ? null : /* @__PURE__ */ get_next_sibling(node);
    node.remove();
    node = next;
  }
}
function unlink_effect(effect2) {
  var parent = effect2.parent;
  var prev = effect2.prev;
  var next = effect2.next;
  if (prev !== null) prev.next = next;
  if (next !== null) next.prev = prev;
  if (parent !== null) {
    if (parent.first === effect2) parent.first = next;
    if (parent.last === effect2) parent.last = prev;
  }
}
function pause_effect(effect2, callback, destroy = true) {
  var transitions = [];
  pause_children(effect2, transitions, true);
  var fn = () => {
    if (destroy) destroy_effect(effect2);
    if (callback) callback();
  };
  var remaining = transitions.length;
  if (remaining > 0) {
    var check = () => --remaining || fn();
    for (var transition2 of transitions) {
      transition2.out(check);
    }
  } else {
    fn();
  }
}
function pause_children(effect2, transitions, local) {
  if ((effect2.f & INERT) !== 0) return;
  effect2.f ^= INERT;
  var t = effect2.nodes && effect2.nodes.t;
  if (t !== null) {
    for (const transition2 of t) {
      if (transition2.is_global || local) {
        transitions.push(transition2);
      }
    }
  }
  var child2 = effect2.first;
  while (child2 !== null) {
    var sibling2 = child2.next;
    var transparent = (child2.f & EFFECT_TRANSPARENT) !== 0 || // If this is a branch effect without a block effect parent,
    // it means the parent block effect was pruned. In that case,
    // transparency information was transferred to the branch effect.
    (child2.f & BRANCH_EFFECT) !== 0 && (effect2.f & BLOCK_EFFECT) !== 0;
    pause_children(child2, transitions, transparent ? local : false);
    child2 = sibling2;
  }
}
function resume_effect(effect2) {
  resume_children(effect2, true);
}
function resume_children(effect2, local) {
  if ((effect2.f & INERT) === 0) return;
  effect2.f ^= INERT;
  if ((effect2.f & CLEAN) === 0) {
    set_signal_status(effect2, DIRTY);
    schedule_effect(effect2);
  }
  var child2 = effect2.first;
  while (child2 !== null) {
    var sibling2 = child2.next;
    var transparent = (child2.f & EFFECT_TRANSPARENT) !== 0 || (child2.f & BRANCH_EFFECT) !== 0;
    resume_children(child2, transparent ? local : false);
    child2 = sibling2;
  }
  var t = effect2.nodes && effect2.nodes.t;
  if (t !== null) {
    for (const transition2 of t) {
      if (transition2.is_global || local) {
        transition2.in();
      }
    }
  }
}
function move_effect(effect2, fragment) {
  if (!effect2.nodes) return;
  var node = effect2.nodes.start;
  var end = effect2.nodes.end;
  while (node !== null) {
    var next = node === end ? null : /* @__PURE__ */ get_next_sibling(node);
    fragment.append(node);
    node = next;
  }
}
const PASSIVE_EVENTS = ["touchstart", "touchmove"];
function is_passive_event(name) {
  return PASSIVE_EVENTS.includes(name);
}
const all_registered_events = /* @__PURE__ */ new Set();
const root_event_handles = /* @__PURE__ */ new Set();
function delegate(events) {
  for (var i = 0; i < events.length; i++) {
    all_registered_events.add(events[i]);
  }
  for (var fn of root_event_handles) {
    fn(events);
  }
}
let last_propagated_event = null;
function handle_event_propagation(event) {
  var handler_element = this;
  var owner_document = (
    /** @type {Node} */
    handler_element.ownerDocument
  );
  var event_name = event.type;
  var path = event.composedPath?.() || [];
  var current_target = (
    /** @type {null | Element} */
    path[0] || event.target
  );
  last_propagated_event = event;
  var path_idx = 0;
  var handled_at = last_propagated_event === event && event.__root;
  if (handled_at) {
    var at_idx = path.indexOf(handled_at);
    if (at_idx !== -1 && (handler_element === document || handler_element === /** @type {any} */
    window)) {
      event.__root = handler_element;
      return;
    }
    var handler_idx = path.indexOf(handler_element);
    if (handler_idx === -1) {
      return;
    }
    if (at_idx <= handler_idx) {
      path_idx = at_idx;
    }
  }
  current_target = /** @type {Element} */
  path[path_idx] || event.target;
  if (current_target === handler_element) return;
  define_property(event, "currentTarget", {
    configurable: true,
    get() {
      return current_target || owner_document;
    }
  });
  var previous_reaction = active_reaction;
  var previous_effect = active_effect;
  set_active_reaction(null);
  set_active_effect(null);
  try {
    var throw_error;
    var other_errors = [];
    while (current_target !== null) {
      var parent_element = current_target.assignedSlot || current_target.parentNode || /** @type {any} */
      current_target.host || null;
      try {
        var delegated = current_target["__" + event_name];
        if (delegated != null && (!/** @type {any} */
        current_target.disabled || // DOM could've been updated already by the time this is reached, so we check this as well
        // -> the target could not have been disabled because it emits the event in the first place
        event.target === current_target)) {
          delegated.call(current_target, event);
        }
      } catch (error) {
        if (throw_error) {
          other_errors.push(error);
        } else {
          throw_error = error;
        }
      }
      if (event.cancelBubble || parent_element === handler_element || parent_element === null) {
        break;
      }
      current_target = parent_element;
    }
    if (throw_error) {
      for (let error of other_errors) {
        queueMicrotask(() => {
          throw error;
        });
      }
      throw throw_error;
    }
  } finally {
    event.__root = handler_element;
    delete event.currentTarget;
    set_active_reaction(previous_reaction);
    set_active_effect(previous_effect);
  }
}
function create_fragment_from_html(html2) {
  var elem = document.createElement("template");
  elem.innerHTML = html2.replaceAll("<!>", "<!---->");
  return elem.content;
}
function assign_nodes(start, end) {
  var effect2 = (
    /** @type {Effect} */
    active_effect
  );
  if (effect2.nodes === null) {
    effect2.nodes = { start, end, a: null, t: null };
  }
}
// @__NO_SIDE_EFFECTS__
function from_html(content, flags2) {
  var is_fragment = (flags2 & TEMPLATE_FRAGMENT) !== 0;
  var use_import_node = (flags2 & TEMPLATE_USE_IMPORT_NODE) !== 0;
  var node;
  var has_start = !content.startsWith("<!>");
  return () => {
    if (node === void 0) {
      node = create_fragment_from_html(has_start ? content : "<!>" + content);
      if (!is_fragment) node = /** @type {TemplateNode} */
      /* @__PURE__ */ get_first_child(node);
    }
    var clone2 = (
      /** @type {TemplateNode} */
      use_import_node || is_firefox ? document.importNode(node, true) : node.cloneNode(true)
    );
    if (is_fragment) {
      var start = (
        /** @type {TemplateNode} */
        /* @__PURE__ */ get_first_child(clone2)
      );
      var end = (
        /** @type {TemplateNode} */
        clone2.lastChild
      );
      assign_nodes(start, end);
    } else {
      assign_nodes(clone2, clone2);
    }
    return clone2;
  };
}
function text$1(value = "") {
  {
    var t = create_text(value + "");
    assign_nodes(t, t);
    return t;
  }
}
function comment() {
  var frag = document.createDocumentFragment();
  var start = document.createComment("");
  var anchor = create_text();
  frag.append(start, anchor);
  assign_nodes(start, anchor);
  return frag;
}
function append(anchor, dom) {
  if (anchor === null) {
    return;
  }
  anchor.before(
    /** @type {Node} */
    dom
  );
}
let should_intro = true;
function set_text(text2, value) {
  var str = value == null ? "" : typeof value === "object" ? value + "" : value;
  if (str !== (text2.__t ??= text2.nodeValue)) {
    text2.__t = str;
    text2.nodeValue = str + "";
  }
}
function mount(component, options) {
  return _mount(component, options);
}
const document_listeners = /* @__PURE__ */ new Map();
function _mount(Component, { target, anchor, props = {}, events, context, intro = true }) {
  init_operations();
  var registered_events = /* @__PURE__ */ new Set();
  var event_handle = (events2) => {
    for (var i = 0; i < events2.length; i++) {
      var event_name = events2[i];
      if (registered_events.has(event_name)) continue;
      registered_events.add(event_name);
      var passive = is_passive_event(event_name);
      target.addEventListener(event_name, handle_event_propagation, { passive });
      var n = document_listeners.get(event_name);
      if (n === void 0) {
        document.addEventListener(event_name, handle_event_propagation, { passive });
        document_listeners.set(event_name, 1);
      } else {
        document_listeners.set(event_name, n + 1);
      }
    }
  };
  event_handle(array_from(all_registered_events));
  root_event_handles.add(event_handle);
  var component = void 0;
  var unmount2 = component_root(() => {
    var anchor_node = anchor ?? target.appendChild(create_text());
    boundary(
      /** @type {TemplateNode} */
      anchor_node,
      {
        pending: () => {
        }
      },
      (anchor_node2) => {
        if (context) {
          push({});
          var ctx = (
            /** @type {ComponentContext} */
            component_context
          );
          ctx.c = context;
        }
        if (events) {
          props.$$events = events;
        }
        should_intro = intro;
        component = Component(anchor_node2, props) || {};
        should_intro = true;
        if (context) {
          pop();
        }
      }
    );
    return () => {
      for (var event_name of registered_events) {
        target.removeEventListener(event_name, handle_event_propagation);
        var n = (
          /** @type {number} */
          document_listeners.get(event_name)
        );
        if (--n === 0) {
          document.removeEventListener(event_name, handle_event_propagation);
          document_listeners.delete(event_name);
        } else {
          document_listeners.set(event_name, n);
        }
      }
      root_event_handles.delete(event_handle);
      if (anchor_node !== anchor) {
        anchor_node.parentNode?.removeChild(anchor_node);
      }
    };
  });
  mounted_components.set(component, unmount2);
  return component;
}
let mounted_components = /* @__PURE__ */ new WeakMap();
function unmount(component, options) {
  const fn = mounted_components.get(component);
  if (fn) {
    mounted_components.delete(component);
    return fn(options);
  }
  return Promise.resolve();
}
class BranchManager {
  /** @type {TemplateNode} */
  anchor;
  /** @type {Map<Batch, Key>} */
  #batches = /* @__PURE__ */ new Map();
  /**
   * Map of keys to effects that are currently rendered in the DOM.
   * These effects are visible and actively part of the document tree.
   * Example:
   * ```
   * {#if condition}
   * 	foo
   * {:else}
   * 	bar
   * {/if}
   * ```
   * Can result in the entries `true->Effect` and `false->Effect`
   * @type {Map<Key, Effect>}
   */
  #onscreen = /* @__PURE__ */ new Map();
  /**
   * Similar to #onscreen with respect to the keys, but contains branches that are not yet
   * in the DOM, because their insertion is deferred.
   * @type {Map<Key, Branch>}
   */
  #offscreen = /* @__PURE__ */ new Map();
  /**
   * Keys of effects that are currently outroing
   * @type {Set<Key>}
   */
  #outroing = /* @__PURE__ */ new Set();
  /**
   * Whether to pause (i.e. outro) on change, or destroy immediately.
   * This is necessary for `<svelte:element>`
   */
  #transition = true;
  /**
   * @param {TemplateNode} anchor
   * @param {boolean} transition
   */
  constructor(anchor, transition2 = true) {
    this.anchor = anchor;
    this.#transition = transition2;
  }
  #commit = () => {
    var batch = (
      /** @type {Batch} */
      current_batch
    );
    if (!this.#batches.has(batch)) return;
    var key = (
      /** @type {Key} */
      this.#batches.get(batch)
    );
    var onscreen = this.#onscreen.get(key);
    if (onscreen) {
      resume_effect(onscreen);
      this.#outroing.delete(key);
    } else {
      var offscreen = this.#offscreen.get(key);
      if (offscreen) {
        this.#onscreen.set(key, offscreen.effect);
        this.#offscreen.delete(key);
        offscreen.fragment.lastChild.remove();
        this.anchor.before(offscreen.fragment);
        onscreen = offscreen.effect;
      }
    }
    for (const [b, k] of this.#batches) {
      this.#batches.delete(b);
      if (b === batch) {
        break;
      }
      const offscreen2 = this.#offscreen.get(k);
      if (offscreen2) {
        destroy_effect(offscreen2.effect);
        this.#offscreen.delete(k);
      }
    }
    for (const [k, effect2] of this.#onscreen) {
      if (k === key || this.#outroing.has(k)) continue;
      const on_destroy = () => {
        const keys2 = Array.from(this.#batches.values());
        if (keys2.includes(k)) {
          var fragment = document.createDocumentFragment();
          move_effect(effect2, fragment);
          fragment.append(create_text());
          this.#offscreen.set(k, { effect: effect2, fragment });
        } else {
          destroy_effect(effect2);
        }
        this.#outroing.delete(k);
        this.#onscreen.delete(k);
      };
      if (this.#transition || !onscreen) {
        this.#outroing.add(k);
        pause_effect(effect2, on_destroy, false);
      } else {
        on_destroy();
      }
    }
  };
  /**
   * @param {Batch} batch
   */
  #discard = (batch) => {
    this.#batches.delete(batch);
    const keys2 = Array.from(this.#batches.values());
    for (const [k, branch2] of this.#offscreen) {
      if (!keys2.includes(k)) {
        destroy_effect(branch2.effect);
        this.#offscreen.delete(k);
      }
    }
  };
  /**
   *
   * @param {any} key
   * @param {null | ((target: TemplateNode) => void)} fn
   */
  ensure(key, fn) {
    var batch = (
      /** @type {Batch} */
      current_batch
    );
    var defer = should_defer_append();
    if (fn && !this.#onscreen.has(key) && !this.#offscreen.has(key)) {
      if (defer) {
        var fragment = document.createDocumentFragment();
        var target = create_text();
        fragment.append(target);
        this.#offscreen.set(key, {
          effect: branch(() => fn(target)),
          fragment
        });
      } else {
        this.#onscreen.set(
          key,
          branch(() => fn(this.anchor))
        );
      }
    }
    this.#batches.set(batch, key);
    if (defer) {
      for (const [k, effect2] of this.#onscreen) {
        if (k === key) {
          batch.skipped_effects.delete(effect2);
        } else {
          batch.skipped_effects.add(effect2);
        }
      }
      for (const [k, branch2] of this.#offscreen) {
        if (k === key) {
          batch.skipped_effects.delete(branch2.effect);
        } else {
          batch.skipped_effects.add(branch2.effect);
        }
      }
      batch.oncommit(this.#commit);
      batch.ondiscard(this.#discard);
    } else {
      this.#commit();
    }
  }
}
function if_block(node, fn, elseif = false) {
  var branches = new BranchManager(node);
  var flags2 = elseif ? EFFECT_TRANSPARENT : 0;
  function update_branch(condition, fn2) {
    branches.ensure(condition, fn2);
  }
  block(() => {
    var has_branch = false;
    fn((fn2, flag = true) => {
      has_branch = true;
      update_branch(flag, fn2);
    });
    if (!has_branch) {
      update_branch(false, null);
    }
  }, flags2);
}
function index(_, i) {
  return i;
}
function pause_effects(state2, to_destroy, controlled_anchor) {
  var transitions = [];
  var length2 = to_destroy.length;
  var group;
  var remaining = to_destroy.length;
  for (var i = 0; i < length2; i++) {
    let effect2 = to_destroy[i];
    pause_effect(
      effect2,
      () => {
        if (group) {
          group.pending.delete(effect2);
          group.done.add(effect2);
          if (group.pending.size === 0) {
            var groups = (
              /** @type {Set<EachOutroGroup>} */
              state2.outrogroups
            );
            destroy_effects(array_from(group.done));
            groups.delete(group);
            if (groups.size === 0) {
              state2.outrogroups = null;
            }
          }
        } else {
          remaining -= 1;
        }
      },
      false
    );
  }
  if (remaining === 0) {
    var fast_path = transitions.length === 0 && controlled_anchor !== null;
    if (fast_path) {
      var anchor = (
        /** @type {Element} */
        controlled_anchor
      );
      var parent_node = (
        /** @type {Element} */
        anchor.parentNode
      );
      clear_text_content(parent_node);
      parent_node.append(anchor);
      state2.items.clear();
    }
    destroy_effects(to_destroy, !fast_path);
  } else {
    group = {
      pending: new Set(to_destroy),
      done: /* @__PURE__ */ new Set()
    };
    (state2.outrogroups ??= /* @__PURE__ */ new Set()).add(group);
  }
}
function destroy_effects(to_destroy, remove_dom = true) {
  for (var i = 0; i < to_destroy.length; i++) {
    destroy_effect(to_destroy[i], remove_dom);
  }
}
var offscreen_anchor;
function each(node, flags2, get_collection, get_key, render_fn, fallback_fn = null) {
  var anchor = node;
  var items = /* @__PURE__ */ new Map();
  var is_controlled = (flags2 & EACH_IS_CONTROLLED) !== 0;
  if (is_controlled) {
    var parent_node = (
      /** @type {Element} */
      node
    );
    anchor = parent_node.appendChild(create_text());
  }
  var fallback = null;
  var each_array = /* @__PURE__ */ derived_safe_equal(() => {
    var collection = get_collection();
    return is_array(collection) ? collection : collection == null ? [] : array_from(collection);
  });
  var array;
  var first_run = true;
  function commit() {
    state2.fallback = fallback;
    reconcile(state2, array, anchor, flags2, get_key);
    if (fallback !== null) {
      if (array.length === 0) {
        if ((fallback.f & EFFECT_OFFSCREEN) === 0) {
          resume_effect(fallback);
        } else {
          fallback.f ^= EFFECT_OFFSCREEN;
          move(fallback, null, anchor);
        }
      } else {
        pause_effect(fallback, () => {
          fallback = null;
        });
      }
    }
  }
  var effect2 = block(() => {
    array = /** @type {V[]} */
    get$1(each_array);
    var length2 = array.length;
    var keys2 = /* @__PURE__ */ new Set();
    var batch = (
      /** @type {Batch} */
      current_batch
    );
    var defer = should_defer_append();
    for (var index2 = 0; index2 < length2; index2 += 1) {
      var value = array[index2];
      var key = get_key(value, index2);
      var item = first_run ? null : items.get(key);
      if (item) {
        if (item.v) internal_set(item.v, value);
        if (item.i) internal_set(item.i, index2);
        if (defer) {
          batch.skipped_effects.delete(item.e);
        }
      } else {
        item = create_item(
          items,
          first_run ? anchor : offscreen_anchor ??= create_text(),
          value,
          key,
          index2,
          render_fn,
          flags2,
          get_collection
        );
        if (!first_run) {
          item.e.f |= EFFECT_OFFSCREEN;
        }
        items.set(key, item);
      }
      keys2.add(key);
    }
    if (length2 === 0 && fallback_fn && !fallback) {
      if (first_run) {
        fallback = branch(() => fallback_fn(anchor));
      } else {
        fallback = branch(() => fallback_fn(offscreen_anchor ??= create_text()));
        fallback.f |= EFFECT_OFFSCREEN;
      }
    }
    if (!first_run) {
      if (defer) {
        for (const [key2, item2] of items) {
          if (!keys2.has(key2)) {
            batch.skipped_effects.add(item2.e);
          }
        }
        batch.oncommit(commit);
        batch.ondiscard(() => {
        });
      } else {
        commit();
      }
    }
    get$1(each_array);
  });
  var state2 = { effect: effect2, items, outrogroups: null, fallback };
  first_run = false;
}
function reconcile(state2, array, anchor, flags2, get_key) {
  var is_animated = (flags2 & EACH_IS_ANIMATED) !== 0;
  var length2 = array.length;
  var items = state2.items;
  var current = state2.effect.first;
  var seen;
  var prev = null;
  var to_animate;
  var matched = [];
  var stashed = [];
  var value;
  var key;
  var effect2;
  var i;
  if (is_animated) {
    for (i = 0; i < length2; i += 1) {
      value = array[i];
      key = get_key(value, i);
      effect2 = /** @type {EachItem} */
      items.get(key).e;
      if ((effect2.f & EFFECT_OFFSCREEN) === 0) {
        effect2.nodes?.a?.measure();
        (to_animate ??= /* @__PURE__ */ new Set()).add(effect2);
      }
    }
  }
  for (i = 0; i < length2; i += 1) {
    value = array[i];
    key = get_key(value, i);
    effect2 = /** @type {EachItem} */
    items.get(key).e;
    if (state2.outrogroups !== null) {
      for (const group of state2.outrogroups) {
        group.pending.delete(effect2);
        group.done.delete(effect2);
      }
    }
    if ((effect2.f & EFFECT_OFFSCREEN) !== 0) {
      effect2.f ^= EFFECT_OFFSCREEN;
      if (effect2 === current) {
        move(effect2, null, anchor);
      } else {
        var next = prev ? prev.next : current;
        if (effect2 === state2.effect.last) {
          state2.effect.last = effect2.prev;
        }
        if (effect2.prev) effect2.prev.next = effect2.next;
        if (effect2.next) effect2.next.prev = effect2.prev;
        link$1(state2, prev, effect2);
        link$1(state2, effect2, next);
        move(effect2, next, anchor);
        prev = effect2;
        matched = [];
        stashed = [];
        current = prev.next;
        continue;
      }
    }
    if ((effect2.f & INERT) !== 0) {
      resume_effect(effect2);
      if (is_animated) {
        effect2.nodes?.a?.unfix();
        (to_animate ??= /* @__PURE__ */ new Set()).delete(effect2);
      }
    }
    if (effect2 !== current) {
      if (seen !== void 0 && seen.has(effect2)) {
        if (matched.length < stashed.length) {
          var start = stashed[0];
          var j;
          prev = start.prev;
          var a = matched[0];
          var b = matched[matched.length - 1];
          for (j = 0; j < matched.length; j += 1) {
            move(matched[j], start, anchor);
          }
          for (j = 0; j < stashed.length; j += 1) {
            seen.delete(stashed[j]);
          }
          link$1(state2, a.prev, b.next);
          link$1(state2, prev, a);
          link$1(state2, b, start);
          current = start;
          prev = b;
          i -= 1;
          matched = [];
          stashed = [];
        } else {
          seen.delete(effect2);
          move(effect2, current, anchor);
          link$1(state2, effect2.prev, effect2.next);
          link$1(state2, effect2, prev === null ? state2.effect.first : prev.next);
          link$1(state2, prev, effect2);
          prev = effect2;
        }
        continue;
      }
      matched = [];
      stashed = [];
      while (current !== null && current !== effect2) {
        (seen ??= /* @__PURE__ */ new Set()).add(current);
        stashed.push(current);
        current = current.next;
      }
      if (current === null) {
        continue;
      }
    }
    if ((effect2.f & EFFECT_OFFSCREEN) === 0) {
      matched.push(effect2);
    }
    prev = effect2;
    current = effect2.next;
  }
  if (state2.outrogroups !== null) {
    for (const group of state2.outrogroups) {
      if (group.pending.size === 0) {
        destroy_effects(array_from(group.done));
        state2.outrogroups?.delete(group);
      }
    }
    if (state2.outrogroups.size === 0) {
      state2.outrogroups = null;
    }
  }
  if (current !== null || seen !== void 0) {
    var to_destroy = [];
    if (seen !== void 0) {
      for (effect2 of seen) {
        if ((effect2.f & INERT) === 0) {
          to_destroy.push(effect2);
        }
      }
    }
    while (current !== null) {
      if ((current.f & INERT) === 0 && current !== state2.fallback) {
        to_destroy.push(current);
      }
      current = current.next;
    }
    var destroy_length = to_destroy.length;
    if (destroy_length > 0) {
      var controlled_anchor = (flags2 & EACH_IS_CONTROLLED) !== 0 && length2 === 0 ? anchor : null;
      if (is_animated) {
        for (i = 0; i < destroy_length; i += 1) {
          to_destroy[i].nodes?.a?.measure();
        }
        for (i = 0; i < destroy_length; i += 1) {
          to_destroy[i].nodes?.a?.fix();
        }
      }
      pause_effects(state2, to_destroy, controlled_anchor);
    }
  }
  if (is_animated) {
    queue_micro_task(() => {
      if (to_animate === void 0) return;
      for (effect2 of to_animate) {
        effect2.nodes?.a?.apply();
      }
    });
  }
}
function create_item(items, anchor, value, key, index2, render_fn, flags2, get_collection) {
  var v = (flags2 & EACH_ITEM_REACTIVE) !== 0 ? (flags2 & EACH_ITEM_IMMUTABLE) === 0 ? /* @__PURE__ */ mutable_source(value, false, false) : source(value) : null;
  var i = (flags2 & EACH_INDEX_REACTIVE) !== 0 ? source(index2) : null;
  return {
    v,
    i,
    e: branch(() => {
      render_fn(anchor, v ?? value, i ?? index2, get_collection);
      return () => {
        items.delete(key);
      };
    })
  };
}
function move(effect2, next, anchor) {
  if (!effect2.nodes) return;
  var node = effect2.nodes.start;
  var end = effect2.nodes.end;
  var dest = next && (next.f & EFFECT_OFFSCREEN) === 0 ? (
    /** @type {EffectNodes} */
    next.nodes.start
  ) : anchor;
  while (node !== null) {
    var next_node = (
      /** @type {TemplateNode} */
      /* @__PURE__ */ get_next_sibling(node)
    );
    dest.before(node);
    if (node === end) {
      return;
    }
    node = next_node;
  }
}
function link$1(state2, prev, next) {
  if (prev === null) {
    state2.effect.first = next;
  } else {
    prev.next = next;
  }
  if (next === null) {
    state2.effect.last = prev;
  } else {
    next.prev = prev;
  }
}
function html$2(node, get_value, svg2 = false, mathml = false, skip_warning = false) {
  var anchor = node;
  var value = "";
  template_effect(() => {
    var effect2 = (
      /** @type {Effect} */
      active_effect
    );
    if (value === (value = get_value() ?? "")) {
      return;
    }
    if (effect2.nodes !== null) {
      remove_effect_dom(
        effect2.nodes.start,
        /** @type {TemplateNode} */
        effect2.nodes.end
      );
      effect2.nodes = null;
    }
    if (value === "") return;
    var html2 = value + "";
    if (svg2) html2 = `<svg>${html2}</svg>`;
    else if (mathml) html2 = `<math>${html2}</math>`;
    var node2 = create_fragment_from_html(html2);
    if (svg2 || mathml) {
      node2 = /** @type {Element} */
      /* @__PURE__ */ get_first_child(node2);
    }
    assign_nodes(
      /** @type {TemplateNode} */
      /* @__PURE__ */ get_first_child(node2),
      /** @type {TemplateNode} */
      node2.lastChild
    );
    if (svg2 || mathml) {
      while (/* @__PURE__ */ get_first_child(node2)) {
        anchor.before(
          /** @type {TemplateNode} */
          /* @__PURE__ */ get_first_child(node2)
        );
      }
    } else {
      anchor.before(node2);
    }
  });
}
const now = () => performance.now();
const raf = {
  // don't access requestAnimationFrame eagerly outside method
  // this allows basic testing of user code without JSDOM
  // bunder will eval and remove ternary when the user's app is built
  tick: (
    /** @param {any} _ */
    (_) => requestAnimationFrame(_)
  ),
  now: () => now(),
  tasks: /* @__PURE__ */ new Set()
};
function run_tasks() {
  const now2 = raf.now();
  raf.tasks.forEach((task) => {
    if (!task.c(now2)) {
      raf.tasks.delete(task);
      task.f();
    }
  });
  if (raf.tasks.size !== 0) {
    raf.tick(run_tasks);
  }
}
function loop(callback) {
  let task;
  if (raf.tasks.size === 0) {
    raf.tick(run_tasks);
  }
  return {
    promise: new Promise((fulfill) => {
      raf.tasks.add(task = { c: callback, f: fulfill });
    }),
    abort() {
      raf.tasks.delete(task);
    }
  };
}
function dispatch_event(element, type) {
  without_reactive_context(() => {
    element.dispatchEvent(new CustomEvent(type));
  });
}
function css_property_to_camelcase(style) {
  if (style === "float") return "cssFloat";
  if (style === "offset") return "cssOffset";
  if (style.startsWith("--")) return style;
  const parts = style.split("-");
  if (parts.length === 1) return parts[0];
  return parts[0] + parts.slice(1).map(
    /** @param {any} word */
    (word) => word[0].toUpperCase() + word.slice(1)
  ).join("");
}
function css_to_keyframe(css) {
  const keyframe = {};
  const parts = css.split(";");
  for (const part of parts) {
    const [property2, value] = part.split(":");
    if (!property2 || value === void 0) break;
    const formatted_property = css_property_to_camelcase(property2.trim());
    keyframe[formatted_property] = value.trim();
  }
  return keyframe;
}
const linear = (t) => t;
function transition(flags2, element, get_fn, get_params) {
  var is_intro = (flags2 & TRANSITION_IN) !== 0;
  var is_outro = (flags2 & TRANSITION_OUT) !== 0;
  var is_both = is_intro && is_outro;
  var is_global = (flags2 & TRANSITION_GLOBAL) !== 0;
  var direction = is_both ? "both" : is_intro ? "in" : "out";
  var current_options;
  var inert = element.inert;
  var overflow = element.style.overflow;
  var intro;
  var outro;
  function get_options() {
    return without_reactive_context(() => {
      return current_options ??= get_fn()(element, get_params?.() ?? /** @type {P} */
      {}, {
        direction
      });
    });
  }
  var transition2 = {
    is_global,
    in() {
      element.inert = inert;
      if (!is_intro) {
        outro?.abort();
        outro?.reset?.();
        return;
      }
      if (!is_outro) {
        intro?.abort();
      }
      dispatch_event(element, "introstart");
      intro = animate(element, get_options(), outro, 1, () => {
        dispatch_event(element, "introend");
        intro?.abort();
        intro = current_options = void 0;
        element.style.overflow = overflow;
      });
    },
    out(fn) {
      if (!is_outro) {
        fn?.();
        current_options = void 0;
        return;
      }
      element.inert = true;
      dispatch_event(element, "outrostart");
      outro = animate(element, get_options(), intro, 0, () => {
        dispatch_event(element, "outroend");
        fn?.();
      });
    },
    stop: () => {
      intro?.abort();
      outro?.abort();
    }
  };
  var e = (
    /** @type {Effect & { nodes: EffectNodes }} */
    active_effect
  );
  (e.nodes.t ??= []).push(transition2);
  if (is_intro && should_intro) {
    var run = is_global;
    if (!run) {
      var block2 = (
        /** @type {Effect | null} */
        e.parent
      );
      while (block2 && (block2.f & EFFECT_TRANSPARENT) !== 0) {
        while (block2 = block2.parent) {
          if ((block2.f & BLOCK_EFFECT) !== 0) break;
        }
      }
      run = !block2 || (block2.f & EFFECT_RAN) !== 0;
    }
    if (run) {
      effect(() => {
        untrack(() => transition2.in());
      });
    }
  }
}
function animate(element, options, counterpart, t2, on_finish) {
  var is_intro = t2 === 1;
  if (is_function(options)) {
    var a;
    var aborted = false;
    queue_micro_task(() => {
      if (aborted) return;
      var o = options({ direction: is_intro ? "in" : "out" });
      a = animate(element, o, counterpart, t2, on_finish);
    });
    return {
      abort: () => {
        aborted = true;
        a?.abort();
      },
      deactivate: () => a.deactivate(),
      reset: () => a.reset(),
      t: () => a.t()
    };
  }
  counterpart?.deactivate();
  if (!options?.duration) {
    on_finish();
    return {
      abort: noop$1,
      deactivate: noop$1,
      reset: noop$1,
      t: () => t2
    };
  }
  const { delay = 0, css, tick, easing = linear } = options;
  var keyframes = [];
  if (is_intro && counterpart === void 0) {
    if (tick) {
      tick(0, 1);
    }
    if (css) {
      var styles2 = css_to_keyframe(css(0, 1));
      keyframes.push(styles2, styles2);
    }
  }
  var get_t = () => 1 - t2;
  var animation = element.animate(keyframes, { duration: delay, fill: "forwards" });
  animation.onfinish = () => {
    animation.cancel();
    var t1 = counterpart?.t() ?? 1 - t2;
    counterpart?.abort();
    var delta = t2 - t1;
    var duration = (
      /** @type {number} */
      options.duration * Math.abs(delta)
    );
    var keyframes2 = [];
    if (duration > 0) {
      var needs_overflow_hidden = false;
      if (css) {
        var n = Math.ceil(duration / (1e3 / 60));
        for (var i = 0; i <= n; i += 1) {
          var t = t1 + delta * easing(i / n);
          var styles3 = css_to_keyframe(css(t, 1 - t));
          keyframes2.push(styles3);
          needs_overflow_hidden ||= styles3.overflow === "hidden";
        }
      }
      if (needs_overflow_hidden) {
        element.style.overflow = "hidden";
      }
      get_t = () => {
        var time = (
          /** @type {number} */
          /** @type {globalThis.Animation} */
          animation.currentTime
        );
        return t1 + delta * easing(time / duration);
      };
      if (tick) {
        loop(() => {
          if (animation.playState !== "running") return false;
          var t3 = get_t();
          tick(t3, 1 - t3);
          return true;
        });
      }
    }
    animation = element.animate(keyframes2, { duration, fill: "forwards" });
    animation.onfinish = () => {
      get_t = () => t2;
      tick?.(t2, 1 - t2);
      on_finish();
    };
  };
  return {
    abort: () => {
      if (animation) {
        animation.cancel();
        animation.effect = null;
        animation.onfinish = noop$1;
      }
    },
    deactivate: () => {
      on_finish = noop$1;
    },
    reset: () => {
      if (t2 === 0) {
        tick?.(1, 0);
      }
    },
    t: () => get_t()
  };
}
function r(e) {
  var t, f, n = "";
  if ("string" == typeof e || "number" == typeof e) n += e;
  else if ("object" == typeof e) if (Array.isArray(e)) {
    var o = e.length;
    for (t = 0; t < o; t++) e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
  } else for (f in e) e[f] && (n && (n += " "), n += f);
  return n;
}
function clsx$1() {
  for (var e, t, f = 0, n = "", o = arguments.length; f < o; f++) (e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
  return n;
}
function clsx(value) {
  if (typeof value === "object") {
    return clsx$1(value);
  } else {
    return value ?? "";
  }
}
const whitespace = [..." 	\n\r\f \v\uFEFF"];
function to_class(value, hash, directives) {
  var classname = value == null ? "" : "" + value;
  if (hash) {
    classname = classname ? classname + " " + hash : hash;
  }
  if (directives) {
    for (var key in directives) {
      if (directives[key]) {
        classname = classname ? classname + " " + key : key;
      } else if (classname.length) {
        var len = key.length;
        var a = 0;
        while ((a = classname.indexOf(key, a)) >= 0) {
          var b = a + len;
          if ((a === 0 || whitespace.includes(classname[a - 1])) && (b === classname.length || whitespace.includes(classname[b]))) {
            classname = (a === 0 ? "" : classname.substring(0, a)) + classname.substring(b + 1);
          } else {
            a = b;
          }
        }
      }
    }
  }
  return classname === "" ? null : classname;
}
function set_class(dom, is_html, value, hash, prev_classes, next_classes) {
  var prev = dom.__className;
  if (prev !== value || prev === void 0) {
    var next_class_name = to_class(value, hash, next_classes);
    {
      if (next_class_name == null) {
        dom.removeAttribute("class");
      } else {
        dom.className = next_class_name;
      }
    }
    dom.__className = value;
  } else if (next_classes && prev_classes !== next_classes) {
    for (var key in next_classes) {
      var is_present = !!next_classes[key];
      if (prev_classes == null || is_present !== !!prev_classes[key]) {
        dom.classList.toggle(key, is_present);
      }
    }
  }
  return next_classes;
}
function select_option(select, value, mounting = false) {
  if (select.multiple) {
    if (value == void 0) {
      return;
    }
    if (!is_array(value)) {
      return select_multiple_invalid_value();
    }
    for (var option of select.options) {
      option.selected = value.includes(get_option_value(option));
    }
    return;
  }
  for (option of select.options) {
    var option_value = get_option_value(option);
    if (is(option_value, value)) {
      option.selected = true;
      return;
    }
  }
  if (!mounting || value !== void 0) {
    select.selectedIndex = -1;
  }
}
function init_select(select) {
  var observer = new MutationObserver(() => {
    select_option(select, select.__value);
  });
  observer.observe(select, {
    // Listen to option element changes
    childList: true,
    subtree: true,
    // because of <optgroup>
    // Listen to option element value attribute changes
    // (doesn't get notified of select value changes,
    // because that property is not reflected as an attribute)
    attributes: true,
    attributeFilter: ["value"]
  });
  teardown(() => {
    observer.disconnect();
  });
}
function bind_select_value(select, get2, set2 = get2) {
  var batches2 = /* @__PURE__ */ new WeakSet();
  var mounting = true;
  listen_to_event_and_reset_event(select, "change", (is_reset) => {
    var query = is_reset ? "[selected]" : ":checked";
    var value;
    if (select.multiple) {
      value = [].map.call(select.querySelectorAll(query), get_option_value);
    } else {
      var selected_option = select.querySelector(query) ?? // will fall back to first non-disabled option if no option is selected
      select.querySelector("option:not([disabled])");
      value = selected_option && get_option_value(selected_option);
    }
    set2(value);
    if (current_batch !== null) {
      batches2.add(current_batch);
    }
  });
  effect(() => {
    var value = get2();
    if (select === document.activeElement) {
      var batch = (
        /** @type {Batch} */
        previous_batch ?? current_batch
      );
      if (batches2.has(batch)) {
        return;
      }
    }
    select_option(select, value, mounting);
    if (mounting && value === void 0) {
      var selected_option = select.querySelector(":checked");
      if (selected_option !== null) {
        value = get_option_value(selected_option);
        set2(value);
      }
    }
    select.__value = value;
    mounting = false;
  });
  init_select(select);
}
function get_option_value(option) {
  if ("__value" in option) {
    return option.__value;
  } else {
    return option.value;
  }
}
const IS_CUSTOM_ELEMENT = Symbol("is custom element");
const IS_HTML = Symbol("is html");
function set_attribute(element, attribute, value, skip_warning) {
  var attributes = get_attributes(element);
  if (attributes[attribute] === (attributes[attribute] = value)) return;
  if (attribute === "loading") {
    element[LOADING_ATTR_SYMBOL] = value;
  }
  if (value == null) {
    element.removeAttribute(attribute);
  } else if (typeof value !== "string" && get_setters(element).includes(attribute)) {
    element[attribute] = value;
  } else {
    element.setAttribute(attribute, value);
  }
}
function get_attributes(element) {
  return (
    /** @type {Record<string | symbol, unknown>} **/
    // @ts-expect-error
    element.__attributes ??= {
      [IS_CUSTOM_ELEMENT]: element.nodeName.includes("-"),
      [IS_HTML]: element.namespaceURI === NAMESPACE_HTML
    }
  );
}
var setters_cache = /* @__PURE__ */ new Map();
function get_setters(element) {
  var cache_key = element.getAttribute("is") || element.nodeName;
  var setters = setters_cache.get(cache_key);
  if (setters) return setters;
  setters_cache.set(cache_key, setters = []);
  var descriptors;
  var proto = element;
  var element_proto = Element.prototype;
  while (element_proto !== proto) {
    descriptors = get_descriptors(proto);
    for (var key in descriptors) {
      if (descriptors[key].set) {
        setters.push(key);
      }
    }
    proto = get_prototype_of(proto);
  }
  return setters;
}
function bind_checked(input, get2, set2 = get2) {
  listen_to_event_and_reset_event(input, "change", (is_reset) => {
    var value = is_reset ? input.defaultChecked : input.checked;
    set2(value);
  });
  if (
    // If we are hydrating and the value has since changed,
    // then use the update value from the input instead.
    // If defaultChecked is set, then checked == defaultChecked
    untrack(get2) == null
  ) {
    set2(input.checked);
  }
  render_effect(() => {
    var value = get2();
    input.checked = Boolean(value);
  });
}
let IS_UNMOUNTED = Symbol();
function store_get(store, store_name, stores) {
  const entry = stores[store_name] ??= {
    store: null,
    source: /* @__PURE__ */ mutable_source(void 0),
    unsubscribe: noop$1
  };
  if (entry.store !== store && !(IS_UNMOUNTED in stores)) {
    entry.unsubscribe();
    entry.store = store ?? null;
    if (store == null) {
      entry.source.v = void 0;
      entry.unsubscribe = noop$1;
    } else {
      var is_synchronous_callback = true;
      entry.unsubscribe = subscribe_to_store(store, (v) => {
        if (is_synchronous_callback) {
          entry.source.v = v;
        } else {
          set$1(entry.source, v);
        }
      });
      is_synchronous_callback = false;
    }
  }
  if (store && IS_UNMOUNTED in stores) {
    return get(store);
  }
  return get$1(entry.source);
}
function setup_stores() {
  const stores = {};
  function cleanup() {
    teardown(() => {
      for (var store_name in stores) {
        const ref = stores[store_name];
        ref.unsubscribe();
      }
      define_property(stores, IS_UNMOUNTED, {
        enumerable: false,
        value: true
      });
    });
  }
  return [stores, cleanup];
}
function onMount(fn) {
  if (component_context === null) {
    lifecycle_outside_component();
  }
  {
    user_effect(() => {
      const cleanup = untrack(fn);
      if (typeof cleanup === "function") return (
        /** @type {() => void} */
        cleanup
      );
    });
  }
}
function subscribe_to_store(store, run, invalidate) {
  if (store == null) {
    run(void 0);
    return noop$1;
  }
  const unsub = untrack(
    () => store.subscribe(
      run,
      // @ts-expect-error
      invalidate
    )
  );
  return unsub.unsubscribe ? () => unsub.unsubscribe() : unsub;
}
const subscriber_queue = [];
function writable(value, start = noop$1) {
  let stop = null;
  const subscribers = /* @__PURE__ */ new Set();
  function set2(new_value) {
    if (safe_not_equal(value, new_value)) {
      value = new_value;
      if (stop) {
        const run_queue = !subscriber_queue.length;
        for (const subscriber of subscribers) {
          subscriber[1]();
          subscriber_queue.push(subscriber, value);
        }
        if (run_queue) {
          for (let i = 0; i < subscriber_queue.length; i += 2) {
            subscriber_queue[i][0](subscriber_queue[i + 1]);
          }
          subscriber_queue.length = 0;
        }
      }
    }
  }
  function update(fn) {
    set2(fn(
      /** @type {T} */
      value
    ));
  }
  function subscribe(run, invalidate = noop$1) {
    const subscriber = [run, invalidate];
    subscribers.add(subscriber);
    if (subscribers.size === 1) {
      stop = start(set2, update) || noop$1;
    }
    run(
      /** @type {T} */
      value
    );
    return () => {
      subscribers.delete(subscriber);
      if (subscribers.size === 0 && stop) {
        stop();
        stop = null;
      }
    };
  }
  return { set: set2, update, subscribe };
}
function get(store) {
  let value;
  subscribe_to_store(store, (_) => value = _)();
  return value;
}
const initialState = {
  controls: cloneDeep(systemControls),
  options: getDefaultOptions().controls,
  settings: getDefaultOptions().settings
};
const controlsStore = writable(initialState);
const PUBLIC_VERSION = "5";
if (typeof window !== "undefined") {
  ((window.__svelte ??= {}).v ??= /* @__PURE__ */ new Set()).add(PUBLIC_VERSION);
}
const caretDown = '<svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 64 64">\n    <path d="M7.474 17.988h49.045L31.996 46.012zm-3.85 3.37 24.522 28.019a5.113 5.113 0 0 0 7.7 0l24.523-28.02c2.894-3.308.542-8.482-3.85-8.482H7.474a5.113 5.113 0 0 0-3.85 8.482"/>\n</svg>\n';
const caretUp = '<svg xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 64 64">\n    <path d="M7.474 46.012h49.045L31.996 17.988Zm-3.85-3.37 24.522-28.019a5.113 5.113 0 0 1 7.7 0l24.523 28.02c2.894 3.308.542 8.482-3.85 8.482H7.474a5.113 5.113 0 0 1-3.85-8.482"/>\n</svg>\n';
var root_3 = /* @__PURE__ */ from_html(`<option> </option>`);
var root_2$1 = /* @__PURE__ */ from_html(`<label class="svelte-18zciyf"> </label> <select class="svelte-18zciyf"></select>`, 1);
var root_5 = /* @__PURE__ */ from_html(`<label class="svelte-18zciyf"><input type="checkbox" class="svelte-18zciyf"/> </label>`);
var root_1$4 = /* @__PURE__ */ from_html(`<div class="action-option svelte-18zciyf"><!></div>`);
function Action_option($$anchor, $$props) {
  push($$props, true);
  let selectedValue = /* @__PURE__ */ state("");
  onMount(() => {
    if ($$props.actionOption) {
      if ($$props.actionOption.type === "select") {
        set$1(selectedValue, $$props.actionOption.value.value, true);
      } else if ($$props.actionOption.type === "toggle") {
        set$1(selectedValue, $$props.actionOption.value, true);
      }
    }
  });
  const handleOptionChange = (event) => {
    if (!$$props.actionInstance || !$$props.actionOption) {
      log.error("Can't apply option value", $$props.actionInstance, $$props.actionOption);
      return;
    }
    if ($$props.actionOption?.type === "toggle") {
      const target = event.target;
      set$1(selectedValue, target.checked, true);
    } else if ($$props.actionOption?.type === "select") {
      const target = event.target;
      set$1(selectedValue, target.value, true);
    }
    $$props.actionInstance.applyOptionValue($$props.name, get$1(selectedValue));
  };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent_2 = ($$anchor2) => {
      var div = root_1$4();
      var node_1 = child(div);
      {
        var consequent = ($$anchor3) => {
          var fragment_1 = root_2$1();
          var label = first_child(fragment_1);
          var text2 = child(label);
          var select = sibling(label, 2);
          select.__change = handleOptionChange;
          each(select, 21, () => $$props.actionOption.choices, index, ($$anchor4, choiceItem) => {
            var option = root_3();
            var text_1 = child(option);
            var option_value = {};
            template_effect(() => {
              set_text(text_1, get$1(choiceItem).title);
              if (option_value !== (option_value = get$1(choiceItem).value)) {
                option.value = (option.__value = get$1(choiceItem).value) ?? "";
              }
            });
            append($$anchor4, option);
          });
          template_effect(() => {
            set_attribute(label, "for", $$props.name);
            set_text(text2, $$props.actionOption.label);
            set_attribute(select, "id", $$props.name);
          });
          bind_select_value(select, () => get$1(selectedValue), ($$value) => set$1(selectedValue, $$value));
          append($$anchor3, fragment_1);
        };
        var alternate = ($$anchor3) => {
          var fragment_2 = comment();
          var node_2 = first_child(fragment_2);
          {
            var consequent_1 = ($$anchor4) => {
              var label_1 = root_5();
              var input = child(label_1);
              input.__change = handleOptionChange;
              var text_2 = sibling(input);
              template_effect(
                ($0) => {
                  set_attribute(label_1, "for", $$props.name);
                  set_attribute(input, "id", $0);
                  set_text(text_2, ` ${$$props.actionOption.label ?? ""}`);
                },
                [() => String($$props.name)]
              );
              bind_checked(input, () => get$1(selectedValue), ($$value) => set$1(selectedValue, $$value));
              append($$anchor4, label_1);
            };
            if_block(
              node_2,
              ($$render) => {
                if ($$props.actionOption.type === "toggle" && typeof get$1(selectedValue) === "boolean") $$render(consequent_1);
              },
              true
            );
          }
          append($$anchor3, fragment_2);
        };
        if_block(node_1, ($$render) => {
          if ($$props.actionOption.type === "select") $$render(consequent);
          else $$render(alternate, false);
        });
      }
      append($$anchor2, div);
    };
    if_block(node, ($$render) => {
      if ($$props.actionOption) $$render(consequent_2);
    });
  }
  append($$anchor, fragment);
  pop();
}
delegate(["change"]);
var root_1$3 = /* @__PURE__ */ from_html(`<button type="submit" class="sub-action svelte-1xmw7n6"> </button>`);
function Sub_action($$anchor, $$props) {
  push($$props, true);
  const handleSubAction = (event) => {
    event.preventDefault();
    if (!$$props.actionInstance || !$$props.subAction) {
      log.error("Can't run a SubAction", $$props.actionInstance, $$props.subAction);
      return;
    }
    $$props.subAction.method();
  };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var button = root_1$3();
      button.__click = handleSubAction;
      var text2 = child(button);
      template_effect(() => {
        set_attribute(button, "id", `subaction-${$$props.name}`);
        set_attribute(button, "title", $$props.subAction.label);
        set_text(text2, $$props.subAction.label);
      });
      append($$anchor2, button);
    };
    if_block(node, ($$render) => {
      if ($$props.subAction) $$render(consequent);
    });
  }
  append($$anchor, fragment);
  pop();
}
delegate(["click"]);
var root_1$2 = /* @__PURE__ */ from_html(`<!> <!>`, 1);
function Control_menu($$anchor, $$props) {
  push($$props, true);
  const gm = getContext("gm");
  const actionType = /* @__PURE__ */ user_derived(() => $$props.control.type);
  const modeName = /* @__PURE__ */ user_derived(() => $$props.control.targetMode);
  const actionInstance = /* @__PURE__ */ user_derived(() => get$1(actionType) && get$1(modeName) ? gm.actionInstances[`${get$1(actionType)}__${get$1(modeName)}`] || null : null);
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent = ($$anchor2) => {
      var fragment_1 = root_1$2();
      var node_1 = first_child(fragment_1);
      each(node_1, 17, () => Object.entries(get$1(actionInstance).options), ([key, item]) => key, ($$anchor3, $$item) => {
        var $$array = /* @__PURE__ */ user_derived(() => to_array(get$1($$item), 2));
        let key = () => get$1($$array)[0];
        let item = () => get$1($$array)[1];
        Action_option($$anchor3, {
          get name() {
            return key();
          },
          get actionInstance() {
            return get$1(actionInstance);
          },
          get actionOption() {
            return item();
          }
        });
      });
      var node_2 = sibling(node_1, 2);
      each(node_2, 17, () => Object.entries(get$1(actionInstance).actions), ([key, item]) => key, ($$anchor3, $$item) => {
        var $$array_1 = /* @__PURE__ */ user_derived(() => to_array(get$1($$item), 2));
        let key = () => get$1($$array_1)[0];
        let item = () => get$1($$array_1)[1];
        Sub_action($$anchor3, {
          get name() {
            return key();
          },
          get actionInstance() {
            return get$1(actionInstance);
          },
          get subAction() {
            return item();
          }
        });
      });
      append($$anchor2, fragment_1);
    };
    if_block(node, ($$render) => {
      if (get$1(actionInstance)) $$render(consequent);
    });
  }
  append($$anchor, fragment);
  pop();
}
const {
  entries,
  setPrototypeOf,
  isFrozen,
  getPrototypeOf,
  getOwnPropertyDescriptor
} = Object;
let {
  freeze,
  seal,
  create
} = Object;
let {
  apply,
  construct
} = typeof Reflect !== "undefined" && Reflect;
if (!freeze) {
  freeze = function freeze2(x) {
    return x;
  };
}
if (!seal) {
  seal = function seal2(x) {
    return x;
  };
}
if (!apply) {
  apply = function apply2(func, thisArg) {
    for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
      args[_key - 2] = arguments[_key];
    }
    return func.apply(thisArg, args);
  };
}
if (!construct) {
  construct = function construct2(Func) {
    for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      args[_key2 - 1] = arguments[_key2];
    }
    return new Func(...args);
  };
}
const arrayForEach = unapply(Array.prototype.forEach);
const arrayLastIndexOf = unapply(Array.prototype.lastIndexOf);
const arrayPop = unapply(Array.prototype.pop);
const arrayPush = unapply(Array.prototype.push);
const arraySplice = unapply(Array.prototype.splice);
const stringToLowerCase = unapply(String.prototype.toLowerCase);
const stringToString = unapply(String.prototype.toString);
const stringMatch = unapply(String.prototype.match);
const stringReplace = unapply(String.prototype.replace);
const stringIndexOf = unapply(String.prototype.indexOf);
const stringTrim = unapply(String.prototype.trim);
const objectHasOwnProperty = unapply(Object.prototype.hasOwnProperty);
const regExpTest = unapply(RegExp.prototype.test);
const typeErrorCreate = unconstruct(TypeError);
function unapply(func) {
  return function(thisArg) {
    if (thisArg instanceof RegExp) {
      thisArg.lastIndex = 0;
    }
    for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
      args[_key3 - 1] = arguments[_key3];
    }
    return apply(func, thisArg, args);
  };
}
function unconstruct(Func) {
  return function() {
    for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
      args[_key4] = arguments[_key4];
    }
    return construct(Func, args);
  };
}
function addToSet(set2, array) {
  let transformCaseFunc = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : stringToLowerCase;
  if (setPrototypeOf) {
    setPrototypeOf(set2, null);
  }
  let l = array.length;
  while (l--) {
    let element = array[l];
    if (typeof element === "string") {
      const lcElement = transformCaseFunc(element);
      if (lcElement !== element) {
        if (!isFrozen(array)) {
          array[l] = lcElement;
        }
        element = lcElement;
      }
    }
    set2[element] = true;
  }
  return set2;
}
function cleanArray(array) {
  for (let index2 = 0; index2 < array.length; index2++) {
    const isPropertyExist = objectHasOwnProperty(array, index2);
    if (!isPropertyExist) {
      array[index2] = null;
    }
  }
  return array;
}
function clone$2(object) {
  const newObject = create(null);
  for (const [property2, value] of entries(object)) {
    const isPropertyExist = objectHasOwnProperty(object, property2);
    if (isPropertyExist) {
      if (Array.isArray(value)) {
        newObject[property2] = cleanArray(value);
      } else if (value && typeof value === "object" && value.constructor === Object) {
        newObject[property2] = clone$2(value);
      } else {
        newObject[property2] = value;
      }
    }
  }
  return newObject;
}
function lookupGetter(object, prop) {
  while (object !== null) {
    const desc = getOwnPropertyDescriptor(object, prop);
    if (desc) {
      if (desc.get) {
        return unapply(desc.get);
      }
      if (typeof desc.value === "function") {
        return unapply(desc.value);
      }
    }
    object = getPrototypeOf(object);
  }
  function fallbackValue() {
    return null;
  }
  return fallbackValue;
}
const html$1 = freeze(["a", "abbr", "acronym", "address", "area", "article", "aside", "audio", "b", "bdi", "bdo", "big", "blink", "blockquote", "body", "br", "button", "canvas", "caption", "center", "cite", "code", "col", "colgroup", "content", "data", "datalist", "dd", "decorator", "del", "details", "dfn", "dialog", "dir", "div", "dl", "dt", "element", "em", "fieldset", "figcaption", "figure", "font", "footer", "form", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hgroup", "hr", "html", "i", "img", "input", "ins", "kbd", "label", "legend", "li", "main", "map", "mark", "marquee", "menu", "menuitem", "meter", "nav", "nobr", "ol", "optgroup", "option", "output", "p", "picture", "pre", "progress", "q", "rp", "rt", "ruby", "s", "samp", "search", "section", "select", "shadow", "slot", "small", "source", "spacer", "span", "strike", "strong", "style", "sub", "summary", "sup", "table", "tbody", "td", "template", "textarea", "tfoot", "th", "thead", "time", "tr", "track", "tt", "u", "ul", "var", "video", "wbr"]);
const svg$1 = freeze(["svg", "a", "altglyph", "altglyphdef", "altglyphitem", "animatecolor", "animatemotion", "animatetransform", "circle", "clippath", "defs", "desc", "ellipse", "enterkeyhint", "exportparts", "filter", "font", "g", "glyph", "glyphref", "hkern", "image", "inputmode", "line", "lineargradient", "marker", "mask", "metadata", "mpath", "part", "path", "pattern", "polygon", "polyline", "radialgradient", "rect", "stop", "style", "switch", "symbol", "text", "textpath", "title", "tref", "tspan", "view", "vkern"]);
const svgFilters = freeze(["feBlend", "feColorMatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feDropShadow", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence"]);
const svgDisallowed = freeze(["animate", "color-profile", "cursor", "discard", "font-face", "font-face-format", "font-face-name", "font-face-src", "font-face-uri", "foreignobject", "hatch", "hatchpath", "mesh", "meshgradient", "meshpatch", "meshrow", "missing-glyph", "script", "set", "solidcolor", "unknown", "use"]);
const mathMl$1 = freeze(["math", "menclose", "merror", "mfenced", "mfrac", "mglyph", "mi", "mlabeledtr", "mmultiscripts", "mn", "mo", "mover", "mpadded", "mphantom", "mroot", "mrow", "ms", "mspace", "msqrt", "mstyle", "msub", "msup", "msubsup", "mtable", "mtd", "mtext", "mtr", "munder", "munderover", "mprescripts"]);
const mathMlDisallowed = freeze(["maction", "maligngroup", "malignmark", "mlongdiv", "mscarries", "mscarry", "msgroup", "mstack", "msline", "msrow", "semantics", "annotation", "annotation-xml", "mprescripts", "none"]);
const text = freeze(["#text"]);
const html = freeze(["accept", "action", "align", "alt", "autocapitalize", "autocomplete", "autopictureinpicture", "autoplay", "background", "bgcolor", "border", "capture", "cellpadding", "cellspacing", "checked", "cite", "class", "clear", "color", "cols", "colspan", "controls", "controlslist", "coords", "crossorigin", "datetime", "decoding", "default", "dir", "disabled", "disablepictureinpicture", "disableremoteplayback", "download", "draggable", "enctype", "enterkeyhint", "exportparts", "face", "for", "headers", "height", "hidden", "high", "href", "hreflang", "id", "inert", "inputmode", "integrity", "ismap", "kind", "label", "lang", "list", "loading", "loop", "low", "max", "maxlength", "media", "method", "min", "minlength", "multiple", "muted", "name", "nonce", "noshade", "novalidate", "nowrap", "open", "optimum", "part", "pattern", "placeholder", "playsinline", "popover", "popovertarget", "popovertargetaction", "poster", "preload", "pubdate", "radiogroup", "readonly", "rel", "required", "rev", "reversed", "role", "rows", "rowspan", "spellcheck", "scope", "selected", "shape", "size", "sizes", "slot", "span", "srclang", "start", "src", "srcset", "step", "style", "summary", "tabindex", "title", "translate", "type", "usemap", "valign", "value", "width", "wrap", "xmlns", "slot"]);
const svg = freeze(["accent-height", "accumulate", "additive", "alignment-baseline", "amplitude", "ascent", "attributename", "attributetype", "azimuth", "basefrequency", "baseline-shift", "begin", "bias", "by", "class", "clip", "clippathunits", "clip-path", "clip-rule", "color", "color-interpolation", "color-interpolation-filters", "color-profile", "color-rendering", "cx", "cy", "d", "dx", "dy", "diffuseconstant", "direction", "display", "divisor", "dur", "edgemode", "elevation", "end", "exponent", "fill", "fill-opacity", "fill-rule", "filter", "filterunits", "flood-color", "flood-opacity", "font-family", "font-size", "font-size-adjust", "font-stretch", "font-style", "font-variant", "font-weight", "fx", "fy", "g1", "g2", "glyph-name", "glyphref", "gradientunits", "gradienttransform", "height", "href", "id", "image-rendering", "in", "in2", "intercept", "k", "k1", "k2", "k3", "k4", "kerning", "keypoints", "keysplines", "keytimes", "lang", "lengthadjust", "letter-spacing", "kernelmatrix", "kernelunitlength", "lighting-color", "local", "marker-end", "marker-mid", "marker-start", "markerheight", "markerunits", "markerwidth", "maskcontentunits", "maskunits", "max", "mask", "mask-type", "media", "method", "mode", "min", "name", "numoctaves", "offset", "operator", "opacity", "order", "orient", "orientation", "origin", "overflow", "paint-order", "path", "pathlength", "patterncontentunits", "patterntransform", "patternunits", "points", "preservealpha", "preserveaspectratio", "primitiveunits", "r", "rx", "ry", "radius", "refx", "refy", "repeatcount", "repeatdur", "restart", "result", "rotate", "scale", "seed", "shape-rendering", "slope", "specularconstant", "specularexponent", "spreadmethod", "startoffset", "stddeviation", "stitchtiles", "stop-color", "stop-opacity", "stroke-dasharray", "stroke-dashoffset", "stroke-linecap", "stroke-linejoin", "stroke-miterlimit", "stroke-opacity", "stroke", "stroke-width", "style", "surfacescale", "systemlanguage", "tabindex", "tablevalues", "targetx", "targety", "transform", "transform-origin", "text-anchor", "text-decoration", "text-rendering", "textlength", "type", "u1", "u2", "unicode", "values", "viewbox", "visibility", "version", "vert-adv-y", "vert-origin-x", "vert-origin-y", "width", "word-spacing", "wrap", "writing-mode", "xchannelselector", "ychannelselector", "x", "x1", "x2", "xmlns", "y", "y1", "y2", "z", "zoomandpan"]);
const mathMl = freeze(["accent", "accentunder", "align", "bevelled", "close", "columnsalign", "columnlines", "columnspan", "denomalign", "depth", "dir", "display", "displaystyle", "encoding", "fence", "frame", "height", "href", "id", "largeop", "length", "linethickness", "lspace", "lquote", "mathbackground", "mathcolor", "mathsize", "mathvariant", "maxsize", "minsize", "movablelimits", "notation", "numalign", "open", "rowalign", "rowlines", "rowspacing", "rowspan", "rspace", "rquote", "scriptlevel", "scriptminsize", "scriptsizemultiplier", "selection", "separator", "separators", "stretchy", "subscriptshift", "supscriptshift", "symmetric", "voffset", "width", "xmlns"]);
const xml = freeze(["xlink:href", "xml:id", "xlink:title", "xml:space", "xmlns:xlink"]);
const MUSTACHE_EXPR = seal(/\{\{[\w\W]*|[\w\W]*\}\}/gm);
const ERB_EXPR = seal(/<%[\w\W]*|[\w\W]*%>/gm);
const TMPLIT_EXPR = seal(/\$\{[\w\W]*/gm);
const DATA_ATTR = seal(/^data-[\-\w.\u00B7-\uFFFF]+$/);
const ARIA_ATTR = seal(/^aria-[\-\w]+$/);
const IS_ALLOWED_URI = seal(
  /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|matrix):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i
  // eslint-disable-line no-useless-escape
);
const IS_SCRIPT_OR_DATA = seal(/^(?:\w+script|data):/i);
const ATTR_WHITESPACE = seal(
  /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g
  // eslint-disable-line no-control-regex
);
const DOCTYPE_NAME = seal(/^html$/i);
const CUSTOM_ELEMENT = seal(/^[a-z][.\w]*(-[.\w]+)+$/i);
var EXPRESSIONS = /* @__PURE__ */ Object.freeze({
  __proto__: null,
  ARIA_ATTR,
  ATTR_WHITESPACE,
  CUSTOM_ELEMENT,
  DATA_ATTR,
  DOCTYPE_NAME,
  ERB_EXPR,
  IS_ALLOWED_URI,
  IS_SCRIPT_OR_DATA,
  MUSTACHE_EXPR,
  TMPLIT_EXPR
});
const NODE_TYPE = {
  element: 1,
  text: 3,
  // Deprecated
  progressingInstruction: 7,
  comment: 8,
  document: 9
};
const getGlobal = function getGlobal2() {
  return typeof window === "undefined" ? null : window;
};
const _createTrustedTypesPolicy = function _createTrustedTypesPolicy2(trustedTypes, purifyHostElement) {
  if (typeof trustedTypes !== "object" || typeof trustedTypes.createPolicy !== "function") {
    return null;
  }
  let suffix = null;
  const ATTR_NAME = "data-tt-policy-suffix";
  if (purifyHostElement && purifyHostElement.hasAttribute(ATTR_NAME)) {
    suffix = purifyHostElement.getAttribute(ATTR_NAME);
  }
  const policyName = "dompurify" + (suffix ? "#" + suffix : "");
  try {
    return trustedTypes.createPolicy(policyName, {
      createHTML(html2) {
        return html2;
      },
      createScriptURL(scriptUrl) {
        return scriptUrl;
      }
    });
  } catch (_) {
    console.warn("TrustedTypes policy " + policyName + " could not be created.");
    return null;
  }
};
const _createHooksMap = function _createHooksMap2() {
  return {
    afterSanitizeAttributes: [],
    afterSanitizeElements: [],
    afterSanitizeShadowDOM: [],
    beforeSanitizeAttributes: [],
    beforeSanitizeElements: [],
    beforeSanitizeShadowDOM: [],
    uponSanitizeAttribute: [],
    uponSanitizeElement: [],
    uponSanitizeShadowNode: []
  };
};
function createDOMPurify() {
  let window2 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : getGlobal();
  const DOMPurify = (root2) => createDOMPurify(root2);
  DOMPurify.version = "3.3.1";
  DOMPurify.removed = [];
  if (!window2 || !window2.document || window2.document.nodeType !== NODE_TYPE.document || !window2.Element) {
    DOMPurify.isSupported = false;
    return DOMPurify;
  }
  let {
    document: document2
  } = window2;
  const originalDocument = document2;
  const currentScript = originalDocument.currentScript;
  const {
    DocumentFragment,
    HTMLTemplateElement,
    Node: Node2,
    Element: Element2,
    NodeFilter,
    NamedNodeMap = window2.NamedNodeMap || window2.MozNamedAttrMap,
    HTMLFormElement,
    DOMParser,
    trustedTypes
  } = window2;
  const ElementPrototype = Element2.prototype;
  const cloneNode = lookupGetter(ElementPrototype, "cloneNode");
  const remove2 = lookupGetter(ElementPrototype, "remove");
  const getNextSibling = lookupGetter(ElementPrototype, "nextSibling");
  const getChildNodes = lookupGetter(ElementPrototype, "childNodes");
  const getParentNode = lookupGetter(ElementPrototype, "parentNode");
  if (typeof HTMLTemplateElement === "function") {
    const template = document2.createElement("template");
    if (template.content && template.content.ownerDocument) {
      document2 = template.content.ownerDocument;
    }
  }
  let trustedTypesPolicy;
  let emptyHTML = "";
  const {
    implementation,
    createNodeIterator,
    createDocumentFragment,
    getElementsByTagName
  } = document2;
  const {
    importNode
  } = originalDocument;
  let hooks = _createHooksMap();
  DOMPurify.isSupported = typeof entries === "function" && typeof getParentNode === "function" && implementation && implementation.createHTMLDocument !== void 0;
  const {
    MUSTACHE_EXPR: MUSTACHE_EXPR2,
    ERB_EXPR: ERB_EXPR2,
    TMPLIT_EXPR: TMPLIT_EXPR2,
    DATA_ATTR: DATA_ATTR2,
    ARIA_ATTR: ARIA_ATTR2,
    IS_SCRIPT_OR_DATA: IS_SCRIPT_OR_DATA2,
    ATTR_WHITESPACE: ATTR_WHITESPACE2,
    CUSTOM_ELEMENT: CUSTOM_ELEMENT2
  } = EXPRESSIONS;
  let {
    IS_ALLOWED_URI: IS_ALLOWED_URI$1
  } = EXPRESSIONS;
  let ALLOWED_TAGS = null;
  const DEFAULT_ALLOWED_TAGS = addToSet({}, [...html$1, ...svg$1, ...svgFilters, ...mathMl$1, ...text]);
  let ALLOWED_ATTR = null;
  const DEFAULT_ALLOWED_ATTR = addToSet({}, [...html, ...svg, ...mathMl, ...xml]);
  let CUSTOM_ELEMENT_HANDLING = Object.seal(create(null, {
    tagNameCheck: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: null
    },
    attributeNameCheck: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: null
    },
    allowCustomizedBuiltInElements: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: false
    }
  }));
  let FORBID_TAGS = null;
  let FORBID_ATTR = null;
  const EXTRA_ELEMENT_HANDLING = Object.seal(create(null, {
    tagCheck: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: null
    },
    attributeCheck: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: null
    }
  }));
  let ALLOW_ARIA_ATTR = true;
  let ALLOW_DATA_ATTR = true;
  let ALLOW_UNKNOWN_PROTOCOLS = false;
  let ALLOW_SELF_CLOSE_IN_ATTR = true;
  let SAFE_FOR_TEMPLATES = false;
  let SAFE_FOR_XML = true;
  let WHOLE_DOCUMENT = false;
  let SET_CONFIG = false;
  let FORCE_BODY = false;
  let RETURN_DOM = false;
  let RETURN_DOM_FRAGMENT = false;
  let RETURN_TRUSTED_TYPE = false;
  let SANITIZE_DOM = true;
  let SANITIZE_NAMED_PROPS = false;
  const SANITIZE_NAMED_PROPS_PREFIX = "user-content-";
  let KEEP_CONTENT = true;
  let IN_PLACE = false;
  let USE_PROFILES = {};
  let FORBID_CONTENTS = null;
  const DEFAULT_FORBID_CONTENTS = addToSet({}, ["annotation-xml", "audio", "colgroup", "desc", "foreignobject", "head", "iframe", "math", "mi", "mn", "mo", "ms", "mtext", "noembed", "noframes", "noscript", "plaintext", "script", "style", "svg", "template", "thead", "title", "video", "xmp"]);
  let DATA_URI_TAGS = null;
  const DEFAULT_DATA_URI_TAGS = addToSet({}, ["audio", "video", "img", "source", "image", "track"]);
  let URI_SAFE_ATTRIBUTES = null;
  const DEFAULT_URI_SAFE_ATTRIBUTES = addToSet({}, ["alt", "class", "for", "id", "label", "name", "pattern", "placeholder", "role", "summary", "title", "value", "style", "xmlns"]);
  const MATHML_NAMESPACE = "http://www.w3.org/1998/Math/MathML";
  const SVG_NAMESPACE = "http://www.w3.org/2000/svg";
  const HTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
  let NAMESPACE = HTML_NAMESPACE;
  let IS_EMPTY_INPUT = false;
  let ALLOWED_NAMESPACES = null;
  const DEFAULT_ALLOWED_NAMESPACES = addToSet({}, [MATHML_NAMESPACE, SVG_NAMESPACE, HTML_NAMESPACE], stringToString);
  let MATHML_TEXT_INTEGRATION_POINTS = addToSet({}, ["mi", "mo", "mn", "ms", "mtext"]);
  let HTML_INTEGRATION_POINTS = addToSet({}, ["annotation-xml"]);
  const COMMON_SVG_AND_HTML_ELEMENTS = addToSet({}, ["title", "style", "font", "a", "script"]);
  let PARSER_MEDIA_TYPE = null;
  const SUPPORTED_PARSER_MEDIA_TYPES = ["application/xhtml+xml", "text/html"];
  const DEFAULT_PARSER_MEDIA_TYPE = "text/html";
  let transformCaseFunc = null;
  let CONFIG = null;
  const formElement = document2.createElement("form");
  const isRegexOrFunction = function isRegexOrFunction2(testValue) {
    return testValue instanceof RegExp || testValue instanceof Function;
  };
  const _parseConfig = function _parseConfig2() {
    let cfg = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    if (CONFIG && CONFIG === cfg) {
      return;
    }
    if (!cfg || typeof cfg !== "object") {
      cfg = {};
    }
    cfg = clone$2(cfg);
    PARSER_MEDIA_TYPE = // eslint-disable-next-line unicorn/prefer-includes
    SUPPORTED_PARSER_MEDIA_TYPES.indexOf(cfg.PARSER_MEDIA_TYPE) === -1 ? DEFAULT_PARSER_MEDIA_TYPE : cfg.PARSER_MEDIA_TYPE;
    transformCaseFunc = PARSER_MEDIA_TYPE === "application/xhtml+xml" ? stringToString : stringToLowerCase;
    ALLOWED_TAGS = objectHasOwnProperty(cfg, "ALLOWED_TAGS") ? addToSet({}, cfg.ALLOWED_TAGS, transformCaseFunc) : DEFAULT_ALLOWED_TAGS;
    ALLOWED_ATTR = objectHasOwnProperty(cfg, "ALLOWED_ATTR") ? addToSet({}, cfg.ALLOWED_ATTR, transformCaseFunc) : DEFAULT_ALLOWED_ATTR;
    ALLOWED_NAMESPACES = objectHasOwnProperty(cfg, "ALLOWED_NAMESPACES") ? addToSet({}, cfg.ALLOWED_NAMESPACES, stringToString) : DEFAULT_ALLOWED_NAMESPACES;
    URI_SAFE_ATTRIBUTES = objectHasOwnProperty(cfg, "ADD_URI_SAFE_ATTR") ? addToSet(clone$2(DEFAULT_URI_SAFE_ATTRIBUTES), cfg.ADD_URI_SAFE_ATTR, transformCaseFunc) : DEFAULT_URI_SAFE_ATTRIBUTES;
    DATA_URI_TAGS = objectHasOwnProperty(cfg, "ADD_DATA_URI_TAGS") ? addToSet(clone$2(DEFAULT_DATA_URI_TAGS), cfg.ADD_DATA_URI_TAGS, transformCaseFunc) : DEFAULT_DATA_URI_TAGS;
    FORBID_CONTENTS = objectHasOwnProperty(cfg, "FORBID_CONTENTS") ? addToSet({}, cfg.FORBID_CONTENTS, transformCaseFunc) : DEFAULT_FORBID_CONTENTS;
    FORBID_TAGS = objectHasOwnProperty(cfg, "FORBID_TAGS") ? addToSet({}, cfg.FORBID_TAGS, transformCaseFunc) : clone$2({});
    FORBID_ATTR = objectHasOwnProperty(cfg, "FORBID_ATTR") ? addToSet({}, cfg.FORBID_ATTR, transformCaseFunc) : clone$2({});
    USE_PROFILES = objectHasOwnProperty(cfg, "USE_PROFILES") ? cfg.USE_PROFILES : false;
    ALLOW_ARIA_ATTR = cfg.ALLOW_ARIA_ATTR !== false;
    ALLOW_DATA_ATTR = cfg.ALLOW_DATA_ATTR !== false;
    ALLOW_UNKNOWN_PROTOCOLS = cfg.ALLOW_UNKNOWN_PROTOCOLS || false;
    ALLOW_SELF_CLOSE_IN_ATTR = cfg.ALLOW_SELF_CLOSE_IN_ATTR !== false;
    SAFE_FOR_TEMPLATES = cfg.SAFE_FOR_TEMPLATES || false;
    SAFE_FOR_XML = cfg.SAFE_FOR_XML !== false;
    WHOLE_DOCUMENT = cfg.WHOLE_DOCUMENT || false;
    RETURN_DOM = cfg.RETURN_DOM || false;
    RETURN_DOM_FRAGMENT = cfg.RETURN_DOM_FRAGMENT || false;
    RETURN_TRUSTED_TYPE = cfg.RETURN_TRUSTED_TYPE || false;
    FORCE_BODY = cfg.FORCE_BODY || false;
    SANITIZE_DOM = cfg.SANITIZE_DOM !== false;
    SANITIZE_NAMED_PROPS = cfg.SANITIZE_NAMED_PROPS || false;
    KEEP_CONTENT = cfg.KEEP_CONTENT !== false;
    IN_PLACE = cfg.IN_PLACE || false;
    IS_ALLOWED_URI$1 = cfg.ALLOWED_URI_REGEXP || IS_ALLOWED_URI;
    NAMESPACE = cfg.NAMESPACE || HTML_NAMESPACE;
    MATHML_TEXT_INTEGRATION_POINTS = cfg.MATHML_TEXT_INTEGRATION_POINTS || MATHML_TEXT_INTEGRATION_POINTS;
    HTML_INTEGRATION_POINTS = cfg.HTML_INTEGRATION_POINTS || HTML_INTEGRATION_POINTS;
    CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || {};
    if (cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck)) {
      CUSTOM_ELEMENT_HANDLING.tagNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck;
    }
    if (cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck)) {
      CUSTOM_ELEMENT_HANDLING.attributeNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck;
    }
    if (cfg.CUSTOM_ELEMENT_HANDLING && typeof cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements === "boolean") {
      CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements = cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements;
    }
    if (SAFE_FOR_TEMPLATES) {
      ALLOW_DATA_ATTR = false;
    }
    if (RETURN_DOM_FRAGMENT) {
      RETURN_DOM = true;
    }
    if (USE_PROFILES) {
      ALLOWED_TAGS = addToSet({}, text);
      ALLOWED_ATTR = [];
      if (USE_PROFILES.html === true) {
        addToSet(ALLOWED_TAGS, html$1);
        addToSet(ALLOWED_ATTR, html);
      }
      if (USE_PROFILES.svg === true) {
        addToSet(ALLOWED_TAGS, svg$1);
        addToSet(ALLOWED_ATTR, svg);
        addToSet(ALLOWED_ATTR, xml);
      }
      if (USE_PROFILES.svgFilters === true) {
        addToSet(ALLOWED_TAGS, svgFilters);
        addToSet(ALLOWED_ATTR, svg);
        addToSet(ALLOWED_ATTR, xml);
      }
      if (USE_PROFILES.mathMl === true) {
        addToSet(ALLOWED_TAGS, mathMl$1);
        addToSet(ALLOWED_ATTR, mathMl);
        addToSet(ALLOWED_ATTR, xml);
      }
    }
    if (cfg.ADD_TAGS) {
      if (typeof cfg.ADD_TAGS === "function") {
        EXTRA_ELEMENT_HANDLING.tagCheck = cfg.ADD_TAGS;
      } else {
        if (ALLOWED_TAGS === DEFAULT_ALLOWED_TAGS) {
          ALLOWED_TAGS = clone$2(ALLOWED_TAGS);
        }
        addToSet(ALLOWED_TAGS, cfg.ADD_TAGS, transformCaseFunc);
      }
    }
    if (cfg.ADD_ATTR) {
      if (typeof cfg.ADD_ATTR === "function") {
        EXTRA_ELEMENT_HANDLING.attributeCheck = cfg.ADD_ATTR;
      } else {
        if (ALLOWED_ATTR === DEFAULT_ALLOWED_ATTR) {
          ALLOWED_ATTR = clone$2(ALLOWED_ATTR);
        }
        addToSet(ALLOWED_ATTR, cfg.ADD_ATTR, transformCaseFunc);
      }
    }
    if (cfg.ADD_URI_SAFE_ATTR) {
      addToSet(URI_SAFE_ATTRIBUTES, cfg.ADD_URI_SAFE_ATTR, transformCaseFunc);
    }
    if (cfg.FORBID_CONTENTS) {
      if (FORBID_CONTENTS === DEFAULT_FORBID_CONTENTS) {
        FORBID_CONTENTS = clone$2(FORBID_CONTENTS);
      }
      addToSet(FORBID_CONTENTS, cfg.FORBID_CONTENTS, transformCaseFunc);
    }
    if (cfg.ADD_FORBID_CONTENTS) {
      if (FORBID_CONTENTS === DEFAULT_FORBID_CONTENTS) {
        FORBID_CONTENTS = clone$2(FORBID_CONTENTS);
      }
      addToSet(FORBID_CONTENTS, cfg.ADD_FORBID_CONTENTS, transformCaseFunc);
    }
    if (KEEP_CONTENT) {
      ALLOWED_TAGS["#text"] = true;
    }
    if (WHOLE_DOCUMENT) {
      addToSet(ALLOWED_TAGS, ["html", "head", "body"]);
    }
    if (ALLOWED_TAGS.table) {
      addToSet(ALLOWED_TAGS, ["tbody"]);
      delete FORBID_TAGS.tbody;
    }
    if (cfg.TRUSTED_TYPES_POLICY) {
      if (typeof cfg.TRUSTED_TYPES_POLICY.createHTML !== "function") {
        throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createHTML" hook.');
      }
      if (typeof cfg.TRUSTED_TYPES_POLICY.createScriptURL !== "function") {
        throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createScriptURL" hook.');
      }
      trustedTypesPolicy = cfg.TRUSTED_TYPES_POLICY;
      emptyHTML = trustedTypesPolicy.createHTML("");
    } else {
      if (trustedTypesPolicy === void 0) {
        trustedTypesPolicy = _createTrustedTypesPolicy(trustedTypes, currentScript);
      }
      if (trustedTypesPolicy !== null && typeof emptyHTML === "string") {
        emptyHTML = trustedTypesPolicy.createHTML("");
      }
    }
    if (freeze) {
      freeze(cfg);
    }
    CONFIG = cfg;
  };
  const ALL_SVG_TAGS = addToSet({}, [...svg$1, ...svgFilters, ...svgDisallowed]);
  const ALL_MATHML_TAGS = addToSet({}, [...mathMl$1, ...mathMlDisallowed]);
  const _checkValidNamespace = function _checkValidNamespace2(element) {
    let parent = getParentNode(element);
    if (!parent || !parent.tagName) {
      parent = {
        namespaceURI: NAMESPACE,
        tagName: "template"
      };
    }
    const tagName = stringToLowerCase(element.tagName);
    const parentTagName = stringToLowerCase(parent.tagName);
    if (!ALLOWED_NAMESPACES[element.namespaceURI]) {
      return false;
    }
    if (element.namespaceURI === SVG_NAMESPACE) {
      if (parent.namespaceURI === HTML_NAMESPACE) {
        return tagName === "svg";
      }
      if (parent.namespaceURI === MATHML_NAMESPACE) {
        return tagName === "svg" && (parentTagName === "annotation-xml" || MATHML_TEXT_INTEGRATION_POINTS[parentTagName]);
      }
      return Boolean(ALL_SVG_TAGS[tagName]);
    }
    if (element.namespaceURI === MATHML_NAMESPACE) {
      if (parent.namespaceURI === HTML_NAMESPACE) {
        return tagName === "math";
      }
      if (parent.namespaceURI === SVG_NAMESPACE) {
        return tagName === "math" && HTML_INTEGRATION_POINTS[parentTagName];
      }
      return Boolean(ALL_MATHML_TAGS[tagName]);
    }
    if (element.namespaceURI === HTML_NAMESPACE) {
      if (parent.namespaceURI === SVG_NAMESPACE && !HTML_INTEGRATION_POINTS[parentTagName]) {
        return false;
      }
      if (parent.namespaceURI === MATHML_NAMESPACE && !MATHML_TEXT_INTEGRATION_POINTS[parentTagName]) {
        return false;
      }
      return !ALL_MATHML_TAGS[tagName] && (COMMON_SVG_AND_HTML_ELEMENTS[tagName] || !ALL_SVG_TAGS[tagName]);
    }
    if (PARSER_MEDIA_TYPE === "application/xhtml+xml" && ALLOWED_NAMESPACES[element.namespaceURI]) {
      return true;
    }
    return false;
  };
  const _forceRemove = function _forceRemove2(node) {
    arrayPush(DOMPurify.removed, {
      element: node
    });
    try {
      getParentNode(node).removeChild(node);
    } catch (_) {
      remove2(node);
    }
  };
  const _removeAttribute = function _removeAttribute2(name, element) {
    try {
      arrayPush(DOMPurify.removed, {
        attribute: element.getAttributeNode(name),
        from: element
      });
    } catch (_) {
      arrayPush(DOMPurify.removed, {
        attribute: null,
        from: element
      });
    }
    element.removeAttribute(name);
    if (name === "is") {
      if (RETURN_DOM || RETURN_DOM_FRAGMENT) {
        try {
          _forceRemove(element);
        } catch (_) {
        }
      } else {
        try {
          element.setAttribute(name, "");
        } catch (_) {
        }
      }
    }
  };
  const _initDocument = function _initDocument2(dirty) {
    let doc = null;
    let leadingWhitespace = null;
    if (FORCE_BODY) {
      dirty = "<remove></remove>" + dirty;
    } else {
      const matches = stringMatch(dirty, /^[\r\n\t ]+/);
      leadingWhitespace = matches && matches[0];
    }
    if (PARSER_MEDIA_TYPE === "application/xhtml+xml" && NAMESPACE === HTML_NAMESPACE) {
      dirty = '<html xmlns="http://www.w3.org/1999/xhtml"><head></head><body>' + dirty + "</body></html>";
    }
    const dirtyPayload = trustedTypesPolicy ? trustedTypesPolicy.createHTML(dirty) : dirty;
    if (NAMESPACE === HTML_NAMESPACE) {
      try {
        doc = new DOMParser().parseFromString(dirtyPayload, PARSER_MEDIA_TYPE);
      } catch (_) {
      }
    }
    if (!doc || !doc.documentElement) {
      doc = implementation.createDocument(NAMESPACE, "template", null);
      try {
        doc.documentElement.innerHTML = IS_EMPTY_INPUT ? emptyHTML : dirtyPayload;
      } catch (_) {
      }
    }
    const body = doc.body || doc.documentElement;
    if (dirty && leadingWhitespace) {
      body.insertBefore(document2.createTextNode(leadingWhitespace), body.childNodes[0] || null);
    }
    if (NAMESPACE === HTML_NAMESPACE) {
      return getElementsByTagName.call(doc, WHOLE_DOCUMENT ? "html" : "body")[0];
    }
    return WHOLE_DOCUMENT ? doc.documentElement : body;
  };
  const _createNodeIterator = function _createNodeIterator2(root2) {
    return createNodeIterator.call(
      root2.ownerDocument || root2,
      root2,
      // eslint-disable-next-line no-bitwise
      NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_COMMENT | NodeFilter.SHOW_TEXT | NodeFilter.SHOW_PROCESSING_INSTRUCTION | NodeFilter.SHOW_CDATA_SECTION,
      null
    );
  };
  const _isClobbered = function _isClobbered2(element) {
    return element instanceof HTMLFormElement && (typeof element.nodeName !== "string" || typeof element.textContent !== "string" || typeof element.removeChild !== "function" || !(element.attributes instanceof NamedNodeMap) || typeof element.removeAttribute !== "function" || typeof element.setAttribute !== "function" || typeof element.namespaceURI !== "string" || typeof element.insertBefore !== "function" || typeof element.hasChildNodes !== "function");
  };
  const _isNode = function _isNode2(value) {
    return typeof Node2 === "function" && value instanceof Node2;
  };
  function _executeHooks(hooks2, currentNode, data) {
    arrayForEach(hooks2, (hook) => {
      hook.call(DOMPurify, currentNode, data, CONFIG);
    });
  }
  const _sanitizeElements = function _sanitizeElements2(currentNode) {
    let content = null;
    _executeHooks(hooks.beforeSanitizeElements, currentNode, null);
    if (_isClobbered(currentNode)) {
      _forceRemove(currentNode);
      return true;
    }
    const tagName = transformCaseFunc(currentNode.nodeName);
    _executeHooks(hooks.uponSanitizeElement, currentNode, {
      tagName,
      allowedTags: ALLOWED_TAGS
    });
    if (SAFE_FOR_XML && currentNode.hasChildNodes() && !_isNode(currentNode.firstElementChild) && regExpTest(/<[/\w!]/g, currentNode.innerHTML) && regExpTest(/<[/\w!]/g, currentNode.textContent)) {
      _forceRemove(currentNode);
      return true;
    }
    if (currentNode.nodeType === NODE_TYPE.progressingInstruction) {
      _forceRemove(currentNode);
      return true;
    }
    if (SAFE_FOR_XML && currentNode.nodeType === NODE_TYPE.comment && regExpTest(/<[/\w]/g, currentNode.data)) {
      _forceRemove(currentNode);
      return true;
    }
    if (!(EXTRA_ELEMENT_HANDLING.tagCheck instanceof Function && EXTRA_ELEMENT_HANDLING.tagCheck(tagName)) && (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName])) {
      if (!FORBID_TAGS[tagName] && _isBasicCustomElement(tagName)) {
        if (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, tagName)) {
          return false;
        }
        if (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(tagName)) {
          return false;
        }
      }
      if (KEEP_CONTENT && !FORBID_CONTENTS[tagName]) {
        const parentNode = getParentNode(currentNode) || currentNode.parentNode;
        const childNodes = getChildNodes(currentNode) || currentNode.childNodes;
        if (childNodes && parentNode) {
          const childCount = childNodes.length;
          for (let i = childCount - 1; i >= 0; --i) {
            const childClone = cloneNode(childNodes[i], true);
            childClone.__removalCount = (currentNode.__removalCount || 0) + 1;
            parentNode.insertBefore(childClone, getNextSibling(currentNode));
          }
        }
      }
      _forceRemove(currentNode);
      return true;
    }
    if (currentNode instanceof Element2 && !_checkValidNamespace(currentNode)) {
      _forceRemove(currentNode);
      return true;
    }
    if ((tagName === "noscript" || tagName === "noembed" || tagName === "noframes") && regExpTest(/<\/no(script|embed|frames)/i, currentNode.innerHTML)) {
      _forceRemove(currentNode);
      return true;
    }
    if (SAFE_FOR_TEMPLATES && currentNode.nodeType === NODE_TYPE.text) {
      content = currentNode.textContent;
      arrayForEach([MUSTACHE_EXPR2, ERB_EXPR2, TMPLIT_EXPR2], (expr) => {
        content = stringReplace(content, expr, " ");
      });
      if (currentNode.textContent !== content) {
        arrayPush(DOMPurify.removed, {
          element: currentNode.cloneNode()
        });
        currentNode.textContent = content;
      }
    }
    _executeHooks(hooks.afterSanitizeElements, currentNode, null);
    return false;
  };
  const _isValidAttribute = function _isValidAttribute2(lcTag, lcName, value) {
    if (SANITIZE_DOM && (lcName === "id" || lcName === "name") && (value in document2 || value in formElement)) {
      return false;
    }
    if (ALLOW_DATA_ATTR && !FORBID_ATTR[lcName] && regExpTest(DATA_ATTR2, lcName)) ;
    else if (ALLOW_ARIA_ATTR && regExpTest(ARIA_ATTR2, lcName)) ;
    else if (EXTRA_ELEMENT_HANDLING.attributeCheck instanceof Function && EXTRA_ELEMENT_HANDLING.attributeCheck(lcName, lcTag)) ;
    else if (!ALLOWED_ATTR[lcName] || FORBID_ATTR[lcName]) {
      if (
        // First condition does a very basic check if a) it's basically a valid custom element tagname AND
        // b) if the tagName passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
        // and c) if the attribute name passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.attributeNameCheck
        _isBasicCustomElement(lcTag) && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, lcTag) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(lcTag)) && (CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.attributeNameCheck, lcName) || CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.attributeNameCheck(lcName, lcTag)) || // Alternative, second condition checks if it's an `is`-attribute, AND
        // the value passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
        lcName === "is" && CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, value) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(value))
      ) ;
      else {
        return false;
      }
    } else if (URI_SAFE_ATTRIBUTES[lcName]) ;
    else if (regExpTest(IS_ALLOWED_URI$1, stringReplace(value, ATTR_WHITESPACE2, ""))) ;
    else if ((lcName === "src" || lcName === "xlink:href" || lcName === "href") && lcTag !== "script" && stringIndexOf(value, "data:") === 0 && DATA_URI_TAGS[lcTag]) ;
    else if (ALLOW_UNKNOWN_PROTOCOLS && !regExpTest(IS_SCRIPT_OR_DATA2, stringReplace(value, ATTR_WHITESPACE2, ""))) ;
    else if (value) {
      return false;
    } else ;
    return true;
  };
  const _isBasicCustomElement = function _isBasicCustomElement2(tagName) {
    return tagName !== "annotation-xml" && stringMatch(tagName, CUSTOM_ELEMENT2);
  };
  const _sanitizeAttributes = function _sanitizeAttributes2(currentNode) {
    _executeHooks(hooks.beforeSanitizeAttributes, currentNode, null);
    const {
      attributes
    } = currentNode;
    if (!attributes || _isClobbered(currentNode)) {
      return;
    }
    const hookEvent = {
      attrName: "",
      attrValue: "",
      keepAttr: true,
      allowedAttributes: ALLOWED_ATTR,
      forceKeepAttr: void 0
    };
    let l = attributes.length;
    while (l--) {
      const attr = attributes[l];
      const {
        name,
        namespaceURI,
        value: attrValue
      } = attr;
      const lcName = transformCaseFunc(name);
      const initValue = attrValue;
      let value = name === "value" ? initValue : stringTrim(initValue);
      hookEvent.attrName = lcName;
      hookEvent.attrValue = value;
      hookEvent.keepAttr = true;
      hookEvent.forceKeepAttr = void 0;
      _executeHooks(hooks.uponSanitizeAttribute, currentNode, hookEvent);
      value = hookEvent.attrValue;
      if (SANITIZE_NAMED_PROPS && (lcName === "id" || lcName === "name")) {
        _removeAttribute(name, currentNode);
        value = SANITIZE_NAMED_PROPS_PREFIX + value;
      }
      if (SAFE_FOR_XML && regExpTest(/((--!?|])>)|<\/(style|title|textarea)/i, value)) {
        _removeAttribute(name, currentNode);
        continue;
      }
      if (lcName === "attributename" && stringMatch(value, "href")) {
        _removeAttribute(name, currentNode);
        continue;
      }
      if (hookEvent.forceKeepAttr) {
        continue;
      }
      if (!hookEvent.keepAttr) {
        _removeAttribute(name, currentNode);
        continue;
      }
      if (!ALLOW_SELF_CLOSE_IN_ATTR && regExpTest(/\/>/i, value)) {
        _removeAttribute(name, currentNode);
        continue;
      }
      if (SAFE_FOR_TEMPLATES) {
        arrayForEach([MUSTACHE_EXPR2, ERB_EXPR2, TMPLIT_EXPR2], (expr) => {
          value = stringReplace(value, expr, " ");
        });
      }
      const lcTag = transformCaseFunc(currentNode.nodeName);
      if (!_isValidAttribute(lcTag, lcName, value)) {
        _removeAttribute(name, currentNode);
        continue;
      }
      if (trustedTypesPolicy && typeof trustedTypes === "object" && typeof trustedTypes.getAttributeType === "function") {
        if (namespaceURI) ;
        else {
          switch (trustedTypes.getAttributeType(lcTag, lcName)) {
            case "TrustedHTML": {
              value = trustedTypesPolicy.createHTML(value);
              break;
            }
            case "TrustedScriptURL": {
              value = trustedTypesPolicy.createScriptURL(value);
              break;
            }
          }
        }
      }
      if (value !== initValue) {
        try {
          if (namespaceURI) {
            currentNode.setAttributeNS(namespaceURI, name, value);
          } else {
            currentNode.setAttribute(name, value);
          }
          if (_isClobbered(currentNode)) {
            _forceRemove(currentNode);
          } else {
            arrayPop(DOMPurify.removed);
          }
        } catch (_) {
          _removeAttribute(name, currentNode);
        }
      }
    }
    _executeHooks(hooks.afterSanitizeAttributes, currentNode, null);
  };
  const _sanitizeShadowDOM = function _sanitizeShadowDOM2(fragment) {
    let shadowNode = null;
    const shadowIterator = _createNodeIterator(fragment);
    _executeHooks(hooks.beforeSanitizeShadowDOM, fragment, null);
    while (shadowNode = shadowIterator.nextNode()) {
      _executeHooks(hooks.uponSanitizeShadowNode, shadowNode, null);
      _sanitizeElements(shadowNode);
      _sanitizeAttributes(shadowNode);
      if (shadowNode.content instanceof DocumentFragment) {
        _sanitizeShadowDOM2(shadowNode.content);
      }
    }
    _executeHooks(hooks.afterSanitizeShadowDOM, fragment, null);
  };
  DOMPurify.sanitize = function(dirty) {
    let cfg = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    let body = null;
    let importedNode = null;
    let currentNode = null;
    let returnNode = null;
    IS_EMPTY_INPUT = !dirty;
    if (IS_EMPTY_INPUT) {
      dirty = "<!-->";
    }
    if (typeof dirty !== "string" && !_isNode(dirty)) {
      if (typeof dirty.toString === "function") {
        dirty = dirty.toString();
        if (typeof dirty !== "string") {
          throw typeErrorCreate("dirty is not a string, aborting");
        }
      } else {
        throw typeErrorCreate("toString is not a function");
      }
    }
    if (!DOMPurify.isSupported) {
      return dirty;
    }
    if (!SET_CONFIG) {
      _parseConfig(cfg);
    }
    DOMPurify.removed = [];
    if (typeof dirty === "string") {
      IN_PLACE = false;
    }
    if (IN_PLACE) {
      if (dirty.nodeName) {
        const tagName = transformCaseFunc(dirty.nodeName);
        if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
          throw typeErrorCreate("root node is forbidden and cannot be sanitized in-place");
        }
      }
    } else if (dirty instanceof Node2) {
      body = _initDocument("<!---->");
      importedNode = body.ownerDocument.importNode(dirty, true);
      if (importedNode.nodeType === NODE_TYPE.element && importedNode.nodeName === "BODY") {
        body = importedNode;
      } else if (importedNode.nodeName === "HTML") {
        body = importedNode;
      } else {
        body.appendChild(importedNode);
      }
    } else {
      if (!RETURN_DOM && !SAFE_FOR_TEMPLATES && !WHOLE_DOCUMENT && // eslint-disable-next-line unicorn/prefer-includes
      dirty.indexOf("<") === -1) {
        return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(dirty) : dirty;
      }
      body = _initDocument(dirty);
      if (!body) {
        return RETURN_DOM ? null : RETURN_TRUSTED_TYPE ? emptyHTML : "";
      }
    }
    if (body && FORCE_BODY) {
      _forceRemove(body.firstChild);
    }
    const nodeIterator = _createNodeIterator(IN_PLACE ? dirty : body);
    while (currentNode = nodeIterator.nextNode()) {
      _sanitizeElements(currentNode);
      _sanitizeAttributes(currentNode);
      if (currentNode.content instanceof DocumentFragment) {
        _sanitizeShadowDOM(currentNode.content);
      }
    }
    if (IN_PLACE) {
      return dirty;
    }
    if (RETURN_DOM) {
      if (RETURN_DOM_FRAGMENT) {
        returnNode = createDocumentFragment.call(body.ownerDocument);
        while (body.firstChild) {
          returnNode.appendChild(body.firstChild);
        }
      } else {
        returnNode = body;
      }
      if (ALLOWED_ATTR.shadowroot || ALLOWED_ATTR.shadowrootmode) {
        returnNode = importNode.call(originalDocument, returnNode, true);
      }
      return returnNode;
    }
    let serializedHTML = WHOLE_DOCUMENT ? body.outerHTML : body.innerHTML;
    if (WHOLE_DOCUMENT && ALLOWED_TAGS["!doctype"] && body.ownerDocument && body.ownerDocument.doctype && body.ownerDocument.doctype.name && regExpTest(DOCTYPE_NAME, body.ownerDocument.doctype.name)) {
      serializedHTML = "<!DOCTYPE " + body.ownerDocument.doctype.name + ">\n" + serializedHTML;
    }
    if (SAFE_FOR_TEMPLATES) {
      arrayForEach([MUSTACHE_EXPR2, ERB_EXPR2, TMPLIT_EXPR2], (expr) => {
        serializedHTML = stringReplace(serializedHTML, expr, " ");
      });
    }
    return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(serializedHTML) : serializedHTML;
  };
  DOMPurify.setConfig = function() {
    let cfg = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    _parseConfig(cfg);
    SET_CONFIG = true;
  };
  DOMPurify.clearConfig = function() {
    CONFIG = null;
    SET_CONFIG = false;
  };
  DOMPurify.isValidAttribute = function(tag, attr, value) {
    if (!CONFIG) {
      _parseConfig({});
    }
    const lcTag = transformCaseFunc(tag);
    const lcName = transformCaseFunc(attr);
    return _isValidAttribute(lcTag, lcName, value);
  };
  DOMPurify.addHook = function(entryPoint, hookFunction) {
    if (typeof hookFunction !== "function") {
      return;
    }
    arrayPush(hooks[entryPoint], hookFunction);
  };
  DOMPurify.removeHook = function(entryPoint, hookFunction) {
    if (hookFunction !== void 0) {
      const index2 = arrayLastIndexOf(hooks[entryPoint], hookFunction);
      return index2 === -1 ? void 0 : arraySplice(hooks[entryPoint], index2, 1)[0];
    }
    return arrayPop(hooks[entryPoint]);
  };
  DOMPurify.removeHooks = function(entryPoint) {
    hooks[entryPoint] = [];
  };
  DOMPurify.removeAllHooks = function() {
    hooks = _createHooksMap();
  };
  return DOMPurify;
}
var purify = createDOMPurify();
var root_6 = /* @__PURE__ */ from_html(`<div><!></div>`);
var root_1$1 = /* @__PURE__ */ from_html(`<div><button type="button"><!></button> <!></div>`);
function Action_control($$anchor, $$props) {
  push($$props, true);
  const $controlsStore = () => store_get(controlsStore, "$controlsStore", $$stores);
  const [$$stores, $$cleanup] = setup_stores();
  const sanitizedSvg = /* @__PURE__ */ user_derived(() => $$props.controlOptions?.icon ? purify.sanitize($$props.controlOptions.icon.trim()) : null);
  const gm = getContext("gm");
  const menuPosition = gm.control.getDefaultPosition();
  const handleClick = () => {
    if ($$props.control && $$props.controlOptions) {
      gm.options.toggleMode($$props.control.type, $$props.control.targetMode);
    } else {
      log.error("Control or controlOptions not defined", $$props.control, $$props.controlOptions);
    }
  };
  var fragment = comment();
  var node = first_child(fragment);
  {
    var consequent_3 = ($$anchor2) => {
      var div = root_1$1();
      var button = child(div);
      let classes;
      button.__click = handleClick;
      var node_1 = child(button);
      {
        var consequent = ($$anchor3) => {
          var fragment_1 = comment();
          var node_2 = first_child(fragment_1);
          html$2(node_2, () => get$1(sanitizedSvg));
          append($$anchor3, fragment_1);
        };
        var alternate_1 = ($$anchor3) => {
          var fragment_2 = comment();
          var node_3 = first_child(fragment_2);
          {
            var consequent_1 = ($$anchor4) => {
              var text2 = text$1();
              template_effect(($0) => set_text(text2, $0), [() => $$props.controlOptions.title.slice(0, 2)]);
              append($$anchor4, text2);
            };
            var alternate = ($$anchor4) => {
              var text_1 = text$1();
              template_effect(() => set_text(text_1, $$props.control.targetMode));
              append($$anchor4, text_1);
            };
            if_block(
              node_3,
              ($$render) => {
                if ($$props.controlOptions.title) $$render(consequent_1);
                else $$render(alternate, false);
              },
              true
            );
          }
          append($$anchor3, fragment_2);
        };
        if_block(node_1, ($$render) => {
          if ($$props.controlOptions.icon) $$render(consequent);
          else $$render(alternate_1, false);
        });
      }
      var node_4 = sibling(button, 2);
      {
        var consequent_2 = ($$anchor3) => {
          var div_1 = root_6();
          let classes_1;
          var node_5 = child(div_1);
          Control_menu(node_5, {
            get control() {
              return $$props.control;
            }
          });
          template_effect(($0) => classes_1 = set_class(div_1, 1, "control-menu svelte-1q85gr9", null, classes_1, $0), [
            () => ({
              "menu-right": menuPosition.endsWith("left"),
              "menu-left": menuPosition.endsWith("right")
            })
          ]);
          append($$anchor3, div_1);
        };
        if_block(node_4, ($$render) => {
          if ($$props.controlOptions.active) $$render(consequent_2);
        });
      }
      template_effect(() => {
        set_class(div, 1, clsx($controlsStore().settings.controlsStyles.controlContainerClass), "svelte-1q85gr9");
        set_attribute(button, "id", `id_${$$props.control.type}_${$$props.control.targetMode}`);
        classes = set_class(button, 1, `${$controlsStore().settings.controlsStyles.controlButtonClass} ${$$props.control.type}-${$$props.control.targetMode}`, "svelte-1q85gr9", classes, { active: $$props.controlOptions.active });
        set_attribute(button, "title", $$props.controlOptions.title);
      });
      append($$anchor2, div);
    };
    if_block(node, ($$render) => {
      if ($$props.control && $$props.controlOptions && $$props.controlOptions.uiEnabled) $$render(consequent_3);
    });
  }
  append($$anchor, fragment);
  pop();
  $$cleanup();
}
delegate(["click"]);
function cubic_out(t) {
  const f = t - 1;
  return f * f * f + 1;
}
function slide(node, { delay = 0, duration = 400, easing = cubic_out, axis = "y" } = {}) {
  const style = getComputedStyle(node);
  const opacity = +style.opacity;
  const primary_property = axis === "y" ? "height" : "width";
  const primary_property_value = parseFloat(style[primary_property]);
  const secondary_properties = axis === "y" ? ["top", "bottom"] : ["left", "right"];
  const capitalized_secondary_properties = secondary_properties.map(
    (e) => (
      /** @type {'Left' | 'Right' | 'Top' | 'Bottom'} */
      `${e[0].toUpperCase()}${e.slice(1)}`
    )
  );
  const padding_start_value = parseFloat(style[`padding${capitalized_secondary_properties[0]}`]);
  const padding_end_value = parseFloat(style[`padding${capitalized_secondary_properties[1]}`]);
  const margin_start_value = parseFloat(style[`margin${capitalized_secondary_properties[0]}`]);
  const margin_end_value = parseFloat(style[`margin${capitalized_secondary_properties[1]}`]);
  const border_width_start_value = parseFloat(
    style[`border${capitalized_secondary_properties[0]}Width`]
  );
  const border_width_end_value = parseFloat(
    style[`border${capitalized_secondary_properties[1]}Width`]
  );
  return {
    delay,
    duration,
    easing,
    css: (t) => `overflow: hidden;opacity: ${Math.min(t * 20, 1) * opacity};${primary_property}: ${t * primary_property_value}px;padding-${secondary_properties[0]}: ${t * padding_start_value}px;padding-${secondary_properties[1]}: ${t * padding_end_value}px;margin-${secondary_properties[0]}: ${t * margin_start_value}px;margin-${secondary_properties[1]}: ${t * margin_end_value}px;border-${secondary_properties[0]}-width: ${t * border_width_start_value}px;border-${secondary_properties[1]}-width: ${t * border_width_end_value}px;min-${primary_property}: 0`
  };
}
var root_1 = /* @__PURE__ */ from_html(`<div><button class="gm-control-button"><!></button></div>`);
var root_4 = /* @__PURE__ */ from_html(`<div></div>`);
var root_2 = /* @__PURE__ */ from_html(`<div></div>`);
var root = /* @__PURE__ */ from_html(`<div class="gm-reactive-controls"><!> <!></div>`);
function Gm_controls($$anchor, $$props) {
  push($$props, true);
  const $controlsStore = () => store_get(controlsStore, "$controlsStore", $$stores);
  const [$$stores, $$cleanup] = setup_stores();
  let expanded = /* @__PURE__ */ state(true);
  let controlsCollapsible = /* @__PURE__ */ user_derived(() => $controlsStore().settings.controlsCollapsible);
  let controlsStyles = /* @__PURE__ */ user_derived(() => $controlsStore().settings.controlsStyles);
  const getControl = (groupKey, mode) => {
    const controlSection = $controlsStore().controls?.[groupKey];
    return controlSection?.[mode] || null;
  };
  const hasVisibleControls = (groupKey, groupControls) => {
    return Object.entries(groupControls).some(([controlKey, controlOptions]) => {
      const control = getControl(groupKey, controlKey);
      return control && controlOptions && controlOptions.uiEnabled;
    });
  };
  const toggleExpanded = () => {
    set$1(expanded, !get$1(expanded));
  };
  const getToggleExpandedIcon = () => {
    return purify.sanitize(get$1(expanded) ? caretDown : caretUp);
  };
  var div = root();
  var node = child(div);
  {
    var consequent = ($$anchor2) => {
      var div_1 = root_1();
      var button = child(div_1);
      button.__click = toggleExpanded;
      var node_1 = child(button);
      html$2(node_1, getToggleExpandedIcon);
      template_effect(() => set_class(div_1, 1, `${get$1(controlsStyles).controlGroupClass} group-settings`));
      append($$anchor2, div_1);
    };
    if_block(node, ($$render) => {
      if (get$1(controlsCollapsible)) $$render(consequent);
    });
  }
  var node_2 = sibling(node, 2);
  {
    var consequent_3 = ($$anchor2) => {
      var div_2 = root_2();
      each(div_2, 5, () => Object.entries($controlsStore().options), ([groupKey, groupControls]) => groupKey, ($$anchor3, $$item) => {
        var $$array = /* @__PURE__ */ user_derived(() => to_array(get$1($$item), 2));
        let groupKey = () => get$1($$array)[0];
        let groupControls = () => get$1($$array)[1];
        var fragment = comment();
        var node_3 = first_child(fragment);
        {
          var consequent_2 = ($$anchor4) => {
            var div_3 = root_4();
            each(div_3, 21, () => Object.entries(groupControls()), ([controlKey, controlOptions]) => controlKey, ($$anchor5, $$item2) => {
              var $$array_1 = /* @__PURE__ */ user_derived(() => to_array(get$1($$item2), 2));
              let controlKey = () => get$1($$array_1)[0];
              let controlOptions = () => get$1($$array_1)[1];
              const control = /* @__PURE__ */ user_derived(() => getControl(groupKey(), controlKey()));
              var fragment_1 = comment();
              var node_4 = first_child(fragment_1);
              {
                var consequent_1 = ($$anchor6) => {
                  Action_control($$anchor6, {
                    get control() {
                      return get$1(control);
                    },
                    get controlOptions() {
                      return controlOptions();
                    }
                  });
                };
                if_block(node_4, ($$render) => {
                  if (get$1(control)) $$render(consequent_1);
                });
              }
              append($$anchor5, fragment_1);
            });
            template_effect(() => set_class(div_3, 1, `${get$1(controlsStyles).controlGroupClass} group-${groupKey()}`));
            append($$anchor4, div_3);
          };
          if_block(node_3, ($$render) => {
            if (hasVisibleControls(groupKey(), groupControls())) $$render(consequent_2);
          });
        }
        append($$anchor3, fragment);
      });
      transition(1, div_2, () => slide, () => ({ duration: 180 }));
      transition(2, div_2, () => slide, () => ({ duration: 140 }));
      append($$anchor2, div_2);
    };
    if_block(node_2, ($$render) => {
      if (get$1(expanded)) $$render(consequent_3);
    });
  }
  append($$anchor, div);
  pop();
  $$cleanup();
}
delegate(["click"]);
class BaseControl {
  gm;
  constructor(gm) {
    this.gm = gm;
  }
}
const typedKeys = (obj) => Object.keys(obj);
const typedValues = (obj) => {
  return Object.values(obj);
};
const includesWithType = (value, validValues) => validValues.includes(value);
class GMControl extends BaseControl {
  controls = cloneDeep(systemControls);
  reactiveControls = null;
  container = void 0;
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:draw`]: this.handleModeEvent.bind(this),
    [`${GM_SYSTEM_PREFIX}:edit`]: this.handleModeEvent.bind(this),
    [`${GM_SYSTEM_PREFIX}:helper`]: this.handleModeEvent.bind(this)
  };
  onAdd() {
    this.createControls();
    this.gm.events.bus.attachEvents(this.eventHandlers);
    if (!this.container) {
      throw new Error("Controls container is not initialized");
    }
    return this.container;
  }
  createControls(containerElement = void 0) {
    if (this.controlsAdded()) {
      log.warn("Can't add controls: controls already added");
      return;
    }
    this.container = containerElement || this.createHtmlContainer();
    this.createReactivePanel();
  }
  onRemove() {
    this.gm.events.bus.detachEvents(this.eventHandlers);
    if (this.reactiveControls) {
      unmount(this.reactiveControls);
      this.reactiveControls = null;
    }
    if (this.container && this.container.parentNode) {
      this.container.parentNode.removeChild(this.container);
    }
    this.container = void 0;
  }
  handleModeEvent(event) {
    if (!isGmModeEvent(event)) {
      return { next: true };
    }
    const trackModes = ["mode_started", "mode_ended"];
    if (trackModes.includes(event.action)) {
      this.updateReactivePanel();
    }
    return { next: true };
  }
  controlsAdded() {
    return !!this.reactiveControls;
  }
  createReactivePanel() {
    if (!this.container) {
      log.error("Can't create reactive panel: container is not initialized");
      return;
    }
    this.syncModeStates();
    const controlsContext = /* @__PURE__ */ new Map();
    controlsContext.set("gm", this.gm);
    this.reactiveControls = mount(Gm_controls, {
      target: this.container,
      context: controlsContext
    });
    this.updateReactivePanel();
  }
  updateReactivePanel() {
    controlsStore.update(() => ({
      controls: this.controls,
      options: this.gm.options.controls,
      settings: this.gm.options.settings
    }));
  }
  createHtmlContainer() {
    const container = document.createElement("div");
    container.classList.add("geoman-controls");
    return container;
  }
  syncModeStates() {
    this.eachControlWithOptions(({ control }) => {
      this.gm.options.syncModeState(control.type, control.targetMode);
    });
  }
  eachControlWithOptions(callback) {
    return typedKeys(this.controls).forEach((modeType) => {
      const section = this.controls[modeType];
      return Object.keys(section).forEach((modeName) => {
        const mode = modeName;
        const control = this.getControl({ modeType, modeName: mode });
        const controlOptions = this.gm.options.getControlOptions({
          modeType,
          modeName: mode
        });
        if (control && controlOptions) {
          callback({ control, controlOptions });
        }
      });
    });
  }
  getControl({
    modeType,
    modeName
  }) {
    if (modeType && modeName) {
      const section = this.controls[modeType];
      return section[modeName] || null;
    }
    return null;
  }
  getDefaultPosition() {
    return this.gm.options.settings.controlsPosition;
  }
}
class EventForwarder {
  gm;
  globalEventsListener = null;
  constructor(gm) {
    this.gm = gm;
  }
  get map() {
    return this.gm.mapAdapter.getMapInstance();
  }
  async processEvent(eventName, payload) {
    await this.fireToMap({
      type: "system",
      eventName: eventName.split(":")[1],
      payload: {
        ...payload,
        level: "user"
      }
    });
    if (payload.action === "mode_start" || payload.action === "mode_end") {
      await this.forwardModeToggledEvent(payload);
    } else if (payload.action === "feature_created") {
      await this.forwardFeatureCreated(payload);
    } else if (payload.action === "feature_removed") {
      await this.forwardFeatureRemoved(payload);
    } else if (payload.action === "feature_updated") {
      await this.forwardFeatureUpdated(payload);
    } else if (payload.action === "feature_edit_start") {
      await this.forwardFeatureEditStart(payload);
    } else if (payload.action === "feature_edit_end") {
      await this.forwardFeatureEditEnd(payload);
    } else if (payload.action === "selection_change") {
      await this.forwardSelectionChanged(payload);
    } else if (payload.action === "loaded" || payload.action === "unloaded") {
      await this.forwardGeomanLoaded(payload);
    }
  }
  async forwardModeToggledEvent(payload) {
    const enabled = payload.action === "mode_start";
    if (payload.actionType === "draw") {
      const eventName = "globaldrawmodetoggled";
      const eventData = {
        name: `${GM_PREFIX}:${eventName}`,
        actionType: payload.actionType,
        action: payload.action,
        enabled,
        shape: payload.mode,
        map: this.map
      };
      await this.fireToMap({ type: "converted", eventName, payload: eventData });
      const eventName2 = enabled ? "drawstart" : "drawend";
      const eventData2 = {
        name: `${GM_PREFIX}:${eventName2}`,
        actionType: payload.actionType,
        action: payload.action,
        shape: payload.mode,
        map: this.map
      };
      await this.fireToMap({
        type: "converted",
        eventName: eventName2,
        payload: eventData2
      });
    } else if (payload.actionType === "edit") {
      const modeName = this.getConvertedEditModeName(payload.mode);
      const eventName = `global${modeName}modetoggled`;
      const eventData = {
        name: `${GM_PREFIX}:${eventName}`,
        actionType: payload.actionType,
        action: payload.action,
        enabled,
        map: this.map
      };
      await this.fireToMap({
        type: "converted",
        eventName,
        payload: eventData
      });
    } else if (payload.actionType === "helper") {
      const eventName = `global${payload.mode}modetoggled`;
      const eventData = {
        name: `${GM_PREFIX}:${eventName}`,
        actionType: payload.actionType,
        action: payload.action,
        enabled,
        map: this.map
      };
      await this.fireToMap({
        type: "converted",
        eventName,
        payload: eventData
      });
    }
  }
  async forwardSelectionChanged(payload) {
    const eventData = {
      name: `${GM_PREFIX}:selection`,
      actionType: payload.actionType,
      action: payload.action,
      selection: payload.selection,
      map: this.map
    };
    await this.fireToMap({ type: "converted", eventName: "selection", payload: eventData });
  }
  async forwardFeatureCreated(payload) {
    const eventData = {
      name: `${GM_PREFIX}:create`,
      actionType: payload.actionType,
      action: payload.action,
      shape: payload.mode,
      feature: payload.featureData,
      map: this.map
    };
    await this.fireToMap({ type: "converted", eventName: "create", payload: eventData });
  }
  async forwardFeatureRemoved(payload) {
    const eventData = {
      name: `${GM_PREFIX}:remove`,
      actionType: payload.actionType,
      action: payload.action,
      shape: payload.mode,
      feature: payload.featureData,
      map: this.map
    };
    await this.fireToMap({ type: "converted", eventName: "remove", payload: eventData });
  }
  async forwardFeatureUpdated(payload) {
    const modeName = this.getConvertedEditModeName(payload.mode);
    const multiFeatureMode = ["lasso"];
    const featuresPayload = {
      name: `${GM_PREFIX}:${modeName}`,
      actionType: payload.actionType,
      action: payload.action,
      map: this.map
    };
    if (payload.sourceFeatures.length === 1 && !multiFeatureMode.includes(payload.mode)) {
      featuresPayload.originalFeature = payload.sourceFeatures[0];
    } else {
      featuresPayload.originalFeatures = payload.sourceFeatures;
    }
    if (payload.targetFeatures.length === 1 && !multiFeatureMode.includes(payload.mode)) {
      featuresPayload.feature = payload.targetFeatures[0];
      featuresPayload.shape = featuresPayload.feature.shape;
    } else {
      featuresPayload.features = payload.targetFeatures;
    }
    await this.fireToMap({ type: "converted", eventName: `${modeName}`, payload: featuresPayload });
  }
  async forwardFeatureEditStart(payload) {
    const modeName = this.getConvertedEditModeName(payload.mode);
    const eventData = {
      name: `${GM_PREFIX}:${modeName}start`,
      actionType: payload.actionType,
      action: payload.action,
      shape: payload.feature.shape,
      feature: payload.feature,
      map: this.map
    };
    await this.fireToMap({ type: "converted", eventName: `${modeName}start`, payload: eventData });
  }
  async forwardFeatureEditEnd(payload) {
    const modeName = this.getConvertedEditModeName(payload.mode);
    const eventData = {
      name: `${GM_PREFIX}:${modeName}end`,
      actionType: payload.actionType,
      action: payload.action,
      shape: payload.feature.shape,
      feature: payload.feature,
      map: this.map
    };
    await this.fireToMap({ type: "converted", eventName: `${modeName}end`, payload: eventData });
  }
  async forwardGeomanLoaded(inputPayload) {
    const payload = {
      name: `${GM_PREFIX}:${inputPayload.action}`,
      actionType: inputPayload.actionType,
      action: inputPayload.action,
      map: this.map,
      [GM_PREFIX]: this.gm
    };
    await this.fireToMap({
      type: "converted",
      eventName: `${payload.action}`,
      payload
    });
  }
  async fireToMap({
    type,
    eventName,
    payload
  }) {
    const prefix = type === "system" ? GM_SYSTEM_PREFIX : GM_PREFIX;
    const eventNameWithPrefix = `${prefix}:${eventName}`;
    const shouldAwaitUpdates = this.gm.options.settings.awaitDataUpdatesOnEvents && "feature" in payload && payload.feature?.source;
    if (shouldAwaitUpdates) {
      const sourceName = payload.feature.source.id;
      await this.gm.features.updateManager.waitForPendingUpdates(sourceName);
    }
    this.globalEventsListener?.(payload);
    this.gm.mapAdapter.fire(eventNameWithPrefix, payload);
  }
  getConvertedEditModeName(mode) {
    return mode === "change" ? "edit" : mode;
  }
}
const isGmControlEvent = (payload) => {
  return isGmEvent(payload) && payload.name === `${GM_SYSTEM_PREFIX}:control:switch`;
};
const isGmEvent = (payload) => {
  const fields = ["level", "name", "actionType", "action"];
  return !!(payload && typeof payload === "object" && fields.every((fieldName) => fieldName in payload));
};
const isBaseMapEvent = (payload) => {
  const fields = ["type", "originalEvent", "target"];
  return !!(payload && typeof payload === "object" && fields.every((fieldName) => fieldName in payload));
};
class EventBus {
  gm;
  forwarder;
  mapEventHandlers = {};
  gmEventHandlers = {};
  constructor(gm) {
    this.gm = gm;
    this.forwarder = new EventForwarder(gm);
  }
  // Track pending event forwarding to maintain event ordering
  // Events are processed sequentially to ensure dragstart fires before drag, etc.
  pendingForward = Promise.resolve();
  fireEvent(eventName, payload) {
    const eventHandler = this.gmEventHandlers[eventName];
    if (!eventHandler) {
      return;
    }
    const { controlHandler } = eventHandler;
    controlHandler(payload);
    this.pendingForward = this.pendingForward.then(
      () => this.forwarder.processEvent(eventName, payload)
    );
  }
  attachEvents(handlers) {
    typedKeys(handlers).forEach((eventName) => {
      const handler = handlers[eventName];
      if (handler) {
        this.on(eventName, handler);
      }
    });
  }
  detachEvents(handlers) {
    typedKeys(handlers).forEach((eventName) => {
      const handler = handlers[eventName];
      if (handler) {
        this.off(eventName, handler);
      }
    });
  }
  detachAllEvents() {
    typedKeys(this.gmEventHandlers).forEach((gmEventName) => {
      const handlers = Array.from(this.gmEventHandlers[gmEventName]?.handlers || []);
      handlers.forEach((handler) => {
        this.off(gmEventName, handler);
      });
    });
    typedKeys(this.mapEventHandlers).forEach((mapEventName) => {
      const handlers = Array.from(this.mapEventHandlers[mapEventName]?.handlers || []);
      handlers.forEach((handler) => {
        this.off(mapEventName, handler);
      });
    });
  }
  on(eventName, handler) {
    if (eventName.startsWith(GM_SYSTEM_PREFIX)) {
      this.onGmEvent(eventName, handler);
    } else {
      this.onMapEvent(eventName, handler);
    }
  }
  onGmEvent(eventName, handler) {
    if (!this.gmEventHandlers[eventName]) {
      this.gmEventHandlers[eventName] = this.createEventSection(eventName);
    }
    this.gmEventHandlers[eventName]?.handlers.unshift(handler);
  }
  onMapEvent(eventName, handler) {
    if (!this.mapEventHandlers[eventName]) {
      const eventSection = this.createEventSection(eventName);
      this.gm.mapAdapter.on(eventName, eventSection.controlHandler);
      this.mapEventHandlers[eventName] = eventSection;
    }
    this.mapEventHandlers[eventName]?.handlers.unshift(handler);
  }
  off(eventName, handler) {
    if (eventName.startsWith(`${GM_SYSTEM_PREFIX}`)) {
      this.offGmEvent(eventName, handler);
    } else {
      this.offMapEvent(eventName, handler);
    }
  }
  offGmEvent(eventName, handler) {
    const eventHandlers = this.gmEventHandlers[eventName]?.handlers || [];
    const handlerIndex = eventHandlers.findIndex((handlerItem) => handler === handlerItem);
    if (handlerIndex === -1) {
      log.warn("MapEvents: handler not found", eventName, handler);
    } else {
      eventHandlers.splice(handlerIndex, 1);
      if (eventHandlers.length === 0) {
        delete this.gmEventHandlers[eventName];
      }
    }
  }
  offMapEvent(eventName, handler) {
    const eventHandlers = this.mapEventHandlers[eventName]?.handlers || [];
    const handlerIndex = eventHandlers.findIndex((handlerItem) => handler === handlerItem);
    if (handlerIndex === -1) {
      log.warn("MapEvents: handler not found", eventName, handler);
    } else {
      eventHandlers.splice(handlerIndex, 1);
      if (eventHandlers.length === 0) {
        const controlHandler = this.mapEventHandlers[eventName]?.controlHandler;
        if (controlHandler) {
          this.gm.mapAdapter.off(eventName, controlHandler);
        }
        delete this.mapEventHandlers[eventName];
      }
    }
  }
  createEventSection(eventName) {
    return {
      handlers: [],
      controlHandler: (event) => {
        let eventHandler;
        if (isGmEvent(event) && eventName.startsWith(`${GM_SYSTEM_PREFIX}`)) {
          eventHandler = this.gmEventHandlers[eventName];
        } else {
          eventHandler = this.mapEventHandlers[eventName];
        }
        if (!eventHandler) {
          log.debug(`No handlers for eventName: "${eventName}"`);
          return;
        }
        eventHandler.handlers.some((handlerItem) => {
          let result;
          if (isGmEvent(event)) {
            result = handlerItem(event);
          } else if (isBaseMapEvent(event)) {
            result = handlerItem(event);
          } else {
            log.error("EventsBus: unknown event type", event);
          }
          if (result && typeof result === "object" && "next" in result) {
            return !result.next;
          } else {
            log.error('EventsBus: handler should return an object with a "next" property');
            return false;
          }
        });
      }
    };
  }
}
class BaseEventListener {
  gm;
  constructor(gm) {
    this.gm = gm;
  }
  trackExclusiveModes(payload) {
    if (payload.action !== "mode_start") {
      return;
    }
    const { sectionName: currentSectionName, modeName: currentModeName } = this.getControlIds(payload) || {};
    const control = this.getControl(payload);
    if (!control?.settings.exclusive) {
      return;
    }
    this.gm.control.eachControlWithOptions((item) => {
      const actionType = item.control.type;
      const targetMode = item.control.targetMode;
      if (actionType === currentSectionName && targetMode === currentModeName) {
        return;
      }
      if (item.controlOptions.active && item.control.settings.exclusive) {
        this.gm.options.disableMode(actionType, targetMode);
      }
    });
  }
  trackRelatedModes(payload) {
    if (!isGmModeEvent(payload)) {
      return;
    }
    this.gm.control.eachControlWithOptions((item) => {
      const control = item.control;
      const { type: actionType, targetMode: modeName } = control;
      if (control.settings.enabledBy?.includes(payload.mode)) {
        if (payload.action === "mode_start") {
          if (this.gm.options.isModeEnabled(actionType, modeName)) {
            this.gm.options.disableMode(actionType, modeName);
          }
          this.gm.options.enableMode(actionType, modeName);
        } else if (payload.action === "mode_end") {
          this.gm.options.disableMode(actionType, modeName);
        } else {
          log.error("Unknown mode action", payload.action);
        }
      }
    });
  }
  getControl(payload) {
    const { modeName, sectionName } = this.getControlIds(payload) || {};
    if (modeName && sectionName) {
      return this.gm.control.getControl({ modeType: sectionName, modeName });
    }
    return null;
  }
  getControlOptions(payload) {
    const { modeName, sectionName } = this.getControlIds(payload) || {};
    if (modeName && sectionName) {
      return this.gm.options.getControlOptions({ modeType: sectionName, modeName });
    }
    return null;
  }
  getControlIds(payload) {
    let sectionName = null;
    let modeName = null;
    if (payload.action === "mode_start") {
      sectionName = payload.actionType;
      modeName = payload.mode;
    } else if (isGmControlEvent(payload)) {
      sectionName = payload.section;
      modeName = payload.mode;
    }
    return sectionName && modeName ? { sectionName, modeName } : null;
  }
}
class ControlEventListener extends BaseEventListener {
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:control`]: this.handleControlEvent.bind(this)
  };
  constructor(gm, bus) {
    super(gm);
    bus.attachEvents(this.eventHandlers);
  }
  handleControlEvent(payload) {
    if (!isGmControlEvent(payload)) {
      return { next: true };
    }
    const control = this.getControl(payload);
    if (!control) {
      log.error("Control not found, event payload", payload);
    }
    return { next: true };
  }
}
const MODE_TYPES = ["draw", "edit", "helper"];
const ACTION_TYPES = [...MODE_TYPES, "control"];
const SHAPE_NAMES = [
  // shapes
  "marker",
  "circle",
  "circle_marker",
  "ellipse",
  "text_marker",
  "line",
  "rectangle",
  "polygon"
];
const SPECIAL_SHAPE_NAMES = [
  "dom_marker",
  "vertex_marker",
  "center_marker",
  "edge_marker",
  "snap_guide"
];
const ALL_SHAPE_NAMES = [...SHAPE_NAMES, ...SPECIAL_SHAPE_NAMES];
const EXTRA_DRAW_MODES = ["freehand", "custom_shape"];
const DRAW_MODES = [...SHAPE_NAMES, ...EXTRA_DRAW_MODES];
const HELPER_MODES = [
  "shape_markers",
  "pin",
  "snapping",
  "snap_guides",
  "measurements",
  "auto_trace",
  "geofencing",
  "zoom_to_features",
  "click_to_edit"
];
const EDIT_MODES = [
  "drag",
  "change",
  "rotate",
  "scale",
  "copy",
  "cut",
  "split",
  "union",
  "difference",
  "line_simplification",
  "lasso",
  "delete"
];
const isLngLat = (value) => {
  return Array.isArray(value) && value.length === 2 && typeof value[0] === "number" && typeof value[1] === "number";
};
const propertyValidators = {
  id: (value) => typeof value === "string" || typeof value === "number",
  shape: (value) => typeof value === "string" && includesWithType(value, ALL_SHAPE_NAMES),
  center: (value) => isLngLat(value),
  xSemiAxis: (value) => typeof value === "number",
  ySemiAxis: (value) => typeof value === "number",
  angle: (value) => typeof value === "number",
  text: (value) => typeof value === "string",
  disableEdit: (value) => typeof value === "boolean",
  group: (value) => typeof value === "string"
};
class BaseDomMarker {
  isMarkerInstanceAvailable() {
    if (this.markerInstance) {
      return true;
    }
    log.error("Marker instance is not available");
    return false;
  }
}
var earthRadius = 63710088e-1;
var factors = {
  centimeters: earthRadius * 100,
  centimetres: earthRadius * 100,
  degrees: 360 / (2 * Math.PI),
  feet: earthRadius * 3.28084,
  inches: earthRadius * 39.37,
  kilometers: earthRadius / 1e3,
  kilometres: earthRadius / 1e3,
  meters: earthRadius,
  metres: earthRadius,
  miles: earthRadius / 1609.344,
  millimeters: earthRadius * 1e3,
  millimetres: earthRadius * 1e3,
  nauticalmiles: earthRadius / 1852,
  radians: 1,
  yards: earthRadius * 1.0936
};
function feature(geom, properties, options = {}) {
  const feat = { type: "Feature" };
  if (options.id === 0 || options.id) {
    feat.id = options.id;
  }
  if (options.bbox) {
    feat.bbox = options.bbox;
  }
  feat.properties = properties || {};
  feat.geometry = geom;
  return feat;
}
function point(coordinates, properties, options = {}) {
  if (!coordinates) {
    throw new Error("coordinates is required");
  }
  if (!Array.isArray(coordinates)) {
    throw new Error("coordinates must be an Array");
  }
  if (coordinates.length < 2) {
    throw new Error("coordinates must be at least 2 numbers long");
  }
  if (!isNumber(coordinates[0]) || !isNumber(coordinates[1])) {
    throw new Error("coordinates must contain numbers");
  }
  const geom = {
    type: "Point",
    coordinates
  };
  return feature(geom, properties, options);
}
function polygon(coordinates, properties, options = {}) {
  for (const ring of coordinates) {
    if (ring.length < 4) {
      throw new Error(
        "Each LinearRing of a Polygon must have 4 or more Positions."
      );
    }
    if (ring[ring.length - 1].length !== ring[0].length) {
      throw new Error("First and last Position are not equivalent.");
    }
    for (let j = 0; j < ring[ring.length - 1].length; j++) {
      if (ring[ring.length - 1][j] !== ring[0][j]) {
        throw new Error("First and last Position are not equivalent.");
      }
    }
  }
  const geom = {
    type: "Polygon",
    coordinates
  };
  return feature(geom, properties, options);
}
function lineString(coordinates, properties, options = {}) {
  if (coordinates.length < 2) {
    throw new Error("coordinates must be an array of two or more positions");
  }
  const geom = {
    type: "LineString",
    coordinates
  };
  return feature(geom, properties, options);
}
function featureCollection(features, options = {}) {
  const fc = { type: "FeatureCollection" };
  if (options.id) {
    fc.id = options.id;
  }
  if (options.bbox) {
    fc.bbox = options.bbox;
  }
  fc.features = features;
  return fc;
}
function multiLineString(coordinates, properties, options = {}) {
  const geom = {
    type: "MultiLineString",
    coordinates
  };
  return feature(geom, properties, options);
}
function multiPolygon(coordinates, properties, options = {}) {
  const geom = {
    type: "MultiPolygon",
    coordinates
  };
  return feature(geom, properties, options);
}
function radiansToLength(radians2, units = "kilometers") {
  const factor = factors[units];
  if (!factor) {
    throw new Error(units + " units is invalid");
  }
  return radians2 * factor;
}
function lengthToRadians(distance2, units = "kilometers") {
  const factor = factors[units];
  if (!factor) {
    throw new Error(units + " units is invalid");
  }
  return distance2 / factor;
}
function radiansToDegrees(radians2) {
  const normalisedRadians = radians2 % (2 * Math.PI);
  return normalisedRadians * 180 / Math.PI;
}
function degreesToRadians(degrees2) {
  const normalisedDegrees = degrees2 % 360;
  return normalisedDegrees * Math.PI / 180;
}
function convertLength(length2, originalUnit = "kilometers", finalUnit = "kilometers") {
  if (!(length2 >= 0)) {
    throw new Error("length must be a positive number");
  }
  return radiansToLength(lengthToRadians(length2, originalUnit), finalUnit);
}
function isNumber(num) {
  return !isNaN(num) && num !== null && !Array.isArray(num);
}
function isObject(input) {
  return input !== null && typeof input === "object" && !Array.isArray(input);
}
function coordEach(geojson, callback, excludeWrapCoord) {
  if (geojson === null) return;
  var j, k, l, geometry, stopG, coords, geometryMaybeCollection, wrapShrink = 0, coordIndex = 0, isGeometryCollection, type = geojson.type, isFeatureCollection = type === "FeatureCollection", isFeature = type === "Feature", stop = isFeatureCollection ? geojson.features.length : 1;
  for (var featureIndex = 0; featureIndex < stop; featureIndex++) {
    geometryMaybeCollection = isFeatureCollection ? geojson.features[featureIndex].geometry : isFeature ? geojson.geometry : geojson;
    isGeometryCollection = geometryMaybeCollection ? geometryMaybeCollection.type === "GeometryCollection" : false;
    stopG = isGeometryCollection ? geometryMaybeCollection.geometries.length : 1;
    for (var geomIndex = 0; geomIndex < stopG; geomIndex++) {
      var multiFeatureIndex = 0;
      var geometryIndex = 0;
      geometry = isGeometryCollection ? geometryMaybeCollection.geometries[geomIndex] : geometryMaybeCollection;
      if (geometry === null) continue;
      coords = geometry.coordinates;
      var geomType = geometry.type;
      wrapShrink = excludeWrapCoord && (geomType === "Polygon" || geomType === "MultiPolygon") ? 1 : 0;
      switch (geomType) {
        case null:
          break;
        case "Point":
          if (callback(
            coords,
            coordIndex,
            featureIndex,
            multiFeatureIndex,
            geometryIndex
          ) === false)
            return false;
          coordIndex++;
          multiFeatureIndex++;
          break;
        case "LineString":
        case "MultiPoint":
          for (j = 0; j < coords.length; j++) {
            if (callback(
              coords[j],
              coordIndex,
              featureIndex,
              multiFeatureIndex,
              geometryIndex
            ) === false)
              return false;
            coordIndex++;
            if (geomType === "MultiPoint") multiFeatureIndex++;
          }
          if (geomType === "LineString") multiFeatureIndex++;
          break;
        case "Polygon":
        case "MultiLineString":
          for (j = 0; j < coords.length; j++) {
            for (k = 0; k < coords[j].length - wrapShrink; k++) {
              if (callback(
                coords[j][k],
                coordIndex,
                featureIndex,
                multiFeatureIndex,
                geometryIndex
              ) === false)
                return false;
              coordIndex++;
            }
            if (geomType === "MultiLineString") multiFeatureIndex++;
            if (geomType === "Polygon") geometryIndex++;
          }
          if (geomType === "Polygon") multiFeatureIndex++;
          break;
        case "MultiPolygon":
          for (j = 0; j < coords.length; j++) {
            geometryIndex = 0;
            for (k = 0; k < coords[j].length; k++) {
              for (l = 0; l < coords[j][k].length - wrapShrink; l++) {
                if (callback(
                  coords[j][k][l],
                  coordIndex,
                  featureIndex,
                  multiFeatureIndex,
                  geometryIndex
                ) === false)
                  return false;
                coordIndex++;
              }
              geometryIndex++;
            }
            multiFeatureIndex++;
          }
          break;
        case "GeometryCollection":
          for (j = 0; j < geometry.geometries.length; j++)
            if (coordEach(geometry.geometries[j], callback, excludeWrapCoord) === false)
              return false;
          break;
        default:
          throw new Error("Unknown Geometry Type");
      }
    }
  }
}
function featureEach(geojson, callback) {
  if (geojson.type === "Feature") {
    callback(geojson, 0);
  } else if (geojson.type === "FeatureCollection") {
    for (var i = 0; i < geojson.features.length; i++) {
      if (callback(geojson.features[i], i) === false) break;
    }
  }
}
function featureReduce(geojson, callback, initialValue) {
  var previousValue = initialValue;
  featureEach(geojson, function(currentFeature, featureIndex) {
    if (featureIndex === 0 && initialValue === void 0)
      previousValue = currentFeature;
    else previousValue = callback(previousValue, currentFeature, featureIndex);
  });
  return previousValue;
}
function geomEach(geojson, callback) {
  var i, j, g, geometry, stopG, geometryMaybeCollection, isGeometryCollection, featureProperties, featureBBox, featureId2, featureIndex = 0, isFeatureCollection = geojson.type === "FeatureCollection", isFeature = geojson.type === "Feature", stop = isFeatureCollection ? geojson.features.length : 1;
  for (i = 0; i < stop; i++) {
    geometryMaybeCollection = isFeatureCollection ? geojson.features[i].geometry : isFeature ? geojson.geometry : geojson;
    featureProperties = isFeatureCollection ? geojson.features[i].properties : isFeature ? geojson.properties : {};
    featureBBox = isFeatureCollection ? geojson.features[i].bbox : isFeature ? geojson.bbox : void 0;
    featureId2 = isFeatureCollection ? geojson.features[i].id : isFeature ? geojson.id : void 0;
    isGeometryCollection = geometryMaybeCollection ? geometryMaybeCollection.type === "GeometryCollection" : false;
    stopG = isGeometryCollection ? geometryMaybeCollection.geometries.length : 1;
    for (g = 0; g < stopG; g++) {
      geometry = isGeometryCollection ? geometryMaybeCollection.geometries[g] : geometryMaybeCollection;
      if (geometry === null) {
        if (callback(
          null,
          featureIndex,
          featureProperties,
          featureBBox,
          featureId2
        ) === false)
          return false;
        continue;
      }
      switch (geometry.type) {
        case "Point":
        case "LineString":
        case "MultiPoint":
        case "Polygon":
        case "MultiLineString":
        case "MultiPolygon": {
          if (callback(
            geometry,
            featureIndex,
            featureProperties,
            featureBBox,
            featureId2
          ) === false)
            return false;
          break;
        }
        case "GeometryCollection": {
          for (j = 0; j < geometry.geometries.length; j++) {
            if (callback(
              geometry.geometries[j],
              featureIndex,
              featureProperties,
              featureBBox,
              featureId2
            ) === false)
              return false;
          }
          break;
        }
        default:
          throw new Error("Unknown Geometry Type");
      }
    }
    featureIndex++;
  }
}
function geomReduce(geojson, callback, initialValue) {
  var previousValue = initialValue;
  geomEach(
    geojson,
    function(currentGeometry, featureIndex, featureProperties, featureBBox, featureId2) {
      previousValue = callback(
        previousValue,
        currentGeometry,
        featureIndex,
        featureProperties,
        featureBBox,
        featureId2
      );
    }
  );
  return previousValue;
}
function flattenEach(geojson, callback) {
  geomEach(geojson, function(geometry, featureIndex, properties, bbox2, id) {
    var type = geometry === null ? null : geometry.type;
    switch (type) {
      case null:
      case "Point":
      case "LineString":
      case "Polygon":
        if (callback(
          feature(geometry, properties, { bbox: bbox2, id }),
          featureIndex,
          0
        ) === false)
          return false;
        return;
    }
    var geomType;
    switch (type) {
      case "MultiPoint":
        geomType = "Point";
        break;
      case "MultiLineString":
        geomType = "LineString";
        break;
      case "MultiPolygon":
        geomType = "Polygon";
        break;
    }
    for (var multiFeatureIndex = 0; multiFeatureIndex < geometry.coordinates.length; multiFeatureIndex++) {
      var coordinate = geometry.coordinates[multiFeatureIndex];
      var geom = {
        type: geomType,
        coordinates: coordinate
      };
      if (callback(feature(geom, properties), featureIndex, multiFeatureIndex) === false)
        return false;
    }
  });
}
function bbox$1(geojson, options = {}) {
  if (geojson.bbox != null && true !== options.recompute) {
    return geojson.bbox;
  }
  const result = [Infinity, Infinity, -Infinity, -Infinity];
  coordEach(geojson, (coord) => {
    if (result[0] > coord[0]) {
      result[0] = coord[0];
    }
    if (result[1] > coord[1]) {
      result[1] = coord[1];
    }
    if (result[2] < coord[0]) {
      result[2] = coord[0];
    }
    if (result[3] < coord[1]) {
      result[3] = coord[1];
    }
  });
  return result;
}
var index_default$g = bbox$1;
function getCoord(coord) {
  if (!coord) {
    throw new Error("coord is required");
  }
  if (!Array.isArray(coord)) {
    if (coord.type === "Feature" && coord.geometry !== null && coord.geometry.type === "Point") {
      return [...coord.geometry.coordinates];
    }
    if (coord.type === "Point") {
      return [...coord.coordinates];
    }
  }
  if (Array.isArray(coord) && coord.length >= 2 && !Array.isArray(coord[0]) && !Array.isArray(coord[1])) {
    return [...coord];
  }
  throw new Error("coord must be GeoJSON Point or an Array of numbers");
}
function getCoords(coords) {
  if (Array.isArray(coords)) {
    return coords;
  }
  if (coords.type === "Feature") {
    if (coords.geometry !== null) {
      return coords.geometry.coordinates;
    }
  } else {
    if (coords.coordinates) {
      return coords.coordinates;
    }
  }
  throw new Error(
    "coords must be GeoJSON Feature, Geometry Object or an Array"
  );
}
function getGeom(geojson) {
  if (geojson.type === "Feature") {
    return geojson.geometry;
  }
  return geojson;
}
function getType(geojson, _name) {
  if (geojson.type === "FeatureCollection") {
    return "FeatureCollection";
  }
  if (geojson.type === "GeometryCollection") {
    return "GeometryCollection";
  }
  if (geojson.type === "Feature" && geojson.geometry !== null) {
    return geojson.geometry.type;
  }
  return geojson.type;
}
function destination(origin, distance2, bearing2, options = {}) {
  const coordinates1 = getCoord(origin);
  const longitude1 = degreesToRadians(coordinates1[0]);
  const latitude1 = degreesToRadians(coordinates1[1]);
  const bearingRad = degreesToRadians(bearing2);
  const radians2 = lengthToRadians(distance2, options.units);
  const latitude2 = Math.asin(
    Math.sin(latitude1) * Math.cos(radians2) + Math.cos(latitude1) * Math.sin(radians2) * Math.cos(bearingRad)
  );
  const longitude2 = longitude1 + Math.atan2(
    Math.sin(bearingRad) * Math.sin(radians2) * Math.cos(latitude1),
    Math.cos(radians2) - Math.sin(latitude1) * Math.sin(latitude2)
  );
  const lng = radiansToDegrees(longitude2);
  const lat = radiansToDegrees(latitude2);
  if (coordinates1[2] !== void 0) {
    return point([lng, lat, coordinates1[2]], options.properties);
  }
  return point([lng, lat], options.properties);
}
function circle(center2, radius, options = {}) {
  const steps = options.steps || 64;
  const properties = options.properties ? options.properties : !Array.isArray(center2) && center2.type === "Feature" && center2.properties ? center2.properties : {};
  const coordinates = [];
  for (let i = 0; i < steps; i++) {
    coordinates.push(
      destination(center2, radius, i * -360 / steps, options).geometry.coordinates
    );
  }
  coordinates.push(coordinates[0]);
  return polygon([coordinates], properties);
}
var index_default$f = circle;
function centroid(geojson, options = {}) {
  let xSum = 0;
  let ySum = 0;
  let len = 0;
  coordEach(
    geojson,
    function(coord) {
      xSum += coord[0];
      ySum += coord[1];
      len++;
    },
    true
  );
  return point([xSum / len, ySum / len], options.properties);
}
var index_default$e = centroid;
function rhumbBearing(start, end, options = {}) {
  let bear360;
  if (options.final) {
    bear360 = calculateRhumbBearing(getCoord(end), getCoord(start));
  } else {
    bear360 = calculateRhumbBearing(getCoord(start), getCoord(end));
  }
  const bear180 = bear360 > 180 ? -(360 - bear360) : bear360;
  return bear180;
}
function calculateRhumbBearing(from, to) {
  const phi1 = degreesToRadians(from[1]);
  const phi2 = degreesToRadians(to[1]);
  let deltaLambda = degreesToRadians(to[0] - from[0]);
  if (deltaLambda > Math.PI) {
    deltaLambda -= 2 * Math.PI;
  }
  if (deltaLambda < -Math.PI) {
    deltaLambda += 2 * Math.PI;
  }
  const deltaPsi = Math.log(
    Math.tan(phi2 / 2 + Math.PI / 4) / Math.tan(phi1 / 2 + Math.PI / 4)
  );
  const theta = Math.atan2(deltaLambda, deltaPsi);
  return (radiansToDegrees(theta) + 360) % 360;
}
function rhumbDistance(from, to, options = {}) {
  const origin = getCoord(from);
  const destination2 = getCoord(to);
  destination2[0] += destination2[0] - origin[0] > 180 ? -360 : origin[0] - destination2[0] > 180 ? 360 : 0;
  const distanceInMeters = calculateRhumbDistance(origin, destination2);
  const distance2 = convertLength(distanceInMeters, "meters", options.units);
  return distance2;
}
function calculateRhumbDistance(origin, destination2, radius) {
  radius = radius === void 0 ? earthRadius : Number(radius);
  const R = radius;
  const phi1 = origin[1] * Math.PI / 180;
  const phi2 = destination2[1] * Math.PI / 180;
  const DeltaPhi = phi2 - phi1;
  let DeltaLambda = Math.abs(destination2[0] - origin[0]) * Math.PI / 180;
  if (DeltaLambda > Math.PI) {
    DeltaLambda -= 2 * Math.PI;
  }
  const DeltaPsi = Math.log(
    Math.tan(phi2 / 2 + Math.PI / 4) / Math.tan(phi1 / 2 + Math.PI / 4)
  );
  const q = Math.abs(DeltaPsi) > 1e-11 ? DeltaPhi / DeltaPsi : Math.cos(phi1);
  const delta = Math.sqrt(
    DeltaPhi * DeltaPhi + q * q * DeltaLambda * DeltaLambda
  );
  const dist = delta * R;
  return dist;
}
function rhumbDestination(origin, distance2, bearing2, options = {}) {
  const wasNegativeDistance = distance2 < 0;
  let distanceInMeters = convertLength(
    Math.abs(distance2),
    options.units,
    "meters"
  );
  if (wasNegativeDistance) distanceInMeters = -Math.abs(distanceInMeters);
  const coords = getCoord(origin);
  const destination2 = calculateRhumbDestination(
    coords,
    distanceInMeters,
    bearing2
  );
  destination2[0] += destination2[0] - coords[0] > 180 ? -360 : coords[0] - destination2[0] > 180 ? 360 : 0;
  return point(destination2, options.properties);
}
function calculateRhumbDestination(origin, distance2, bearing2, radius) {
  radius = radius === void 0 ? earthRadius : Number(radius);
  const delta = distance2 / radius;
  const lambda1 = origin[0] * Math.PI / 180;
  const phi1 = degreesToRadians(origin[1]);
  const theta = degreesToRadians(bearing2);
  const DeltaPhi = delta * Math.cos(theta);
  let phi2 = phi1 + DeltaPhi;
  if (Math.abs(phi2) > Math.PI / 2) {
    phi2 = phi2 > 0 ? Math.PI - phi2 : -Math.PI - phi2;
  }
  const DeltaPsi = Math.log(
    Math.tan(phi2 / 2 + Math.PI / 4) / Math.tan(phi1 / 2 + Math.PI / 4)
  );
  const q = Math.abs(DeltaPsi) > 1e-11 ? DeltaPhi / DeltaPsi : Math.cos(phi1);
  const DeltaLambda = delta * Math.sin(theta) / q;
  const lambda2 = lambda1 + DeltaLambda;
  return [
    (lambda2 * 180 / Math.PI + 540) % 360 - 180,
    phi2 * 180 / Math.PI
  ];
}
function clone$1(geojson) {
  if (!geojson) {
    throw new Error("geojson is required");
  }
  switch (geojson.type) {
    case "Feature":
      return cloneFeature(geojson);
    case "FeatureCollection":
      return cloneFeatureCollection(geojson);
    case "Point":
    case "LineString":
    case "Polygon":
    case "MultiPoint":
    case "MultiLineString":
    case "MultiPolygon":
    case "GeometryCollection":
      return cloneGeometry(geojson);
    default:
      throw new Error("unknown GeoJSON type");
  }
}
function cloneFeature(geojson) {
  const cloned = { type: "Feature" };
  Object.keys(geojson).forEach((key) => {
    switch (key) {
      case "type":
      case "properties":
      case "geometry":
        return;
      default:
        cloned[key] = geojson[key];
    }
  });
  cloned.properties = cloneProperties(geojson.properties);
  if (geojson.geometry == null) {
    cloned.geometry = null;
  } else {
    cloned.geometry = cloneGeometry(geojson.geometry);
  }
  return cloned;
}
function cloneProperties(properties) {
  const cloned = {};
  if (!properties) {
    return cloned;
  }
  Object.keys(properties).forEach((key) => {
    const value = properties[key];
    if (typeof value === "object") {
      if (value === null) {
        cloned[key] = null;
      } else if (Array.isArray(value)) {
        cloned[key] = value.map((item) => {
          return item;
        });
      } else {
        cloned[key] = cloneProperties(value);
      }
    } else {
      cloned[key] = value;
    }
  });
  return cloned;
}
function cloneFeatureCollection(geojson) {
  const cloned = { type: "FeatureCollection" };
  Object.keys(geojson).forEach((key) => {
    switch (key) {
      case "type":
      case "features":
        return;
      default:
        cloned[key] = geojson[key];
    }
  });
  cloned.features = geojson.features.map((feature2) => {
    return cloneFeature(feature2);
  });
  return cloned;
}
function cloneGeometry(geometry) {
  const geom = { type: geometry.type };
  if (geometry.bbox) {
    geom.bbox = geometry.bbox;
  }
  if (geometry.type === "GeometryCollection") {
    geom.geometries = geometry.geometries.map((g) => {
      return cloneGeometry(g);
    });
    return geom;
  }
  geom.coordinates = deepSlice(geometry.coordinates);
  return geom;
}
function deepSlice(coords) {
  const cloned = coords;
  if (typeof cloned[0] !== "object") {
    return cloned.slice();
  }
  return cloned.map((coord) => {
    return deepSlice(coord);
  });
}
var index_default$d = clone$1;
function transformRotate(geojson, angle, options) {
  options = options || {};
  if (!isObject(options)) throw new Error("options is invalid");
  const pivot = options.pivot;
  const mutate = options.mutate;
  if (!geojson) throw new Error("geojson is required");
  if (angle === void 0 || angle === null || isNaN(angle))
    throw new Error("angle is required");
  if (angle === 0) return geojson;
  const pivotCoord = pivot != null ? pivot : centroid(geojson);
  if (mutate === false || mutate === void 0) geojson = clone$1(geojson);
  coordEach(geojson, function(pointCoords) {
    const initialAngle = rhumbBearing(pivotCoord, pointCoords);
    const finalAngle = initialAngle + angle;
    const distance2 = rhumbDistance(pivotCoord, pointCoords);
    const newCoords = getCoords(
      rhumbDestination(pivotCoord, distance2, finalAngle)
    );
    pointCoords[0] = newCoords[0];
    pointCoords[1] = newCoords[1];
  });
  return geojson;
}
var index_default$c = transformRotate;
function ellipse(center2, xSemiAxis, ySemiAxis, options) {
  options = options || {};
  let steps = options.steps || 64;
  const units = options.units || "kilometers";
  let angle = options.angle || 0;
  const pivot = options.pivot || center2;
  const properties = options.properties || {};
  if (!center2) throw new Error("center is required");
  if (!xSemiAxis) throw new Error("xSemiAxis is required");
  if (!ySemiAxis) throw new Error("ySemiAxis is required");
  if (!isObject(options)) throw new Error("options must be an object");
  if (!isNumber(steps)) throw new Error("steps must be a number");
  if (!isNumber(angle)) throw new Error("angle must be a number");
  const centerCoords = getCoord(transformRotate(point(getCoord(center2)), angle, { pivot }));
  angle = -90 + angle;
  steps = Math.ceil(steps / 4);
  const quadrantParameters = [];
  const parameters = [];
  const a = xSemiAxis;
  const b = ySemiAxis;
  const c = b;
  const m = (a - b) / (Math.PI / 2);
  const A = (a + b) * Math.PI / 4;
  const v = 0.5;
  const k = steps;
  let w = 0;
  let x = 0;
  for (let i = 0; i < steps; i++) {
    x += w;
    if (m === 0) {
      w = A / k / c;
    } else {
      w = (-(m * x + c) + Math.sqrt(Math.pow(m * x + c, 2) - 4 * (v * m) * -(A / k))) / (2 * (v * m));
    }
    if (x != 0) {
      quadrantParameters.push(x);
    }
  }
  parameters.push(0);
  for (let i = 0; i < quadrantParameters.length; i++) {
    parameters.push(quadrantParameters[i]);
  }
  parameters.push(Math.PI / 2);
  for (let i = 0; i < quadrantParameters.length; i++) {
    parameters.push(Math.PI - quadrantParameters[quadrantParameters.length - i - 1]);
  }
  parameters.push(Math.PI);
  for (let i = 0; i < quadrantParameters.length; i++) {
    parameters.push(Math.PI + quadrantParameters[i]);
  }
  parameters.push(3 * Math.PI / 2);
  for (let i = 0; i < quadrantParameters.length; i++) {
    parameters.push(2 * Math.PI - quadrantParameters[quadrantParameters.length - i - 1]);
  }
  parameters.push(0);
  const coords = [];
  for (const param of parameters) {
    const theta = Math.atan2(b * Math.sin(param), a * Math.cos(param));
    const r2 = Math.sqrt(
      Math.pow(a, 2) * Math.pow(b, 2) / (Math.pow(a * Math.sin(theta), 2) + Math.pow(b * Math.cos(theta), 2))
    );
    coords.push(
      destination(centerCoords, r2, angle + radiansToDegrees(theta), {
        units
      }).geometry.coordinates
    );
  }
  return polygon([coords], properties);
}
function bearing(start, end, options = {}) {
  if (options.final === true) {
    return calculateFinalBearing(start, end);
  }
  const coordinates1 = getCoord(start);
  const coordinates2 = getCoord(end);
  const lon1 = degreesToRadians(coordinates1[0]);
  const lon2 = degreesToRadians(coordinates2[0]);
  const lat1 = degreesToRadians(coordinates1[1]);
  const lat2 = degreesToRadians(coordinates2[1]);
  const a = Math.sin(lon2 - lon1) * Math.cos(lat2);
  const b = Math.cos(lat1) * Math.sin(lat2) - Math.sin(lat1) * Math.cos(lat2) * Math.cos(lon2 - lon1);
  return radiansToDegrees(Math.atan2(a, b));
}
function calculateFinalBearing(start, end) {
  let bear = bearing(end, start);
  bear = (bear + 180) % 360;
  return bear;
}
var index_default$b = bearing;
function distance(from, to, options = {}) {
  var coordinates1 = getCoord(from);
  var coordinates2 = getCoord(to);
  var dLat = degreesToRadians(coordinates2[1] - coordinates1[1]);
  var dLon = degreesToRadians(coordinates2[0] - coordinates1[0]);
  var lat1 = degreesToRadians(coordinates1[1]);
  var lat2 = degreesToRadians(coordinates2[1]);
  var a = Math.pow(Math.sin(dLat / 2), 2) + Math.pow(Math.sin(dLon / 2), 2) * Math.cos(lat1) * Math.cos(lat2);
  return radiansToLength(
    2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a)),
    options.units
  );
}
var index_default$a = distance;
function polygonToLine(poly, options = {}) {
  const geom = getGeom(poly);
  if (!options.properties && poly.type === "Feature") {
    options.properties = poly.properties;
  }
  switch (geom.type) {
    case "Polygon":
      return singlePolygonToLine(geom, options);
    case "MultiPolygon":
      return multiPolygonToLine(geom, options);
    default:
      throw new Error("invalid poly");
  }
}
function singlePolygonToLine(poly, options = {}) {
  const geom = getGeom(poly);
  const coords = geom.coordinates;
  const properties = options.properties ? options.properties : poly.type === "Feature" ? poly.properties : {};
  return coordsToLine(coords, properties);
}
function multiPolygonToLine(multiPoly, options = {}) {
  const geom = getGeom(multiPoly);
  const coords = geom.coordinates;
  const properties = options.properties ? options.properties : multiPoly.type === "Feature" ? multiPoly.properties : {};
  const lines = [];
  coords.forEach((coord) => {
    lines.push(coordsToLine(coord, properties));
  });
  return featureCollection(lines);
}
function coordsToLine(coords, properties) {
  if (coords.length > 1) {
    return multiLineString(coords, properties);
  }
  return lineString(coords[0], properties);
}
const isEqualPosition = (position1, position2) => {
  return position1[0] === position2[0] && position1[1] === position2[1];
};
const isLineStringFeature = (geoJson) => {
  return geoJson.type === "Feature" && geoJson.geometry.type === "LineString";
};
const isMultiLineStringFeature = (geoJson) => {
  return geoJson.type === "Feature" && geoJson.geometry.type === "MultiLineString";
};
const isPolygonFeature = (geoJson) => {
  return geoJson.type === "Feature" && geoJson.geometry.type === "Polygon";
};
const isMultiPolygonFeature = (geoJson) => {
  return geoJson.type === "Feature" && geoJson.geometry.type === "MultiPolygon";
};
const multiLineStringToFeatureCollection = (geoJson) => {
  return {
    type: "FeatureCollection",
    features: geoJson.geometry.coordinates.map((lineCoordinates) => ({
      type: "Feature",
      properties: geoJson.properties,
      geometry: {
        type: "LineString",
        coordinates: lineCoordinates
      }
    }))
  };
};
const getLngLatDiff = (startLngLat, endLngLat) => ({
  lng: endLngLat[0] - startLngLat[0],
  lat: endLngLat[1] - startLngLat[1]
});
const isPosition = (position) => Array.isArray(position) && position.length >= 2 && position.length <= 3 && position.every((item) => typeof item === "number");
const eachCoordinateWithPath = (geoJson, callback, skipClosingCoordinate = false) => {
  let counter = 0;
  const nestedKeys = ["features", "geometries", "geometry", "coordinates"];
  const traverseGeoJson = (geoJsonItem, currentPath, isPolygon = false) => {
    if (isPosition(geoJsonItem)) {
      callback({ coordinate: geoJsonItem, path: currentPath }, counter);
      counter += 1;
    } else if (Array.isArray(geoJsonItem)) {
      geoJsonItem.forEach((subItem, index2) => {
        if (isPolygon && skipClosingCoordinate && index2 === geoJsonItem.length - 1 && isPosition(subItem)) {
          return;
        }
        traverseGeoJson(subItem, [...currentPath, index2], isPolygon);
      });
    } else if (typeof geoJsonItem === "object" && geoJsonItem !== null) {
      typedKeys(geoJsonItem).forEach((key) => {
        const subItem = geoJsonItem[key];
        if (nestedKeys.includes(key) && subItem) {
          const itemType = "type" in geoJsonItem && geoJsonItem.type;
          const nextIsPolygon = itemType === "Polygon" || itemType === "MultiPolygon";
          traverseGeoJson(subItem, [...currentPath, key], nextIsPolygon);
        }
      });
    }
  };
  traverseGeoJson(geoJson, []);
};
const findCoordinateWithPath = (geoJson, coordinate) => {
  let targetPosition = { coordinate: [0, 0], path: [] };
  let targetIndex = -1;
  try {
    eachCoordinateWithPath(geoJson, (position, index2) => {
      if (coordinate[0] === position.coordinate[0] && coordinate[1] === position.coordinate[1]) {
        targetIndex = index2;
        targetPosition = position;
        throw new Error("stop");
      }
    });
  } catch {
    if (targetPosition) {
      return {
        index: targetIndex,
        coordinate: targetPosition.coordinate,
        path: targetPosition.path
      };
    }
  }
  return null;
};
const eachSegmentWithPath = (geoJson, callback) => {
  let counter = 0;
  const nestedKeys = ["features", "geometries", "geometry", "coordinates"];
  const traverseGeoJson = (geoJsonItem, nextGeoJsonItem, currentPath, nextPath) => {
    if (isPosition(geoJsonItem) && isPosition(nextGeoJsonItem)) {
      callback(
        {
          start: { coordinate: [...geoJsonItem], path: currentPath },
          end: { coordinate: [...nextGeoJsonItem], path: nextPath }
        },
        counter
      );
      counter += 1;
    } else if (Array.isArray(geoJsonItem)) {
      geoJsonItem.forEach((subItem, index2) => {
        traverseGeoJson(
          subItem,
          geoJsonItem[index2 + 1],
          [...currentPath, index2],
          [...currentPath, index2 + 1]
        );
      });
    } else if (typeof geoJsonItem === "object" && geoJsonItem !== null) {
      typedKeys(geoJsonItem).forEach((key) => {
        const subItem = geoJsonItem[key];
        if (nestedKeys.includes(key) && subItem) {
          traverseGeoJson(subItem, void 0, [...currentPath, key], []);
        }
      });
    }
  };
  traverseGeoJson(geoJson, void 0, [], []);
};
const findCoordinateIndices = (geoJson, lngLat) => {
  let indices = {
    absCoordIndex: -1,
    featureIndex: -1,
    multiFeatureIndex: -1,
    geometryIndex: -1
  };
  try {
    coordEach(
      geoJson,
      (currentCoord, coordIndex, featureIndex, multiFeatureIndex, geometryIndex) => {
        if (currentCoord[0] === lngLat[0] && currentCoord[1] === lngLat[1]) {
          indices = { absCoordIndex: coordIndex, featureIndex, multiFeatureIndex, geometryIndex };
          throw new Error("found");
        }
      }
    );
  } catch {
  }
  return indices;
};
const twoCoordsToLineString = (position1, position2, properties = {}) => {
  return {
    type: "Feature",
    properties,
    geometry: {
      type: "LineString",
      coordinates: [position1, position2]
    }
  };
};
const getBboxFromTwoCoords = (lngLat1, lngLat2) => {
  const [lng1, lat1] = lngLat1;
  const [lng2, lat2] = lngLat2;
  const minX = Math.min(lng1, lng2);
  const minY = Math.min(lat1, lat2);
  const maxX = Math.max(lng1, lng2);
  const maxY = Math.max(lat1, lat2);
  return [minX, minY, maxX, maxY];
};
const twoCoordsToGeoJsonRectangle = (lngLat1, lngLat2) => {
  const bBox = getBboxFromTwoCoords(lngLat1, lngLat2);
  const southWest = [bBox[0], bBox[1]];
  const northEast = [bBox[2], bBox[3]];
  const minX = southWest[0];
  const minY = southWest[1];
  const maxX = northEast[0];
  const maxY = northEast[1];
  return {
    type: "Feature",
    properties: {
      shape: "rectangle"
    },
    geometry: {
      type: "Polygon",
      coordinates: [
        [
          [minX, minY],
          [maxX, minY],
          [maxX, maxY],
          [minX, maxY],
          [minX, minY]
        ]
      ]
    }
  };
};
const geoJsonPointToLngLat = (geoJson) => {
  return [geoJson.geometry.coordinates[0], geoJson.geometry.coordinates[1]];
};
const getGeoJsonBounds = (geoJson) => {
  const bounds = index_default$g(geoJson);
  return [
    [bounds[0], bounds[1]],
    // South-West (min-x, min-y)
    [bounds[2], bounds[3]]
    // North-East (max-x, max-y)
  ];
};
const boundsToBBox = (bounds) => {
  return [bounds[0][0], bounds[0][1], bounds[1][0], bounds[1][1]];
};
const bBoxContains = (bBoxBounds, lngLat) => {
  const [minX, minY, maxX, maxY] = bBoxBounds;
  const [lng, lat] = lngLat;
  return lng >= minX && lng <= maxX && lat >= minY && lat <= maxY;
};
const boundsContains = (bounds, lngLat) => {
  const bBoxBounds = boundsToBBox(bounds);
  return bBoxContains(bBoxBounds, lngLat);
};
const getGeoJsonCoordinatesCount = (geoJson) => {
  let count = 0;
  coordEach(
    geoJson,
    () => {
      count += 1;
    },
    true
  );
  return count;
};
const getAllGeoJsonCoordinates = (geoJson) => {
  const coordinates = [];
  coordEach(
    geoJson,
    (coord) => {
      coordinates.push([coord[0], coord[1]]);
    },
    true
  );
  return coordinates;
};
const allCoordinatesEqual = (geoJson) => {
  const featureLngLats = getAllGeoJsonCoordinates(geoJson);
  return featureLngLats.some((lngLat) => !isEqual(featureLngLats[0], lngLat));
};
const getGeoJsonFirstPoint = (shapeGeoJson) => {
  let firstPoint = null;
  try {
    eachCoordinateWithPath(shapeGeoJson, (position) => {
      firstPoint = position.coordinate;
      throw new Error("found");
    });
  } catch {
    return firstPoint;
  }
  return null;
};
const getEuclideanDistance = (point1, point2) => {
  return Math.sqrt((point1[0] - point2[0]) ** 2 + (point1[1] - point2[1]) ** 2);
};
const getEuclideanSegmentNearestPoint = (linePoint1, linePoint2, targetPoint) => {
  const [x12, y12] = [linePoint1[0], linePoint1[1]];
  const [x2, y2] = [linePoint2[0], linePoint2[1]];
  const [px, py] = [targetPoint[0], targetPoint[1]];
  const vx = x2 - x12;
  const vy = y2 - y12;
  const wx = px - x12;
  const wy = py - y12;
  const c1 = wx * vx + wy * vy;
  const c2 = vx * vx + vy * vy;
  let b = c1 / c2;
  b = Math.max(0, Math.min(1, b));
  return [x12 + b * vx, y12 + b * vy];
};
const removeVertexFromLine = (lineGeoJson, vertexLngLat) => {
  const { absCoordIndex } = findCoordinateIndices(lineGeoJson, vertexLngLat);
  if (absCoordIndex !== -1) {
    lineGeoJson.geometry.coordinates.splice(absCoordIndex, 1);
    return true;
  }
  return false;
};
const removeVertexFromPolygon = (shapeGeoJson, vertexLngLat) => {
  const coordIndices = findCoordinateIndices(shapeGeoJson, vertexLngLat);
  if (coordIndices.absCoordIndex !== -1) {
    const coordinatesPath = [coordIndices.geometryIndex];
    const coordinates = get$2(
      shapeGeoJson.geometry.coordinates,
      coordinatesPath
    );
    const targetCoordIndex = coordinates.findIndex((coord) => isEqual(coord, vertexLngLat));
    if (coordinates.length <= 4) {
      shapeGeoJson.geometry.coordinates.splice(coordIndices.geometryIndex, 1);
      return true;
    }
    coordinates.splice(targetCoordIndex, 1);
    if (targetCoordIndex === 0) {
      coordinates[coordinates.length - 1] = [...coordinates[0]];
    }
    return true;
  }
  return false;
};
const removeVertexFromMultiPolygon = (shapeGeoJson, vertexLngLat) => {
  const coordIndices = findCoordinateIndices(shapeGeoJson, vertexLngLat);
  if (coordIndices.absCoordIndex !== -1) {
    const coordinatesPath = [
      coordIndices.multiFeatureIndex,
      coordIndices.geometryIndex
    ];
    const coordinates = get$2(
      shapeGeoJson.geometry.coordinates,
      coordinatesPath
    );
    const targetCoordIndex = coordinates.findIndex((coord) => isEqual(coord, vertexLngLat));
    if (coordinates.length <= 4) {
      coordinatesPath.pop();
      const coordinatesContainer = get$2(
        shapeGeoJson.geometry.coordinates,
        coordIndices.multiFeatureIndex
      );
      coordinatesContainer.splice(coordIndices.geometryIndex, 1);
      if (coordinatesContainer.length === 0) {
        shapeGeoJson.geometry.coordinates.splice(coordIndices.multiFeatureIndex, 1);
      }
      return true;
    }
    coordinates.splice(targetCoordIndex, 1);
    if (targetCoordIndex === 0) {
      coordinates[coordinates.length - 1] = [...coordinates[0]];
    }
    return true;
  }
  return false;
};
const removeVertexFromGeoJsonFeature = (shapeGeoJson, vertexLngLat) => {
  if (isLineStringFeature(shapeGeoJson)) {
    return removeVertexFromLine(shapeGeoJson, vertexLngLat);
  }
  if (isPolygonFeature(shapeGeoJson)) {
    return removeVertexFromPolygon(shapeGeoJson, vertexLngLat);
  }
  if (isMultiPolygonFeature(shapeGeoJson)) {
    return removeVertexFromMultiPolygon(shapeGeoJson, vertexLngLat);
  }
  return false;
};
const calculatePerimeter = (geoJson) => {
  let perimeter = 0;
  eachSegmentWithPath(geoJson, (segment) => {
    perimeter += distance(segment.start.coordinate, segment.end.coordinate, {
      units: "meters"
    });
  });
  return perimeter;
};
const convertToLineStringFeatureCollection = (sourceFeatureCollection) => {
  const targetFeatureCollection = {
    type: "FeatureCollection",
    features: []
  };
  sourceFeatureCollection.features.forEach((feature2) => {
    if (isLineStringFeature(feature2)) {
      targetFeatureCollection.features.push(feature2);
    } else if (isPolygonFeature(feature2)) {
      const lineFeature = singlePolygonToLine(feature2);
      if (isLineStringFeature(lineFeature)) {
        targetFeatureCollection.features.push(lineFeature);
      } else if (isMultiLineStringFeature(lineFeature)) {
        const line2 = multiLineStringToFeatureCollection(lineFeature);
        line2.features.forEach((item) => {
          targetFeatureCollection.features.push(item);
        });
      }
    } else if (isMultiPolygonFeature(feature2)) {
      const featureCollection2 = multiPolygonToLine(feature2);
      featureCollection2.features.forEach((lineFeature) => {
        if (isLineStringFeature(lineFeature)) {
          targetFeatureCollection.features.push(lineFeature);
        } else if (isMultiLineStringFeature(lineFeature)) {
          const line2 = multiLineStringToFeatureCollection(lineFeature);
          line2.features.forEach((item) => {
            targetFeatureCollection.features.push(item);
          });
        }
      });
    } else {
      log.warn("AutoTraceHelper.getFeaturesGeoJsonAsLines: Feature is not supported", feature2);
    }
  });
  return targetFeatureCollection;
};
const lngLatToGeoJsonPoint = (position, shape = "marker") => {
  return {
    type: "Feature",
    properties: {
      shape
    },
    geometry: {
      type: "Point",
      coordinates: position
    }
  };
};
const getGeoJsonCircle = ({
  center: center2,
  radius,
  steps = 80,
  properties = {}
}) => {
  const circleGeoJson = index_default$f(center2, radius, {
    steps,
    units: "meters",
    properties: {
      ...properties,
      [`${FEATURE_PROPERTY_PREFIX}shape`]: "circle",
      [`${FEATURE_PROPERTY_PREFIX}center`]: center2
    }
  });
  circleGeoJson.geometry.coordinates[0][0] = [...circleGeoJson.geometry.coordinates[0][0]];
  return circleGeoJson;
};
const getEllipseParameters = ({
  center: center2,
  xSemiAxisLngLat,
  rimLngLat
}) => {
  let xSemiAxis = distance(center2, xSemiAxisLngLat, { units: "meters" });
  if (xSemiAxis === 0) {
    xSemiAxis = 1;
  }
  const cwAngle = index_default$b(center2, xSemiAxisLngLat) - 90;
  let ySemiAxis = 0;
  if (rimLngLat) {
    const ccwAngle = -cwAngle;
    const ccwAngleRad = ccwAngle * Math.PI / 180;
    const ccwRimAngle = -(index_default$b(center2, rimLngLat) - 90);
    const ccwRimAngleRad = ccwRimAngle * Math.PI / 180;
    const dRim = distance(center2, rimLngLat, { units: "meters" });
    const px = dRim * Math.cos(ccwRimAngleRad);
    const py = dRim * Math.sin(ccwRimAngleRad);
    const pxReproj = px * Math.cos(ccwAngleRad) + py * Math.sin(ccwAngleRad);
    const pyReproj = px * -Math.sin(ccwAngleRad) + py * Math.cos(ccwAngleRad);
    const xPart = pxReproj * pxReproj / (xSemiAxis * xSemiAxis);
    ySemiAxis = Math.abs(pyReproj) / Math.sqrt(1 - xPart);
    if (isNaN(ySemiAxis)) {
      ySemiAxis = 0;
    }
  }
  return {
    xSemiAxis,
    ySemiAxis,
    angle: cwAngle
  };
};
const ellipseSteps = 80;
const getGeoJsonEllipse = ({
  center: center2,
  xSemiAxis,
  ySemiAxis,
  angle,
  properties = {}
}) => {
  const options = {
    steps: ellipseSteps,
    angle,
    units: "meters"
  };
  if (ySemiAxis === void 0 || ySemiAxis === 0) {
    const ellipseGeoJson2 = ellipse(center2, xSemiAxis, 1, options);
    return lineString(ellipseGeoJson2.geometry.coordinates[0].slice(0, 41), {
      __gm_shape: "line"
    });
  }
  const ellipseGeoJson = ellipse(center2, xSemiAxis, ySemiAxis, {
    ...options,
    properties: {
      ...properties,
      [`${FEATURE_PROPERTY_PREFIX}shape`]: "ellipse",
      [`${FEATURE_PROPERTY_PREFIX}center`]: center2,
      [`${FEATURE_PROPERTY_PREFIX}xSemiAxis`]: xSemiAxis,
      [`${FEATURE_PROPERTY_PREFIX}ySemiAxis`]: ySemiAxis,
      [`${FEATURE_PROPERTY_PREFIX}angle`]: angle
    }
  });
  ellipseGeoJson.geometry.coordinates[0][0] = [...ellipseGeoJson.geometry.coordinates[0][0]];
  return ellipseGeoJson;
};
const toPolygonAllowedShapes = [
  "circle",
  "ellipse",
  "rectangle"
];
class FeatureData {
  gm;
  id = "no-id";
  parent = null;
  markers;
  source;
  _geoJson = null;
  constructor(parameters) {
    this.gm = parameters.gm;
    this.id = parameters.id;
    this.source = parameters.source;
    this.parent = parameters.parent;
    this.markers = /* @__PURE__ */ new Map();
    const geoJson = {
      ...parameters.geoJsonShapeFeature,
      properties: {
        ...this.parseExtraProperties(parameters.geoJsonShapeFeature),
        ...this.parseGmShapeProperties(parameters.geoJsonShapeFeature)
      }
    };
    if (parameters.skipSourceUpdate) {
      this._geoJson = {
        ...geoJson,
        id: this.id
      };
      if (this.shape === "circle") {
        const shapeCentroid = geoJsonPointToLngLat(index_default$e(this._geoJson));
        this._geoJson.properties[`${FEATURE_PROPERTY_PREFIX}center`] = shapeCentroid;
      }
    } else {
      this.addGeoJson(geoJson);
    }
  }
  get shape() {
    const value = this.getShapeProperty("shape");
    if (typeof value === "string" && includesWithType(value, ALL_SHAPE_NAMES)) {
      return value;
    }
    throw new Error(`Wrong shape type: "${value}"`);
  }
  set shape(shape) {
    this.setShapeProperty("shape", shape);
  }
  get temporary() {
    return this.source.id === SOURCES.temporary;
  }
  get sourceName() {
    return this.source.id;
  }
  getShapeProperty(name, inputGeoJson) {
    const geoJsonProperties = inputGeoJson?.properties || this._geoJson?.properties || {};
    const validator = propertyValidators[name];
    const value = geoJsonProperties[`${FEATURE_PROPERTY_PREFIX}${name}`] ?? geoJsonProperties[name];
    if (validator && validator(value)) {
      return value;
    }
    return void 0;
  }
  setShapeProperty(name, value) {
    if (!this._geoJson) {
      log.error(`FeatureData.setShapeProperty(): geojson is not set`);
      return;
    }
    this._geoJson.properties[`${FEATURE_PROPERTY_PREFIX}${name}`] = value;
    this._updateAllProperties(this._geoJson.properties);
  }
  deleteShapeProperty(name) {
    if (!this._geoJson) {
      log.error(`FeatureData.deleteShapeProperty(): geojson is not set`);
      return;
    }
    this.removeProperties([`${FEATURE_PROPERTY_PREFIX}${name}`], false);
  }
  parseGmShapeProperties(geoJson) {
    const shape = this.getShapeProperty("shape", geoJson) || this.gm.features.getFeatureShapeByGeoJson(geoJson);
    if (!shape) {
      log.error(`FeatureData.importGmShapeProperties(): unknown shape: ${shape}`);
    }
    const allProperties = Object.fromEntries(
      typedKeys(propertyValidators).map((name) => [name, this.getShapeProperty(name, geoJson)])
    );
    const properties = {
      ...allProperties,
      id: this.id,
      shape: shape || void 0
    };
    return Object.fromEntries(
      typedKeys(properties).filter((fieldName) => properties[fieldName] !== void 0).map((fieldName) => [`${FEATURE_PROPERTY_PREFIX}${fieldName}`, properties[fieldName]])
    );
  }
  parseExtraProperties(geoJson) {
    const extraProperties = cloneDeep(geoJson.properties) || {};
    typedKeys(propertyValidators).forEach((name) => {
      delete extraProperties[name];
      delete extraProperties[`${FEATURE_PROPERTY_PREFIX}${name}`];
    });
    return extraProperties;
  }
  getGeoJson() {
    if (this._geoJson) {
      return this._geoJson;
    } else {
      throw new Error(`Missing GeoJSON for feature: "${this.shape}:${this.id}"`);
    }
  }
  addGeoJson(geoJson) {
    this._geoJson = {
      ...geoJson,
      id: this.id
    };
    if (this.shape === "circle") {
      const shapeCentroid = geoJsonPointToLngLat(index_default$e(this._geoJson));
      this._geoJson.properties[`${FEATURE_PROPERTY_PREFIX}center`] = shapeCentroid;
    }
    const add2 = (/* @__PURE__ */ new Map()).set(this.id, this._geoJson);
    this.gm.features.updateManager.updateSource({
      diff: { add: add2 },
      sourceName: this.sourceName
    });
  }
  removeGeoJson() {
    if (!this._geoJson) {
      throw new Error(`Feature not found: "${this.id}"`);
    }
    this.gm.features.updateManager.updateSource({
      diff: { remove: /* @__PURE__ */ new Set([this.id]) },
      sourceName: this.sourceName
    });
  }
  removeMarkers() {
    this.markers.forEach((markerData) => {
      if (markerData.instance instanceof BaseDomMarker) {
        markerData.instance.remove();
      } else {
        this.gm.features.delete(markerData.instance);
      }
    });
    this.markers = /* @__PURE__ */ new Map();
  }
  /**
   * Updates the geometry of this feature.
   *
   * @param geometry - The new geometry for the feature
   *
   * @example
   * // Update a marker's position
   * feature.updateGeometry({ type: 'Point', coordinates: [10, 52] });
   *
   * // Update a polygon's coordinates
   * feature.updateGeometry({
   *   type: 'Polygon',
   *   coordinates: [[[0, 0], [1, 0], [1, 1], [0, 1], [0, 0]]]
   * });
   */
  updateGeometry(geometry) {
    const featureGeoJson = this.getGeoJson();
    if (!featureGeoJson) {
      throw new Error(`Feature not found: "${this.id}"`);
    }
    this._geoJson = { ...featureGeoJson, geometry };
    const update = (/* @__PURE__ */ new Map()).set(this.id, {
      id: this.id,
      newGeometry: this._geoJson.geometry
    });
    this.gm.features.updateManager.updateSource({
      diff: { update },
      sourceName: this.sourceName
    });
  }
  /**
   * @deprecated Use `updateGeometry()` instead.
   */
  updateGeoJsonGeometry(geometry) {
    this.updateGeometry(geometry);
  }
  /**
   * Updates custom properties on this feature. Properties are merged with existing ones.
   *
   * Internal Geoman properties (prefixed with `gm_`) cannot be modified through this method
   * and will be preserved.
   *
   * @param properties - Object containing properties to update or delete (set to undefined)
   *
   * @example
   * // Add or update properties
   * feature.updateProperties({ color: 'red', size: 10 });
   *
   * // Delete a property
   * feature.updateProperties({ color: undefined });
   *
   * // Mix of updates and deletions
   * feature.updateProperties({ color: 'blue', oldProp: undefined });
   */
  updateProperties(properties) {
    if (!this._geoJson) {
      throw new Error(`Feature not found: "${this.id}"`);
    }
    const nextProperties = { ...this._geoJson.properties };
    const featureDiff = {
      id: this.id,
      addOrUpdateProperties: [],
      removeProperties: []
    };
    const mandatoryProperties = this.parseGmShapeProperties(this._geoJson);
    const protectedKeys = new Set(Object.keys(mandatoryProperties));
    for (const [key, value] of Object.entries(properties)) {
      if (protectedKeys.has(key)) {
        continue;
      }
      if (typeof value === "undefined") {
        delete nextProperties[key];
        featureDiff.removeProperties?.push(key);
      } else {
        nextProperties[key] = value;
        featureDiff.addOrUpdateProperties?.push({ key, value });
      }
    }
    const update = (/* @__PURE__ */ new Map()).set(this.id, featureDiff);
    this._geoJson.properties = nextProperties;
    this.gm.features.updateManager.updateSource({
      diff: { update },
      sourceName: this.sourceName
    });
  }
  /**
   * Replaces all custom properties on this feature. Existing custom properties are removed
   * and replaced with the provided ones.
   *
   * Internal Geoman properties (prefixed with `gm_`) cannot be modified and will be preserved.
   *
   * @param properties - Object containing the new properties (replaces all existing custom properties)
   *
   * @example
   * // Replace all custom properties
   * feature.setProperties({ name: 'New Feature', category: 'poi' });
   */
  setProperties(properties) {
    if (!this._geoJson) {
      throw new Error(`Feature not found: "${this.id}"`);
    }
    const mandatoryProperties = this.parseGmShapeProperties(this._geoJson);
    const filteredProperties = {};
    for (const [key, value] of Object.entries(properties)) {
      if (value !== void 0 && !(key in mandatoryProperties)) {
        filteredProperties[key] = value;
      }
    }
    this._geoJson.properties = {
      ...filteredProperties,
      ...mandatoryProperties
    };
    const add2 = (/* @__PURE__ */ new Map()).set(this.id, this._geoJson);
    this.gm.features.updateManager.updateSource({
      diff: { add: add2 },
      sourceName: this.sourceName
    });
  }
  removeProperties(fieldNames, preserveInternals = true) {
    if (!this._geoJson) {
      throw new Error(`Feature not found: "${this.id}"`);
    }
    const deniedKeys = preserveInternals ? typedKeys(propertyValidators).map((fieldName) => `${FEATURE_PROPERTY_PREFIX}${fieldName}`) : [];
    const keysToRemove = fieldNames.filter((fieldName) => !deniedKeys.includes(fieldName));
    const nextProperties = { ...this._geoJson.properties };
    keysToRemove.forEach((key) => {
      delete nextProperties[key];
    });
    const update = (/* @__PURE__ */ new Map()).set(this.id, {
      id: this.id,
      removeProperties: keysToRemove,
      // issue with MapLibre 5.15 keep empty but present
      addOrUpdateProperties: []
    });
    this._geoJson.properties = nextProperties;
    this.gm.features.updateManager.updateSource({
      diff: { update },
      sourceName: this.sourceName
    });
  }
  /**
   * Internal method to update all properties including Geoman system properties.
   * This should only be used by internal Geoman code (edit modes, draw modes, etc.).
   *
   * @internal
   * @param properties - Properties to merge with existing ones
   */
  _updateAllProperties(properties) {
    if (!this._geoJson) {
      throw new Error(`Feature not found: "${this.id}"`);
    }
    this._geoJson.properties = { ...this._geoJson.properties, ...properties };
    const update = (/* @__PURE__ */ new Map()).set(this.id, {
      id: this.id,
      addOrUpdateProperties: Object.entries(properties || {}).map(([key, value]) => ({
        key,
        value
      }))
    });
    this.gm.features.updateManager.updateSource({
      diff: { update },
      sourceName: this.sourceName
    });
  }
  /**
   * @deprecated Use `updateProperties()` instead. Set property value to `undefined` to delete it.
   */
  updateGeoJsonProperties(properties) {
    this._updateAllProperties(properties);
  }
  /**
   * @deprecated Use `setProperties()` instead.
   */
  setGeoJsonCustomProperties(properties) {
    this.setProperties(properties || {});
  }
  /**
   * @deprecated Use `updateProperties()` instead.
   */
  updateGeoJsonCustomProperties(properties) {
    this.updateProperties(properties || {});
  }
  /**
   * @deprecated Use `updateProperties({ propName: undefined })` instead.
   */
  deleteGeoJsonCustomProperties(fieldNames) {
    const deleteProps = {};
    for (const fieldName of fieldNames) {
      deleteProps[fieldName] = void 0;
    }
    this.updateProperties(deleteProps);
  }
  convertToPolygon() {
    if (this.isConvertableToPolygon()) {
      this.shape = "polygon";
      this.deleteShapeProperty("center");
      this.deleteShapeProperty("angle");
      this.deleteShapeProperty("xSemiAxis");
      this.deleteShapeProperty("ySemiAxis");
      return true;
    }
    return false;
  }
  isConvertableToPolygon() {
    return toPolygonAllowedShapes.includes(this.shape);
  }
  // changeSource({ sourceName, atomic }: { sourceName: FeatureSourceName; atomic: boolean }) {
  //   if (atomic) {
  //     this.gm.features.updateManager.withAtomicSourcesUpdate(() =>
  //       this.actualChangeSource({ sourceName, atomic }),
  //     );
  //   } else {
  //     this.actualChangeSource({ sourceName, atomic });
  //   }
  // }
  changeSource({ sourceName, atomic }) {
    if (this.source.id === sourceName) {
      log.error(
        `FeatureData.changeSource: feature "${this.id}" already has the source "${sourceName}"`
      );
      return;
    }
    const source2 = this.gm.features.sources[sourceName];
    if (!source2) {
      log.error(`FeatureData.changeSource: missing source "${sourceName}"`);
      return;
    }
    const shapeGeoJson = this.getGeoJson();
    if (!shapeGeoJson) {
      log.error("FeatureData.changeSource: missing shape GeoJSON");
      return;
    }
    this.removeGeoJson();
    this.source = source2;
    this.addGeoJson(shapeGeoJson);
    this.markers.forEach((markerData) => {
      if (markerData.instance instanceof FeatureData) {
        markerData.instance.changeSource({
          sourceName: sourceName === SOURCES.temporary ? SOURCES.temporary : SOURCES.internal,
          atomic
        });
      }
    });
  }
  fireFeatureUpdatedEvent({ mode }) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:feature_updated`,
      level: "system",
      actionType: "edit",
      action: "feature_updated",
      mode,
      sourceFeatures: [this],
      targetFeatures: [this],
      markerData: null
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
  delete() {
    this.removeGeoJson();
    this.removeMarkers();
  }
}
const isGmHelperEvent = (payload) => {
  return isGmEvent(payload) && payload.actionType === "helper";
};
const isGmGeofencingViolationEvent = (payload) => {
  return isGmEvent(payload) && payload.name === `${GM_SYSTEM_PREFIX}:helper:geofencing_violation`;
};
class BaseAction {
  gm;
  options = {};
  settings = {};
  actions = {};
  flags = {
    featureCreateAllowed: true,
    featureUpdateAllowed: true,
    actionInProgress: false
  };
  internalEventHandlers = {
    [`${GM_SYSTEM_PREFIX}:helper`]: this.handleHelperEvent.bind(this)
  };
  constructor(gm) {
    this.gm = gm;
  }
  get snappingHelper() {
    return this.gm.actionInstances.helper__snapping || null;
  }
  startAction() {
    this.gm.events.bus.attachEvents(this.internalEventHandlers);
    this.gm.events.bus.attachEvents(this.eventHandlers);
    this.onStartAction();
  }
  endAction() {
    this.onEndAction();
    this.gm.events.bus.detachEvents(this.eventHandlers);
    this.gm.events.bus.detachEvents(this.internalEventHandlers);
  }
  getOptionValue(name) {
    const option = this.options[name];
    if (!option) {
      throw new Error(`Option ${name} not found`);
    }
    if (["toggle", "hidden"].includes(option.type)) {
      return option.value;
    } else if (option.type === "select") {
      return option.value.value;
    } else {
      throw new Error(`Unknown option type: ${JSON.stringify(option)}`);
    }
  }
  getSettingValue(name) {
    if (name in this.settings) {
      return this.settings[name];
    }
    return void 0;
  }
  applyOptionValue(name, value) {
    const option = this.options[name];
    if (!option) {
      log.error("Option not found", name, value);
      return;
    }
    if (option.type === "toggle" && typeof value === "boolean") {
      option.value = value;
    } else if (option.type === "select") {
      const choiceItemValue = option.choices.find((item) => item.value === value);
      if (choiceItemValue) {
        option.value = choiceItemValue;
      }
    } else if (option.type === "hidden") {
      option.value = value;
    } else {
      log.error("Can't apply option value", name, value, option);
    }
  }
  handleHelperEvent(event) {
    if (isGmGeofencingViolationEvent(event)) {
      return this.handleGeofencingViolationEvent(event);
    }
    return { next: true };
  }
  handleGeofencingViolationEvent(event) {
    if (event.actionType === "draw") {
      this.flags.featureCreateAllowed = false;
    } else if (event.actionType === "edit") {
      this.flags.featureUpdateAllowed = false;
    }
    return { next: true };
  }
}
const isGmDrawEvent = (payload) => {
  return isGmEvent(payload) && payload.actionType === "draw";
};
const isGmDrawShapeEvent = (payload) => {
  return isGmEvent(payload) && (payload.name === `${GM_SYSTEM_PREFIX}:draw:shape` || payload.name === `${GM_SYSTEM_PREFIX}:draw:shape_with_data`);
};
const isGmDrawLineDrawerEvent = (payload) => {
  return isGmDrawShapeEvent(payload) && payload.variant === "line_drawer";
};
const isGmDrawFreehandDrawerEvent = (payload) => {
  return isGmDrawShapeEvent(payload) && payload.variant === "freehand_drawer";
};
class BaseDraw extends BaseAction {
  actionType = "draw";
  shape = null;
  featureData = null;
  saveFeature() {
    if (this.featureData) {
      const featureGeoJson = this.featureData.getGeoJson();
      this.removeTmpFeature();
      this.gm.features.createFeature({
        sourceName: SOURCES.main,
        shapeGeoJson: featureGeoJson
      });
    } else {
      log.error("BaseDraw.saveFeature: no featureData to save");
    }
  }
  removeTmpFeature() {
    if (this.featureData) {
      if (!this.featureData.temporary) {
        log.error("Not a temporary feature to remove", this.featureData);
      }
      this.gm.features.delete(this.featureData);
      this.featureData = null;
    }
  }
  fireBeforeFeatureCreate({
    geoJsonFeatures,
    forceMode = void 0
  }) {
    this.flags.featureCreateAllowed = true;
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:feature:before_create`,
      level: "system",
      actionType: "draw",
      mode: forceMode || this.mode,
      action: "before_create",
      geoJsonFeatures
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:${this.actionType}`, payload);
  }
  fireMarkerPointerStartEvent() {
    if (!this.gm.markerPointer.marker || !this.shape) {
      return;
    }
    const marker2 = this.gm.markerPointer.marker;
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      variant: null,
      actionType: "draw",
      mode: this.shape,
      action: "start",
      markerData: {
        type: "dom",
        instance: marker2,
        position: {
          coordinate: marker2.getLngLat(),
          path: [-1]
        }
      },
      featureData: this.featureData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
  fireMarkerPointerUpdateEvent() {
    if (!this.gm.markerPointer.marker || !this.shape) {
      return;
    }
    const marker2 = this.gm.markerPointer.marker;
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      variant: null,
      actionType: "draw",
      mode: this.shape,
      action: "update",
      markerData: {
        type: "dom",
        instance: marker2,
        position: {
          coordinate: marker2.getLngLat(),
          path: [-1]
        }
      },
      featureData: this.featureData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
  fireMarkerPointerFinishEvent() {
    if (!this.shape) {
      return;
    }
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape`,
      level: "system",
      variant: null,
      actionType: "draw",
      mode: this.shape,
      action: "finish"
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
  forwardLineDrawerEvent(payload) {
    if (!isGmDrawLineDrawerEvent(payload) || !this.shape) {
      return { next: true };
    }
    if (payload.action === "start" || payload.action === "update") {
      const eventData = {
        name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
        level: "system",
        actionType: "draw",
        mode: this.shape,
        variant: null,
        action: payload.action,
        featureData: payload.featureData,
        markerData: payload.markerData
      };
      this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, eventData);
    } else if (payload.action === "finish" || payload.action === "cancel") {
      const eventData = {
        name: `${GM_SYSTEM_PREFIX}:draw:shape`,
        level: "system",
        actionType: "draw",
        mode: this.shape,
        variant: null,
        action: payload.action
      };
      this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, eventData);
    }
    return { next: true };
  }
  fireStartEvent(featureData, markerData = null) {
    if (!this.shape) {
      return;
    }
    const event = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: this.shape,
      variant: null,
      action: "start",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, event);
  }
  fireUpdateEvent(featureData, markerData = null) {
    if (!this.shape) {
      return;
    }
    const event = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: this.shape,
      variant: null,
      action: "update",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, event);
  }
  fireFinishEvent() {
    if (!this.shape) {
      return;
    }
    const event = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape`,
      level: "system",
      actionType: "draw",
      mode: this.shape,
      variant: null,
      action: "finish"
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, event);
  }
}
class BaseCircle extends BaseDraw {
  mode = "circle";
  shape = "circle";
  eventHandlers = {
    mousemove: this.onMouseMove.bind(this),
    click: this.onMouseClick.bind(this)
  };
  circleCenterPoint = null;
  circleCenterLngLat = null;
  onStartAction() {
    this.gm.markerPointer.enable();
  }
  onEndAction() {
    this.removeTmpFeature();
    this.gm.markerPointer.disable();
    this.fireFinishEvent();
  }
  getFeatureGeoJson(position) {
    return {
      type: "Feature",
      properties: {
        __gm_shape: this.shape
      },
      geometry: {
        type: "Point",
        coordinates: position
      }
    };
  }
  getControlMarkerData() {
    const marker2 = this.gm.markerPointer.marker;
    if (!marker2) {
      return null;
    }
    return {
      type: "dom",
      instance: marker2,
      position: {
        coordinate: marker2.getLngLat(),
        path: [-1]
      }
    };
  }
  createFeature() {
    const featureData = this.gm.features.createFeature({
      shapeGeoJson: this.getFeatureGeoJson(this.circleCenterLngLat || [0, 0]),
      sourceName: SOURCES.temporary
    });
    if (featureData && this.circleCenterLngLat) {
      featureData.setShapeProperty("center", this.circleCenterLngLat);
    }
    return featureData;
  }
  createMarker() {
    const element = document.createElement("div");
    element.innerHTML = circleMarker;
    const svgElement = element.firstChild;
    svgElement.style.color = "#278cda";
    svgElement.style.width = "28px";
    svgElement.style.height = "28px";
    svgElement.style.pointerEvents = "none";
    return this.gm.mapAdapter.createDomMarker(
      {
        draggable: false,
        anchor: "center",
        element: svgElement
      },
      [0, 0]
    );
  }
}
class DrawCircleMarker extends BaseCircle {
  mode = "circle_marker";
  shape = "circle_marker";
  onStartAction() {
    this.gm.markerPointer.enable({
      customMarker: this.createMarker()
    });
  }
  onEndAction() {
    this.fireMarkerPointerFinishEvent();
    super.onEndAction();
  }
  onMouseMove() {
    this.fireMarkerPointerUpdateEvent();
    return { next: true };
  }
  onMouseClick(event) {
    this.gm.features.clearSelection();
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    this.fireBeforeFeatureCreate({ geoJsonFeatures: [this.getFeatureGeoJson(lngLat)] });
    if (this.flags.featureCreateAllowed) {
      this.featureData = this.createFeature();
      this.circleCenterLngLat = lngLat;
      this.circleCenterPoint = this.gm.mapAdapter.project(this.circleCenterLngLat);
      this.updateFeaturePosition(this.circleCenterLngLat);
      this.saveFeature();
    }
    return { next: false };
  }
  updateFeaturePosition(lngLat) {
    if (!this.featureData) {
      return;
    }
    const shapeGeoJson = this.getFeatureGeoJson(lngLat);
    this.featureData.updateGeoJsonGeometry(shapeGeoJson.geometry);
  }
}
const isTouchScreen = () => {
  if (window.PointerEvent && "maxTouchPoints" in navigator) {
    if (navigator.maxTouchPoints > 0) {
      return !window.matchMedia("(hover: hover)").matches;
    }
    return false;
  }
  if (matchMedia("(hover: none)").matches) {
    return true;
  }
  if ("ontouchstart" in window) {
    return true;
  }
  return "msMaxTouchPoints" in navigator && typeof navigator.msMaxTouchPoints === "number" && navigator.msMaxTouchPoints > 0;
};
const convertToThrottled = (methods, context, wait = 10) => {
  const throttledMethods = { ...methods };
  typedKeys(methods).forEach((key) => {
    const method = methods[key];
    if (typeof method === "function") {
      throttledMethods[key] = throttle(method.bind(context), wait, {
        leading: true,
        trailing: false
      });
    } else {
      log.error("convertToThrottled: item is not a function", methods[key]);
    }
  });
  return throttledMethods;
};
const convertToDebounced = (methods, context, wait = 10) => {
  const debouncedMethods = { ...methods };
  typedKeys(methods).forEach((key) => {
    const method = methods[key];
    if (typeof method === "function") {
      debouncedMethods[key] = debounce(method.bind(context), wait, {
        leading: false,
        trailing: true
      });
    } else {
      log.error("convertToDebounced: item is not a function", methods[key]);
    }
  });
  return debouncedMethods;
};
const withPromiseTimeoutRace = async (promise, errorMessage) => {
  const defaultErrorMessage = "Promise race timeout";
  await Promise.race([
    promise,
    new Promise((_, reject) => {
      setTimeout(
        () => reject(
          new Error(
            `Timeout ${LOAD_TIMEOUT / 1e3} seconds: ${errorMessage || defaultErrorMessage}`
          )
        ),
        LOAD_TIMEOUT
      );
    })
  ]);
};
const pointerEvents = [
  "click",
  "dblclick",
  "mousedown",
  "mouseup",
  "mousemove",
  "mouseenter",
  "mouseleave",
  "mouseover",
  "mouseout",
  "contextmenu",
  "touchstart",
  "touchend",
  "touchmove",
  "touchcancel"
];
const baseMapEventNames = ["load"];
function isPointerEventName(key) {
  return pointerEvents.includes(key);
}
function isBaseMapEventName(key) {
  return baseMapEventNames.includes(key);
}
const isMapWithOnceMethod = (map) => {
  return !!(map && typeof map === "object" && "once" in map && typeof map.once === "function");
};
const isMapPointerEvent = (event, options = { warning: false }) => {
  if (!event) {
    if (options.warning) {
      log.warn("Empty event", event);
    }
    return false;
  }
  const isCursorEvent = typeof event === "object" && "lngLat" in event && "point" in event && "type" in event && "originalEvent" in event && typeof event.type === "string" && pointerEvents.includes(event.type);
  if (!isCursorEvent && options.warning) {
    log.warn("Not a pointer event", event);
  }
  return isCursorEvent;
};
const isPointerEventWithModifiers = (event) => {
  return event.originalEvent.ctrlKey || event.originalEvent.shiftKey || event.originalEvent.altKey || event.originalEvent.metaKey;
};
const isPartialLayer = (object) => {
  const allowedTypes = ["symbol", "fill", "line", "circle"];
  return !!object && typeof object === "object" && "type" in object && allowedTypes.includes(object.type);
};
class DrawCircle extends BaseCircle {
  mode = "circle";
  shape = "circle";
  throttledMethods = convertToThrottled(
    {
      updateFeatureGeoJson: this.updateFeatureGeoJson
    },
    this,
    this.gm.options.settings.throttlingDelay
  );
  onMouseClick(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    if (this.circleCenterPoint && this.circleCenterLngLat) {
      this.fireBeforeFeatureCreate({
        geoJsonFeatures: [this.getCircleGeoJson(this.circleCenterLngLat, lngLat)]
      });
      if (this.flags.featureCreateAllowed) {
        this.saveCircleFeature(lngLat);
        this.circleCenterLngLat = null;
        this.circleCenterPoint = null;
        this.fireFinishEvent();
      }
    } else {
      this.gm.features.clearSelection();
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [this.getFeatureGeoJson(lngLat)] });
      if (this.flags.featureCreateAllowed) {
        this.circleCenterLngLat = lngLat;
        this.circleCenterPoint = this.gm.mapAdapter.project(this.circleCenterLngLat);
        this.featureData = this.createFeature();
        const markerData = this.getControlMarkerData();
        if (this.featureData && markerData) {
          this.fireStartEvent(this.featureData, markerData);
        }
      }
    }
    return { next: false };
  }
  onMouseMove() {
    if (this.circleCenterLngLat && this.gm.markerPointer.marker) {
      const markerLngLat = this.gm.markerPointer.marker.getLngLat();
      this.fireBeforeFeatureCreate({
        geoJsonFeatures: [this.getCircleGeoJson(this.circleCenterLngLat, markerLngLat)]
      });
      if (this.flags.featureCreateAllowed) {
        this.throttledMethods.updateFeatureGeoJson(markerLngLat);
      }
    }
    if (!this.circleCenterPoint) {
      this.fireMarkerPointerUpdateEvent();
    }
    return { next: false };
  }
  updateFeatureGeoJson(rimLngLat) {
    if (this.featureData && this.circleCenterLngLat) {
      const featureGeoJson = this.getCircleGeoJson(this.circleCenterLngLat, rimLngLat);
      this.featureData.updateGeoJsonGeometry(featureGeoJson.geometry);
      const markerData = this.getControlMarkerData();
      if (markerData) {
        this.fireUpdateEvent(this.featureData, markerData);
      }
    }
  }
  saveCircleFeature(eventLngLat) {
    if (!this.circleCenterLngLat) {
      return;
    }
    if (this.featureData) {
      const lngLat = this.gm.markerPointer.marker?.getLngLat() || eventLngLat;
      this.updateFeatureGeoJson(lngLat);
      this.featureData.setShapeProperty("center", this.circleCenterLngLat);
      if (this.isFeatureGeoJsonValid()) {
        this.saveFeature();
      } else {
        this.removeTmpFeature();
      }
    }
  }
  isFeatureGeoJsonValid() {
    if (!this.featureData) {
      return false;
    }
    return allCoordinatesEqual(this.featureData.getGeoJson());
  }
  getCircleGeoJson(center2, endLngLat) {
    const radius = this.gm.mapAdapter.getDistance(center2, endLngLat);
    const circleGeoJson = getGeoJsonCircle({ center: center2, radius });
    return {
      ...circleGeoJson,
      properties: {
        shape: this.shape
      }
    };
  }
}
class BaseHelper extends BaseAction {
  actionType = "helper";
}
const isSnapGuidesHelper = (instance) => {
  return !!instance && typeof instance === "object" && instance instanceof BaseHelper && "removeSnapGuides" in instance && "updateSnapGuides" in instance && instance.mode === "snap_guides" && typeof instance.removeSnapGuides === "function" && typeof instance.updateSnapGuides === "function";
};
const isAutoTraceHelper = (instance) => {
  return !!instance && typeof instance === "object" && instance instanceof BaseHelper && "getShortestPath" in instance && instance.mode === "auto_trace" && typeof instance.getShortestPath === "function";
};
const isPinHelper = (instance) => {
  return !!instance && typeof instance === "object" && instance instanceof BaseHelper && "getSharedMarkers" in instance && instance.mode === "pin" && typeof instance.getSharedMarkers === "function";
};
function lineToPolygon(lines, options = {}) {
  var _a, _b, _c;
  var properties = options.properties;
  var autoComplete = (_a = options.autoComplete) != null ? _a : true;
  var orderCoords = (_b = options.orderCoords) != null ? _b : true;
  var mutate = (_c = options.mutate) != null ? _c : false;
  if (!mutate) {
    lines = clone$1(lines);
  }
  switch (lines.type) {
    case "FeatureCollection":
      var coords = [];
      lines.features.forEach(function(line2) {
        coords.push(
          getCoords(lineStringToPolygon(line2, {}, autoComplete, orderCoords))
        );
      });
      return multiPolygon(coords, properties);
    default:
      return lineStringToPolygon(lines, properties, autoComplete, orderCoords);
  }
}
function lineStringToPolygon(line2, properties, autoComplete, orderCoords) {
  properties = properties ? properties : line2.type === "Feature" ? line2.properties : {};
  var geom = getGeom(line2);
  var coords = geom.coordinates;
  var type = geom.type;
  if (!coords.length) throw new Error("line must contain coordinates");
  switch (type) {
    case "LineString":
      if (autoComplete) coords = autoCompleteCoords(coords);
      return polygon([coords], properties);
    case "MultiLineString":
      var multiCoords = [];
      var largestArea = 0;
      coords.forEach(function(coord) {
        if (autoComplete) coord = autoCompleteCoords(coord);
        if (orderCoords) {
          var area2 = calculateArea$1(bbox$1(lineString(coord)));
          if (area2 > largestArea) {
            multiCoords.unshift(coord);
            largestArea = area2;
          } else multiCoords.push(coord);
        } else {
          multiCoords.push(coord);
        }
      });
      return polygon(multiCoords, properties);
    default:
      throw new Error("geometry type " + type + " is not supported");
  }
}
function autoCompleteCoords(coords) {
  var first = coords[0];
  var x12 = first[0];
  var y12 = first[1];
  var last = coords[coords.length - 1];
  var x2 = last[0];
  var y2 = last[1];
  if (x12 !== x2 || y12 !== y2) {
    coords.push(first);
  }
  return coords;
}
function calculateArea$1(bbox2) {
  var west = bbox2[0];
  var south = bbox2[1];
  var east = bbox2[2];
  var north = bbox2[3];
  return Math.abs(west - east) * Math.abs(south - north);
}
var index_default$9 = lineToPolygon;
class LineDrawer extends BaseDraw {
  mode = "line";
  snappingKey = "line_drawer";
  drawOptions;
  shapeLngLats = [];
  throttledMethods = convertToThrottled(
    {
      onMouseMove: this.onMouseMove
    },
    this,
    this.gm.options.settings.throttlingDelay
  );
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:helper`]: this.handleGmHelperEvent.bind(this),
    click: this.onMouseClick.bind(this),
    mousemove: this.throttledMethods.onMouseMove.bind(this)
  };
  drawerEventHandlers = {
    firstMarkerClick: null,
    lastMarkerClick: null,
    nMarkerClick: null
  };
  constructor(gm, options = {
    snappingMarkers: "none",
    targetShape: "line"
  }) {
    super(gm);
    this.drawOptions = options;
  }
  get snapGuidesInstance() {
    const instance = this.gm.actionInstances.helper__snap_guides;
    return isSnapGuidesHelper(instance) ? instance : null;
  }
  get autoTraceEnabled() {
    return this.gm.options.controls.helper.auto_trace?.active || false;
  }
  get autoTraceHelperInstance() {
    if (!this.autoTraceEnabled) {
      return null;
    }
    return Object.values(this.gm.actionInstances).find(isAutoTraceHelper) || null;
  }
  onStartAction() {
    this.gm.markerPointer.enable();
  }
  onEndAction() {
    this.gm.markerPointer.disable();
    this.endShape();
    this.snapGuidesInstance?.removeSnapGuides();
    this.clearDrawerHandlers();
  }
  clearDrawerHandlers() {
    this.drawerEventHandlers.firstMarkerClick = null;
    this.drawerEventHandlers.lastMarkerClick = null;
    this.drawerEventHandlers.nMarkerClick = null;
  }
  handleGmHelperEvent(event) {
    if (!isGmHelperEvent(event)) {
      log.error("LineDrawer.handleGmHelperEvent: invalid event", event);
      return { next: true };
    }
    if (event.mode === "snap_guides" && event.action === "mode_start") {
      this.updateSnapGuides();
    }
    return { next: true };
  }
  updateSnapGuides() {
    if (this.snapGuidesInstance) {
      const shapeGeoJson = this.featureData?.getGeoJson();
      if (shapeGeoJson) {
        shapeGeoJson.geometry.coordinates.pop();
      }
      this.snapGuidesInstance.updateSnapGuides(
        shapeGeoJson || null,
        this.shapeLngLats.at(-1) || null,
        true
      );
    }
  }
  on(eventType, handler) {
    this.drawerEventHandlers[eventType] = handler;
  }
  onMouseClick(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    if (this.featureData) {
      const markerInfo = this.getClickedMarkerInfo(event);
      this.handleNextVertex(lngLat, markerInfo);
    } else if (this.isFeatureAllowed(lngLatToGeoJsonPoint(lngLat))) {
      this.gm.features.clearSelection();
      this.startShape(lngLat);
    }
    this.updateSnapGuides();
    return { next: true };
  }
  handleNextVertex(lngLat, clickedMarkerInfo) {
    if (!this.featureData) {
      log.error("LineDrawer.handleNextVertex: no featureData");
      return;
    }
    const totalMarkersCount = this.featureData.markers.size;
    const eventData = this.getMarkerClickEventData(clickedMarkerInfo.index);
    if (clickedMarkerInfo.index < totalMarkersCount - 1) {
      this.addPoint(lngLat, clickedMarkerInfo);
    }
    if (clickedMarkerInfo.index === -1) {
      return;
    }
    if (clickedMarkerInfo.index === 0) {
      this.drawerEventHandlers.firstMarkerClick?.(eventData);
    } else if (clickedMarkerInfo.index > 0 && clickedMarkerInfo.index === totalMarkersCount - 1) {
      this.drawerEventHandlers.lastMarkerClick?.(eventData);
    }
    if (clickedMarkerInfo.index >= 0) {
      this.drawerEventHandlers.nMarkerClick?.(eventData);
    }
  }
  getMarkerClickEventData(markerIndex) {
    const featureGeoJson = this.getFeatureGeoJson({ withControlMarker: false });
    return {
      markerIndex,
      shapeCoordinates: this.getShapeCoordinates({ withControlMarker: false }),
      geoJson: featureGeoJson,
      bounds: getGeoJsonBounds(featureGeoJson)
    };
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    if (this.featureData && this.shapeLngLats.length) {
      this.updateFeatureSource();
    }
    return { next: true };
  }
  startShape(startLngLat) {
    this.shapeLngLats = [startLngLat];
    this.featureData = this.gm.features.createFeature({
      shapeGeoJson: this.getFeatureGeoJson({ withControlMarker: true }),
      sourceName: SOURCES.temporary
    });
    const markerData = {
      type: "dom",
      instance: this.createMarker(startLngLat),
      position: {
        coordinate: startLngLat,
        path: ["geometry", "coordinates", 0]
      }
    };
    if (this.featureData) {
      this.featureData.markers.set(markerData.position.path.join("."), markerData);
      this.setSnapping();
      this.fireStartEvent(this.featureData, markerData);
    }
    this.gm.mapAdapter.disableMapInteractions(["doubleClickZoom"]);
  }
  endShape() {
    const featureGeoJson = this.getFeatureGeoJson({ withControlMarker: false });
    this.removeSnapping();
    this.removeTmpFeature();
    this.shapeLngLats = [];
    this.gm.mapAdapter.enableMapInteractions(["doubleClickZoom"]);
    this.fireStopEvent(featureGeoJson);
  }
  setSnapping() {
    if (!this.snappingHelper) {
      return;
    }
    const snappingMarkers = this.drawOptions.snappingMarkers;
    if (snappingMarkers === "none") {
      this.snappingHelper.setCustomSnappingCoordinates(this.snappingKey, []);
    } else if (snappingMarkers === "all" && this.shapeLngLats.length) {
      this.snappingHelper.setCustomSnappingCoordinates(this.snappingKey, this.shapeLngLats);
    } else if (snappingMarkers === "first" && this.shapeLngLats.length) {
      this.snappingHelper.setCustomSnappingCoordinates(this.snappingKey, [this.shapeLngLats[0]]);
    } else if (snappingMarkers === "last" && this.shapeLngLats.length) {
      this.snappingHelper.setCustomSnappingCoordinates(this.snappingKey, [
        this.shapeLngLats[this.shapeLngLats.length - 1]
      ]);
    } else {
      log.error("LineDrawer.setSnapping: invalid data", snappingMarkers, this.shapeLngLats);
    }
  }
  removeSnapping() {
    if (!this.snappingHelper) {
      return;
    }
    this.snappingHelper.clearCustomSnappingCoordinates(this.snappingKey);
  }
  getClickedMarkerInfo(event) {
    if (!this.featureData) {
      return { index: -1, path: null };
    }
    let markerIndex = 0;
    let markerPath = null;
    try {
      this.featureData.markers.forEach((markerData, key) => {
        if (markerData.instance instanceof BaseDomMarker) {
          const markerElement = markerData.instance.getElement() || null;
          const eventTarget = event.originalEvent.target;
          const eventElement = eventTarget instanceof Element ? eventTarget : null;
          if (markerElement && markerElement.contains(eventElement)) {
            markerPath = key;
            throw new Error("stop");
          }
        }
        markerIndex += 1;
      });
    } catch {
      if (markerPath) {
        return { index: markerIndex, path: markerPath };
      }
    }
    return { index: -1, path: null };
  }
  addPoint(newLngLat, existingMarkerInfo) {
    const featureData = this.featureData;
    if (!featureData) {
      log.error("LineDrawer.addPoint: no featureData");
      return;
    }
    const addedLngLats = this.getAddedLngLats(newLngLat, existingMarkerInfo);
    const featureGeoJson = this.getFeatureGeoJsonWithType({
      withControlMarker: true,
      coordinates: this.shapeLngLats.concat(addedLngLats)
    });
    if (this.isFeatureAllowed(featureGeoJson)) {
      addedLngLats.forEach((lngLat) => {
        this.shapeLngLats.push(lngLat);
        const markerData = this.addMarker(lngLat, featureData);
        this.fireUpdateEvent(featureData, markerData);
      });
      this.updateFeatureSource();
    }
  }
  isFeatureAllowed(featureGeoJson) {
    if (this.gm.getActiveDrawModes().length) {
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [featureGeoJson] });
      return this.flags.featureCreateAllowed;
    }
    return true;
  }
  getAddedLngLats(newLngLat, existingMarkerInfo) {
    const featureData = this.featureData;
    if (!featureData) {
      log.error("LineDrawer.getCurrentLngLats: no featureData");
      return [];
    }
    const targetLngLat = this.getMarkerInfoLngLat(existingMarkerInfo) || newLngLat;
    const pathPositions = this.getAutoTracePath(targetLngLat);
    return [...pathPositions?.slice(1, -1) || [], targetLngLat];
  }
  getAutoTracePath(finishLngLat) {
    const previousLngLat = this.shapeLngLats.at(-1);
    if (this.autoTraceEnabled && this.autoTraceHelperInstance && previousLngLat) {
      const path = this.autoTraceHelperInstance.getShortestPath(previousLngLat, finishLngLat);
      return path || null;
    }
    return null;
  }
  getMarkerInfoLngLat(markerInfo) {
    if (this.featureData && markerInfo.path) {
      const markerData = this.featureData.markers.get(markerInfo.path);
      if (markerData && markerData.type === "dom") {
        return markerData.instance.getLngLat();
      } else {
        log.error("LineDrawer.addPoint: no markerData", markerInfo);
      }
    }
    return null;
  }
  addMarker(lngLat, featureData) {
    const markerData = {
      type: "dom",
      instance: this.createMarker(lngLat),
      position: {
        coordinate: lngLat,
        path: ["geometry", "coordinates", this.shapeLngLats.length]
      }
    };
    featureData.markers.set(markerData.position.path.join("."), {
      type: "dom",
      instance: markerData.instance,
      position: {
        coordinate: lngLat,
        path: []
      }
    });
    return markerData;
  }
  createMarker(lngLat) {
    return this.gm.mapAdapter.createDomMarker(
      {
        element: this.gm.createSvgMarkerElement("control", {
          pointerEvents: "auto",
          cursor: "pointer"
        }),
        anchor: "center"
      },
      lngLat
    );
  }
  updateFeatureSource() {
    if (!this.featureData) {
      return;
    }
    this.featureData.updateGeoJsonGeometry(
      this.getFeatureGeoJson({ withControlMarker: true }).geometry
    );
    if (this.gm.markerPointer.marker) {
      const markerData = {
        type: "dom",
        instance: this.gm.markerPointer.marker,
        position: {
          coordinate: this.gm.markerPointer.marker.getLngLat(),
          path: ["geometry", "coordinates", this.shapeLngLats.length]
        }
      };
      this.fireUpdateEvent(this.featureData, markerData);
    }
  }
  getFeatureGeoJson({
    withControlMarker,
    coordinates = void 0
  }) {
    return {
      type: "Feature",
      properties: {
        shape: "line"
      },
      geometry: {
        type: "LineString",
        coordinates: coordinates || this.getShapeCoordinates({ withControlMarker })
      }
    };
  }
  getFeatureGeoJsonWithType({
    withControlMarker,
    coordinates = void 0
  }) {
    const featureGeoJson = this.getFeatureGeoJson({ withControlMarker, coordinates });
    if (this.drawOptions.targetShape === "polygon" && featureGeoJson.geometry.coordinates.length > 3) {
      return index_default$9(featureGeoJson, {
        properties: featureGeoJson.properties
      });
    }
    return featureGeoJson;
  }
  getShapeCoordinates({ withControlMarker }) {
    const coordinates = [...this.shapeLngLats];
    if (withControlMarker && this.gm.markerPointer.marker) {
      coordinates.push(this.gm.markerPointer.marker.getLngLat());
    }
    return coordinates;
  }
  fireStartEvent(featureData, markerData) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: "line",
      variant: "line_drawer",
      action: "start",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
  fireUpdateEvent(featureData, markerData) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: "line",
      variant: "line_drawer",
      action: "update",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
  fireStopEvent(featureGeoJson) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: "line",
      action: "finish",
      variant: "line_drawer",
      geoJsonFeature: featureGeoJson,
      markerData: null,
      featureData: null
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
}
class DrawLine extends BaseDraw {
  mode = "line";
  shape = "line";
  lineDrawer = new LineDrawer(this.gm, { snappingMarkers: "first", targetShape: "line" });
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:draw`]: this.forwardLineDrawerEvent.bind(this),
    mousemove: this.onMouseMove.bind(this)
  };
  onStartAction() {
    this.lineDrawer.startAction();
    this.lineDrawer.on("nMarkerClick", this.lineFinished.bind(this));
  }
  onEndAction() {
    this.lineDrawer.endAction();
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    if (!this.lineDrawer.featureData) {
      this.fireMarkerPointerUpdateEvent();
    }
    return { next: true };
  }
  lineFinished(event) {
    this.lineDrawer.endShape();
    let shapeCoordinates = event.shapeCoordinates;
    if (event.markerIndex > 0) {
      shapeCoordinates = shapeCoordinates.slice(0, event.markerIndex + 1);
    }
    if (shapeCoordinates.length < 2) {
      return null;
    }
    return this.gm.features.createFeature({
      shapeGeoJson: this.getFeatureGeoJson(shapeCoordinates),
      sourceName: SOURCES.main
    });
  }
  getFeatureGeoJson(shapeCoordinates) {
    return {
      type: "Feature",
      properties: {
        shape: this.shape
      },
      geometry: {
        type: "LineString",
        coordinates: shapeCoordinates
      }
    };
  }
}
class DrawMarker extends BaseDraw {
  mode = "marker";
  shape = "marker";
  eventHandlers = {
    click: this.onMouseClick.bind(this),
    mousemove: this.onMouseMove.bind(this)
  };
  onStartAction() {
    const customMarker = this.createMarker();
    this.gm.markerPointer.enable({ customMarker });
    this.fireMarkerPointerStartEvent();
  }
  onEndAction() {
    this.gm.markerPointer.disable();
    this.fireMarkerPointerFinishEvent();
  }
  onMouseClick(event) {
    if (isMapPointerEvent(event)) {
      this.gm.features.clearSelection();
      this.featureData = this.createFeature(event);
      if (this.featureData) {
        this.saveFeature();
      }
    }
    return { next: false };
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event) || !this.gm.markerPointer.marker) {
      return { next: true };
    }
    this.fireMarkerPointerUpdateEvent();
    return { next: true };
  }
  createFeature(event) {
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    const geoJson = this.getFeatureGeoJson(lngLat);
    if (geoJson) {
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [geoJson] });
      if (this.flags.featureCreateAllowed) {
        return this.gm.features.createFeature({
          shapeGeoJson: geoJson,
          sourceName: SOURCES.temporary
        });
      }
    }
    return null;
  }
  getFeatureGeoJson(lngLat) {
    return {
      type: "Feature",
      properties: {
        shape: this.shape
      },
      geometry: {
        type: "Point",
        coordinates: lngLat
      }
    };
  }
  createMarker(sourceName = SOURCES.temporary) {
    const markerStyles = this.gm.options.layerStyles.marker[sourceName];
    const symbolLayer = markerStyles?.find((layer) => layer.type === "symbol");
    const iconOpacity = symbolLayer?.paint && "icon-opacity" in symbolLayer.paint ? symbolLayer.paint["icon-opacity"] : void 0;
    const iconSize = symbolLayer?.layout && "icon-size" in symbolLayer.layout ? symbolLayer.layout["icon-size"] : void 0;
    const baseSize = 36;
    const defaultIconSize = 0.18;
    const pixelSize = iconSize !== void 0 ? Math.round(baseSize * (iconSize / defaultIconSize)) : baseSize;
    const sizeStr = `${pixelSize}px`;
    const iconElement = this.gm.createSvgMarkerElement("default", {
      width: sizeStr,
      height: sizeStr,
      pointerEvents: "none",
      ...iconOpacity !== void 0 && { opacity: String(iconOpacity) }
    });
    return this.gm.mapAdapter.createDomMarker(
      {
        draggable: false,
        anchor: "bottom",
        element: iconElement
      },
      [0, 0]
    );
  }
}
function combine(fc) {
  var groups = {
    MultiPoint: {
      coordinates: [],
      properties: []
    },
    MultiLineString: {
      coordinates: [],
      properties: []
    },
    MultiPolygon: {
      coordinates: [],
      properties: []
    }
  };
  featureEach(fc, (feature2) => {
    var _a;
    switch ((_a = feature2.geometry) == null ? void 0 : _a.type) {
      case "Point":
        groups.MultiPoint.coordinates.push(feature2.geometry.coordinates);
        groups.MultiPoint.properties.push(feature2.properties);
        break;
      case "MultiPoint":
        groups.MultiPoint.coordinates.push(...feature2.geometry.coordinates);
        groups.MultiPoint.properties.push(feature2.properties);
        break;
      case "LineString":
        groups.MultiLineString.coordinates.push(feature2.geometry.coordinates);
        groups.MultiLineString.properties.push(feature2.properties);
        break;
      case "MultiLineString":
        groups.MultiLineString.coordinates.push(
          ...feature2.geometry.coordinates
        );
        groups.MultiLineString.properties.push(feature2.properties);
        break;
      case "Polygon":
        groups.MultiPolygon.coordinates.push(feature2.geometry.coordinates);
        groups.MultiPolygon.properties.push(feature2.properties);
        break;
      case "MultiPolygon":
        groups.MultiPolygon.coordinates.push(...feature2.geometry.coordinates);
        groups.MultiPolygon.properties.push(feature2.properties);
        break;
    }
  });
  return featureCollection(
    Object.keys(groups).filter(function(key) {
      return groups[key].coordinates.length;
    }).sort().map(function(key) {
      var geometry = { type: key, coordinates: groups[key].coordinates };
      var properties = { collectedProperties: groups[key].properties };
      return feature(geometry, properties);
    })
  );
}
var index_default$8 = combine;
function quickselect(arr, k, left, right, compare2) {
  quickselectStep(arr, k, left || 0, right || arr.length - 1, compare2 || defaultCompare$1);
}
function quickselectStep(arr, k, left, right, compare2) {
  while (right > left) {
    if (right - left > 600) {
      var n = right - left + 1;
      var m = k - left + 1;
      var z = Math.log(n);
      var s = 0.5 * Math.exp(2 * z / 3);
      var sd = 0.5 * Math.sqrt(z * s * (n - s) / n) * (m - n / 2 < 0 ? -1 : 1);
      var newLeft = Math.max(left, Math.floor(k - m * s / n + sd));
      var newRight = Math.min(right, Math.floor(k + (n - m) * s / n + sd));
      quickselectStep(arr, k, newLeft, newRight, compare2);
    }
    var t = arr[k];
    var i = left;
    var j = right;
    swap(arr, left, k);
    if (compare2(arr[right], t) > 0) swap(arr, left, right);
    while (i < j) {
      swap(arr, i, j);
      i++;
      j--;
      while (compare2(arr[i], t) < 0) i++;
      while (compare2(arr[j], t) > 0) j--;
    }
    if (compare2(arr[left], t) === 0) swap(arr, left, j);
    else {
      j++;
      swap(arr, j, right);
    }
    if (j <= k) left = j + 1;
    if (k <= j) right = j - 1;
  }
}
function swap(arr, i, j) {
  var tmp = arr[i];
  arr[i] = arr[j];
  arr[j] = tmp;
}
function defaultCompare$1(a, b) {
  return a < b ? -1 : a > b ? 1 : 0;
}
class RBush {
  constructor(maxEntries = 9) {
    this._maxEntries = Math.max(4, maxEntries);
    this._minEntries = Math.max(2, Math.ceil(this._maxEntries * 0.4));
    this.clear();
  }
  all() {
    return this._all(this.data, []);
  }
  search(bbox2) {
    let node = this.data;
    const result = [];
    if (!intersects(bbox2, node)) return result;
    const toBBox = this.toBBox;
    const nodesToSearch = [];
    while (node) {
      for (let i = 0; i < node.children.length; i++) {
        const child2 = node.children[i];
        const childBBox = node.leaf ? toBBox(child2) : child2;
        if (intersects(bbox2, childBBox)) {
          if (node.leaf) result.push(child2);
          else if (contains(bbox2, childBBox)) this._all(child2, result);
          else nodesToSearch.push(child2);
        }
      }
      node = nodesToSearch.pop();
    }
    return result;
  }
  collides(bbox2) {
    let node = this.data;
    if (!intersects(bbox2, node)) return false;
    const nodesToSearch = [];
    while (node) {
      for (let i = 0; i < node.children.length; i++) {
        const child2 = node.children[i];
        const childBBox = node.leaf ? this.toBBox(child2) : child2;
        if (intersects(bbox2, childBBox)) {
          if (node.leaf || contains(bbox2, childBBox)) return true;
          nodesToSearch.push(child2);
        }
      }
      node = nodesToSearch.pop();
    }
    return false;
  }
  load(data) {
    if (!(data && data.length)) return this;
    if (data.length < this._minEntries) {
      for (let i = 0; i < data.length; i++) {
        this.insert(data[i]);
      }
      return this;
    }
    let node = this._build(data.slice(), 0, data.length - 1, 0);
    if (!this.data.children.length) {
      this.data = node;
    } else if (this.data.height === node.height) {
      this._splitRoot(this.data, node);
    } else {
      if (this.data.height < node.height) {
        const tmpNode = this.data;
        this.data = node;
        node = tmpNode;
      }
      this._insert(node, this.data.height - node.height - 1, true);
    }
    return this;
  }
  insert(item) {
    if (item) this._insert(item, this.data.height - 1);
    return this;
  }
  clear() {
    this.data = createNode([]);
    return this;
  }
  remove(item, equalsFn) {
    if (!item) return this;
    let node = this.data;
    const bbox2 = this.toBBox(item);
    const path = [];
    const indexes = [];
    let i, parent, goingUp;
    while (node || path.length) {
      if (!node) {
        node = path.pop();
        parent = path[path.length - 1];
        i = indexes.pop();
        goingUp = true;
      }
      if (node.leaf) {
        const index2 = findItem(item, node.children, equalsFn);
        if (index2 !== -1) {
          node.children.splice(index2, 1);
          path.push(node);
          this._condense(path);
          return this;
        }
      }
      if (!goingUp && !node.leaf && contains(node, bbox2)) {
        path.push(node);
        indexes.push(i);
        i = 0;
        parent = node;
        node = node.children[0];
      } else if (parent) {
        i++;
        node = parent.children[i];
        goingUp = false;
      } else node = null;
    }
    return this;
  }
  toBBox(item) {
    return item;
  }
  compareMinX(a, b) {
    return a.minX - b.minX;
  }
  compareMinY(a, b) {
    return a.minY - b.minY;
  }
  toJSON() {
    return this.data;
  }
  fromJSON(data) {
    this.data = data;
    return this;
  }
  _all(node, result) {
    const nodesToSearch = [];
    while (node) {
      if (node.leaf) result.push(...node.children);
      else nodesToSearch.push(...node.children);
      node = nodesToSearch.pop();
    }
    return result;
  }
  _build(items, left, right, height) {
    const N = right - left + 1;
    let M = this._maxEntries;
    let node;
    if (N <= M) {
      node = createNode(items.slice(left, right + 1));
      calcBBox(node, this.toBBox);
      return node;
    }
    if (!height) {
      height = Math.ceil(Math.log(N) / Math.log(M));
      M = Math.ceil(N / Math.pow(M, height - 1));
    }
    node = createNode([]);
    node.leaf = false;
    node.height = height;
    const N2 = Math.ceil(N / M);
    const N1 = N2 * Math.ceil(Math.sqrt(M));
    multiSelect(items, left, right, N1, this.compareMinX);
    for (let i = left; i <= right; i += N1) {
      const right2 = Math.min(i + N1 - 1, right);
      multiSelect(items, i, right2, N2, this.compareMinY);
      for (let j = i; j <= right2; j += N2) {
        const right3 = Math.min(j + N2 - 1, right2);
        node.children.push(this._build(items, j, right3, height - 1));
      }
    }
    calcBBox(node, this.toBBox);
    return node;
  }
  _chooseSubtree(bbox2, node, level, path) {
    while (true) {
      path.push(node);
      if (node.leaf || path.length - 1 === level) break;
      let minArea = Infinity;
      let minEnlargement = Infinity;
      let targetNode;
      for (let i = 0; i < node.children.length; i++) {
        const child2 = node.children[i];
        const area2 = bboxArea(child2);
        const enlargement = enlargedArea(bbox2, child2) - area2;
        if (enlargement < minEnlargement) {
          minEnlargement = enlargement;
          minArea = area2 < minArea ? area2 : minArea;
          targetNode = child2;
        } else if (enlargement === minEnlargement) {
          if (area2 < minArea) {
            minArea = area2;
            targetNode = child2;
          }
        }
      }
      node = targetNode || node.children[0];
    }
    return node;
  }
  _insert(item, level, isNode) {
    const bbox2 = isNode ? item : this.toBBox(item);
    const insertPath = [];
    const node = this._chooseSubtree(bbox2, this.data, level, insertPath);
    node.children.push(item);
    extend(node, bbox2);
    while (level >= 0) {
      if (insertPath[level].children.length > this._maxEntries) {
        this._split(insertPath, level);
        level--;
      } else break;
    }
    this._adjustParentBBoxes(bbox2, insertPath, level);
  }
  // split overflowed node into two
  _split(insertPath, level) {
    const node = insertPath[level];
    const M = node.children.length;
    const m = this._minEntries;
    this._chooseSplitAxis(node, m, M);
    const splitIndex = this._chooseSplitIndex(node, m, M);
    const newNode = createNode(node.children.splice(splitIndex, node.children.length - splitIndex));
    newNode.height = node.height;
    newNode.leaf = node.leaf;
    calcBBox(node, this.toBBox);
    calcBBox(newNode, this.toBBox);
    if (level) insertPath[level - 1].children.push(newNode);
    else this._splitRoot(node, newNode);
  }
  _splitRoot(node, newNode) {
    this.data = createNode([node, newNode]);
    this.data.height = node.height + 1;
    this.data.leaf = false;
    calcBBox(this.data, this.toBBox);
  }
  _chooseSplitIndex(node, m, M) {
    let index2;
    let minOverlap = Infinity;
    let minArea = Infinity;
    for (let i = m; i <= M - m; i++) {
      const bbox1 = distBBox(node, 0, i, this.toBBox);
      const bbox2 = distBBox(node, i, M, this.toBBox);
      const overlap = intersectionArea(bbox1, bbox2);
      const area2 = bboxArea(bbox1) + bboxArea(bbox2);
      if (overlap < minOverlap) {
        minOverlap = overlap;
        index2 = i;
        minArea = area2 < minArea ? area2 : minArea;
      } else if (overlap === minOverlap) {
        if (area2 < minArea) {
          minArea = area2;
          index2 = i;
        }
      }
    }
    return index2 || M - m;
  }
  // sorts node children by the best axis for split
  _chooseSplitAxis(node, m, M) {
    const compareMinX = node.leaf ? this.compareMinX : compareNodeMinX;
    const compareMinY = node.leaf ? this.compareMinY : compareNodeMinY;
    const xMargin = this._allDistMargin(node, m, M, compareMinX);
    const yMargin = this._allDistMargin(node, m, M, compareMinY);
    if (xMargin < yMargin) node.children.sort(compareMinX);
  }
  // total margin of all possible split distributions where each node is at least m full
  _allDistMargin(node, m, M, compare2) {
    node.children.sort(compare2);
    const toBBox = this.toBBox;
    const leftBBox = distBBox(node, 0, m, toBBox);
    const rightBBox = distBBox(node, M - m, M, toBBox);
    let margin = bboxMargin(leftBBox) + bboxMargin(rightBBox);
    for (let i = m; i < M - m; i++) {
      const child2 = node.children[i];
      extend(leftBBox, node.leaf ? toBBox(child2) : child2);
      margin += bboxMargin(leftBBox);
    }
    for (let i = M - m - 1; i >= m; i--) {
      const child2 = node.children[i];
      extend(rightBBox, node.leaf ? toBBox(child2) : child2);
      margin += bboxMargin(rightBBox);
    }
    return margin;
  }
  _adjustParentBBoxes(bbox2, path, level) {
    for (let i = level; i >= 0; i--) {
      extend(path[i], bbox2);
    }
  }
  _condense(path) {
    for (let i = path.length - 1, siblings; i >= 0; i--) {
      if (path[i].children.length === 0) {
        if (i > 0) {
          siblings = path[i - 1].children;
          siblings.splice(siblings.indexOf(path[i]), 1);
        } else this.clear();
      } else calcBBox(path[i], this.toBBox);
    }
  }
}
function findItem(item, items, equalsFn) {
  if (!equalsFn) return items.indexOf(item);
  for (let i = 0; i < items.length; i++) {
    if (equalsFn(item, items[i])) return i;
  }
  return -1;
}
function calcBBox(node, toBBox) {
  distBBox(node, 0, node.children.length, toBBox, node);
}
function distBBox(node, k, p, toBBox, destNode) {
  if (!destNode) destNode = createNode(null);
  destNode.minX = Infinity;
  destNode.minY = Infinity;
  destNode.maxX = -Infinity;
  destNode.maxY = -Infinity;
  for (let i = k; i < p; i++) {
    const child2 = node.children[i];
    extend(destNode, node.leaf ? toBBox(child2) : child2);
  }
  return destNode;
}
function extend(a, b) {
  a.minX = Math.min(a.minX, b.minX);
  a.minY = Math.min(a.minY, b.minY);
  a.maxX = Math.max(a.maxX, b.maxX);
  a.maxY = Math.max(a.maxY, b.maxY);
  return a;
}
function compareNodeMinX(a, b) {
  return a.minX - b.minX;
}
function compareNodeMinY(a, b) {
  return a.minY - b.minY;
}
function bboxArea(a) {
  return (a.maxX - a.minX) * (a.maxY - a.minY);
}
function bboxMargin(a) {
  return a.maxX - a.minX + (a.maxY - a.minY);
}
function enlargedArea(a, b) {
  return (Math.max(b.maxX, a.maxX) - Math.min(b.minX, a.minX)) * (Math.max(b.maxY, a.maxY) - Math.min(b.minY, a.minY));
}
function intersectionArea(a, b) {
  const minX = Math.max(a.minX, b.minX);
  const minY = Math.max(a.minY, b.minY);
  const maxX = Math.min(a.maxX, b.maxX);
  const maxY = Math.min(a.maxY, b.maxY);
  return Math.max(0, maxX - minX) * Math.max(0, maxY - minY);
}
function contains(a, b) {
  return a.minX <= b.minX && a.minY <= b.minY && b.maxX <= a.maxX && b.maxY <= a.maxY;
}
function intersects(a, b) {
  return b.minX <= a.maxX && b.minY <= a.maxY && b.maxX >= a.minX && b.maxY >= a.minY;
}
function createNode(children) {
  return {
    children,
    height: 1,
    leaf: true,
    minX: Infinity,
    minY: Infinity,
    maxX: -Infinity,
    maxY: -Infinity
  };
}
function multiSelect(arr, left, right, n, compare2) {
  const stack = [left, right];
  while (stack.length) {
    right = stack.pop();
    left = stack.pop();
    if (right - left <= n) continue;
    const mid = left + Math.ceil((right - left) / n / 2) * n;
    quickselect(arr, mid, left, right, compare2);
    stack.push(left, mid, mid, right);
  }
}
function area(geojson) {
  return geomReduce(
    geojson,
    (value, geom) => {
      return value + calculateArea(geom);
    },
    0
  );
}
function calculateArea(geom) {
  let total = 0;
  let i;
  switch (geom.type) {
    case "Polygon":
      return polygonArea(geom.coordinates);
    case "MultiPolygon":
      for (i = 0; i < geom.coordinates.length; i++) {
        total += polygonArea(geom.coordinates[i]);
      }
      return total;
    case "Point":
    case "MultiPoint":
    case "LineString":
    case "MultiLineString":
      return 0;
  }
  return 0;
}
function polygonArea(coords) {
  let total = 0;
  if (coords && coords.length > 0) {
    total += Math.abs(ringArea(coords[0]));
    for (let i = 1; i < coords.length; i++) {
      total -= Math.abs(ringArea(coords[i]));
    }
  }
  return total;
}
var FACTOR = earthRadius * earthRadius / 2;
var PI_OVER_180 = Math.PI / 180;
function ringArea(coords) {
  const coordsLength = coords.length - 1;
  if (coordsLength <= 2) return 0;
  let total = 0;
  let i = 0;
  while (i < coordsLength) {
    const lower = coords[i];
    const middle = coords[i + 1 === coordsLength ? 0 : i + 1];
    const upper = coords[i + 2 >= coordsLength ? (i + 2) % coordsLength : i + 2];
    const lowerX = lower[0] * PI_OVER_180;
    const middleY = middle[1] * PI_OVER_180;
    const upperX = upper[0] * PI_OVER_180;
    total += (upperX - lowerX) * Math.sin(middleY);
    i++;
  }
  return total * FACTOR;
}
const epsilon$1 = 11102230246251565e-32;
const splitter = 134217729;
const resulterrbound = (3 + 8 * epsilon$1) * epsilon$1;
function sum$1(elen, e, flen, f, h) {
  let Q, Qnew, hh, bvirt;
  let enow = e[0];
  let fnow = f[0];
  let eindex = 0;
  let findex = 0;
  if (fnow > enow === fnow > -enow) {
    Q = enow;
    enow = e[++eindex];
  } else {
    Q = fnow;
    fnow = f[++findex];
  }
  let hindex = 0;
  if (eindex < elen && findex < flen) {
    if (fnow > enow === fnow > -enow) {
      Qnew = enow + Q;
      hh = Q - (Qnew - enow);
      enow = e[++eindex];
    } else {
      Qnew = fnow + Q;
      hh = Q - (Qnew - fnow);
      fnow = f[++findex];
    }
    Q = Qnew;
    if (hh !== 0) {
      h[hindex++] = hh;
    }
    while (eindex < elen && findex < flen) {
      if (fnow > enow === fnow > -enow) {
        Qnew = Q + enow;
        bvirt = Qnew - Q;
        hh = Q - (Qnew - bvirt) + (enow - bvirt);
        enow = e[++eindex];
      } else {
        Qnew = Q + fnow;
        bvirt = Qnew - Q;
        hh = Q - (Qnew - bvirt) + (fnow - bvirt);
        fnow = f[++findex];
      }
      Q = Qnew;
      if (hh !== 0) {
        h[hindex++] = hh;
      }
    }
  }
  while (eindex < elen) {
    Qnew = Q + enow;
    bvirt = Qnew - Q;
    hh = Q - (Qnew - bvirt) + (enow - bvirt);
    enow = e[++eindex];
    Q = Qnew;
    if (hh !== 0) {
      h[hindex++] = hh;
    }
  }
  while (findex < flen) {
    Qnew = Q + fnow;
    bvirt = Qnew - Q;
    hh = Q - (Qnew - bvirt) + (fnow - bvirt);
    fnow = f[++findex];
    Q = Qnew;
    if (hh !== 0) {
      h[hindex++] = hh;
    }
  }
  if (Q !== 0 || hindex === 0) {
    h[hindex++] = Q;
  }
  return hindex;
}
function estimate(elen, e) {
  let Q = e[0];
  for (let i = 1; i < elen; i++) Q += e[i];
  return Q;
}
function vec(n) {
  return new Float64Array(n);
}
const ccwerrboundA = (3 + 16 * epsilon$1) * epsilon$1;
const ccwerrboundB = (2 + 12 * epsilon$1) * epsilon$1;
const ccwerrboundC = (9 + 64 * epsilon$1) * epsilon$1 * epsilon$1;
const B = vec(4);
const C1 = vec(8);
const C2 = vec(12);
const D = vec(16);
const u = vec(4);
function orient2dadapt(ax, ay, bx, by, cx, cy, detsum) {
  let acxtail, acytail, bcxtail, bcytail;
  let bvirt, c, ahi, alo, bhi, blo, _i, _j, _0, s1, s0, t1, t0, u3;
  const acx = ax - cx;
  const bcx = bx - cx;
  const acy = ay - cy;
  const bcy = by - cy;
  s1 = acx * bcy;
  c = splitter * acx;
  ahi = c - (c - acx);
  alo = acx - ahi;
  c = splitter * bcy;
  bhi = c - (c - bcy);
  blo = bcy - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t1 = acy * bcx;
  c = splitter * acy;
  ahi = c - (c - acy);
  alo = acy - ahi;
  c = splitter * bcx;
  bhi = c - (c - bcx);
  blo = bcx - bhi;
  t0 = alo * blo - (t1 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t0;
  bvirt = s0 - _i;
  B[0] = s0 - (_i + bvirt) + (bvirt - t0);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t1;
  bvirt = _0 - _i;
  B[1] = _0 - (_i + bvirt) + (bvirt - t1);
  u3 = _j + _i;
  bvirt = u3 - _j;
  B[2] = _j - (u3 - bvirt) + (_i - bvirt);
  B[3] = u3;
  let det = estimate(4, B);
  let errbound = ccwerrboundB * detsum;
  if (det >= errbound || -det >= errbound) {
    return det;
  }
  bvirt = ax - acx;
  acxtail = ax - (acx + bvirt) + (bvirt - cx);
  bvirt = bx - bcx;
  bcxtail = bx - (bcx + bvirt) + (bvirt - cx);
  bvirt = ay - acy;
  acytail = ay - (acy + bvirt) + (bvirt - cy);
  bvirt = by - bcy;
  bcytail = by - (bcy + bvirt) + (bvirt - cy);
  if (acxtail === 0 && acytail === 0 && bcxtail === 0 && bcytail === 0) {
    return det;
  }
  errbound = ccwerrboundC * detsum + resulterrbound * Math.abs(det);
  det += acx * bcytail + bcy * acxtail - (acy * bcxtail + bcx * acytail);
  if (det >= errbound || -det >= errbound) return det;
  s1 = acxtail * bcy;
  c = splitter * acxtail;
  ahi = c - (c - acxtail);
  alo = acxtail - ahi;
  c = splitter * bcy;
  bhi = c - (c - bcy);
  blo = bcy - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t1 = acytail * bcx;
  c = splitter * acytail;
  ahi = c - (c - acytail);
  alo = acytail - ahi;
  c = splitter * bcx;
  bhi = c - (c - bcx);
  blo = bcx - bhi;
  t0 = alo * blo - (t1 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t0;
  bvirt = s0 - _i;
  u[0] = s0 - (_i + bvirt) + (bvirt - t0);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t1;
  bvirt = _0 - _i;
  u[1] = _0 - (_i + bvirt) + (bvirt - t1);
  u3 = _j + _i;
  bvirt = u3 - _j;
  u[2] = _j - (u3 - bvirt) + (_i - bvirt);
  u[3] = u3;
  const C1len = sum$1(4, B, 4, u, C1);
  s1 = acx * bcytail;
  c = splitter * acx;
  ahi = c - (c - acx);
  alo = acx - ahi;
  c = splitter * bcytail;
  bhi = c - (c - bcytail);
  blo = bcytail - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t1 = acy * bcxtail;
  c = splitter * acy;
  ahi = c - (c - acy);
  alo = acy - ahi;
  c = splitter * bcxtail;
  bhi = c - (c - bcxtail);
  blo = bcxtail - bhi;
  t0 = alo * blo - (t1 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t0;
  bvirt = s0 - _i;
  u[0] = s0 - (_i + bvirt) + (bvirt - t0);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t1;
  bvirt = _0 - _i;
  u[1] = _0 - (_i + bvirt) + (bvirt - t1);
  u3 = _j + _i;
  bvirt = u3 - _j;
  u[2] = _j - (u3 - bvirt) + (_i - bvirt);
  u[3] = u3;
  const C2len = sum$1(C1len, C1, 4, u, C2);
  s1 = acxtail * bcytail;
  c = splitter * acxtail;
  ahi = c - (c - acxtail);
  alo = acxtail - ahi;
  c = splitter * bcytail;
  bhi = c - (c - bcytail);
  blo = bcytail - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t1 = acytail * bcxtail;
  c = splitter * acytail;
  ahi = c - (c - acytail);
  alo = acytail - ahi;
  c = splitter * bcxtail;
  bhi = c - (c - bcxtail);
  blo = bcxtail - bhi;
  t0 = alo * blo - (t1 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t0;
  bvirt = s0 - _i;
  u[0] = s0 - (_i + bvirt) + (bvirt - t0);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t1;
  bvirt = _0 - _i;
  u[1] = _0 - (_i + bvirt) + (bvirt - t1);
  u3 = _j + _i;
  bvirt = u3 - _j;
  u[2] = _j - (u3 - bvirt) + (_i - bvirt);
  u[3] = u3;
  const Dlen = sum$1(C2len, C2, 4, u, D);
  return D[Dlen - 1];
}
function orient2d(ax, ay, bx, by, cx, cy) {
  const detleft = (ay - cy) * (bx - cx);
  const detright = (ax - cx) * (by - cy);
  const det = detleft - detright;
  const detsum = Math.abs(detleft + detright);
  if (Math.abs(det) >= ccwerrboundA * detsum) return det;
  return -orient2dadapt(ax, ay, bx, by, cx, cy, detsum);
}
function pointInPolygon(p, polygon2) {
  var i;
  var ii;
  var k = 0;
  var f;
  var u1;
  var v1;
  var u2;
  var v2;
  var currentP;
  var nextP;
  var x = p[0];
  var y = p[1];
  var numContours = polygon2.length;
  for (i = 0; i < numContours; i++) {
    ii = 0;
    var contour = polygon2[i];
    var contourLen = contour.length - 1;
    currentP = contour[0];
    if (currentP[0] !== contour[contourLen][0] && currentP[1] !== contour[contourLen][1]) {
      throw new Error("First and last coordinates in a ring must be the same");
    }
    u1 = currentP[0] - x;
    v1 = currentP[1] - y;
    for (ii; ii < contourLen; ii++) {
      nextP = contour[ii + 1];
      u2 = nextP[0] - x;
      v2 = nextP[1] - y;
      if (v1 === 0 && v2 === 0) {
        if (u2 <= 0 && u1 >= 0 || u1 <= 0 && u2 >= 0) {
          return 0;
        }
      } else if (v2 >= 0 && v1 <= 0 || v2 <= 0 && v1 >= 0) {
        f = orient2d(u1, u2, v1, v2, 0, 0);
        if (f === 0) {
          return 0;
        }
        if (f > 0 && v2 > 0 && v1 <= 0 || f < 0 && v2 <= 0 && v1 > 0) {
          k++;
        }
      }
      currentP = nextP;
      v1 = v2;
      u1 = u2;
    }
  }
  if (k % 2 === 0) {
    return false;
  }
  return true;
}
function booleanPointInPolygon(point2, polygon2, options = {}) {
  if (!point2) {
    throw new Error("point is required");
  }
  if (!polygon2) {
    throw new Error("polygon is required");
  }
  const pt = getCoord(point2);
  const geom = getGeom(polygon2);
  const type = geom.type;
  const bbox2 = polygon2.bbox;
  let polys = geom.coordinates;
  if (bbox2 && inBBox(pt, bbox2) === false) {
    return false;
  }
  if (type === "Polygon") {
    polys = [polys];
  }
  let result = false;
  for (var i = 0; i < polys.length; ++i) {
    const polyResult = pointInPolygon(pt, polys[i]);
    if (polyResult === 0) return options.ignoreBoundary ? false : true;
    else if (polyResult) result = true;
  }
  return result;
}
function inBBox(pt, bbox2) {
  return bbox2[0] <= pt[0] && bbox2[1] <= pt[1] && bbox2[2] >= pt[0] && bbox2[3] >= pt[1];
}
function geojsonPolygonSelfIntersections(feature2, filterFn, useSpatialIndex) {
  if (feature2.geometry.type !== "Polygon")
    throw new Error("The input feature must be a Polygon");
  if (useSpatialIndex === void 0) useSpatialIndex = 1;
  var coord = feature2.geometry.coordinates;
  var output = [];
  var seen = {};
  if (useSpatialIndex) {
    var allEdgesAsRbushTreeItems = [];
    for (var ring0 = 0; ring0 < coord.length; ring0++) {
      for (var edge0 = 0; edge0 < coord[ring0].length - 1; edge0++) {
        allEdgesAsRbushTreeItems.push(rbushTreeItem(ring0, edge0));
      }
    }
    var tree = new RBush();
    tree.load(allEdgesAsRbushTreeItems);
  }
  for (var ringA = 0; ringA < coord.length; ringA++) {
    for (var edgeA = 0; edgeA < coord[ringA].length - 1; edgeA++) {
      if (useSpatialIndex) {
        var bboxOverlaps = tree.search(rbushTreeItem(ringA, edgeA));
        bboxOverlaps.forEach(function(bboxIsect) {
          var ring12 = bboxIsect.ring;
          var edge12 = bboxIsect.edge;
          ifIsectAddToOutput(ringA, edgeA, ring12, edge12);
        });
      } else {
        for (var ring1 = 0; ring1 < coord.length; ring1++) {
          for (var edge1 = 0; edge1 < coord[ring1].length - 1; edge1++) {
            ifIsectAddToOutput(ringA, edgeA, ring1, edge1);
          }
        }
      }
    }
  }
  if (!filterFn)
    output = {
      type: "Feature",
      geometry: { type: "MultiPoint", coordinates: output }
    };
  return output;
  function ifIsectAddToOutput(ring02, edge02, ring12, edge12) {
    var start0 = coord[ring02][edge02];
    var end0 = coord[ring02][edge02 + 1];
    var start1 = coord[ring12][edge12];
    var end1 = coord[ring12][edge12 + 1];
    var isect = intersect(start0, end0, start1, end1);
    if (isect === null) return;
    var frac0;
    var frac1;
    if (end0[0] !== start0[0]) {
      frac0 = (isect[0] - start0[0]) / (end0[0] - start0[0]);
    } else {
      frac0 = (isect[1] - start0[1]) / (end0[1] - start0[1]);
    }
    if (end1[0] !== start1[0]) {
      frac1 = (isect[0] - start1[0]) / (end1[0] - start1[0]);
    } else {
      frac1 = (isect[1] - start1[1]) / (end1[1] - start1[1]);
    }
    if (frac0 >= 1 || frac0 <= 0 || frac1 >= 1 || frac1 <= 0) return;
    var key = isect;
    var unique = !seen[key];
    if (unique) {
      seen[key] = true;
    }
    if (filterFn) {
      output.push(
        filterFn(
          isect,
          ring02,
          edge02,
          start0,
          end0,
          frac0,
          ring12,
          edge12,
          start1,
          end1,
          frac1,
          unique
        )
      );
    } else {
      output.push(isect);
    }
  }
  function rbushTreeItem(ring, edge) {
    var start = coord[ring][edge];
    var end = coord[ring][edge + 1];
    var minX;
    var maxX;
    var minY;
    var maxY;
    if (start[0] < end[0]) {
      minX = start[0];
      maxX = end[0];
    } else {
      minX = end[0];
      maxX = start[0];
    }
    if (start[1] < end[1]) {
      minY = start[1];
      maxY = end[1];
    } else {
      minY = end[1];
      maxY = start[1];
    }
    return {
      minX,
      minY,
      maxX,
      maxY,
      ring,
      edge
    };
  }
}
function intersect(start0, end0, start1, end1) {
  if (equalArrays(start0, start1) || equalArrays(start0, end1) || equalArrays(end0, start1) || equalArrays(end1, start1))
    return null;
  var x02 = start0[0], y02 = start0[1], x12 = end0[0], y12 = end0[1], x2 = start1[0], y2 = start1[1], x3 = end1[0], y3 = end1[1];
  var denom = (x02 - x12) * (y2 - y3) - (y02 - y12) * (x2 - x3);
  if (denom === 0) return null;
  var x4 = ((x02 * y12 - y02 * x12) * (x2 - x3) - (x02 - x12) * (x2 * y3 - y2 * x3)) / denom;
  var y4 = ((x02 * y12 - y02 * x12) * (y2 - y3) - (y02 - y12) * (x2 * y3 - y2 * x3)) / denom;
  return [x4, y4];
}
function equalArrays(array1, array2) {
  if (!array1 || !array2) return false;
  if (array1.length !== array2.length) return false;
  for (var i = 0, l = array1.length; i < l; i++) {
    if (array1[i] instanceof Array && array2[i] instanceof Array) {
      if (!equalArrays(array1[i], array2[i])) return false;
    } else if (array1[i] !== array2[i]) {
      return false;
    }
  }
  return true;
}
function simplepolygon(feature2) {
  if (feature2.type != "Feature")
    throw new Error("The input must a geojson object of type Feature");
  if (feature2.geometry === void 0 || feature2.geometry == null)
    throw new Error(
      "The input must a geojson object with a non-empty geometry"
    );
  if (feature2.geometry.type != "Polygon")
    throw new Error("The input must be a geojson Polygon");
  var numRings = feature2.geometry.coordinates.length;
  var vertices = [];
  for (var i = 0; i < numRings; i++) {
    var ring = feature2.geometry.coordinates[i];
    if (!equalArrays2(ring[0], ring[ring.length - 1])) {
      ring.push(ring[0]);
    }
    for (var j = 0; j < ring.length - 1; j++) {
      vertices.push(ring[j]);
    }
  }
  if (!isUnique(vertices))
    throw new Error(
      "The input polygon may not have duplicate vertices (except for the first and last vertex of each ring)"
    );
  var numvertices = vertices.length;
  var selfIsectsData = geojsonPolygonSelfIntersections(
    feature2,
    function filterFn(isect, ring0, edge0, start0, end0, frac0, ring1, edge1, start1, end1, frac1, unique) {
      return [
        isect,
        ring0,
        edge0,
        start0,
        end0,
        frac0,
        ring1,
        edge1,
        start1,
        end1,
        frac1,
        unique
      ];
    }
  );
  var numSelfIsect = selfIsectsData.length;
  if (numSelfIsect == 0) {
    var outputFeatureArray = [];
    for (var i = 0; i < numRings; i++) {
      outputFeatureArray.push(
        polygon([feature2.geometry.coordinates[i]], {
          parent: -1,
          winding: windingOfRing(feature2.geometry.coordinates[i])
        })
      );
    }
    var output = featureCollection(outputFeatureArray);
    determineParents();
    setNetWinding();
    return output;
  }
  var pseudoVtxListByRingAndEdge = [];
  var isectList = [];
  for (var i = 0; i < numRings; i++) {
    pseudoVtxListByRingAndEdge.push([]);
    for (var j = 0; j < feature2.geometry.coordinates[i].length - 1; j++) {
      pseudoVtxListByRingAndEdge[i].push([
        new PseudoVtx(
          feature2.geometry.coordinates[i][modulo(j + 1, feature2.geometry.coordinates[i].length - 1)],
          1,
          [i, j],
          [i, modulo(j + 1, feature2.geometry.coordinates[i].length - 1)],
          void 0
        )
      ]);
      isectList.push(
        new Isect(
          feature2.geometry.coordinates[i][j],
          [i, modulo(j - 1, feature2.geometry.coordinates[i].length - 1)],
          [i, j],
          void 0,
          void 0,
          false,
          true
        )
      );
    }
  }
  for (var i = 0; i < numSelfIsect; i++) {
    pseudoVtxListByRingAndEdge[selfIsectsData[i][1]][selfIsectsData[i][2]].push(
      new PseudoVtx(
        selfIsectsData[i][0],
        selfIsectsData[i][5],
        [selfIsectsData[i][1], selfIsectsData[i][2]],
        [selfIsectsData[i][6], selfIsectsData[i][7]],
        void 0
      )
    );
    if (selfIsectsData[i][11])
      isectList.push(
        new Isect(
          selfIsectsData[i][0],
          [selfIsectsData[i][1], selfIsectsData[i][2]],
          [selfIsectsData[i][6], selfIsectsData[i][7]],
          void 0,
          void 0,
          true,
          true
        )
      );
  }
  var numIsect = isectList.length;
  for (var i = 0; i < pseudoVtxListByRingAndEdge.length; i++) {
    for (var j = 0; j < pseudoVtxListByRingAndEdge[i].length; j++) {
      pseudoVtxListByRingAndEdge[i][j].sort(function(a, b) {
        return a.param < b.param ? -1 : 1;
      });
    }
  }
  var allIsectsAsIsectRbushTreeItem = [];
  for (var i = 0; i < numIsect; i++) {
    allIsectsAsIsectRbushTreeItem.push({
      minX: isectList[i].coord[0],
      minY: isectList[i].coord[1],
      maxX: isectList[i].coord[0],
      maxY: isectList[i].coord[1],
      index: i
    });
  }
  var isectRbushTree = new RBush();
  isectRbushTree.load(allIsectsAsIsectRbushTreeItem);
  for (var i = 0; i < pseudoVtxListByRingAndEdge.length; i++) {
    for (var j = 0; j < pseudoVtxListByRingAndEdge[i].length; j++) {
      for (var k = 0; k < pseudoVtxListByRingAndEdge[i][j].length; k++) {
        var coordToFind;
        if (k == pseudoVtxListByRingAndEdge[i][j].length - 1) {
          coordToFind = pseudoVtxListByRingAndEdge[i][modulo(j + 1, feature2.geometry.coordinates[i].length - 1)][0].coord;
        } else {
          coordToFind = pseudoVtxListByRingAndEdge[i][j][k + 1].coord;
        }
        var IsectRbushTreeItemFound = isectRbushTree.search({
          minX: coordToFind[0],
          minY: coordToFind[1],
          maxX: coordToFind[0],
          maxY: coordToFind[1]
        })[0];
        pseudoVtxListByRingAndEdge[i][j][k].nxtIsectAlongEdgeIn = IsectRbushTreeItemFound.index;
      }
    }
  }
  for (var i = 0; i < pseudoVtxListByRingAndEdge.length; i++) {
    for (var j = 0; j < pseudoVtxListByRingAndEdge[i].length; j++) {
      for (var k = 0; k < pseudoVtxListByRingAndEdge[i][j].length; k++) {
        var coordToFind = pseudoVtxListByRingAndEdge[i][j][k].coord;
        var IsectRbushTreeItemFound = isectRbushTree.search({
          minX: coordToFind[0],
          minY: coordToFind[1],
          maxX: coordToFind[0],
          maxY: coordToFind[1]
        })[0];
        var l = IsectRbushTreeItemFound.index;
        if (l < numvertices) {
          isectList[l].nxtIsectAlongRingAndEdge2 = pseudoVtxListByRingAndEdge[i][j][k].nxtIsectAlongEdgeIn;
        } else {
          if (equalArrays2(
            isectList[l].ringAndEdge1,
            pseudoVtxListByRingAndEdge[i][j][k].ringAndEdgeIn
          )) {
            isectList[l].nxtIsectAlongRingAndEdge1 = pseudoVtxListByRingAndEdge[i][j][k].nxtIsectAlongEdgeIn;
          } else {
            isectList[l].nxtIsectAlongRingAndEdge2 = pseudoVtxListByRingAndEdge[i][j][k].nxtIsectAlongEdgeIn;
          }
        }
      }
    }
  }
  var queue = [];
  var i = 0;
  for (var j = 0; j < numRings; j++) {
    var leftIsect = i;
    for (var k = 0; k < feature2.geometry.coordinates[j].length - 1; k++) {
      if (isectList[i].coord[0] < isectList[leftIsect].coord[0]) {
        leftIsect = i;
      }
      i++;
    }
    var isectAfterLeftIsect = isectList[leftIsect].nxtIsectAlongRingAndEdge2;
    for (var k = 0; k < isectList.length; k++) {
      if (isectList[k].nxtIsectAlongRingAndEdge1 == leftIsect || isectList[k].nxtIsectAlongRingAndEdge2 == leftIsect) {
        var isectBeforeLeftIsect = k;
        break;
      }
    }
    var windingAtIsect = isConvex(
      [
        isectList[isectBeforeLeftIsect].coord,
        isectList[leftIsect].coord,
        isectList[isectAfterLeftIsect].coord
      ],
      true
    ) ? 1 : -1;
    queue.push({ isect: leftIsect, parent: -1, winding: windingAtIsect });
  }
  queue.sort(function(a, b) {
    return isectList[a.isect].coord > isectList[b.isect].coord ? -1 : 1;
  });
  var outputFeatureArray = [];
  while (queue.length > 0) {
    var popped = queue.pop();
    var startIsect = popped.isect;
    var currentOutputRingParent = popped.parent;
    var currentOutputRingWinding = popped.winding;
    var currentOutputRing = outputFeatureArray.length;
    var currentOutputRingCoords = [isectList[startIsect].coord];
    var currentIsect = startIsect;
    if (isectList[startIsect].ringAndEdge1Walkable) {
      var walkingRingAndEdge = isectList[startIsect].ringAndEdge1;
      var nxtIsect = isectList[startIsect].nxtIsectAlongRingAndEdge1;
    } else {
      var walkingRingAndEdge = isectList[startIsect].ringAndEdge2;
      var nxtIsect = isectList[startIsect].nxtIsectAlongRingAndEdge2;
    }
    while (!equalArrays2(isectList[startIsect].coord, isectList[nxtIsect].coord)) {
      currentOutputRingCoords.push(isectList[nxtIsect].coord);
      var nxtIsectInQueue = void 0;
      for (var i = 0; i < queue.length; i++) {
        if (queue[i].isect == nxtIsect) {
          nxtIsectInQueue = i;
          break;
        }
      }
      if (nxtIsectInQueue != void 0) {
        queue.splice(nxtIsectInQueue, 1);
      }
      if (equalArrays2(walkingRingAndEdge, isectList[nxtIsect].ringAndEdge1)) {
        walkingRingAndEdge = isectList[nxtIsect].ringAndEdge2;
        isectList[nxtIsect].ringAndEdge2Walkable = false;
        if (isectList[nxtIsect].ringAndEdge1Walkable) {
          var pushing = { isect: nxtIsect };
          if (isConvex(
            [
              isectList[currentIsect].coord,
              isectList[nxtIsect].coord,
              isectList[isectList[nxtIsect].nxtIsectAlongRingAndEdge2].coord
            ],
            currentOutputRingWinding == 1
          )) {
            pushing.parent = currentOutputRingParent;
            pushing.winding = -currentOutputRingWinding;
          } else {
            pushing.parent = currentOutputRing;
            pushing.winding = currentOutputRingWinding;
          }
          queue.push(pushing);
        }
        currentIsect = nxtIsect;
        nxtIsect = isectList[nxtIsect].nxtIsectAlongRingAndEdge2;
      } else {
        walkingRingAndEdge = isectList[nxtIsect].ringAndEdge1;
        isectList[nxtIsect].ringAndEdge1Walkable = false;
        if (isectList[nxtIsect].ringAndEdge2Walkable) {
          var pushing = { isect: nxtIsect };
          if (isConvex(
            [
              isectList[currentIsect].coord,
              isectList[nxtIsect].coord,
              isectList[isectList[nxtIsect].nxtIsectAlongRingAndEdge1].coord
            ],
            currentOutputRingWinding == 1
          )) {
            pushing.parent = currentOutputRingParent;
            pushing.winding = -currentOutputRingWinding;
          } else {
            pushing.parent = currentOutputRing;
            pushing.winding = currentOutputRingWinding;
          }
          queue.push(pushing);
        }
        currentIsect = nxtIsect;
        nxtIsect = isectList[nxtIsect].nxtIsectAlongRingAndEdge1;
      }
    }
    currentOutputRingCoords.push(isectList[nxtIsect].coord);
    outputFeatureArray.push(
      polygon([currentOutputRingCoords], {
        index: currentOutputRing,
        parent: currentOutputRingParent,
        winding: currentOutputRingWinding,
        netWinding: void 0
      })
    );
  }
  var output = featureCollection(outputFeatureArray);
  determineParents();
  setNetWinding();
  function determineParents() {
    var featuresWithoutParent = [];
    for (var i2 = 0; i2 < output.features.length; i2++) {
      if (output.features[i2].properties.parent == -1)
        featuresWithoutParent.push(i2);
    }
    if (featuresWithoutParent.length > 1) {
      for (var i2 = 0; i2 < featuresWithoutParent.length; i2++) {
        var parent = -1;
        var parentArea = Infinity;
        for (var j2 = 0; j2 < output.features.length; j2++) {
          if (featuresWithoutParent[i2] == j2) continue;
          if (booleanPointInPolygon(
            output.features[featuresWithoutParent[i2]].geometry.coordinates[0][0],
            output.features[j2],
            { ignoreBoundary: true }
          )) {
            if (area(output.features[j2]) < parentArea) {
              parent = j2;
            }
          }
        }
        output.features[featuresWithoutParent[i2]].properties.parent = parent;
      }
    }
  }
  function setNetWinding() {
    for (var i2 = 0; i2 < output.features.length; i2++) {
      if (output.features[i2].properties.parent == -1) {
        var netWinding = output.features[i2].properties.winding;
        output.features[i2].properties.netWinding = netWinding;
        setNetWindingOfChildren(i2, netWinding);
      }
    }
  }
  function setNetWindingOfChildren(parent, ParentNetWinding) {
    for (var i2 = 0; i2 < output.features.length; i2++) {
      if (output.features[i2].properties.parent == parent) {
        var netWinding = ParentNetWinding + output.features[i2].properties.winding;
        output.features[i2].properties.netWinding = netWinding;
        setNetWindingOfChildren(i2, netWinding);
      }
    }
  }
  return output;
}
var PseudoVtx = function(coord, param, ringAndEdgeIn, ringAndEdgeOut, nxtIsectAlongEdgeIn) {
  this.coord = coord;
  this.param = param;
  this.ringAndEdgeIn = ringAndEdgeIn;
  this.ringAndEdgeOut = ringAndEdgeOut;
  this.nxtIsectAlongEdgeIn = nxtIsectAlongEdgeIn;
};
var Isect = function(coord, ringAndEdge1, ringAndEdge2, nxtIsectAlongRingAndEdge1, nxtIsectAlongRingAndEdge2, ringAndEdge1Walkable, ringAndEdge2Walkable) {
  this.coord = coord;
  this.ringAndEdge1 = ringAndEdge1;
  this.ringAndEdge2 = ringAndEdge2;
  this.nxtIsectAlongRingAndEdge1 = nxtIsectAlongRingAndEdge1;
  this.nxtIsectAlongRingAndEdge2 = nxtIsectAlongRingAndEdge2;
  this.ringAndEdge1Walkable = ringAndEdge1Walkable;
  this.ringAndEdge2Walkable = ringAndEdge2Walkable;
};
function isConvex(pts, righthanded) {
  if (typeof righthanded === "undefined") righthanded = true;
  if (pts.length != 3)
    throw new Error("This function requires an array of three points [x,y]");
  var d = (pts[1][0] - pts[0][0]) * (pts[2][1] - pts[0][1]) - (pts[1][1] - pts[0][1]) * (pts[2][0] - pts[0][0]);
  return d >= 0 == righthanded;
}
function windingOfRing(ring) {
  var leftVtx = 0;
  for (var i = 0; i < ring.length - 1; i++) {
    if (ring[i][0] < ring[leftVtx][0]) leftVtx = i;
  }
  if (isConvex(
    [
      ring[modulo(leftVtx - 1, ring.length - 1)],
      ring[leftVtx],
      ring[modulo(leftVtx + 1, ring.length - 1)]
    ],
    true
  )) {
    var winding = 1;
  } else {
    var winding = -1;
  }
  return winding;
}
function equalArrays2(array1, array2) {
  if (!array1 || !array2) return false;
  if (array1.length != array2.length) return false;
  for (var i = 0, l = array1.length; i < l; i++) {
    if (array1[i] instanceof Array && array2[i] instanceof Array) {
      if (!equalArrays2(array1[i], array2[i])) return false;
    } else if (array1[i] != array2[i]) {
      return false;
    }
  }
  return true;
}
function modulo(n, m) {
  return (n % m + m) % m;
}
function isUnique(array) {
  var u2 = {};
  var isUnique2 = 1;
  for (var i = 0, l = array.length; i < l; ++i) {
    if (Object.prototype.hasOwnProperty.call(u2, array[i])) {
      isUnique2 = 0;
      break;
    }
    u2[array[i]] = 1;
  }
  return isUnique2;
}
function unkinkPolygon(geojson) {
  var features = [];
  flattenEach(geojson, function(feature2) {
    if (feature2.geometry.type !== "Polygon") return;
    featureEach(simplepolygon(feature2), function(poly) {
      features.push(polygon(poly.geometry.coordinates, feature2.properties));
    });
  });
  return featureCollection(features);
}
var index_default$7 = unkinkPolygon;
class DrawPolygon extends BaseDraw {
  mode = "polygon";
  shape = "polygon";
  lineDrawer = new LineDrawer(this.gm, { snappingMarkers: "first", targetShape: "polygon" });
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:draw`]: this.forwardLineDrawerEvent.bind(this),
    mousemove: this.onMouseMove.bind(this)
  };
  onEndAction() {
    this.lineDrawer.endAction();
  }
  onStartAction() {
    this.lineDrawer.startAction();
    this.lineDrawer.on("firstMarkerClick", this.polygonFinished.bind(this));
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    if (!this.lineDrawer.featureData) {
      this.fireMarkerPointerUpdateEvent();
    }
    return { next: true };
  }
  polygonFinished(event) {
    this.lineDrawer.endShape();
    if (event.shapeCoordinates.length < 3) {
      return;
    }
    const geoJsonPolygon = this.fixShapeGeoJson(index_default$9(event.geoJson));
    if (!geoJsonPolygon) {
      return;
    }
    this.gm.features.createFeature({
      shapeGeoJson: {
        ...geoJsonPolygon,
        properties: {
          // we don't need to have collected properties for a new polygon
          // ...geoJsonPolygon.properties,
          shape: this.shape
        }
      },
      sourceName: SOURCES.main
    });
  }
  fixShapeGeoJson(geoJsonPolygon) {
    try {
      const geoJson = index_default$d(index_default$8(index_default$7(geoJsonPolygon)));
      return geoJson.features[0];
    } catch {
      return null;
    }
  }
}
class DrawRectangle extends BaseDraw {
  mode = "rectangle";
  shape = "rectangle";
  startLngLat = null;
  eventHandlers = {
    mousemove: this.onMouseMove.bind(this),
    click: this.onMouseClick.bind(this)
  };
  throttledMethods = convertToThrottled(
    {
      updateFeaturePosition: this.updateFeaturePosition
    },
    this,
    this.gm.options.settings.throttlingDelay
  );
  onStartAction() {
    this.gm.markerPointer.enable();
  }
  onEndAction() {
    this.removeTmpFeature();
    this.startLngLat = null;
    this.gm.markerPointer.disable();
    this.fireFinishEvent();
  }
  onMouseClick(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: false };
    }
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    if (this.startLngLat) {
      const geoJson = this.getFeatureGeoJson(getBboxFromTwoCoords(this.startLngLat, lngLat));
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [geoJson] });
      if (this.flags.featureCreateAllowed) {
        this.finishShape(lngLat);
      }
    } else {
      this.gm.features.clearSelection();
      const geoJson = this.getFeatureGeoJson(getBboxFromTwoCoords(lngLat, lngLat));
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [geoJson] });
      if (this.flags.featureCreateAllowed) {
        const featureData = this.startShape(lngLat);
        if (featureData) {
          const markerData = this.getControlMarkerData(["geometry", "coordinates", 4]);
          this.fireStartEvent(featureData, markerData);
        }
      }
    }
    return { next: false };
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: false };
    }
    if (!this.startLngLat) {
      this.fireMarkerPointerUpdateEvent();
      return { next: false };
    }
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    const bounds = getBboxFromTwoCoords(this.startLngLat, lngLat);
    const geoJson = this.getFeatureGeoJson(bounds);
    this.fireBeforeFeatureCreate({ geoJsonFeatures: [geoJson] });
    if (this.flags.featureCreateAllowed) {
      this.throttledMethods.updateFeaturePosition(bounds);
    }
    return { next: false };
  }
  startShape(lngLat) {
    this.startLngLat = lngLat;
    const bounds = getBboxFromTwoCoords(this.startLngLat, this.startLngLat);
    this.featureData = this.createFeature(bounds);
    return this.featureData;
  }
  finishShape(lngLat) {
    if (this.startLngLat) {
      const bounds = getBboxFromTwoCoords(this.startLngLat, lngLat);
      this.throttledMethods.updateFeaturePosition(bounds);
    }
    if (this.featureData) {
      if (this.isFeatureGeoJsonValid()) {
        this.saveFeature();
      } else {
        this.removeTmpFeature();
      }
    }
    this.startLngLat = null;
    this.fireFinishEvent();
  }
  createFeature(bounds) {
    return this.gm.features.createFeature({
      shapeGeoJson: this.getFeatureGeoJson(bounds),
      sourceName: SOURCES.temporary
    });
  }
  isFeatureGeoJsonValid() {
    if (!this.featureData) {
      return false;
    }
    return allCoordinatesEqual(this.featureData.getGeoJson());
  }
  getFeatureGeoJson(bounds) {
    const rectangleGeoJson = twoCoordsToGeoJsonRectangle(
      [bounds[0], bounds[1]],
      [bounds[2], bounds[3]]
    );
    return {
      ...rectangleGeoJson,
      properties: {
        shape: this.shape
      }
    };
  }
  updateFeaturePosition(bounds) {
    if (!this.featureData) {
      return;
    }
    const rectangleData = twoCoordsToGeoJsonRectangle(
      [bounds[0], bounds[1]],
      [bounds[2], bounds[3]]
    );
    this.featureData.updateGeoJsonGeometry(rectangleData.geometry);
    const markerData = this.getControlMarkerData(["geometry", "coordinates", 4]);
    this.fireUpdateEvent(this.featureData, markerData);
  }
  getControlMarkerData(path) {
    const controlMarker = this.gm.markerPointer.marker;
    if (!controlMarker) {
      return null;
    }
    return {
      type: "dom",
      instance: controlMarker,
      position: {
        coordinate: controlMarker.getLngLat(),
        path
      }
    };
  }
  fireStartEvent(featureData, markerData) {
    const event = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: this.shape,
      variant: null,
      action: "start",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, event);
  }
  fireUpdateEvent(featureData, markerData) {
    const event = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      actionType: "draw",
      mode: this.shape,
      variant: null,
      action: "update",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, event);
  }
  fireFinishEvent() {
    const event = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape`,
      level: "system",
      actionType: "draw",
      mode: this.shape,
      variant: null,
      action: "finish"
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, event);
  }
}
class DrawTextMarker extends BaseDraw {
  mode = "text_marker";
  shape = "text_marker";
  textarea = null;
  eventHandlers = {
    click: this.onMouseClick.bind(this),
    mousemove: this.onMouseMove.bind(this)
  };
  onStartAction() {
    this.gm.markerPointer.enable({ invisibleMarker: true });
  }
  onEndAction() {
    this.removeTextarea();
    this.removeTmpFeature();
    this.featureData = null;
    this.gm.markerPointer.disable();
    this.fireMarkerPointerFinishEvent();
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    this.fireMarkerPointerUpdateEvent();
    return { next: true };
  }
  onMouseClick(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    if (this.textarea) {
      this.endShape();
      this.gm.markerPointer.enable({ invisibleMarker: true, lngLat: event.lngLat.toArray() });
      this.fireMarkerPointerUpdateEvent();
    } else {
      this.gm.features.clearSelection();
      const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [this.getFeatureGeoJson(lngLat)] });
      if (this.flags.featureCreateAllowed) {
        this.featureData = this.createFeature(lngLat);
        this.gm.markerPointer.disable();
        this.fireMarkerPointerFinishEvent();
      }
    }
    return { next: false };
  }
  createFeature(lngLat) {
    const point2 = this.gm.mapAdapter.project(lngLat);
    this.createTextarea(point2);
    return this.gm.features.createFeature({
      shapeGeoJson: this.getFeatureGeoJson(lngLat),
      sourceName: SOURCES.temporary
    });
  }
  endShape() {
    const text2 = this.textarea?.value || "";
    this.removeTextarea();
    if (text2.trim()) {
      this.updateFeatureSource(text2);
      this.saveFeature();
    } else {
      this.removeTmpFeature();
    }
  }
  createTextarea(point2) {
    this.textarea = document.createElement("textarea");
    this.textarea.style.position = "absolute";
    this.textarea.style.left = `${point2[0]}px`;
    this.textarea.style.top = `${point2[1]}px`;
    this.textarea.style.opacity = "0.7";
    this.gm.mapAdapter.getContainer().appendChild(this.textarea);
    this.textarea.focus();
  }
  removeTextarea() {
    this.textarea?.remove();
    this.textarea = null;
  }
  getFeatureGeoJson(lngLat) {
    return {
      type: "Feature",
      properties: {
        shape: this.shape,
        text: ""
      },
      geometry: {
        type: "Point",
        coordinates: lngLat
      }
    };
  }
  updateFeatureSource(text2) {
    if (!this.featureData) {
      return;
    }
    this.featureData._updateAllProperties({ shape: this.shape, text: text2 });
  }
}
class DrawEllipse extends BaseCircle {
  mode = "ellipse";
  shape = "ellipse";
  xSemiAxisPoint = null;
  xSemiAxisLngLat = null;
  throttledMethods = convertToThrottled(
    {
      updateFeatureGeoJson: this.updateFeatureGeoJson
    },
    this,
    this.gm.options.settings.throttlingDelay
  );
  onMouseClick(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    const lngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    if (this.circleCenterLngLat && this.xSemiAxisLngLat) {
      this.fireBeforeFeatureCreate({
        geoJsonFeatures: [
          this.getEllipseGeoJson(this.circleCenterLngLat, this.xSemiAxisLngLat, lngLat)
        ]
      });
      if (this.flags.featureCreateAllowed) {
        this.saveEllipseFeature(lngLat);
        this.circleCenterLngLat = null;
        this.circleCenterPoint = null;
        this.xSemiAxisPoint = null;
        this.xSemiAxisLngLat = null;
        this.fireFinishEvent();
      }
    } else if (this.circleCenterLngLat) {
      this.fireBeforeFeatureCreate({
        geoJsonFeatures: [this.getEllipseGeoJson(this.circleCenterLngLat, lngLat)]
      });
      if (this.flags.featureCreateAllowed) {
        this.xSemiAxisLngLat = lngLat;
        this.xSemiAxisPoint = this.gm.mapAdapter.project(this.circleCenterLngLat);
      }
    } else {
      this.gm.features.clearSelection();
      this.fireBeforeFeatureCreate({ geoJsonFeatures: [this.getFeatureGeoJson(lngLat)] });
      if (this.flags.featureCreateAllowed) {
        this.circleCenterLngLat = lngLat;
        this.circleCenterPoint = this.gm.mapAdapter.project(this.circleCenterLngLat);
        this.featureData = this.createFeature();
        const markerData = this.getControlMarkerData();
        if (this.featureData && markerData) {
          this.fireStartEvent(this.featureData, markerData);
        }
      }
    }
    return { next: false };
  }
  onMouseMove() {
    if (this.circleCenterLngLat && this.gm.markerPointer.marker) {
      const markerLngLat = this.gm.markerPointer.marker.getLngLat();
      this.fireBeforeFeatureCreate({
        geoJsonFeatures: [
          this.xSemiAxisLngLat ? this.getEllipseGeoJson(this.circleCenterLngLat, this.xSemiAxisLngLat, markerLngLat) : this.getEllipseGeoJson(this.circleCenterLngLat, markerLngLat)
        ]
      });
      if (this.flags.featureCreateAllowed) {
        this.throttledMethods.updateFeatureGeoJson(markerLngLat);
      }
    }
    if (!this.circleCenterLngLat) {
      this.fireMarkerPointerUpdateEvent();
    }
    return { next: false };
  }
  updateFeatureGeoJson(eventLngLat) {
    if (!this.featureData || !this.circleCenterLngLat) {
      return;
    }
    const featureGeoJson = this.xSemiAxisLngLat ? this.getEllipseGeoJson(this.circleCenterLngLat, this.xSemiAxisLngLat, eventLngLat) : this.getEllipseGeoJson(this.circleCenterLngLat, eventLngLat);
    this.featureData.updateGeoJsonGeometry(featureGeoJson.geometry);
    this.featureData.updateGeoJsonProperties({
      __gm_shape: featureGeoJson.properties.__gm_shape
    });
    const markerData = this.getControlMarkerData();
    if (markerData) {
      this.fireUpdateEvent(this.featureData, markerData);
    }
  }
  saveEllipseFeature(eventLngLat) {
    if (!this.circleCenterLngLat || !this.xSemiAxisLngLat) {
      return;
    }
    if (this.featureData) {
      const lngLat = this.gm.markerPointer.marker?.getLngLat() || eventLngLat;
      this.updateFeatureGeoJson(lngLat);
      const { xSemiAxis, ySemiAxis, angle } = getEllipseParameters({
        center: this.circleCenterLngLat,
        xSemiAxisLngLat: this.xSemiAxisLngLat,
        rimLngLat: lngLat
      });
      this.featureData.setShapeProperty("center", this.circleCenterLngLat);
      this.featureData.setShapeProperty("xSemiAxis", xSemiAxis);
      this.featureData.setShapeProperty("ySemiAxis", ySemiAxis);
      this.featureData.setShapeProperty("angle", angle);
      if (this.isFeatureGeoJsonValid()) {
        this.saveFeature();
      } else {
        this.removeTmpFeature();
      }
    }
  }
  isFeatureGeoJsonValid() {
    if (!this.featureData) {
      return false;
    }
    return allCoordinatesEqual(this.featureData.getGeoJson());
  }
  getEllipseGeoJson(center2, xSemiAxisLngLat, rimLngLat) {
    const { xSemiAxis, ySemiAxis, angle } = getEllipseParameters({
      center: center2,
      xSemiAxisLngLat,
      rimLngLat
    });
    return getGeoJsonEllipse({ center: center2, xSemiAxis, ySemiAxis, angle });
  }
}
const drawClassMap = {
  marker: DrawMarker,
  ellipse: DrawEllipse,
  circle: DrawCircle,
  circle_marker: DrawCircleMarker,
  text_marker: DrawTextMarker,
  line: DrawLine,
  rectangle: DrawRectangle,
  polygon: DrawPolygon,
  freehand: null,
  custom_shape: null
};
const createDrawInstance = (gm, shape) => {
  if (drawClassMap[shape]) {
    return new drawClassMap[shape](gm);
  }
  log.error(`Draw "${shape}" is not available`);
  return null;
};
const isGmEditEvent = (payload) => {
  return isGmEvent(payload) && payload.actionType === "edit";
};
const isModeType = (name) => {
  return MODE_TYPES.includes(name);
};
const isActionType = (name) => {
  return ACTION_TYPES.includes(name);
};
const isDrawModeName = (name) => {
  return DRAW_MODES.includes(name);
};
const isEditModeName = (name) => {
  return EDIT_MODES.includes(name);
};
const isHelperModeName = (name) => {
  return HELPER_MODES.includes(name);
};
const isModeName = (name) => {
  return isDrawModeName(name) || isEditModeName(name) || isHelperModeName(name);
};
class DrawEventListener extends BaseEventListener {
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:draw`]: this.handleDrawEvent.bind(this)
  };
  constructor(gm, bus) {
    super(gm);
    bus.attachEvents(this.eventHandlers);
  }
  handleDrawEvent(payload) {
    if (!isGmDrawEvent(payload)) {
      return { next: true };
    }
    const actionInstanceKey = `${payload.actionType}__${payload.mode}`;
    if (payload.action === "mode_start") {
      this.trackExclusiveModes(payload);
      this.start(actionInstanceKey, payload);
      this.trackRelatedModes(payload);
    } else if (payload.action === "mode_end") {
      this.trackRelatedModes(payload);
      this.end(actionInstanceKey);
    }
    return { next: true };
  }
  start(actionInstanceKey, payload) {
    const actionInstance = createDrawInstance(this.gm, payload.mode);
    if (!actionInstance) {
      return;
    }
    if (actionInstanceKey in this.gm.actionInstances) {
      log.error(`Action instance "${actionInstanceKey}" already exists`);
    }
    this.gm.actionInstances[actionInstanceKey] = actionInstance;
    actionInstance.startAction();
  }
  end(actionInstanceKey) {
    const actionInstance = this.gm.actionInstances[actionInstanceKey];
    if (actionInstance instanceof BaseDraw) {
      actionInstance.endAction();
      delete this.gm.actionInstances[actionInstanceKey];
    } else {
      console.error(`Wrong action instance for draw event "${actionInstanceKey}":`, actionInstance);
    }
  }
}
class BaseEdit extends BaseAction {
  actionType = "edit";
  featureData = null;
  cursorExcludedLayerIds = ["rectangle-line", "polygon-line", "circle-line"];
  layerEventHandlersData = [];
  startAction() {
    this.setEventsForLayers("mouseenter", this.setCursorToPointer.bind(this));
    this.setEventsForLayers("mouseleave", this.setCursorToEmpty.bind(this));
    super.startAction();
  }
  endAction() {
    this.clearEventsForLayers();
    super.endAction();
  }
  setCursorToPointer() {
    if (this.flags.actionInProgress) {
      return;
    }
    this.gm.mapAdapter.setCursor("pointer");
  }
  setCursorToEmpty() {
    if (this.flags.actionInProgress) {
      return;
    }
    this.gm.mapAdapter.setCursor("");
  }
  getFeatureByMouseEvent({
    event,
    sourceNames
  }) {
    const featureData = this.gm.features.getFeatureByMouseEvent({
      event,
      sourceNames
    });
    if (!featureData || featureData.getShapeProperty("disableEdit") === true) {
      return null;
    }
    return featureData;
  }
  setEventsForLayers(eventName, callback) {
    const targetLayerIds = this.gm.features.layers.map((layer) => layer.id).filter(
      (layerId) => !this.cursorExcludedLayerIds.some((subName) => layerId.includes(subName))
    );
    targetLayerIds.forEach((layerId) => {
      this.gm.mapAdapter.on(eventName, layerId, callback);
      this.layerEventHandlersData.push({ eventName, layerId, callback });
    });
  }
  clearEventsForLayers() {
    this.layerEventHandlersData.forEach(({ eventName, layerId, callback }) => {
      this.gm.mapAdapter.off(eventName, layerId, callback);
    });
    this.layerEventHandlersData = [];
  }
  updateFeatureGeoJson({
    featureData,
    featureGeoJson,
    forceMode = void 0
  }) {
    if (!this.flags.featureUpdateAllowed) {
      return false;
    }
    featureData.updateGeoJsonGeometry(featureGeoJson.geometry);
    if (!isEqual(featureData.getGeoJson().properties, featureGeoJson.properties)) {
      featureData._updateAllProperties(featureGeoJson.properties);
    }
    this.fireFeatureUpdatedEvent({
      sourceFeatures: [featureData],
      targetFeatures: [featureData],
      forceMode
    });
    return true;
  }
  fireBeforeFeatureUpdate({
    features,
    geoJsonFeatures,
    forceMode = void 0
  }) {
    this.flags.featureUpdateAllowed = true;
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:feature:before_update`,
      level: "system",
      actionType: "edit",
      mode: forceMode || this.mode,
      action: "before_update",
      features,
      geoJsonFeatures
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:${this.actionType}`, payload);
  }
  fireFeatureUpdatedEvent({
    sourceFeatures,
    targetFeatures,
    markerData = void 0,
    forceMode = void 0
  }) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:feature_updated`,
      level: "system",
      actionType: "edit",
      action: "feature_updated",
      mode: forceMode || this.mode,
      sourceFeatures,
      targetFeatures,
      markerData: markerData || null
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
  fireFeatureEditStartEvent({
    feature: feature2,
    forceMode = void 0
  }) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:feature_edit_start`,
      level: "system",
      actionType: "edit",
      action: "feature_edit_start",
      mode: forceMode || this.mode,
      feature: feature2
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
  fireFeatureEditEndEvent({
    feature: feature2,
    forceMode = void 0
  }) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:feature_edit_end`,
      level: "system",
      actionType: "edit",
      action: "feature_edit_end",
      mode: forceMode || this.mode,
      feature: feature2
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
  fireMarkerPointerUpdateEvent() {
    if (!this.gm.markerPointer.marker) {
      return;
    }
    const marker2 = this.gm.markerPointer.marker;
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
      level: "system",
      variant: null,
      actionType: "draw",
      mode: this.getLineDrawerMode(),
      action: "update",
      markerData: {
        type: "dom",
        instance: marker2,
        position: {
          coordinate: marker2.getLngLat(),
          path: [-1]
        }
      },
      featureData: null
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
  }
  forwardLineDrawerEvent(payload) {
    if (!isGmDrawLineDrawerEvent(payload) || !["cut", "split"].includes(this.mode)) {
      return { next: true };
    }
    if (payload.action === "start" || payload.action === "update") {
      const eventData = {
        name: `${GM_SYSTEM_PREFIX}:draw:shape_with_data`,
        level: "system",
        actionType: "draw",
        mode: this.getLineDrawerMode(),
        variant: null,
        action: payload.action,
        featureData: payload.featureData,
        markerData: payload.markerData
      };
      this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, eventData);
    } else if (payload.action === "finish" || payload.action === "cancel") {
      const eventData = {
        name: `${GM_SYSTEM_PREFIX}:draw:shape`,
        level: "system",
        actionType: "draw",
        mode: this.getLineDrawerMode(),
        variant: null,
        action: payload.action
      };
      this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, eventData);
    }
    return { next: true };
  }
  fireFeatureRemovedEvent(featureData) {
    if (includesWithType(featureData.shape, SHAPE_NAMES)) {
      const payload = {
        name: `${GM_SYSTEM_PREFIX}:edit:feature_removed`,
        level: "system",
        actionType: "edit",
        mode: featureData.shape,
        action: "feature_removed",
        featureData
      };
      this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
    }
  }
  getLineDrawerMode() {
    if (this.mode === "cut") {
      return "polygon";
    } else if (this.mode === "split") {
      return "line";
    }
    return "line";
  }
}
function center(geojson, options = {}) {
  const ext = bbox$1(geojson);
  const x = (ext[0] + ext[2]) / 2;
  const y = (ext[1] + ext[3]) / 2;
  return point([x, y], options.properties, options);
}
var jsts_min$1 = { exports: {} };
var jsts_min = jsts_min$1.exports;
var hasRequiredJsts_min;
function requireJsts_min() {
  if (hasRequiredJsts_min) return jsts_min$1.exports;
  hasRequiredJsts_min = 1;
  (function(module2, exports2) {
    !(function(t, e) {
      module2.exports = e();
    })(jsts_min, (function() {
      function t(t2, e2) {
        (null == e2 || e2 > t2.length) && (e2 = t2.length);
        for (var n2 = 0, i2 = Array(e2); n2 < e2; n2++) i2[n2] = t2[n2];
        return i2;
      }
      function e(t2, e2, n2) {
        return e2 = u2(e2), (function(t3, e3) {
          if (e3 && ("object" == typeof e3 || "function" == typeof e3)) return e3;
          if (void 0 !== e3) throw new TypeError("Derived constructors may only return object or undefined");
          return (function(t4) {
            if (void 0 === t4) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
            return t4;
          })(t3);
        })(t2, h() ? Reflect.construct(e2, n2 || [], u2(t2).constructor) : e2.apply(t2, n2));
      }
      function n(t2, e2) {
        if (!(t2 instanceof e2)) throw new TypeError("Cannot call a class as a function");
      }
      function i(t2, e2, n2) {
        if (h()) return Reflect.construct.apply(null, arguments);
        var i2 = [null];
        i2.push.apply(i2, e2);
        var r3 = new (t2.bind.apply(t2, i2))();
        return n2 && c(r3, n2.prototype), r3;
      }
      function r2(t2, e2) {
        for (var n2 = 0; n2 < e2.length; n2++) {
          var i2 = e2[n2];
          i2.enumerable = i2.enumerable || false, i2.configurable = true, "value" in i2 && (i2.writable = true), Object.defineProperty(t2, v(i2.key), i2);
        }
      }
      function s(t2, e2, n2) {
        return e2 && r2(t2.prototype, e2), n2 && r2(t2, n2), Object.defineProperty(t2, "prototype", { writable: false }), t2;
      }
      function a(t2, e2) {
        var n2 = "undefined" != typeof Symbol && t2[Symbol.iterator] || t2["@@iterator"];
        if (!n2) {
          if (Array.isArray(t2) || (n2 = y(t2)) || e2) {
            n2 && (t2 = n2);
            var i2 = 0, r3 = function() {
            };
            return { s: r3, n: function() {
              return i2 >= t2.length ? { done: true } : { done: false, value: t2[i2++] };
            }, e: function(t3) {
              throw t3;
            }, f: r3 };
          }
          throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
        }
        var s2, a2 = true, o2 = false;
        return { s: function() {
          n2 = n2.call(t2);
        }, n: function() {
          var t3 = n2.next();
          return a2 = t3.done, t3;
        }, e: function(t3) {
          o2 = true, s2 = t3;
        }, f: function() {
          try {
            a2 || null == n2.return || n2.return();
          } finally {
            if (o2) throw s2;
          }
        } };
      }
      function o() {
        return o = "undefined" != typeof Reflect && Reflect.get ? Reflect.get.bind() : function(t2, e2, n2) {
          var i2 = (function(t3, e3) {
            for (; !{}.hasOwnProperty.call(t3, e3) && null !== (t3 = u2(t3)); ) ;
            return t3;
          })(t2, e2);
          if (i2) {
            var r3 = Object.getOwnPropertyDescriptor(i2, e2);
            return r3.get ? r3.get.call(arguments.length < 3 ? t2 : n2) : r3.value;
          }
        }, o.apply(null, arguments);
      }
      function u2(t2) {
        return u2 = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(t3) {
          return t3.__proto__ || Object.getPrototypeOf(t3);
        }, u2(t2);
      }
      function l(t2, e2) {
        if ("function" != typeof e2 && null !== e2) throw new TypeError("Super expression must either be null or a function");
        t2.prototype = Object.create(e2 && e2.prototype, { constructor: { value: t2, writable: true, configurable: true } }), Object.defineProperty(t2, "prototype", { writable: false }), e2 && c(t2, e2);
      }
      function h() {
        try {
          var t2 = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], (function() {
          })));
        } catch (t3) {
        }
        return (h = function() {
          return !!t2;
        })();
      }
      function c(t2, e2) {
        return c = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t3, e3) {
          return t3.__proto__ = e3, t3;
        }, c(t2, e2);
      }
      function f(t2, e2, n2, i2) {
        var r3 = o(u2(1 & i2 ? t2.prototype : t2), e2, n2);
        return 2 & i2 && "function" == typeof r3 ? function(t3) {
          return r3.apply(n2, t3);
        } : r3;
      }
      function g(e2) {
        return (function(e3) {
          if (Array.isArray(e3)) return t(e3);
        })(e2) || (function(t2) {
          if ("undefined" != typeof Symbol && null != t2[Symbol.iterator] || null != t2["@@iterator"]) return Array.from(t2);
        })(e2) || y(e2) || (function() {
          throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
        })();
      }
      function v(t2) {
        var e2 = (function(t3, e3) {
          if ("object" != typeof t3 || !t3) return t3;
          var n2 = t3[Symbol.toPrimitive];
          if (void 0 !== n2) {
            var i2 = n2.call(t3, e3);
            if ("object" != typeof i2) return i2;
            throw new TypeError("@@toPrimitive must return a primitive value.");
          }
          return String(t3);
        })(t2, "string");
        return "symbol" == typeof e2 ? e2 : e2 + "";
      }
      function y(e2, n2) {
        if (e2) {
          if ("string" == typeof e2) return t(e2, n2);
          var i2 = {}.toString.call(e2).slice(8, -1);
          return "Object" === i2 && e2.constructor && (i2 = e2.constructor.name), "Map" === i2 || "Set" === i2 ? Array.from(e2) : "Arguments" === i2 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(i2) ? t(e2, n2) : void 0;
        }
      }
      function d(t2) {
        var e2 = "function" == typeof Map ? /* @__PURE__ */ new Map() : void 0;
        return d = function(t3) {
          if (null === t3 || !(function(t4) {
            try {
              return -1 !== Function.toString.call(t4).indexOf("[native code]");
            } catch (e3) {
              return "function" == typeof t4;
            }
          })(t3)) return t3;
          if ("function" != typeof t3) throw new TypeError("Super expression must either be null or a function");
          if (void 0 !== e2) {
            if (e2.has(t3)) return e2.get(t3);
            e2.set(t3, n2);
          }
          function n2() {
            return i(t3, arguments, u2(this).constructor);
          }
          return n2.prototype = Object.create(t3.prototype, { constructor: { value: n2, enumerable: false, writable: true, configurable: true } }), c(n2, t3);
        }, d(t2);
      }
      var _ = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getEndCapStyle", value: function() {
          return this._endCapStyle;
        } }, { key: "isSingleSided", value: function() {
          return this._isSingleSided;
        } }, { key: "setQuadrantSegments", value: function(e2) {
          this._quadrantSegments = e2, 0 === this._quadrantSegments && (this._joinStyle = t2.JOIN_BEVEL), this._quadrantSegments < 0 && (this._joinStyle = t2.JOIN_MITRE, this._mitreLimit = Math.abs(this._quadrantSegments)), e2 <= 0 && (this._quadrantSegments = 1), this._joinStyle !== t2.JOIN_ROUND && (this._quadrantSegments = t2.DEFAULT_QUADRANT_SEGMENTS);
        } }, { key: "getJoinStyle", value: function() {
          return this._joinStyle;
        } }, { key: "setJoinStyle", value: function(t3) {
          this._joinStyle = t3;
        } }, { key: "setSimplifyFactor", value: function(t3) {
          this._simplifyFactor = t3 < 0 ? 0 : t3;
        } }, { key: "getSimplifyFactor", value: function() {
          return this._simplifyFactor;
        } }, { key: "getQuadrantSegments", value: function() {
          return this._quadrantSegments;
        } }, { key: "setEndCapStyle", value: function(t3) {
          this._endCapStyle = t3;
        } }, { key: "getMitreLimit", value: function() {
          return this._mitreLimit;
        } }, { key: "setMitreLimit", value: function(t3) {
          this._mitreLimit = t3;
        } }, { key: "setSingleSided", value: function(t3) {
          this._isSingleSided = t3;
        } }], [{ key: "constructor_", value: function() {
          if (this._quadrantSegments = t2.DEFAULT_QUADRANT_SEGMENTS, this._endCapStyle = t2.CAP_ROUND, this._joinStyle = t2.JOIN_ROUND, this._mitreLimit = t2.DEFAULT_MITRE_LIMIT, this._isSingleSided = false, this._simplifyFactor = t2.DEFAULT_SIMPLIFY_FACTOR, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            this.setQuadrantSegments(e2);
          } else if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            this.setQuadrantSegments(n2), this.setEndCapStyle(i2);
          } else if (4 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1], a2 = arguments[2], o2 = arguments[3];
            this.setQuadrantSegments(r3), this.setEndCapStyle(s2), this.setJoinStyle(a2), this.setMitreLimit(o2);
          }
        } }, { key: "bufferDistanceError", value: function(t3) {
          var e2 = Math.PI / 2 / t3;
          return 1 - Math.cos(e2 / 2);
        } }]);
      })();
      _.CAP_ROUND = 1, _.CAP_FLAT = 2, _.CAP_SQUARE = 3, _.JOIN_ROUND = 1, _.JOIN_MITRE = 2, _.JOIN_BEVEL = 3, _.DEFAULT_QUADRANT_SEGMENTS = 8, _.DEFAULT_MITRE_LIMIT = 5, _.DEFAULT_SIMPLIFY_FACTOR = 0.01;
      var p = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ Exception: i2 })[0], r3;
        }
        return l(i2, t2), s(i2, [{ key: "toString", value: function() {
          return this.message;
        } }]);
      })(d(Error)), m = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ IllegalArgumentException: i2 })[0], r3;
        }
        return l(i2, t2), s(i2);
      })(p), k = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "filter", value: function(t2) {
        } }]);
      })();
      function x() {
      }
      function I() {
      }
      function E() {
      }
      var N, T, S, L, C, R, w, O, b = (function() {
        return s((function t2() {
          n(this, t2);
        }), null, [{ key: "equalsWithTolerance", value: function(t2, e2, n2) {
          return Math.abs(t2 - e2) <= n2;
        } }]);
      })(), M = (function() {
        return s((function t2(e2, i2) {
          n(this, t2), this.low = i2 || 0, this.high = e2 || 0;
        }), null, [{ key: "toBinaryString", value: function(t2) {
          var e2, n2 = "";
          for (e2 = 2147483648; e2 > 0; e2 >>>= 1) n2 += (t2.high & e2) === e2 ? "1" : "0";
          for (e2 = 2147483648; e2 > 0; e2 >>>= 1) n2 += (t2.low & e2) === e2 ? "1" : "0";
          return n2;
        } }]);
      })();
      function A() {
      }
      function P() {
      }
      A.NaN = NaN, A.isNaN = function(t2) {
        return Number.isNaN(t2);
      }, A.isInfinite = function(t2) {
        return !Number.isFinite(t2);
      }, A.MAX_VALUE = Number.MAX_VALUE, A.POSITIVE_INFINITY = Number.POSITIVE_INFINITY, A.NEGATIVE_INFINITY = Number.NEGATIVE_INFINITY, "function" == typeof Float64Array && "function" == typeof Int32Array ? (R = 2146435072, w = new Float64Array(1), O = new Int32Array(w.buffer), A.doubleToLongBits = function(t2) {
        w[0] = t2;
        var e2 = 0 | O[0], n2 = 0 | O[1];
        return (n2 & R) === R && 1048575 & n2 && 0 !== e2 && (e2 = 0, n2 = 2146959360), new M(n2, e2);
      }, A.longBitsToDouble = function(t2) {
        return O[0] = t2.low, O[1] = t2.high, w[0];
      }) : (N = 1023, T = Math.log2, S = Math.floor, L = Math.pow, C = (function() {
        for (var t2 = 53; t2 > 0; t2--) {
          var e2 = L(2, t2) - 1;
          if (S(T(e2)) + 1 === t2) return e2;
        }
        return 0;
      })(), A.doubleToLongBits = function(t2) {
        var e2, n2, i2, r3, s2, a2, o2, u3, l2;
        if (t2 < 0 || 1 / t2 === Number.NEGATIVE_INFINITY ? (a2 = 1 << 31, t2 = -t2) : a2 = 0, 0 === t2) return new M(u3 = a2, l2 = 0);
        if (t2 === 1 / 0) return new M(u3 = 2146435072 | a2, l2 = 0);
        if (t2 != t2) return new M(u3 = 2146959360, l2 = 0);
        if (r3 = 0, l2 = 0, (e2 = S(t2)) > 1) if (e2 <= C) (r3 = S(T(e2))) <= 20 ? (l2 = 0, u3 = e2 << 20 - r3 & 1048575) : (l2 = e2 % (n2 = L(2, i2 = r3 - 20)) << 32 - i2, u3 = e2 / n2 & 1048575);
        else for (i2 = e2, l2 = 0; 0 !== (i2 = S(n2 = i2 / 2)); ) r3++, l2 >>>= 1, l2 |= (1 & u3) << 31, u3 >>>= 1, n2 !== i2 && (u3 |= 524288);
        if (o2 = r3 + N, s2 = 0 === e2, e2 = t2 - e2, r3 < 52 && 0 !== e2) for (i2 = 0; ; ) {
          if ((n2 = 2 * e2) >= 1 ? (e2 = n2 - 1, s2 ? (o2--, s2 = false) : (i2 <<= 1, i2 |= 1, r3++)) : (e2 = n2, s2 ? 0 == --o2 && (r3++, s2 = false) : (i2 <<= 1, r3++)), 20 === r3) u3 |= i2, i2 = 0;
          else if (52 === r3) {
            l2 |= i2;
            break;
          }
          if (1 === n2) {
            r3 < 20 ? u3 |= i2 << 20 - r3 : r3 < 52 && (l2 |= i2 << 52 - r3);
            break;
          }
        }
        return u3 |= o2 << 20, new M(u3 |= a2, l2);
      }, A.longBitsToDouble = function(t2) {
        var e2, n2, i2, r3, s2 = t2.high, a2 = t2.low, o2 = s2 & 1 << 31 ? -1 : 1;
        for (i2 = ((2146435072 & s2) >> 20) - N, r3 = 0, n2 = 1 << 19, e2 = 1; e2 <= 20; e2++) s2 & n2 && (r3 += L(2, -e2)), n2 >>>= 1;
        for (n2 = 1 << 31, e2 = 21; e2 <= 52; e2++) a2 & n2 && (r3 += L(2, -e2)), n2 >>>= 1;
        if (-1023 === i2) {
          if (0 === r3) return 0 * o2;
          i2 = -1022;
        } else {
          if (1024 === i2) return 0 === r3 ? o2 / 0 : NaN;
          r3 += 1;
        }
        return o2 * r3 * L(2, i2);
      });
      var D2 = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ RuntimeException: i2 })[0], r3;
        }
        return l(i2, t2), s(i2);
      })(p), F = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, null, [{ key: "constructor_", value: function() {
          if (0 === arguments.length) D2.constructor_.call(this);
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            D2.constructor_.call(this, t3);
          }
        } }]);
      })(D2), G = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "shouldNeverReachHere", value: function() {
          if (0 === arguments.length) t2.shouldNeverReachHere(null);
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            throw new F("Should never reach here" + (null !== e2 ? ": " + e2 : ""));
          }
        } }, { key: "isTrue", value: function() {
          if (1 === arguments.length) {
            var e2 = arguments[0];
            t2.isTrue(e2, null);
          } else if (2 === arguments.length) {
            var n2 = arguments[1];
            if (!arguments[0]) throw null === n2 ? new F() : new F(n2);
          }
        } }, { key: "equals", value: function() {
          if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            t2.equals(e2, n2, null);
          } else if (3 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1], s2 = arguments[2];
            if (!r3.equals(i2)) throw new F("Expected " + i2 + " but encountered " + r3 + (null !== s2 ? ": " + s2 : ""));
          }
        } }]);
      })(), q = new ArrayBuffer(8), Y = new Float64Array(q), z = new Int32Array(q), X = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getM", value: function() {
          return A.NaN;
        } }, { key: "setOrdinate", value: function(e2, n2) {
          switch (e2) {
            case t2.X:
              this.x = n2;
              break;
            case t2.Y:
              this.y = n2;
              break;
            case t2.Z:
              this.setZ(n2);
              break;
            default:
              throw new m("Invalid ordinate index: " + e2);
          }
        } }, { key: "equals2D", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.x === t3.x && this.y === t3.y;
          }
          if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            return !!b.equalsWithTolerance(this.x, e2.x, n2) && !!b.equalsWithTolerance(this.y, e2.y, n2);
          }
        } }, { key: "setM", value: function(e2) {
          throw new m("Invalid ordinate index: " + t2.M);
        } }, { key: "getZ", value: function() {
          return this.z;
        } }, { key: "getOrdinate", value: function(e2) {
          switch (e2) {
            case t2.X:
              return this.x;
            case t2.Y:
              return this.y;
            case t2.Z:
              return this.getZ();
          }
          throw new m("Invalid ordinate index: " + e2);
        } }, { key: "equals3D", value: function(t3) {
          return this.x === t3.x && this.y === t3.y && (this.getZ() === t3.getZ() || A.isNaN(this.getZ()) && A.isNaN(t3.getZ()));
        } }, { key: "equals", value: function(e2) {
          return e2 instanceof t2 && this.equals2D(e2);
        } }, { key: "equalInZ", value: function(t3, e2) {
          return b.equalsWithTolerance(this.getZ(), t3.getZ(), e2);
        } }, { key: "setX", value: function(t3) {
          this.x = t3;
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3;
          return this.x < e2.x ? -1 : this.x > e2.x ? 1 : this.y < e2.y ? -1 : this.y > e2.y ? 1 : 0;
        } }, { key: "getX", value: function() {
          return this.x;
        } }, { key: "setZ", value: function(t3) {
          this.z = t3;
        } }, { key: "clone", value: function() {
          try {
            return null;
          } catch (t3) {
            if (t3 instanceof CloneNotSupportedException) return G.shouldNeverReachHere("this shouldn't happen because this class is Cloneable"), null;
            throw t3;
          }
        } }, { key: "copy", value: function() {
          return new t2(this);
        } }, { key: "toString", value: function() {
          return "(" + this.x + ", " + this.y + ", " + this.getZ() + ")";
        } }, { key: "distance3D", value: function(t3) {
          var e2 = this.x - t3.x, n2 = this.y - t3.y, i2 = this.getZ() - t3.getZ();
          return Math.sqrt(e2 * e2 + n2 * n2 + i2 * i2);
        } }, { key: "getY", value: function() {
          return this.y;
        } }, { key: "setY", value: function(t3) {
          this.y = t3;
        } }, { key: "distance", value: function(t3) {
          var e2 = this.x - t3.x, n2 = this.y - t3.y;
          return Math.sqrt(e2 * e2 + n2 * n2);
        } }, { key: "hashCode", value: function() {
          var e2 = 17;
          return e2 = 37 * (e2 = 37 * e2 + t2.hashCode(this.x)) + t2.hashCode(this.y);
        } }, { key: "setCoordinate", value: function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ();
        } }, { key: "interfaces_", get: function() {
          return [x, I, E];
        } }], [{ key: "constructor_", value: function() {
          if (this.x = null, this.y = null, this.z = null, 0 === arguments.length) t2.constructor_.call(this, 0, 0);
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            t2.constructor_.call(this, e2.x, e2.y, e2.getZ());
          } else if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            t2.constructor_.call(this, n2, i2, t2.NULL_ORDINATE);
          } else if (3 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1], a2 = arguments[2];
            this.x = r3, this.y = s2, this.z = a2;
          }
        } }, { key: "hashCode", value: function(t3) {
          return Y[0] = t3, z[0] ^ z[1];
        } }]);
      })(), B2 = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "compare", value: function(e2, n2) {
          var i2 = t2.compare(e2.x, n2.x);
          if (0 !== i2) return i2;
          var r3 = t2.compare(e2.y, n2.y);
          return 0 !== r3 ? r3 : this._dimensionsToTest <= 2 ? 0 : t2.compare(e2.getZ(), n2.getZ());
        } }, { key: "interfaces_", get: function() {
          return [P];
        } }], [{ key: "constructor_", value: function() {
          if (this._dimensionsToTest = 2, 0 === arguments.length) t2.constructor_.call(this, 2);
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            if (2 !== e2 && 3 !== e2) throw new m("only 2 or 3 dimensions may be specified");
            this._dimensionsToTest = e2;
          }
        } }, { key: "compare", value: function(t3, e2) {
          return t3 < e2 ? -1 : t3 > e2 ? 1 : A.isNaN(t3) ? A.isNaN(e2) ? 0 : -1 : A.isNaN(e2) ? 1 : 0;
        } }]);
      })();
      X.DimensionalComparator = B2, X.NULL_ORDINATE = A.NaN, X.X = 0, X.Y = 1, X.Z = 2, X.M = 3;
      var U = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getArea", value: function() {
          return this.getWidth() * this.getHeight();
        } }, { key: "equals", value: function(e2) {
          if (!(e2 instanceof t2)) return false;
          var n2 = e2;
          return this.isNull() ? n2.isNull() : this._maxx === n2.getMaxX() && this._maxy === n2.getMaxY() && this._minx === n2.getMinX() && this._miny === n2.getMinY();
        } }, { key: "intersection", value: function(e2) {
          if (this.isNull() || e2.isNull() || !this.intersects(e2)) return new t2();
          var n2 = this._minx > e2._minx ? this._minx : e2._minx, i2 = this._miny > e2._miny ? this._miny : e2._miny;
          return new t2(n2, this._maxx < e2._maxx ? this._maxx : e2._maxx, i2, this._maxy < e2._maxy ? this._maxy : e2._maxy);
        } }, { key: "isNull", value: function() {
          return this._maxx < this._minx;
        } }, { key: "getMaxX", value: function() {
          return this._maxx;
        } }, { key: "covers", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              return this.covers(e2.x, e2.y);
            }
            if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              return !this.isNull() && !n2.isNull() && (n2.getMinX() >= this._minx && n2.getMaxX() <= this._maxx && n2.getMinY() >= this._miny && n2.getMaxY() <= this._maxy);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            return !this.isNull() && (i2 >= this._minx && i2 <= this._maxx && r3 >= this._miny && r3 <= this._maxy);
          }
        } }, { key: "intersects", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e2 = arguments[0];
              return !this.isNull() && !e2.isNull() && !(e2._minx > this._maxx || e2._maxx < this._minx || e2._miny > this._maxy || e2._maxy < this._miny);
            }
            if (arguments[0] instanceof X) {
              var n2 = arguments[0];
              return this.intersects(n2.x, n2.y);
            }
          } else if (2 === arguments.length) {
            if (arguments[0] instanceof X && arguments[1] instanceof X) {
              var i2 = arguments[0], r3 = arguments[1];
              return !this.isNull() && (!((i2.x < r3.x ? i2.x : r3.x) > this._maxx) && (!((i2.x > r3.x ? i2.x : r3.x) < this._minx) && (!((i2.y < r3.y ? i2.y : r3.y) > this._maxy) && !((i2.y > r3.y ? i2.y : r3.y) < this._miny))));
            }
            if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
              var s2 = arguments[0], a2 = arguments[1];
              return !this.isNull() && !(s2 > this._maxx || s2 < this._minx || a2 > this._maxy || a2 < this._miny);
            }
          }
        } }, { key: "getMinY", value: function() {
          return this._miny;
        } }, { key: "getDiameter", value: function() {
          if (this.isNull()) return 0;
          var t3 = this.getWidth(), e2 = this.getHeight();
          return Math.sqrt(t3 * t3 + e2 * e2);
        } }, { key: "getMinX", value: function() {
          return this._minx;
        } }, { key: "expandToInclude", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              this.expandToInclude(e2.x, e2.y);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              if (n2.isNull()) return null;
              this.isNull() ? (this._minx = n2.getMinX(), this._maxx = n2.getMaxX(), this._miny = n2.getMinY(), this._maxy = n2.getMaxY()) : (n2._minx < this._minx && (this._minx = n2._minx), n2._maxx > this._maxx && (this._maxx = n2._maxx), n2._miny < this._miny && (this._miny = n2._miny), n2._maxy > this._maxy && (this._maxy = n2._maxy));
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            this.isNull() ? (this._minx = i2, this._maxx = i2, this._miny = r3, this._maxy = r3) : (i2 < this._minx && (this._minx = i2), i2 > this._maxx && (this._maxx = i2), r3 < this._miny && (this._miny = r3), r3 > this._maxy && (this._maxy = r3));
          }
        } }, { key: "minExtent", value: function() {
          if (this.isNull()) return 0;
          var t3 = this.getWidth(), e2 = this.getHeight();
          return t3 < e2 ? t3 : e2;
        } }, { key: "getWidth", value: function() {
          return this.isNull() ? 0 : this._maxx - this._minx;
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3;
          return this.isNull() ? e2.isNull() ? 0 : -1 : e2.isNull() ? 1 : this._minx < e2._minx ? -1 : this._minx > e2._minx ? 1 : this._miny < e2._miny ? -1 : this._miny > e2._miny ? 1 : this._maxx < e2._maxx ? -1 : this._maxx > e2._maxx ? 1 : this._maxy < e2._maxy ? -1 : this._maxy > e2._maxy ? 1 : 0;
        } }, { key: "translate", value: function(t3, e2) {
          if (this.isNull()) return null;
          this.init(this.getMinX() + t3, this.getMaxX() + t3, this.getMinY() + e2, this.getMaxY() + e2);
        } }, { key: "copy", value: function() {
          return new t2(this);
        } }, { key: "toString", value: function() {
          return "Env[" + this._minx + " : " + this._maxx + ", " + this._miny + " : " + this._maxy + "]";
        } }, { key: "setToNull", value: function() {
          this._minx = 0, this._maxx = -1, this._miny = 0, this._maxy = -1;
        } }, { key: "disjoint", value: function(t3) {
          return !(!this.isNull() && !t3.isNull()) || (t3._minx > this._maxx || t3._maxx < this._minx || t3._miny > this._maxy || t3._maxy < this._miny);
        } }, { key: "getHeight", value: function() {
          return this.isNull() ? 0 : this._maxy - this._miny;
        } }, { key: "maxExtent", value: function() {
          if (this.isNull()) return 0;
          var t3 = this.getWidth(), e2 = this.getHeight();
          return t3 > e2 ? t3 : e2;
        } }, { key: "expandBy", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.expandBy(t3, t3);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            if (this.isNull()) return null;
            this._minx -= e2, this._maxx += e2, this._miny -= n2, this._maxy += n2, (this._minx > this._maxx || this._miny > this._maxy) && this.setToNull();
          }
        } }, { key: "contains", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e2 = arguments[0];
              return this.covers(e2);
            }
            if (arguments[0] instanceof X) {
              var n2 = arguments[0];
              return this.covers(n2);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            return this.covers(i2, r3);
          }
        } }, { key: "centre", value: function() {
          return this.isNull() ? null : new X((this.getMinX() + this.getMaxX()) / 2, (this.getMinY() + this.getMaxY()) / 2);
        } }, { key: "init", value: function() {
          if (0 === arguments.length) this.setToNull();
          else if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              this.init(e2.x, e2.x, e2.y, e2.y);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this._minx = n2._minx, this._maxx = n2._maxx, this._miny = n2._miny, this._maxy = n2._maxy;
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            this.init(i2.x, r3.x, i2.y, r3.y);
          } else if (4 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u3 = arguments[3];
            s2 < a2 ? (this._minx = s2, this._maxx = a2) : (this._minx = a2, this._maxx = s2), o2 < u3 ? (this._miny = o2, this._maxy = u3) : (this._miny = u3, this._maxy = o2);
          }
        } }, { key: "getMaxY", value: function() {
          return this._maxy;
        } }, { key: "distance", value: function(t3) {
          if (this.intersects(t3)) return 0;
          var e2 = 0;
          this._maxx < t3._minx ? e2 = t3._minx - this._maxx : this._minx > t3._maxx && (e2 = this._minx - t3._maxx);
          var n2 = 0;
          return this._maxy < t3._miny ? n2 = t3._miny - this._maxy : this._miny > t3._maxy && (n2 = this._miny - t3._maxy), 0 === e2 ? n2 : 0 === n2 ? e2 : Math.sqrt(e2 * e2 + n2 * n2);
        } }, { key: "hashCode", value: function() {
          var t3 = 17;
          return t3 = 37 * (t3 = 37 * (t3 = 37 * (t3 = 37 * t3 + X.hashCode(this._minx)) + X.hashCode(this._maxx)) + X.hashCode(this._miny)) + X.hashCode(this._maxy);
        } }, { key: "interfaces_", get: function() {
          return [x, E];
        } }], [{ key: "constructor_", value: function() {
          if (this._minx = null, this._maxx = null, this._miny = null, this._maxy = null, 0 === arguments.length) this.init();
          else if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              this.init(e2.x, e2.x, e2.y, e2.y);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this.init(n2);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            this.init(i2.x, r3.x, i2.y, r3.y);
          } else if (4 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u3 = arguments[3];
            this.init(s2, a2, o2, u3);
          }
        } }, { key: "intersects", value: function() {
          if (3 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
            return n2.x >= (t3.x < e2.x ? t3.x : e2.x) && n2.x <= (t3.x > e2.x ? t3.x : e2.x) && n2.y >= (t3.y < e2.y ? t3.y : e2.y) && n2.y <= (t3.y > e2.y ? t3.y : e2.y);
          }
          if (4 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1], s2 = arguments[2], a2 = arguments[3], o2 = Math.min(s2.x, a2.x), u3 = Math.max(s2.x, a2.x), l2 = Math.min(i2.x, r3.x), h2 = Math.max(i2.x, r3.x);
            return !(l2 > u3) && (!(h2 < o2) && (o2 = Math.min(s2.y, a2.y), u3 = Math.max(s2.y, a2.y), l2 = Math.min(i2.y, r3.y), h2 = Math.max(i2.y, r3.y), !(l2 > u3) && !(h2 < o2)));
          }
        } }]);
      })(), V = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "isGeometryCollection", value: function() {
          return this.getTypeCode() === t2.TYPECODE_GEOMETRYCOLLECTION;
        } }, { key: "getFactory", value: function() {
          return this._factory;
        } }, { key: "getGeometryN", value: function(t3) {
          return this;
        } }, { key: "getArea", value: function() {
          return 0;
        } }, { key: "isRectangle", value: function() {
          return false;
        } }, { key: "equalsExact", value: function(t3) {
          return this === t3 || this.equalsExact(t3, 0);
        } }, { key: "geometryChanged", value: function() {
          this.apply(t2.geometryChangedFilter);
        } }, { key: "geometryChangedAction", value: function() {
          this._envelope = null;
        } }, { key: "equalsNorm", value: function(t3) {
          return null !== t3 && this.norm().equalsExact(t3.norm());
        } }, { key: "getLength", value: function() {
          return 0;
        } }, { key: "getNumGeometries", value: function() {
          return 1;
        } }, { key: "compareTo", value: function() {
          var t3;
          if (1 === arguments.length) {
            var e2 = arguments[0];
            return t3 = e2, this.getTypeCode() !== t3.getTypeCode() ? this.getTypeCode() - t3.getTypeCode() : this.isEmpty() && t3.isEmpty() ? 0 : this.isEmpty() ? -1 : t3.isEmpty() ? 1 : this.compareToSameClass(e2);
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            return t3 = n2, this.getTypeCode() !== t3.getTypeCode() ? this.getTypeCode() - t3.getTypeCode() : this.isEmpty() && t3.isEmpty() ? 0 : this.isEmpty() ? -1 : t3.isEmpty() ? 1 : this.compareToSameClass(n2, i2);
          }
        } }, { key: "getUserData", value: function() {
          return this._userData;
        } }, { key: "getSRID", value: function() {
          return this._SRID;
        } }, { key: "getEnvelope", value: function() {
          return this.getFactory().toGeometry(this.getEnvelopeInternal());
        } }, { key: "checkNotGeometryCollection", value: function(e2) {
          if (e2.getTypeCode() === t2.TYPECODE_GEOMETRYCOLLECTION) throw new m("This method does not support GeometryCollection arguments");
        } }, { key: "equal", value: function(t3, e2, n2) {
          return 0 === n2 ? t3.equals(e2) : t3.distance(e2) <= n2;
        } }, { key: "norm", value: function() {
          var t3 = this.copy();
          return t3.normalize(), t3;
        } }, { key: "reverse", value: function() {
          var t3 = this.reverseInternal();
          return null != this.envelope && (t3.envelope = this.envelope.copy()), t3.setSRID(this.getSRID()), t3;
        } }, { key: "copy", value: function() {
          var t3 = this.copyInternal();
          return t3.envelope = null == this._envelope ? null : this._envelope.copy(), t3._SRID = this._SRID, t3._userData = this._userData, t3;
        } }, { key: "getPrecisionModel", value: function() {
          return this._factory.getPrecisionModel();
        } }, { key: "getEnvelopeInternal", value: function() {
          return null === this._envelope && (this._envelope = this.computeEnvelopeInternal()), new U(this._envelope);
        } }, { key: "setSRID", value: function(t3) {
          this._SRID = t3;
        } }, { key: "setUserData", value: function(t3) {
          this._userData = t3;
        } }, { key: "compare", value: function(t3, e2) {
          for (var n2 = t3.iterator(), i2 = e2.iterator(); n2.hasNext() && i2.hasNext(); ) {
            var r3 = n2.next(), s2 = i2.next(), a2 = r3.compareTo(s2);
            if (0 !== a2) return a2;
          }
          return n2.hasNext() ? 1 : i2.hasNext() ? -1 : 0;
        } }, { key: "hashCode", value: function() {
          return this.getEnvelopeInternal().hashCode();
        } }, { key: "isEquivalentClass", value: function(t3) {
          return this.getClass() === t3.getClass();
        } }, { key: "isGeometryCollectionOrDerived", value: function() {
          return this.getTypeCode() === t2.TYPECODE_GEOMETRYCOLLECTION || this.getTypeCode() === t2.TYPECODE_MULTIPOINT || this.getTypeCode() === t2.TYPECODE_MULTILINESTRING || this.getTypeCode() === t2.TYPECODE_MULTIPOLYGON;
        } }, { key: "interfaces_", get: function() {
          return [I, x, E];
        } }, { key: "getClass", value: function() {
          return t2;
        } }], [{ key: "hasNonEmptyElements", value: function(t3) {
          for (var e2 = 0; e2 < t3.length; e2++) if (!t3[e2].isEmpty()) return true;
          return false;
        } }, { key: "hasNullElements", value: function(t3) {
          for (var e2 = 0; e2 < t3.length; e2++) if (null === t3[e2]) return true;
          return false;
        } }]);
      })();
      V.constructor_ = function(t2) {
        t2 && (this._envelope = null, this._userData = null, this._factory = t2, this._SRID = t2.getSRID());
      }, V.TYPECODE_POINT = 0, V.TYPECODE_MULTIPOINT = 1, V.TYPECODE_LINESTRING = 2, V.TYPECODE_LINEARRING = 3, V.TYPECODE_MULTILINESTRING = 4, V.TYPECODE_POLYGON = 5, V.TYPECODE_MULTIPOLYGON = 6, V.TYPECODE_GEOMETRYCOLLECTION = 7, V.TYPENAME_POINT = "Point", V.TYPENAME_MULTIPOINT = "MultiPoint", V.TYPENAME_LINESTRING = "LineString", V.TYPENAME_LINEARRING = "LinearRing", V.TYPENAME_MULTILINESTRING = "MultiLineString", V.TYPENAME_POLYGON = "Polygon", V.TYPENAME_MULTIPOLYGON = "MultiPolygon", V.TYPENAME_GEOMETRYCOLLECTION = "GeometryCollection", V.geometryChangedFilter = { get interfaces_() {
        return [k];
      }, filter: function(t2) {
        t2.geometryChangedAction();
      } };
      var H = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "toLocationSymbol", value: function(e2) {
          switch (e2) {
            case t2.EXTERIOR:
              return "e";
            case t2.BOUNDARY:
              return "b";
            case t2.INTERIOR:
              return "i";
            case t2.NONE:
              return "-";
          }
          throw new m("Unknown location value: " + e2);
        } }]);
      })();
      H.INTERIOR = 0, H.BOUNDARY = 1, H.EXTERIOR = 2, H.NONE = -1;
      var Z = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "add", value: function() {
        } }, { key: "addAll", value: function() {
        } }, { key: "isEmpty", value: function() {
        } }, { key: "iterator", value: function() {
        } }, { key: "size", value: function() {
        } }, { key: "toArray", value: function() {
        } }, { key: "remove", value: function() {
        } }]);
      })(), j = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ NoSuchElementException: i2 })[0], r3;
        }
        return l(i2, t2), s(i2);
      })(p), W = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ UnsupportedOperationException: i2 })[0], r3;
        }
        return l(i2, t2), s(i2);
      })(p), K = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        return l(i2, t2), s(i2, [{ key: "contains", value: function() {
        } }]);
      })(Z), J = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2)).map = /* @__PURE__ */ new Map(), t3 instanceof Z && r3.addAll(t3), r3;
        }
        return l(i2, t2), s(i2, [{ key: "contains", value: function(t3) {
          var e2 = t3.hashCode ? t3.hashCode() : t3;
          return !!this.map.has(e2);
        } }, { key: "add", value: function(t3) {
          var e2 = t3.hashCode ? t3.hashCode() : t3;
          return !this.map.has(e2) && !!this.map.set(e2, t3);
        } }, { key: "addAll", value: function(t3) {
          var e2, n2 = a(t3);
          try {
            for (n2.s(); !(e2 = n2.n()).done; ) {
              var i3 = e2.value;
              this.add(i3);
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
          return true;
        } }, { key: "remove", value: function() {
          throw new W();
        } }, { key: "size", value: function() {
          return this.map.size;
        } }, { key: "isEmpty", value: function() {
          return 0 === this.map.size;
        } }, { key: "toArray", value: function() {
          return Array.from(this.map.values());
        } }, { key: "iterator", value: function() {
          return new Q(this.map);
        } }, { key: Symbol.iterator, value: function() {
          return this.map;
        } }]);
      })(K), Q = (function() {
        return s((function t2(e2) {
          n(this, t2), this.iterator = e2.values();
          var i2 = this.iterator.next(), r3 = i2.done, s2 = i2.value;
          this.done = r3, this.value = s2;
        }), [{ key: "next", value: function() {
          if (this.done) throw new j();
          var t2 = this.value, e2 = this.iterator.next(), n2 = e2.done, i2 = e2.value;
          return this.done = n2, this.value = i2, t2;
        } }, { key: "hasNext", value: function() {
          return !this.done;
        } }, { key: "remove", value: function() {
          throw new W();
        } }]);
      })(), $ = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "opposite", value: function(e2) {
          return e2 === t2.LEFT ? t2.RIGHT : e2 === t2.RIGHT ? t2.LEFT : e2;
        } }]);
      })();
      $.ON = 0, $.LEFT = 1, $.RIGHT = 2;
      var tt = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ EmptyStackException: i2 })[0], r3;
        }
        return l(i2, t2), s(i2);
      })(p), et = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2, [t3])).name = Object.keys({ IndexOutOfBoundsException: i2 })[0], r3;
        }
        return l(i2, t2), s(i2);
      })(p), nt = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        return l(i2, t2), s(i2, [{ key: "get", value: function() {
        } }, { key: "set", value: function() {
        } }, { key: "isEmpty", value: function() {
        } }]);
      })(Z), it = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), (t3 = e(this, i2)).array = [], t3;
        }
        return l(i2, t2), s(i2, [{ key: "add", value: function(t3) {
          return this.array.push(t3), true;
        } }, { key: "get", value: function(t3) {
          if (t3 < 0 || t3 >= this.size()) throw new et();
          return this.array[t3];
        } }, { key: "push", value: function(t3) {
          return this.array.push(t3), t3;
        } }, { key: "pop", value: function() {
          if (0 === this.array.length) throw new tt();
          return this.array.pop();
        } }, { key: "peek", value: function() {
          if (0 === this.array.length) throw new tt();
          return this.array[this.array.length - 1];
        } }, { key: "empty", value: function() {
          return 0 === this.array.length;
        } }, { key: "isEmpty", value: function() {
          return this.empty();
        } }, { key: "search", value: function(t3) {
          return this.array.indexOf(t3);
        } }, { key: "size", value: function() {
          return this.array.length;
        } }, { key: "toArray", value: function() {
          return this.array.slice();
        } }]);
      })(nt);
      function rt(t2, e2) {
        return t2.interfaces_ && t2.interfaces_.indexOf(e2) > -1;
      }
      var st = (function() {
        return s((function t2(e2) {
          n(this, t2), this.str = e2;
        }), [{ key: "append", value: function(t2) {
          this.str += t2;
        } }, { key: "setCharAt", value: function(t2, e2) {
          this.str = this.str.substr(0, t2) + e2 + this.str.substr(t2 + 1);
        } }, { key: "toString", value: function() {
          return this.str;
        } }]);
      })(), at = (function() {
        function t2(e2) {
          n(this, t2), this.value = e2;
        }
        return s(t2, [{ key: "intValue", value: function() {
          return this.value;
        } }, { key: "compareTo", value: function(t3) {
          return this.value < t3 ? -1 : this.value > t3 ? 1 : 0;
        } }], [{ key: "compare", value: function(t3, e2) {
          return t3 < e2 ? -1 : t3 > e2 ? 1 : 0;
        } }, { key: "isNan", value: function(t3) {
          return Number.isNaN(t3);
        } }, { key: "valueOf", value: function(e2) {
          return new t2(e2);
        } }]);
      })(), ot = (function() {
        return s((function t2() {
          n(this, t2);
        }), null, [{ key: "isWhitespace", value: function(t2) {
          return t2 <= 32 && t2 >= 0 || 127 === t2;
        } }, { key: "toUpperCase", value: function(t2) {
          return t2.toUpperCase();
        } }]);
      })(), ut = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "le", value: function(t3) {
          return this._hi < t3._hi || this._hi === t3._hi && this._lo <= t3._lo;
        } }, { key: "extractSignificantDigits", value: function(e2, n2) {
          var i2 = this.abs(), r3 = t2.magnitude(i2._hi), s2 = t2.TEN.pow(r3);
          (i2 = i2.divide(s2)).gt(t2.TEN) ? (i2 = i2.divide(t2.TEN), r3 += 1) : i2.lt(t2.ONE) && (i2 = i2.multiply(t2.TEN), r3 -= 1);
          for (var a2 = r3 + 1, o2 = new st(), u3 = t2.MAX_PRINT_DIGITS - 1, l2 = 0; l2 <= u3; l2++) {
            e2 && l2 === a2 && o2.append(".");
            var h2 = Math.trunc(i2._hi);
            if (h2 < 0) break;
            var c2 = false, f2 = 0;
            h2 > 9 ? (c2 = true, f2 = "9") : f2 = "0" + h2, o2.append(f2), i2 = i2.subtract(t2.valueOf(h2)).multiply(t2.TEN), c2 && i2.selfAdd(t2.TEN);
            var g2 = true, v2 = t2.magnitude(i2._hi);
            if (v2 < 0 && Math.abs(v2) >= u3 - l2 && (g2 = false), !g2) break;
          }
          return n2[0] = r3, o2.toString();
        } }, { key: "sqr", value: function() {
          return this.multiply(this);
        } }, { key: "doubleValue", value: function() {
          return this._hi + this._lo;
        } }, { key: "subtract", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return this.add(e2.negate());
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return this.add(-n2);
          }
        } }, { key: "equals", value: function() {
          if (1 === arguments.length && arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return this._hi === e2._hi && this._lo === e2._lo;
          }
        } }, { key: "isZero", value: function() {
          return 0 === this._hi && 0 === this._lo;
        } }, { key: "selfSubtract", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return this.isNaN() ? this : this.selfAdd(-e2._hi, -e2._lo);
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return this.isNaN() ? this : this.selfAdd(-n2, 0);
          }
        } }, { key: "getSpecialNumberString", value: function() {
          return this.isZero() ? "0.0" : this.isNaN() ? "NaN " : null;
        } }, { key: "min", value: function(t3) {
          return this.le(t3) ? this : t3;
        } }, { key: "selfDivide", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e2 = arguments[0];
              return this.selfDivide(e2._hi, e2._lo);
            }
            if ("number" == typeof arguments[0]) {
              var n2 = arguments[0];
              return this.selfDivide(n2, 0);
            }
          } else if (2 === arguments.length) {
            var i2, r3, s2, a2, o2 = arguments[0], u3 = arguments[1], l2 = null, h2 = null, c2 = null, f2 = null;
            return s2 = this._hi / o2, f2 = (l2 = (c2 = t2.SPLIT * s2) - (l2 = c2 - s2)) * (h2 = (f2 = t2.SPLIT * o2) - (h2 = f2 - o2)) - (a2 = s2 * o2) + l2 * (r3 = o2 - h2) + (i2 = s2 - l2) * h2 + i2 * r3, f2 = s2 + (c2 = (this._hi - a2 - f2 + this._lo - s2 * u3) / o2), this._hi = f2, this._lo = s2 - f2 + c2, this;
          }
        } }, { key: "dump", value: function() {
          return "DD<" + this._hi + ", " + this._lo + ">";
        } }, { key: "divide", value: function() {
          if (arguments[0] instanceof t2) {
            var e2, n2, i2, r3, s2 = arguments[0], a2 = null, o2 = null, u3 = null, l2 = null;
            return e2 = (i2 = this._hi / s2._hi) - (a2 = (u3 = t2.SPLIT * i2) - (a2 = u3 - i2)), l2 = a2 * (o2 = (l2 = t2.SPLIT * s2._hi) - (o2 = l2 - s2._hi)) - (r3 = i2 * s2._hi) + a2 * (n2 = s2._hi - o2) + e2 * o2 + e2 * n2, new t2(l2 = i2 + (u3 = (this._hi - r3 - l2 + this._lo - i2 * s2._lo) / s2._hi), i2 - l2 + u3);
          }
          if ("number" == typeof arguments[0]) {
            var h2 = arguments[0];
            return A.isNaN(h2) ? t2.createNaN() : t2.copy(this).selfDivide(h2, 0);
          }
        } }, { key: "ge", value: function(t3) {
          return this._hi > t3._hi || this._hi === t3._hi && this._lo >= t3._lo;
        } }, { key: "pow", value: function(e2) {
          if (0 === e2) return t2.valueOf(1);
          var n2 = new t2(this), i2 = t2.valueOf(1), r3 = Math.abs(e2);
          if (r3 > 1) for (; r3 > 0; ) r3 % 2 == 1 && i2.selfMultiply(n2), (r3 /= 2) > 0 && (n2 = n2.sqr());
          else i2 = n2;
          return e2 < 0 ? i2.reciprocal() : i2;
        } }, { key: "ceil", value: function() {
          if (this.isNaN()) return t2.NaN;
          var e2 = Math.ceil(this._hi), n2 = 0;
          return e2 === this._hi && (n2 = Math.ceil(this._lo)), new t2(e2, n2);
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3;
          return this._hi < e2._hi ? -1 : this._hi > e2._hi ? 1 : this._lo < e2._lo ? -1 : this._lo > e2._lo ? 1 : 0;
        } }, { key: "rint", value: function() {
          return this.isNaN() ? this : this.add(0.5).floor();
        } }, { key: "setValue", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return this.init(e2), this;
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return this.init(n2), this;
          }
        } }, { key: "max", value: function(t3) {
          return this.ge(t3) ? this : t3;
        } }, { key: "sqrt", value: function() {
          if (this.isZero()) return t2.valueOf(0);
          if (this.isNegative()) return t2.NaN;
          var e2 = 1 / Math.sqrt(this._hi), n2 = this._hi * e2, i2 = t2.valueOf(n2), r3 = this.subtract(i2.sqr())._hi * (0.5 * e2);
          return i2.add(r3);
        } }, { key: "selfAdd", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e2 = arguments[0];
              return this.selfAdd(e2._hi, e2._lo);
            }
            if ("number" == typeof arguments[0]) {
              var n2, i2, r3, s2, a2, o2 = arguments[0], u3 = null;
              return u3 = (r3 = this._hi + o2) - (s2 = r3 - this._hi), i2 = (a2 = (u3 = o2 - s2 + (this._hi - u3)) + this._lo) + (r3 - (n2 = r3 + a2)), this._hi = n2 + i2, this._lo = i2 + (n2 - this._hi), this;
            }
          } else if (2 === arguments.length) {
            var l2, h2, c2, f2, g2 = arguments[0], v2 = arguments[1], y2 = null, d2 = null, _2 = null;
            c2 = this._hi + g2, h2 = this._lo + v2, d2 = c2 - (_2 = c2 - this._hi), y2 = h2 - (f2 = h2 - this._lo);
            var p2 = (l2 = c2 + (_2 = (d2 = g2 - _2 + (this._hi - d2)) + h2)) + (_2 = (y2 = v2 - f2 + (this._lo - y2)) + (_2 + (c2 - l2))), m2 = _2 + (l2 - p2);
            return this._hi = p2, this._lo = m2, this;
          }
        } }, { key: "selfMultiply", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof t2) {
              var e2 = arguments[0];
              return this.selfMultiply(e2._hi, e2._lo);
            }
            if ("number" == typeof arguments[0]) {
              var n2 = arguments[0];
              return this.selfMultiply(n2, 0);
            }
          } else if (2 === arguments.length) {
            var i2, r3, s2 = arguments[0], a2 = arguments[1], o2 = null, u3 = null, l2 = null, h2 = null;
            o2 = (l2 = t2.SPLIT * this._hi) - this._hi, h2 = t2.SPLIT * s2, o2 = l2 - o2, i2 = this._hi - o2, u3 = h2 - s2;
            var c2 = (l2 = this._hi * s2) + (h2 = o2 * (u3 = h2 - u3) - l2 + o2 * (r3 = s2 - u3) + i2 * u3 + i2 * r3 + (this._hi * a2 + this._lo * s2)), f2 = h2 + (o2 = l2 - c2);
            return this._hi = c2, this._lo = f2, this;
          }
        } }, { key: "selfSqr", value: function() {
          return this.selfMultiply(this);
        } }, { key: "floor", value: function() {
          if (this.isNaN()) return t2.NaN;
          var e2 = Math.floor(this._hi), n2 = 0;
          return e2 === this._hi && (n2 = Math.floor(this._lo)), new t2(e2, n2);
        } }, { key: "negate", value: function() {
          return this.isNaN() ? this : new t2(-this._hi, -this._lo);
        } }, { key: "clone", value: function() {
          try {
            return null;
          } catch (t3) {
            if (t3 instanceof CloneNotSupportedException) return null;
            throw t3;
          }
        } }, { key: "multiply", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return e2.isNaN() ? t2.createNaN() : t2.copy(this).selfMultiply(e2);
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return A.isNaN(n2) ? t2.createNaN() : t2.copy(this).selfMultiply(n2, 0);
          }
        } }, { key: "isNaN", value: function() {
          return A.isNaN(this._hi);
        } }, { key: "intValue", value: function() {
          return Math.trunc(this._hi);
        } }, { key: "toString", value: function() {
          var e2 = t2.magnitude(this._hi);
          return e2 >= -3 && e2 <= 20 ? this.toStandardNotation() : this.toSciNotation();
        } }, { key: "toStandardNotation", value: function() {
          var e2 = this.getSpecialNumberString();
          if (null !== e2) return e2;
          var n2 = new Array(1).fill(null), i2 = this.extractSignificantDigits(true, n2), r3 = n2[0] + 1, s2 = i2;
          if ("." === i2.charAt(0)) s2 = "0" + i2;
          else if (r3 < 0) s2 = "0." + t2.stringOfChar("0", -r3) + i2;
          else if (-1 === i2.indexOf(".")) {
            var a2 = r3 - i2.length;
            s2 = i2 + t2.stringOfChar("0", a2) + ".0";
          }
          return this.isNegative() ? "-" + s2 : s2;
        } }, { key: "reciprocal", value: function() {
          var e2, n2, i2, r3, s2 = null, a2 = null, o2 = null, u3 = null;
          e2 = (i2 = 1 / this._hi) - (s2 = (o2 = t2.SPLIT * i2) - (s2 = o2 - i2)), a2 = (u3 = t2.SPLIT * this._hi) - this._hi;
          var l2 = i2 + (o2 = (1 - (r3 = i2 * this._hi) - (u3 = s2 * (a2 = u3 - a2) - r3 + s2 * (n2 = this._hi - a2) + e2 * a2 + e2 * n2) - i2 * this._lo) / this._hi);
          return new t2(l2, i2 - l2 + o2);
        } }, { key: "toSciNotation", value: function() {
          if (this.isZero()) return t2.SCI_NOT_ZERO;
          var e2 = this.getSpecialNumberString();
          if (null !== e2) return e2;
          var n2 = new Array(1).fill(null), i2 = this.extractSignificantDigits(false, n2), r3 = t2.SCI_NOT_EXPONENT_CHAR + n2[0];
          if ("0" === i2.charAt(0)) throw new IllegalStateException("Found leading zero: " + i2);
          var s2 = "";
          i2.length > 1 && (s2 = i2.substring(1));
          var a2 = i2.charAt(0) + "." + s2;
          return this.isNegative() ? "-" + a2 + r3 : a2 + r3;
        } }, { key: "abs", value: function() {
          return this.isNaN() ? t2.NaN : this.isNegative() ? this.negate() : new t2(this);
        } }, { key: "isPositive", value: function() {
          return this._hi > 0 || 0 === this._hi && this._lo > 0;
        } }, { key: "lt", value: function(t3) {
          return this._hi < t3._hi || this._hi === t3._hi && this._lo < t3._lo;
        } }, { key: "add", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return t2.copy(this).selfAdd(e2);
          }
          if ("number" == typeof arguments[0]) {
            var n2 = arguments[0];
            return t2.copy(this).selfAdd(n2);
          }
        } }, { key: "init", value: function() {
          if (1 === arguments.length) {
            if ("number" == typeof arguments[0]) {
              var e2 = arguments[0];
              this._hi = e2, this._lo = 0;
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this._hi = n2._hi, this._lo = n2._lo;
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            this._hi = i2, this._lo = r3;
          }
        } }, { key: "gt", value: function(t3) {
          return this._hi > t3._hi || this._hi === t3._hi && this._lo > t3._lo;
        } }, { key: "isNegative", value: function() {
          return this._hi < 0 || 0 === this._hi && this._lo < 0;
        } }, { key: "trunc", value: function() {
          return this.isNaN() ? t2.NaN : this.isPositive() ? this.floor() : this.ceil();
        } }, { key: "signum", value: function() {
          return this._hi > 0 ? 1 : this._hi < 0 ? -1 : this._lo > 0 ? 1 : this._lo < 0 ? -1 : 0;
        } }, { key: "interfaces_", get: function() {
          return [E, x, I];
        } }], [{ key: "constructor_", value: function() {
          if (this._hi = 0, this._lo = 0, 0 === arguments.length) this.init(0);
          else if (1 === arguments.length) {
            if ("number" == typeof arguments[0]) {
              var e2 = arguments[0];
              this.init(e2);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this.init(n2);
            } else if ("string" == typeof arguments[0]) {
              var i2 = arguments[0];
              t2.constructor_.call(this, t2.parse(i2));
            }
          } else if (2 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1];
            this.init(r3, s2);
          }
        } }, { key: "determinant", value: function() {
          if ("number" == typeof arguments[3] && "number" == typeof arguments[2] && "number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var e2 = arguments[0], n2 = arguments[1], i2 = arguments[2], r3 = arguments[3];
            return t2.determinant(t2.valueOf(e2), t2.valueOf(n2), t2.valueOf(i2), t2.valueOf(r3));
          }
          if (arguments[3] instanceof t2 && arguments[2] instanceof t2 && arguments[0] instanceof t2 && arguments[1] instanceof t2) {
            var s2 = arguments[1], a2 = arguments[2], o2 = arguments[3];
            return arguments[0].multiply(o2).selfSubtract(s2.multiply(a2));
          }
        } }, { key: "sqr", value: function(e2) {
          return t2.valueOf(e2).selfMultiply(e2);
        } }, { key: "valueOf", value: function() {
          if ("string" == typeof arguments[0]) {
            var e2 = arguments[0];
            return t2.parse(e2);
          }
          if ("number" == typeof arguments[0]) return new t2(arguments[0]);
        } }, { key: "sqrt", value: function(e2) {
          return t2.valueOf(e2).sqrt();
        } }, { key: "parse", value: function(e2) {
          for (var n2 = 0, i2 = e2.length; ot.isWhitespace(e2.charAt(n2)); ) n2++;
          var r3 = false;
          if (n2 < i2) {
            var s2 = e2.charAt(n2);
            "-" !== s2 && "+" !== s2 || (n2++, "-" === s2 && (r3 = true));
          }
          for (var a2 = new t2(), o2 = 0, u3 = 0, l2 = 0, h2 = false; !(n2 >= i2); ) {
            var c2 = e2.charAt(n2);
            if (n2++, ot.isDigit(c2)) {
              var f2 = c2 - "0";
              a2.selfMultiply(t2.TEN), a2.selfAdd(f2), o2++;
            } else {
              if ("." !== c2) {
                if ("e" === c2 || "E" === c2) {
                  var g2 = e2.substring(n2);
                  try {
                    l2 = at.parseInt(g2);
                  } catch (t3) {
                    throw t3 instanceof NumberFormatException ? new NumberFormatException("Invalid exponent " + g2 + " in string " + e2) : t3;
                  }
                  break;
                }
                throw new NumberFormatException("Unexpected character '" + c2 + "' at position " + n2 + " in string " + e2);
              }
              u3 = o2, h2 = true;
            }
          }
          var v2 = a2;
          h2 || (u3 = o2);
          var y2 = o2 - u3 - l2;
          if (0 === y2) v2 = a2;
          else if (y2 > 0) {
            var d2 = t2.TEN.pow(y2);
            v2 = a2.divide(d2);
          } else if (y2 < 0) {
            var _2 = t2.TEN.pow(-y2);
            v2 = a2.multiply(_2);
          }
          return r3 ? v2.negate() : v2;
        } }, { key: "createNaN", value: function() {
          return new t2(A.NaN, A.NaN);
        } }, { key: "copy", value: function(e2) {
          return new t2(e2);
        } }, { key: "magnitude", value: function(t3) {
          var e2 = Math.abs(t3), n2 = Math.log(e2) / Math.log(10), i2 = Math.trunc(Math.floor(n2));
          return 10 * Math.pow(10, i2) <= e2 && (i2 += 1), i2;
        } }, { key: "stringOfChar", value: function(t3, e2) {
          for (var n2 = new st(), i2 = 0; i2 < e2; i2++) n2.append(t3);
          return n2.toString();
        } }]);
      })();
      ut.PI = new ut(3.141592653589793, 12246467991473532e-32), ut.TWO_PI = new ut(6.283185307179586, 24492935982947064e-32), ut.PI_2 = new ut(1.5707963267948966, 6123233995736766e-32), ut.E = new ut(2.718281828459045, 14456468917292502e-32), ut.NaN = new ut(A.NaN, A.NaN), ut.EPS = 123259516440783e-46, ut.SPLIT = 134217729, ut.MAX_PRINT_DIGITS = 32, ut.TEN = ut.valueOf(10), ut.ONE = ut.valueOf(1), ut.SCI_NOT_EXPONENT_CHAR = "E", ut.SCI_NOT_ZERO = "0.0E0";
      var lt = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "orientationIndex", value: function(e2, n2, i2) {
          var r3 = t2.orientationIndexFilter(e2, n2, i2);
          if (r3 <= 1) return r3;
          var s2 = ut.valueOf(n2.x).selfAdd(-e2.x), a2 = ut.valueOf(n2.y).selfAdd(-e2.y), o2 = ut.valueOf(i2.x).selfAdd(-n2.x), u3 = ut.valueOf(i2.y).selfAdd(-n2.y);
          return s2.selfMultiply(u3).selfSubtract(a2.selfMultiply(o2)).signum();
        } }, { key: "signOfDet2x2", value: function() {
          if (arguments[3] instanceof ut && arguments[2] instanceof ut && arguments[0] instanceof ut && arguments[1] instanceof ut) {
            var t3 = arguments[1], e2 = arguments[2], n2 = arguments[3];
            return arguments[0].multiply(n2).selfSubtract(t3.multiply(e2)).signum();
          }
          if ("number" == typeof arguments[3] && "number" == typeof arguments[2] && "number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var i2 = arguments[0], r3 = arguments[1], s2 = arguments[2], a2 = arguments[3], o2 = ut.valueOf(i2), u3 = ut.valueOf(r3), l2 = ut.valueOf(s2), h2 = ut.valueOf(a2);
            return o2.multiply(h2).selfSubtract(u3.multiply(l2)).signum();
          }
        } }, { key: "intersection", value: function(t3, e2, n2, i2) {
          var r3 = new ut(t3.y).selfSubtract(e2.y), s2 = new ut(e2.x).selfSubtract(t3.x), a2 = new ut(t3.x).selfMultiply(e2.y).selfSubtract(new ut(e2.x).selfMultiply(t3.y)), o2 = new ut(n2.y).selfSubtract(i2.y), u3 = new ut(i2.x).selfSubtract(n2.x), l2 = new ut(n2.x).selfMultiply(i2.y).selfSubtract(new ut(i2.x).selfMultiply(n2.y)), h2 = s2.multiply(l2).selfSubtract(u3.multiply(a2)), c2 = o2.multiply(a2).selfSubtract(r3.multiply(l2)), f2 = r3.multiply(u3).selfSubtract(o2.multiply(s2)), g2 = h2.selfDivide(f2).doubleValue(), v2 = c2.selfDivide(f2).doubleValue();
          return A.isNaN(g2) || A.isInfinite(g2) || A.isNaN(v2) || A.isInfinite(v2) ? null : new X(g2, v2);
        } }, { key: "orientationIndexFilter", value: function(e2, n2, i2) {
          var r3 = null, s2 = (e2.x - i2.x) * (n2.y - i2.y), a2 = (e2.y - i2.y) * (n2.x - i2.x), o2 = s2 - a2;
          if (s2 > 0) {
            if (a2 <= 0) return t2.signum(o2);
            r3 = s2 + a2;
          } else {
            if (!(s2 < 0)) return t2.signum(o2);
            if (a2 >= 0) return t2.signum(o2);
            r3 = -s2 - a2;
          }
          var u3 = t2.DP_SAFE_EPSILON * r3;
          return o2 >= u3 || -o2 >= u3 ? t2.signum(o2) : 2;
        } }, { key: "signum", value: function(t3) {
          return t3 > 0 ? 1 : t3 < 0 ? -1 : 0;
        } }]);
      })();
      lt.DP_SAFE_EPSILON = 1e-15;
      var ht = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "getM", value: function(t2) {
          if (this.hasM()) {
            var e2 = this.getDimension() - this.getMeasures();
            return this.getOrdinate(t2, e2);
          }
          return A.NaN;
        } }, { key: "setOrdinate", value: function(t2, e2, n2) {
        } }, { key: "getZ", value: function(t2) {
          return this.hasZ() ? this.getOrdinate(t2, 2) : A.NaN;
        } }, { key: "size", value: function() {
        } }, { key: "getOrdinate", value: function(t2, e2) {
        } }, { key: "getCoordinate", value: function() {
        } }, { key: "getCoordinateCopy", value: function(t2) {
        } }, { key: "createCoordinate", value: function() {
        } }, { key: "getDimension", value: function() {
        } }, { key: "hasM", value: function() {
          return this.getMeasures() > 0;
        } }, { key: "getX", value: function(t2) {
        } }, { key: "hasZ", value: function() {
          return this.getDimension() - this.getMeasures() > 2;
        } }, { key: "getMeasures", value: function() {
          return 0;
        } }, { key: "expandEnvelope", value: function(t2) {
        } }, { key: "copy", value: function() {
        } }, { key: "getY", value: function(t2) {
        } }, { key: "toCoordinateArray", value: function() {
        } }, { key: "interfaces_", get: function() {
          return [I];
        } }]);
      })();
      ht.X = 0, ht.Y = 1, ht.Z = 2, ht.M = 3;
      var ct = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "index", value: function(t3, e2, n2) {
          return lt.orientationIndex(t3, e2, n2);
        } }, { key: "isCCW", value: function() {
          if (arguments[0] instanceof Array) {
            var e2 = arguments[0], n2 = e2.length - 1;
            if (n2 < 3) throw new m("Ring has fewer than 4 points, so orientation cannot be determined");
            for (var i2 = e2[0], r3 = 0, s2 = 1; s2 <= n2; s2++) {
              var a2 = e2[s2];
              a2.y > i2.y && (i2 = a2, r3 = s2);
            }
            var o2 = r3;
            do {
              (o2 -= 1) < 0 && (o2 = n2);
            } while (e2[o2].equals2D(i2) && o2 !== r3);
            var u3 = r3;
            do {
              u3 = (u3 + 1) % n2;
            } while (e2[u3].equals2D(i2) && u3 !== r3);
            var l2 = e2[o2], h2 = e2[u3];
            if (l2.equals2D(i2) || h2.equals2D(i2) || l2.equals2D(h2)) return false;
            var c2 = t2.index(l2, i2, h2);
            return 0 === c2 ? l2.x > h2.x : c2 > 0;
          }
          if (rt(arguments[0], ht)) {
            var f2 = arguments[0], g2 = f2.size() - 1;
            if (g2 < 3) throw new m("Ring has fewer than 4 points, so orientation cannot be determined");
            for (var v2 = f2.getCoordinate(0), y2 = 0, d2 = 1; d2 <= g2; d2++) {
              var _2 = f2.getCoordinate(d2);
              _2.y > v2.y && (v2 = _2, y2 = d2);
            }
            var p2 = null, k2 = y2;
            do {
              (k2 -= 1) < 0 && (k2 = g2), p2 = f2.getCoordinate(k2);
            } while (p2.equals2D(v2) && k2 !== y2);
            var x2 = null, I2 = y2;
            do {
              I2 = (I2 + 1) % g2, x2 = f2.getCoordinate(I2);
            } while (x2.equals2D(v2) && I2 !== y2);
            if (p2.equals2D(v2) || x2.equals2D(v2) || p2.equals2D(x2)) return false;
            var E2 = t2.index(p2, v2, x2);
            return 0 === E2 ? p2.x > x2.x : E2 > 0;
          }
        } }]);
      })();
      ct.CLOCKWISE = -1, ct.RIGHT = ct.CLOCKWISE, ct.COUNTERCLOCKWISE = 1, ct.LEFT = ct.COUNTERCLOCKWISE, ct.COLLINEAR = 0, ct.STRAIGHT = ct.COLLINEAR;
      var ft = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getCoordinate", value: function() {
          return this._minCoord;
        } }, { key: "getRightmostSide", value: function(t2, e2) {
          var n2 = this.getRightmostSideOfSegment(t2, e2);
          return n2 < 0 && (n2 = this.getRightmostSideOfSegment(t2, e2 - 1)), n2 < 0 && (this._minCoord = null, this.checkForRightmostCoordinate(t2)), n2;
        } }, { key: "findRightmostEdgeAtVertex", value: function() {
          var t2 = this._minDe.getEdge().getCoordinates();
          G.isTrue(this._minIndex > 0 && this._minIndex < t2.length, "rightmost point expected to be interior vertex of edge");
          var e2 = t2[this._minIndex - 1], n2 = t2[this._minIndex + 1], i2 = ct.index(this._minCoord, n2, e2), r3 = false;
          (e2.y < this._minCoord.y && n2.y < this._minCoord.y && i2 === ct.COUNTERCLOCKWISE || e2.y > this._minCoord.y && n2.y > this._minCoord.y && i2 === ct.CLOCKWISE) && (r3 = true), r3 && (this._minIndex = this._minIndex - 1);
        } }, { key: "getRightmostSideOfSegment", value: function(t2, e2) {
          var n2 = t2.getEdge().getCoordinates();
          if (e2 < 0 || e2 + 1 >= n2.length) return -1;
          if (n2[e2].y === n2[e2 + 1].y) return -1;
          var i2 = $.LEFT;
          return n2[e2].y < n2[e2 + 1].y && (i2 = $.RIGHT), i2;
        } }, { key: "getEdge", value: function() {
          return this._orientedDe;
        } }, { key: "checkForRightmostCoordinate", value: function(t2) {
          for (var e2 = t2.getEdge().getCoordinates(), n2 = 0; n2 < e2.length - 1; n2++) (null === this._minCoord || e2[n2].x > this._minCoord.x) && (this._minDe = t2, this._minIndex = n2, this._minCoord = e2[n2]);
        } }, { key: "findRightmostEdgeAtNode", value: function() {
          var t2 = this._minDe.getNode().getEdges();
          this._minDe = t2.getRightmostEdge(), this._minDe.isForward() || (this._minDe = this._minDe.getSym(), this._minIndex = this._minDe.getEdge().getCoordinates().length - 1);
        } }, { key: "findEdge", value: function(t2) {
          for (var e2 = t2.iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            n2.isForward() && this.checkForRightmostCoordinate(n2);
          }
          G.isTrue(0 !== this._minIndex || this._minCoord.equals(this._minDe.getCoordinate()), "inconsistency in rightmost processing"), 0 === this._minIndex ? this.findRightmostEdgeAtNode() : this.findRightmostEdgeAtVertex(), this._orientedDe = this._minDe, this.getRightmostSide(this._minDe, this._minIndex) === $.LEFT && (this._orientedDe = this._minDe.getSym());
        } }], [{ key: "constructor_", value: function() {
          this._minIndex = -1, this._minCoord = null, this._minDe = null, this._orientedDe = null;
        } }]);
      })(), gt = (function(t2) {
        function i2(t3, r3) {
          var s2;
          return n(this, i2), (s2 = e(this, i2, [r3 ? t3 + " [ " + r3 + " ]" : t3])).pt = r3 ? new X(r3) : void 0, s2.name = Object.keys({ TopologyException: i2 })[0], s2;
        }
        return l(i2, t2), s(i2, [{ key: "getCoordinate", value: function() {
          return this.pt;
        } }]);
      })(D2), vt = (function() {
        return s((function t2() {
          n(this, t2), this.array = [];
        }), [{ key: "addLast", value: function(t2) {
          this.array.push(t2);
        } }, { key: "removeFirst", value: function() {
          return this.array.shift();
        } }, { key: "isEmpty", value: function() {
          return 0 === this.array.length;
        } }]);
      })(), yt = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2)).array = [], t3 instanceof Z && r3.addAll(t3), r3;
        }
        return l(i2, t2), s(i2, [{ key: "interfaces_", get: function() {
          return [nt, Z];
        } }, { key: "ensureCapacity", value: function() {
        } }, { key: "add", value: function(t3) {
          return 1 === arguments.length ? this.array.push(t3) : this.array.splice(arguments[0], 0, arguments[1]), true;
        } }, { key: "clear", value: function() {
          this.array = [];
        } }, { key: "addAll", value: function(t3) {
          var e2, n2 = a(t3);
          try {
            for (n2.s(); !(e2 = n2.n()).done; ) {
              var i3 = e2.value;
              this.array.push(i3);
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
        } }, { key: "set", value: function(t3, e2) {
          var n2 = this.array[t3];
          return this.array[t3] = e2, n2;
        } }, { key: "iterator", value: function() {
          return new dt(this);
        } }, { key: "get", value: function(t3) {
          if (t3 < 0 || t3 >= this.size()) throw new et();
          return this.array[t3];
        } }, { key: "isEmpty", value: function() {
          return 0 === this.array.length;
        } }, { key: "sort", value: function(t3) {
          t3 ? this.array.sort((function(e2, n2) {
            return t3.compare(e2, n2);
          })) : this.array.sort();
        } }, { key: "size", value: function() {
          return this.array.length;
        } }, { key: "toArray", value: function() {
          return this.array.slice();
        } }, { key: "remove", value: function(t3) {
          for (var e2 = 0, n2 = this.array.length; e2 < n2; e2++) if (this.array[e2] === t3) return !!this.array.splice(e2, 1);
          return false;
        } }, { key: Symbol.iterator, value: function() {
          return this.array.values();
        } }]);
      })(nt), dt = (function() {
        return s((function t2(e2) {
          n(this, t2), this.arrayList = e2, this.position = 0;
        }), [{ key: "next", value: function() {
          if (this.position === this.arrayList.size()) throw new j();
          return this.arrayList.get(this.position++);
        } }, { key: "hasNext", value: function() {
          return this.position < this.arrayList.size();
        } }, { key: "set", value: function(t2) {
          return this.arrayList.set(this.position - 1, t2);
        } }, { key: "remove", value: function() {
          this.arrayList.remove(this.arrayList.get(this.position));
        } }]);
      })(), _t = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "clearVisitedEdges", value: function() {
          for (var t2 = this._dirEdgeList.iterator(); t2.hasNext(); ) {
            t2.next().setVisited(false);
          }
        } }, { key: "getRightmostCoordinate", value: function() {
          return this._rightMostCoord;
        } }, { key: "computeNodeDepth", value: function(t2) {
          for (var e2 = null, n2 = t2.getEdges().iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            if (i2.isVisited() || i2.getSym().isVisited()) {
              e2 = i2;
              break;
            }
          }
          if (null === e2) throw new gt("unable to find edge to compute depths at " + t2.getCoordinate());
          t2.getEdges().computeDepths(e2);
          for (var r3 = t2.getEdges().iterator(); r3.hasNext(); ) {
            var s2 = r3.next();
            s2.setVisited(true), this.copySymDepths(s2);
          }
        } }, { key: "computeDepth", value: function(t2) {
          this.clearVisitedEdges();
          var e2 = this._finder.getEdge();
          e2.getNode(), e2.getLabel(), e2.setEdgeDepths($.RIGHT, t2), this.copySymDepths(e2), this.computeDepths(e2);
        } }, { key: "create", value: function(t2) {
          this.addReachable(t2), this._finder.findEdge(this._dirEdgeList), this._rightMostCoord = this._finder.getCoordinate();
        } }, { key: "findResultEdges", value: function() {
          for (var t2 = this._dirEdgeList.iterator(); t2.hasNext(); ) {
            var e2 = t2.next();
            e2.getDepth($.RIGHT) >= 1 && e2.getDepth($.LEFT) <= 0 && !e2.isInteriorAreaEdge() && e2.setInResult(true);
          }
        } }, { key: "computeDepths", value: function(t2) {
          var e2 = new J(), n2 = new vt(), i2 = t2.getNode();
          for (n2.addLast(i2), e2.add(i2), t2.setVisited(true); !n2.isEmpty(); ) {
            var r3 = n2.removeFirst();
            e2.add(r3), this.computeNodeDepth(r3);
            for (var s2 = r3.getEdges().iterator(); s2.hasNext(); ) {
              var a2 = s2.next().getSym();
              if (!a2.isVisited()) {
                var o2 = a2.getNode();
                e2.contains(o2) || (n2.addLast(o2), e2.add(o2));
              }
            }
          }
        } }, { key: "compareTo", value: function(t2) {
          var e2 = t2;
          return this._rightMostCoord.x < e2._rightMostCoord.x ? -1 : this._rightMostCoord.x > e2._rightMostCoord.x ? 1 : 0;
        } }, { key: "getEnvelope", value: function() {
          if (null === this._env) {
            for (var t2 = new U(), e2 = this._dirEdgeList.iterator(); e2.hasNext(); ) for (var n2 = e2.next().getEdge().getCoordinates(), i2 = 0; i2 < n2.length - 1; i2++) t2.expandToInclude(n2[i2]);
            this._env = t2;
          }
          return this._env;
        } }, { key: "addReachable", value: function(t2) {
          var e2 = new it();
          for (e2.add(t2); !e2.empty(); ) {
            var n2 = e2.pop();
            this.add(n2, e2);
          }
        } }, { key: "copySymDepths", value: function(t2) {
          var e2 = t2.getSym();
          e2.setDepth($.LEFT, t2.getDepth($.RIGHT)), e2.setDepth($.RIGHT, t2.getDepth($.LEFT));
        } }, { key: "add", value: function(t2, e2) {
          t2.setVisited(true), this._nodes.add(t2);
          for (var n2 = t2.getEdges().iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            this._dirEdgeList.add(i2);
            var r3 = i2.getSym().getNode();
            r3.isVisited() || e2.push(r3);
          }
        } }, { key: "getNodes", value: function() {
          return this._nodes;
        } }, { key: "getDirectedEdges", value: function() {
          return this._dirEdgeList;
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          this._finder = null, this._dirEdgeList = new yt(), this._nodes = new yt(), this._rightMostCoord = null, this._env = null, this._finder = new ft();
        } }]);
      })(), pt = (function() {
        return s((function t2() {
          n(this, t2);
        }), null, [{ key: "intersection", value: function(t2, e2, n2, i2) {
          var r3 = t2.x < e2.x ? t2.x : e2.x, s2 = t2.y < e2.y ? t2.y : e2.y, a2 = t2.x > e2.x ? t2.x : e2.x, o2 = t2.y > e2.y ? t2.y : e2.y, u3 = n2.x < i2.x ? n2.x : i2.x, l2 = n2.y < i2.y ? n2.y : i2.y, h2 = n2.x > i2.x ? n2.x : i2.x, c2 = n2.y > i2.y ? n2.y : i2.y, f2 = ((r3 > u3 ? r3 : u3) + (a2 < h2 ? a2 : h2)) / 2, g2 = ((s2 > l2 ? s2 : l2) + (o2 < c2 ? o2 : c2)) / 2, v2 = t2.x - f2, y2 = t2.y - g2, d2 = e2.x - f2, _2 = e2.y - g2, p2 = n2.x - f2, m2 = n2.y - g2, k2 = i2.x - f2, x2 = i2.y - g2, I2 = y2 - _2, E2 = d2 - v2, N2 = v2 * _2 - d2 * y2, T2 = m2 - x2, S2 = k2 - p2, L2 = p2 * x2 - k2 * m2, C3 = I2 * S2 - T2 * E2, R2 = (E2 * L2 - S2 * N2) / C3, w2 = (T2 * N2 - I2 * L2) / C3;
          return A.isNaN(R2) || A.isInfinite(R2) || A.isNaN(w2) || A.isInfinite(w2) ? null : new X(R2 + f2, w2 + g2);
        } }]);
      })(), mt = (function() {
        return s((function t2() {
          n(this, t2);
        }), null, [{ key: "arraycopy", value: function(t2, e2, n2, i2, r3) {
          for (var s2 = 0, a2 = e2; a2 < e2 + r3; a2++) n2[i2 + s2] = t2[a2], s2++;
        } }, { key: "getProperty", value: function(t2) {
          return { "line.separator": "\n" }[t2];
        } }]);
      })(), kt = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "log10", value: function(e2) {
          var n2 = Math.log(e2);
          return A.isInfinite(n2) || A.isNaN(n2) ? n2 : n2 / t2.LOG_10;
        } }, { key: "min", value: function(t3, e2, n2, i2) {
          var r3 = t3;
          return e2 < r3 && (r3 = e2), n2 < r3 && (r3 = n2), i2 < r3 && (r3 = i2), r3;
        } }, { key: "clamp", value: function() {
          if ("number" == typeof arguments[2] && "number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
            return t3 < e2 ? e2 : t3 > n2 ? n2 : t3;
          }
          if (Number.isInteger(arguments[2]) && Number.isInteger(arguments[0]) && Number.isInteger(arguments[1])) {
            var i2 = arguments[0], r3 = arguments[1], s2 = arguments[2];
            return i2 < r3 ? r3 : i2 > s2 ? s2 : i2;
          }
        } }, { key: "wrap", value: function(t3, e2) {
          return t3 < 0 ? e2 - -t3 % e2 : t3 % e2;
        } }, { key: "max", value: function() {
          if (3 === arguments.length) {
            var t3 = arguments[1], e2 = arguments[2], n2 = arguments[0];
            return t3 > n2 && (n2 = t3), e2 > n2 && (n2 = e2), n2;
          }
          if (4 === arguments.length) {
            var i2 = arguments[1], r3 = arguments[2], s2 = arguments[3], a2 = arguments[0];
            return i2 > a2 && (a2 = i2), r3 > a2 && (a2 = r3), s2 > a2 && (a2 = s2), a2;
          }
        } }, { key: "average", value: function(t3, e2) {
          return (t3 + e2) / 2;
        } }]);
      })();
      kt.LOG_10 = Math.log(10);
      var xt = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "segmentToSegment", value: function(e2, n2, i2, r3) {
          if (e2.equals(n2)) return t2.pointToSegment(e2, i2, r3);
          if (i2.equals(r3)) return t2.pointToSegment(r3, e2, n2);
          var s2 = false;
          if (U.intersects(e2, n2, i2, r3)) {
            var a2 = (n2.x - e2.x) * (r3.y - i2.y) - (n2.y - e2.y) * (r3.x - i2.x);
            if (0 === a2) s2 = true;
            else {
              var o2 = (e2.y - i2.y) * (r3.x - i2.x) - (e2.x - i2.x) * (r3.y - i2.y), u3 = ((e2.y - i2.y) * (n2.x - e2.x) - (e2.x - i2.x) * (n2.y - e2.y)) / a2, l2 = o2 / a2;
              (l2 < 0 || l2 > 1 || u3 < 0 || u3 > 1) && (s2 = true);
            }
          } else s2 = true;
          return s2 ? kt.min(t2.pointToSegment(e2, i2, r3), t2.pointToSegment(n2, i2, r3), t2.pointToSegment(i2, e2, n2), t2.pointToSegment(r3, e2, n2)) : 0;
        } }, { key: "pointToSegment", value: function(t3, e2, n2) {
          if (e2.x === n2.x && e2.y === n2.y) return t3.distance(e2);
          var i2 = (n2.x - e2.x) * (n2.x - e2.x) + (n2.y - e2.y) * (n2.y - e2.y), r3 = ((t3.x - e2.x) * (n2.x - e2.x) + (t3.y - e2.y) * (n2.y - e2.y)) / i2;
          if (r3 <= 0) return t3.distance(e2);
          if (r3 >= 1) return t3.distance(n2);
          var s2 = ((e2.y - t3.y) * (n2.x - e2.x) - (e2.x - t3.x) * (n2.y - e2.y)) / i2;
          return Math.abs(s2) * Math.sqrt(i2);
        } }, { key: "pointToLinePerpendicular", value: function(t3, e2, n2) {
          var i2 = (n2.x - e2.x) * (n2.x - e2.x) + (n2.y - e2.y) * (n2.y - e2.y), r3 = ((e2.y - t3.y) * (n2.x - e2.x) - (e2.x - t3.x) * (n2.y - e2.y)) / i2;
          return Math.abs(r3) * Math.sqrt(i2);
        } }, { key: "pointToSegmentString", value: function(e2, n2) {
          if (0 === n2.length) throw new m("Line array must contain at least one vertex");
          for (var i2 = e2.distance(n2[0]), r3 = 0; r3 < n2.length - 1; r3++) {
            var s2 = t2.pointToSegment(e2, n2[r3], n2[r3 + 1]);
            s2 < i2 && (i2 = s2);
          }
          return i2;
        } }]);
      })(), It = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "create", value: function() {
          if (1 === arguments.length) arguments[0] instanceof Array || rt(arguments[0], ht);
          else if (2 === arguments.length) ;
          else if (3 === arguments.length) {
            var t2 = arguments[0], e2 = arguments[1];
            return this.create(t2, e2);
          }
        } }]);
      })(), Et = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "filter", value: function(t2) {
        } }]);
      })(), Nt = (function() {
        return s((function t2() {
          n(this, t2);
        }), null, [{ key: "ofLine", value: function(t2) {
          var e2 = t2.size();
          if (e2 <= 1) return 0;
          var n2 = 0, i2 = new X();
          t2.getCoordinate(0, i2);
          for (var r3 = i2.x, s2 = i2.y, a2 = 1; a2 < e2; a2++) {
            t2.getCoordinate(a2, i2);
            var o2 = i2.x, u3 = i2.y, l2 = o2 - r3, h2 = u3 - s2;
            n2 += Math.sqrt(l2 * l2 + h2 * h2), r3 = o2, s2 = u3;
          }
          return n2;
        } }]);
      })(), Tt = s((function t2() {
        n(this, t2);
      })), St = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "copyCoord", value: function(t3, e2, n2, i2) {
          for (var r3 = Math.min(t3.getDimension(), n2.getDimension()), s2 = 0; s2 < r3; s2++) n2.setOrdinate(i2, s2, t3.getOrdinate(e2, s2));
        } }, { key: "isRing", value: function(t3) {
          var e2 = t3.size();
          return 0 === e2 || !(e2 <= 3) && (t3.getOrdinate(0, ht.X) === t3.getOrdinate(e2 - 1, ht.X) && t3.getOrdinate(0, ht.Y) === t3.getOrdinate(e2 - 1, ht.Y));
        } }, { key: "scroll", value: function() {
          if (2 === arguments.length) {
            if (rt(arguments[0], ht) && Number.isInteger(arguments[1])) {
              var e2 = arguments[0], n2 = arguments[1];
              t2.scroll(e2, n2, t2.isRing(e2));
            } else if (rt(arguments[0], ht) && arguments[1] instanceof X) {
              var i2 = arguments[0], r3 = arguments[1], s2 = t2.indexOf(r3, i2);
              if (s2 <= 0) return null;
              t2.scroll(i2, s2);
            }
          } else if (3 === arguments.length) {
            var a2 = arguments[0], o2 = arguments[1], u3 = arguments[2];
            if (o2 <= 0) return null;
            for (var l2 = a2.copy(), h2 = u3 ? a2.size() - 1 : a2.size(), c2 = 0; c2 < h2; c2++) for (var f2 = 0; f2 < a2.getDimension(); f2++) a2.setOrdinate(c2, f2, l2.getOrdinate((o2 + c2) % h2, f2));
            if (u3) for (var g2 = 0; g2 < a2.getDimension(); g2++) a2.setOrdinate(h2, g2, a2.getOrdinate(0, g2));
          }
        } }, { key: "isEqual", value: function(t3, e2) {
          var n2 = t3.size();
          if (n2 !== e2.size()) return false;
          for (var i2 = Math.min(t3.getDimension(), e2.getDimension()), r3 = 0; r3 < n2; r3++) for (var s2 = 0; s2 < i2; s2++) {
            var a2 = t3.getOrdinate(r3, s2), o2 = e2.getOrdinate(r3, s2);
            if (t3.getOrdinate(r3, s2) !== e2.getOrdinate(r3, s2) && (!A.isNaN(a2) || !A.isNaN(o2))) return false;
          }
          return true;
        } }, { key: "minCoordinateIndex", value: function() {
          if (1 === arguments.length) {
            var e2 = arguments[0];
            return t2.minCoordinateIndex(e2, 0, e2.size() - 1);
          }
          if (3 === arguments.length) {
            for (var n2 = arguments[0], i2 = arguments[2], r3 = -1, s2 = null, a2 = arguments[1]; a2 <= i2; a2++) {
              var o2 = n2.getCoordinate(a2);
              (null === s2 || s2.compareTo(o2) > 0) && (s2 = o2, r3 = a2);
            }
            return r3;
          }
        } }, { key: "extend", value: function(e2, n2, i2) {
          var r3 = e2.create(i2, n2.getDimension()), s2 = n2.size();
          if (t2.copy(n2, 0, r3, 0, s2), s2 > 0) for (var a2 = s2; a2 < i2; a2++) t2.copy(n2, s2 - 1, r3, a2, 1);
          return r3;
        } }, { key: "reverse", value: function(e2) {
          for (var n2 = e2.size() - 1, i2 = Math.trunc(n2 / 2), r3 = 0; r3 <= i2; r3++) t2.swap(e2, r3, n2 - r3);
        } }, { key: "swap", value: function(t3, e2, n2) {
          if (e2 === n2) return null;
          for (var i2 = 0; i2 < t3.getDimension(); i2++) {
            var r3 = t3.getOrdinate(e2, i2);
            t3.setOrdinate(e2, i2, t3.getOrdinate(n2, i2)), t3.setOrdinate(n2, i2, r3);
          }
        } }, { key: "copy", value: function(e2, n2, i2, r3, s2) {
          for (var a2 = 0; a2 < s2; a2++) t2.copyCoord(e2, n2 + a2, i2, r3 + a2);
        } }, { key: "ensureValidRing", value: function(e2, n2) {
          var i2 = n2.size();
          return 0 === i2 ? n2 : i2 <= 3 ? t2.createClosedRing(e2, n2, 4) : n2.getOrdinate(0, ht.X) === n2.getOrdinate(i2 - 1, ht.X) && n2.getOrdinate(0, ht.Y) === n2.getOrdinate(i2 - 1, ht.Y) ? n2 : t2.createClosedRing(e2, n2, i2 + 1);
        } }, { key: "indexOf", value: function(t3, e2) {
          for (var n2 = 0; n2 < e2.size(); n2++) if (t3.x === e2.getOrdinate(n2, ht.X) && t3.y === e2.getOrdinate(n2, ht.Y)) return n2;
          return -1;
        } }, { key: "createClosedRing", value: function(e2, n2, i2) {
          var r3 = e2.create(i2, n2.getDimension()), s2 = n2.size();
          t2.copy(n2, 0, r3, 0, s2);
          for (var a2 = s2; a2 < i2; a2++) t2.copy(n2, 0, r3, a2, 1);
          return r3;
        } }, { key: "minCoordinate", value: function(t3) {
          for (var e2 = null, n2 = 0; n2 < t3.size(); n2++) {
            var i2 = t3.getCoordinate(n2);
            (null === e2 || e2.compareTo(i2) > 0) && (e2 = i2);
          }
          return e2;
        } }]);
      })(), Lt = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "toDimensionSymbol", value: function(e2) {
          switch (e2) {
            case t2.FALSE:
              return t2.SYM_FALSE;
            case t2.TRUE:
              return t2.SYM_TRUE;
            case t2.DONTCARE:
              return t2.SYM_DONTCARE;
            case t2.P:
              return t2.SYM_P;
            case t2.L:
              return t2.SYM_L;
            case t2.A:
              return t2.SYM_A;
          }
          throw new m("Unknown dimension value: " + e2);
        } }, { key: "toDimensionValue", value: function(e2) {
          switch (ot.toUpperCase(e2)) {
            case t2.SYM_FALSE:
              return t2.FALSE;
            case t2.SYM_TRUE:
              return t2.TRUE;
            case t2.SYM_DONTCARE:
              return t2.DONTCARE;
            case t2.SYM_P:
              return t2.P;
            case t2.SYM_L:
              return t2.L;
            case t2.SYM_A:
              return t2.A;
          }
          throw new m("Unknown dimension symbol: " + e2);
        } }]);
      })();
      Lt.P = 0, Lt.L = 1, Lt.A = 2, Lt.FALSE = -1, Lt.TRUE = -2, Lt.DONTCARE = -3, Lt.SYM_FALSE = "F", Lt.SYM_TRUE = "T", Lt.SYM_DONTCARE = "*", Lt.SYM_P = "0", Lt.SYM_L = "1", Lt.SYM_A = "2";
      var Ct = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "filter", value: function(t2) {
        } }]);
      })(), Rt = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "filter", value: function(t2, e2) {
        } }, { key: "isDone", value: function() {
        } }, { key: "isGeometryChanged", value: function() {
        } }]);
      })(), wt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: function() {
          return this.isEmpty() ? new U() : this._points.expandEnvelope(new U());
        } }, { key: "isRing", value: function() {
          return this.isClosed() && this.isSimple();
        } }, { key: "getCoordinates", value: function() {
          return this._points.toCoordinateArray();
        } }, { key: "copyInternal", value: function() {
          return new i2(this._points.copy(), this._factory);
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            if (!this.isEquivalentClass(t3)) return false;
            var n2 = t3;
            if (this._points.size() !== n2._points.size()) return false;
            for (var r3 = 0; r3 < this._points.size(); r3++) if (!this.equal(this._points.getCoordinate(r3), n2._points.getCoordinate(r3), e2)) return false;
            return true;
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "normalize", value: function() {
          for (var t3 = 0; t3 < Math.trunc(this._points.size() / 2); t3++) {
            var e2 = this._points.size() - 1 - t3;
            if (!this._points.getCoordinate(t3).equals(this._points.getCoordinate(e2))) {
              if (this._points.getCoordinate(t3).compareTo(this._points.getCoordinate(e2)) > 0) {
                var n2 = this._points.copy();
                St.reverse(n2), this._points = n2;
              }
              return null;
            }
          }
        } }, { key: "getCoordinate", value: function() {
          return this.isEmpty() ? null : this._points.getCoordinate(0);
        } }, { key: "getBoundaryDimension", value: function() {
          return this.isClosed() ? Lt.FALSE : 0;
        } }, { key: "isClosed", value: function() {
          return !this.isEmpty() && this.getCoordinateN(0).equals2D(this.getCoordinateN(this.getNumPoints() - 1));
        } }, { key: "reverseInternal", value: function() {
          var t3 = this._points.copy();
          return St.reverse(t3), this.getFactory().createLineString(t3);
        } }, { key: "getEndPoint", value: function() {
          return this.isEmpty() ? null : this.getPointN(this.getNumPoints() - 1);
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_LINESTRING;
        } }, { key: "getDimension", value: function() {
          return 1;
        } }, { key: "getLength", value: function() {
          return Nt.ofLine(this._points);
        } }, { key: "getNumPoints", value: function() {
          return this._points.size();
        } }, { key: "compareToSameClass", value: function() {
          if (1 === arguments.length) {
            for (var t3 = arguments[0], e2 = 0, n2 = 0; e2 < this._points.size() && n2 < t3._points.size(); ) {
              var i3 = this._points.getCoordinate(e2).compareTo(t3._points.getCoordinate(n2));
              if (0 !== i3) return i3;
              e2++, n2++;
            }
            return e2 < this._points.size() ? 1 : n2 < t3._points.size() ? -1 : 0;
          }
          if (2 === arguments.length) {
            var r3 = arguments[0];
            return arguments[1].compare(this._points, r3._points);
          }
        } }, { key: "apply", value: function() {
          if (rt(arguments[0], Et)) for (var t3 = arguments[0], e2 = 0; e2 < this._points.size(); e2++) t3.filter(this._points.getCoordinate(e2));
          else if (rt(arguments[0], Rt)) {
            var n2 = arguments[0];
            if (0 === this._points.size()) return null;
            for (var i3 = 0; i3 < this._points.size() && (n2.filter(this._points, i3), !n2.isDone()); i3++) ;
            n2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            arguments[0].filter(this);
          } else if (rt(arguments[0], k)) {
            arguments[0].filter(this);
          }
        } }, { key: "getBoundary", value: function() {
          throw new W();
        } }, { key: "isEquivalentClass", value: function(t3) {
          return t3 instanceof i2;
        } }, { key: "getCoordinateN", value: function(t3) {
          return this._points.getCoordinate(t3);
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_LINESTRING;
        } }, { key: "getCoordinateSequence", value: function() {
          return this._points;
        } }, { key: "isEmpty", value: function() {
          return 0 === this._points.size();
        } }, { key: "init", value: function(t3) {
          if (null === t3 && (t3 = this.getFactory().getCoordinateSequenceFactory().create([])), 1 === t3.size()) throw new m("Invalid number of points in LineString (found " + t3.size() + " - must be 0 or >= 2)");
          this._points = t3;
        } }, { key: "isCoordinate", value: function(t3) {
          for (var e2 = 0; e2 < this._points.size(); e2++) if (this._points.getCoordinate(e2).equals(t3)) return true;
          return false;
        } }, { key: "getStartPoint", value: function() {
          return this.isEmpty() ? null : this.getPointN(0);
        } }, { key: "getPointN", value: function(t3) {
          return this.getFactory().createPoint(this._points.getCoordinate(t3));
        } }, { key: "interfaces_", get: function() {
          return [Tt];
        } }], [{ key: "constructor_", value: function() {
          if (this._points = null, 0 === arguments.length) ;
          else if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            V.constructor_.call(this, e2), this.init(t3);
          }
        } }]);
      })(V), Ot = s((function t2() {
        n(this, t2);
      })), bt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: function() {
          if (this.isEmpty()) return new U();
          var t3 = new U();
          return t3.expandToInclude(this._coordinates.getX(0), this._coordinates.getY(0)), t3;
        } }, { key: "getCoordinates", value: function() {
          return this.isEmpty() ? [] : [this.getCoordinate()];
        } }, { key: "copyInternal", value: function() {
          return new i2(this._coordinates.copy(), this._factory);
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            return !!this.isEquivalentClass(t3) && (!(!this.isEmpty() || !t3.isEmpty()) || this.isEmpty() === t3.isEmpty() && this.equal(t3.getCoordinate(), this.getCoordinate(), e2));
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "normalize", value: function() {
        } }, { key: "getCoordinate", value: function() {
          return 0 !== this._coordinates.size() ? this._coordinates.getCoordinate(0) : null;
        } }, { key: "getBoundaryDimension", value: function() {
          return Lt.FALSE;
        } }, { key: "reverseInternal", value: function() {
          return this.getFactory().createPoint(this._coordinates.copy());
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_POINT;
        } }, { key: "getDimension", value: function() {
          return 0;
        } }, { key: "getNumPoints", value: function() {
          return this.isEmpty() ? 0 : 1;
        } }, { key: "getX", value: function() {
          if (null === this.getCoordinate()) throw new IllegalStateException("getX called on empty Point");
          return this.getCoordinate().x;
        } }, { key: "compareToSameClass", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.getCoordinate().compareTo(t3.getCoordinate());
          }
          if (2 === arguments.length) {
            var e2 = arguments[0];
            return arguments[1].compare(this._coordinates, e2._coordinates);
          }
        } }, { key: "apply", value: function() {
          if (rt(arguments[0], Et)) {
            var t3 = arguments[0];
            if (this.isEmpty()) return null;
            t3.filter(this.getCoordinate());
          } else if (rt(arguments[0], Rt)) {
            var e2 = arguments[0];
            if (this.isEmpty()) return null;
            e2.filter(this._coordinates, 0), e2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            arguments[0].filter(this);
          } else if (rt(arguments[0], k)) {
            arguments[0].filter(this);
          }
        } }, { key: "getBoundary", value: function() {
          return this.getFactory().createGeometryCollection();
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_POINT;
        } }, { key: "getCoordinateSequence", value: function() {
          return this._coordinates;
        } }, { key: "getY", value: function() {
          if (null === this.getCoordinate()) throw new IllegalStateException("getY called on empty Point");
          return this.getCoordinate().y;
        } }, { key: "isEmpty", value: function() {
          return 0 === this._coordinates.size();
        } }, { key: "init", value: function(t3) {
          null === t3 && (t3 = this.getFactory().getCoordinateSequenceFactory().create([])), G.isTrue(t3.size() <= 1), this._coordinates = t3;
        } }, { key: "isSimple", value: function() {
          return true;
        } }, { key: "interfaces_", get: function() {
          return [Ot];
        } }], [{ key: "constructor_", value: function() {
          this._coordinates = null;
          var t3 = arguments[0], e2 = arguments[1];
          V.constructor_.call(this, e2), this.init(t3);
        } }]);
      })(V), Mt = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "ofRing", value: function() {
          if (arguments[0] instanceof Array) {
            var e2 = arguments[0];
            return Math.abs(t2.ofRingSigned(e2));
          }
          if (rt(arguments[0], ht)) {
            var n2 = arguments[0];
            return Math.abs(t2.ofRingSigned(n2));
          }
        } }, { key: "ofRingSigned", value: function() {
          if (arguments[0] instanceof Array) {
            var t3 = arguments[0];
            if (t3.length < 3) return 0;
            for (var e2 = 0, n2 = t3[0].x, i2 = 1; i2 < t3.length - 1; i2++) {
              var r3 = t3[i2].x - n2, s2 = t3[i2 + 1].y;
              e2 += r3 * (t3[i2 - 1].y - s2);
            }
            return e2 / 2;
          }
          if (rt(arguments[0], ht)) {
            var a2 = arguments[0], o2 = a2.size();
            if (o2 < 3) return 0;
            var u3 = new X(), l2 = new X(), h2 = new X();
            a2.getCoordinate(0, l2), a2.getCoordinate(1, h2);
            var c2 = l2.x;
            h2.x -= c2;
            for (var f2 = 0, g2 = 1; g2 < o2 - 1; g2++) u3.y = l2.y, l2.x = h2.x, l2.y = h2.y, a2.getCoordinate(g2 + 1, h2), h2.x -= c2, f2 += l2.x * (u3.y - h2.y);
            return f2 / 2;
          }
        } }]);
      })(), At = (function() {
        return s((function t2() {
          n(this, t2);
        }), null, [{ key: "sort", value: function() {
          var t2 = arguments, e2 = arguments[0];
          if (1 === arguments.length) e2.sort((function(t3, e3) {
            return t3.compareTo(e3);
          }));
          else if (2 === arguments.length) e2.sort((function(e3, n3) {
            return t2[1].compare(e3, n3);
          }));
          else if (3 === arguments.length) {
            var n2 = e2.slice(arguments[1], arguments[2]);
            n2.sort();
            var i2 = e2.slice(0, arguments[1]).concat(n2, e2.slice(arguments[2], e2.length));
            e2.splice(0, e2.length);
            var r3, s2 = a(i2);
            try {
              for (s2.s(); !(r3 = s2.n()).done; ) {
                var o2 = r3.value;
                e2.push(o2);
              }
            } catch (t3) {
              s2.e(t3);
            } finally {
              s2.f();
            }
          } else if (4 === arguments.length) {
            var u3 = e2.slice(arguments[1], arguments[2]);
            u3.sort((function(e3, n3) {
              return t2[3].compare(e3, n3);
            }));
            var l2 = e2.slice(0, arguments[1]).concat(u3, e2.slice(arguments[2], e2.length));
            e2.splice(0, e2.length);
            var h2, c2 = a(l2);
            try {
              for (c2.s(); !(h2 = c2.n()).done; ) {
                var f2 = h2.value;
                e2.push(f2);
              }
            } catch (t3) {
              c2.e(t3);
            } finally {
              c2.f();
            }
          }
        } }, { key: "asList", value: function(t2) {
          var e2, n2 = new yt(), i2 = a(t2);
          try {
            for (i2.s(); !(e2 = i2.n()).done; ) {
              var r3 = e2.value;
              n2.add(r3);
            }
          } catch (t3) {
            i2.e(t3);
          } finally {
            i2.f();
          }
          return n2;
        } }, { key: "copyOf", value: function(t2, e2) {
          return t2.slice(0, e2);
        } }]);
      })(), Pt = s((function t2() {
        n(this, t2);
      })), Dt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: function() {
          return this._shell.getEnvelopeInternal();
        } }, { key: "getCoordinates", value: function() {
          if (this.isEmpty()) return [];
          for (var t3 = new Array(this.getNumPoints()).fill(null), e2 = -1, n2 = this._shell.getCoordinates(), i3 = 0; i3 < n2.length; i3++) t3[++e2] = n2[i3];
          for (var r3 = 0; r3 < this._holes.length; r3++) for (var s2 = this._holes[r3].getCoordinates(), a2 = 0; a2 < s2.length; a2++) t3[++e2] = s2[a2];
          return t3;
        } }, { key: "getArea", value: function() {
          var t3 = 0;
          t3 += Mt.ofRing(this._shell.getCoordinateSequence());
          for (var e2 = 0; e2 < this._holes.length; e2++) t3 -= Mt.ofRing(this._holes[e2].getCoordinateSequence());
          return t3;
        } }, { key: "copyInternal", value: function() {
          for (var t3 = this._shell.copy(), e2 = new Array(this._holes.length).fill(null), n2 = 0; n2 < this._holes.length; n2++) e2[n2] = this._holes[n2].copy();
          return new i2(t3, e2, this._factory);
        } }, { key: "isRectangle", value: function() {
          if (0 !== this.getNumInteriorRing()) return false;
          if (null === this._shell) return false;
          if (5 !== this._shell.getNumPoints()) return false;
          for (var t3 = this._shell.getCoordinateSequence(), e2 = this.getEnvelopeInternal(), n2 = 0; n2 < 5; n2++) {
            var i3 = t3.getX(n2);
            if (i3 !== e2.getMinX() && i3 !== e2.getMaxX()) return false;
            var r3 = t3.getY(n2);
            if (r3 !== e2.getMinY() && r3 !== e2.getMaxY()) return false;
          }
          for (var s2 = t3.getX(0), a2 = t3.getY(0), o2 = 1; o2 <= 4; o2++) {
            var u3 = t3.getX(o2), l2 = t3.getY(o2);
            if (u3 !== s2 === (l2 !== a2)) return false;
            s2 = u3, a2 = l2;
          }
          return true;
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            if (!this.isEquivalentClass(t3)) return false;
            var n2 = t3, r3 = this._shell, s2 = n2._shell;
            if (!r3.equalsExact(s2, e2)) return false;
            if (this._holes.length !== n2._holes.length) return false;
            for (var a2 = 0; a2 < this._holes.length; a2++) if (!this._holes[a2].equalsExact(n2._holes[a2], e2)) return false;
            return true;
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "normalize", value: function() {
          if (0 === arguments.length) {
            this._shell = this.normalized(this._shell, true);
            for (var t3 = 0; t3 < this._holes.length; t3++) this._holes[t3] = this.normalized(this._holes[t3], false);
            At.sort(this._holes);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            if (e2.isEmpty()) return null;
            var i3 = e2.getCoordinateSequence(), r3 = St.minCoordinateIndex(i3, 0, i3.size() - 2);
            St.scroll(i3, r3, true), ct.isCCW(i3) === n2 && St.reverse(i3);
          }
        } }, { key: "getCoordinate", value: function() {
          return this._shell.getCoordinate();
        } }, { key: "getNumInteriorRing", value: function() {
          return this._holes.length;
        } }, { key: "getBoundaryDimension", value: function() {
          return 1;
        } }, { key: "reverseInternal", value: function() {
          for (var t3 = this.getExteriorRing().reverse(), e2 = new Array(this.getNumInteriorRing()).fill(null), n2 = 0; n2 < e2.length; n2++) e2[n2] = this.getInteriorRingN(n2).reverse();
          return this.getFactory().createPolygon(t3, e2);
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_POLYGON;
        } }, { key: "getDimension", value: function() {
          return 2;
        } }, { key: "getLength", value: function() {
          var t3 = 0;
          t3 += this._shell.getLength();
          for (var e2 = 0; e2 < this._holes.length; e2++) t3 += this._holes[e2].getLength();
          return t3;
        } }, { key: "getNumPoints", value: function() {
          for (var t3 = this._shell.getNumPoints(), e2 = 0; e2 < this._holes.length; e2++) t3 += this._holes[e2].getNumPoints();
          return t3;
        } }, { key: "convexHull", value: function() {
          return this.getExteriorRing().convexHull();
        } }, { key: "normalized", value: function(t3, e2) {
          var n2 = t3.copy();
          return this.normalize(n2, e2), n2;
        } }, { key: "compareToSameClass", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e2 = this._shell, n2 = t3._shell;
            return e2.compareToSameClass(n2);
          }
          if (2 === arguments.length) {
            var i3 = arguments[1], r3 = arguments[0], s2 = this._shell, a2 = r3._shell, o2 = s2.compareToSameClass(a2, i3);
            if (0 !== o2) return o2;
            for (var u3 = this.getNumInteriorRing(), l2 = r3.getNumInteriorRing(), h2 = 0; h2 < u3 && h2 < l2; ) {
              var c2 = this.getInteriorRingN(h2), f2 = r3.getInteriorRingN(h2), g2 = c2.compareToSameClass(f2, i3);
              if (0 !== g2) return g2;
              h2++;
            }
            return h2 < u3 ? 1 : h2 < l2 ? -1 : 0;
          }
        } }, { key: "apply", value: function() {
          if (rt(arguments[0], Et)) {
            var t3 = arguments[0];
            this._shell.apply(t3);
            for (var e2 = 0; e2 < this._holes.length; e2++) this._holes[e2].apply(t3);
          } else if (rt(arguments[0], Rt)) {
            var n2 = arguments[0];
            if (this._shell.apply(n2), !n2.isDone()) for (var i3 = 0; i3 < this._holes.length && (this._holes[i3].apply(n2), !n2.isDone()); i3++) ;
            n2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            arguments[0].filter(this);
          } else if (rt(arguments[0], k)) {
            var r3 = arguments[0];
            r3.filter(this), this._shell.apply(r3);
            for (var s2 = 0; s2 < this._holes.length; s2++) this._holes[s2].apply(r3);
          }
        } }, { key: "getBoundary", value: function() {
          if (this.isEmpty()) return this.getFactory().createMultiLineString();
          var t3 = new Array(this._holes.length + 1).fill(null);
          t3[0] = this._shell;
          for (var e2 = 0; e2 < this._holes.length; e2++) t3[e2 + 1] = this._holes[e2];
          return t3.length <= 1 ? this.getFactory().createLinearRing(t3[0].getCoordinateSequence()) : this.getFactory().createMultiLineString(t3);
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_POLYGON;
        } }, { key: "getExteriorRing", value: function() {
          return this._shell;
        } }, { key: "isEmpty", value: function() {
          return this._shell.isEmpty();
        } }, { key: "getInteriorRingN", value: function(t3) {
          return this._holes[t3];
        } }, { key: "interfaces_", get: function() {
          return [Pt];
        } }], [{ key: "constructor_", value: function() {
          this._shell = null, this._holes = null;
          var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          if (V.constructor_.call(this, n2), null === t3 && (t3 = this.getFactory().createLinearRing()), null === e2 && (e2 = []), V.hasNullElements(e2)) throw new m("holes must not contain null elements");
          if (t3.isEmpty() && V.hasNonEmptyElements(e2)) throw new m("shell is empty but holes are not");
          this._shell = t3, this._holes = e2;
        } }]);
      })(V), Ft = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        return l(i2, t2), s(i2);
      })(K), Gt = (function(t2) {
        function i2(t3) {
          var r3;
          return n(this, i2), (r3 = e(this, i2)).array = [], t3 instanceof Z && r3.addAll(t3), r3;
        }
        return l(i2, t2), s(i2, [{ key: "contains", value: function(t3) {
          var e2, n2 = a(this.array);
          try {
            for (n2.s(); !(e2 = n2.n()).done; ) {
              if (0 === e2.value.compareTo(t3)) return true;
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
          return false;
        } }, { key: "add", value: function(t3) {
          if (this.contains(t3)) return false;
          for (var e2 = 0, n2 = this.array.length; e2 < n2; e2++) {
            if (1 === this.array[e2].compareTo(t3)) return !!this.array.splice(e2, 0, t3);
          }
          return this.array.push(t3), true;
        } }, { key: "addAll", value: function(t3) {
          var e2, n2 = a(t3);
          try {
            for (n2.s(); !(e2 = n2.n()).done; ) {
              var i3 = e2.value;
              this.add(i3);
            }
          } catch (t4) {
            n2.e(t4);
          } finally {
            n2.f();
          }
          return true;
        } }, { key: "remove", value: function() {
          throw new W();
        } }, { key: "size", value: function() {
          return this.array.length;
        } }, { key: "isEmpty", value: function() {
          return 0 === this.array.length;
        } }, { key: "toArray", value: function() {
          return this.array.slice();
        } }, { key: "iterator", value: function() {
          return new qt(this.array);
        } }]);
      })(Ft), qt = (function() {
        return s((function t2(e2) {
          n(this, t2), this.array = e2, this.position = 0;
        }), [{ key: "next", value: function() {
          if (this.position === this.array.length) throw new j();
          return this.array[this.position++];
        } }, { key: "hasNext", value: function() {
          return this.position < this.array.length;
        } }, { key: "remove", value: function() {
          throw new W();
        } }]);
      })(), Yt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "computeEnvelopeInternal", value: function() {
          for (var t3 = new U(), e2 = 0; e2 < this._geometries.length; e2++) t3.expandToInclude(this._geometries[e2].getEnvelopeInternal());
          return t3;
        } }, { key: "getGeometryN", value: function(t3) {
          return this._geometries[t3];
        } }, { key: "getCoordinates", value: function() {
          for (var t3 = new Array(this.getNumPoints()).fill(null), e2 = -1, n2 = 0; n2 < this._geometries.length; n2++) for (var i3 = this._geometries[n2].getCoordinates(), r3 = 0; r3 < i3.length; r3++) t3[++e2] = i3[r3];
          return t3;
        } }, { key: "getArea", value: function() {
          for (var t3 = 0, e2 = 0; e2 < this._geometries.length; e2++) t3 += this._geometries[e2].getArea();
          return t3;
        } }, { key: "copyInternal", value: function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e2 = 0; e2 < t3.length; e2++) t3[e2] = this._geometries[e2].copy();
          return new i2(t3, this._factory);
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            if (!this.isEquivalentClass(t3)) return false;
            var n2 = t3;
            if (this._geometries.length !== n2._geometries.length) return false;
            for (var r3 = 0; r3 < this._geometries.length; r3++) if (!this._geometries[r3].equalsExact(n2._geometries[r3], e2)) return false;
            return true;
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "normalize", value: function() {
          for (var t3 = 0; t3 < this._geometries.length; t3++) this._geometries[t3].normalize();
          At.sort(this._geometries);
        } }, { key: "getCoordinate", value: function() {
          return this.isEmpty() ? null : this._geometries[0].getCoordinate();
        } }, { key: "getBoundaryDimension", value: function() {
          for (var t3 = Lt.FALSE, e2 = 0; e2 < this._geometries.length; e2++) t3 = Math.max(t3, this._geometries[e2].getBoundaryDimension());
          return t3;
        } }, { key: "reverseInternal", value: function() {
          for (var t3 = this._geometries.length, e2 = new yt(t3), n2 = 0; n2 < t3; n2++) e2.add(this._geometries[n2].reverse());
          return this.getFactory().buildGeometry(e2);
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_GEOMETRYCOLLECTION;
        } }, { key: "getDimension", value: function() {
          for (var t3 = Lt.FALSE, e2 = 0; e2 < this._geometries.length; e2++) t3 = Math.max(t3, this._geometries[e2].getDimension());
          return t3;
        } }, { key: "getLength", value: function() {
          for (var t3 = 0, e2 = 0; e2 < this._geometries.length; e2++) t3 += this._geometries[e2].getLength();
          return t3;
        } }, { key: "getNumPoints", value: function() {
          for (var t3 = 0, e2 = 0; e2 < this._geometries.length; e2++) t3 += this._geometries[e2].getNumPoints();
          return t3;
        } }, { key: "getNumGeometries", value: function() {
          return this._geometries.length;
        } }, { key: "compareToSameClass", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e2 = new Gt(At.asList(this._geometries)), n2 = new Gt(At.asList(t3._geometries));
            return this.compare(e2, n2);
          }
          if (2 === arguments.length) {
            for (var i3 = arguments[1], r3 = arguments[0], s2 = this.getNumGeometries(), a2 = r3.getNumGeometries(), o2 = 0; o2 < s2 && o2 < a2; ) {
              var u3 = this.getGeometryN(o2), l2 = r3.getGeometryN(o2), h2 = u3.compareToSameClass(l2, i3);
              if (0 !== h2) return h2;
              o2++;
            }
            return o2 < s2 ? 1 : o2 < a2 ? -1 : 0;
          }
        } }, { key: "apply", value: function() {
          if (rt(arguments[0], Et)) for (var t3 = arguments[0], e2 = 0; e2 < this._geometries.length; e2++) this._geometries[e2].apply(t3);
          else if (rt(arguments[0], Rt)) {
            var n2 = arguments[0];
            if (0 === this._geometries.length) return null;
            for (var i3 = 0; i3 < this._geometries.length && (this._geometries[i3].apply(n2), !n2.isDone()); i3++) ;
            n2.isGeometryChanged() && this.geometryChanged();
          } else if (rt(arguments[0], Ct)) {
            var r3 = arguments[0];
            r3.filter(this);
            for (var s2 = 0; s2 < this._geometries.length; s2++) this._geometries[s2].apply(r3);
          } else if (rt(arguments[0], k)) {
            var a2 = arguments[0];
            a2.filter(this);
            for (var o2 = 0; o2 < this._geometries.length; o2++) this._geometries[o2].apply(a2);
          }
        } }, { key: "getBoundary", value: function() {
          return V.checkNotGeometryCollection(this), G.shouldNeverReachHere(), null;
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_GEOMETRYCOLLECTION;
        } }, { key: "isEmpty", value: function() {
          for (var t3 = 0; t3 < this._geometries.length; t3++) if (!this._geometries[t3].isEmpty()) return false;
          return true;
        } }], [{ key: "constructor_", value: function() {
          if (this._geometries = null, 0 === arguments.length) ;
          else if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            if (V.constructor_.call(this, e2), null === t3 && (t3 = []), V.hasNullElements(t3)) throw new m("geometries must not contain null elements");
            this._geometries = t3;
          }
        } }]);
      })(V), zt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e2 = 0; e2 < t3.length; e2++) t3[e2] = this._geometries[e2].copy();
          return new i2(t3, this._factory);
        } }, { key: "isValid", value: function() {
          return true;
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            return !!this.isEquivalentClass(t3) && f(i2, "equalsExact", this, 1).call(this, t3, e2);
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "getCoordinate", value: function() {
          if (1 === arguments.length && Number.isInteger(arguments[0])) {
            var t3 = arguments[0];
            return this._geometries[t3].getCoordinate();
          }
          return f(i2, "getCoordinate", this, 1).apply(this, arguments);
        } }, { key: "getBoundaryDimension", value: function() {
          return Lt.FALSE;
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_MULTIPOINT;
        } }, { key: "getDimension", value: function() {
          return 0;
        } }, { key: "getBoundary", value: function() {
          return this.getFactory().createGeometryCollection();
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_MULTIPOINT;
        } }, { key: "interfaces_", get: function() {
          return [Ot];
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0], e2 = arguments[1];
          Yt.constructor_.call(this, t3, e2);
        } }]);
      })(Yt), Xt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: function() {
          return new i2(this._points.copy(), this._factory);
        } }, { key: "getBoundaryDimension", value: function() {
          return Lt.FALSE;
        } }, { key: "isClosed", value: function() {
          return !!this.isEmpty() || f(i2, "isClosed", this, 1).call(this);
        } }, { key: "reverseInternal", value: function() {
          var t3 = this._points.copy();
          return St.reverse(t3), this.getFactory().createLinearRing(t3);
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_LINEARRING;
        } }, { key: "validateConstruction", value: function() {
          if (!this.isEmpty() && !f(i2, "isClosed", this, 1).call(this)) throw new m("Points of LinearRing do not form a closed linestring");
          if (this.getCoordinateSequence().size() >= 1 && this.getCoordinateSequence().size() < i2.MINIMUM_VALID_SIZE) throw new m("Invalid number of points in LinearRing (found " + this.getCoordinateSequence().size() + " - must be 0 or >= 4)");
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_LINEARRING;
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0], e2 = arguments[1];
          wt.constructor_.call(this, t3, e2), this.validateConstruction();
        } }]);
      })(wt);
      Xt.MINIMUM_VALID_SIZE = 4;
      var Bt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "setOrdinate", value: function(t3, e2) {
          switch (t3) {
            case i2.X:
              this.x = e2;
              break;
            case i2.Y:
              this.y = e2;
              break;
            default:
              throw new m("Invalid ordinate index: " + t3);
          }
        } }, { key: "getZ", value: function() {
          return X.NULL_ORDINATE;
        } }, { key: "getOrdinate", value: function(t3) {
          switch (t3) {
            case i2.X:
              return this.x;
            case i2.Y:
              return this.y;
          }
          throw new m("Invalid ordinate index: " + t3);
        } }, { key: "setZ", value: function(t3) {
          throw new m("CoordinateXY dimension 2 does not support z-ordinate");
        } }, { key: "copy", value: function() {
          return new i2(this);
        } }, { key: "toString", value: function() {
          return "(" + this.x + ", " + this.y + ")";
        } }, { key: "setCoordinate", value: function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ();
        } }], [{ key: "constructor_", value: function() {
          if (0 === arguments.length) X.constructor_.call(this);
          else if (1 === arguments.length) {
            if (arguments[0] instanceof i2) {
              var t3 = arguments[0];
              X.constructor_.call(this, t3.x, t3.y);
            } else if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              X.constructor_.call(this, e2.x, e2.y);
            }
          } else if (2 === arguments.length) {
            var n2 = arguments[0], r3 = arguments[1];
            X.constructor_.call(this, n2, r3, X.NULL_ORDINATE);
          }
        } }]);
      })(X);
      Bt.X = 0, Bt.Y = 1, Bt.Z = -1, Bt.M = -1;
      var Ut = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "getM", value: function() {
          return this._m;
        } }, { key: "setOrdinate", value: function(t3, e2) {
          switch (t3) {
            case i2.X:
              this.x = e2;
              break;
            case i2.Y:
              this.y = e2;
              break;
            case i2.M:
              this._m = e2;
              break;
            default:
              throw new m("Invalid ordinate index: " + t3);
          }
        } }, { key: "setM", value: function(t3) {
          this._m = t3;
        } }, { key: "getZ", value: function() {
          return X.NULL_ORDINATE;
        } }, { key: "getOrdinate", value: function(t3) {
          switch (t3) {
            case i2.X:
              return this.x;
            case i2.Y:
              return this.y;
            case i2.M:
              return this._m;
          }
          throw new m("Invalid ordinate index: " + t3);
        } }, { key: "setZ", value: function(t3) {
          throw new m("CoordinateXY dimension 2 does not support z-ordinate");
        } }, { key: "copy", value: function() {
          return new i2(this);
        } }, { key: "toString", value: function() {
          return "(" + this.x + ", " + this.y + " m=" + this.getM() + ")";
        } }, { key: "setCoordinate", value: function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ(), this._m = t3.getM();
        } }], [{ key: "constructor_", value: function() {
          if (this._m = null, 0 === arguments.length) X.constructor_.call(this), this._m = 0;
          else if (1 === arguments.length) {
            if (arguments[0] instanceof i2) {
              var t3 = arguments[0];
              X.constructor_.call(this, t3.x, t3.y), this._m = t3._m;
            } else if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              X.constructor_.call(this, e2.x, e2.y), this._m = this.getM();
            }
          } else if (3 === arguments.length) {
            var n2 = arguments[0], r3 = arguments[1], s2 = arguments[2];
            X.constructor_.call(this, n2, r3, X.NULL_ORDINATE), this._m = s2;
          }
        } }]);
      })(X);
      Ut.X = 0, Ut.Y = 1, Ut.Z = -1, Ut.M = 2;
      var Vt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "getM", value: function() {
          return this._m;
        } }, { key: "setOrdinate", value: function(t3, e2) {
          switch (t3) {
            case X.X:
              this.x = e2;
              break;
            case X.Y:
              this.y = e2;
              break;
            case X.Z:
              this.z = e2;
              break;
            case X.M:
              this._m = e2;
              break;
            default:
              throw new m("Invalid ordinate index: " + t3);
          }
        } }, { key: "setM", value: function(t3) {
          this._m = t3;
        } }, { key: "getOrdinate", value: function(t3) {
          switch (t3) {
            case X.X:
              return this.x;
            case X.Y:
              return this.y;
            case X.Z:
              return this.getZ();
            case X.M:
              return this.getM();
          }
          throw new m("Invalid ordinate index: " + t3);
        } }, { key: "copy", value: function() {
          return new i2(this);
        } }, { key: "toString", value: function() {
          return "(" + this.x + ", " + this.y + ", " + this.getZ() + " m=" + this.getM() + ")";
        } }, { key: "setCoordinate", value: function(t3) {
          this.x = t3.x, this.y = t3.y, this.z = t3.getZ(), this._m = t3.getM();
        } }], [{ key: "constructor_", value: function() {
          if (this._m = null, 0 === arguments.length) X.constructor_.call(this), this._m = 0;
          else if (1 === arguments.length) {
            if (arguments[0] instanceof i2) {
              var t3 = arguments[0];
              X.constructor_.call(this, t3), this._m = t3._m;
            } else if (arguments[0] instanceof X) {
              var e2 = arguments[0];
              X.constructor_.call(this, e2), this._m = this.getM();
            }
          } else if (4 === arguments.length) {
            var n2 = arguments[0], r3 = arguments[1], s2 = arguments[2], a2 = arguments[3];
            X.constructor_.call(this, n2, r3, s2), this._m = a2;
          }
        } }]);
      })(X), Ht = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "measures", value: function(t3) {
          return t3 instanceof Bt ? 0 : t3 instanceof Ut || t3 instanceof Vt ? 1 : 0;
        } }, { key: "dimension", value: function(t3) {
          return t3 instanceof Bt ? 2 : t3 instanceof Ut ? 3 : t3 instanceof Vt ? 4 : 3;
        } }, { key: "create", value: function() {
          if (1 === arguments.length) {
            var e2 = arguments[0];
            return t2.create(e2, 0);
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            return 2 === n2 ? new Bt() : 3 === n2 && 0 === i2 ? new X() : 3 === n2 && 1 === i2 ? new Ut() : 4 === n2 && 1 === i2 ? new Vt() : new X();
          }
        } }]);
      })(), Zt = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "getCoordinate", value: function(t3) {
          return this.get(t3);
        } }, { key: "addAll", value: function() {
          if (2 === arguments.length && "boolean" == typeof arguments[1] && rt(arguments[0], Z)) {
            for (var t3 = arguments[1], e2 = false, n2 = arguments[0].iterator(); n2.hasNext(); ) this.add(n2.next(), t3), e2 = true;
            return e2;
          }
          return f(i2, "addAll", this, 1).apply(this, arguments);
        } }, { key: "clone", value: function() {
          for (var t3 = f(i2, "clone", this, 1).call(this), e2 = 0; e2 < this.size(); e2++) t3.add(e2, this.get(e2).clone());
          return t3;
        } }, { key: "toCoordinateArray", value: function() {
          if (0 === arguments.length) return this.toArray(i2.coordArrayType);
          if (1 === arguments.length) {
            if (arguments[0]) return this.toArray(i2.coordArrayType);
            for (var t3 = this.size(), e2 = new Array(t3).fill(null), n2 = 0; n2 < t3; n2++) e2[n2] = this.get(t3 - n2 - 1);
            return e2;
          }
        } }, { key: "add", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return f(i2, "add", this, 1).call(this, t3);
          }
          if (2 === arguments.length) {
            if (arguments[0] instanceof Array && "boolean" == typeof arguments[1]) {
              var e2 = arguments[0], n2 = arguments[1];
              return this.add(e2, n2, true), true;
            }
            if (arguments[0] instanceof X && "boolean" == typeof arguments[1]) {
              var r3 = arguments[0];
              if (!arguments[1] && this.size() >= 1) {
                if (this.get(this.size() - 1).equals2D(r3)) return null;
              }
              f(i2, "add", this, 1).call(this, r3);
            } else if (arguments[0] instanceof Object && "boolean" == typeof arguments[1]) {
              var s2 = arguments[0], a2 = arguments[1];
              return this.add(s2, a2), true;
            }
          } else if (3 === arguments.length) {
            if ("boolean" == typeof arguments[2] && arguments[0] instanceof Array && "boolean" == typeof arguments[1]) {
              var o2 = arguments[0], u3 = arguments[1];
              if (arguments[2]) for (var l2 = 0; l2 < o2.length; l2++) this.add(o2[l2], u3);
              else for (var h2 = o2.length - 1; h2 >= 0; h2--) this.add(o2[h2], u3);
              return true;
            }
            if ("boolean" == typeof arguments[2] && Number.isInteger(arguments[0]) && arguments[1] instanceof X) {
              var c2 = arguments[0], g2 = arguments[1];
              if (!arguments[2]) {
                var v2 = this.size();
                if (v2 > 0) {
                  if (c2 > 0) {
                    if (this.get(c2 - 1).equals2D(g2)) return null;
                  }
                  if (c2 < v2) {
                    if (this.get(c2).equals2D(g2)) return null;
                  }
                }
              }
              f(i2, "add", this, 1).call(this, c2, g2);
            }
          } else if (4 === arguments.length) {
            var y2 = arguments[0], d2 = arguments[1], _2 = arguments[2], p2 = arguments[3], m2 = 1;
            _2 > p2 && (m2 = -1);
            for (var k2 = _2; k2 !== p2; k2 += m2) this.add(y2[k2], d2);
            return true;
          }
        } }, { key: "closeRing", value: function() {
          if (this.size() > 0) {
            var t3 = this.get(0).copy();
            this.add(t3, false);
          }
        } }], [{ key: "constructor_", value: function() {
          if (0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            this.ensureCapacity(t3.length), this.add(t3, true);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            this.ensureCapacity(e2.length), this.add(e2, n2);
          }
        } }]);
      })(yt);
      Zt.coordArrayType = new Array(0).fill(null);
      var jt = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "isRing", value: function(t3) {
          return !(t3.length < 4) && !!t3[0].equals2D(t3[t3.length - 1]);
        } }, { key: "ptNotInList", value: function(e2, n2) {
          for (var i2 = 0; i2 < e2.length; i2++) {
            var r3 = e2[i2];
            if (t2.indexOf(r3, n2) < 0) return r3;
          }
          return null;
        } }, { key: "scroll", value: function(e2, n2) {
          var i2 = t2.indexOf(n2, e2);
          if (i2 < 0) return null;
          var r3 = new Array(e2.length).fill(null);
          mt.arraycopy(e2, i2, r3, 0, e2.length - i2), mt.arraycopy(e2, 0, r3, e2.length - i2, i2), mt.arraycopy(r3, 0, e2, 0, e2.length);
        } }, { key: "equals", value: function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            if (t3 === e2) return true;
            if (null === t3 || null === e2) return false;
            if (t3.length !== e2.length) return false;
            for (var n2 = 0; n2 < t3.length; n2++) if (!t3[n2].equals(e2[n2])) return false;
            return true;
          }
          if (3 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1], s2 = arguments[2];
            if (i2 === r3) return true;
            if (null === i2 || null === r3) return false;
            if (i2.length !== r3.length) return false;
            for (var a2 = 0; a2 < i2.length; a2++) if (0 !== s2.compare(i2[a2], r3[a2])) return false;
            return true;
          }
        } }, { key: "intersection", value: function(t3, e2) {
          for (var n2 = new Zt(), i2 = 0; i2 < t3.length; i2++) e2.intersects(t3[i2]) && n2.add(t3[i2], true);
          return n2.toCoordinateArray();
        } }, { key: "measures", value: function(t3) {
          if (null === t3 || 0 === t3.length) return 0;
          var e2, n2 = 0, i2 = a(t3);
          try {
            for (i2.s(); !(e2 = i2.n()).done; ) {
              var r3 = e2.value;
              n2 = Math.max(n2, Ht.measures(r3));
            }
          } catch (t4) {
            i2.e(t4);
          } finally {
            i2.f();
          }
          return n2;
        } }, { key: "hasRepeatedPoints", value: function(t3) {
          for (var e2 = 1; e2 < t3.length; e2++) if (t3[e2 - 1].equals(t3[e2])) return true;
          return false;
        } }, { key: "removeRepeatedPoints", value: function(e2) {
          return t2.hasRepeatedPoints(e2) ? new Zt(e2, false).toCoordinateArray() : e2;
        } }, { key: "reverse", value: function(t3) {
          for (var e2 = t3.length - 1, n2 = Math.trunc(e2 / 2), i2 = 0; i2 <= n2; i2++) {
            var r3 = t3[i2];
            t3[i2] = t3[e2 - i2], t3[e2 - i2] = r3;
          }
        } }, { key: "removeNull", value: function(t3) {
          for (var e2 = 0, n2 = 0; n2 < t3.length; n2++) null !== t3[n2] && e2++;
          var i2 = new Array(e2).fill(null);
          if (0 === e2) return i2;
          for (var r3 = 0, s2 = 0; s2 < t3.length; s2++) null !== t3[s2] && (i2[r3++] = t3[s2]);
          return i2;
        } }, { key: "copyDeep", value: function() {
          if (1 === arguments.length) {
            for (var t3 = arguments[0], e2 = new Array(t3.length).fill(null), n2 = 0; n2 < t3.length; n2++) e2[n2] = t3[n2].copy();
            return e2;
          }
          if (5 === arguments.length) for (var i2 = arguments[0], r3 = arguments[1], s2 = arguments[2], a2 = arguments[3], o2 = arguments[4], u3 = 0; u3 < o2; u3++) s2[a2 + u3] = i2[r3 + u3].copy();
        } }, { key: "isEqualReversed", value: function(t3, e2) {
          for (var n2 = 0; n2 < t3.length; n2++) {
            var i2 = t3[n2], r3 = e2[t3.length - n2 - 1];
            if (0 !== i2.compareTo(r3)) return false;
          }
          return true;
        } }, { key: "envelope", value: function(t3) {
          for (var e2 = new U(), n2 = 0; n2 < t3.length; n2++) e2.expandToInclude(t3[n2]);
          return e2;
        } }, { key: "toCoordinateArray", value: function(e2) {
          return e2.toArray(t2.coordArrayType);
        } }, { key: "dimension", value: function(t3) {
          if (null === t3 || 0 === t3.length) return 3;
          var e2, n2 = 0, i2 = a(t3);
          try {
            for (i2.s(); !(e2 = i2.n()).done; ) {
              var r3 = e2.value;
              n2 = Math.max(n2, Ht.dimension(r3));
            }
          } catch (t4) {
            i2.e(t4);
          } finally {
            i2.f();
          }
          return n2;
        } }, { key: "atLeastNCoordinatesOrNothing", value: function(t3, e2) {
          return e2.length >= t3 ? e2 : [];
        } }, { key: "indexOf", value: function(t3, e2) {
          for (var n2 = 0; n2 < e2.length; n2++) if (t3.equals(e2[n2])) return n2;
          return -1;
        } }, { key: "increasingDirection", value: function(t3) {
          for (var e2 = 0; e2 < Math.trunc(t3.length / 2); e2++) {
            var n2 = t3.length - 1 - e2, i2 = t3[e2].compareTo(t3[n2]);
            if (0 !== i2) return i2;
          }
          return 1;
        } }, { key: "compare", value: function(t3, e2) {
          for (var n2 = 0; n2 < t3.length && n2 < e2.length; ) {
            var i2 = t3[n2].compareTo(e2[n2]);
            if (0 !== i2) return i2;
            n2++;
          }
          return n2 < e2.length ? -1 : n2 < t3.length ? 1 : 0;
        } }, { key: "minCoordinate", value: function(t3) {
          for (var e2 = null, n2 = 0; n2 < t3.length; n2++) (null === e2 || e2.compareTo(t3[n2]) > 0) && (e2 = t3[n2]);
          return e2;
        } }, { key: "extract", value: function(t3, e2, n2) {
          e2 = kt.clamp(e2, 0, t3.length);
          var i2 = (n2 = kt.clamp(n2, -1, t3.length)) - e2 + 1;
          n2 < 0 && (i2 = 0), e2 >= t3.length && (i2 = 0), n2 < e2 && (i2 = 0);
          var r3 = new Array(i2).fill(null);
          if (0 === i2) return r3;
          for (var s2 = 0, a2 = e2; a2 <= n2; a2++) r3[s2++] = t3[a2];
          return r3;
        } }]);
      })(), Wt = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "compare", value: function(t2, e2) {
          var n2 = t2, i2 = e2;
          return jt.compare(n2, i2);
        } }, { key: "interfaces_", get: function() {
          return [P];
        } }]);
      })(), Kt = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "compare", value: function(t2, e2) {
          var n2 = t2, i2 = e2;
          if (n2.length < i2.length) return -1;
          if (n2.length > i2.length) return 1;
          if (0 === n2.length) return 0;
          var r3 = jt.compare(n2, i2);
          return jt.isEqualReversed(n2, i2) ? 0 : r3;
        } }, { key: "OLDcompare", value: function(t2, e2) {
          var n2 = t2, i2 = e2;
          if (n2.length < i2.length) return -1;
          if (n2.length > i2.length) return 1;
          if (0 === n2.length) return 0;
          for (var r3 = jt.increasingDirection(n2), s2 = jt.increasingDirection(i2), a2 = r3 > 0 ? 0 : n2.length - 1, o2 = s2 > 0 ? 0 : n2.length - 1, u3 = 0; u3 < n2.length; u3++) {
            var l2 = n2[a2].compareTo(i2[o2]);
            if (0 !== l2) return l2;
            a2 += r3, o2 += s2;
          }
          return 0;
        } }, { key: "interfaces_", get: function() {
          return [P];
        } }]);
      })();
      jt.ForwardComparator = Wt, jt.BidirectionalComparator = Kt, jt.coordArrayType = new Array(0).fill(null);
      var Jt = (function() {
        return s((function t2(e2) {
          n(this, t2), this.str = e2;
        }), [{ key: "append", value: function(t2) {
          this.str += t2;
        } }, { key: "setCharAt", value: function(t2, e2) {
          this.str = this.str.substr(0, t2) + e2 + this.str.substr(t2 + 1);
        } }, { key: "toString", value: function() {
          return this.str;
        } }]);
      })(), Qt = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getM", value: function(t3) {
          return this.hasM() ? this._coordinates[t3].getM() : A.NaN;
        } }, { key: "setOrdinate", value: function(t3, e2, n2) {
          switch (e2) {
            case ht.X:
              this._coordinates[t3].x = n2;
              break;
            case ht.Y:
              this._coordinates[t3].y = n2;
              break;
            default:
              this._coordinates[t3].setOrdinate(e2, n2);
          }
        } }, { key: "getZ", value: function(t3) {
          return this.hasZ() ? this._coordinates[t3].getZ() : A.NaN;
        } }, { key: "size", value: function() {
          return this._coordinates.length;
        } }, { key: "getOrdinate", value: function(t3, e2) {
          switch (e2) {
            case ht.X:
              return this._coordinates[t3].x;
            case ht.Y:
              return this._coordinates[t3].y;
            default:
              return this._coordinates[t3].getOrdinate(e2);
          }
        } }, { key: "getCoordinate", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this._coordinates[t3];
          }
          if (2 === arguments.length) {
            var e2 = arguments[0];
            arguments[1].setCoordinate(this._coordinates[e2]);
          }
        } }, { key: "getCoordinateCopy", value: function(t3) {
          var e2 = this.createCoordinate();
          return e2.setCoordinate(this._coordinates[t3]), e2;
        } }, { key: "createCoordinate", value: function() {
          return Ht.create(this.getDimension(), this.getMeasures());
        } }, { key: "getDimension", value: function() {
          return this._dimension;
        } }, { key: "getX", value: function(t3) {
          return this._coordinates[t3].x;
        } }, { key: "getMeasures", value: function() {
          return this._measures;
        } }, { key: "expandEnvelope", value: function(t3) {
          for (var e2 = 0; e2 < this._coordinates.length; e2++) t3.expandToInclude(this._coordinates[e2]);
          return t3;
        } }, { key: "copy", value: function() {
          for (var e2 = new Array(this.size()).fill(null), n2 = 0; n2 < this._coordinates.length; n2++) {
            var i2 = this.createCoordinate();
            i2.setCoordinate(this._coordinates[n2]), e2[n2] = i2;
          }
          return new t2(e2, this._dimension, this._measures);
        } }, { key: "toString", value: function() {
          if (this._coordinates.length > 0) {
            var t3 = new Jt(17 * this._coordinates.length);
            t3.append("("), t3.append(this._coordinates[0]);
            for (var e2 = 1; e2 < this._coordinates.length; e2++) t3.append(", "), t3.append(this._coordinates[e2]);
            return t3.append(")"), t3.toString();
          }
          return "()";
        } }, { key: "getY", value: function(t3) {
          return this._coordinates[t3].y;
        } }, { key: "toCoordinateArray", value: function() {
          return this._coordinates;
        } }, { key: "interfaces_", get: function() {
          return [ht, E];
        } }], [{ key: "constructor_", value: function() {
          if (this._dimension = 3, this._measures = 0, this._coordinates = null, 1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var e2 = arguments[0];
              t2.constructor_.call(this, e2, jt.dimension(e2), jt.measures(e2));
            } else if (Number.isInteger(arguments[0])) {
              var n2 = arguments[0];
              this._coordinates = new Array(n2).fill(null);
              for (var i2 = 0; i2 < n2; i2++) this._coordinates[i2] = new X();
            } else if (rt(arguments[0], ht)) {
              var r3 = arguments[0];
              if (null === r3) return this._coordinates = new Array(0).fill(null), null;
              this._dimension = r3.getDimension(), this._measures = r3.getMeasures(), this._coordinates = new Array(r3.size()).fill(null);
              for (var s2 = 0; s2 < this._coordinates.length; s2++) this._coordinates[s2] = r3.getCoordinateCopy(s2);
            }
          } else if (2 === arguments.length) {
            if (arguments[0] instanceof Array && Number.isInteger(arguments[1])) {
              var a2 = arguments[0], o2 = arguments[1];
              t2.constructor_.call(this, a2, o2, jt.measures(a2));
            } else if (Number.isInteger(arguments[0]) && Number.isInteger(arguments[1])) {
              var u3 = arguments[0], l2 = arguments[1];
              this._coordinates = new Array(u3).fill(null), this._dimension = l2;
              for (var h2 = 0; h2 < u3; h2++) this._coordinates[h2] = Ht.create(l2);
            }
          } else if (3 === arguments.length) {
            if (Number.isInteger(arguments[2]) && arguments[0] instanceof Array && Number.isInteger(arguments[1])) {
              var c2 = arguments[0], f2 = arguments[1], g2 = arguments[2];
              this._dimension = f2, this._measures = g2, this._coordinates = null === c2 ? new Array(0).fill(null) : c2;
            } else if (Number.isInteger(arguments[2]) && Number.isInteger(arguments[0]) && Number.isInteger(arguments[1])) {
              var v2 = arguments[0], y2 = arguments[1], d2 = arguments[2];
              this._coordinates = new Array(v2).fill(null), this._dimension = y2, this._measures = d2;
              for (var _2 = 0; _2 < v2; _2++) this._coordinates[_2] = this.createCoordinate();
            }
          }
        } }]);
      })(), $t = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, [{ key: "readResolve", value: function() {
          return t2.instance();
        } }, { key: "create", value: function() {
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) return new Qt(arguments[0]);
            if (rt(arguments[0], ht)) return new Qt(arguments[0]);
          } else {
            if (2 === arguments.length) {
              var t3 = arguments[1];
              return t3 > 3 && (t3 = 3), t3 < 2 && (t3 = 2), new Qt(arguments[0], t3);
            }
            if (3 === arguments.length) {
              var e2 = arguments[2], n2 = arguments[1] - e2;
              return e2 > 1 && (e2 = 1), n2 > 3 && (n2 = 3), n2 < 2 && (n2 = 2), new Qt(arguments[0], n2 + e2, e2);
            }
          }
        } }, { key: "interfaces_", get: function() {
          return [It, E];
        } }], [{ key: "instance", value: function() {
          return t2.instanceObject;
        } }]);
      })();
      $t.instanceObject = new $t();
      var te = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e2 = 0; e2 < t3.length; e2++) t3[e2] = this._geometries[e2].copy();
          return new i2(t3, this._factory);
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            return !!this.isEquivalentClass(t3) && f(i2, "equalsExact", this, 1).call(this, t3, e2);
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "getBoundaryDimension", value: function() {
          return 1;
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_MULTIPOLYGON;
        } }, { key: "getDimension", value: function() {
          return 2;
        } }, { key: "getBoundary", value: function() {
          if (this.isEmpty()) return this.getFactory().createMultiLineString();
          for (var t3 = new yt(), e2 = 0; e2 < this._geometries.length; e2++) for (var n2 = this._geometries[e2].getBoundary(), i3 = 0; i3 < n2.getNumGeometries(); i3++) t3.add(n2.getGeometryN(i3));
          var r3 = new Array(t3.size()).fill(null);
          return this.getFactory().createMultiLineString(t3.toArray(r3));
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_MULTIPOLYGON;
        } }, { key: "interfaces_", get: function() {
          return [Pt];
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0], e2 = arguments[1];
          Yt.constructor_.call(this, t3, e2);
        } }]);
      })(Yt), ee = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "get", value: function() {
        } }, { key: "put", value: function() {
        } }, { key: "size", value: function() {
        } }, { key: "values", value: function() {
        } }, { key: "entrySet", value: function() {
        } }]);
      })(), ne = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), (t3 = e(this, i2)).map = /* @__PURE__ */ new Map(), t3;
        }
        return l(i2, t2), s(i2, [{ key: "get", value: function(t3) {
          return this.map.get(t3) || null;
        } }, { key: "put", value: function(t3, e2) {
          return this.map.set(t3, e2), e2;
        } }, { key: "values", value: function() {
          for (var t3 = new yt(), e2 = this.map.values(), n2 = e2.next(); !n2.done; ) t3.add(n2.value), n2 = e2.next();
          return t3;
        } }, { key: "entrySet", value: function() {
          var t3 = new J();
          return this.map.entries().forEach((function(e2) {
            return t3.add(e2);
          })), t3;
        } }, { key: "size", value: function() {
          return this.map.size();
        } }]);
      })(ee), ie = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "equals", value: function(e2) {
          if (!(e2 instanceof t2)) return false;
          var n2 = e2;
          return this._modelType === n2._modelType && this._scale === n2._scale;
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3, n2 = this.getMaximumSignificantDigits(), i2 = e2.getMaximumSignificantDigits();
          return at.compare(n2, i2);
        } }, { key: "getScale", value: function() {
          return this._scale;
        } }, { key: "isFloating", value: function() {
          return this._modelType === t2.FLOATING || this._modelType === t2.FLOATING_SINGLE;
        } }, { key: "getType", value: function() {
          return this._modelType;
        } }, { key: "toString", value: function() {
          var e2 = "UNKNOWN";
          return this._modelType === t2.FLOATING ? e2 = "Floating" : this._modelType === t2.FLOATING_SINGLE ? e2 = "Floating-Single" : this._modelType === t2.FIXED && (e2 = "Fixed (Scale=" + this.getScale() + ")"), e2;
        } }, { key: "makePrecise", value: function() {
          if ("number" == typeof arguments[0]) {
            var e2 = arguments[0];
            return A.isNaN(e2) || this._modelType === t2.FLOATING_SINGLE ? e2 : this._modelType === t2.FIXED ? Math.round(e2 * this._scale) / this._scale : e2;
          }
          if (arguments[0] instanceof X) {
            var n2 = arguments[0];
            if (this._modelType === t2.FLOATING) return null;
            n2.x = this.makePrecise(n2.x), n2.y = this.makePrecise(n2.y);
          }
        } }, { key: "getMaximumSignificantDigits", value: function() {
          var e2 = 16;
          return this._modelType === t2.FLOATING ? e2 = 16 : this._modelType === t2.FLOATING_SINGLE ? e2 = 6 : this._modelType === t2.FIXED && (e2 = 1 + Math.trunc(Math.ceil(Math.log(this.getScale()) / Math.log(10)))), e2;
        } }, { key: "setScale", value: function(t3) {
          this._scale = Math.abs(t3);
        } }, { key: "interfaces_", get: function() {
          return [E, x];
        } }], [{ key: "constructor_", value: function() {
          if (this._modelType = null, this._scale = null, 0 === arguments.length) this._modelType = t2.FLOATING;
          else if (1 === arguments.length) {
            if (arguments[0] instanceof re) {
              var e2 = arguments[0];
              this._modelType = e2, e2 === t2.FIXED && this.setScale(1);
            } else if ("number" == typeof arguments[0]) {
              var n2 = arguments[0];
              this._modelType = t2.FIXED, this.setScale(n2);
            } else if (arguments[0] instanceof t2) {
              var i2 = arguments[0];
              this._modelType = i2._modelType, this._scale = i2._scale;
            }
          }
        } }, { key: "mostPrecise", value: function(t3, e2) {
          return t3.compareTo(e2) >= 0 ? t3 : e2;
        } }]);
      })(), re = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "readResolve", value: function() {
          return t2.nameToTypeMap.get(this._name);
        } }, { key: "toString", value: function() {
          return this._name;
        } }, { key: "interfaces_", get: function() {
          return [E];
        } }], [{ key: "constructor_", value: function() {
          this._name = null;
          var e2 = arguments[0];
          this._name = e2, t2.nameToTypeMap.put(e2, this);
        } }]);
      })();
      re.nameToTypeMap = new ne(), ie.Type = re, ie.FIXED = new re("FIXED"), ie.FLOATING = new re("FLOATING"), ie.FLOATING_SINGLE = new re("FLOATING SINGLE"), ie.maximumPreciseValue = 9007199254740992;
      var se = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "copyInternal", value: function() {
          for (var t3 = new Array(this._geometries.length).fill(null), e2 = 0; e2 < t3.length; e2++) t3[e2] = this._geometries[e2].copy();
          return new i2(t3, this._factory);
        } }, { key: "equalsExact", value: function() {
          if (2 === arguments.length && "number" == typeof arguments[1] && arguments[0] instanceof V) {
            var t3 = arguments[0], e2 = arguments[1];
            return !!this.isEquivalentClass(t3) && f(i2, "equalsExact", this, 1).call(this, t3, e2);
          }
          return f(i2, "equalsExact", this, 1).apply(this, arguments);
        } }, { key: "getBoundaryDimension", value: function() {
          return this.isClosed() ? Lt.FALSE : 0;
        } }, { key: "isClosed", value: function() {
          if (this.isEmpty()) return false;
          for (var t3 = 0; t3 < this._geometries.length; t3++) if (!this._geometries[t3].isClosed()) return false;
          return true;
        } }, { key: "getTypeCode", value: function() {
          return V.TYPECODE_MULTILINESTRING;
        } }, { key: "getDimension", value: function() {
          return 1;
        } }, { key: "getBoundary", value: function() {
          throw new W();
        } }, { key: "getGeometryType", value: function() {
          return V.TYPENAME_MULTILINESTRING;
        } }, { key: "interfaces_", get: function() {
          return [Tt];
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0], e2 = arguments[1];
          Yt.constructor_.call(this, t3, e2);
        } }]);
      })(Yt), ae = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "createEmpty", value: function(t3) {
          switch (t3) {
            case -1:
              return this.createGeometryCollection();
            case 0:
              return this.createPoint();
            case 1:
              return this.createLineString();
            case 2:
              return this.createPolygon();
            default:
              throw new m("Invalid dimension: " + t3);
          }
        } }, { key: "toGeometry", value: function(t3) {
          return t3.isNull() ? this.createPoint() : t3.getMinX() === t3.getMaxX() && t3.getMinY() === t3.getMaxY() ? this.createPoint(new X(t3.getMinX(), t3.getMinY())) : t3.getMinX() === t3.getMaxX() || t3.getMinY() === t3.getMaxY() ? this.createLineString([new X(t3.getMinX(), t3.getMinY()), new X(t3.getMaxX(), t3.getMaxY())]) : this.createPolygon(this.createLinearRing([new X(t3.getMinX(), t3.getMinY()), new X(t3.getMinX(), t3.getMaxY()), new X(t3.getMaxX(), t3.getMaxY()), new X(t3.getMaxX(), t3.getMinY()), new X(t3.getMinX(), t3.getMinY())]), null);
        } }, { key: "createLineString", value: function() {
          if (0 === arguments.length) return this.createLineString(this.getCoordinateSequenceFactory().create([]));
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var t3 = arguments[0];
              return this.createLineString(null !== t3 ? this.getCoordinateSequenceFactory().create(t3) : null);
            }
            if (rt(arguments[0], ht)) return new wt(arguments[0], this);
          }
        } }, { key: "createMultiLineString", value: function() {
          return 0 === arguments.length ? new se(null, this) : 1 === arguments.length ? new se(arguments[0], this) : void 0;
        } }, { key: "buildGeometry", value: function(e2) {
          for (var n2 = null, i2 = false, r3 = false, s2 = e2.iterator(); s2.hasNext(); ) {
            var a2 = s2.next(), o2 = a2.getTypeCode();
            null === n2 && (n2 = o2), o2 !== n2 && (i2 = true), a2 instanceof Yt && (r3 = true);
          }
          if (null === n2) return this.createGeometryCollection();
          if (i2 || r3) return this.createGeometryCollection(t2.toGeometryArray(e2));
          var u3 = e2.iterator().next();
          if (e2.size() > 1) {
            if (u3 instanceof Dt) return this.createMultiPolygon(t2.toPolygonArray(e2));
            if (u3 instanceof wt) return this.createMultiLineString(t2.toLineStringArray(e2));
            if (u3 instanceof bt) return this.createMultiPoint(t2.toPointArray(e2));
            G.shouldNeverReachHere("Unhandled geometry type: " + u3.getGeometryType());
          }
          return u3;
        } }, { key: "createMultiPointFromCoords", value: function(t3) {
          return this.createMultiPoint(null !== t3 ? this.getCoordinateSequenceFactory().create(t3) : null);
        } }, { key: "createPoint", value: function() {
          if (0 === arguments.length) return this.createPoint(this.getCoordinateSequenceFactory().create([]));
          if (1 === arguments.length) {
            if (arguments[0] instanceof X) {
              var t3 = arguments[0];
              return this.createPoint(null !== t3 ? this.getCoordinateSequenceFactory().create([t3]) : null);
            }
            if (rt(arguments[0], ht)) return new bt(arguments[0], this);
          }
        } }, { key: "getCoordinateSequenceFactory", value: function() {
          return this._coordinateSequenceFactory;
        } }, { key: "createPolygon", value: function() {
          if (0 === arguments.length) return this.createPolygon(null, null);
          if (1 === arguments.length) {
            if (rt(arguments[0], ht)) {
              var t3 = arguments[0];
              return this.createPolygon(this.createLinearRing(t3));
            }
            if (arguments[0] instanceof Array) {
              var e2 = arguments[0];
              return this.createPolygon(this.createLinearRing(e2));
            }
            if (arguments[0] instanceof Xt) {
              var n2 = arguments[0];
              return this.createPolygon(n2, null);
            }
          } else if (2 === arguments.length) {
            return new Dt(arguments[0], arguments[1], this);
          }
        } }, { key: "getSRID", value: function() {
          return this._SRID;
        } }, { key: "createGeometryCollection", value: function() {
          return 0 === arguments.length ? new Yt(null, this) : 1 === arguments.length ? new Yt(arguments[0], this) : void 0;
        } }, { key: "getPrecisionModel", value: function() {
          return this._precisionModel;
        } }, { key: "createLinearRing", value: function() {
          if (0 === arguments.length) return this.createLinearRing(this.getCoordinateSequenceFactory().create([]));
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var t3 = arguments[0];
              return this.createLinearRing(null !== t3 ? this.getCoordinateSequenceFactory().create(t3) : null);
            }
            if (rt(arguments[0], ht)) return new Xt(arguments[0], this);
          }
        } }, { key: "createMultiPolygon", value: function() {
          return 0 === arguments.length ? new te(null, this) : 1 === arguments.length ? new te(arguments[0], this) : void 0;
        } }, { key: "createMultiPoint", value: function() {
          if (0 === arguments.length) return new zt(null, this);
          if (1 === arguments.length) {
            if (arguments[0] instanceof Array) return new zt(arguments[0], this);
            if (rt(arguments[0], ht)) {
              var t3 = arguments[0];
              if (null === t3) return this.createMultiPoint(new Array(0).fill(null));
              for (var e2 = new Array(t3.size()).fill(null), n2 = 0; n2 < t3.size(); n2++) {
                var i2 = this.getCoordinateSequenceFactory().create(1, t3.getDimension(), t3.getMeasures());
                St.copy(t3, n2, i2, 0, 1), e2[n2] = this.createPoint(i2);
              }
              return this.createMultiPoint(e2);
            }
          }
        } }, { key: "interfaces_", get: function() {
          return [E];
        } }], [{ key: "constructor_", value: function() {
          if (this._precisionModel = null, this._coordinateSequenceFactory = null, this._SRID = null, 0 === arguments.length) t2.constructor_.call(this, new ie(), 0);
          else if (1 === arguments.length) {
            if (rt(arguments[0], It)) {
              var e2 = arguments[0];
              t2.constructor_.call(this, new ie(), 0, e2);
            } else if (arguments[0] instanceof ie) {
              var n2 = arguments[0];
              t2.constructor_.call(this, n2, 0, t2.getDefaultCoordinateSequenceFactory());
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            t2.constructor_.call(this, i2, r3, t2.getDefaultCoordinateSequenceFactory());
          } else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2];
            this._precisionModel = s2, this._coordinateSequenceFactory = o2, this._SRID = a2;
          }
        } }, { key: "toMultiPolygonArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "toGeometryArray", value: function(t3) {
          if (null === t3) return null;
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "getDefaultCoordinateSequenceFactory", value: function() {
          return $t.instance();
        } }, { key: "toMultiLineStringArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "toLineStringArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "toMultiPointArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "toLinearRingArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "toPointArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "toPolygonArray", value: function(t3) {
          var e2 = new Array(t3.size()).fill(null);
          return t3.toArray(e2);
        } }, { key: "createPointFromInternalCoord", value: function(t3, e2) {
          return e2.getPrecisionModel().makePrecise(t3), e2.getFactory().createPoint(t3);
        } }]);
      })(), oe = "XY", ue = "XYZ", le = "XYM", he = "XYZM", ce = { POINT: "Point", LINE_STRING: "LineString", LINEAR_RING: "LinearRing", POLYGON: "Polygon", MULTI_POINT: "MultiPoint", MULTI_LINE_STRING: "MultiLineString", MULTI_POLYGON: "MultiPolygon", GEOMETRY_COLLECTION: "GeometryCollection", CIRCLE: "Circle" }, fe = "EMPTY", ge = 1, ve = 2, ye = 3, de = 4, _e = 5, pe = 6;
      for (var me in ce) ce[me].toUpperCase();
      var ke = (function() {
        return s((function t2(e2) {
          n(this, t2), this.wkt = e2, this.index_ = -1;
        }), [{ key: "isAlpha_", value: function(t2) {
          return t2 >= "a" && t2 <= "z" || t2 >= "A" && t2 <= "Z";
        } }, { key: "isNumeric_", value: function(t2, e2) {
          return t2 >= "0" && t2 <= "9" || "." == t2 && !(void 0 !== e2 && e2);
        } }, { key: "isWhiteSpace_", value: function(t2) {
          return " " == t2 || "	" == t2 || "\r" == t2 || "\n" == t2;
        } }, { key: "nextChar_", value: function() {
          return this.wkt.charAt(++this.index_);
        } }, { key: "nextToken", value: function() {
          var t2, e2 = this.nextChar_(), n2 = this.index_, i2 = e2;
          if ("(" == e2) t2 = ve;
          else if ("," == e2) t2 = _e;
          else if (")" == e2) t2 = ye;
          else if (this.isNumeric_(e2) || "-" == e2) t2 = de, i2 = this.readNumber_();
          else if (this.isAlpha_(e2)) t2 = ge, i2 = this.readText_();
          else {
            if (this.isWhiteSpace_(e2)) return this.nextToken();
            if ("" !== e2) throw new Error("Unexpected character: " + e2);
            t2 = pe;
          }
          return { position: n2, value: i2, type: t2 };
        } }, { key: "readNumber_", value: function() {
          var t2, e2 = this.index_, n2 = false, i2 = false;
          do {
            "." == t2 ? n2 = true : "e" != t2 && "E" != t2 || (i2 = true), t2 = this.nextChar_();
          } while (this.isNumeric_(t2, n2) || !i2 && ("e" == t2 || "E" == t2) || i2 && ("-" == t2 || "+" == t2));
          return parseFloat(this.wkt.substring(e2, this.index_--));
        } }, { key: "readText_", value: function() {
          var t2, e2 = this.index_;
          do {
            t2 = this.nextChar_();
          } while (this.isAlpha_(t2));
          return this.wkt.substring(e2, this.index_--).toUpperCase();
        } }]);
      })(), xe = (function() {
        return s((function t2(e2, i2) {
          n(this, t2), this.lexer_ = e2, this.token_, this.layout_ = oe, this.factory = i2;
        }), [{ key: "consume_", value: function() {
          this.token_ = this.lexer_.nextToken();
        } }, { key: "isTokenType", value: function(t2) {
          return this.token_.type == t2;
        } }, { key: "match", value: function(t2) {
          var e2 = this.isTokenType(t2);
          return e2 && this.consume_(), e2;
        } }, { key: "parse", value: function() {
          return this.consume_(), this.parseGeometry_();
        } }, { key: "parseGeometryLayout_", value: function() {
          var t2 = oe, e2 = this.token_;
          if (this.isTokenType(ge)) {
            var n2 = e2.value;
            "Z" === n2 ? t2 = ue : "M" === n2 ? t2 = le : "ZM" === n2 && (t2 = he), t2 !== oe && this.consume_();
          }
          return t2;
        } }, { key: "parseGeometryCollectionText_", value: function() {
          if (this.match(ve)) {
            var t2 = [];
            do {
              t2.push(this.parseGeometry_());
            } while (this.match(_e));
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parsePointText_", value: function() {
          if (this.match(ve)) {
            var t2 = this.parsePoint_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return null;
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parseLineStringText_", value: function() {
          if (this.match(ve)) {
            var t2 = this.parsePointList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parsePolygonText_", value: function() {
          if (this.match(ve)) {
            var t2 = this.parseLineStringTextList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parseMultiPointText_", value: function() {
          var t2;
          if (this.match(ve)) {
            if (t2 = this.token_.type == ve ? this.parsePointTextList_() : this.parsePointList_(), this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parseMultiLineStringText_", value: function() {
          if (this.match(ve)) {
            var t2 = this.parseLineStringTextList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parseMultiPolygonText_", value: function() {
          if (this.match(ve)) {
            var t2 = this.parsePolygonTextList_();
            if (this.match(ye)) return t2;
          } else if (this.isEmptyGeometry_()) return [];
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parsePoint_", value: function() {
          for (var t2 = [], e2 = this.layout_.length, n2 = 0; n2 < e2; ++n2) {
            var i2 = this.token_;
            if (!this.match(de)) break;
            t2.push(i2.value);
          }
          if (t2.length == e2) return t2;
          throw new Error(this.formatErrorMessage_());
        } }, { key: "parsePointList_", value: function() {
          for (var t2 = [this.parsePoint_()]; this.match(_e); ) t2.push(this.parsePoint_());
          return t2;
        } }, { key: "parsePointTextList_", value: function() {
          for (var t2 = [this.parsePointText_()]; this.match(_e); ) t2.push(this.parsePointText_());
          return t2;
        } }, { key: "parseLineStringTextList_", value: function() {
          for (var t2 = [this.parseLineStringText_()]; this.match(_e); ) t2.push(this.parseLineStringText_());
          return t2;
        } }, { key: "parsePolygonTextList_", value: function() {
          for (var t2 = [this.parsePolygonText_()]; this.match(_e); ) t2.push(this.parsePolygonText_());
          return t2;
        } }, { key: "isEmptyGeometry_", value: function() {
          var t2 = this.isTokenType(ge) && this.token_.value == fe;
          return t2 && this.consume_(), t2;
        } }, { key: "formatErrorMessage_", value: function() {
          return "Unexpected `" + this.token_.value + "` at position " + this.token_.position + " in `" + this.lexer_.wkt + "`";
        } }, { key: "parseGeometry_", value: function() {
          var t2 = this.factory, e2 = function(t3) {
            return i(X, g(t3));
          }, n2 = function(n3) {
            var i2 = n3.map((function(n4) {
              return t2.createLinearRing(n4.map(e2));
            }));
            return i2.length > 1 ? t2.createPolygon(i2[0], i2.slice(1)) : t2.createPolygon(i2[0]);
          }, r3 = this.token_;
          if (this.match(ge)) {
            var s2 = r3.value;
            if (this.layout_ = this.parseGeometryLayout_(), "GEOMETRYCOLLECTION" == s2) {
              var a2 = this.parseGeometryCollectionText_();
              return t2.createGeometryCollection(a2);
            }
            switch (s2) {
              case "POINT":
                var o2 = this.parsePointText_();
                return o2 ? t2.createPoint(i(X, g(o2))) : t2.createPoint();
              case "LINESTRING":
                var u3 = this.parseLineStringText_().map(e2);
                return t2.createLineString(u3);
              case "LINEARRING":
                var l2 = this.parseLineStringText_().map(e2);
                return t2.createLinearRing(l2);
              case "POLYGON":
                var h2 = this.parsePolygonText_();
                return h2 && 0 !== h2.length ? n2(h2) : t2.createPolygon();
              case "MULTIPOINT":
                var c2 = this.parseMultiPointText_();
                if (!c2 || 0 === c2.length) return t2.createMultiPoint();
                var f2 = c2.map(e2).map((function(e3) {
                  return t2.createPoint(e3);
                }));
                return t2.createMultiPoint(f2);
              case "MULTILINESTRING":
                var v2 = this.parseMultiLineStringText_().map((function(n3) {
                  return t2.createLineString(n3.map(e2));
                }));
                return t2.createMultiLineString(v2);
              case "MULTIPOLYGON":
                var y2 = this.parseMultiPolygonText_();
                if (!y2 || 0 === y2.length) return t2.createMultiPolygon();
                var d2 = y2.map(n2);
                return t2.createMultiPolygon(d2);
              default:
                throw new Error("Invalid geometry type: " + s2);
            }
          }
          throw new Error(this.formatErrorMessage_());
        } }]);
      })();
      function Ie(t2) {
        if (t2.isEmpty()) return "";
        var e2 = t2.getCoordinate(), n2 = [e2.x, e2.y];
        return void 0 === e2.z || Number.isNaN(e2.z) || n2.push(e2.z), void 0 === e2.m || Number.isNaN(e2.m) || n2.push(e2.m), n2.join(" ");
      }
      function Ee(t2) {
        for (var e2 = t2.getCoordinates().map((function(t3) {
          var e3 = [t3.x, t3.y];
          return void 0 === t3.z || Number.isNaN(t3.z) || e3.push(t3.z), void 0 === t3.m || Number.isNaN(t3.m) || e3.push(t3.m), e3;
        })), n2 = [], i2 = 0, r3 = e2.length; i2 < r3; ++i2) n2.push(e2[i2].join(" "));
        return n2.join(", ");
      }
      function Ne(t2) {
        var e2 = [];
        e2.push("(" + Ee(t2.getExteriorRing()) + ")");
        for (var n2 = 0, i2 = t2.getNumInteriorRing(); n2 < i2; ++n2) e2.push("(" + Ee(t2.getInteriorRingN(n2)) + ")");
        return e2.join(", ");
      }
      var Te = { Point: Ie, LineString: Ee, LinearRing: Ee, Polygon: Ne, MultiPoint: function(t2) {
        for (var e2 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e2.push("(" + Ie(t2.getGeometryN(n2)) + ")");
        return e2.join(", ");
      }, MultiLineString: function(t2) {
        for (var e2 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e2.push("(" + Ee(t2.getGeometryN(n2)) + ")");
        return e2.join(", ");
      }, MultiPolygon: function(t2) {
        for (var e2 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e2.push("(" + Ne(t2.getGeometryN(n2)) + ")");
        return e2.join(", ");
      }, GeometryCollection: function(t2) {
        for (var e2 = [], n2 = 0, i2 = t2.getNumGeometries(); n2 < i2; ++n2) e2.push(Se(t2.getGeometryN(n2)));
        return e2.join(", ");
      } };
      function Se(t2) {
        var e2 = t2.getGeometryType(), n2 = Te[e2];
        e2 = e2.toUpperCase();
        var i2 = (function(t3) {
          var e3 = "";
          if (t3.isEmpty()) return e3;
          var n3 = t3.getCoordinate();
          return void 0 === n3.z || Number.isNaN(n3.z) || (e3 += "Z"), void 0 === n3.m || Number.isNaN(n3.m) || (e3 += "M"), e3;
        })(t2);
        return i2.length > 0 && (e2 += " " + i2), t2.isEmpty() ? e2 + " " + fe : e2 + " (" + n2(t2) + ")";
      }
      var Le = (function() {
        return s((function t2(e2) {
          n(this, t2), this.geometryFactory = e2 || new ae(), this.precisionModel = this.geometryFactory.getPrecisionModel();
        }), [{ key: "read", value: function(t2) {
          var e2 = new ke(t2);
          return new xe(e2, this.geometryFactory).parse();
        } }, { key: "write", value: function(t2) {
          return Se(t2);
        } }]);
      })(), Ce = (function() {
        return s((function t2(e2) {
          n(this, t2), this.parser = new Le(e2);
        }), [{ key: "write", value: function(t2) {
          return this.parser.write(t2);
        } }], [{ key: "toLineString", value: function(t2, e2) {
          if (2 !== arguments.length) throw new Error("Not implemented");
          return "LINESTRING ( " + t2.x + " " + t2.y + ", " + e2.x + " " + e2.y + " )";
        } }]);
      })(), Re = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getIndexAlongSegment", value: function(t3, e2) {
          return this.computeIntLineIndex(), this._intLineIndex[t3][e2];
        } }, { key: "getTopologySummary", value: function() {
          var t3 = new Jt();
          return this.isEndPoint() && t3.append(" endpoint"), this._isProper && t3.append(" proper"), this.isCollinear() && t3.append(" collinear"), t3.toString();
        } }, { key: "computeIntersection", value: function(t3, e2, n2, i2) {
          this._inputLines[0][0] = t3, this._inputLines[0][1] = e2, this._inputLines[1][0] = n2, this._inputLines[1][1] = i2, this._result = this.computeIntersect(t3, e2, n2, i2);
        } }, { key: "getIntersectionNum", value: function() {
          return this._result;
        } }, { key: "computeIntLineIndex", value: function() {
          if (0 === arguments.length) null === this._intLineIndex && (this._intLineIndex = Array(2).fill().map((function() {
            return Array(2);
          })), this.computeIntLineIndex(0), this.computeIntLineIndex(1));
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            this.getEdgeDistance(t3, 0) > this.getEdgeDistance(t3, 1) ? (this._intLineIndex[t3][0] = 0, this._intLineIndex[t3][1] = 1) : (this._intLineIndex[t3][0] = 1, this._intLineIndex[t3][1] = 0);
          }
        } }, { key: "isProper", value: function() {
          return this.hasIntersection() && this._isProper;
        } }, { key: "setPrecisionModel", value: function(t3) {
          this._precisionModel = t3;
        } }, { key: "isInteriorIntersection", value: function() {
          if (0 === arguments.length) return !!this.isInteriorIntersection(0) || !!this.isInteriorIntersection(1);
          if (1 === arguments.length) {
            for (var t3 = arguments[0], e2 = 0; e2 < this._result; e2++) if (!this._intPt[e2].equals2D(this._inputLines[t3][0]) && !this._intPt[e2].equals2D(this._inputLines[t3][1])) return true;
            return false;
          }
        } }, { key: "getIntersection", value: function(t3) {
          return this._intPt[t3];
        } }, { key: "isEndPoint", value: function() {
          return this.hasIntersection() && !this._isProper;
        } }, { key: "hasIntersection", value: function() {
          return this._result !== t2.NO_INTERSECTION;
        } }, { key: "getEdgeDistance", value: function(e2, n2) {
          return t2.computeEdgeDistance(this._intPt[n2], this._inputLines[e2][0], this._inputLines[e2][1]);
        } }, { key: "isCollinear", value: function() {
          return this._result === t2.COLLINEAR_INTERSECTION;
        } }, { key: "toString", value: function() {
          return Ce.toLineString(this._inputLines[0][0], this._inputLines[0][1]) + " - " + Ce.toLineString(this._inputLines[1][0], this._inputLines[1][1]) + this.getTopologySummary();
        } }, { key: "getEndpoint", value: function(t3, e2) {
          return this._inputLines[t3][e2];
        } }, { key: "isIntersection", value: function(t3) {
          for (var e2 = 0; e2 < this._result; e2++) if (this._intPt[e2].equals2D(t3)) return true;
          return false;
        } }, { key: "getIntersectionAlongSegment", value: function(t3, e2) {
          return this.computeIntLineIndex(), this._intPt[this._intLineIndex[t3][e2]];
        } }], [{ key: "constructor_", value: function() {
          this._result = null, this._inputLines = Array(2).fill().map((function() {
            return Array(2);
          })), this._intPt = new Array(2).fill(null), this._intLineIndex = null, this._isProper = null, this._pa = null, this._pb = null, this._precisionModel = null, this._intPt[0] = new X(), this._intPt[1] = new X(), this._pa = this._intPt[0], this._pb = this._intPt[1], this._result = 0;
        } }, { key: "computeEdgeDistance", value: function(t3, e2, n2) {
          var i2 = Math.abs(n2.x - e2.x), r3 = Math.abs(n2.y - e2.y), s2 = -1;
          if (t3.equals(e2)) s2 = 0;
          else if (t3.equals(n2)) s2 = i2 > r3 ? i2 : r3;
          else {
            var a2 = Math.abs(t3.x - e2.x), o2 = Math.abs(t3.y - e2.y);
            0 !== (s2 = i2 > r3 ? a2 : o2) || t3.equals(e2) || (s2 = Math.max(a2, o2));
          }
          return G.isTrue(!(0 === s2 && !t3.equals(e2)), "Bad distance calculation"), s2;
        } }, { key: "nonRobustComputeEdgeDistance", value: function(t3, e2, n2) {
          var i2 = t3.x - e2.x, r3 = t3.y - e2.y, s2 = Math.sqrt(i2 * i2 + r3 * r3);
          return G.isTrue(!(0 === s2 && !t3.equals(e2)), "Invalid distance calculation"), s2;
        } }]);
      })();
      Re.DONT_INTERSECT = 0, Re.DO_INTERSECT = 1, Re.COLLINEAR = 2, Re.NO_INTERSECTION = 0, Re.POINT_INTERSECTION = 1, Re.COLLINEAR_INTERSECTION = 2;
      var we = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2);
        }
        return l(i2, t2), s(i2, [{ key: "isInSegmentEnvelopes", value: function(t3) {
          var e2 = new U(this._inputLines[0][0], this._inputLines[0][1]), n2 = new U(this._inputLines[1][0], this._inputLines[1][1]);
          return e2.contains(t3) && n2.contains(t3);
        } }, { key: "computeIntersection", value: function() {
          if (3 !== arguments.length) return f(i2, "computeIntersection", this, 1).apply(this, arguments);
          var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          if (this._isProper = false, U.intersects(e2, n2, t3) && 0 === ct.index(e2, n2, t3) && 0 === ct.index(n2, e2, t3)) return this._isProper = true, (t3.equals(e2) || t3.equals(n2)) && (this._isProper = false), this._result = Re.POINT_INTERSECTION, null;
          this._result = Re.NO_INTERSECTION;
        } }, { key: "intersection", value: function(t3, e2, n2, r3) {
          var s2 = this.intersectionSafe(t3, e2, n2, r3);
          return this.isInSegmentEnvelopes(s2) || (s2 = new X(i2.nearestEndpoint(t3, e2, n2, r3))), null !== this._precisionModel && this._precisionModel.makePrecise(s2), s2;
        } }, { key: "checkDD", value: function(t3, e2, n2, i3, r3) {
          var s2 = lt.intersection(t3, e2, n2, i3), a2 = this.isInSegmentEnvelopes(s2);
          mt.out.println("DD in env = " + a2 + "  --------------------- " + s2), r3.distance(s2) > 1e-4 && mt.out.println("Distance = " + r3.distance(s2));
        } }, { key: "intersectionSafe", value: function(t3, e2, n2, r3) {
          var s2 = pt.intersection(t3, e2, n2, r3);
          return null === s2 && (s2 = i2.nearestEndpoint(t3, e2, n2, r3)), s2;
        } }, { key: "computeCollinearIntersection", value: function(t3, e2, n2, i3) {
          var r3 = U.intersects(t3, e2, n2), s2 = U.intersects(t3, e2, i3), a2 = U.intersects(n2, i3, t3), o2 = U.intersects(n2, i3, e2);
          return r3 && s2 ? (this._intPt[0] = n2, this._intPt[1] = i3, Re.COLLINEAR_INTERSECTION) : a2 && o2 ? (this._intPt[0] = t3, this._intPt[1] = e2, Re.COLLINEAR_INTERSECTION) : r3 && a2 ? (this._intPt[0] = n2, this._intPt[1] = t3, !n2.equals(t3) || s2 || o2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : r3 && o2 ? (this._intPt[0] = n2, this._intPt[1] = e2, !n2.equals(e2) || s2 || a2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : s2 && a2 ? (this._intPt[0] = i3, this._intPt[1] = t3, !i3.equals(t3) || r3 || o2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : s2 && o2 ? (this._intPt[0] = i3, this._intPt[1] = e2, !i3.equals(e2) || r3 || a2 ? Re.COLLINEAR_INTERSECTION : Re.POINT_INTERSECTION) : Re.NO_INTERSECTION;
        } }, { key: "computeIntersect", value: function(t3, e2, n2, i3) {
          if (this._isProper = false, !U.intersects(t3, e2, n2, i3)) return Re.NO_INTERSECTION;
          var r3 = ct.index(t3, e2, n2), s2 = ct.index(t3, e2, i3);
          if (r3 > 0 && s2 > 0 || r3 < 0 && s2 < 0) return Re.NO_INTERSECTION;
          var a2 = ct.index(n2, i3, t3), o2 = ct.index(n2, i3, e2);
          return a2 > 0 && o2 > 0 || a2 < 0 && o2 < 0 ? Re.NO_INTERSECTION : 0 === r3 && 0 === s2 && 0 === a2 && 0 === o2 ? this.computeCollinearIntersection(t3, e2, n2, i3) : (0 === r3 || 0 === s2 || 0 === a2 || 0 === o2 ? (this._isProper = false, t3.equals2D(n2) || t3.equals2D(i3) ? this._intPt[0] = t3 : e2.equals2D(n2) || e2.equals2D(i3) ? this._intPt[0] = e2 : 0 === r3 ? this._intPt[0] = new X(n2) : 0 === s2 ? this._intPt[0] = new X(i3) : 0 === a2 ? this._intPt[0] = new X(t3) : 0 === o2 && (this._intPt[0] = new X(e2))) : (this._isProper = true, this._intPt[0] = this.intersection(t3, e2, n2, i3)), Re.POINT_INTERSECTION);
        } }], [{ key: "nearestEndpoint", value: function(t3, e2, n2, i3) {
          var r3 = t3, s2 = xt.pointToSegment(t3, n2, i3), a2 = xt.pointToSegment(e2, n2, i3);
          return a2 < s2 && (s2 = a2, r3 = e2), (a2 = xt.pointToSegment(n2, t3, e2)) < s2 && (s2 = a2, r3 = n2), (a2 = xt.pointToSegment(i3, t3, e2)) < s2 && (s2 = a2, r3 = i3), r3;
        } }]);
      })(Re), Oe = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "countSegment", value: function(t3, e2) {
          if (t3.x < this._p.x && e2.x < this._p.x) return null;
          if (this._p.x === e2.x && this._p.y === e2.y) return this._isPointOnSegment = true, null;
          if (t3.y === this._p.y && e2.y === this._p.y) {
            var n2 = t3.x, i2 = e2.x;
            return n2 > i2 && (n2 = e2.x, i2 = t3.x), this._p.x >= n2 && this._p.x <= i2 && (this._isPointOnSegment = true), null;
          }
          if (t3.y > this._p.y && e2.y <= this._p.y || e2.y > this._p.y && t3.y <= this._p.y) {
            var r3 = ct.index(t3, e2, this._p);
            if (r3 === ct.COLLINEAR) return this._isPointOnSegment = true, null;
            e2.y < t3.y && (r3 = -r3), r3 === ct.LEFT && this._crossingCount++;
          }
        } }, { key: "isPointInPolygon", value: function() {
          return this.getLocation() !== H.EXTERIOR;
        } }, { key: "getLocation", value: function() {
          return this._isPointOnSegment ? H.BOUNDARY : this._crossingCount % 2 == 1 ? H.INTERIOR : H.EXTERIOR;
        } }, { key: "isOnSegment", value: function() {
          return this._isPointOnSegment;
        } }], [{ key: "constructor_", value: function() {
          this._p = null, this._crossingCount = 0, this._isPointOnSegment = false;
          var t3 = arguments[0];
          this._p = t3;
        } }, { key: "locatePointInRing", value: function() {
          if (arguments[0] instanceof X && rt(arguments[1], ht)) {
            for (var e2 = arguments[1], n2 = new t2(arguments[0]), i2 = new X(), r3 = new X(), s2 = 1; s2 < e2.size(); s2++) if (e2.getCoordinate(s2, i2), e2.getCoordinate(s2 - 1, r3), n2.countSegment(i2, r3), n2.isOnSegment()) return n2.getLocation();
            return n2.getLocation();
          }
          if (arguments[0] instanceof X && arguments[1] instanceof Array) {
            for (var a2 = arguments[1], o2 = new t2(arguments[0]), u3 = 1; u3 < a2.length; u3++) {
              var l2 = a2[u3], h2 = a2[u3 - 1];
              if (o2.countSegment(l2, h2), o2.isOnSegment()) return o2.getLocation();
            }
            return o2.getLocation();
          }
        } }]);
      })(), be = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "isOnLine", value: function() {
          if (arguments[0] instanceof X && rt(arguments[1], ht)) {
            for (var t3 = arguments[0], e2 = arguments[1], n2 = new we(), i2 = new X(), r3 = new X(), s2 = e2.size(), a2 = 1; a2 < s2; a2++) if (e2.getCoordinate(a2 - 1, i2), e2.getCoordinate(a2, r3), n2.computeIntersection(t3, i2, r3), n2.hasIntersection()) return true;
            return false;
          }
          if (arguments[0] instanceof X && arguments[1] instanceof Array) {
            for (var o2 = arguments[0], u3 = arguments[1], l2 = new we(), h2 = 1; h2 < u3.length; h2++) {
              var c2 = u3[h2 - 1], f2 = u3[h2];
              if (l2.computeIntersection(o2, c2, f2), l2.hasIntersection()) return true;
            }
            return false;
          }
        } }, { key: "locateInRing", value: function(t3, e2) {
          return Oe.locatePointInRing(t3, e2);
        } }, { key: "isInRing", value: function(e2, n2) {
          return t2.locateInRing(e2, n2) !== H.EXTERIOR;
        } }]);
      })(), Me = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "setAllLocations", value: function(t3) {
          for (var e2 = 0; e2 < this.location.length; e2++) this.location[e2] = t3;
        } }, { key: "isNull", value: function() {
          for (var t3 = 0; t3 < this.location.length; t3++) if (this.location[t3] !== H.NONE) return false;
          return true;
        } }, { key: "setAllLocationsIfNull", value: function(t3) {
          for (var e2 = 0; e2 < this.location.length; e2++) this.location[e2] === H.NONE && (this.location[e2] = t3);
        } }, { key: "isLine", value: function() {
          return 1 === this.location.length;
        } }, { key: "merge", value: function(t3) {
          if (t3.location.length > this.location.length) {
            var e2 = new Array(3).fill(null);
            e2[$.ON] = this.location[$.ON], e2[$.LEFT] = H.NONE, e2[$.RIGHT] = H.NONE, this.location = e2;
          }
          for (var n2 = 0; n2 < this.location.length; n2++) this.location[n2] === H.NONE && n2 < t3.location.length && (this.location[n2] = t3.location[n2]);
        } }, { key: "getLocations", value: function() {
          return this.location;
        } }, { key: "flip", value: function() {
          if (this.location.length <= 1) return null;
          var t3 = this.location[$.LEFT];
          this.location[$.LEFT] = this.location[$.RIGHT], this.location[$.RIGHT] = t3;
        } }, { key: "toString", value: function() {
          var t3 = new st();
          return this.location.length > 1 && t3.append(H.toLocationSymbol(this.location[$.LEFT])), t3.append(H.toLocationSymbol(this.location[$.ON])), this.location.length > 1 && t3.append(H.toLocationSymbol(this.location[$.RIGHT])), t3.toString();
        } }, { key: "setLocations", value: function(t3, e2, n2) {
          this.location[$.ON] = t3, this.location[$.LEFT] = e2, this.location[$.RIGHT] = n2;
        } }, { key: "get", value: function(t3) {
          return t3 < this.location.length ? this.location[t3] : H.NONE;
        } }, { key: "isArea", value: function() {
          return this.location.length > 1;
        } }, { key: "isAnyNull", value: function() {
          for (var t3 = 0; t3 < this.location.length; t3++) if (this.location[t3] === H.NONE) return true;
          return false;
        } }, { key: "setLocation", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.setLocation($.ON, t3);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            this.location[e2] = n2;
          }
        } }, { key: "init", value: function(t3) {
          this.location = new Array(t3).fill(null), this.setAllLocations(H.NONE);
        } }, { key: "isEqualOnSide", value: function(t3, e2) {
          return this.location[e2] === t3.location[e2];
        } }, { key: "allPositionsEqual", value: function(t3) {
          for (var e2 = 0; e2 < this.location.length; e2++) if (this.location[e2] !== t3) return false;
          return true;
        } }], [{ key: "constructor_", value: function() {
          if (this.location = null, 1 === arguments.length) {
            if (arguments[0] instanceof Array) {
              var e2 = arguments[0];
              this.init(e2.length);
            } else if (Number.isInteger(arguments[0])) {
              var n2 = arguments[0];
              this.init(1), this.location[$.ON] = n2;
            } else if (arguments[0] instanceof t2) {
              var i2 = arguments[0];
              if (this.init(i2.location.length), null !== i2) for (var r3 = 0; r3 < this.location.length; r3++) this.location[r3] = i2.location[r3];
            }
          } else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2];
            this.init(3), this.location[$.ON] = s2, this.location[$.LEFT] = a2, this.location[$.RIGHT] = o2;
          }
        } }]);
      })(), Ae = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getGeometryCount", value: function() {
          var t3 = 0;
          return this.elt[0].isNull() || t3++, this.elt[1].isNull() || t3++, t3;
        } }, { key: "setAllLocations", value: function(t3, e2) {
          this.elt[t3].setAllLocations(e2);
        } }, { key: "isNull", value: function(t3) {
          return this.elt[t3].isNull();
        } }, { key: "setAllLocationsIfNull", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.setAllLocationsIfNull(0, t3), this.setAllLocationsIfNull(1, t3);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            this.elt[e2].setAllLocationsIfNull(n2);
          }
        } }, { key: "isLine", value: function(t3) {
          return this.elt[t3].isLine();
        } }, { key: "merge", value: function(t3) {
          for (var e2 = 0; e2 < 2; e2++) null === this.elt[e2] && null !== t3.elt[e2] ? this.elt[e2] = new Me(t3.elt[e2]) : this.elt[e2].merge(t3.elt[e2]);
        } }, { key: "flip", value: function() {
          this.elt[0].flip(), this.elt[1].flip();
        } }, { key: "getLocation", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.elt[t3].get($.ON);
          }
          if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            return this.elt[e2].get(n2);
          }
        } }, { key: "toString", value: function() {
          var t3 = new st();
          return null !== this.elt[0] && (t3.append("A:"), t3.append(this.elt[0].toString())), null !== this.elt[1] && (t3.append(" B:"), t3.append(this.elt[1].toString())), t3.toString();
        } }, { key: "isArea", value: function() {
          if (0 === arguments.length) return this.elt[0].isArea() || this.elt[1].isArea();
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.elt[t3].isArea();
          }
        } }, { key: "isAnyNull", value: function(t3) {
          return this.elt[t3].isAnyNull();
        } }, { key: "setLocation", value: function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            this.elt[t3].setLocation($.ON, e2);
          } else if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r3 = arguments[2];
            this.elt[n2].setLocation(i2, r3);
          }
        } }, { key: "isEqualOnSide", value: function(t3, e2) {
          return this.elt[0].isEqualOnSide(t3.elt[0], e2) && this.elt[1].isEqualOnSide(t3.elt[1], e2);
        } }, { key: "allPositionsEqual", value: function(t3, e2) {
          return this.elt[t3].allPositionsEqual(e2);
        } }, { key: "toLine", value: function(t3) {
          this.elt[t3].isArea() && (this.elt[t3] = new Me(this.elt[t3].location[0]));
        } }], [{ key: "constructor_", value: function() {
          if (this.elt = new Array(2).fill(null), 1 === arguments.length) {
            if (Number.isInteger(arguments[0])) {
              var e2 = arguments[0];
              this.elt[0] = new Me(e2), this.elt[1] = new Me(e2);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              this.elt[0] = new Me(n2.elt[0]), this.elt[1] = new Me(n2.elt[1]);
            }
          } else if (2 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            this.elt[0] = new Me(H.NONE), this.elt[1] = new Me(H.NONE), this.elt[i2].setLocation(r3);
          } else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2];
            this.elt[0] = new Me(s2, a2, o2), this.elt[1] = new Me(s2, a2, o2);
          } else if (4 === arguments.length) {
            var u3 = arguments[0], l2 = arguments[1], h2 = arguments[2], c2 = arguments[3];
            this.elt[0] = new Me(H.NONE, H.NONE, H.NONE), this.elt[1] = new Me(H.NONE, H.NONE, H.NONE), this.elt[u3].setLocations(l2, h2, c2);
          }
        } }, { key: "toLineLabel", value: function(e2) {
          for (var n2 = new t2(H.NONE), i2 = 0; i2 < 2; i2++) n2.setLocation(i2, e2.getLocation(i2));
          return n2;
        } }]);
      })(), Pe = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "computeRing", value: function() {
          if (null !== this._ring) return null;
          for (var t2 = new Array(this._pts.size()).fill(null), e2 = 0; e2 < this._pts.size(); e2++) t2[e2] = this._pts.get(e2);
          this._ring = this._geometryFactory.createLinearRing(t2), this._isHole = ct.isCCW(this._ring.getCoordinates());
        } }, { key: "isIsolated", value: function() {
          return 1 === this._label.getGeometryCount();
        } }, { key: "computePoints", value: function(t2) {
          this._startDe = t2;
          var e2 = t2, n2 = true;
          do {
            if (null === e2) throw new gt("Found null DirectedEdge");
            if (e2.getEdgeRing() === this) throw new gt("Directed Edge visited twice during ring-building at " + e2.getCoordinate());
            this._edges.add(e2);
            var i2 = e2.getLabel();
            G.isTrue(i2.isArea()), this.mergeLabel(i2), this.addPoints(e2.getEdge(), e2.isForward(), n2), n2 = false, this.setEdgeRing(e2, this), e2 = this.getNext(e2);
          } while (e2 !== this._startDe);
        } }, { key: "getLinearRing", value: function() {
          return this._ring;
        } }, { key: "getCoordinate", value: function(t2) {
          return this._pts.get(t2);
        } }, { key: "computeMaxNodeDegree", value: function() {
          this._maxNodeDegree = 0;
          var t2 = this._startDe;
          do {
            var e2 = t2.getNode().getEdges().getOutgoingDegree(this);
            e2 > this._maxNodeDegree && (this._maxNodeDegree = e2), t2 = this.getNext(t2);
          } while (t2 !== this._startDe);
          this._maxNodeDegree *= 2;
        } }, { key: "addPoints", value: function(t2, e2, n2) {
          var i2 = t2.getCoordinates();
          if (e2) {
            var r3 = 1;
            n2 && (r3 = 0);
            for (var s2 = r3; s2 < i2.length; s2++) this._pts.add(i2[s2]);
          } else {
            var a2 = i2.length - 2;
            n2 && (a2 = i2.length - 1);
            for (var o2 = a2; o2 >= 0; o2--) this._pts.add(i2[o2]);
          }
        } }, { key: "isHole", value: function() {
          return this._isHole;
        } }, { key: "setInResult", value: function() {
          var t2 = this._startDe;
          do {
            t2.getEdge().setInResult(true), t2 = t2.getNext();
          } while (t2 !== this._startDe);
        } }, { key: "containsPoint", value: function(t2) {
          var e2 = this.getLinearRing();
          if (!e2.getEnvelopeInternal().contains(t2)) return false;
          if (!be.isInRing(t2, e2.getCoordinates())) return false;
          for (var n2 = this._holes.iterator(); n2.hasNext(); ) {
            if (n2.next().containsPoint(t2)) return false;
          }
          return true;
        } }, { key: "addHole", value: function(t2) {
          this._holes.add(t2);
        } }, { key: "isShell", value: function() {
          return null === this._shell;
        } }, { key: "getLabel", value: function() {
          return this._label;
        } }, { key: "getEdges", value: function() {
          return this._edges;
        } }, { key: "getMaxNodeDegree", value: function() {
          return this._maxNodeDegree < 0 && this.computeMaxNodeDegree(), this._maxNodeDegree;
        } }, { key: "getShell", value: function() {
          return this._shell;
        } }, { key: "mergeLabel", value: function() {
          if (1 === arguments.length) {
            var t2 = arguments[0];
            this.mergeLabel(t2, 0), this.mergeLabel(t2, 1);
          } else if (2 === arguments.length) {
            var e2 = arguments[1], n2 = arguments[0].getLocation(e2, $.RIGHT);
            if (n2 === H.NONE) return null;
            if (this._label.getLocation(e2) === H.NONE) return this._label.setLocation(e2, n2), null;
          }
        } }, { key: "setShell", value: function(t2) {
          this._shell = t2, null !== t2 && t2.addHole(this);
        } }, { key: "toPolygon", value: function(t2) {
          for (var e2 = new Array(this._holes.size()).fill(null), n2 = 0; n2 < this._holes.size(); n2++) e2[n2] = this._holes.get(n2).getLinearRing();
          return t2.createPolygon(this.getLinearRing(), e2);
        } }], [{ key: "constructor_", value: function() {
          if (this._startDe = null, this._maxNodeDegree = -1, this._edges = new yt(), this._pts = new yt(), this._label = new Ae(H.NONE), this._ring = null, this._isHole = null, this._shell = null, this._holes = new yt(), this._geometryFactory = null, 0 === arguments.length) ;
          else if (2 === arguments.length) {
            var t2 = arguments[0], e2 = arguments[1];
            this._geometryFactory = e2, this.computePoints(t2), this.computeRing();
          }
        } }]);
      })(), De = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "setEdgeRing", value: function(t3, e2) {
          t3.setMinEdgeRing(e2);
        } }, { key: "getNext", value: function(t3) {
          return t3.getNextMin();
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0], e2 = arguments[1];
          Pe.constructor_.call(this, t3, e2);
        } }]);
      })(Pe), Fe = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "buildMinimalRings", value: function() {
          var t3 = new yt(), e2 = this._startDe;
          do {
            if (null === e2.getMinEdgeRing()) {
              var n2 = new De(e2, this._geometryFactory);
              t3.add(n2);
            }
            e2 = e2.getNext();
          } while (e2 !== this._startDe);
          return t3;
        } }, { key: "setEdgeRing", value: function(t3, e2) {
          t3.setEdgeRing(e2);
        } }, { key: "linkDirectedEdgesForMinimalEdgeRings", value: function() {
          var t3 = this._startDe;
          do {
            t3.getNode().getEdges().linkMinimalDirectedEdges(this), t3 = t3.getNext();
          } while (t3 !== this._startDe);
        } }, { key: "getNext", value: function(t3) {
          return t3.getNext();
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0], e2 = arguments[1];
          Pe.constructor_.call(this, t3, e2);
        } }]);
      })(Pe), Ge = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "setVisited", value: function(t2) {
          this._isVisited = t2;
        } }, { key: "setInResult", value: function(t2) {
          this._isInResult = t2;
        } }, { key: "isCovered", value: function() {
          return this._isCovered;
        } }, { key: "isCoveredSet", value: function() {
          return this._isCoveredSet;
        } }, { key: "setLabel", value: function(t2) {
          this._label = t2;
        } }, { key: "getLabel", value: function() {
          return this._label;
        } }, { key: "setCovered", value: function(t2) {
          this._isCovered = t2, this._isCoveredSet = true;
        } }, { key: "updateIM", value: function(t2) {
          G.isTrue(this._label.getGeometryCount() >= 2, "found partial label"), this.computeIM(t2);
        } }, { key: "isInResult", value: function() {
          return this._isInResult;
        } }, { key: "isVisited", value: function() {
          return this._isVisited;
        } }], [{ key: "constructor_", value: function() {
          if (this._label = null, this._isInResult = false, this._isCovered = false, this._isCoveredSet = false, this._isVisited = false, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this._label = t2;
          }
        } }]);
      })(), qe = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "isIncidentEdgeInResult", value: function() {
          for (var t3 = this.getEdges().getEdges().iterator(); t3.hasNext(); ) {
            if (t3.next().getEdge().isInResult()) return true;
          }
          return false;
        } }, { key: "isIsolated", value: function() {
          return 1 === this._label.getGeometryCount();
        } }, { key: "getCoordinate", value: function() {
          return this._coord;
        } }, { key: "print", value: function(t3) {
          t3.println("node " + this._coord + " lbl: " + this._label);
        } }, { key: "computeIM", value: function(t3) {
        } }, { key: "computeMergedLocation", value: function(t3, e2) {
          var n2 = H.NONE;
          if (n2 = this._label.getLocation(e2), !t3.isNull(e2)) {
            var i3 = t3.getLocation(e2);
            n2 !== H.BOUNDARY && (n2 = i3);
          }
          return n2;
        } }, { key: "setLabel", value: function() {
          if (2 !== arguments.length || !Number.isInteger(arguments[1]) || !Number.isInteger(arguments[0])) return f(i2, "setLabel", this, 1).apply(this, arguments);
          var t3 = arguments[0], e2 = arguments[1];
          null === this._label ? this._label = new Ae(t3, e2) : this._label.setLocation(t3, e2);
        } }, { key: "getEdges", value: function() {
          return this._edges;
        } }, { key: "mergeLabel", value: function() {
          if (arguments[0] instanceof i2) {
            var t3 = arguments[0];
            this.mergeLabel(t3._label);
          } else if (arguments[0] instanceof Ae) for (var e2 = arguments[0], n2 = 0; n2 < 2; n2++) {
            var r3 = this.computeMergedLocation(e2, n2);
            this._label.getLocation(n2) === H.NONE && this._label.setLocation(n2, r3);
          }
        } }, { key: "add", value: function(t3) {
          this._edges.insert(t3), t3.setNode(this);
        } }, { key: "setLabelBoundary", value: function(t3) {
          if (null === this._label) return null;
          var e2 = H.NONE;
          null !== this._label && (e2 = this._label.getLocation(t3));
          var n2 = null;
          switch (e2) {
            case H.BOUNDARY:
              n2 = H.INTERIOR;
              break;
            case H.INTERIOR:
            default:
              n2 = H.BOUNDARY;
          }
          this._label.setLocation(t3, n2);
        } }], [{ key: "constructor_", value: function() {
          this._coord = null, this._edges = null;
          var t3 = arguments[0], e2 = arguments[1];
          this._coord = t3, this._edges = e2, this._label = new Ae(0, H.NONE);
        } }]);
      })(Ge), Ye = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2, arguments);
        }
        return l(i2, t2), s(i2);
      })(ee);
      function ze(t2) {
        return null == t2 ? 0 : t2.color;
      }
      function Xe(t2) {
        return null == t2 ? null : t2.parent;
      }
      function Be(t2, e2) {
        null !== t2 && (t2.color = e2);
      }
      function Ue(t2) {
        return null == t2 ? null : t2.left;
      }
      function Ve(t2) {
        return null == t2 ? null : t2.right;
      }
      var He = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), (t3 = e(this, i2)).root_ = null, t3.size_ = 0, t3;
        }
        return l(i2, t2), s(i2, [{ key: "get", value: function(t3) {
          for (var e2 = this.root_; null !== e2; ) {
            var n2 = t3.compareTo(e2.key);
            if (n2 < 0) e2 = e2.left;
            else {
              if (!(n2 > 0)) return e2.value;
              e2 = e2.right;
            }
          }
          return null;
        } }, { key: "put", value: function(t3, e2) {
          if (null === this.root_) return this.root_ = { key: t3, value: e2, left: null, right: null, parent: null, color: 0, getValue: function() {
            return this.value;
          }, getKey: function() {
            return this.key;
          } }, this.size_ = 1, null;
          var n2, i3, r3 = this.root_;
          do {
            if (n2 = r3, (i3 = t3.compareTo(r3.key)) < 0) r3 = r3.left;
            else {
              if (!(i3 > 0)) {
                var s2 = r3.value;
                return r3.value = e2, s2;
              }
              r3 = r3.right;
            }
          } while (null !== r3);
          var a2 = { key: t3, left: null, right: null, value: e2, parent: n2, color: 0, getValue: function() {
            return this.value;
          }, getKey: function() {
            return this.key;
          } };
          return i3 < 0 ? n2.left = a2 : n2.right = a2, this.fixAfterInsertion(a2), this.size_++, null;
        } }, { key: "fixAfterInsertion", value: function(t3) {
          var e2;
          for (t3.color = 1; null != t3 && t3 !== this.root_ && 1 === t3.parent.color; ) Xe(t3) === Ue(Xe(Xe(t3))) ? 1 === ze(e2 = Ve(Xe(Xe(t3)))) ? (Be(Xe(t3), 0), Be(e2, 0), Be(Xe(Xe(t3)), 1), t3 = Xe(Xe(t3))) : (t3 === Ve(Xe(t3)) && (t3 = Xe(t3), this.rotateLeft(t3)), Be(Xe(t3), 0), Be(Xe(Xe(t3)), 1), this.rotateRight(Xe(Xe(t3)))) : 1 === ze(e2 = Ue(Xe(Xe(t3)))) ? (Be(Xe(t3), 0), Be(e2, 0), Be(Xe(Xe(t3)), 1), t3 = Xe(Xe(t3))) : (t3 === Ue(Xe(t3)) && (t3 = Xe(t3), this.rotateRight(t3)), Be(Xe(t3), 0), Be(Xe(Xe(t3)), 1), this.rotateLeft(Xe(Xe(t3))));
          this.root_.color = 0;
        } }, { key: "values", value: function() {
          var t3 = new yt(), e2 = this.getFirstEntry();
          if (null !== e2) for (t3.add(e2.value); null !== (e2 = i2.successor(e2)); ) t3.add(e2.value);
          return t3;
        } }, { key: "entrySet", value: function() {
          var t3 = new J(), e2 = this.getFirstEntry();
          if (null !== e2) for (t3.add(e2); null !== (e2 = i2.successor(e2)); ) t3.add(e2);
          return t3;
        } }, { key: "rotateLeft", value: function(t3) {
          if (null != t3) {
            var e2 = t3.right;
            t3.right = e2.left, null != e2.left && (e2.left.parent = t3), e2.parent = t3.parent, null == t3.parent ? this.root_ = e2 : t3.parent.left === t3 ? t3.parent.left = e2 : t3.parent.right = e2, e2.left = t3, t3.parent = e2;
          }
        } }, { key: "rotateRight", value: function(t3) {
          if (null != t3) {
            var e2 = t3.left;
            t3.left = e2.right, null != e2.right && (e2.right.parent = t3), e2.parent = t3.parent, null == t3.parent ? this.root_ = e2 : t3.parent.right === t3 ? t3.parent.right = e2 : t3.parent.left = e2, e2.right = t3, t3.parent = e2;
          }
        } }, { key: "getFirstEntry", value: function() {
          var t3 = this.root_;
          if (null != t3) for (; null != t3.left; ) t3 = t3.left;
          return t3;
        } }, { key: "size", value: function() {
          return this.size_;
        } }, { key: "containsKey", value: function(t3) {
          for (var e2 = this.root_; null !== e2; ) {
            var n2 = t3.compareTo(e2.key);
            if (n2 < 0) e2 = e2.left;
            else {
              if (!(n2 > 0)) return true;
              e2 = e2.right;
            }
          }
          return false;
        } }], [{ key: "successor", value: function(t3) {
          var e2;
          if (null === t3) return null;
          if (null !== t3.right) {
            for (e2 = t3.right; null !== e2.left; ) e2 = e2.left;
            return e2;
          }
          e2 = t3.parent;
          for (var n2 = t3; null !== e2 && n2 === e2.right; ) n2 = e2, e2 = e2.parent;
          return e2;
        } }]);
      })(Ye), Ze = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "find", value: function(t2) {
          return this.nodeMap.get(t2);
        } }, { key: "addNode", value: function() {
          if (arguments[0] instanceof X) {
            var t2 = arguments[0], e2 = this.nodeMap.get(t2);
            return null === e2 && (e2 = this.nodeFact.createNode(t2), this.nodeMap.put(t2, e2)), e2;
          }
          if (arguments[0] instanceof qe) {
            var n2 = arguments[0], i2 = this.nodeMap.get(n2.getCoordinate());
            return null === i2 ? (this.nodeMap.put(n2.getCoordinate(), n2), n2) : (i2.mergeLabel(n2), i2);
          }
        } }, { key: "print", value: function(t2) {
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            e2.next().print(t2);
          }
        } }, { key: "iterator", value: function() {
          return this.nodeMap.values().iterator();
        } }, { key: "values", value: function() {
          return this.nodeMap.values();
        } }, { key: "getBoundaryNodes", value: function(t2) {
          for (var e2 = new yt(), n2 = this.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            i2.getLabel().getLocation(t2) === H.BOUNDARY && e2.add(i2);
          }
          return e2;
        } }, { key: "add", value: function(t2) {
          var e2 = t2.getCoordinate();
          this.addNode(e2).add(t2);
        } }], [{ key: "constructor_", value: function() {
          this.nodeMap = new He(), this.nodeFact = null;
          var t2 = arguments[0];
          this.nodeFact = t2;
        } }]);
      })(), je = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "isNorthern", value: function(e2) {
          return e2 === t2.NE || e2 === t2.NW;
        } }, { key: "isOpposite", value: function(t3, e2) {
          return t3 !== e2 && 2 === (t3 - e2 + 4) % 4;
        } }, { key: "commonHalfPlane", value: function(t3, e2) {
          if (t3 === e2) return t3;
          if (2 === (t3 - e2 + 4) % 4) return -1;
          var n2 = t3 < e2 ? t3 : e2;
          return 0 === n2 && 3 === (t3 > e2 ? t3 : e2) ? 3 : n2;
        } }, { key: "isInHalfPlane", value: function(e2, n2) {
          return n2 === t2.SE ? e2 === t2.SE || e2 === t2.SW : e2 === n2 || e2 === n2 + 1;
        } }, { key: "quadrant", value: function() {
          if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var e2 = arguments[0], n2 = arguments[1];
            if (0 === e2 && 0 === n2) throw new m("Cannot compute the quadrant for point ( " + e2 + ", " + n2 + " )");
            return e2 >= 0 ? n2 >= 0 ? t2.NE : t2.SE : n2 >= 0 ? t2.NW : t2.SW;
          }
          if (arguments[0] instanceof X && arguments[1] instanceof X) {
            var i2 = arguments[0], r3 = arguments[1];
            if (r3.x === i2.x && r3.y === i2.y) throw new m("Cannot compute the quadrant for two identical points " + i2);
            return r3.x >= i2.x ? r3.y >= i2.y ? t2.NE : t2.SE : r3.y >= i2.y ? t2.NW : t2.SW;
          }
        } }]);
      })();
      je.NE = 0, je.NW = 1, je.SW = 2, je.SE = 3;
      var We = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "compareDirection", value: function(t3) {
          return this._dx === t3._dx && this._dy === t3._dy ? 0 : this._quadrant > t3._quadrant ? 1 : this._quadrant < t3._quadrant ? -1 : ct.index(t3._p0, t3._p1, this._p1);
        } }, { key: "getDy", value: function() {
          return this._dy;
        } }, { key: "getCoordinate", value: function() {
          return this._p0;
        } }, { key: "setNode", value: function(t3) {
          this._node = t3;
        } }, { key: "print", value: function(t3) {
          var e2 = Math.atan2(this._dy, this._dx), n2 = this.getClass().getName(), i2 = n2.lastIndexOf("."), r3 = n2.substring(i2 + 1);
          t3.print("  " + r3 + ": " + this._p0 + " - " + this._p1 + " " + this._quadrant + ":" + e2 + "   " + this._label);
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3;
          return this.compareDirection(e2);
        } }, { key: "getDirectedCoordinate", value: function() {
          return this._p1;
        } }, { key: "getDx", value: function() {
          return this._dx;
        } }, { key: "getLabel", value: function() {
          return this._label;
        } }, { key: "getEdge", value: function() {
          return this._edge;
        } }, { key: "getQuadrant", value: function() {
          return this._quadrant;
        } }, { key: "getNode", value: function() {
          return this._node;
        } }, { key: "toString", value: function() {
          var t3 = Math.atan2(this._dy, this._dx), e2 = this.getClass().getName(), n2 = e2.lastIndexOf(".");
          return "  " + e2.substring(n2 + 1) + ": " + this._p0 + " - " + this._p1 + " " + this._quadrant + ":" + t3 + "   " + this._label;
        } }, { key: "computeLabel", value: function(t3) {
        } }, { key: "init", value: function(t3, e2) {
          this._p0 = t3, this._p1 = e2, this._dx = e2.x - t3.x, this._dy = e2.y - t3.y, this._quadrant = je.quadrant(this._dx, this._dy), G.isTrue(!(0 === this._dx && 0 === this._dy), "EdgeEnd with identical endpoints found");
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          if (this._edge = null, this._label = null, this._node = null, this._p0 = null, this._p1 = null, this._dx = null, this._dy = null, this._quadrant = null, 1 === arguments.length) {
            var e2 = arguments[0];
            this._edge = e2;
          } else if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r3 = arguments[2];
            t2.constructor_.call(this, n2, i2, r3, null);
          } else if (4 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u3 = arguments[3];
            t2.constructor_.call(this, s2), this.init(a2, o2), this._label = u3;
          }
        } }]);
      })(), Ke = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "getNextMin", value: function() {
          return this._nextMin;
        } }, { key: "getDepth", value: function(t3) {
          return this._depth[t3];
        } }, { key: "setVisited", value: function(t3) {
          this._isVisited = t3;
        } }, { key: "computeDirectedLabel", value: function() {
          this._label = new Ae(this._edge.getLabel()), this._isForward || this._label.flip();
        } }, { key: "getNext", value: function() {
          return this._next;
        } }, { key: "setDepth", value: function(t3, e2) {
          if (-999 !== this._depth[t3] && this._depth[t3] !== e2) throw new gt("assigned depths do not match", this.getCoordinate());
          this._depth[t3] = e2;
        } }, { key: "isInteriorAreaEdge", value: function() {
          for (var t3 = true, e2 = 0; e2 < 2; e2++) this._label.isArea(e2) && this._label.getLocation(e2, $.LEFT) === H.INTERIOR && this._label.getLocation(e2, $.RIGHT) === H.INTERIOR || (t3 = false);
          return t3;
        } }, { key: "setNextMin", value: function(t3) {
          this._nextMin = t3;
        } }, { key: "print", value: function(t3) {
          f(i2, "print", this, 1).call(this, t3), t3.print(" " + this._depth[$.LEFT] + "/" + this._depth[$.RIGHT]), t3.print(" (" + this.getDepthDelta() + ")"), this._isInResult && t3.print(" inResult");
        } }, { key: "setMinEdgeRing", value: function(t3) {
          this._minEdgeRing = t3;
        } }, { key: "isLineEdge", value: function() {
          var t3 = this._label.isLine(0) || this._label.isLine(1), e2 = !this._label.isArea(0) || this._label.allPositionsEqual(0, H.EXTERIOR), n2 = !this._label.isArea(1) || this._label.allPositionsEqual(1, H.EXTERIOR);
          return t3 && e2 && n2;
        } }, { key: "setEdgeRing", value: function(t3) {
          this._edgeRing = t3;
        } }, { key: "getMinEdgeRing", value: function() {
          return this._minEdgeRing;
        } }, { key: "getDepthDelta", value: function() {
          var t3 = this._edge.getDepthDelta();
          return this._isForward || (t3 = -t3), t3;
        } }, { key: "setInResult", value: function(t3) {
          this._isInResult = t3;
        } }, { key: "getSym", value: function() {
          return this._sym;
        } }, { key: "isForward", value: function() {
          return this._isForward;
        } }, { key: "getEdge", value: function() {
          return this._edge;
        } }, { key: "printEdge", value: function(t3) {
          this.print(t3), t3.print(" "), this._isForward ? this._edge.print(t3) : this._edge.printReverse(t3);
        } }, { key: "setSym", value: function(t3) {
          this._sym = t3;
        } }, { key: "setVisitedEdge", value: function(t3) {
          this.setVisited(t3), this._sym.setVisited(t3);
        } }, { key: "setEdgeDepths", value: function(t3, e2) {
          var n2 = this.getEdge().getDepthDelta();
          this._isForward || (n2 = -n2);
          var i3 = 1;
          t3 === $.LEFT && (i3 = -1);
          var r3 = $.opposite(t3), s2 = e2 + n2 * i3;
          this.setDepth(t3, e2), this.setDepth(r3, s2);
        } }, { key: "getEdgeRing", value: function() {
          return this._edgeRing;
        } }, { key: "isInResult", value: function() {
          return this._isInResult;
        } }, { key: "setNext", value: function(t3) {
          this._next = t3;
        } }, { key: "isVisited", value: function() {
          return this._isVisited;
        } }], [{ key: "constructor_", value: function() {
          this._isForward = null, this._isInResult = false, this._isVisited = false, this._sym = null, this._next = null, this._nextMin = null, this._edgeRing = null, this._minEdgeRing = null, this._depth = [0, -999, -999];
          var t3 = arguments[0], e2 = arguments[1];
          if (We.constructor_.call(this, t3), this._isForward = e2, e2) this.init(t3.getCoordinate(0), t3.getCoordinate(1));
          else {
            var n2 = t3.getNumPoints() - 1;
            this.init(t3.getCoordinate(n2), t3.getCoordinate(n2 - 1));
          }
          this.computeDirectedLabel();
        } }, { key: "depthFactor", value: function(t3, e2) {
          return t3 === H.EXTERIOR && e2 === H.INTERIOR ? 1 : t3 === H.INTERIOR && e2 === H.EXTERIOR ? -1 : 0;
        } }]);
      })(We), Je = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "createNode", value: function(t2) {
          return new qe(t2, null);
        } }]);
      })(), Qe = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "printEdges", value: function(t2) {
          t2.println("Edges:");
          for (var e2 = 0; e2 < this._edges.size(); e2++) {
            t2.println("edge " + e2 + ":");
            var n2 = this._edges.get(e2);
            n2.print(t2), n2.eiList.print(t2);
          }
        } }, { key: "find", value: function(t2) {
          return this._nodes.find(t2);
        } }, { key: "addNode", value: function() {
          if (arguments[0] instanceof qe) {
            var t2 = arguments[0];
            return this._nodes.addNode(t2);
          }
          if (arguments[0] instanceof X) {
            var e2 = arguments[0];
            return this._nodes.addNode(e2);
          }
        } }, { key: "getNodeIterator", value: function() {
          return this._nodes.iterator();
        } }, { key: "linkResultDirectedEdges", value: function() {
          for (var t2 = this._nodes.iterator(); t2.hasNext(); ) {
            t2.next().getEdges().linkResultDirectedEdges();
          }
        } }, { key: "debugPrintln", value: function(t2) {
          mt.out.println(t2);
        } }, { key: "isBoundaryNode", value: function(t2, e2) {
          var n2 = this._nodes.find(e2);
          if (null === n2) return false;
          var i2 = n2.getLabel();
          return null !== i2 && i2.getLocation(t2) === H.BOUNDARY;
        } }, { key: "linkAllDirectedEdges", value: function() {
          for (var t2 = this._nodes.iterator(); t2.hasNext(); ) {
            t2.next().getEdges().linkAllDirectedEdges();
          }
        } }, { key: "matchInSameDirection", value: function(t2, e2, n2, i2) {
          return !!t2.equals(n2) && (ct.index(t2, e2, i2) === ct.COLLINEAR && je.quadrant(t2, e2) === je.quadrant(n2, i2));
        } }, { key: "getEdgeEnds", value: function() {
          return this._edgeEndList;
        } }, { key: "debugPrint", value: function(t2) {
          mt.out.print(t2);
        } }, { key: "getEdgeIterator", value: function() {
          return this._edges.iterator();
        } }, { key: "findEdgeInSameDirection", value: function(t2, e2) {
          for (var n2 = 0; n2 < this._edges.size(); n2++) {
            var i2 = this._edges.get(n2), r3 = i2.getCoordinates();
            if (this.matchInSameDirection(t2, e2, r3[0], r3[1])) return i2;
            if (this.matchInSameDirection(t2, e2, r3[r3.length - 1], r3[r3.length - 2])) return i2;
          }
          return null;
        } }, { key: "insertEdge", value: function(t2) {
          this._edges.add(t2);
        } }, { key: "findEdgeEnd", value: function(t2) {
          for (var e2 = this.getEdgeEnds().iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            if (n2.getEdge() === t2) return n2;
          }
          return null;
        } }, { key: "addEdges", value: function(t2) {
          for (var e2 = t2.iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            this._edges.add(n2);
            var i2 = new Ke(n2, true), r3 = new Ke(n2, false);
            i2.setSym(r3), r3.setSym(i2), this.add(i2), this.add(r3);
          }
        } }, { key: "add", value: function(t2) {
          this._nodes.add(t2), this._edgeEndList.add(t2);
        } }, { key: "getNodes", value: function() {
          return this._nodes.values();
        } }, { key: "findEdge", value: function(t2, e2) {
          for (var n2 = 0; n2 < this._edges.size(); n2++) {
            var i2 = this._edges.get(n2), r3 = i2.getCoordinates();
            if (t2.equals(r3[0]) && e2.equals(r3[1])) return i2;
          }
          return null;
        } }], [{ key: "constructor_", value: function() {
          if (this._edges = new yt(), this._nodes = null, this._edgeEndList = new yt(), 0 === arguments.length) this._nodes = new Ze(new Je());
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this._nodes = new Ze(t2);
          }
        } }, { key: "linkResultDirectedEdges", value: function(t2) {
          for (var e2 = t2.iterator(); e2.hasNext(); ) {
            e2.next().getEdges().linkResultDirectedEdges();
          }
        } }]);
      })(), $e = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "sortShellsAndHoles", value: function(t3, e2, n2) {
          for (var i2 = t3.iterator(); i2.hasNext(); ) {
            var r3 = i2.next();
            r3.isHole() ? n2.add(r3) : e2.add(r3);
          }
        } }, { key: "computePolygons", value: function(t3) {
          for (var e2 = new yt(), n2 = t3.iterator(); n2.hasNext(); ) {
            var i2 = n2.next().toPolygon(this._geometryFactory);
            e2.add(i2);
          }
          return e2;
        } }, { key: "placeFreeHoles", value: function(e2, n2) {
          for (var i2 = n2.iterator(); i2.hasNext(); ) {
            var r3 = i2.next();
            if (null === r3.getShell()) {
              var s2 = t2.findEdgeRingContaining(r3, e2);
              if (null === s2) throw new gt("unable to assign hole to a shell", r3.getCoordinate(0));
              r3.setShell(s2);
            }
          }
        } }, { key: "buildMinimalEdgeRings", value: function(t3, e2, n2) {
          for (var i2 = new yt(), r3 = t3.iterator(); r3.hasNext(); ) {
            var s2 = r3.next();
            if (s2.getMaxNodeDegree() > 2) {
              s2.linkDirectedEdgesForMinimalEdgeRings();
              var a2 = s2.buildMinimalRings(), o2 = this.findShell(a2);
              null !== o2 ? (this.placePolygonHoles(o2, a2), e2.add(o2)) : n2.addAll(a2);
            } else i2.add(s2);
          }
          return i2;
        } }, { key: "buildMaximalEdgeRings", value: function(t3) {
          for (var e2 = new yt(), n2 = t3.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            if (i2.isInResult() && i2.getLabel().isArea() && null === i2.getEdgeRing()) {
              var r3 = new Fe(i2, this._geometryFactory);
              e2.add(r3), r3.setInResult();
            }
          }
          return e2;
        } }, { key: "placePolygonHoles", value: function(t3, e2) {
          for (var n2 = e2.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            i2.isHole() && i2.setShell(t3);
          }
        } }, { key: "getPolygons", value: function() {
          return this.computePolygons(this._shellList);
        } }, { key: "findShell", value: function(t3) {
          for (var e2 = 0, n2 = null, i2 = t3.iterator(); i2.hasNext(); ) {
            var r3 = i2.next();
            r3.isHole() || (n2 = r3, e2++);
          }
          return G.isTrue(e2 <= 1, "found two shells in MinimalEdgeRing list"), n2;
        } }, { key: "add", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.add(t3.getEdgeEnds(), t3.getNodes());
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            Qe.linkResultDirectedEdges(n2);
            var i2 = this.buildMaximalEdgeRings(e2), r3 = new yt(), s2 = this.buildMinimalEdgeRings(i2, this._shellList, r3);
            this.sortShellsAndHoles(s2, this._shellList, r3), this.placeFreeHoles(this._shellList, r3);
          }
        } }], [{ key: "constructor_", value: function() {
          this._geometryFactory = null, this._shellList = new yt();
          var t3 = arguments[0];
          this._geometryFactory = t3;
        } }, { key: "findEdgeRingContaining", value: function(t3, e2) {
          for (var n2 = t3.getLinearRing(), i2 = n2.getEnvelopeInternal(), r3 = n2.getCoordinateN(0), s2 = null, a2 = null, o2 = e2.iterator(); o2.hasNext(); ) {
            var u3 = o2.next(), l2 = u3.getLinearRing(), h2 = l2.getEnvelopeInternal();
            if (!h2.equals(i2) && h2.contains(i2)) {
              r3 = jt.ptNotInList(n2.getCoordinates(), l2.getCoordinates());
              var c2 = false;
              be.isInRing(r3, l2.getCoordinates()) && (c2 = true), c2 && (null === s2 || a2.contains(h2)) && (a2 = (s2 = u3).getLinearRing().getEnvelopeInternal());
            }
          }
          return s2;
        } }]);
      })(), tn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "getBounds", value: function() {
        } }]);
      })(), en = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getItem", value: function() {
          return this._item;
        } }, { key: "getBounds", value: function() {
          return this._bounds;
        } }, { key: "interfaces_", get: function() {
          return [tn, E];
        } }], [{ key: "constructor_", value: function() {
          this._bounds = null, this._item = null;
          var t2 = arguments[0], e2 = arguments[1];
          this._bounds = t2, this._item = e2;
        } }]);
      })(), nn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "poll", value: function() {
          if (this.isEmpty()) return null;
          var t2 = this._items.get(1);
          return this._items.set(1, this._items.get(this._size)), this._size -= 1, this.reorder(1), t2;
        } }, { key: "size", value: function() {
          return this._size;
        } }, { key: "reorder", value: function(t2) {
          for (var e2 = null, n2 = this._items.get(t2); 2 * t2 <= this._size && ((e2 = 2 * t2) !== this._size && this._items.get(e2 + 1).compareTo(this._items.get(e2)) < 0 && e2++, this._items.get(e2).compareTo(n2) < 0); t2 = e2) this._items.set(t2, this._items.get(e2));
          this._items.set(t2, n2);
        } }, { key: "clear", value: function() {
          this._size = 0, this._items.clear();
        } }, { key: "peek", value: function() {
          return this.isEmpty() ? null : this._items.get(1);
        } }, { key: "isEmpty", value: function() {
          return 0 === this._size;
        } }, { key: "add", value: function(t2) {
          this._items.add(null), this._size += 1;
          var e2 = this._size;
          for (this._items.set(0, t2); t2.compareTo(this._items.get(Math.trunc(e2 / 2))) < 0; e2 /= 2) this._items.set(e2, this._items.get(Math.trunc(e2 / 2)));
          this._items.set(e2, t2);
        } }], [{ key: "constructor_", value: function() {
          this._size = null, this._items = null, this._size = 0, this._items = new yt(), this._items.add(null);
        } }]);
      })(), rn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "insert", value: function(t2, e2) {
        } }, { key: "remove", value: function(t2, e2) {
        } }, { key: "query", value: function() {
        } }]);
      })(), sn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getLevel", value: function() {
          return this._level;
        } }, { key: "size", value: function() {
          return this._childBoundables.size();
        } }, { key: "getChildBoundables", value: function() {
          return this._childBoundables;
        } }, { key: "addChildBoundable", value: function(t2) {
          G.isTrue(null === this._bounds), this._childBoundables.add(t2);
        } }, { key: "isEmpty", value: function() {
          return this._childBoundables.isEmpty();
        } }, { key: "getBounds", value: function() {
          return null === this._bounds && (this._bounds = this.computeBounds()), this._bounds;
        } }, { key: "interfaces_", get: function() {
          return [tn, E];
        } }], [{ key: "constructor_", value: function() {
          if (this._childBoundables = new yt(), this._bounds = null, this._level = null, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this._level = t2;
          }
        } }]);
      })(), an = { reverseOrder: function() {
        return { compare: function(t2, e2) {
          return e2.compareTo(t2);
        } };
      }, min: function(t2) {
        return an.sort(t2), t2.get(0);
      }, sort: function(t2, e2) {
        var n2 = t2.toArray();
        e2 ? At.sort(n2, e2) : At.sort(n2);
        for (var i2 = t2.iterator(), r3 = 0, s2 = n2.length; r3 < s2; r3++) i2.next(), i2.set(n2[r3]);
      }, singletonList: function(t2) {
        var e2 = new yt();
        return e2.add(t2), e2;
      } }, on = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "maxDistance", value: function(e2, n2, i2, r3, s2, a2, o2, u3) {
          var l2 = t2.distance(e2, n2, s2, a2);
          return l2 = Math.max(l2, t2.distance(e2, n2, o2, u3)), l2 = Math.max(l2, t2.distance(i2, r3, s2, a2)), l2 = Math.max(l2, t2.distance(i2, r3, o2, u3));
        } }, { key: "distance", value: function(t3, e2, n2, i2) {
          var r3 = n2 - t3, s2 = i2 - e2;
          return Math.sqrt(r3 * r3 + s2 * s2);
        } }, { key: "maximumDistance", value: function(e2, n2) {
          var i2 = Math.min(e2.getMinX(), n2.getMinX()), r3 = Math.min(e2.getMinY(), n2.getMinY()), s2 = Math.max(e2.getMaxX(), n2.getMaxX()), a2 = Math.max(e2.getMaxY(), n2.getMaxY());
          return t2.distance(i2, r3, s2, a2);
        } }, { key: "minMaxDistance", value: function(e2, n2) {
          var i2 = e2.getMinX(), r3 = e2.getMinY(), s2 = e2.getMaxX(), a2 = e2.getMaxY(), o2 = n2.getMinX(), u3 = n2.getMinY(), l2 = n2.getMaxX(), h2 = n2.getMaxY(), c2 = t2.maxDistance(i2, r3, i2, a2, o2, u3, o2, h2);
          return c2 = Math.min(c2, t2.maxDistance(i2, r3, i2, a2, o2, u3, l2, u3)), c2 = Math.min(c2, t2.maxDistance(i2, r3, i2, a2, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(i2, r3, i2, a2, l2, h2, l2, u3)), c2 = Math.min(c2, t2.maxDistance(i2, r3, s2, r3, o2, u3, o2, h2)), c2 = Math.min(c2, t2.maxDistance(i2, r3, s2, r3, o2, u3, l2, u3)), c2 = Math.min(c2, t2.maxDistance(i2, r3, s2, r3, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(i2, r3, s2, r3, l2, h2, l2, u3)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, o2, u3, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, o2, u3, l2, u3)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, i2, a2, l2, h2, l2, u3)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r3, o2, u3, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r3, o2, u3, l2, u3)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r3, l2, h2, o2, h2)), c2 = Math.min(c2, t2.maxDistance(s2, a2, s2, r3, l2, h2, l2, u3));
        } }]);
      })(), un = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "maximumDistance", value: function() {
          return on.maximumDistance(this._boundable1.getBounds(), this._boundable2.getBounds());
        } }, { key: "expandToQueue", value: function(e2, n2) {
          var i2 = t2.isComposite(this._boundable1), r3 = t2.isComposite(this._boundable2);
          if (i2 && r3) return t2.area(this._boundable1) > t2.area(this._boundable2) ? (this.expand(this._boundable1, this._boundable2, false, e2, n2), null) : (this.expand(this._boundable2, this._boundable1, true, e2, n2), null);
          if (i2) return this.expand(this._boundable1, this._boundable2, false, e2, n2), null;
          if (r3) return this.expand(this._boundable2, this._boundable1, true, e2, n2), null;
          throw new m("neither boundable is composite");
        } }, { key: "isLeaves", value: function() {
          return !(t2.isComposite(this._boundable1) || t2.isComposite(this._boundable2));
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3;
          return this._distance < e2._distance ? -1 : this._distance > e2._distance ? 1 : 0;
        } }, { key: "expand", value: function(e2, n2, i2, r3, s2) {
          for (var a2 = e2.getChildBoundables().iterator(); a2.hasNext(); ) {
            var o2 = a2.next(), u3 = null;
            (u3 = i2 ? new t2(n2, o2, this._itemDistance) : new t2(o2, n2, this._itemDistance)).getDistance() < s2 && r3.add(u3);
          }
        } }, { key: "getBoundable", value: function(t3) {
          return 0 === t3 ? this._boundable1 : this._boundable2;
        } }, { key: "getDistance", value: function() {
          return this._distance;
        } }, { key: "distance", value: function() {
          return this.isLeaves() ? this._itemDistance.distance(this._boundable1, this._boundable2) : this._boundable1.getBounds().distance(this._boundable2.getBounds());
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          this._boundable1 = null, this._boundable2 = null, this._distance = null, this._itemDistance = null;
          var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          this._boundable1 = t3, this._boundable2 = e2, this._itemDistance = n2, this._distance = this.distance();
        } }, { key: "area", value: function(t3) {
          return t3.getBounds().getArea();
        } }, { key: "isComposite", value: function(t3) {
          return t3 instanceof sn;
        } }]);
      })(), ln = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "visitItem", value: function(t2) {
        } }]);
      })(), hn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "queryInternal", value: function() {
          if (rt(arguments[2], ln) && arguments[0] instanceof Object && arguments[1] instanceof sn) for (var t3 = arguments[0], e2 = arguments[2], n2 = arguments[1].getChildBoundables(), i2 = 0; i2 < n2.size(); i2++) {
            var r3 = n2.get(i2);
            this.getIntersectsOp().intersects(r3.getBounds(), t3) && (r3 instanceof sn ? this.queryInternal(t3, r3, e2) : r3 instanceof en ? e2.visitItem(r3.getItem()) : G.shouldNeverReachHere());
          }
          else if (rt(arguments[2], nt) && arguments[0] instanceof Object && arguments[1] instanceof sn) for (var s2 = arguments[0], a2 = arguments[2], o2 = arguments[1].getChildBoundables(), u3 = 0; u3 < o2.size(); u3++) {
            var l2 = o2.get(u3);
            this.getIntersectsOp().intersects(l2.getBounds(), s2) && (l2 instanceof sn ? this.queryInternal(s2, l2, a2) : l2 instanceof en ? a2.add(l2.getItem()) : G.shouldNeverReachHere());
          }
        } }, { key: "getNodeCapacity", value: function() {
          return this._nodeCapacity;
        } }, { key: "lastNode", value: function(t3) {
          return t3.get(t3.size() - 1);
        } }, { key: "size", value: function() {
          if (0 === arguments.length) return this.isEmpty() ? 0 : (this.build(), this.size(this._root));
          if (1 === arguments.length) {
            for (var t3 = 0, e2 = arguments[0].getChildBoundables().iterator(); e2.hasNext(); ) {
              var n2 = e2.next();
              n2 instanceof sn ? t3 += this.size(n2) : n2 instanceof en && (t3 += 1);
            }
            return t3;
          }
        } }, { key: "removeItem", value: function(t3, e2) {
          for (var n2 = null, i2 = t3.getChildBoundables().iterator(); i2.hasNext(); ) {
            var r3 = i2.next();
            r3 instanceof en && r3.getItem() === e2 && (n2 = r3);
          }
          return null !== n2 && (t3.getChildBoundables().remove(n2), true);
        } }, { key: "itemsTree", value: function() {
          if (0 === arguments.length) {
            this.build();
            var t3 = this.itemsTree(this._root);
            return null === t3 ? new yt() : t3;
          }
          if (1 === arguments.length) {
            for (var e2 = arguments[0], n2 = new yt(), i2 = e2.getChildBoundables().iterator(); i2.hasNext(); ) {
              var r3 = i2.next();
              if (r3 instanceof sn) {
                var s2 = this.itemsTree(r3);
                null !== s2 && n2.add(s2);
              } else r3 instanceof en ? n2.add(r3.getItem()) : G.shouldNeverReachHere();
            }
            return n2.size() <= 0 ? null : n2;
          }
        } }, { key: "insert", value: function(t3, e2) {
          G.isTrue(!this._built, "Cannot insert items into an STR packed R-tree after it has been built."), this._itemBoundables.add(new en(t3, e2));
        } }, { key: "boundablesAtLevel", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e2 = new yt();
            return this.boundablesAtLevel(t3, this._root, e2), e2;
          }
          if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r3 = arguments[2];
            if (G.isTrue(n2 > -2), i2.getLevel() === n2) return r3.add(i2), null;
            for (var s2 = i2.getChildBoundables().iterator(); s2.hasNext(); ) {
              var a2 = s2.next();
              a2 instanceof sn ? this.boundablesAtLevel(n2, a2, r3) : (G.isTrue(a2 instanceof en), -1 === n2 && r3.add(a2));
            }
            return null;
          }
        } }, { key: "query", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.build();
            var e2 = new yt();
            return this.isEmpty() || this.getIntersectsOp().intersects(this._root.getBounds(), t3) && this.queryInternal(t3, this._root, e2), e2;
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            if (this.build(), this.isEmpty()) return null;
            this.getIntersectsOp().intersects(this._root.getBounds(), n2) && this.queryInternal(n2, this._root, i2);
          }
        } }, { key: "build", value: function() {
          if (this._built) return null;
          this._root = this._itemBoundables.isEmpty() ? this.createNode(0) : this.createHigherLevels(this._itemBoundables, -1), this._itemBoundables = null, this._built = true;
        } }, { key: "getRoot", value: function() {
          return this.build(), this._root;
        } }, { key: "remove", value: function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            return this.build(), !!this.getIntersectsOp().intersects(this._root.getBounds(), t3) && this.remove(t3, this._root, e2);
          }
          if (3 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r3 = arguments[2], s2 = this.removeItem(i2, r3);
            if (s2) return true;
            for (var a2 = null, o2 = i2.getChildBoundables().iterator(); o2.hasNext(); ) {
              var u3 = o2.next();
              if (this.getIntersectsOp().intersects(u3.getBounds(), n2) && (u3 instanceof sn && (s2 = this.remove(n2, u3, r3)))) {
                a2 = u3;
                break;
              }
            }
            return null !== a2 && a2.getChildBoundables().isEmpty() && i2.getChildBoundables().remove(a2), s2;
          }
        } }, { key: "createHigherLevels", value: function(t3, e2) {
          G.isTrue(!t3.isEmpty());
          var n2 = this.createParentBoundables(t3, e2 + 1);
          return 1 === n2.size() ? n2.get(0) : this.createHigherLevels(n2, e2 + 1);
        } }, { key: "depth", value: function() {
          if (0 === arguments.length) return this.isEmpty() ? 0 : (this.build(), this.depth(this._root));
          if (1 === arguments.length) {
            for (var t3 = 0, e2 = arguments[0].getChildBoundables().iterator(); e2.hasNext(); ) {
              var n2 = e2.next();
              if (n2 instanceof sn) {
                var i2 = this.depth(n2);
                i2 > t3 && (t3 = i2);
              }
            }
            return t3 + 1;
          }
        } }, { key: "createParentBoundables", value: function(t3, e2) {
          G.isTrue(!t3.isEmpty());
          var n2 = new yt();
          n2.add(this.createNode(e2));
          var i2 = new yt(t3);
          an.sort(i2, this.getComparator());
          for (var r3 = i2.iterator(); r3.hasNext(); ) {
            var s2 = r3.next();
            this.lastNode(n2).getChildBoundables().size() === this.getNodeCapacity() && n2.add(this.createNode(e2)), this.lastNode(n2).addChildBoundable(s2);
          }
          return n2;
        } }, { key: "isEmpty", value: function() {
          return this._built ? this._root.isEmpty() : this._itemBoundables.isEmpty();
        } }, { key: "interfaces_", get: function() {
          return [E];
        } }], [{ key: "constructor_", value: function() {
          if (this._root = null, this._built = false, this._itemBoundables = new yt(), this._nodeCapacity = null, 0 === arguments.length) t2.constructor_.call(this, t2.DEFAULT_NODE_CAPACITY);
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            G.isTrue(e2 > 1, "Node capacity must be greater than 1"), this._nodeCapacity = e2;
          }
        } }, { key: "compareDoubles", value: function(t3, e2) {
          return t3 > e2 ? 1 : t3 < e2 ? -1 : 0;
        } }]);
      })();
      hn.IntersectsOp = function() {
      }, hn.DEFAULT_NODE_CAPACITY = 10;
      var cn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "distance", value: function(t2, e2) {
        } }]);
      })(), fn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "createParentBoundablesFromVerticalSlices", value: function(t3, e2) {
          G.isTrue(t3.length > 0);
          for (var n2 = new yt(), i3 = 0; i3 < t3.length; i3++) n2.addAll(this.createParentBoundablesFromVerticalSlice(t3[i3], e2));
          return n2;
        } }, { key: "nearestNeighbourK", value: function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            return this.nearestNeighbourK(t3, A.POSITIVE_INFINITY, e2);
          }
          if (3 === arguments.length) {
            var n2 = arguments[0], r3 = arguments[2], s2 = arguments[1], a2 = new nn();
            a2.add(n2);
            for (var o2 = new nn(); !a2.isEmpty() && s2 >= 0; ) {
              var u3 = a2.poll(), l2 = u3.getDistance();
              if (l2 >= s2) break;
              if (u3.isLeaves()) if (o2.size() < r3) o2.add(u3);
              else o2.peek().getDistance() > l2 && (o2.poll(), o2.add(u3)), s2 = o2.peek().getDistance();
              else u3.expandToQueue(a2, s2);
            }
            return i2.getItems(o2);
          }
        } }, { key: "createNode", value: function(t3) {
          return new gn(t3);
        } }, { key: "size", value: function() {
          return 0 === arguments.length ? f(i2, "size", this, 1).call(this) : f(i2, "size", this, 1).apply(this, arguments);
        } }, { key: "insert", value: function() {
          if (!(2 === arguments.length && arguments[1] instanceof Object && arguments[0] instanceof U)) return f(i2, "insert", this, 1).apply(this, arguments);
          var t3 = arguments[0], e2 = arguments[1];
          if (t3.isNull()) return null;
          f(i2, "insert", this, 1).call(this, t3, e2);
        } }, { key: "getIntersectsOp", value: function() {
          return i2.intersectsOp;
        } }, { key: "verticalSlices", value: function(t3, e2) {
          for (var n2 = Math.trunc(Math.ceil(t3.size() / e2)), i3 = new Array(e2).fill(null), r3 = t3.iterator(), s2 = 0; s2 < e2; s2++) {
            i3[s2] = new yt();
            for (var a2 = 0; r3.hasNext() && a2 < n2; ) {
              var o2 = r3.next();
              i3[s2].add(o2), a2++;
            }
          }
          return i3;
        } }, { key: "query", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return f(i2, "query", this, 1).call(this, t3);
          }
          if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            f(i2, "query", this, 1).call(this, e2, n2);
          }
        } }, { key: "getComparator", value: function() {
          return i2.yComparator;
        } }, { key: "createParentBoundablesFromVerticalSlice", value: function(t3, e2) {
          return f(i2, "createParentBoundables", this, 1).call(this, t3, e2);
        } }, { key: "remove", value: function() {
          if (2 === arguments.length && arguments[1] instanceof Object && arguments[0] instanceof U) {
            var t3 = arguments[0], e2 = arguments[1];
            return f(i2, "remove", this, 1).call(this, t3, e2);
          }
          return f(i2, "remove", this, 1).apply(this, arguments);
        } }, { key: "depth", value: function() {
          return 0 === arguments.length ? f(i2, "depth", this, 1).call(this) : f(i2, "depth", this, 1).apply(this, arguments);
        } }, { key: "createParentBoundables", value: function(t3, e2) {
          G.isTrue(!t3.isEmpty());
          var n2 = Math.trunc(Math.ceil(t3.size() / this.getNodeCapacity())), r3 = new yt(t3);
          an.sort(r3, i2.xComparator);
          var s2 = this.verticalSlices(r3, Math.trunc(Math.ceil(Math.sqrt(n2))));
          return this.createParentBoundablesFromVerticalSlices(s2, e2);
        } }, { key: "nearestNeighbour", value: function() {
          if (1 === arguments.length) {
            if (rt(arguments[0], cn)) {
              var t3 = arguments[0];
              if (this.isEmpty()) return null;
              var e2 = new un(this.getRoot(), this.getRoot(), t3);
              return this.nearestNeighbour(e2);
            }
            if (arguments[0] instanceof un) {
              var n2 = arguments[0], i3 = A.POSITIVE_INFINITY, r3 = null, s2 = new nn();
              for (s2.add(n2); !s2.isEmpty() && i3 > 0; ) {
                var a2 = s2.poll(), o2 = a2.getDistance();
                if (o2 >= i3) break;
                a2.isLeaves() ? (i3 = o2, r3 = a2) : a2.expandToQueue(s2, i3);
              }
              return null === r3 ? null : [r3.getBoundable(0).getItem(), r3.getBoundable(1).getItem()];
            }
          } else {
            if (2 === arguments.length) {
              var u3 = arguments[0], l2 = arguments[1];
              if (this.isEmpty() || u3.isEmpty()) return null;
              var h2 = new un(this.getRoot(), u3.getRoot(), l2);
              return this.nearestNeighbour(h2);
            }
            if (3 === arguments.length) {
              var c2 = arguments[2], f2 = new en(arguments[0], arguments[1]), g2 = new un(this.getRoot(), f2, c2);
              return this.nearestNeighbour(g2)[0];
            }
            if (4 === arguments.length) {
              var v2 = arguments[2], y2 = arguments[3], d2 = new en(arguments[0], arguments[1]), _2 = new un(this.getRoot(), d2, v2);
              return this.nearestNeighbourK(_2, y2);
            }
          }
        } }, { key: "isWithinDistance", value: function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1], n2 = A.POSITIVE_INFINITY, i3 = new nn();
            for (i3.add(t3); !i3.isEmpty(); ) {
              var r3 = i3.poll(), s2 = r3.getDistance();
              if (s2 > e2) return false;
              if (r3.maximumDistance() <= e2) return true;
              if (r3.isLeaves()) {
                if ((n2 = s2) <= e2) return true;
              } else r3.expandToQueue(i3, n2);
            }
            return false;
          }
          if (3 === arguments.length) {
            var a2 = arguments[0], o2 = arguments[1], u3 = arguments[2], l2 = new un(this.getRoot(), a2.getRoot(), o2);
            return this.isWithinDistance(l2, u3);
          }
        } }, { key: "interfaces_", get: function() {
          return [rn, E];
        } }], [{ key: "constructor_", value: function() {
          if (0 === arguments.length) i2.constructor_.call(this, i2.DEFAULT_NODE_CAPACITY);
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            hn.constructor_.call(this, t3);
          }
        } }, { key: "centreX", value: function(t3) {
          return i2.avg(t3.getMinX(), t3.getMaxX());
        } }, { key: "avg", value: function(t3, e2) {
          return (t3 + e2) / 2;
        } }, { key: "getItems", value: function(t3) {
          for (var e2 = new Array(t3.size()).fill(null), n2 = 0; !t3.isEmpty(); ) {
            var i3 = t3.poll();
            e2[n2] = i3.getBoundable(0).getItem(), n2++;
          }
          return e2;
        } }, { key: "centreY", value: function(t3) {
          return i2.avg(t3.getMinY(), t3.getMaxY());
        } }]);
      })(hn), gn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "computeBounds", value: function() {
          for (var t3 = null, e2 = this.getChildBoundables().iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            null === t3 ? t3 = new U(n2.getBounds()) : t3.expandToInclude(n2.getBounds());
          }
          return t3;
        } }], [{ key: "constructor_", value: function() {
          var t3 = arguments[0];
          sn.constructor_.call(this, t3);
        } }]);
      })(sn);
      fn.STRtreeNode = gn, fn.xComparator = new ((function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "interfaces_", get: function() {
          return [P];
        } }, { key: "compare", value: function(t2, e2) {
          return hn.compareDoubles(fn.centreX(t2.getBounds()), fn.centreX(e2.getBounds()));
        } }]);
      })())(), fn.yComparator = new ((function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "interfaces_", get: function() {
          return [P];
        } }, { key: "compare", value: function(t2, e2) {
          return hn.compareDoubles(fn.centreY(t2.getBounds()), fn.centreY(e2.getBounds()));
        } }]);
      })())(), fn.intersectsOp = new ((function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "interfaces_", get: function() {
          return [IntersectsOp];
        } }, { key: "intersects", value: function(t2, e2) {
          return t2.intersects(e2);
        } }]);
      })())(), fn.DEFAULT_NODE_CAPACITY = 10;
      var vn = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "relativeSign", value: function(t3, e2) {
          return t3 < e2 ? -1 : t3 > e2 ? 1 : 0;
        } }, { key: "compare", value: function(e2, n2, i2) {
          if (n2.equals2D(i2)) return 0;
          var r3 = t2.relativeSign(n2.x, i2.x), s2 = t2.relativeSign(n2.y, i2.y);
          switch (e2) {
            case 0:
              return t2.compareValue(r3, s2);
            case 1:
              return t2.compareValue(s2, r3);
            case 2:
              return t2.compareValue(s2, -r3);
            case 3:
              return t2.compareValue(-r3, s2);
            case 4:
              return t2.compareValue(-r3, -s2);
            case 5:
              return t2.compareValue(-s2, -r3);
            case 6:
              return t2.compareValue(-s2, r3);
            case 7:
              return t2.compareValue(r3, -s2);
          }
          return G.shouldNeverReachHere("invalid octant value"), 0;
        } }, { key: "compareValue", value: function(t3, e2) {
          return t3 < 0 ? -1 : t3 > 0 ? 1 : e2 < 0 ? -1 : e2 > 0 ? 1 : 0;
        } }]);
      })(), yn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getCoordinate", value: function() {
          return this.coord;
        } }, { key: "print", value: function(t2) {
          t2.print(this.coord), t2.print(" seg # = " + this.segmentIndex);
        } }, { key: "compareTo", value: function(t2) {
          var e2 = t2;
          return this.segmentIndex < e2.segmentIndex ? -1 : this.segmentIndex > e2.segmentIndex ? 1 : this.coord.equals2D(e2.coord) ? 0 : this._isInterior ? e2._isInterior ? vn.compare(this._segmentOctant, this.coord, e2.coord) : 1 : -1;
        } }, { key: "isEndPoint", value: function(t2) {
          return 0 === this.segmentIndex && !this._isInterior || this.segmentIndex === t2;
        } }, { key: "toString", value: function() {
          return this.segmentIndex + ":" + this.coord.toString();
        } }, { key: "isInterior", value: function() {
          return this._isInterior;
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          this._segString = null, this.coord = null, this.segmentIndex = null, this._segmentOctant = null, this._isInterior = null;
          var t2 = arguments[0], e2 = arguments[1], n2 = arguments[2], i2 = arguments[3];
          this._segString = t2, this.coord = new X(e2), this.segmentIndex = n2, this._segmentOctant = i2, this._isInterior = !e2.equals2D(t2.getCoordinate(n2));
        } }]);
      })(), dn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "hasNext", value: function() {
        } }, { key: "next", value: function() {
        } }, { key: "remove", value: function() {
        } }]);
      })(), _n = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getSplitCoordinates", value: function() {
          var t2 = new Zt();
          this.addEndpoints();
          for (var e2 = this.iterator(), n2 = e2.next(); e2.hasNext(); ) {
            var i2 = e2.next();
            this.addEdgeCoordinates(n2, i2, t2), n2 = i2;
          }
          return t2.toCoordinateArray();
        } }, { key: "addCollapsedNodes", value: function() {
          var t2 = new yt();
          this.findCollapsesFromInsertedNodes(t2), this.findCollapsesFromExistingVertices(t2);
          for (var e2 = t2.iterator(); e2.hasNext(); ) {
            var n2 = e2.next().intValue();
            this.add(this._edge.getCoordinate(n2), n2);
          }
        } }, { key: "createSplitEdgePts", value: function(t2, e2) {
          var n2 = e2.segmentIndex - t2.segmentIndex + 2;
          if (2 === n2) return [new X(t2.coord), new X(e2.coord)];
          var i2 = this._edge.getCoordinate(e2.segmentIndex), r3 = e2.isInterior() || !e2.coord.equals2D(i2);
          r3 || n2--;
          var s2 = new Array(n2).fill(null), a2 = 0;
          s2[a2++] = new X(t2.coord);
          for (var o2 = t2.segmentIndex + 1; o2 <= e2.segmentIndex; o2++) s2[a2++] = this._edge.getCoordinate(o2);
          return r3 && (s2[a2] = new X(e2.coord)), s2;
        } }, { key: "print", value: function(t2) {
          t2.println("Intersections:");
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            e2.next().print(t2);
          }
        } }, { key: "findCollapsesFromExistingVertices", value: function(t2) {
          for (var e2 = 0; e2 < this._edge.size() - 2; e2++) {
            var n2 = this._edge.getCoordinate(e2);
            this._edge.getCoordinate(e2 + 1);
            var i2 = this._edge.getCoordinate(e2 + 2);
            n2.equals2D(i2) && t2.add(at.valueOf(e2 + 1));
          }
        } }, { key: "addEdgeCoordinates", value: function(t2, e2, n2) {
          var i2 = this.createSplitEdgePts(t2, e2);
          n2.add(i2, false);
        } }, { key: "iterator", value: function() {
          return this._nodeMap.values().iterator();
        } }, { key: "addSplitEdges", value: function(t2) {
          this.addEndpoints(), this.addCollapsedNodes();
          for (var e2 = this.iterator(), n2 = e2.next(); e2.hasNext(); ) {
            var i2 = e2.next(), r3 = this.createSplitEdge(n2, i2);
            t2.add(r3), n2 = i2;
          }
        } }, { key: "findCollapseIndex", value: function(t2, e2, n2) {
          if (!t2.coord.equals2D(e2.coord)) return false;
          var i2 = e2.segmentIndex - t2.segmentIndex;
          return e2.isInterior() || i2--, 1 === i2 && (n2[0] = t2.segmentIndex + 1, true);
        } }, { key: "findCollapsesFromInsertedNodes", value: function(t2) {
          for (var e2 = new Array(1).fill(null), n2 = this.iterator(), i2 = n2.next(); n2.hasNext(); ) {
            var r3 = n2.next();
            this.findCollapseIndex(i2, r3, e2) && t2.add(at.valueOf(e2[0])), i2 = r3;
          }
        } }, { key: "getEdge", value: function() {
          return this._edge;
        } }, { key: "addEndpoints", value: function() {
          var t2 = this._edge.size() - 1;
          this.add(this._edge.getCoordinate(0), 0), this.add(this._edge.getCoordinate(t2), t2);
        } }, { key: "createSplitEdge", value: function(t2, e2) {
          var n2 = this.createSplitEdgePts(t2, e2);
          return new xn(n2, this._edge.getData());
        } }, { key: "add", value: function(t2, e2) {
          var n2 = new yn(this._edge, t2, e2, this._edge.getSegmentOctant(e2)), i2 = this._nodeMap.get(n2);
          return null !== i2 ? (G.isTrue(i2.coord.equals2D(t2), "Found equal nodes with different coordinates"), i2) : (this._nodeMap.put(n2, n2), n2);
        } }, { key: "checkSplitEdgesCorrectness", value: function(t2) {
          var e2 = this._edge.getCoordinates(), n2 = t2.get(0).getCoordinate(0);
          if (!n2.equals2D(e2[0])) throw new D2("bad split edge start point at " + n2);
          var i2 = t2.get(t2.size() - 1).getCoordinates(), r3 = i2[i2.length - 1];
          if (!r3.equals2D(e2[e2.length - 1])) throw new D2("bad split edge end point at " + r3);
        } }], [{ key: "constructor_", value: function() {
          this._nodeMap = new He(), this._edge = null;
          var t2 = arguments[0];
          this._edge = t2;
        } }]);
      })(), pn = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "octant", value: function() {
          if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
            var e2 = arguments[0], n2 = arguments[1];
            if (0 === e2 && 0 === n2) throw new m("Cannot compute the octant for point ( " + e2 + ", " + n2 + " )");
            var i2 = Math.abs(e2), r3 = Math.abs(n2);
            return e2 >= 0 ? n2 >= 0 ? i2 >= r3 ? 0 : 1 : i2 >= r3 ? 7 : 6 : n2 >= 0 ? i2 >= r3 ? 3 : 2 : i2 >= r3 ? 4 : 5;
          }
          if (arguments[0] instanceof X && arguments[1] instanceof X) {
            var s2 = arguments[0], a2 = arguments[1], o2 = a2.x - s2.x, u3 = a2.y - s2.y;
            if (0 === o2 && 0 === u3) throw new m("Cannot compute the octant for two identical points " + s2);
            return t2.octant(o2, u3);
          }
        } }]);
      })(), mn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "getCoordinates", value: function() {
        } }, { key: "size", value: function() {
        } }, { key: "getCoordinate", value: function(t2) {
        } }, { key: "isClosed", value: function() {
        } }, { key: "setData", value: function(t2) {
        } }, { key: "getData", value: function() {
        } }]);
      })(), kn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "addIntersection", value: function(t2, e2) {
        } }, { key: "interfaces_", get: function() {
          return [mn];
        } }]);
      })(), xn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getCoordinates", value: function() {
          return this._pts;
        } }, { key: "size", value: function() {
          return this._pts.length;
        } }, { key: "getCoordinate", value: function(t3) {
          return this._pts[t3];
        } }, { key: "isClosed", value: function() {
          return this._pts[0].equals(this._pts[this._pts.length - 1]);
        } }, { key: "getSegmentOctant", value: function(t3) {
          return t3 === this._pts.length - 1 ? -1 : this.safeOctant(this.getCoordinate(t3), this.getCoordinate(t3 + 1));
        } }, { key: "setData", value: function(t3) {
          this._data = t3;
        } }, { key: "safeOctant", value: function(t3, e2) {
          return t3.equals2D(e2) ? 0 : pn.octant(t3, e2);
        } }, { key: "getData", value: function() {
          return this._data;
        } }, { key: "addIntersection", value: function() {
          if (2 === arguments.length) {
            var t3 = arguments[0], e2 = arguments[1];
            this.addIntersectionNode(t3, e2);
          } else if (4 === arguments.length) {
            var n2 = arguments[1], i2 = arguments[3], r3 = new X(arguments[0].getIntersection(i2));
            this.addIntersection(r3, n2);
          }
        } }, { key: "toString", value: function() {
          return Ce.toLineString(new Qt(this._pts));
        } }, { key: "getNodeList", value: function() {
          return this._nodeList;
        } }, { key: "addIntersectionNode", value: function(t3, e2) {
          var n2 = e2, i2 = n2 + 1;
          if (i2 < this._pts.length) {
            var r3 = this._pts[i2];
            t3.equals2D(r3) && (n2 = i2);
          }
          return this._nodeList.add(t3, n2);
        } }, { key: "addIntersections", value: function(t3, e2, n2) {
          for (var i2 = 0; i2 < t3.getIntersectionNum(); i2++) this.addIntersection(t3, e2, n2, i2);
        } }, { key: "interfaces_", get: function() {
          return [kn];
        } }], [{ key: "constructor_", value: function() {
          this._nodeList = new _n(this), this._pts = null, this._data = null;
          var t3 = arguments[0], e2 = arguments[1];
          this._pts = t3, this._data = e2;
        } }, { key: "getNodedSubstrings", value: function() {
          if (1 === arguments.length) {
            var e2 = arguments[0], n2 = new yt();
            return t2.getNodedSubstrings(e2, n2), n2;
          }
          if (2 === arguments.length) for (var i2 = arguments[1], r3 = arguments[0].iterator(); r3.hasNext(); ) {
            r3.next().getNodeList().addSplitEdges(i2);
          }
        } }]);
      })(), In = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "minX", value: function() {
          return Math.min(this.p0.x, this.p1.x);
        } }, { key: "orientationIndex", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0], n2 = ct.index(this.p0, this.p1, e2.p0), i2 = ct.index(this.p0, this.p1, e2.p1);
            return n2 >= 0 && i2 >= 0 || n2 <= 0 && i2 <= 0 ? Math.max(n2, i2) : 0;
          }
          if (arguments[0] instanceof X) {
            var r3 = arguments[0];
            return ct.index(this.p0, this.p1, r3);
          }
        } }, { key: "toGeometry", value: function(t3) {
          return t3.createLineString([this.p0, this.p1]);
        } }, { key: "isVertical", value: function() {
          return this.p0.x === this.p1.x;
        } }, { key: "equals", value: function(e2) {
          if (!(e2 instanceof t2)) return false;
          var n2 = e2;
          return this.p0.equals(n2.p0) && this.p1.equals(n2.p1);
        } }, { key: "intersection", value: function(t3) {
          var e2 = new we();
          return e2.computeIntersection(this.p0, this.p1, t3.p0, t3.p1), e2.hasIntersection() ? e2.getIntersection(0) : null;
        } }, { key: "project", value: function() {
          if (arguments[0] instanceof X) {
            var e2 = arguments[0];
            if (e2.equals(this.p0) || e2.equals(this.p1)) return new X(e2);
            var n2 = this.projectionFactor(e2), i2 = new X();
            return i2.x = this.p0.x + n2 * (this.p1.x - this.p0.x), i2.y = this.p0.y + n2 * (this.p1.y - this.p0.y), i2;
          }
          if (arguments[0] instanceof t2) {
            var r3 = arguments[0], s2 = this.projectionFactor(r3.p0), a2 = this.projectionFactor(r3.p1);
            if (s2 >= 1 && a2 >= 1) return null;
            if (s2 <= 0 && a2 <= 0) return null;
            var o2 = this.project(r3.p0);
            s2 < 0 && (o2 = this.p0), s2 > 1 && (o2 = this.p1);
            var u3 = this.project(r3.p1);
            return a2 < 0 && (u3 = this.p0), a2 > 1 && (u3 = this.p1), new t2(o2, u3);
          }
        } }, { key: "normalize", value: function() {
          this.p1.compareTo(this.p0) < 0 && this.reverse();
        } }, { key: "angle", value: function() {
          return Math.atan2(this.p1.y - this.p0.y, this.p1.x - this.p0.x);
        } }, { key: "getCoordinate", value: function(t3) {
          return 0 === t3 ? this.p0 : this.p1;
        } }, { key: "distancePerpendicular", value: function(t3) {
          return xt.pointToLinePerpendicular(t3, this.p0, this.p1);
        } }, { key: "minY", value: function() {
          return Math.min(this.p0.y, this.p1.y);
        } }, { key: "midPoint", value: function() {
          return t2.midPoint(this.p0, this.p1);
        } }, { key: "projectionFactor", value: function(t3) {
          if (t3.equals(this.p0)) return 0;
          if (t3.equals(this.p1)) return 1;
          var e2 = this.p1.x - this.p0.x, n2 = this.p1.y - this.p0.y, i2 = e2 * e2 + n2 * n2;
          return i2 <= 0 ? A.NaN : ((t3.x - this.p0.x) * e2 + (t3.y - this.p0.y) * n2) / i2;
        } }, { key: "closestPoints", value: function(t3) {
          var e2 = this.intersection(t3);
          if (null !== e2) return [e2, e2];
          var n2 = new Array(2).fill(null), i2 = A.MAX_VALUE, r3 = null, s2 = this.closestPoint(t3.p0);
          i2 = s2.distance(t3.p0), n2[0] = s2, n2[1] = t3.p0;
          var a2 = this.closestPoint(t3.p1);
          (r3 = a2.distance(t3.p1)) < i2 && (i2 = r3, n2[0] = a2, n2[1] = t3.p1);
          var o2 = t3.closestPoint(this.p0);
          (r3 = o2.distance(this.p0)) < i2 && (i2 = r3, n2[0] = this.p0, n2[1] = o2);
          var u3 = t3.closestPoint(this.p1);
          return (r3 = u3.distance(this.p1)) < i2 && (i2 = r3, n2[0] = this.p1, n2[1] = u3), n2;
        } }, { key: "closestPoint", value: function(t3) {
          var e2 = this.projectionFactor(t3);
          return e2 > 0 && e2 < 1 ? this.project(t3) : this.p0.distance(t3) < this.p1.distance(t3) ? this.p0 : this.p1;
        } }, { key: "maxX", value: function() {
          return Math.max(this.p0.x, this.p1.x);
        } }, { key: "getLength", value: function() {
          return this.p0.distance(this.p1);
        } }, { key: "compareTo", value: function(t3) {
          var e2 = t3, n2 = this.p0.compareTo(e2.p0);
          return 0 !== n2 ? n2 : this.p1.compareTo(e2.p1);
        } }, { key: "reverse", value: function() {
          var t3 = this.p0;
          this.p0 = this.p1, this.p1 = t3;
        } }, { key: "equalsTopo", value: function(t3) {
          return this.p0.equals(t3.p0) && this.p1.equals(t3.p1) || this.p0.equals(t3.p1) && this.p1.equals(t3.p0);
        } }, { key: "lineIntersection", value: function(t3) {
          return pt.intersection(this.p0, this.p1, t3.p0, t3.p1);
        } }, { key: "maxY", value: function() {
          return Math.max(this.p0.y, this.p1.y);
        } }, { key: "pointAlongOffset", value: function(t3, e2) {
          var n2 = this.p0.x + t3 * (this.p1.x - this.p0.x), i2 = this.p0.y + t3 * (this.p1.y - this.p0.y), r3 = this.p1.x - this.p0.x, s2 = this.p1.y - this.p0.y, a2 = Math.sqrt(r3 * r3 + s2 * s2), o2 = 0, u3 = 0;
          if (0 !== e2) {
            if (a2 <= 0) throw new IllegalStateException("Cannot compute offset from zero-length line segment");
            o2 = e2 * r3 / a2, u3 = e2 * s2 / a2;
          }
          return new X(n2 - u3, i2 + o2);
        } }, { key: "setCoordinates", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            this.setCoordinates(t3.p0, t3.p1);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            this.p0.x = e2.x, this.p0.y = e2.y, this.p1.x = n2.x, this.p1.y = n2.y;
          }
        } }, { key: "segmentFraction", value: function(t3) {
          var e2 = this.projectionFactor(t3);
          return e2 < 0 ? e2 = 0 : (e2 > 1 || A.isNaN(e2)) && (e2 = 1), e2;
        } }, { key: "toString", value: function() {
          return "LINESTRING( " + this.p0.x + " " + this.p0.y + ", " + this.p1.x + " " + this.p1.y + ")";
        } }, { key: "isHorizontal", value: function() {
          return this.p0.y === this.p1.y;
        } }, { key: "reflect", value: function(t3) {
          var e2 = this.p1.getY() - this.p0.getY(), n2 = this.p0.getX() - this.p1.getX(), i2 = this.p0.getY() * (this.p1.getX() - this.p0.getX()) - this.p0.getX() * (this.p1.getY() - this.p0.getY()), r3 = e2 * e2 + n2 * n2, s2 = e2 * e2 - n2 * n2, a2 = t3.getX(), o2 = t3.getY();
          return new X((-s2 * a2 - 2 * e2 * n2 * o2 - 2 * e2 * i2) / r3, (s2 * o2 - 2 * e2 * n2 * a2 - 2 * n2 * i2) / r3);
        } }, { key: "distance", value: function() {
          if (arguments[0] instanceof t2) {
            var e2 = arguments[0];
            return xt.segmentToSegment(this.p0, this.p1, e2.p0, e2.p1);
          }
          if (arguments[0] instanceof X) {
            var n2 = arguments[0];
            return xt.pointToSegment(n2, this.p0, this.p1);
          }
        } }, { key: "pointAlong", value: function(t3) {
          var e2 = new X();
          return e2.x = this.p0.x + t3 * (this.p1.x - this.p0.x), e2.y = this.p0.y + t3 * (this.p1.y - this.p0.y), e2;
        } }, { key: "hashCode", value: function() {
          var t3 = A.doubleToLongBits(this.p0.x);
          t3 ^= 31 * A.doubleToLongBits(this.p0.y);
          var e2 = Math.trunc(t3) ^ Math.trunc(t3 >> 32), n2 = A.doubleToLongBits(this.p1.x);
          return n2 ^= 31 * A.doubleToLongBits(this.p1.y), e2 ^ (Math.trunc(n2) ^ Math.trunc(n2 >> 32));
        } }, { key: "interfaces_", get: function() {
          return [x, E];
        } }], [{ key: "constructor_", value: function() {
          if (this.p0 = null, this.p1 = null, 0 === arguments.length) t2.constructor_.call(this, new X(), new X());
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            t2.constructor_.call(this, e2.p0, e2.p1);
          } else if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1];
            this.p0 = n2, this.p1 = i2;
          } else if (4 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1], a2 = arguments[2], o2 = arguments[3];
            t2.constructor_.call(this, new X(r3, s2), new X(a2, o2));
          }
        } }, { key: "midPoint", value: function(t3, e2) {
          return new X((t3.x + e2.x) / 2, (t3.y + e2.y) / 2);
        } }]);
      })(), En = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "overlap", value: function() {
          if (2 === arguments.length) ;
          else if (4 === arguments.length) {
            var t2 = arguments[1], e2 = arguments[2], n2 = arguments[3];
            arguments[0].getLineSegment(t2, this._overlapSeg1), e2.getLineSegment(n2, this._overlapSeg2), this.overlap(this._overlapSeg1, this._overlapSeg2);
          }
        } }], [{ key: "constructor_", value: function() {
          this._overlapSeg1 = new In(), this._overlapSeg2 = new In();
        } }]);
      })(), Nn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getLineSegment", value: function(t2, e2) {
          e2.p0 = this._pts[t2], e2.p1 = this._pts[t2 + 1];
        } }, { key: "computeSelect", value: function(t2, e2, n2, i2) {
          var r3 = this._pts[e2], s2 = this._pts[n2];
          if (n2 - e2 == 1) return i2.select(this, e2), null;
          if (!t2.intersects(r3, s2)) return null;
          var a2 = Math.trunc((e2 + n2) / 2);
          e2 < a2 && this.computeSelect(t2, e2, a2, i2), a2 < n2 && this.computeSelect(t2, a2, n2, i2);
        } }, { key: "getCoordinates", value: function() {
          for (var t2 = new Array(this._end - this._start + 1).fill(null), e2 = 0, n2 = this._start; n2 <= this._end; n2++) t2[e2++] = this._pts[n2];
          return t2;
        } }, { key: "computeOverlaps", value: function() {
          if (2 === arguments.length) {
            var t2 = arguments[0], e2 = arguments[1];
            this.computeOverlaps(this._start, this._end, t2, t2._start, t2._end, e2);
          } else if (6 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r3 = arguments[2], s2 = arguments[3], a2 = arguments[4], o2 = arguments[5];
            if (i2 - n2 == 1 && a2 - s2 == 1) return o2.overlap(this, n2, r3, s2), null;
            if (!this.overlaps(n2, i2, r3, s2, a2)) return null;
            var u3 = Math.trunc((n2 + i2) / 2), l2 = Math.trunc((s2 + a2) / 2);
            n2 < u3 && (s2 < l2 && this.computeOverlaps(n2, u3, r3, s2, l2, o2), l2 < a2 && this.computeOverlaps(n2, u3, r3, l2, a2, o2)), u3 < i2 && (s2 < l2 && this.computeOverlaps(u3, i2, r3, s2, l2, o2), l2 < a2 && this.computeOverlaps(u3, i2, r3, l2, a2, o2));
          }
        } }, { key: "setId", value: function(t2) {
          this._id = t2;
        } }, { key: "select", value: function(t2, e2) {
          this.computeSelect(t2, this._start, this._end, e2);
        } }, { key: "getEnvelope", value: function() {
          if (null === this._env) {
            var t2 = this._pts[this._start], e2 = this._pts[this._end];
            this._env = new U(t2, e2);
          }
          return this._env;
        } }, { key: "overlaps", value: function(t2, e2, n2, i2, r3) {
          return U.intersects(this._pts[t2], this._pts[e2], n2._pts[i2], n2._pts[r3]);
        } }, { key: "getEndIndex", value: function() {
          return this._end;
        } }, { key: "getStartIndex", value: function() {
          return this._start;
        } }, { key: "getContext", value: function() {
          return this._context;
        } }, { key: "getId", value: function() {
          return this._id;
        } }], [{ key: "constructor_", value: function() {
          this._pts = null, this._start = null, this._end = null, this._env = null, this._context = null, this._id = null;
          var t2 = arguments[0], e2 = arguments[1], n2 = arguments[2], i2 = arguments[3];
          this._pts = t2, this._start = e2, this._end = n2, this._context = i2;
        } }]);
      })(), Tn = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "findChainEnd", value: function(t3, e2) {
          for (var n2 = e2; n2 < t3.length - 1 && t3[n2].equals2D(t3[n2 + 1]); ) n2++;
          if (n2 >= t3.length - 1) return t3.length - 1;
          for (var i2 = je.quadrant(t3[n2], t3[n2 + 1]), r3 = e2 + 1; r3 < t3.length; ) {
            if (!t3[r3 - 1].equals2D(t3[r3])) {
              if (je.quadrant(t3[r3 - 1], t3[r3]) !== i2) break;
            }
            r3++;
          }
          return r3 - 1;
        } }, { key: "getChains", value: function() {
          if (1 === arguments.length) {
            var e2 = arguments[0];
            return t2.getChains(e2, null);
          }
          if (2 === arguments.length) {
            var n2 = arguments[0], i2 = arguments[1], r3 = new yt(), s2 = 0;
            do {
              var a2 = t2.findChainEnd(n2, s2), o2 = new Nn(n2, s2, a2, i2);
              r3.add(o2), s2 = a2;
            } while (s2 < n2.length - 1);
            return r3;
          }
        } }]);
      })(), Sn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "computeNodes", value: function(t2) {
        } }, { key: "getNodedSubstrings", value: function() {
        } }]);
      })(), Ln = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "setSegmentIntersector", value: function(t2) {
          this._segInt = t2;
        } }, { key: "interfaces_", get: function() {
          return [Sn];
        } }], [{ key: "constructor_", value: function() {
          if (this._segInt = null, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t2 = arguments[0];
            this.setSegmentIntersector(t2);
          }
        } }]);
      })(), Cn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "getMonotoneChains", value: function() {
          return this._monoChains;
        } }, { key: "getNodedSubstrings", value: function() {
          return xn.getNodedSubstrings(this._nodedSegStrings);
        } }, { key: "getIndex", value: function() {
          return this._index;
        } }, { key: "add", value: function(t3) {
          for (var e2 = Tn.getChains(t3.getCoordinates(), t3).iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            n2.setId(this._idCounter++), this._index.insert(n2.getEnvelope(), n2), this._monoChains.add(n2);
          }
        } }, { key: "computeNodes", value: function(t3) {
          this._nodedSegStrings = t3;
          for (var e2 = t3.iterator(); e2.hasNext(); ) this.add(e2.next());
          this.intersectChains();
        } }, { key: "intersectChains", value: function() {
          for (var t3 = new Rn(this._segInt), e2 = this._monoChains.iterator(); e2.hasNext(); ) for (var n2 = e2.next(), i3 = this._index.query(n2.getEnvelope()).iterator(); i3.hasNext(); ) {
            var r3 = i3.next();
            if (r3.getId() > n2.getId() && (n2.computeOverlaps(r3, t3), this._nOverlaps++), this._segInt.isDone()) return null;
          }
        } }], [{ key: "constructor_", value: function() {
          if (this._monoChains = new yt(), this._index = new fn(), this._idCounter = 0, this._nodedSegStrings = null, this._nOverlaps = 0, 0 === arguments.length) ;
          else if (1 === arguments.length) {
            var t3 = arguments[0];
            Ln.constructor_.call(this, t3);
          }
        } }]);
      })(Ln), Rn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "overlap", value: function() {
          if (4 !== arguments.length) return f(i2, "overlap", this, 1).apply(this, arguments);
          var t3 = arguments[1], e2 = arguments[2], n2 = arguments[3], r3 = arguments[0].getContext(), s2 = e2.getContext();
          this._si.processIntersections(r3, t3, s2, n2);
        } }], [{ key: "constructor_", value: function() {
          this._si = null;
          var t3 = arguments[0];
          this._si = t3;
        } }]);
      })(En);
      Cn.SegmentOverlapAction = Rn;
      var wn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "isDeletable", value: function(t3, e2, n2, i2) {
          var r3 = this._inputLine[t3], s2 = this._inputLine[e2], a2 = this._inputLine[n2];
          return !!this.isConcave(r3, s2, a2) && (!!this.isShallow(r3, s2, a2, i2) && this.isShallowSampled(r3, s2, t3, n2, i2));
        } }, { key: "deleteShallowConcavities", value: function() {
          for (var e2 = 1, n2 = this.findNextNonDeletedIndex(e2), i2 = this.findNextNonDeletedIndex(n2), r3 = false; i2 < this._inputLine.length; ) {
            var s2 = false;
            this.isDeletable(e2, n2, i2, this._distanceTol) && (this._isDeleted[n2] = t2.DELETE, s2 = true, r3 = true), e2 = s2 ? i2 : n2, n2 = this.findNextNonDeletedIndex(e2), i2 = this.findNextNonDeletedIndex(n2);
          }
          return r3;
        } }, { key: "isShallowConcavity", value: function(t3, e2, n2, i2) {
          return ct.index(t3, e2, n2) === this._angleOrientation && xt.pointToSegment(e2, t3, n2) < i2;
        } }, { key: "isShallowSampled", value: function(e2, n2, i2, r3, s2) {
          var a2 = Math.trunc((r3 - i2) / t2.NUM_PTS_TO_CHECK);
          a2 <= 0 && (a2 = 1);
          for (var o2 = i2; o2 < r3; o2 += a2) if (!this.isShallow(e2, n2, this._inputLine[o2], s2)) return false;
          return true;
        } }, { key: "isConcave", value: function(t3, e2, n2) {
          var i2 = ct.index(t3, e2, n2) === this._angleOrientation;
          return i2;
        } }, { key: "simplify", value: function(t3) {
          this._distanceTol = Math.abs(t3), t3 < 0 && (this._angleOrientation = ct.CLOCKWISE), this._isDeleted = new Array(this._inputLine.length).fill(null);
          var e2 = false;
          do {
            e2 = this.deleteShallowConcavities();
          } while (e2);
          return this.collapseLine();
        } }, { key: "findNextNonDeletedIndex", value: function(e2) {
          for (var n2 = e2 + 1; n2 < this._inputLine.length && this._isDeleted[n2] === t2.DELETE; ) n2++;
          return n2;
        } }, { key: "isShallow", value: function(t3, e2, n2, i2) {
          return xt.pointToSegment(e2, t3, n2) < i2;
        } }, { key: "collapseLine", value: function() {
          for (var e2 = new Zt(), n2 = 0; n2 < this._inputLine.length; n2++) this._isDeleted[n2] !== t2.DELETE && e2.add(this._inputLine[n2]);
          return e2.toCoordinateArray();
        } }], [{ key: "constructor_", value: function() {
          this._inputLine = null, this._distanceTol = null, this._isDeleted = null, this._angleOrientation = ct.COUNTERCLOCKWISE;
          var t3 = arguments[0];
          this._inputLine = t3;
        } }, { key: "simplify", value: function(e2, n2) {
          return new t2(e2).simplify(n2);
        } }]);
      })();
      wn.INIT = 0, wn.DELETE = 1, wn.KEEP = 1, wn.NUM_PTS_TO_CHECK = 10;
      var On = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getCoordinates", value: function() {
          return this._ptList.toArray(t2.COORDINATE_ARRAY_TYPE);
        } }, { key: "setPrecisionModel", value: function(t3) {
          this._precisionModel = t3;
        } }, { key: "addPt", value: function(t3) {
          var e2 = new X(t3);
          if (this._precisionModel.makePrecise(e2), this.isRedundant(e2)) return null;
          this._ptList.add(e2);
        } }, { key: "reverse", value: function() {
        } }, { key: "addPts", value: function(t3, e2) {
          if (e2) for (var n2 = 0; n2 < t3.length; n2++) this.addPt(t3[n2]);
          else for (var i2 = t3.length - 1; i2 >= 0; i2--) this.addPt(t3[i2]);
        } }, { key: "isRedundant", value: function(t3) {
          if (this._ptList.size() < 1) return false;
          var e2 = this._ptList.get(this._ptList.size() - 1);
          return t3.distance(e2) < this._minimimVertexDistance;
        } }, { key: "toString", value: function() {
          return new ae().createLineString(this.getCoordinates()).toString();
        } }, { key: "closeRing", value: function() {
          if (this._ptList.size() < 1) return null;
          var t3 = new X(this._ptList.get(0)), e2 = this._ptList.get(this._ptList.size() - 1);
          if (t3.equals(e2)) return null;
          this._ptList.add(t3);
        } }, { key: "setMinimumVertexDistance", value: function(t3) {
          this._minimimVertexDistance = t3;
        } }], [{ key: "constructor_", value: function() {
          this._ptList = null, this._precisionModel = null, this._minimimVertexDistance = 0, this._ptList = new yt();
        } }]);
      })();
      On.COORDINATE_ARRAY_TYPE = new Array(0).fill(null);
      var bn = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, null, [{ key: "toDegrees", value: function(t3) {
          return 180 * t3 / Math.PI;
        } }, { key: "normalize", value: function(e2) {
          for (; e2 > Math.PI; ) e2 -= t2.PI_TIMES_2;
          for (; e2 <= -Math.PI; ) e2 += t2.PI_TIMES_2;
          return e2;
        } }, { key: "angle", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return Math.atan2(t3.y, t3.x);
          }
          if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1], i2 = n2.x - e2.x, r3 = n2.y - e2.y;
            return Math.atan2(r3, i2);
          }
        } }, { key: "isAcute", value: function(t3, e2, n2) {
          var i2 = t3.x - e2.x, r3 = t3.y - e2.y;
          return i2 * (n2.x - e2.x) + r3 * (n2.y - e2.y) > 0;
        } }, { key: "isObtuse", value: function(t3, e2, n2) {
          var i2 = t3.x - e2.x, r3 = t3.y - e2.y;
          return i2 * (n2.x - e2.x) + r3 * (n2.y - e2.y) < 0;
        } }, { key: "interiorAngle", value: function(e2, n2, i2) {
          var r3 = t2.angle(n2, e2), s2 = t2.angle(n2, i2);
          return Math.abs(s2 - r3);
        } }, { key: "normalizePositive", value: function(e2) {
          if (e2 < 0) {
            for (; e2 < 0; ) e2 += t2.PI_TIMES_2;
            e2 >= t2.PI_TIMES_2 && (e2 = 0);
          } else {
            for (; e2 >= t2.PI_TIMES_2; ) e2 -= t2.PI_TIMES_2;
            e2 < 0 && (e2 = 0);
          }
          return e2;
        } }, { key: "angleBetween", value: function(e2, n2, i2) {
          var r3 = t2.angle(n2, e2), s2 = t2.angle(n2, i2);
          return t2.diff(r3, s2);
        } }, { key: "diff", value: function(t3, e2) {
          var n2 = null;
          return (n2 = t3 < e2 ? e2 - t3 : t3 - e2) > Math.PI && (n2 = 2 * Math.PI - n2), n2;
        } }, { key: "toRadians", value: function(t3) {
          return t3 * Math.PI / 180;
        } }, { key: "getTurn", value: function(e2, n2) {
          var i2 = Math.sin(n2 - e2);
          return i2 > 0 ? t2.COUNTERCLOCKWISE : i2 < 0 ? t2.CLOCKWISE : t2.NONE;
        } }, { key: "angleBetweenOriented", value: function(e2, n2, i2) {
          var r3 = t2.angle(n2, e2), s2 = t2.angle(n2, i2) - r3;
          return s2 <= -Math.PI ? s2 + t2.PI_TIMES_2 : s2 > Math.PI ? s2 - t2.PI_TIMES_2 : s2;
        } }]);
      })();
      bn.PI_TIMES_2 = 2 * Math.PI, bn.PI_OVER_2 = Math.PI / 2, bn.PI_OVER_4 = Math.PI / 4, bn.COUNTERCLOCKWISE = ct.COUNTERCLOCKWISE, bn.CLOCKWISE = ct.CLOCKWISE, bn.NONE = ct.COLLINEAR;
      var Mn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "addNextSegment", value: function(t3, e2) {
          if (this._s0 = this._s1, this._s1 = this._s2, this._s2 = t3, this._seg0.setCoordinates(this._s0, this._s1), this.computeOffsetSegment(this._seg0, this._side, this._distance, this._offset0), this._seg1.setCoordinates(this._s1, this._s2), this.computeOffsetSegment(this._seg1, this._side, this._distance, this._offset1), this._s1.equals(this._s2)) return null;
          var n2 = ct.index(this._s0, this._s1, this._s2), i2 = n2 === ct.CLOCKWISE && this._side === $.LEFT || n2 === ct.COUNTERCLOCKWISE && this._side === $.RIGHT;
          0 === n2 ? this.addCollinear(e2) : i2 ? this.addOutsideTurn(n2, e2) : this.addInsideTurn(n2, e2);
        } }, { key: "addLineEndCap", value: function(t3, e2) {
          var n2 = new In(t3, e2), i2 = new In();
          this.computeOffsetSegment(n2, $.LEFT, this._distance, i2);
          var r3 = new In();
          this.computeOffsetSegment(n2, $.RIGHT, this._distance, r3);
          var s2 = e2.x - t3.x, a2 = e2.y - t3.y, o2 = Math.atan2(a2, s2);
          switch (this._bufParams.getEndCapStyle()) {
            case _.CAP_ROUND:
              this._segList.addPt(i2.p1), this.addDirectedFillet(e2, o2 + Math.PI / 2, o2 - Math.PI / 2, ct.CLOCKWISE, this._distance), this._segList.addPt(r3.p1);
              break;
            case _.CAP_FLAT:
              this._segList.addPt(i2.p1), this._segList.addPt(r3.p1);
              break;
            case _.CAP_SQUARE:
              var u3 = new X();
              u3.x = Math.abs(this._distance) * Math.cos(o2), u3.y = Math.abs(this._distance) * Math.sin(o2);
              var l2 = new X(i2.p1.x + u3.x, i2.p1.y + u3.y), h2 = new X(r3.p1.x + u3.x, r3.p1.y + u3.y);
              this._segList.addPt(l2), this._segList.addPt(h2);
          }
        } }, { key: "getCoordinates", value: function() {
          return this._segList.getCoordinates();
        } }, { key: "addMitreJoin", value: function(t3, e2, n2, i2) {
          var r3 = pt.intersection(e2.p0, e2.p1, n2.p0, n2.p1);
          if (null !== r3 && (i2 <= 0 ? 1 : r3.distance(t3) / Math.abs(i2)) <= this._bufParams.getMitreLimit()) return this._segList.addPt(r3), null;
          this.addLimitedMitreJoin(e2, n2, i2, this._bufParams.getMitreLimit());
        } }, { key: "addOutsideTurn", value: function(e2, n2) {
          if (this._offset0.p1.distance(this._offset1.p0) < this._distance * t2.OFFSET_SEGMENT_SEPARATION_FACTOR) return this._segList.addPt(this._offset0.p1), null;
          this._bufParams.getJoinStyle() === _.JOIN_MITRE ? this.addMitreJoin(this._s1, this._offset0, this._offset1, this._distance) : this._bufParams.getJoinStyle() === _.JOIN_BEVEL ? this.addBevelJoin(this._offset0, this._offset1) : (n2 && this._segList.addPt(this._offset0.p1), this.addCornerFillet(this._s1, this._offset0.p1, this._offset1.p0, e2, this._distance), this._segList.addPt(this._offset1.p0));
        } }, { key: "createSquare", value: function(t3) {
          this._segList.addPt(new X(t3.x + this._distance, t3.y + this._distance)), this._segList.addPt(new X(t3.x + this._distance, t3.y - this._distance)), this._segList.addPt(new X(t3.x - this._distance, t3.y - this._distance)), this._segList.addPt(new X(t3.x - this._distance, t3.y + this._distance)), this._segList.closeRing();
        } }, { key: "addSegments", value: function(t3, e2) {
          this._segList.addPts(t3, e2);
        } }, { key: "addFirstSegment", value: function() {
          this._segList.addPt(this._offset1.p0);
        } }, { key: "addCornerFillet", value: function(t3, e2, n2, i2, r3) {
          var s2 = e2.x - t3.x, a2 = e2.y - t3.y, o2 = Math.atan2(a2, s2), u3 = n2.x - t3.x, l2 = n2.y - t3.y, h2 = Math.atan2(l2, u3);
          i2 === ct.CLOCKWISE ? o2 <= h2 && (o2 += 2 * Math.PI) : o2 >= h2 && (o2 -= 2 * Math.PI), this._segList.addPt(e2), this.addDirectedFillet(t3, o2, h2, i2, r3), this._segList.addPt(n2);
        } }, { key: "addLastSegment", value: function() {
          this._segList.addPt(this._offset1.p1);
        } }, { key: "initSideSegments", value: function(t3, e2, n2) {
          this._s1 = t3, this._s2 = e2, this._side = n2, this._seg1.setCoordinates(t3, e2), this.computeOffsetSegment(this._seg1, n2, this._distance, this._offset1);
        } }, { key: "addLimitedMitreJoin", value: function(t3, e2, n2, i2) {
          var r3 = this._seg0.p1, s2 = bn.angle(r3, this._seg0.p0), a2 = bn.angleBetweenOriented(this._seg0.p0, r3, this._seg1.p1) / 2, o2 = bn.normalize(s2 + a2), u3 = bn.normalize(o2 + Math.PI), l2 = i2 * n2, h2 = n2 - l2 * Math.abs(Math.sin(a2)), c2 = r3.x + l2 * Math.cos(u3), f2 = r3.y + l2 * Math.sin(u3), g2 = new X(c2, f2), v2 = new In(r3, g2), y2 = v2.pointAlongOffset(1, h2), d2 = v2.pointAlongOffset(1, -h2);
          this._side === $.LEFT ? (this._segList.addPt(y2), this._segList.addPt(d2)) : (this._segList.addPt(d2), this._segList.addPt(y2));
        } }, { key: "addDirectedFillet", value: function(t3, e2, n2, i2, r3) {
          var s2 = i2 === ct.CLOCKWISE ? -1 : 1, a2 = Math.abs(e2 - n2), o2 = Math.trunc(a2 / this._filletAngleQuantum + 0.5);
          if (o2 < 1) return null;
          for (var u3 = a2 / o2, l2 = new X(), h2 = 0; h2 < o2; h2++) {
            var c2 = e2 + s2 * h2 * u3;
            l2.x = t3.x + r3 * Math.cos(c2), l2.y = t3.y + r3 * Math.sin(c2), this._segList.addPt(l2);
          }
        } }, { key: "computeOffsetSegment", value: function(t3, e2, n2, i2) {
          var r3 = e2 === $.LEFT ? 1 : -1, s2 = t3.p1.x - t3.p0.x, a2 = t3.p1.y - t3.p0.y, o2 = Math.sqrt(s2 * s2 + a2 * a2), u3 = r3 * n2 * s2 / o2, l2 = r3 * n2 * a2 / o2;
          i2.p0.x = t3.p0.x - l2, i2.p0.y = t3.p0.y + u3, i2.p1.x = t3.p1.x - l2, i2.p1.y = t3.p1.y + u3;
        } }, { key: "addInsideTurn", value: function(e2, n2) {
          if (this._li.computeIntersection(this._offset0.p0, this._offset0.p1, this._offset1.p0, this._offset1.p1), this._li.hasIntersection()) this._segList.addPt(this._li.getIntersection(0));
          else if (this._hasNarrowConcaveAngle = true, this._offset0.p1.distance(this._offset1.p0) < this._distance * t2.INSIDE_TURN_VERTEX_SNAP_DISTANCE_FACTOR) this._segList.addPt(this._offset0.p1);
          else {
            if (this._segList.addPt(this._offset0.p1), this._closingSegLengthFactor > 0) {
              var i2 = new X((this._closingSegLengthFactor * this._offset0.p1.x + this._s1.x) / (this._closingSegLengthFactor + 1), (this._closingSegLengthFactor * this._offset0.p1.y + this._s1.y) / (this._closingSegLengthFactor + 1));
              this._segList.addPt(i2);
              var r3 = new X((this._closingSegLengthFactor * this._offset1.p0.x + this._s1.x) / (this._closingSegLengthFactor + 1), (this._closingSegLengthFactor * this._offset1.p0.y + this._s1.y) / (this._closingSegLengthFactor + 1));
              this._segList.addPt(r3);
            } else this._segList.addPt(this._s1);
            this._segList.addPt(this._offset1.p0);
          }
        } }, { key: "createCircle", value: function(t3) {
          var e2 = new X(t3.x + this._distance, t3.y);
          this._segList.addPt(e2), this.addDirectedFillet(t3, 0, 2 * Math.PI, -1, this._distance), this._segList.closeRing();
        } }, { key: "addBevelJoin", value: function(t3, e2) {
          this._segList.addPt(t3.p1), this._segList.addPt(e2.p0);
        } }, { key: "init", value: function(e2) {
          this._distance = e2, this._maxCurveSegmentError = e2 * (1 - Math.cos(this._filletAngleQuantum / 2)), this._segList = new On(), this._segList.setPrecisionModel(this._precisionModel), this._segList.setMinimumVertexDistance(e2 * t2.CURVE_VERTEX_SNAP_DISTANCE_FACTOR);
        } }, { key: "addCollinear", value: function(t3) {
          this._li.computeIntersection(this._s0, this._s1, this._s1, this._s2), this._li.getIntersectionNum() >= 2 && (this._bufParams.getJoinStyle() === _.JOIN_BEVEL || this._bufParams.getJoinStyle() === _.JOIN_MITRE ? (t3 && this._segList.addPt(this._offset0.p1), this._segList.addPt(this._offset1.p0)) : this.addCornerFillet(this._s1, this._offset0.p1, this._offset1.p0, ct.CLOCKWISE, this._distance));
        } }, { key: "closeRing", value: function() {
          this._segList.closeRing();
        } }, { key: "hasNarrowConcaveAngle", value: function() {
          return this._hasNarrowConcaveAngle;
        } }], [{ key: "constructor_", value: function() {
          this._maxCurveSegmentError = 0, this._filletAngleQuantum = null, this._closingSegLengthFactor = 1, this._segList = null, this._distance = 0, this._precisionModel = null, this._bufParams = null, this._li = null, this._s0 = null, this._s1 = null, this._s2 = null, this._seg0 = new In(), this._seg1 = new In(), this._offset0 = new In(), this._offset1 = new In(), this._side = 0, this._hasNarrowConcaveAngle = false;
          var e2 = arguments[0], n2 = arguments[1], i2 = arguments[2];
          this._precisionModel = e2, this._bufParams = n2, this._li = new we(), this._filletAngleQuantum = Math.PI / 2 / n2.getQuadrantSegments(), n2.getQuadrantSegments() >= 8 && n2.getJoinStyle() === _.JOIN_ROUND && (this._closingSegLengthFactor = t2.MAX_CLOSING_SEG_LEN_FACTOR), this.init(i2);
        } }]);
      })();
      Mn.OFFSET_SEGMENT_SEPARATION_FACTOR = 1e-3, Mn.INSIDE_TURN_VERTEX_SNAP_DISTANCE_FACTOR = 1e-3, Mn.CURVE_VERTEX_SNAP_DISTANCE_FACTOR = 1e-6, Mn.MAX_CLOSING_SEG_LEN_FACTOR = 80;
      var An = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getOffsetCurve", value: function(t3, e2) {
          if (this._distance = e2, 0 === e2) return null;
          var n2 = e2 < 0, i2 = Math.abs(e2), r3 = this.getSegGen(i2);
          t3.length <= 1 ? this.computePointCurve(t3[0], r3) : this.computeOffsetCurve(t3, n2, r3);
          var s2 = r3.getCoordinates();
          return n2 && jt.reverse(s2), s2;
        } }, { key: "computeSingleSidedBufferCurve", value: function(t3, e2, n2) {
          var i2 = this.simplifyTolerance(this._distance);
          if (e2) {
            n2.addSegments(t3, true);
            var r3 = wn.simplify(t3, -i2), s2 = r3.length - 1;
            n2.initSideSegments(r3[s2], r3[s2 - 1], $.LEFT), n2.addFirstSegment();
            for (var a2 = s2 - 2; a2 >= 0; a2--) n2.addNextSegment(r3[a2], true);
          } else {
            n2.addSegments(t3, false);
            var o2 = wn.simplify(t3, i2), u3 = o2.length - 1;
            n2.initSideSegments(o2[0], o2[1], $.LEFT), n2.addFirstSegment();
            for (var l2 = 2; l2 <= u3; l2++) n2.addNextSegment(o2[l2], true);
          }
          n2.addLastSegment(), n2.closeRing();
        } }, { key: "computeRingBufferCurve", value: function(t3, e2, n2) {
          var i2 = this.simplifyTolerance(this._distance);
          e2 === $.RIGHT && (i2 = -i2);
          var r3 = wn.simplify(t3, i2), s2 = r3.length - 1;
          n2.initSideSegments(r3[s2 - 1], r3[0], e2);
          for (var a2 = 1; a2 <= s2; a2++) {
            var o2 = 1 !== a2;
            n2.addNextSegment(r3[a2], o2);
          }
          n2.closeRing();
        } }, { key: "computeLineBufferCurve", value: function(t3, e2) {
          var n2 = this.simplifyTolerance(this._distance), i2 = wn.simplify(t3, n2), r3 = i2.length - 1;
          e2.initSideSegments(i2[0], i2[1], $.LEFT);
          for (var s2 = 2; s2 <= r3; s2++) e2.addNextSegment(i2[s2], true);
          e2.addLastSegment(), e2.addLineEndCap(i2[r3 - 1], i2[r3]);
          var a2 = wn.simplify(t3, -n2), o2 = a2.length - 1;
          e2.initSideSegments(a2[o2], a2[o2 - 1], $.LEFT);
          for (var u3 = o2 - 2; u3 >= 0; u3--) e2.addNextSegment(a2[u3], true);
          e2.addLastSegment(), e2.addLineEndCap(a2[1], a2[0]), e2.closeRing();
        } }, { key: "computePointCurve", value: function(t3, e2) {
          switch (this._bufParams.getEndCapStyle()) {
            case _.CAP_ROUND:
              e2.createCircle(t3);
              break;
            case _.CAP_SQUARE:
              e2.createSquare(t3);
          }
        } }, { key: "getLineCurve", value: function(t3, e2) {
          if (this._distance = e2, this.isLineOffsetEmpty(e2)) return null;
          var n2 = Math.abs(e2), i2 = this.getSegGen(n2);
          if (t3.length <= 1) this.computePointCurve(t3[0], i2);
          else if (this._bufParams.isSingleSided()) {
            var r3 = e2 < 0;
            this.computeSingleSidedBufferCurve(t3, r3, i2);
          } else this.computeLineBufferCurve(t3, i2);
          return i2.getCoordinates();
        } }, { key: "getBufferParameters", value: function() {
          return this._bufParams;
        } }, { key: "simplifyTolerance", value: function(t3) {
          return t3 * this._bufParams.getSimplifyFactor();
        } }, { key: "getRingCurve", value: function(e2, n2, i2) {
          if (this._distance = i2, e2.length <= 2) return this.getLineCurve(e2, i2);
          if (0 === i2) return t2.copyCoordinates(e2);
          var r3 = this.getSegGen(i2);
          return this.computeRingBufferCurve(e2, n2, r3), r3.getCoordinates();
        } }, { key: "computeOffsetCurve", value: function(t3, e2, n2) {
          var i2 = this.simplifyTolerance(this._distance);
          if (e2) {
            var r3 = wn.simplify(t3, -i2), s2 = r3.length - 1;
            n2.initSideSegments(r3[s2], r3[s2 - 1], $.LEFT), n2.addFirstSegment();
            for (var a2 = s2 - 2; a2 >= 0; a2--) n2.addNextSegment(r3[a2], true);
          } else {
            var o2 = wn.simplify(t3, i2), u3 = o2.length - 1;
            n2.initSideSegments(o2[0], o2[1], $.LEFT), n2.addFirstSegment();
            for (var l2 = 2; l2 <= u3; l2++) n2.addNextSegment(o2[l2], true);
          }
          n2.addLastSegment();
        } }, { key: "isLineOffsetEmpty", value: function(t3) {
          return 0 === t3 || t3 < 0 && !this._bufParams.isSingleSided();
        } }, { key: "getSegGen", value: function(t3) {
          return new Mn(this._precisionModel, this._bufParams, t3);
        } }], [{ key: "constructor_", value: function() {
          this._distance = 0, this._precisionModel = null, this._bufParams = null;
          var t3 = arguments[0], e2 = arguments[1];
          this._precisionModel = t3, this._bufParams = e2;
        } }, { key: "copyCoordinates", value: function(t3) {
          for (var e2 = new Array(t3.length).fill(null), n2 = 0; n2 < e2.length; n2++) e2[n2] = new X(t3[n2]);
          return e2;
        } }]);
      })(), Pn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "findStabbedSegments", value: function() {
          if (1 === arguments.length) {
            for (var t2 = arguments[0], e2 = new yt(), n2 = this._subgraphs.iterator(); n2.hasNext(); ) {
              var i2 = n2.next(), r3 = i2.getEnvelope();
              t2.y < r3.getMinY() || t2.y > r3.getMaxY() || this.findStabbedSegments(t2, i2.getDirectedEdges(), e2);
            }
            return e2;
          }
          if (3 === arguments.length) {
            if (rt(arguments[2], nt) && arguments[0] instanceof X && arguments[1] instanceof Ke) for (var s2 = arguments[0], a2 = arguments[1], o2 = arguments[2], u3 = a2.getEdge().getCoordinates(), l2 = 0; l2 < u3.length - 1; l2++) {
              if (this._seg.p0 = u3[l2], this._seg.p1 = u3[l2 + 1], this._seg.p0.y > this._seg.p1.y && this._seg.reverse(), !(Math.max(this._seg.p0.x, this._seg.p1.x) < s2.x || this._seg.isHorizontal() || s2.y < this._seg.p0.y || s2.y > this._seg.p1.y || ct.index(this._seg.p0, this._seg.p1, s2) === ct.RIGHT)) {
                var h2 = a2.getDepth($.LEFT);
                this._seg.p0.equals(u3[l2]) || (h2 = a2.getDepth($.RIGHT));
                var c2 = new Dn(this._seg, h2);
                o2.add(c2);
              }
            }
            else if (rt(arguments[2], nt) && arguments[0] instanceof X && rt(arguments[1], nt)) for (var f2 = arguments[0], g2 = arguments[2], v2 = arguments[1].iterator(); v2.hasNext(); ) {
              var y2 = v2.next();
              y2.isForward() && this.findStabbedSegments(f2, y2, g2);
            }
          }
        } }, { key: "getDepth", value: function(t2) {
          var e2 = this.findStabbedSegments(t2);
          return 0 === e2.size() ? 0 : an.min(e2)._leftDepth;
        } }], [{ key: "constructor_", value: function() {
          this._subgraphs = null, this._seg = new In();
          var t2 = arguments[0];
          this._subgraphs = t2;
        } }]);
      })(), Dn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "compareTo", value: function(t2) {
          var e2 = t2;
          if (this._upwardSeg.minX() >= e2._upwardSeg.maxX()) return 1;
          if (this._upwardSeg.maxX() <= e2._upwardSeg.minX()) return -1;
          var n2 = this._upwardSeg.orientationIndex(e2._upwardSeg);
          return 0 !== n2 || 0 !== (n2 = -1 * e2._upwardSeg.orientationIndex(this._upwardSeg)) ? n2 : this._upwardSeg.compareTo(e2._upwardSeg);
        } }, { key: "compareX", value: function(t2, e2) {
          var n2 = t2.p0.compareTo(e2.p0);
          return 0 !== n2 ? n2 : t2.p1.compareTo(e2.p1);
        } }, { key: "toString", value: function() {
          return this._upwardSeg.toString();
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          this._upwardSeg = null, this._leftDepth = null;
          var t2 = arguments[0], e2 = arguments[1];
          this._upwardSeg = new In(t2), this._leftDepth = e2;
        } }]);
      })();
      Pn.DepthSegment = Dn;
      var Fn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, null, [{ key: "constructor_", value: function() {
          p.constructor_.call(this, "Projective point not representable on the Cartesian plane.");
        } }]);
      })(p), Gn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getY", value: function() {
          var t3 = this.y / this.w;
          if (A.isNaN(t3) || A.isInfinite(t3)) throw new Fn();
          return t3;
        } }, { key: "getX", value: function() {
          var t3 = this.x / this.w;
          if (A.isNaN(t3) || A.isInfinite(t3)) throw new Fn();
          return t3;
        } }, { key: "getCoordinate", value: function() {
          var t3 = new X();
          return t3.x = this.getX(), t3.y = this.getY(), t3;
        } }], [{ key: "constructor_", value: function() {
          if (this.x = null, this.y = null, this.w = null, 0 === arguments.length) this.x = 0, this.y = 0, this.w = 1;
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            this.x = e2.x, this.y = e2.y, this.w = 1;
          } else if (2 === arguments.length) {
            if ("number" == typeof arguments[0] && "number" == typeof arguments[1]) {
              var n2 = arguments[0], i2 = arguments[1];
              this.x = n2, this.y = i2, this.w = 1;
            } else if (arguments[0] instanceof t2 && arguments[1] instanceof t2) {
              var r3 = arguments[0], s2 = arguments[1];
              this.x = r3.y * s2.w - s2.y * r3.w, this.y = s2.x * r3.w - r3.x * s2.w, this.w = r3.x * s2.y - s2.x * r3.y;
            } else if (arguments[0] instanceof X && arguments[1] instanceof X) {
              var a2 = arguments[0], o2 = arguments[1];
              this.x = a2.y - o2.y, this.y = o2.x - a2.x, this.w = a2.x * o2.y - o2.x * a2.y;
            }
          } else if (3 === arguments.length) {
            var u3 = arguments[0], l2 = arguments[1], h2 = arguments[2];
            this.x = u3, this.y = l2, this.w = h2;
          } else if (4 === arguments.length) {
            var c2 = arguments[0], f2 = arguments[1], g2 = arguments[2], v2 = arguments[3], y2 = c2.y - f2.y, d2 = f2.x - c2.x, _2 = c2.x * f2.y - f2.x * c2.y, p2 = g2.y - v2.y, m2 = v2.x - g2.x, k2 = g2.x * v2.y - v2.x * g2.y;
            this.x = d2 * k2 - m2 * _2, this.y = p2 * _2 - y2 * k2, this.w = y2 * m2 - p2 * d2;
          }
        } }]);
      })(), qn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "area", value: function() {
          return t2.area(this.p0, this.p1, this.p2);
        } }, { key: "signedArea", value: function() {
          return t2.signedArea(this.p0, this.p1, this.p2);
        } }, { key: "interpolateZ", value: function(e2) {
          if (null === e2) throw new m("Supplied point is null.");
          return t2.interpolateZ(e2, this.p0, this.p1, this.p2);
        } }, { key: "longestSideLength", value: function() {
          return t2.longestSideLength(this.p0, this.p1, this.p2);
        } }, { key: "isAcute", value: function() {
          return t2.isAcute(this.p0, this.p1, this.p2);
        } }, { key: "circumcentre", value: function() {
          return t2.circumcentre(this.p0, this.p1, this.p2);
        } }, { key: "area3D", value: function() {
          return t2.area3D(this.p0, this.p1, this.p2);
        } }, { key: "centroid", value: function() {
          return t2.centroid(this.p0, this.p1, this.p2);
        } }, { key: "inCentre", value: function() {
          return t2.inCentre(this.p0, this.p1, this.p2);
        } }], [{ key: "constructor_", value: function() {
          this.p0 = null, this.p1 = null, this.p2 = null;
          var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          this.p0 = t3, this.p1 = e2, this.p2 = n2;
        } }, { key: "area", value: function(t3, e2, n2) {
          return Math.abs(((n2.x - t3.x) * (e2.y - t3.y) - (e2.x - t3.x) * (n2.y - t3.y)) / 2);
        } }, { key: "signedArea", value: function(t3, e2, n2) {
          return ((n2.x - t3.x) * (e2.y - t3.y) - (e2.x - t3.x) * (n2.y - t3.y)) / 2;
        } }, { key: "det", value: function(t3, e2, n2, i2) {
          return t3 * i2 - e2 * n2;
        } }, { key: "interpolateZ", value: function(t3, e2, n2, i2) {
          var r3 = e2.x, s2 = e2.y, a2 = n2.x - r3, o2 = i2.x - r3, u3 = n2.y - s2, l2 = i2.y - s2, h2 = a2 * l2 - o2 * u3, c2 = t3.x - r3, f2 = t3.y - s2, g2 = (l2 * c2 - o2 * f2) / h2, v2 = (-u3 * c2 + a2 * f2) / h2;
          return e2.getZ() + g2 * (n2.getZ() - e2.getZ()) + v2 * (i2.getZ() - e2.getZ());
        } }, { key: "longestSideLength", value: function(t3, e2, n2) {
          var i2 = t3.distance(e2), r3 = e2.distance(n2), s2 = n2.distance(t3), a2 = i2;
          return r3 > a2 && (a2 = r3), s2 > a2 && (a2 = s2), a2;
        } }, { key: "circumcentreDD", value: function(t3, e2, n2) {
          var i2 = ut.valueOf(t3.x).subtract(n2.x), r3 = ut.valueOf(t3.y).subtract(n2.y), s2 = ut.valueOf(e2.x).subtract(n2.x), a2 = ut.valueOf(e2.y).subtract(n2.y), o2 = ut.determinant(i2, r3, s2, a2).multiply(2), u3 = i2.sqr().add(r3.sqr()), l2 = s2.sqr().add(a2.sqr()), h2 = ut.determinant(r3, u3, a2, l2), c2 = ut.determinant(i2, u3, s2, l2), f2 = ut.valueOf(n2.x).subtract(h2.divide(o2)).doubleValue(), g2 = ut.valueOf(n2.y).add(c2.divide(o2)).doubleValue();
          return new X(f2, g2);
        } }, { key: "isAcute", value: function(t3, e2, n2) {
          return !!bn.isAcute(t3, e2, n2) && (!!bn.isAcute(e2, n2, t3) && !!bn.isAcute(n2, t3, e2));
        } }, { key: "circumcentre", value: function(e2, n2, i2) {
          var r3 = i2.x, s2 = i2.y, a2 = e2.x - r3, o2 = e2.y - s2, u3 = n2.x - r3, l2 = n2.y - s2, h2 = 2 * t2.det(a2, o2, u3, l2), c2 = t2.det(o2, a2 * a2 + o2 * o2, l2, u3 * u3 + l2 * l2), f2 = t2.det(a2, a2 * a2 + o2 * o2, u3, u3 * u3 + l2 * l2);
          return new X(r3 - c2 / h2, s2 + f2 / h2);
        } }, { key: "perpendicularBisector", value: function(t3, e2) {
          var n2 = e2.x - t3.x, i2 = e2.y - t3.y, r3 = new Gn(t3.x + n2 / 2, t3.y + i2 / 2, 1), s2 = new Gn(t3.x - i2 + n2 / 2, t3.y + n2 + i2 / 2, 1);
          return new Gn(r3, s2);
        } }, { key: "angleBisector", value: function(t3, e2, n2) {
          var i2 = e2.distance(t3), r3 = i2 / (i2 + e2.distance(n2)), s2 = n2.x - t3.x, a2 = n2.y - t3.y;
          return new X(t3.x + r3 * s2, t3.y + r3 * a2);
        } }, { key: "area3D", value: function(t3, e2, n2) {
          var i2 = e2.x - t3.x, r3 = e2.y - t3.y, s2 = e2.getZ() - t3.getZ(), a2 = n2.x - t3.x, o2 = n2.y - t3.y, u3 = n2.getZ() - t3.getZ(), l2 = r3 * u3 - s2 * o2, h2 = s2 * a2 - i2 * u3, c2 = i2 * o2 - r3 * a2, f2 = l2 * l2 + h2 * h2 + c2 * c2, g2 = Math.sqrt(f2) / 2;
          return g2;
        } }, { key: "centroid", value: function(t3, e2, n2) {
          var i2 = (t3.x + e2.x + n2.x) / 3, r3 = (t3.y + e2.y + n2.y) / 3;
          return new X(i2, r3);
        } }, { key: "inCentre", value: function(t3, e2, n2) {
          var i2 = e2.distance(n2), r3 = t3.distance(n2), s2 = t3.distance(e2), a2 = i2 + r3 + s2, o2 = (i2 * t3.x + r3 * e2.x + s2 * n2.x) / a2, u3 = (i2 * t3.y + r3 * e2.y + s2 * n2.y) / a2;
          return new X(o2, u3);
        } }]);
      })(), Yn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "addRingSide", value: function(t2, e2, n2, i2, r3) {
          if (0 === e2 && t2.length < Xt.MINIMUM_VALID_SIZE) return null;
          var s2 = i2, a2 = r3;
          t2.length >= Xt.MINIMUM_VALID_SIZE && ct.isCCW(t2) && (s2 = r3, a2 = i2, n2 = $.opposite(n2));
          var o2 = this._curveBuilder.getRingCurve(t2, n2, e2);
          this.addCurve(o2, s2, a2);
        } }, { key: "addRingBothSides", value: function(t2, e2) {
          this.addRingSide(t2, e2, $.LEFT, H.EXTERIOR, H.INTERIOR), this.addRingSide(t2, e2, $.RIGHT, H.INTERIOR, H.EXTERIOR);
        } }, { key: "addPoint", value: function(t2) {
          if (this._distance <= 0) return null;
          var e2 = t2.getCoordinates(), n2 = this._curveBuilder.getLineCurve(e2, this._distance);
          this.addCurve(n2, H.EXTERIOR, H.INTERIOR);
        } }, { key: "addPolygon", value: function(t2) {
          var e2 = this._distance, n2 = $.LEFT;
          this._distance < 0 && (e2 = -this._distance, n2 = $.RIGHT);
          var i2 = t2.getExteriorRing(), r3 = jt.removeRepeatedPoints(i2.getCoordinates());
          if (this._distance < 0 && this.isErodedCompletely(i2, this._distance)) return null;
          if (this._distance <= 0 && r3.length < 3) return null;
          this.addRingSide(r3, e2, n2, H.EXTERIOR, H.INTERIOR);
          for (var s2 = 0; s2 < t2.getNumInteriorRing(); s2++) {
            var a2 = t2.getInteriorRingN(s2), o2 = jt.removeRepeatedPoints(a2.getCoordinates());
            this._distance > 0 && this.isErodedCompletely(a2, -this._distance) || this.addRingSide(o2, e2, $.opposite(n2), H.INTERIOR, H.EXTERIOR);
          }
        } }, { key: "isTriangleErodedCompletely", value: function(t2, e2) {
          var n2 = new qn(t2[0], t2[1], t2[2]), i2 = n2.inCentre();
          return xt.pointToSegment(i2, n2.p0, n2.p1) < Math.abs(e2);
        } }, { key: "addLineString", value: function(t2) {
          if (this._curveBuilder.isLineOffsetEmpty(this._distance)) return null;
          var e2 = jt.removeRepeatedPoints(t2.getCoordinates());
          if (jt.isRing(e2) && !this._curveBuilder.getBufferParameters().isSingleSided()) this.addRingBothSides(e2, this._distance);
          else {
            var n2 = this._curveBuilder.getLineCurve(e2, this._distance);
            this.addCurve(n2, H.EXTERIOR, H.INTERIOR);
          }
        } }, { key: "addCurve", value: function(t2, e2, n2) {
          if (null === t2 || t2.length < 2) return null;
          var i2 = new xn(t2, new Ae(0, H.BOUNDARY, e2, n2));
          this._curveList.add(i2);
        } }, { key: "getCurves", value: function() {
          return this.add(this._inputGeom), this._curveList;
        } }, { key: "add", value: function(t2) {
          if (t2.isEmpty()) return null;
          if (t2 instanceof Dt) this.addPolygon(t2);
          else if (t2 instanceof wt) this.addLineString(t2);
          else if (t2 instanceof bt) this.addPoint(t2);
          else if (t2 instanceof zt) this.addCollection(t2);
          else if (t2 instanceof se) this.addCollection(t2);
          else if (t2 instanceof te) this.addCollection(t2);
          else {
            if (!(t2 instanceof Yt)) throw new W(t2.getGeometryType());
            this.addCollection(t2);
          }
        } }, { key: "isErodedCompletely", value: function(t2, e2) {
          var n2 = t2.getCoordinates();
          if (n2.length < 4) return e2 < 0;
          if (4 === n2.length) return this.isTriangleErodedCompletely(n2, e2);
          var i2 = t2.getEnvelopeInternal(), r3 = Math.min(i2.getHeight(), i2.getWidth());
          return e2 < 0 && 2 * Math.abs(e2) > r3;
        } }, { key: "addCollection", value: function(t2) {
          for (var e2 = 0; e2 < t2.getNumGeometries(); e2++) {
            var n2 = t2.getGeometryN(e2);
            this.add(n2);
          }
        } }], [{ key: "constructor_", value: function() {
          this._inputGeom = null, this._distance = null, this._curveBuilder = null, this._curveList = new yt();
          var t2 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          this._inputGeom = t2, this._distance = e2, this._curveBuilder = n2;
        } }]);
      })(), zn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "locate", value: function(t2) {
        } }]);
      })(), Xn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "next", value: function() {
          if (this._atStart) return this._atStart = false, t2.isAtomic(this._parent) && this._index++, this._parent;
          if (null !== this._subcollectionIterator) {
            if (this._subcollectionIterator.hasNext()) return this._subcollectionIterator.next();
            this._subcollectionIterator = null;
          }
          if (this._index >= this._max) throw new j();
          var e2 = this._parent.getGeometryN(this._index++);
          return e2 instanceof Yt ? (this._subcollectionIterator = new t2(e2), this._subcollectionIterator.next()) : e2;
        } }, { key: "remove", value: function() {
          throw new W(this.getClass().getName());
        } }, { key: "hasNext", value: function() {
          if (this._atStart) return true;
          if (null !== this._subcollectionIterator) {
            if (this._subcollectionIterator.hasNext()) return true;
            this._subcollectionIterator = null;
          }
          return !(this._index >= this._max);
        } }, { key: "interfaces_", get: function() {
          return [dn];
        } }], [{ key: "constructor_", value: function() {
          this._parent = null, this._atStart = null, this._max = null, this._index = null, this._subcollectionIterator = null;
          var t3 = arguments[0];
          this._parent = t3, this._atStart = true, this._index = 0, this._max = t3.getNumGeometries();
        } }, { key: "isAtomic", value: function(t3) {
          return !(t3 instanceof Yt);
        } }]);
      })(), Bn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "locate", value: function(e2) {
          return t2.locate(e2, this._geom);
        } }, { key: "interfaces_", get: function() {
          return [zn];
        } }], [{ key: "constructor_", value: function() {
          this._geom = null;
          var t3 = arguments[0];
          this._geom = t3;
        } }, { key: "locatePointInPolygon", value: function(e2, n2) {
          if (n2.isEmpty()) return H.EXTERIOR;
          var i2 = n2.getExteriorRing(), r3 = t2.locatePointInRing(e2, i2);
          if (r3 !== H.INTERIOR) return r3;
          for (var s2 = 0; s2 < n2.getNumInteriorRing(); s2++) {
            var a2 = n2.getInteriorRingN(s2), o2 = t2.locatePointInRing(e2, a2);
            if (o2 === H.BOUNDARY) return H.BOUNDARY;
            if (o2 === H.INTERIOR) return H.EXTERIOR;
          }
          return H.INTERIOR;
        } }, { key: "locatePointInRing", value: function(t3, e2) {
          return e2.getEnvelopeInternal().intersects(t3) ? be.locateInRing(t3, e2.getCoordinates()) : H.EXTERIOR;
        } }, { key: "containsPointInPolygon", value: function(e2, n2) {
          return H.EXTERIOR !== t2.locatePointInPolygon(e2, n2);
        } }, { key: "locateInGeometry", value: function(e2, n2) {
          if (n2 instanceof Dt) return t2.locatePointInPolygon(e2, n2);
          if (n2 instanceof Yt) for (var i2 = new Xn(n2); i2.hasNext(); ) {
            var r3 = i2.next();
            if (r3 !== n2) {
              var s2 = t2.locateInGeometry(e2, r3);
              if (s2 !== H.EXTERIOR) return s2;
            }
          }
          return H.EXTERIOR;
        } }, { key: "isContained", value: function(e2, n2) {
          return H.EXTERIOR !== t2.locate(e2, n2);
        } }, { key: "locate", value: function(e2, n2) {
          return n2.isEmpty() ? H.EXTERIOR : n2.getEnvelopeInternal().intersects(e2) ? t2.locateInGeometry(e2, n2) : H.EXTERIOR;
        } }]);
      })(), Un = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getNextCW", value: function(t2) {
          this.getEdges();
          var e2 = this._edgeList.indexOf(t2), n2 = e2 - 1;
          return 0 === e2 && (n2 = this._edgeList.size() - 1), this._edgeList.get(n2);
        } }, { key: "propagateSideLabels", value: function(t2) {
          for (var e2 = H.NONE, n2 = this.iterator(); n2.hasNext(); ) {
            var i2 = n2.next().getLabel();
            i2.isArea(t2) && i2.getLocation(t2, $.LEFT) !== H.NONE && (e2 = i2.getLocation(t2, $.LEFT));
          }
          if (e2 === H.NONE) return null;
          for (var r3 = e2, s2 = this.iterator(); s2.hasNext(); ) {
            var a2 = s2.next(), o2 = a2.getLabel();
            if (o2.getLocation(t2, $.ON) === H.NONE && o2.setLocation(t2, $.ON, r3), o2.isArea(t2)) {
              var u3 = o2.getLocation(t2, $.LEFT), l2 = o2.getLocation(t2, $.RIGHT);
              if (l2 !== H.NONE) {
                if (l2 !== r3) throw new gt("side location conflict", a2.getCoordinate());
                u3 === H.NONE && G.shouldNeverReachHere("found single null side (at " + a2.getCoordinate() + ")"), r3 = u3;
              } else G.isTrue(o2.getLocation(t2, $.LEFT) === H.NONE, "found single null side"), o2.setLocation(t2, $.RIGHT, r3), o2.setLocation(t2, $.LEFT, r3);
            }
          }
        } }, { key: "getCoordinate", value: function() {
          var t2 = this.iterator();
          return t2.hasNext() ? t2.next().getCoordinate() : null;
        } }, { key: "print", value: function(t2) {
          mt.out.println("EdgeEndStar:   " + this.getCoordinate());
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            e2.next().print(t2);
          }
        } }, { key: "isAreaLabelsConsistent", value: function(t2) {
          return this.computeEdgeEndLabels(t2.getBoundaryNodeRule()), this.checkAreaLabelsConsistent(0);
        } }, { key: "checkAreaLabelsConsistent", value: function(t2) {
          var e2 = this.getEdges();
          if (e2.size() <= 0) return true;
          var n2 = e2.size() - 1, i2 = e2.get(n2).getLabel().getLocation(t2, $.LEFT);
          G.isTrue(i2 !== H.NONE, "Found unlabelled area edge");
          for (var r3 = i2, s2 = this.iterator(); s2.hasNext(); ) {
            var a2 = s2.next().getLabel();
            G.isTrue(a2.isArea(t2), "Found non-area edge");
            var o2 = a2.getLocation(t2, $.LEFT), u3 = a2.getLocation(t2, $.RIGHT);
            if (o2 === u3) return false;
            if (u3 !== r3) return false;
            r3 = o2;
          }
          return true;
        } }, { key: "findIndex", value: function(t2) {
          this.iterator();
          for (var e2 = 0; e2 < this._edgeList.size(); e2++) {
            if (this._edgeList.get(e2) === t2) return e2;
          }
          return -1;
        } }, { key: "iterator", value: function() {
          return this.getEdges().iterator();
        } }, { key: "getEdges", value: function() {
          return null === this._edgeList && (this._edgeList = new yt(this._edgeMap.values())), this._edgeList;
        } }, { key: "getLocation", value: function(t2, e2, n2) {
          return this._ptInAreaLocation[t2] === H.NONE && (this._ptInAreaLocation[t2] = Bn.locate(e2, n2[t2].getGeometry())), this._ptInAreaLocation[t2];
        } }, { key: "toString", value: function() {
          var t2 = new st();
          t2.append("EdgeEndStar:   " + this.getCoordinate()), t2.append("\n");
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            t2.append(n2), t2.append("\n");
          }
          return t2.toString();
        } }, { key: "computeEdgeEndLabels", value: function(t2) {
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            e2.next().computeLabel(t2);
          }
        } }, { key: "computeLabelling", value: function(t2) {
          this.computeEdgeEndLabels(t2[0].getBoundaryNodeRule()), this.propagateSideLabels(0), this.propagateSideLabels(1);
          for (var e2 = [false, false], n2 = this.iterator(); n2.hasNext(); ) for (var i2 = n2.next().getLabel(), r3 = 0; r3 < 2; r3++) i2.isLine(r3) && i2.getLocation(r3) === H.BOUNDARY && (e2[r3] = true);
          for (var s2 = this.iterator(); s2.hasNext(); ) for (var a2 = s2.next(), o2 = a2.getLabel(), u3 = 0; u3 < 2; u3++) if (o2.isAnyNull(u3)) {
            var l2 = H.NONE;
            if (e2[u3]) l2 = H.EXTERIOR;
            else {
              var h2 = a2.getCoordinate();
              l2 = this.getLocation(u3, h2, t2);
            }
            o2.setAllLocationsIfNull(u3, l2);
          }
        } }, { key: "getDegree", value: function() {
          return this._edgeMap.size();
        } }, { key: "insertEdgeEnd", value: function(t2, e2) {
          this._edgeMap.put(t2, e2), this._edgeList = null;
        } }], [{ key: "constructor_", value: function() {
          this._edgeMap = new He(), this._edgeList = null, this._ptInAreaLocation = [H.NONE, H.NONE];
        } }]);
      })(), Vn = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "linkResultDirectedEdges", value: function() {
          this.getResultAreaEdges();
          for (var t3 = null, e2 = null, n2 = this._SCANNING_FOR_INCOMING, i3 = 0; i3 < this._resultAreaEdgeList.size(); i3++) {
            var r3 = this._resultAreaEdgeList.get(i3), s2 = r3.getSym();
            if (r3.getLabel().isArea()) switch (null === t3 && r3.isInResult() && (t3 = r3), n2) {
              case this._SCANNING_FOR_INCOMING:
                if (!s2.isInResult()) continue;
                e2 = s2, n2 = this._LINKING_TO_OUTGOING;
                break;
              case this._LINKING_TO_OUTGOING:
                if (!r3.isInResult()) continue;
                e2.setNext(r3), n2 = this._SCANNING_FOR_INCOMING;
            }
          }
          if (n2 === this._LINKING_TO_OUTGOING) {
            if (null === t3) throw new gt("no outgoing dirEdge found", this.getCoordinate());
            G.isTrue(t3.isInResult(), "unable to link last incoming dirEdge"), e2.setNext(t3);
          }
        } }, { key: "insert", value: function(t3) {
          var e2 = t3;
          this.insertEdgeEnd(e2, e2);
        } }, { key: "getRightmostEdge", value: function() {
          var t3 = this.getEdges(), e2 = t3.size();
          if (e2 < 1) return null;
          var n2 = t3.get(0);
          if (1 === e2) return n2;
          var i3 = t3.get(e2 - 1), r3 = n2.getQuadrant(), s2 = i3.getQuadrant();
          return je.isNorthern(r3) && je.isNorthern(s2) ? n2 : je.isNorthern(r3) || je.isNorthern(s2) ? 0 !== n2.getDy() ? n2 : 0 !== i3.getDy() ? i3 : (G.shouldNeverReachHere("found two horizontal edges incident on node"), null) : i3;
        } }, { key: "print", value: function(t3) {
          mt.out.println("DirectedEdgeStar: " + this.getCoordinate());
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            var n2 = e2.next();
            t3.print("out "), n2.print(t3), t3.println(), t3.print("in "), n2.getSym().print(t3), t3.println();
          }
        } }, { key: "getResultAreaEdges", value: function() {
          if (null !== this._resultAreaEdgeList) return this._resultAreaEdgeList;
          this._resultAreaEdgeList = new yt();
          for (var t3 = this.iterator(); t3.hasNext(); ) {
            var e2 = t3.next();
            (e2.isInResult() || e2.getSym().isInResult()) && this._resultAreaEdgeList.add(e2);
          }
          return this._resultAreaEdgeList;
        } }, { key: "updateLabelling", value: function(t3) {
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            var n2 = e2.next().getLabel();
            n2.setAllLocationsIfNull(0, t3.getLocation(0)), n2.setAllLocationsIfNull(1, t3.getLocation(1));
          }
        } }, { key: "linkAllDirectedEdges", value: function() {
          this.getEdges();
          for (var t3 = null, e2 = null, n2 = this._edgeList.size() - 1; n2 >= 0; n2--) {
            var i3 = this._edgeList.get(n2), r3 = i3.getSym();
            null === e2 && (e2 = r3), null !== t3 && r3.setNext(t3), t3 = i3;
          }
          e2.setNext(t3);
        } }, { key: "computeDepths", value: function() {
          if (1 === arguments.length) {
            var t3 = arguments[0], e2 = this.findIndex(t3), n2 = t3.getDepth($.LEFT), i3 = t3.getDepth($.RIGHT), r3 = this.computeDepths(e2 + 1, this._edgeList.size(), n2);
            if (this.computeDepths(0, e2, r3) !== i3) throw new gt("depth mismatch at " + t3.getCoordinate());
          } else if (3 === arguments.length) {
            for (var s2 = arguments[1], a2 = arguments[2], o2 = arguments[0]; o2 < s2; o2++) {
              var u3 = this._edgeList.get(o2);
              u3.setEdgeDepths($.RIGHT, a2), a2 = u3.getDepth($.LEFT);
            }
            return a2;
          }
        } }, { key: "mergeSymLabels", value: function() {
          for (var t3 = this.iterator(); t3.hasNext(); ) {
            var e2 = t3.next();
            e2.getLabel().merge(e2.getSym().getLabel());
          }
        } }, { key: "linkMinimalDirectedEdges", value: function(t3) {
          for (var e2 = null, n2 = null, i3 = this._SCANNING_FOR_INCOMING, r3 = this._resultAreaEdgeList.size() - 1; r3 >= 0; r3--) {
            var s2 = this._resultAreaEdgeList.get(r3), a2 = s2.getSym();
            switch (null === e2 && s2.getEdgeRing() === t3 && (e2 = s2), i3) {
              case this._SCANNING_FOR_INCOMING:
                if (a2.getEdgeRing() !== t3) continue;
                n2 = a2, i3 = this._LINKING_TO_OUTGOING;
                break;
              case this._LINKING_TO_OUTGOING:
                if (s2.getEdgeRing() !== t3) continue;
                n2.setNextMin(s2), i3 = this._SCANNING_FOR_INCOMING;
            }
          }
          i3 === this._LINKING_TO_OUTGOING && (G.isTrue(null !== e2, "found null for first outgoing dirEdge"), G.isTrue(e2.getEdgeRing() === t3, "unable to link last incoming dirEdge"), n2.setNextMin(e2));
        } }, { key: "getOutgoingDegree", value: function() {
          if (0 === arguments.length) {
            for (var t3 = 0, e2 = this.iterator(); e2.hasNext(); ) {
              e2.next().isInResult() && t3++;
            }
            return t3;
          }
          if (1 === arguments.length) {
            for (var n2 = arguments[0], i3 = 0, r3 = this.iterator(); r3.hasNext(); ) {
              r3.next().getEdgeRing() === n2 && i3++;
            }
            return i3;
          }
        } }, { key: "getLabel", value: function() {
          return this._label;
        } }, { key: "findCoveredLineEdges", value: function() {
          for (var t3 = H.NONE, e2 = this.iterator(); e2.hasNext(); ) {
            var n2 = e2.next(), i3 = n2.getSym();
            if (!n2.isLineEdge()) {
              if (n2.isInResult()) {
                t3 = H.INTERIOR;
                break;
              }
              if (i3.isInResult()) {
                t3 = H.EXTERIOR;
                break;
              }
            }
          }
          if (t3 === H.NONE) return null;
          for (var r3 = t3, s2 = this.iterator(); s2.hasNext(); ) {
            var a2 = s2.next(), o2 = a2.getSym();
            a2.isLineEdge() ? a2.getEdge().setCovered(r3 === H.INTERIOR) : (a2.isInResult() && (r3 = H.EXTERIOR), o2.isInResult() && (r3 = H.INTERIOR));
          }
        } }, { key: "computeLabelling", value: function(t3) {
          f(i2, "computeLabelling", this, 1).call(this, t3), this._label = new Ae(H.NONE);
          for (var e2 = this.iterator(); e2.hasNext(); ) for (var n2 = e2.next().getEdge().getLabel(), r3 = 0; r3 < 2; r3++) {
            var s2 = n2.getLocation(r3);
            s2 !== H.INTERIOR && s2 !== H.BOUNDARY || this._label.setLocation(r3, H.INTERIOR);
          }
        } }], [{ key: "constructor_", value: function() {
          this._resultAreaEdgeList = null, this._label = null, this._SCANNING_FOR_INCOMING = 1, this._LINKING_TO_OUTGOING = 2;
        } }]);
      })(Un), Hn = (function(t2) {
        function i2() {
          return n(this, i2), e(this, i2);
        }
        return l(i2, t2), s(i2, [{ key: "createNode", value: function(t3) {
          return new qe(t3, new Vn());
        } }]);
      })(Je), Zn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "compareTo", value: function(e2) {
          var n2 = e2;
          return t2.compareOriented(this._pts, this._orientation, n2._pts, n2._orientation);
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          this._pts = null, this._orientation = null;
          var e2 = arguments[0];
          this._pts = e2, this._orientation = t2.orientation(e2);
        } }, { key: "orientation", value: function(t3) {
          return 1 === jt.increasingDirection(t3);
        } }, { key: "compareOriented", value: function(t3, e2, n2, i2) {
          for (var r3 = e2 ? 1 : -1, s2 = i2 ? 1 : -1, a2 = e2 ? t3.length : -1, o2 = i2 ? n2.length : -1, u3 = e2 ? 0 : t3.length - 1, l2 = i2 ? 0 : n2.length - 1; ; ) {
            var h2 = t3[u3].compareTo(n2[l2]);
            if (0 !== h2) return h2;
            var c2 = (u3 += r3) === a2, f2 = (l2 += s2) === o2;
            if (c2 && !f2) return -1;
            if (!c2 && f2) return 1;
            if (c2 && f2) return 0;
          }
        } }]);
      })(), jn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "print", value: function(t2) {
          t2.print("MULTILINESTRING ( ");
          for (var e2 = 0; e2 < this._edges.size(); e2++) {
            var n2 = this._edges.get(e2);
            e2 > 0 && t2.print(","), t2.print("(");
            for (var i2 = n2.getCoordinates(), r3 = 0; r3 < i2.length; r3++) r3 > 0 && t2.print(","), t2.print(i2[r3].x + " " + i2[r3].y);
            t2.println(")");
          }
          t2.print(")  ");
        } }, { key: "addAll", value: function(t2) {
          for (var e2 = t2.iterator(); e2.hasNext(); ) this.add(e2.next());
        } }, { key: "findEdgeIndex", value: function(t2) {
          for (var e2 = 0; e2 < this._edges.size(); e2++) if (this._edges.get(e2).equals(t2)) return e2;
          return -1;
        } }, { key: "iterator", value: function() {
          return this._edges.iterator();
        } }, { key: "getEdges", value: function() {
          return this._edges;
        } }, { key: "get", value: function(t2) {
          return this._edges.get(t2);
        } }, { key: "findEqualEdge", value: function(t2) {
          var e2 = new Zn(t2.getCoordinates());
          return this._ocaMap.get(e2);
        } }, { key: "add", value: function(t2) {
          this._edges.add(t2);
          var e2 = new Zn(t2.getCoordinates());
          this._ocaMap.put(e2, t2);
        } }], [{ key: "constructor_", value: function() {
          this._edges = new yt(), this._ocaMap = new He();
        } }]);
      })(), Wn = (function() {
        return s((function t2() {
          n(this, t2);
        }), [{ key: "processIntersections", value: function(t2, e2, n2, i2) {
        } }, { key: "isDone", value: function() {
        } }]);
      })(), Kn = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "isTrivialIntersection", value: function(e2, n2, i2, r3) {
          if (e2 === i2 && 1 === this._li.getIntersectionNum()) {
            if (t2.isAdjacentSegments(n2, r3)) return true;
            if (e2.isClosed()) {
              var s2 = e2.size() - 1;
              if (0 === n2 && r3 === s2 || 0 === r3 && n2 === s2) return true;
            }
          }
          return false;
        } }, { key: "getProperIntersectionPoint", value: function() {
          return this._properIntersectionPoint;
        } }, { key: "hasProperInteriorIntersection", value: function() {
          return this._hasProperInterior;
        } }, { key: "getLineIntersector", value: function() {
          return this._li;
        } }, { key: "hasProperIntersection", value: function() {
          return this._hasProper;
        } }, { key: "processIntersections", value: function(t3, e2, n2, i2) {
          if (t3 === n2 && e2 === i2) return null;
          this.numTests++;
          var r3 = t3.getCoordinates()[e2], s2 = t3.getCoordinates()[e2 + 1], a2 = n2.getCoordinates()[i2], o2 = n2.getCoordinates()[i2 + 1];
          this._li.computeIntersection(r3, s2, a2, o2), this._li.hasIntersection() && (this.numIntersections++, this._li.isInteriorIntersection() && (this.numInteriorIntersections++, this._hasInterior = true), this.isTrivialIntersection(t3, e2, n2, i2) || (this._hasIntersection = true, t3.addIntersections(this._li, e2, 0), n2.addIntersections(this._li, i2, 1), this._li.isProper() && (this.numProperIntersections++, this._hasProper = true, this._hasProperInterior = true)));
        } }, { key: "hasIntersection", value: function() {
          return this._hasIntersection;
        } }, { key: "isDone", value: function() {
          return false;
        } }, { key: "hasInteriorIntersection", value: function() {
          return this._hasInterior;
        } }, { key: "interfaces_", get: function() {
          return [Wn];
        } }], [{ key: "constructor_", value: function() {
          this._hasIntersection = false, this._hasProper = false, this._hasProperInterior = false, this._hasInterior = false, this._properIntersectionPoint = null, this._li = null, this._isSelfIntersection = null, this.numIntersections = 0, this.numInteriorIntersections = 0, this.numProperIntersections = 0, this.numTests = 0;
          var t3 = arguments[0];
          this._li = t3;
        } }, { key: "isAdjacentSegments", value: function(t3, e2) {
          return 1 === Math.abs(t3 - e2);
        } }]);
      })(), Jn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getSegmentIndex", value: function() {
          return this.segmentIndex;
        } }, { key: "getCoordinate", value: function() {
          return this.coord;
        } }, { key: "print", value: function(t2) {
          t2.print(this.coord), t2.print(" seg # = " + this.segmentIndex), t2.println(" dist = " + this.dist);
        } }, { key: "compareTo", value: function(t2) {
          var e2 = t2;
          return this.compare(e2.segmentIndex, e2.dist);
        } }, { key: "isEndPoint", value: function(t2) {
          return 0 === this.segmentIndex && 0 === this.dist || this.segmentIndex === t2;
        } }, { key: "toString", value: function() {
          return this.coord + " seg # = " + this.segmentIndex + " dist = " + this.dist;
        } }, { key: "getDistance", value: function() {
          return this.dist;
        } }, { key: "compare", value: function(t2, e2) {
          return this.segmentIndex < t2 ? -1 : this.segmentIndex > t2 ? 1 : this.dist < e2 ? -1 : this.dist > e2 ? 1 : 0;
        } }, { key: "interfaces_", get: function() {
          return [x];
        } }], [{ key: "constructor_", value: function() {
          this.coord = null, this.segmentIndex = null, this.dist = null;
          var t2 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          this.coord = new X(t2), this.segmentIndex = e2, this.dist = n2;
        } }]);
      })(), Qn = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "print", value: function(t2) {
          t2.println("Intersections:");
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            e2.next().print(t2);
          }
        } }, { key: "iterator", value: function() {
          return this._nodeMap.values().iterator();
        } }, { key: "addSplitEdges", value: function(t2) {
          this.addEndpoints();
          for (var e2 = this.iterator(), n2 = e2.next(); e2.hasNext(); ) {
            var i2 = e2.next(), r3 = this.createSplitEdge(n2, i2);
            t2.add(r3), n2 = i2;
          }
        } }, { key: "addEndpoints", value: function() {
          var t2 = this.edge.pts.length - 1;
          this.add(this.edge.pts[0], 0, 0), this.add(this.edge.pts[t2], t2, 0);
        } }, { key: "createSplitEdge", value: function(t2, e2) {
          var n2 = e2.segmentIndex - t2.segmentIndex + 2, i2 = this.edge.pts[e2.segmentIndex], r3 = e2.dist > 0 || !e2.coord.equals2D(i2);
          r3 || n2--;
          var s2 = new Array(n2).fill(null), a2 = 0;
          s2[a2++] = new X(t2.coord);
          for (var o2 = t2.segmentIndex + 1; o2 <= e2.segmentIndex; o2++) s2[a2++] = this.edge.pts[o2];
          return r3 && (s2[a2] = e2.coord), new ri(s2, new Ae(this.edge._label));
        } }, { key: "add", value: function(t2, e2, n2) {
          var i2 = new Jn(t2, e2, n2), r3 = this._nodeMap.get(i2);
          return null !== r3 ? r3 : (this._nodeMap.put(i2, i2), i2);
        } }, { key: "isIntersection", value: function(t2) {
          for (var e2 = this.iterator(); e2.hasNext(); ) {
            if (e2.next().coord.equals(t2)) return true;
          }
          return false;
        } }], [{ key: "constructor_", value: function() {
          this._nodeMap = new He(), this.edge = null;
          var t2 = arguments[0];
          this.edge = t2;
        } }]);
      })(), $n = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "isIntersects", value: function() {
          return !this.isDisjoint();
        } }, { key: "isCovers", value: function() {
          return (t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) || t2.isTrue(this._matrix[H.INTERIOR][H.BOUNDARY]) || t2.isTrue(this._matrix[H.BOUNDARY][H.INTERIOR]) || t2.isTrue(this._matrix[H.BOUNDARY][H.BOUNDARY])) && this._matrix[H.EXTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.BOUNDARY] === Lt.FALSE;
        } }, { key: "isCoveredBy", value: function() {
          return (t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) || t2.isTrue(this._matrix[H.INTERIOR][H.BOUNDARY]) || t2.isTrue(this._matrix[H.BOUNDARY][H.INTERIOR]) || t2.isTrue(this._matrix[H.BOUNDARY][H.BOUNDARY])) && this._matrix[H.INTERIOR][H.EXTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.EXTERIOR] === Lt.FALSE;
        } }, { key: "set", value: function() {
          if (1 === arguments.length) for (var t3 = arguments[0], e2 = 0; e2 < t3.length; e2++) {
            var n2 = Math.trunc(e2 / 3), i2 = e2 % 3;
            this._matrix[n2][i2] = Lt.toDimensionValue(t3.charAt(e2));
          }
          else if (3 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1], a2 = arguments[2];
            this._matrix[r3][s2] = a2;
          }
        } }, { key: "isContains", value: function() {
          return t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && this._matrix[H.EXTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.BOUNDARY] === Lt.FALSE;
        } }, { key: "setAtLeast", value: function() {
          if (1 === arguments.length) for (var t3 = arguments[0], e2 = 0; e2 < t3.length; e2++) {
            var n2 = Math.trunc(e2 / 3), i2 = e2 % 3;
            this.setAtLeast(n2, i2, Lt.toDimensionValue(t3.charAt(e2)));
          }
          else if (3 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1], a2 = arguments[2];
            this._matrix[r3][s2] < a2 && (this._matrix[r3][s2] = a2);
          }
        } }, { key: "setAtLeastIfValid", value: function(t3, e2, n2) {
          t3 >= 0 && e2 >= 0 && this.setAtLeast(t3, e2, n2);
        } }, { key: "isWithin", value: function() {
          return t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && this._matrix[H.INTERIOR][H.EXTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.EXTERIOR] === Lt.FALSE;
        } }, { key: "isTouches", value: function(e2, n2) {
          return e2 > n2 ? this.isTouches(n2, e2) : (e2 === Lt.A && n2 === Lt.A || e2 === Lt.L && n2 === Lt.L || e2 === Lt.L && n2 === Lt.A || e2 === Lt.P && n2 === Lt.A || e2 === Lt.P && n2 === Lt.L) && (this._matrix[H.INTERIOR][H.INTERIOR] === Lt.FALSE && (t2.isTrue(this._matrix[H.INTERIOR][H.BOUNDARY]) || t2.isTrue(this._matrix[H.BOUNDARY][H.INTERIOR]) || t2.isTrue(this._matrix[H.BOUNDARY][H.BOUNDARY])));
        } }, { key: "isOverlaps", value: function(e2, n2) {
          return e2 === Lt.P && n2 === Lt.P || e2 === Lt.A && n2 === Lt.A ? t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && t2.isTrue(this._matrix[H.INTERIOR][H.EXTERIOR]) && t2.isTrue(this._matrix[H.EXTERIOR][H.INTERIOR]) : e2 === Lt.L && n2 === Lt.L && (1 === this._matrix[H.INTERIOR][H.INTERIOR] && t2.isTrue(this._matrix[H.INTERIOR][H.EXTERIOR]) && t2.isTrue(this._matrix[H.EXTERIOR][H.INTERIOR]));
        } }, { key: "isEquals", value: function(e2, n2) {
          return e2 === n2 && (t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && this._matrix[H.INTERIOR][H.EXTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.EXTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.EXTERIOR][H.BOUNDARY] === Lt.FALSE);
        } }, { key: "toString", value: function() {
          for (var t3 = new Jt("123456789"), e2 = 0; e2 < 3; e2++) for (var n2 = 0; n2 < 3; n2++) t3.setCharAt(3 * e2 + n2, Lt.toDimensionSymbol(this._matrix[e2][n2]));
          return t3.toString();
        } }, { key: "setAll", value: function(t3) {
          for (var e2 = 0; e2 < 3; e2++) for (var n2 = 0; n2 < 3; n2++) this._matrix[e2][n2] = t3;
        } }, { key: "get", value: function(t3, e2) {
          return this._matrix[t3][e2];
        } }, { key: "transpose", value: function() {
          var t3 = this._matrix[1][0];
          return this._matrix[1][0] = this._matrix[0][1], this._matrix[0][1] = t3, t3 = this._matrix[2][0], this._matrix[2][0] = this._matrix[0][2], this._matrix[0][2] = t3, t3 = this._matrix[2][1], this._matrix[2][1] = this._matrix[1][2], this._matrix[1][2] = t3, this;
        } }, { key: "matches", value: function(e2) {
          if (9 !== e2.length) throw new m("Should be length 9: " + e2);
          for (var n2 = 0; n2 < 3; n2++) for (var i2 = 0; i2 < 3; i2++) if (!t2.matches(this._matrix[n2][i2], e2.charAt(3 * n2 + i2))) return false;
          return true;
        } }, { key: "add", value: function(t3) {
          for (var e2 = 0; e2 < 3; e2++) for (var n2 = 0; n2 < 3; n2++) this.setAtLeast(e2, n2, t3.get(e2, n2));
        } }, { key: "isDisjoint", value: function() {
          return this._matrix[H.INTERIOR][H.INTERIOR] === Lt.FALSE && this._matrix[H.INTERIOR][H.BOUNDARY] === Lt.FALSE && this._matrix[H.BOUNDARY][H.INTERIOR] === Lt.FALSE && this._matrix[H.BOUNDARY][H.BOUNDARY] === Lt.FALSE;
        } }, { key: "isCrosses", value: function(e2, n2) {
          return e2 === Lt.P && n2 === Lt.L || e2 === Lt.P && n2 === Lt.A || e2 === Lt.L && n2 === Lt.A ? t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && t2.isTrue(this._matrix[H.INTERIOR][H.EXTERIOR]) : e2 === Lt.L && n2 === Lt.P || e2 === Lt.A && n2 === Lt.P || e2 === Lt.A && n2 === Lt.L ? t2.isTrue(this._matrix[H.INTERIOR][H.INTERIOR]) && t2.isTrue(this._matrix[H.EXTERIOR][H.INTERIOR]) : e2 === Lt.L && n2 === Lt.L && 0 === this._matrix[H.INTERIOR][H.INTERIOR];
        } }, { key: "interfaces_", get: function() {
          return [I];
        } }], [{ key: "constructor_", value: function() {
          if (this._matrix = null, 0 === arguments.length) this._matrix = Array(3).fill().map((function() {
            return Array(3);
          })), this.setAll(Lt.FALSE);
          else if (1 === arguments.length) {
            if ("string" == typeof arguments[0]) {
              var e2 = arguments[0];
              t2.constructor_.call(this), this.set(e2);
            } else if (arguments[0] instanceof t2) {
              var n2 = arguments[0];
              t2.constructor_.call(this), this._matrix[H.INTERIOR][H.INTERIOR] = n2._matrix[H.INTERIOR][H.INTERIOR], this._matrix[H.INTERIOR][H.BOUNDARY] = n2._matrix[H.INTERIOR][H.BOUNDARY], this._matrix[H.INTERIOR][H.EXTERIOR] = n2._matrix[H.INTERIOR][H.EXTERIOR], this._matrix[H.BOUNDARY][H.INTERIOR] = n2._matrix[H.BOUNDARY][H.INTERIOR], this._matrix[H.BOUNDARY][H.BOUNDARY] = n2._matrix[H.BOUNDARY][H.BOUNDARY], this._matrix[H.BOUNDARY][H.EXTERIOR] = n2._matrix[H.BOUNDARY][H.EXTERIOR], this._matrix[H.EXTERIOR][H.INTERIOR] = n2._matrix[H.EXTERIOR][H.INTERIOR], this._matrix[H.EXTERIOR][H.BOUNDARY] = n2._matrix[H.EXTERIOR][H.BOUNDARY], this._matrix[H.EXTERIOR][H.EXTERIOR] = n2._matrix[H.EXTERIOR][H.EXTERIOR];
            }
          }
        } }, { key: "matches", value: function() {
          if (Number.isInteger(arguments[0]) && "string" == typeof arguments[1]) {
            var e2 = arguments[0], n2 = arguments[1];
            return n2 === Lt.SYM_DONTCARE || (n2 === Lt.SYM_TRUE && (e2 >= 0 || e2 === Lt.TRUE) || (n2 === Lt.SYM_FALSE && e2 === Lt.FALSE || (n2 === Lt.SYM_P && e2 === Lt.P || (n2 === Lt.SYM_L && e2 === Lt.L || n2 === Lt.SYM_A && e2 === Lt.A))));
          }
          if ("string" == typeof arguments[0] && "string" == typeof arguments[1]) {
            var i2 = arguments[1];
            return new t2(arguments[0]).matches(i2);
          }
        } }, { key: "isTrue", value: function(t3) {
          return t3 >= 0 || t3 === Lt.TRUE;
        } }]);
      })(), ti = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "size", value: function() {
          return this._size;
        } }, { key: "addAll", value: function(t3) {
          return null === t3 || 0 === t3.length ? null : (this.ensureCapacity(this._size + t3.length), mt.arraycopy(t3, 0, this._data, this._size, t3.length), void (this._size += t3.length));
        } }, { key: "ensureCapacity", value: function(t3) {
          if (t3 <= this._data.length) return null;
          var e2 = Math.max(t3, 2 * this._data.length);
          this._data = At.copyOf(this._data, e2);
        } }, { key: "toArray", value: function() {
          var t3 = new Array(this._size).fill(null);
          return mt.arraycopy(this._data, 0, t3, 0, this._size), t3;
        } }, { key: "add", value: function(t3) {
          this.ensureCapacity(this._size + 1), this._data[this._size] = t3, ++this._size;
        } }], [{ key: "constructor_", value: function() {
          if (this._data = null, this._size = 0, 0 === arguments.length) t2.constructor_.call(this, 10);
          else if (1 === arguments.length) {
            var e2 = arguments[0];
            this._data = new Array(e2).fill(null);
          }
        } }]);
      })(), ei = (function() {
        function t2() {
          n(this, t2);
        }
        return s(t2, [{ key: "getChainStartIndices", value: function(t3) {
          var e2 = 0, n2 = new ti(Math.trunc(t3.length / 2));
          n2.add(e2);
          do {
            var i2 = this.findChainEnd(t3, e2);
            n2.add(i2), e2 = i2;
          } while (e2 < t3.length - 1);
          return n2.toArray();
        } }, { key: "findChainEnd", value: function(t3, e2) {
          for (var n2 = je.quadrant(t3[e2], t3[e2 + 1]), i2 = e2 + 1; i2 < t3.length; ) {
            if (je.quadrant(t3[i2 - 1], t3[i2]) !== n2) break;
            i2++;
          }
          return i2 - 1;
        } }, { key: "OLDgetChainStartIndices", value: function(e2) {
          var n2 = 0, i2 = new yt();
          i2.add(n2);
          do {
            var r3 = this.findChainEnd(e2, n2);
            i2.add(r3), n2 = r3;
          } while (n2 < e2.length - 1);
          return t2.toIntArray(i2);
        } }], [{ key: "toIntArray", value: function(t3) {
          for (var e2 = new Array(t3.size()).fill(null), n2 = 0; n2 < e2.length; n2++) e2[n2] = t3.get(n2).intValue();
          return e2;
        } }]);
      })(), ni = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "getCoordinates", value: function() {
          return this.pts;
        } }, { key: "getMaxX", value: function(t2) {
          var e2 = this.pts[this.startIndex[t2]].x, n2 = this.pts[this.startIndex[t2 + 1]].x;
          return e2 > n2 ? e2 : n2;
        } }, { key: "getMinX", value: function(t2) {
          var e2 = this.pts[this.startIndex[t2]].x, n2 = this.pts[this.startIndex[t2 + 1]].x;
          return e2 < n2 ? e2 : n2;
        } }, { key: "computeIntersectsForChain", value: function() {
          if (4 === arguments.length) {
            var t2 = arguments[0], e2 = arguments[1], n2 = arguments[2], i2 = arguments[3];
            this.computeIntersectsForChain(this.startIndex[t2], this.startIndex[t2 + 1], e2, e2.startIndex[n2], e2.startIndex[n2 + 1], i2);
          } else if (6 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1], a2 = arguments[2], o2 = arguments[3], u3 = arguments[4], l2 = arguments[5];
            if (s2 - r3 == 1 && u3 - o2 == 1) return l2.addIntersections(this.e, r3, a2.e, o2), null;
            if (!this.overlaps(r3, s2, a2, o2, u3)) return null;
            var h2 = Math.trunc((r3 + s2) / 2), c2 = Math.trunc((o2 + u3) / 2);
            r3 < h2 && (o2 < c2 && this.computeIntersectsForChain(r3, h2, a2, o2, c2, l2), c2 < u3 && this.computeIntersectsForChain(r3, h2, a2, c2, u3, l2)), h2 < s2 && (o2 < c2 && this.computeIntersectsForChain(h2, s2, a2, o2, c2, l2), c2 < u3 && this.computeIntersectsForChain(h2, s2, a2, c2, u3, l2));
          }
        } }, { key: "overlaps", value: function(t2, e2, n2, i2, r3) {
          return U.intersects(this.pts[t2], this.pts[e2], n2.pts[i2], n2.pts[r3]);
        } }, { key: "getStartIndexes", value: function() {
          return this.startIndex;
        } }, { key: "computeIntersects", value: function(t2, e2) {
          for (var n2 = 0; n2 < this.startIndex.length - 1; n2++) for (var i2 = 0; i2 < t2.startIndex.length - 1; i2++) this.computeIntersectsForChain(n2, t2, i2, e2);
        } }], [{ key: "constructor_", value: function() {
          this.e = null, this.pts = null, this.startIndex = null;
          var t2 = arguments[0];
          this.e = t2, this.pts = t2.getCoordinates();
          var e2 = new ei();
          this.startIndex = e2.getChainStartIndices(this.pts);
        } }]);
      })(), ii = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "getDepth", value: function(t3, e2) {
          return this._depth[t3][e2];
        } }, { key: "setDepth", value: function(t3, e2, n2) {
          this._depth[t3][e2] = n2;
        } }, { key: "isNull", value: function() {
          if (0 === arguments.length) {
            for (var e2 = 0; e2 < 2; e2++) for (var n2 = 0; n2 < 3; n2++) if (this._depth[e2][n2] !== t2.NULL_VALUE) return false;
            return true;
          }
          if (1 === arguments.length) {
            var i2 = arguments[0];
            return this._depth[i2][1] === t2.NULL_VALUE;
          }
          if (2 === arguments.length) {
            var r3 = arguments[0], s2 = arguments[1];
            return this._depth[r3][s2] === t2.NULL_VALUE;
          }
        } }, { key: "normalize", value: function() {
          for (var t3 = 0; t3 < 2; t3++) if (!this.isNull(t3)) {
            var e2 = this._depth[t3][1];
            this._depth[t3][2] < e2 && (e2 = this._depth[t3][2]), e2 < 0 && (e2 = 0);
            for (var n2 = 1; n2 < 3; n2++) {
              var i2 = 0;
              this._depth[t3][n2] > e2 && (i2 = 1), this._depth[t3][n2] = i2;
            }
          }
        } }, { key: "getDelta", value: function(t3) {
          return this._depth[t3][$.RIGHT] - this._depth[t3][$.LEFT];
        } }, { key: "getLocation", value: function(t3, e2) {
          return this._depth[t3][e2] <= 0 ? H.EXTERIOR : H.INTERIOR;
        } }, { key: "toString", value: function() {
          return "A: " + this._depth[0][1] + "," + this._depth[0][2] + " B: " + this._depth[1][1] + "," + this._depth[1][2];
        } }, { key: "add", value: function() {
          if (1 === arguments.length) for (var e2 = arguments[0], n2 = 0; n2 < 2; n2++) for (var i2 = 1; i2 < 3; i2++) {
            var r3 = e2.getLocation(n2, i2);
            r3 !== H.EXTERIOR && r3 !== H.INTERIOR || (this.isNull(n2, i2) ? this._depth[n2][i2] = t2.depthAtLocation(r3) : this._depth[n2][i2] += t2.depthAtLocation(r3));
          }
          else if (3 === arguments.length) {
            var s2 = arguments[0], a2 = arguments[1];
            arguments[2] === H.INTERIOR && this._depth[s2][a2]++;
          }
        } }], [{ key: "constructor_", value: function() {
          this._depth = Array(2).fill().map((function() {
            return Array(3);
          }));
          for (var e2 = 0; e2 < 2; e2++) for (var n2 = 0; n2 < 3; n2++) this._depth[e2][n2] = t2.NULL_VALUE;
        } }, { key: "depthAtLocation", value: function(e2) {
          return e2 === H.EXTERIOR ? 0 : e2 === H.INTERIOR ? 1 : t2.NULL_VALUE;
        } }]);
      })();
      ii.NULL_VALUE = -1;
      var ri = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "getDepth", value: function() {
          return this._depth;
        } }, { key: "getCollapsedEdge", value: function() {
          var t3 = new Array(2).fill(null);
          return t3[0] = this.pts[0], t3[1] = this.pts[1], new i2(t3, Ae.toLineLabel(this._label));
        } }, { key: "isIsolated", value: function() {
          return this._isIsolated;
        } }, { key: "getCoordinates", value: function() {
          return this.pts;
        } }, { key: "setIsolated", value: function(t3) {
          this._isIsolated = t3;
        } }, { key: "setName", value: function(t3) {
          this._name = t3;
        } }, { key: "equals", value: function(t3) {
          if (!(t3 instanceof i2)) return false;
          var e2 = t3;
          if (this.pts.length !== e2.pts.length) return false;
          for (var n2 = true, r3 = true, s2 = this.pts.length, a2 = 0; a2 < this.pts.length; a2++) if (this.pts[a2].equals2D(e2.pts[a2]) || (n2 = false), this.pts[a2].equals2D(e2.pts[--s2]) || (r3 = false), !n2 && !r3) return false;
          return true;
        } }, { key: "getCoordinate", value: function() {
          if (0 === arguments.length) return this.pts.length > 0 ? this.pts[0] : null;
          if (1 === arguments.length) {
            var t3 = arguments[0];
            return this.pts[t3];
          }
        } }, { key: "print", value: function(t3) {
          t3.print("edge " + this._name + ": "), t3.print("LINESTRING (");
          for (var e2 = 0; e2 < this.pts.length; e2++) e2 > 0 && t3.print(","), t3.print(this.pts[e2].x + " " + this.pts[e2].y);
          t3.print(")  " + this._label + " " + this._depthDelta);
        } }, { key: "computeIM", value: function(t3) {
          i2.updateIM(this._label, t3);
        } }, { key: "isCollapsed", value: function() {
          return !!this._label.isArea() && (3 === this.pts.length && !!this.pts[0].equals(this.pts[2]));
        } }, { key: "isClosed", value: function() {
          return this.pts[0].equals(this.pts[this.pts.length - 1]);
        } }, { key: "getMaximumSegmentIndex", value: function() {
          return this.pts.length - 1;
        } }, { key: "getDepthDelta", value: function() {
          return this._depthDelta;
        } }, { key: "getNumPoints", value: function() {
          return this.pts.length;
        } }, { key: "printReverse", value: function(t3) {
          t3.print("edge " + this._name + ": ");
          for (var e2 = this.pts.length - 1; e2 >= 0; e2--) t3.print(this.pts[e2] + " ");
          t3.println("");
        } }, { key: "getMonotoneChainEdge", value: function() {
          return null === this._mce && (this._mce = new ni(this)), this._mce;
        } }, { key: "getEnvelope", value: function() {
          if (null === this._env) {
            this._env = new U();
            for (var t3 = 0; t3 < this.pts.length; t3++) this._env.expandToInclude(this.pts[t3]);
          }
          return this._env;
        } }, { key: "addIntersection", value: function(t3, e2, n2, i3) {
          var r3 = new X(t3.getIntersection(i3)), s2 = e2, a2 = t3.getEdgeDistance(n2, i3), o2 = s2 + 1;
          if (o2 < this.pts.length) {
            var u3 = this.pts[o2];
            r3.equals2D(u3) && (s2 = o2, a2 = 0);
          }
          this.eiList.add(r3, s2, a2);
        } }, { key: "toString", value: function() {
          var t3 = new Jt();
          t3.append("edge " + this._name + ": "), t3.append("LINESTRING (");
          for (var e2 = 0; e2 < this.pts.length; e2++) e2 > 0 && t3.append(","), t3.append(this.pts[e2].x + " " + this.pts[e2].y);
          return t3.append(")  " + this._label + " " + this._depthDelta), t3.toString();
        } }, { key: "isPointwiseEqual", value: function(t3) {
          if (this.pts.length !== t3.pts.length) return false;
          for (var e2 = 0; e2 < this.pts.length; e2++) if (!this.pts[e2].equals2D(t3.pts[e2])) return false;
          return true;
        } }, { key: "setDepthDelta", value: function(t3) {
          this._depthDelta = t3;
        } }, { key: "getEdgeIntersectionList", value: function() {
          return this.eiList;
        } }, { key: "addIntersections", value: function(t3, e2, n2) {
          for (var i3 = 0; i3 < t3.getIntersectionNum(); i3++) this.addIntersection(t3, e2, n2, i3);
        } }], [{ key: "constructor_", value: function() {
          if (this.pts = null, this._env = null, this.eiList = new Qn(this), this._name = null, this._mce = null, this._isIsolated = true, this._depth = new ii(), this._depthDelta = 0, 1 === arguments.length) {
            var t3 = arguments[0];
            i2.constructor_.call(this, t3, null);
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            this.pts = e2, this._label = n2;
          }
        } }, { key: "updateIM", value: function() {
          if (!(2 === arguments.length && arguments[1] instanceof $n && arguments[0] instanceof Ae)) return f(i2, "updateIM", this).apply(this, arguments);
          var t3 = arguments[0], e2 = arguments[1];
          e2.setAtLeastIfValid(t3.getLocation(0, $.ON), t3.getLocation(1, $.ON), 1), t3.isArea() && (e2.setAtLeastIfValid(t3.getLocation(0, $.LEFT), t3.getLocation(1, $.LEFT), 2), e2.setAtLeastIfValid(t3.getLocation(0, $.RIGHT), t3.getLocation(1, $.RIGHT), 2));
        } }]);
      })(Ge), si = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "setWorkingPrecisionModel", value: function(t3) {
          this._workingPrecisionModel = t3;
        } }, { key: "insertUniqueEdge", value: function(e2) {
          var n2 = this._edgeList.findEqualEdge(e2);
          if (null !== n2) {
            var i2 = n2.getLabel(), r3 = e2.getLabel();
            n2.isPointwiseEqual(e2) || (r3 = new Ae(e2.getLabel())).flip(), i2.merge(r3);
            var s2 = t2.depthDelta(r3), a2 = n2.getDepthDelta() + s2;
            n2.setDepthDelta(a2);
          } else this._edgeList.add(e2), e2.setDepthDelta(t2.depthDelta(e2.getLabel()));
        } }, { key: "buildSubgraphs", value: function(t3, e2) {
          for (var n2 = new yt(), i2 = t3.iterator(); i2.hasNext(); ) {
            var r3 = i2.next(), s2 = r3.getRightmostCoordinate(), a2 = new Pn(n2).getDepth(s2);
            r3.computeDepth(a2), r3.findResultEdges(), n2.add(r3), e2.add(r3.getDirectedEdges(), r3.getNodes());
          }
        } }, { key: "createSubgraphs", value: function(t3) {
          for (var e2 = new yt(), n2 = t3.getNodes().iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            if (!i2.isVisited()) {
              var r3 = new _t();
              r3.create(i2), e2.add(r3);
            }
          }
          return an.sort(e2, an.reverseOrder()), e2;
        } }, { key: "createEmptyResultGeometry", value: function() {
          return this._geomFact.createPolygon();
        } }, { key: "getNoder", value: function(t3) {
          if (null !== this._workingNoder) return this._workingNoder;
          var e2 = new Cn(), n2 = new we();
          return n2.setPrecisionModel(t3), e2.setSegmentIntersector(new Kn(n2)), e2;
        } }, { key: "buffer", value: function(t3, e2) {
          var n2 = this._workingPrecisionModel;
          null === n2 && (n2 = t3.getPrecisionModel()), this._geomFact = t3.getFactory();
          var i2 = new An(n2, this._bufParams), r3 = new Yn(t3, e2, i2).getCurves();
          if (r3.size() <= 0) return this.createEmptyResultGeometry();
          this.computeNodedEdges(r3, n2), this._graph = new Qe(new Hn()), this._graph.addEdges(this._edgeList.getEdges());
          var s2 = this.createSubgraphs(this._graph), a2 = new $e(this._geomFact);
          this.buildSubgraphs(s2, a2);
          var o2 = a2.getPolygons();
          return o2.size() <= 0 ? this.createEmptyResultGeometry() : this._geomFact.buildGeometry(o2);
        } }, { key: "computeNodedEdges", value: function(t3, e2) {
          var n2 = this.getNoder(e2);
          n2.computeNodes(t3);
          for (var i2 = n2.getNodedSubstrings().iterator(); i2.hasNext(); ) {
            var r3 = i2.next(), s2 = r3.getCoordinates();
            if (2 !== s2.length || !s2[0].equals2D(s2[1])) {
              var a2 = r3.getData(), o2 = new ri(r3.getCoordinates(), new Ae(a2));
              this.insertUniqueEdge(o2);
            }
          }
        } }, { key: "setNoder", value: function(t3) {
          this._workingNoder = t3;
        } }], [{ key: "constructor_", value: function() {
          this._bufParams = null, this._workingPrecisionModel = null, this._workingNoder = null, this._geomFact = null, this._graph = null, this._edgeList = new jn();
          var t3 = arguments[0];
          this._bufParams = t3;
        } }, { key: "depthDelta", value: function(t3) {
          var e2 = t3.getLocation(0, $.LEFT), n2 = t3.getLocation(0, $.RIGHT);
          return e2 === H.INTERIOR && n2 === H.EXTERIOR ? 1 : e2 === H.EXTERIOR && n2 === H.INTERIOR ? -1 : 0;
        } }, { key: "convertSegStrings", value: function(t3) {
          for (var e2 = new ae(), n2 = new yt(); t3.hasNext(); ) {
            var i2 = t3.next(), r3 = e2.createLineString(i2.getCoordinates());
            n2.add(r3);
          }
          return e2.buildGeometry(n2);
        } }]);
      })(), ai = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "rescale", value: function() {
          if (rt(arguments[0], Z)) for (var t3 = arguments[0].iterator(); t3.hasNext(); ) {
            var e2 = t3.next();
            this.rescale(e2.getCoordinates());
          }
          else if (arguments[0] instanceof Array) {
            for (var n2 = arguments[0], i2 = 0; i2 < n2.length; i2++) n2[i2].x = n2[i2].x / this._scaleFactor + this._offsetX, n2[i2].y = n2[i2].y / this._scaleFactor + this._offsetY;
            2 === n2.length && n2[0].equals2D(n2[1]) && mt.out.println(n2);
          }
        } }, { key: "scale", value: function() {
          if (rt(arguments[0], Z)) {
            for (var t3 = arguments[0], e2 = new yt(t3.size()), n2 = t3.iterator(); n2.hasNext(); ) {
              var i2 = n2.next();
              e2.add(new xn(this.scale(i2.getCoordinates()), i2.getData()));
            }
            return e2;
          }
          if (arguments[0] instanceof Array) {
            for (var r3 = arguments[0], s2 = new Array(r3.length).fill(null), a2 = 0; a2 < r3.length; a2++) s2[a2] = new X(Math.round((r3[a2].x - this._offsetX) * this._scaleFactor), Math.round((r3[a2].y - this._offsetY) * this._scaleFactor), r3[a2].getZ());
            return jt.removeRepeatedPoints(s2);
          }
        } }, { key: "isIntegerPrecision", value: function() {
          return 1 === this._scaleFactor;
        } }, { key: "getNodedSubstrings", value: function() {
          var t3 = this._noder.getNodedSubstrings();
          return this._isScaled && this.rescale(t3), t3;
        } }, { key: "computeNodes", value: function(t3) {
          var e2 = t3;
          this._isScaled && (e2 = this.scale(t3)), this._noder.computeNodes(e2);
        } }, { key: "interfaces_", get: function() {
          return [Sn];
        } }], [{ key: "constructor_", value: function() {
          if (this._noder = null, this._scaleFactor = null, this._offsetX = null, this._offsetY = null, this._isScaled = false, 2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            t2.constructor_.call(this, e2, n2, 0, 0);
          } else if (4 === arguments.length) {
            var i2 = arguments[0], r3 = arguments[1];
            this._noder = i2, this._scaleFactor = r3, this._isScaled = !this.isIntegerPrecision();
          }
        } }]);
      })(), oi = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "checkEndPtVertexIntersections", value: function() {
          if (0 === arguments.length) for (var t3 = this._segStrings.iterator(); t3.hasNext(); ) {
            var e2 = t3.next().getCoordinates();
            this.checkEndPtVertexIntersections(e2[0], this._segStrings), this.checkEndPtVertexIntersections(e2[e2.length - 1], this._segStrings);
          }
          else if (2 === arguments.length) {
            for (var n2 = arguments[0], i2 = arguments[1].iterator(); i2.hasNext(); ) for (var r3 = i2.next().getCoordinates(), s2 = 1; s2 < r3.length - 1; s2++) if (r3[s2].equals(n2)) throw new D2("found endpt/interior pt intersection at index " + s2 + " :pt " + n2);
          }
        } }, { key: "checkInteriorIntersections", value: function() {
          if (0 === arguments.length) for (var t3 = this._segStrings.iterator(); t3.hasNext(); ) for (var e2 = t3.next(), n2 = this._segStrings.iterator(); n2.hasNext(); ) {
            var i2 = n2.next();
            this.checkInteriorIntersections(e2, i2);
          }
          else if (2 === arguments.length) for (var r3 = arguments[0], s2 = arguments[1], a2 = r3.getCoordinates(), o2 = s2.getCoordinates(), u3 = 0; u3 < a2.length - 1; u3++) for (var l2 = 0; l2 < o2.length - 1; l2++) this.checkInteriorIntersections(r3, u3, s2, l2);
          else if (4 === arguments.length) {
            var h2 = arguments[0], c2 = arguments[1], f2 = arguments[2], g2 = arguments[3];
            if (h2 === f2 && c2 === g2) return null;
            var v2 = h2.getCoordinates()[c2], y2 = h2.getCoordinates()[c2 + 1], d2 = f2.getCoordinates()[g2], _2 = f2.getCoordinates()[g2 + 1];
            if (this._li.computeIntersection(v2, y2, d2, _2), this._li.hasIntersection() && (this._li.isProper() || this.hasInteriorIntersection(this._li, v2, y2) || this.hasInteriorIntersection(this._li, d2, _2))) throw new D2("found non-noded intersection at " + v2 + "-" + y2 + " and " + d2 + "-" + _2);
          }
        } }, { key: "checkValid", value: function() {
          this.checkEndPtVertexIntersections(), this.checkInteriorIntersections(), this.checkCollapses();
        } }, { key: "checkCollapses", value: function() {
          if (0 === arguments.length) for (var t3 = this._segStrings.iterator(); t3.hasNext(); ) {
            var e2 = t3.next();
            this.checkCollapses(e2);
          }
          else if (1 === arguments.length) for (var n2 = arguments[0].getCoordinates(), i2 = 0; i2 < n2.length - 2; i2++) this.checkCollapse(n2[i2], n2[i2 + 1], n2[i2 + 2]);
        } }, { key: "hasInteriorIntersection", value: function(t3, e2, n2) {
          for (var i2 = 0; i2 < t3.getIntersectionNum(); i2++) {
            var r3 = t3.getIntersection(i2);
            if (!r3.equals(e2) && !r3.equals(n2)) return true;
          }
          return false;
        } }, { key: "checkCollapse", value: function(e2, n2, i2) {
          if (e2.equals(i2)) throw new D2("found non-noded collapse at " + t2.fact.createLineString([e2, n2, i2]));
        } }], [{ key: "constructor_", value: function() {
          this._li = new we(), this._segStrings = null;
          var t3 = arguments[0];
          this._segStrings = t3;
        } }]);
      })();
      oi.fact = new ae();
      var ui = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "intersectsScaled", value: function(t3, e2) {
          var n2 = Math.min(t3.x, e2.x), i2 = Math.max(t3.x, e2.x), r3 = Math.min(t3.y, e2.y), s2 = Math.max(t3.y, e2.y), a2 = this._maxx < n2 || this._minx > i2 || this._maxy < r3 || this._miny > s2;
          if (a2) return false;
          var o2 = this.intersectsToleranceSquare(t3, e2);
          return G.isTrue(!(a2 && o2), "Found bad envelope test"), o2;
        } }, { key: "initCorners", value: function(t3) {
          var e2 = 0.5;
          this._minx = t3.x - e2, this._maxx = t3.x + e2, this._miny = t3.y - e2, this._maxy = t3.y + e2, this._corner[0] = new X(this._maxx, this._maxy), this._corner[1] = new X(this._minx, this._maxy), this._corner[2] = new X(this._minx, this._miny), this._corner[3] = new X(this._maxx, this._miny);
        } }, { key: "intersects", value: function(t3, e2) {
          return 1 === this._scaleFactor ? this.intersectsScaled(t3, e2) : (this.copyScaled(t3, this._p0Scaled), this.copyScaled(e2, this._p1Scaled), this.intersectsScaled(this._p0Scaled, this._p1Scaled));
        } }, { key: "scale", value: function(t3) {
          return Math.round(t3 * this._scaleFactor);
        } }, { key: "getCoordinate", value: function() {
          return this._originalPt;
        } }, { key: "copyScaled", value: function(t3, e2) {
          e2.x = this.scale(t3.x), e2.y = this.scale(t3.y);
        } }, { key: "getSafeEnvelope", value: function() {
          if (null === this._safeEnv) {
            var e2 = t2.SAFE_ENV_EXPANSION_FACTOR / this._scaleFactor;
            this._safeEnv = new U(this._originalPt.x - e2, this._originalPt.x + e2, this._originalPt.y - e2, this._originalPt.y + e2);
          }
          return this._safeEnv;
        } }, { key: "intersectsPixelClosure", value: function(t3, e2) {
          return this._li.computeIntersection(t3, e2, this._corner[0], this._corner[1]), !!this._li.hasIntersection() || (this._li.computeIntersection(t3, e2, this._corner[1], this._corner[2]), !!this._li.hasIntersection() || (this._li.computeIntersection(t3, e2, this._corner[2], this._corner[3]), !!this._li.hasIntersection() || (this._li.computeIntersection(t3, e2, this._corner[3], this._corner[0]), !!this._li.hasIntersection())));
        } }, { key: "intersectsToleranceSquare", value: function(t3, e2) {
          var n2 = false, i2 = false;
          return this._li.computeIntersection(t3, e2, this._corner[0], this._corner[1]), !!this._li.isProper() || (this._li.computeIntersection(t3, e2, this._corner[1], this._corner[2]), !!this._li.isProper() || (this._li.hasIntersection() && (n2 = true), this._li.computeIntersection(t3, e2, this._corner[2], this._corner[3]), !!this._li.isProper() || (this._li.hasIntersection() && (i2 = true), this._li.computeIntersection(t3, e2, this._corner[3], this._corner[0]), !!this._li.isProper() || (!(!n2 || !i2) || (!!t3.equals(this._pt) || !!e2.equals(this._pt))))));
        } }, { key: "addSnappedNode", value: function(t3, e2) {
          var n2 = t3.getCoordinate(e2), i2 = t3.getCoordinate(e2 + 1);
          return !!this.intersects(n2, i2) && (t3.addIntersection(this.getCoordinate(), e2), true);
        } }], [{ key: "constructor_", value: function() {
          this._li = null, this._pt = null, this._originalPt = null, this._ptScaled = null, this._p0Scaled = null, this._p1Scaled = null, this._scaleFactor = null, this._minx = null, this._maxx = null, this._miny = null, this._maxy = null, this._corner = new Array(4).fill(null), this._safeEnv = null;
          var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          if (this._originalPt = t3, this._pt = t3, this._scaleFactor = e2, this._li = n2, e2 <= 0) throw new m("Scale factor must be non-zero");
          1 !== e2 && (this._pt = new X(this.scale(t3.x), this.scale(t3.y)), this._p0Scaled = new X(), this._p1Scaled = new X()), this.initCorners(this._pt);
        } }]);
      })();
      ui.SAFE_ENV_EXPANSION_FACTOR = 0.75;
      var li = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "select", value: function() {
          if (1 === arguments.length) ;
          else if (2 === arguments.length) {
            var t2 = arguments[1];
            arguments[0].getLineSegment(t2, this.selectedSegment), this.select(this.selectedSegment);
          }
        } }], [{ key: "constructor_", value: function() {
          this.selectedSegment = new In();
        } }]);
      })(), hi = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "snap", value: function() {
          if (1 === arguments.length) {
            var t2 = arguments[0];
            return this.snap(t2, null, -1);
          }
          if (3 === arguments.length) {
            var e2 = arguments[0], i2 = arguments[1], r3 = arguments[2], a2 = e2.getSafeEnvelope(), o2 = new ci(e2, i2, r3);
            return this._index.query(a2, new ((function() {
              return s((function t3() {
                n(this, t3);
              }), [{ key: "interfaces_", get: function() {
                return [ln];
              } }, { key: "visitItem", value: function(t3) {
                t3.select(a2, o2);
              } }]);
            })())()), o2.isNodeAdded();
          }
        } }], [{ key: "constructor_", value: function() {
          this._index = null;
          var t2 = arguments[0];
          this._index = t2;
        } }]);
      })(), ci = (function(t2) {
        function i2() {
          var t3;
          return n(this, i2), t3 = e(this, i2), i2.constructor_.apply(t3, arguments), t3;
        }
        return l(i2, t2), s(i2, [{ key: "isNodeAdded", value: function() {
          return this._isNodeAdded;
        } }, { key: "select", value: function() {
          if (!(2 === arguments.length && Number.isInteger(arguments[1]) && arguments[0] instanceof Nn)) return f(i2, "select", this, 1).apply(this, arguments);
          var t3 = arguments[1], e2 = arguments[0].getContext();
          if (this._parentEdge === e2 && (t3 === this._hotPixelVertexIndex || t3 + 1 === this._hotPixelVertexIndex)) return null;
          this._isNodeAdded |= this._hotPixel.addSnappedNode(e2, t3);
        } }], [{ key: "constructor_", value: function() {
          this._hotPixel = null, this._parentEdge = null, this._hotPixelVertexIndex = null, this._isNodeAdded = false;
          var t3 = arguments[0], e2 = arguments[1], n2 = arguments[2];
          this._hotPixel = t3, this._parentEdge = e2, this._hotPixelVertexIndex = n2;
        } }]);
      })(li);
      hi.HotPixelSnapAction = ci;
      var fi = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "processIntersections", value: function(t2, e2, n2, i2) {
          if (t2 === n2 && e2 === i2) return null;
          var r3 = t2.getCoordinates()[e2], s2 = t2.getCoordinates()[e2 + 1], a2 = n2.getCoordinates()[i2], o2 = n2.getCoordinates()[i2 + 1];
          if (this._li.computeIntersection(r3, s2, a2, o2), this._li.hasIntersection() && this._li.isInteriorIntersection()) {
            for (var u3 = 0; u3 < this._li.getIntersectionNum(); u3++) this._interiorIntersections.add(this._li.getIntersection(u3));
            t2.addIntersections(this._li, e2, 0), n2.addIntersections(this._li, i2, 1);
          }
        } }, { key: "isDone", value: function() {
          return false;
        } }, { key: "getInteriorIntersections", value: function() {
          return this._interiorIntersections;
        } }, { key: "interfaces_", get: function() {
          return [Wn];
        } }], [{ key: "constructor_", value: function() {
          this._li = null, this._interiorIntersections = null;
          var t2 = arguments[0];
          this._li = t2, this._interiorIntersections = new yt();
        } }]);
      })(), gi = (function() {
        return s((function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }), [{ key: "checkCorrectness", value: function(t2) {
          var e2 = xn.getNodedSubstrings(t2), n2 = new oi(e2);
          try {
            n2.checkValid();
          } catch (t3) {
            if (!(t3 instanceof p)) throw t3;
            t3.printStackTrace();
          }
        } }, { key: "getNodedSubstrings", value: function() {
          return xn.getNodedSubstrings(this._nodedSegStrings);
        } }, { key: "snapRound", value: function(t2, e2) {
          var n2 = this.findInteriorIntersections(t2, e2);
          this.computeIntersectionSnaps(n2), this.computeVertexSnaps(t2);
        } }, { key: "findInteriorIntersections", value: function(t2, e2) {
          var n2 = new fi(e2);
          return this._noder.setSegmentIntersector(n2), this._noder.computeNodes(t2), n2.getInteriorIntersections();
        } }, { key: "computeVertexSnaps", value: function() {
          if (rt(arguments[0], Z)) for (var t2 = arguments[0].iterator(); t2.hasNext(); ) {
            var e2 = t2.next();
            this.computeVertexSnaps(e2);
          }
          else if (arguments[0] instanceof xn) for (var n2 = arguments[0], i2 = n2.getCoordinates(), r3 = 0; r3 < i2.length; r3++) {
            var s2 = new ui(i2[r3], this._scaleFactor, this._li);
            this._pointSnapper.snap(s2, n2, r3) && n2.addIntersection(i2[r3], r3);
          }
        } }, { key: "computeNodes", value: function(t2) {
          this._nodedSegStrings = t2, this._noder = new Cn(), this._pointSnapper = new hi(this._noder.getIndex()), this.snapRound(t2, this._li);
        } }, { key: "computeIntersectionSnaps", value: function(t2) {
          for (var e2 = t2.iterator(); e2.hasNext(); ) {
            var n2 = e2.next(), i2 = new ui(n2, this._scaleFactor, this._li);
            this._pointSnapper.snap(i2);
          }
        } }, { key: "interfaces_", get: function() {
          return [Sn];
        } }], [{ key: "constructor_", value: function() {
          this._pm = null, this._li = null, this._scaleFactor = null, this._noder = null, this._pointSnapper = null, this._nodedSegStrings = null;
          var t2 = arguments[0];
          this._pm = t2, this._li = new we(), this._li.setPrecisionModel(t2), this._scaleFactor = t2.getScale();
        } }]);
      })(), vi = (function() {
        function t2() {
          n(this, t2), t2.constructor_.apply(this, arguments);
        }
        return s(t2, [{ key: "bufferFixedPrecision", value: function(t3) {
          var e2 = new ai(new gi(new ie(1)), t3.getScale()), n2 = new si(this._bufParams);
          n2.setWorkingPrecisionModel(t3), n2.setNoder(e2), this._resultGeometry = n2.buffer(this._argGeom, this._distance);
        } }, { key: "bufferReducedPrecision", value: function() {
          if (0 === arguments.length) {
            for (var e2 = t2.MAX_PRECISION_DIGITS; e2 >= 0; e2--) {
              try {
                this.bufferReducedPrecision(e2);
              } catch (t3) {
                if (!(t3 instanceof gt)) throw t3;
                this._saveException = t3;
              }
              if (null !== this._resultGeometry) return null;
            }
            throw this._saveException;
          }
          if (1 === arguments.length) {
            var n2 = arguments[0], i2 = t2.precisionScaleFactor(this._argGeom, this._distance, n2), r3 = new ie(i2);
            this.bufferFixedPrecision(r3);
          }
        } }, { key: "computeGeometry", value: function() {
          if (this.bufferOriginalPrecision(), null !== this._resultGeometry) return null;
          var t3 = this._argGeom.getFactory().getPrecisionModel();
          t3.getType() === ie.FIXED ? this.bufferFixedPrecision(t3) : this.bufferReducedPrecision();
        } }, { key: "setQuadrantSegments", value: function(t3) {
          this._bufParams.setQuadrantSegments(t3);
        } }, { key: "bufferOriginalPrecision", value: function() {
          try {
            var t3 = new si(this._bufParams);
            this._resultGeometry = t3.buffer(this._argGeom, this._distance);
          } catch (t4) {
            if (!(t4 instanceof D2)) throw t4;
            this._saveException = t4;
          }
        } }, { key: "getResultGeometry", value: function(t3) {
          return this._distance = t3, this.computeGeometry(), this._resultGeometry;
        } }, { key: "setEndCapStyle", value: function(t3) {
          this._bufParams.setEndCapStyle(t3);
        } }], [{ key: "constructor_", value: function() {
          if (this._argGeom = null, this._distance = null, this._bufParams = new _(), this._resultGeometry = null, this._saveException = null, 1 === arguments.length) {
            var t3 = arguments[0];
            this._argGeom = t3;
          } else if (2 === arguments.length) {
            var e2 = arguments[0], n2 = arguments[1];
            this._argGeom = e2, this._bufParams = n2;
          }
        } }, { key: "bufferOp", value: function() {
          if (2 === arguments.length) {
            var e2 = arguments[1];
            return new t2(arguments[0]).getResultGeometry(e2);
          }
          if (3 === arguments.length) {
            if (Number.isInteger(arguments[2]) && arguments[0] instanceof V && "number" == typeof arguments[1]) {
              var n2 = arguments[1], i2 = arguments[2], r3 = new t2(arguments[0]);
              return r3.setQuadrantSegments(i2), r3.getResultGeometry(n2);
            }
            if (arguments[2] instanceof _ && arguments[0] instanceof V && "number" == typeof arguments[1]) {
              var s2 = arguments[1];
              return new t2(arguments[0], arguments[2]).getResultGeometry(s2);
            }
          } else if (4 === arguments.length) {
            var a2 = arguments[1], o2 = arguments[2], u3 = arguments[3], l2 = new t2(arguments[0]);
            return l2.setQuadrantSegments(o2), l2.setEndCapStyle(u3), l2.getResultGeometry(a2);
          }
        } }, { key: "precisionScaleFactor", value: function(t3, e2, n2) {
          var i2 = t3.getEnvelopeInternal(), r3 = kt.max(Math.abs(i2.getMaxX()), Math.abs(i2.getMaxY()), Math.abs(i2.getMinX()), Math.abs(i2.getMinY())) + 2 * (e2 > 0 ? e2 : 0), s2 = n2 - Math.trunc(Math.log(r3) / Math.log(10) + 1);
          return Math.pow(10, s2);
        } }]);
      })();
      vi.CAP_ROUND = _.CAP_ROUND, vi.CAP_BUTT = _.CAP_FLAT, vi.CAP_FLAT = _.CAP_FLAT, vi.CAP_SQUARE = _.CAP_SQUARE, vi.MAX_PRECISION_DIGITS = 12;
      var yi = ["Point", "MultiPoint", "LineString", "MultiLineString", "Polygon", "MultiPolygon"], di = (function() {
        return s((function t2(e2) {
          n(this, t2), this.geometryFactory = e2 || new ae();
        }), [{ key: "read", value: function(t2) {
          var e2, n2 = (e2 = "string" == typeof t2 ? JSON.parse(t2) : t2).type;
          if (!_i[n2]) throw new Error("Unknown GeoJSON type: " + e2.type);
          return -1 !== yi.indexOf(n2) ? _i[n2].call(this, e2.coordinates) : "GeometryCollection" === n2 ? _i[n2].call(this, e2.geometries) : _i[n2].call(this, e2);
        } }, { key: "write", value: function(t2) {
          var e2 = t2.getGeometryType();
          if (!pi2[e2]) throw new Error("Geometry is not supported");
          return pi2[e2].call(this, t2);
        } }]);
      })(), _i = { Feature: function(t2) {
        var e2 = {};
        for (var n2 in t2) e2[n2] = t2[n2];
        if (t2.geometry) {
          var i2 = t2.geometry.type;
          if (!_i[i2]) throw new Error("Unknown GeoJSON type: " + t2.type);
          e2.geometry = this.read(t2.geometry);
        }
        return t2.bbox && (e2.bbox = _i.bbox.call(this, t2.bbox)), e2;
      }, FeatureCollection: function(t2) {
        var e2 = {};
        if (t2.features) {
          e2.features = [];
          for (var n2 = 0; n2 < t2.features.length; ++n2) e2.features.push(this.read(t2.features[n2]));
        }
        return t2.bbox && (e2.bbox = this.parse.bbox.call(this, t2.bbox)), e2;
      }, coordinates: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2.length; ++n2) {
          var r3 = t2[n2];
          e2.push(i(X, g(r3)));
        }
        return e2;
      }, bbox: function(t2) {
        return this.geometryFactory.createLinearRing([new X(t2[0], t2[1]), new X(t2[2], t2[1]), new X(t2[2], t2[3]), new X(t2[0], t2[3]), new X(t2[0], t2[1])]);
      }, Point: function(t2) {
        var e2 = i(X, g(t2));
        return this.geometryFactory.createPoint(e2);
      }, MultiPoint: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2.length; ++n2) e2.push(_i.Point.call(this, t2[n2]));
        return this.geometryFactory.createMultiPoint(e2);
      }, LineString: function(t2) {
        var e2 = _i.coordinates.call(this, t2);
        return this.geometryFactory.createLineString(e2);
      }, MultiLineString: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2.length; ++n2) e2.push(_i.LineString.call(this, t2[n2]));
        return this.geometryFactory.createMultiLineString(e2);
      }, Polygon: function(t2) {
        for (var e2 = _i.coordinates.call(this, t2[0]), n2 = this.geometryFactory.createLinearRing(e2), i2 = [], r3 = 1; r3 < t2.length; ++r3) {
          var s2 = t2[r3], a2 = _i.coordinates.call(this, s2), o2 = this.geometryFactory.createLinearRing(a2);
          i2.push(o2);
        }
        return this.geometryFactory.createPolygon(n2, i2);
      }, MultiPolygon: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2.length; ++n2) {
          var i2 = t2[n2];
          e2.push(_i.Polygon.call(this, i2));
        }
        return this.geometryFactory.createMultiPolygon(e2);
      }, GeometryCollection: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2.length; ++n2) {
          var i2 = t2[n2];
          e2.push(this.read(i2));
        }
        return this.geometryFactory.createGeometryCollection(e2);
      } }, pi2 = { coordinate: function(t2) {
        var e2 = [t2.x, t2.y];
        return t2.z && e2.push(t2.z), t2.m && e2.push(t2.m), e2;
      }, Point: function(t2) {
        return { type: "Point", coordinates: pi2.coordinate.call(this, t2.getCoordinate()) };
      }, MultiPoint: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r3 = pi2.Point.call(this, i2);
          e2.push(r3.coordinates);
        }
        return { type: "MultiPoint", coordinates: e2 };
      }, LineString: function(t2) {
        for (var e2 = [], n2 = t2.getCoordinates(), i2 = 0; i2 < n2.length; ++i2) {
          var r3 = n2[i2];
          e2.push(pi2.coordinate.call(this, r3));
        }
        return { type: "LineString", coordinates: e2 };
      }, MultiLineString: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r3 = pi2.LineString.call(this, i2);
          e2.push(r3.coordinates);
        }
        return { type: "MultiLineString", coordinates: e2 };
      }, Polygon: function(t2) {
        var e2 = [], n2 = pi2.LineString.call(this, t2._shell);
        e2.push(n2.coordinates);
        for (var i2 = 0; i2 < t2._holes.length; ++i2) {
          var r3 = t2._holes[i2], s2 = pi2.LineString.call(this, r3);
          e2.push(s2.coordinates);
        }
        return { type: "Polygon", coordinates: e2 };
      }, MultiPolygon: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r3 = pi2.Polygon.call(this, i2);
          e2.push(r3.coordinates);
        }
        return { type: "MultiPolygon", coordinates: e2 };
      }, GeometryCollection: function(t2) {
        for (var e2 = [], n2 = 0; n2 < t2._geometries.length; ++n2) {
          var i2 = t2._geometries[n2], r3 = i2.getGeometryType();
          e2.push(pi2[r3].call(this, i2));
        }
        return { type: "GeometryCollection", geometries: e2 };
      } };
      return { BufferOp: vi, GeoJSONReader: (function() {
        return s((function t2(e2) {
          n(this, t2), this.parser = new di(e2 || new ae());
        }), [{ key: "read", value: function(t2) {
          return this.parser.read(t2);
        } }]);
      })(), GeoJSONWriter: (function() {
        return s((function t2() {
          n(this, t2), this.parser = new di(this.geometryFactory);
        }), [{ key: "write", value: function(t2) {
          return this.parser.write(t2);
        } }]);
      })() };
    }));
  })(jsts_min$1);
  return jsts_min$1.exports;
}
var jsts_minExports = requireJsts_min();
const jsts = /* @__PURE__ */ getDefaultExportFromCjs(jsts_minExports);
function adder() {
  return new Adder();
}
function Adder() {
  this.reset();
}
Adder.prototype = {
  constructor: Adder,
  reset: function() {
    this.s = // rounded value
    this.t = 0;
  },
  add: function(y) {
    add(temp, y, this.t);
    add(this, temp.s, this.s);
    if (this.s) this.t += temp.t;
    else this.s = temp.t;
  },
  valueOf: function() {
    return this.s;
  }
};
var temp = new Adder();
function add(adder2, a, b) {
  var x = adder2.s = a + b, bv = x - a, av = x - bv;
  adder2.t = a - av + (b - bv);
}
var epsilon = 1e-6;
var pi = Math.PI;
var halfPi = pi / 2;
var quarterPi = pi / 4;
var tau = pi * 2;
var degrees = 180 / pi;
var radians = pi / 180;
var abs = Math.abs;
var atan = Math.atan;
var atan2 = Math.atan2;
var cos = Math.cos;
var sin = Math.sin;
var sqrt = Math.sqrt;
function acos(x) {
  return x > 1 ? 0 : x < -1 ? pi : Math.acos(x);
}
function asin(x) {
  return x > 1 ? halfPi : x < -1 ? -halfPi : Math.asin(x);
}
function noop() {
}
function streamGeometry(geometry, stream) {
  if (geometry && streamGeometryType.hasOwnProperty(geometry.type)) {
    streamGeometryType[geometry.type](geometry, stream);
  }
}
var streamObjectType = {
  Feature: function(object, stream) {
    streamGeometry(object.geometry, stream);
  },
  FeatureCollection: function(object, stream) {
    var features = object.features, i = -1, n = features.length;
    while (++i < n) streamGeometry(features[i].geometry, stream);
  }
};
var streamGeometryType = {
  Sphere: function(object, stream) {
    stream.sphere();
  },
  Point: function(object, stream) {
    object = object.coordinates;
    stream.point(object[0], object[1], object[2]);
  },
  MultiPoint: function(object, stream) {
    var coordinates = object.coordinates, i = -1, n = coordinates.length;
    while (++i < n) object = coordinates[i], stream.point(object[0], object[1], object[2]);
  },
  LineString: function(object, stream) {
    streamLine(object.coordinates, stream, 0);
  },
  MultiLineString: function(object, stream) {
    var coordinates = object.coordinates, i = -1, n = coordinates.length;
    while (++i < n) streamLine(coordinates[i], stream, 0);
  },
  Polygon: function(object, stream) {
    streamPolygon(object.coordinates, stream);
  },
  MultiPolygon: function(object, stream) {
    var coordinates = object.coordinates, i = -1, n = coordinates.length;
    while (++i < n) streamPolygon(coordinates[i], stream);
  },
  GeometryCollection: function(object, stream) {
    var geometries = object.geometries, i = -1, n = geometries.length;
    while (++i < n) streamGeometry(geometries[i], stream);
  }
};
function streamLine(coordinates, stream, closed) {
  var i = -1, n = coordinates.length - closed, coordinate;
  stream.lineStart();
  while (++i < n) coordinate = coordinates[i], stream.point(coordinate[0], coordinate[1], coordinate[2]);
  stream.lineEnd();
}
function streamPolygon(coordinates, stream) {
  var i = -1, n = coordinates.length;
  stream.polygonStart();
  while (++i < n) streamLine(coordinates[i], stream, 1);
  stream.polygonEnd();
}
function geoStream(object, stream) {
  if (object && streamObjectType.hasOwnProperty(object.type)) {
    streamObjectType[object.type](object, stream);
  } else {
    streamGeometry(object, stream);
  }
}
adder();
adder();
function spherical(cartesian2) {
  return [atan2(cartesian2[1], cartesian2[0]), asin(cartesian2[2])];
}
function cartesian(spherical2) {
  var lambda = spherical2[0], phi = spherical2[1], cosPhi = cos(phi);
  return [cosPhi * cos(lambda), cosPhi * sin(lambda), sin(phi)];
}
function cartesianDot(a, b) {
  return a[0] * b[0] + a[1] * b[1] + a[2] * b[2];
}
function cartesianCross(a, b) {
  return [a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0]];
}
function cartesianAddInPlace(a, b) {
  a[0] += b[0], a[1] += b[1], a[2] += b[2];
}
function cartesianScale(vector, k) {
  return [vector[0] * k, vector[1] * k, vector[2] * k];
}
function cartesianNormalizeInPlace(d) {
  var l = sqrt(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]);
  d[0] /= l, d[1] /= l, d[2] /= l;
}
adder();
function compose(a, b) {
  function compose2(x, y) {
    return x = a(x, y), b(x[0], x[1]);
  }
  if (a.invert && b.invert) compose2.invert = function(x, y) {
    return x = b.invert(x, y), x && a.invert(x[0], x[1]);
  };
  return compose2;
}
function rotationIdentity(lambda, phi) {
  return [lambda > pi ? lambda - tau : lambda < -pi ? lambda + tau : lambda, phi];
}
rotationIdentity.invert = rotationIdentity;
function rotateRadians(deltaLambda, deltaPhi, deltaGamma) {
  return (deltaLambda %= tau) ? deltaPhi || deltaGamma ? compose(rotationLambda(deltaLambda), rotationPhiGamma(deltaPhi, deltaGamma)) : rotationLambda(deltaLambda) : deltaPhi || deltaGamma ? rotationPhiGamma(deltaPhi, deltaGamma) : rotationIdentity;
}
function forwardRotationLambda(deltaLambda) {
  return function(lambda, phi) {
    return lambda += deltaLambda, [lambda > pi ? lambda - tau : lambda < -pi ? lambda + tau : lambda, phi];
  };
}
function rotationLambda(deltaLambda) {
  var rotation = forwardRotationLambda(deltaLambda);
  rotation.invert = forwardRotationLambda(-deltaLambda);
  return rotation;
}
function rotationPhiGamma(deltaPhi, deltaGamma) {
  var cosDeltaPhi = cos(deltaPhi), sinDeltaPhi = sin(deltaPhi), cosDeltaGamma = cos(deltaGamma), sinDeltaGamma = sin(deltaGamma);
  function rotation(lambda, phi) {
    var cosPhi = cos(phi), x = cos(lambda) * cosPhi, y = sin(lambda) * cosPhi, z = sin(phi), k = z * cosDeltaPhi + x * sinDeltaPhi;
    return [
      atan2(y * cosDeltaGamma - k * sinDeltaGamma, x * cosDeltaPhi - z * sinDeltaPhi),
      asin(k * cosDeltaGamma + y * sinDeltaGamma)
    ];
  }
  rotation.invert = function(lambda, phi) {
    var cosPhi = cos(phi), x = cos(lambda) * cosPhi, y = sin(lambda) * cosPhi, z = sin(phi), k = z * cosDeltaGamma - y * sinDeltaGamma;
    return [
      atan2(y * cosDeltaGamma + z * sinDeltaGamma, x * cosDeltaPhi + k * sinDeltaPhi),
      asin(k * cosDeltaPhi - x * sinDeltaPhi)
    ];
  };
  return rotation;
}
function circleStream(stream, radius, delta, direction, t0, t1) {
  if (!delta) return;
  var cosRadius = cos(radius), sinRadius = sin(radius), step = direction * delta;
  if (t0 == null) {
    t0 = radius + direction * tau;
    t1 = radius - step / 2;
  } else {
    t0 = circleRadius(cosRadius, t0);
    t1 = circleRadius(cosRadius, t1);
    if (direction > 0 ? t0 < t1 : t0 > t1) t0 += direction * tau;
  }
  for (var point2, t = t0; direction > 0 ? t > t1 : t < t1; t -= step) {
    point2 = spherical([cosRadius, -sinRadius * cos(t), -sinRadius * sin(t)]);
    stream.point(point2[0], point2[1]);
  }
}
function circleRadius(cosRadius, point2) {
  point2 = cartesian(point2), point2[0] -= cosRadius;
  cartesianNormalizeInPlace(point2);
  var radius = acos(-point2[1]);
  return ((-point2[2] < 0 ? -radius : radius) + tau - epsilon) % tau;
}
function clipBuffer() {
  var lines = [], line2;
  return {
    point: function(x, y) {
      line2.push([x, y]);
    },
    lineStart: function() {
      lines.push(line2 = []);
    },
    lineEnd: noop,
    rejoin: function() {
      if (lines.length > 1) lines.push(lines.pop().concat(lines.shift()));
    },
    result: function() {
      var result = lines;
      lines = [];
      line2 = null;
      return result;
    }
  };
}
function clipLine(a, b, x02, y02, x12, y12) {
  var ax = a[0], ay = a[1], bx = b[0], by = b[1], t0 = 0, t1 = 1, dx = bx - ax, dy = by - ay, r2;
  r2 = x02 - ax;
  if (!dx && r2 > 0) return;
  r2 /= dx;
  if (dx < 0) {
    if (r2 < t0) return;
    if (r2 < t1) t1 = r2;
  } else if (dx > 0) {
    if (r2 > t1) return;
    if (r2 > t0) t0 = r2;
  }
  r2 = x12 - ax;
  if (!dx && r2 < 0) return;
  r2 /= dx;
  if (dx < 0) {
    if (r2 > t1) return;
    if (r2 > t0) t0 = r2;
  } else if (dx > 0) {
    if (r2 < t0) return;
    if (r2 < t1) t1 = r2;
  }
  r2 = y02 - ay;
  if (!dy && r2 > 0) return;
  r2 /= dy;
  if (dy < 0) {
    if (r2 < t0) return;
    if (r2 < t1) t1 = r2;
  } else if (dy > 0) {
    if (r2 > t1) return;
    if (r2 > t0) t0 = r2;
  }
  r2 = y12 - ay;
  if (!dy && r2 < 0) return;
  r2 /= dy;
  if (dy < 0) {
    if (r2 > t1) return;
    if (r2 > t0) t0 = r2;
  } else if (dy > 0) {
    if (r2 < t0) return;
    if (r2 < t1) t1 = r2;
  }
  if (t0 > 0) a[0] = ax + t0 * dx, a[1] = ay + t0 * dy;
  if (t1 < 1) b[0] = ax + t1 * dx, b[1] = ay + t1 * dy;
  return true;
}
function pointEqual(a, b) {
  return abs(a[0] - b[0]) < epsilon && abs(a[1] - b[1]) < epsilon;
}
function Intersection(point2, points, other, entry) {
  this.x = point2;
  this.z = points;
  this.o = other;
  this.e = entry;
  this.v = false;
  this.n = this.p = null;
}
function clipPolygon(segments, compareIntersection2, startInside, interpolate, stream) {
  var subject = [], clip2 = [], i, n;
  segments.forEach(function(segment) {
    if ((n2 = segment.length - 1) <= 0) return;
    var n2, p0 = segment[0], p1 = segment[n2], x;
    if (pointEqual(p0, p1)) {
      stream.lineStart();
      for (i = 0; i < n2; ++i) stream.point((p0 = segment[i])[0], p0[1]);
      stream.lineEnd();
      return;
    }
    subject.push(x = new Intersection(p0, segment, null, true));
    clip2.push(x.o = new Intersection(p0, null, x, false));
    subject.push(x = new Intersection(p1, segment, null, false));
    clip2.push(x.o = new Intersection(p1, null, x, true));
  });
  if (!subject.length) return;
  clip2.sort(compareIntersection2);
  link(subject);
  link(clip2);
  for (i = 0, n = clip2.length; i < n; ++i) {
    clip2[i].e = startInside = !startInside;
  }
  var start = subject[0], points, point2;
  while (1) {
    var current = start, isSubject = true;
    while (current.v) if ((current = current.n) === start) return;
    points = current.z;
    stream.lineStart();
    do {
      current.v = current.o.v = true;
      if (current.e) {
        if (isSubject) {
          for (i = 0, n = points.length; i < n; ++i) stream.point((point2 = points[i])[0], point2[1]);
        } else {
          interpolate(current.x, current.n.x, 1, stream);
        }
        current = current.n;
      } else {
        if (isSubject) {
          points = current.p.z;
          for (i = points.length - 1; i >= 0; --i) stream.point((point2 = points[i])[0], point2[1]);
        } else {
          interpolate(current.x, current.p.x, -1, stream);
        }
        current = current.p;
      }
      current = current.o;
      points = current.z;
      isSubject = !isSubject;
    } while (!current.v);
    stream.lineEnd();
  }
}
function link(array) {
  if (!(n = array.length)) return;
  var n, i = 0, a = array[0], b;
  while (++i < n) {
    a.n = b = array[i];
    b.p = a;
    a = b;
  }
  a.n = b = array[0];
  b.p = a;
}
function ascending(a, b) {
  return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
}
function bisector(compare2) {
  if (compare2.length === 1) compare2 = ascendingComparator(compare2);
  return {
    left: function(a, x, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a.length;
      while (lo < hi) {
        var mid = lo + hi >>> 1;
        if (compare2(a[mid], x) < 0) lo = mid + 1;
        else hi = mid;
      }
      return lo;
    },
    right: function(a, x, lo, hi) {
      if (lo == null) lo = 0;
      if (hi == null) hi = a.length;
      while (lo < hi) {
        var mid = lo + hi >>> 1;
        if (compare2(a[mid], x) > 0) hi = mid;
        else lo = mid + 1;
      }
      return lo;
    }
  };
}
function ascendingComparator(f) {
  return function(d, x) {
    return ascending(f(d), x);
  };
}
bisector(ascending);
function merge(arrays) {
  var n = arrays.length, m, i = -1, j = 0, merged, array;
  while (++i < n) j += arrays[i].length;
  merged = new Array(j);
  while (--n >= 0) {
    array = arrays[n];
    m = array.length;
    while (--m >= 0) {
      merged[--j] = array[m];
    }
  }
  return merged;
}
var clipMax = 1e9, clipMin = -clipMax;
function clipExtent(x02, y02, x12, y12) {
  function visible(x, y) {
    return x02 <= x && x <= x12 && y02 <= y && y <= y12;
  }
  function interpolate(from, to, direction, stream) {
    var a = 0, a1 = 0;
    if (from == null || (a = corner(from, direction)) !== (a1 = corner(to, direction)) || comparePoint(from, to) < 0 ^ direction > 0) {
      do
        stream.point(a === 0 || a === 3 ? x02 : x12, a > 1 ? y12 : y02);
      while ((a = (a + direction + 4) % 4) !== a1);
    } else {
      stream.point(to[0], to[1]);
    }
  }
  function corner(p, direction) {
    return abs(p[0] - x02) < epsilon ? direction > 0 ? 0 : 3 : abs(p[0] - x12) < epsilon ? direction > 0 ? 2 : 1 : abs(p[1] - y02) < epsilon ? direction > 0 ? 1 : 0 : direction > 0 ? 3 : 2;
  }
  function compareIntersection2(a, b) {
    return comparePoint(a.x, b.x);
  }
  function comparePoint(a, b) {
    var ca = corner(a, 1), cb = corner(b, 1);
    return ca !== cb ? ca - cb : ca === 0 ? b[1] - a[1] : ca === 1 ? a[0] - b[0] : ca === 2 ? a[1] - b[1] : b[0] - a[0];
  }
  return function(stream) {
    var activeStream = stream, bufferStream = clipBuffer(), segments, polygon2, ring, x__, y__, v__, x_, y_, v_, first, clean;
    var clipStream = {
      point: point2,
      lineStart,
      lineEnd,
      polygonStart,
      polygonEnd
    };
    function point2(x, y) {
      if (visible(x, y)) activeStream.point(x, y);
    }
    function polygonInside() {
      var winding = 0;
      for (var i = 0, n = polygon2.length; i < n; ++i) {
        for (var ring2 = polygon2[i], j = 1, m = ring2.length, point3 = ring2[0], a0, a1, b0 = point3[0], b1 = point3[1]; j < m; ++j) {
          a0 = b0, a1 = b1, point3 = ring2[j], b0 = point3[0], b1 = point3[1];
          if (a1 <= y12) {
            if (b1 > y12 && (b0 - a0) * (y12 - a1) > (b1 - a1) * (x02 - a0)) ++winding;
          } else {
            if (b1 <= y12 && (b0 - a0) * (y12 - a1) < (b1 - a1) * (x02 - a0)) --winding;
          }
        }
      }
      return winding;
    }
    function polygonStart() {
      activeStream = bufferStream, segments = [], polygon2 = [], clean = true;
    }
    function polygonEnd() {
      var startInside = polygonInside(), cleanInside = clean && startInside, visible2 = (segments = merge(segments)).length;
      if (cleanInside || visible2) {
        stream.polygonStart();
        if (cleanInside) {
          stream.lineStart();
          interpolate(null, null, 1, stream);
          stream.lineEnd();
        }
        if (visible2) {
          clipPolygon(segments, compareIntersection2, startInside, interpolate, stream);
        }
        stream.polygonEnd();
      }
      activeStream = stream, segments = polygon2 = ring = null;
    }
    function lineStart() {
      clipStream.point = linePoint;
      if (polygon2) polygon2.push(ring = []);
      first = true;
      v_ = false;
      x_ = y_ = NaN;
    }
    function lineEnd() {
      if (segments) {
        linePoint(x__, y__);
        if (v__ && v_) bufferStream.rejoin();
        segments.push(bufferStream.result());
      }
      clipStream.point = point2;
      if (v_) activeStream.lineEnd();
    }
    function linePoint(x, y) {
      var v = visible(x, y);
      if (polygon2) ring.push([x, y]);
      if (first) {
        x__ = x, y__ = y, v__ = v;
        first = false;
        if (v) {
          activeStream.lineStart();
          activeStream.point(x, y);
        }
      } else {
        if (v && v_) activeStream.point(x, y);
        else {
          var a = [x_ = Math.max(clipMin, Math.min(clipMax, x_)), y_ = Math.max(clipMin, Math.min(clipMax, y_))], b = [x = Math.max(clipMin, Math.min(clipMax, x)), y = Math.max(clipMin, Math.min(clipMax, y))];
          if (clipLine(a, b, x02, y02, x12, y12)) {
            if (!v_) {
              activeStream.lineStart();
              activeStream.point(a[0], a[1]);
            }
            activeStream.point(b[0], b[1]);
            if (!v) activeStream.lineEnd();
            clean = false;
          } else if (v) {
            activeStream.lineStart();
            activeStream.point(x, y);
            clean = false;
          }
        }
      }
      x_ = x, y_ = y, v_ = v;
    }
    return clipStream;
  };
}
var sum = adder();
function polygonContains(polygon2, point2) {
  var lambda = point2[0], phi = point2[1], normal = [sin(lambda), -cos(lambda), 0], angle = 0, winding = 0;
  sum.reset();
  for (var i = 0, n = polygon2.length; i < n; ++i) {
    if (!(m = (ring = polygon2[i]).length)) continue;
    var ring, m, point0 = ring[m - 1], lambda0 = point0[0], phi0 = point0[1] / 2 + quarterPi, sinPhi0 = sin(phi0), cosPhi0 = cos(phi0);
    for (var j = 0; j < m; ++j, lambda0 = lambda1, sinPhi0 = sinPhi1, cosPhi0 = cosPhi1, point0 = point1) {
      var point1 = ring[j], lambda1 = point1[0], phi1 = point1[1] / 2 + quarterPi, sinPhi1 = sin(phi1), cosPhi1 = cos(phi1), delta = lambda1 - lambda0, sign = delta >= 0 ? 1 : -1, absDelta = sign * delta, antimeridian = absDelta > pi, k = sinPhi0 * sinPhi1;
      sum.add(atan2(k * sign * sin(absDelta), cosPhi0 * cosPhi1 + k * cos(absDelta)));
      angle += antimeridian ? delta + sign * tau : delta;
      if (antimeridian ^ lambda0 >= lambda ^ lambda1 >= lambda) {
        var arc = cartesianCross(cartesian(point0), cartesian(point1));
        cartesianNormalizeInPlace(arc);
        var intersection2 = cartesianCross(normal, arc);
        cartesianNormalizeInPlace(intersection2);
        var phiArc = (antimeridian ^ delta >= 0 ? -1 : 1) * asin(intersection2[2]);
        if (phi > phiArc || phi === phiArc && (arc[0] || arc[1])) {
          winding += antimeridian ^ delta >= 0 ? 1 : -1;
        }
      }
    }
  }
  return (angle < -epsilon || angle < epsilon && sum < -epsilon) ^ winding & 1;
}
adder();
function identity(x) {
  return x;
}
adder();
adder();
var x0 = Infinity, y0 = x0, x1 = -x0, y1 = x1;
var boundsStream = {
  point: boundsPoint,
  lineStart: noop,
  lineEnd: noop,
  polygonStart: noop,
  polygonEnd: noop,
  result: function() {
    var bounds = [[x0, y0], [x1, y1]];
    x1 = y1 = -(y0 = x0 = Infinity);
    return bounds;
  }
};
function boundsPoint(x, y) {
  if (x < x0) x0 = x;
  if (x > x1) x1 = x;
  if (y < y0) y0 = y;
  if (y > y1) y1 = y;
}
adder();
function clip(pointVisible, clipLine2, interpolate, start) {
  return function(rotate2, sink) {
    var line2 = clipLine2(sink), rotatedStart = rotate2.invert(start[0], start[1]), ringBuffer = clipBuffer(), ringSink = clipLine2(ringBuffer), polygonStarted = false, polygon2, segments, ring;
    var clip2 = {
      point: point2,
      lineStart,
      lineEnd,
      polygonStart: function() {
        clip2.point = pointRing;
        clip2.lineStart = ringStart;
        clip2.lineEnd = ringEnd;
        segments = [];
        polygon2 = [];
      },
      polygonEnd: function() {
        clip2.point = point2;
        clip2.lineStart = lineStart;
        clip2.lineEnd = lineEnd;
        segments = merge(segments);
        var startInside = polygonContains(polygon2, rotatedStart);
        if (segments.length) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          clipPolygon(segments, compareIntersection, startInside, interpolate, sink);
        } else if (startInside) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          interpolate(null, null, 1, sink);
          sink.lineEnd();
        }
        if (polygonStarted) sink.polygonEnd(), polygonStarted = false;
        segments = polygon2 = null;
      },
      sphere: function() {
        sink.polygonStart();
        sink.lineStart();
        interpolate(null, null, 1, sink);
        sink.lineEnd();
        sink.polygonEnd();
      }
    };
    function point2(lambda, phi) {
      var point3 = rotate2(lambda, phi);
      if (pointVisible(lambda = point3[0], phi = point3[1])) sink.point(lambda, phi);
    }
    function pointLine(lambda, phi) {
      var point3 = rotate2(lambda, phi);
      line2.point(point3[0], point3[1]);
    }
    function lineStart() {
      clip2.point = pointLine;
      line2.lineStart();
    }
    function lineEnd() {
      clip2.point = point2;
      line2.lineEnd();
    }
    function pointRing(lambda, phi) {
      ring.push([lambda, phi]);
      var point3 = rotate2(lambda, phi);
      ringSink.point(point3[0], point3[1]);
    }
    function ringStart() {
      ringSink.lineStart();
      ring = [];
    }
    function ringEnd() {
      pointRing(ring[0][0], ring[0][1]);
      ringSink.lineEnd();
      var clean = ringSink.clean(), ringSegments = ringBuffer.result(), i, n = ringSegments.length, m, segment, point3;
      ring.pop();
      polygon2.push(ring);
      ring = null;
      if (!n) return;
      if (clean & 1) {
        segment = ringSegments[0];
        if ((m = segment.length - 1) > 0) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          for (i = 0; i < m; ++i) sink.point((point3 = segment[i])[0], point3[1]);
          sink.lineEnd();
        }
        return;
      }
      if (n > 1 && clean & 2) ringSegments.push(ringSegments.pop().concat(ringSegments.shift()));
      segments.push(ringSegments.filter(validSegment));
    }
    return clip2;
  };
}
function validSegment(segment) {
  return segment.length > 1;
}
function compareIntersection(a, b) {
  return ((a = a.x)[0] < 0 ? a[1] - halfPi - epsilon : halfPi - a[1]) - ((b = b.x)[0] < 0 ? b[1] - halfPi - epsilon : halfPi - b[1]);
}
const clipAntimeridian = clip(
  function() {
    return true;
  },
  clipAntimeridianLine,
  clipAntimeridianInterpolate,
  [-pi, -halfPi]
);
function clipAntimeridianLine(stream) {
  var lambda0 = NaN, phi0 = NaN, sign0 = NaN, clean;
  return {
    lineStart: function() {
      stream.lineStart();
      clean = 1;
    },
    point: function(lambda1, phi1) {
      var sign1 = lambda1 > 0 ? pi : -pi, delta = abs(lambda1 - lambda0);
      if (abs(delta - pi) < epsilon) {
        stream.point(lambda0, phi0 = (phi0 + phi1) / 2 > 0 ? halfPi : -halfPi);
        stream.point(sign0, phi0);
        stream.lineEnd();
        stream.lineStart();
        stream.point(sign1, phi0);
        stream.point(lambda1, phi0);
        clean = 0;
      } else if (sign0 !== sign1 && delta >= pi) {
        if (abs(lambda0 - sign0) < epsilon) lambda0 -= sign0 * epsilon;
        if (abs(lambda1 - sign1) < epsilon) lambda1 -= sign1 * epsilon;
        phi0 = clipAntimeridianIntersect(lambda0, phi0, lambda1, phi1);
        stream.point(sign0, phi0);
        stream.lineEnd();
        stream.lineStart();
        stream.point(sign1, phi0);
        clean = 0;
      }
      stream.point(lambda0 = lambda1, phi0 = phi1);
      sign0 = sign1;
    },
    lineEnd: function() {
      stream.lineEnd();
      lambda0 = phi0 = NaN;
    },
    clean: function() {
      return 2 - clean;
    }
  };
}
function clipAntimeridianIntersect(lambda0, phi0, lambda1, phi1) {
  var cosPhi0, cosPhi1, sinLambda0Lambda1 = sin(lambda0 - lambda1);
  return abs(sinLambda0Lambda1) > epsilon ? atan((sin(phi0) * (cosPhi1 = cos(phi1)) * sin(lambda1) - sin(phi1) * (cosPhi0 = cos(phi0)) * sin(lambda0)) / (cosPhi0 * cosPhi1 * sinLambda0Lambda1)) : (phi0 + phi1) / 2;
}
function clipAntimeridianInterpolate(from, to, direction, stream) {
  var phi;
  if (from == null) {
    phi = direction * halfPi;
    stream.point(-pi, phi);
    stream.point(0, phi);
    stream.point(pi, phi);
    stream.point(pi, 0);
    stream.point(pi, -phi);
    stream.point(0, -phi);
    stream.point(-pi, -phi);
    stream.point(-pi, 0);
    stream.point(-pi, phi);
  } else if (abs(from[0] - to[0]) > epsilon) {
    var lambda = from[0] < to[0] ? pi : -pi;
    phi = direction * lambda / 2;
    stream.point(-lambda, phi);
    stream.point(0, phi);
    stream.point(lambda, phi);
  } else {
    stream.point(to[0], to[1]);
  }
}
function clipCircle(radius, delta) {
  var cr = cos(radius), smallRadius = cr > 0, notHemisphere = abs(cr) > epsilon;
  function interpolate(from, to, direction, stream) {
    circleStream(stream, radius, delta, direction, from, to);
  }
  function visible(lambda, phi) {
    return cos(lambda) * cos(phi) > cr;
  }
  function clipLine2(stream) {
    var point0, c0, v0, v00, clean;
    return {
      lineStart: function() {
        v00 = v0 = false;
        clean = 1;
      },
      point: function(lambda, phi) {
        var point1 = [lambda, phi], point2, v = visible(lambda, phi), c = smallRadius ? v ? 0 : code(lambda, phi) : v ? code(lambda + (lambda < 0 ? pi : -pi), phi) : 0;
        if (!point0 && (v00 = v0 = v)) stream.lineStart();
        if (v !== v0) {
          point2 = intersect2(point0, point1);
          if (!point2 || pointEqual(point0, point2) || pointEqual(point1, point2)) {
            point1[0] += epsilon;
            point1[1] += epsilon;
            v = visible(point1[0], point1[1]);
          }
        }
        if (v !== v0) {
          clean = 0;
          if (v) {
            stream.lineStart();
            point2 = intersect2(point1, point0);
            stream.point(point2[0], point2[1]);
          } else {
            point2 = intersect2(point0, point1);
            stream.point(point2[0], point2[1]);
            stream.lineEnd();
          }
          point0 = point2;
        } else if (notHemisphere && point0 && smallRadius ^ v) {
          var t;
          if (!(c & c0) && (t = intersect2(point1, point0, true))) {
            clean = 0;
            if (smallRadius) {
              stream.lineStart();
              stream.point(t[0][0], t[0][1]);
              stream.point(t[1][0], t[1][1]);
              stream.lineEnd();
            } else {
              stream.point(t[1][0], t[1][1]);
              stream.lineEnd();
              stream.lineStart();
              stream.point(t[0][0], t[0][1]);
            }
          }
        }
        if (v && (!point0 || !pointEqual(point0, point1))) {
          stream.point(point1[0], point1[1]);
        }
        point0 = point1, v0 = v, c0 = c;
      },
      lineEnd: function() {
        if (v0) stream.lineEnd();
        point0 = null;
      },
      // Rejoin first and last segments if there were intersections and the first
      // and last points were visible.
      clean: function() {
        return clean | (v00 && v0) << 1;
      }
    };
  }
  function intersect2(a, b, two) {
    var pa = cartesian(a), pb = cartesian(b);
    var n1 = [1, 0, 0], n2 = cartesianCross(pa, pb), n2n2 = cartesianDot(n2, n2), n1n2 = n2[0], determinant = n2n2 - n1n2 * n1n2;
    if (!determinant) return !two && a;
    var c1 = cr * n2n2 / determinant, c2 = -cr * n1n2 / determinant, n1xn2 = cartesianCross(n1, n2), A = cartesianScale(n1, c1), B2 = cartesianScale(n2, c2);
    cartesianAddInPlace(A, B2);
    var u2 = n1xn2, w = cartesianDot(A, u2), uu = cartesianDot(u2, u2), t2 = w * w - uu * (cartesianDot(A, A) - 1);
    if (t2 < 0) return;
    var t = sqrt(t2), q = cartesianScale(u2, (-w - t) / uu);
    cartesianAddInPlace(q, A);
    q = spherical(q);
    if (!two) return q;
    var lambda0 = a[0], lambda1 = b[0], phi0 = a[1], phi1 = b[1], z;
    if (lambda1 < lambda0) z = lambda0, lambda0 = lambda1, lambda1 = z;
    var delta2 = lambda1 - lambda0, polar = abs(delta2 - pi) < epsilon, meridian = polar || delta2 < epsilon;
    if (!polar && phi1 < phi0) z = phi0, phi0 = phi1, phi1 = z;
    if (meridian ? polar ? phi0 + phi1 > 0 ^ q[1] < (abs(q[0] - lambda0) < epsilon ? phi0 : phi1) : phi0 <= q[1] && q[1] <= phi1 : delta2 > pi ^ (lambda0 <= q[0] && q[0] <= lambda1)) {
      var q1 = cartesianScale(u2, (-w + t) / uu);
      cartesianAddInPlace(q1, A);
      return [q, spherical(q1)];
    }
  }
  function code(lambda, phi) {
    var r2 = smallRadius ? radius : pi - radius, code2 = 0;
    if (lambda < -r2) code2 |= 1;
    else if (lambda > r2) code2 |= 2;
    if (phi < -r2) code2 |= 4;
    else if (phi > r2) code2 |= 8;
    return code2;
  }
  return clip(visible, clipLine2, interpolate, smallRadius ? [0, -radius] : [-pi, radius - pi]);
}
function transformer(methods) {
  return function(stream) {
    var s = new TransformStream();
    for (var key in methods) s[key] = methods[key];
    s.stream = stream;
    return s;
  };
}
function TransformStream() {
}
TransformStream.prototype = {
  constructor: TransformStream,
  point: function(x, y) {
    this.stream.point(x, y);
  },
  sphere: function() {
    this.stream.sphere();
  },
  lineStart: function() {
    this.stream.lineStart();
  },
  lineEnd: function() {
    this.stream.lineEnd();
  },
  polygonStart: function() {
    this.stream.polygonStart();
  },
  polygonEnd: function() {
    this.stream.polygonEnd();
  }
};
function fitExtent(projection2, extent, object) {
  var w = extent[1][0] - extent[0][0], h = extent[1][1] - extent[0][1], clip2 = projection2.clipExtent && projection2.clipExtent();
  projection2.scale(150).translate([0, 0]);
  if (clip2 != null) projection2.clipExtent(null);
  geoStream(object, projection2.stream(boundsStream));
  var b = boundsStream.result(), k = Math.min(w / (b[1][0] - b[0][0]), h / (b[1][1] - b[0][1])), x = +extent[0][0] + (w - k * (b[1][0] + b[0][0])) / 2, y = +extent[0][1] + (h - k * (b[1][1] + b[0][1])) / 2;
  if (clip2 != null) projection2.clipExtent(clip2);
  return projection2.scale(k * 150).translate([x, y]);
}
function fitSize(projection2, size, object) {
  return fitExtent(projection2, [[0, 0], size], object);
}
var maxDepth = 16, cosMinDistance = cos(30 * radians);
function resample(project, delta2) {
  return +delta2 ? resample$1(project, delta2) : resampleNone(project);
}
function resampleNone(project) {
  return transformer({
    point: function(x, y) {
      x = project(x, y);
      this.stream.point(x[0], x[1]);
    }
  });
}
function resample$1(project, delta2) {
  function resampleLineTo(x02, y02, lambda0, a0, b0, c0, x12, y12, lambda1, a1, b1, c1, depth, stream) {
    var dx = x12 - x02, dy = y12 - y02, d2 = dx * dx + dy * dy;
    if (d2 > 4 * delta2 && depth--) {
      var a = a0 + a1, b = b0 + b1, c = c0 + c1, m = sqrt(a * a + b * b + c * c), phi2 = asin(c /= m), lambda2 = abs(abs(c) - 1) < epsilon || abs(lambda0 - lambda1) < epsilon ? (lambda0 + lambda1) / 2 : atan2(b, a), p = project(lambda2, phi2), x2 = p[0], y2 = p[1], dx2 = x2 - x02, dy2 = y2 - y02, dz = dy * dx2 - dx * dy2;
      if (dz * dz / d2 > delta2 || abs((dx * dx2 + dy * dy2) / d2 - 0.5) > 0.3 || a0 * a1 + b0 * b1 + c0 * c1 < cosMinDistance) {
        resampleLineTo(x02, y02, lambda0, a0, b0, c0, x2, y2, lambda2, a /= m, b /= m, c, depth, stream);
        stream.point(x2, y2);
        resampleLineTo(x2, y2, lambda2, a, b, c, x12, y12, lambda1, a1, b1, c1, depth, stream);
      }
    }
  }
  return function(stream) {
    var lambda00, x00, y00, a00, b00, c00, lambda0, x02, y02, a0, b0, c0;
    var resampleStream = {
      point: point2,
      lineStart,
      lineEnd,
      polygonStart: function() {
        stream.polygonStart();
        resampleStream.lineStart = ringStart;
      },
      polygonEnd: function() {
        stream.polygonEnd();
        resampleStream.lineStart = lineStart;
      }
    };
    function point2(x, y) {
      x = project(x, y);
      stream.point(x[0], x[1]);
    }
    function lineStart() {
      x02 = NaN;
      resampleStream.point = linePoint;
      stream.lineStart();
    }
    function linePoint(lambda, phi) {
      var c = cartesian([lambda, phi]), p = project(lambda, phi);
      resampleLineTo(x02, y02, lambda0, a0, b0, c0, x02 = p[0], y02 = p[1], lambda0 = lambda, a0 = c[0], b0 = c[1], c0 = c[2], maxDepth, stream);
      stream.point(x02, y02);
    }
    function lineEnd() {
      resampleStream.point = point2;
      stream.lineEnd();
    }
    function ringStart() {
      lineStart();
      resampleStream.point = ringPoint;
      resampleStream.lineEnd = ringEnd;
    }
    function ringPoint(lambda, phi) {
      linePoint(lambda00 = lambda, phi), x00 = x02, y00 = y02, a00 = a0, b00 = b0, c00 = c0;
      resampleStream.point = linePoint;
    }
    function ringEnd() {
      resampleLineTo(x02, y02, lambda0, a0, b0, c0, x00, y00, lambda00, a00, b00, c00, maxDepth, stream);
      resampleStream.lineEnd = lineEnd;
      lineEnd();
    }
    return resampleStream;
  };
}
var transformRadians = transformer({
  point: function(x, y) {
    this.stream.point(x * radians, y * radians);
  }
});
function projection(project) {
  return projectionMutator(function() {
    return project;
  })();
}
function projectionMutator(projectAt) {
  var project, k = 150, x = 480, y = 250, dx, dy, lambda = 0, phi = 0, deltaLambda = 0, deltaPhi = 0, deltaGamma = 0, rotate2, projectRotate, theta = null, preclip = clipAntimeridian, x02 = null, y02, x12, y12, postclip = identity, delta2 = 0.5, projectResample = resample(projectTransform, delta2), cache, cacheStream;
  function projection2(point2) {
    point2 = projectRotate(point2[0] * radians, point2[1] * radians);
    return [point2[0] * k + dx, dy - point2[1] * k];
  }
  function invert(point2) {
    point2 = projectRotate.invert((point2[0] - dx) / k, (dy - point2[1]) / k);
    return point2 && [point2[0] * degrees, point2[1] * degrees];
  }
  function projectTransform(x2, y2) {
    return x2 = project(x2, y2), [x2[0] * k + dx, dy - x2[1] * k];
  }
  projection2.stream = function(stream) {
    return cache && cacheStream === stream ? cache : cache = transformRadians(preclip(rotate2, projectResample(postclip(cacheStream = stream))));
  };
  projection2.clipAngle = function(_) {
    return arguments.length ? (preclip = +_ ? clipCircle(theta = _ * radians, 6 * radians) : (theta = null, clipAntimeridian), reset()) : theta * degrees;
  };
  projection2.clipExtent = function(_) {
    return arguments.length ? (postclip = _ == null ? (x02 = y02 = x12 = y12 = null, identity) : clipExtent(x02 = +_[0][0], y02 = +_[0][1], x12 = +_[1][0], y12 = +_[1][1]), reset()) : x02 == null ? null : [[x02, y02], [x12, y12]];
  };
  projection2.scale = function(_) {
    return arguments.length ? (k = +_, recenter()) : k;
  };
  projection2.translate = function(_) {
    return arguments.length ? (x = +_[0], y = +_[1], recenter()) : [x, y];
  };
  projection2.center = function(_) {
    return arguments.length ? (lambda = _[0] % 360 * radians, phi = _[1] % 360 * radians, recenter()) : [lambda * degrees, phi * degrees];
  };
  projection2.rotate = function(_) {
    return arguments.length ? (deltaLambda = _[0] % 360 * radians, deltaPhi = _[1] % 360 * radians, deltaGamma = _.length > 2 ? _[2] % 360 * radians : 0, recenter()) : [deltaLambda * degrees, deltaPhi * degrees, deltaGamma * degrees];
  };
  projection2.precision = function(_) {
    return arguments.length ? (projectResample = resample(projectTransform, delta2 = _ * _), reset()) : sqrt(delta2);
  };
  projection2.fitExtent = function(extent, object) {
    return fitExtent(projection2, extent, object);
  };
  projection2.fitSize = function(size, object) {
    return fitSize(projection2, size, object);
  };
  function recenter() {
    projectRotate = compose(rotate2 = rotateRadians(deltaLambda, deltaPhi, deltaGamma), project);
    var center2 = project(lambda, phi);
    dx = x - center2[0] * k;
    dy = y + center2[1] * k;
    return reset();
  }
  function reset() {
    cache = cacheStream = null;
    return projection2;
  }
  return function() {
    project = projectAt.apply(this, arguments);
    projection2.invert = project.invert && invert;
    return recenter();
  };
}
function azimuthalRaw(scale2) {
  return function(x, y) {
    var cx = cos(x), cy = cos(y), k = scale2(cx * cy);
    return [
      k * cy * sin(x),
      k * sin(y)
    ];
  };
}
function azimuthalInvert(angle) {
  return function(x, y) {
    var z = sqrt(x * x + y * y), c = angle(z), sc = sin(c), cc = cos(c);
    return [
      atan2(x * sc, z * cc),
      asin(z && y * sc / z)
    ];
  };
}
var azimuthalEqualAreaRaw = azimuthalRaw(function(cxcy) {
  return sqrt(2 / (1 + cxcy));
});
azimuthalEqualAreaRaw.invert = azimuthalInvert(function(z) {
  return 2 * asin(z / 2);
});
var azimuthalEquidistantRaw = azimuthalRaw(function(c) {
  return (c = acos(c)) && c / sin(c);
});
azimuthalEquidistantRaw.invert = azimuthalInvert(function(z) {
  return z;
});
function geoAzimuthalEquidistant() {
  return projection(azimuthalEquidistantRaw).scale(79.4188).clipAngle(180 - 1e-3);
}
function equirectangularRaw(lambda, phi) {
  return [lambda, phi];
}
equirectangularRaw.invert = equirectangularRaw;
var { BufferOp, GeoJSONReader, GeoJSONWriter } = jsts;
function buffer(geojson, radius, options) {
  options = options || {};
  var units = options.units || "kilometers";
  var steps = options.steps || 8;
  if (!geojson) throw new Error("geojson is required");
  if (typeof options !== "object") throw new Error("options must be an object");
  if (typeof steps !== "number") throw new Error("steps must be an number");
  if (radius === void 0) throw new Error("radius is required");
  if (steps <= 0) throw new Error("steps must be greater than 0");
  var results = [];
  switch (geojson.type) {
    case "GeometryCollection":
      geomEach(geojson, function(geometry) {
        var buffered = bufferFeature(geometry, radius, units, steps);
        if (buffered) results.push(buffered);
      });
      return featureCollection(results);
    case "FeatureCollection":
      featureEach(geojson, function(feature2) {
        var multiBuffered = bufferFeature(feature2, radius, units, steps);
        if (multiBuffered) {
          featureEach(multiBuffered, function(buffered) {
            if (buffered) results.push(buffered);
          });
        }
      });
      return featureCollection(results);
  }
  return bufferFeature(geojson, radius, units, steps);
}
function bufferFeature(geojson, radius, units, steps) {
  var properties = geojson.properties || {};
  var geometry = geojson.type === "Feature" ? geojson.geometry : geojson;
  if (geometry.type === "GeometryCollection") {
    var results = [];
    geomEach(geojson, function(geometry2) {
      var buffered2 = bufferFeature(geometry2, radius, units, steps);
      if (buffered2) results.push(buffered2);
    });
    return featureCollection(results);
  }
  var projection2 = defineProjection(geometry);
  var projected = {
    type: geometry.type,
    coordinates: projectCoords(geometry.coordinates, projection2)
  };
  var reader = new GeoJSONReader();
  var geom = reader.read(projected);
  var distance2 = radiansToLength(lengthToRadians(radius, units), "meters");
  var buffered = BufferOp.bufferOp(geom, distance2, steps);
  var writer = new GeoJSONWriter();
  buffered = writer.write(buffered);
  if (coordsIsNaN(buffered.coordinates)) return void 0;
  var result = {
    type: buffered.type,
    coordinates: unprojectCoords(buffered.coordinates, projection2)
  };
  return feature(result, properties);
}
function coordsIsNaN(coords) {
  if (Array.isArray(coords[0])) return coordsIsNaN(coords[0]);
  return isNaN(coords[0]);
}
function projectCoords(coords, proj) {
  if (typeof coords[0] !== "object") return proj(coords);
  return coords.map(function(coord) {
    return projectCoords(coord, proj);
  });
}
function unprojectCoords(coords, proj) {
  if (typeof coords[0] !== "object") return proj.invert(coords);
  return coords.map(function(coord) {
    return unprojectCoords(coord, proj);
  });
}
function defineProjection(geojson) {
  var coords = center(geojson).geometry.coordinates;
  var rotation = [-coords[0], -coords[1]];
  return geoAzimuthalEquidistant().rotate(rotation).scale(earthRadius);
}
var index_default$6 = buffer;
class TinyQueue {
  constructor(data = [], compare2 = defaultCompare) {
    this.data = data;
    this.length = this.data.length;
    this.compare = compare2;
    if (this.length > 0) {
      for (let i = (this.length >> 1) - 1; i >= 0; i--) this._down(i);
    }
  }
  push(item) {
    this.data.push(item);
    this.length++;
    this._up(this.length - 1);
  }
  pop() {
    if (this.length === 0) return void 0;
    const top = this.data[0];
    const bottom = this.data.pop();
    this.length--;
    if (this.length > 0) {
      this.data[0] = bottom;
      this._down(0);
    }
    return top;
  }
  peek() {
    return this.data[0];
  }
  _up(pos) {
    const { data, compare: compare2 } = this;
    const item = data[pos];
    while (pos > 0) {
      const parent = pos - 1 >> 1;
      const current = data[parent];
      if (compare2(item, current) >= 0) break;
      data[pos] = current;
      pos = parent;
    }
    data[pos] = item;
  }
  _down(pos) {
    const { data, compare: compare2 } = this;
    const halfLength = this.length >> 1;
    const item = data[pos];
    while (pos < halfLength) {
      let left = (pos << 1) + 1;
      let best = data[left];
      const right = left + 1;
      if (right < this.length && compare2(data[right], best) < 0) {
        left = right;
        best = data[right];
      }
      if (compare2(best, item) >= 0) break;
      data[pos] = best;
      pos = left;
    }
    data[pos] = item;
  }
}
function defaultCompare(a, b) {
  return a < b ? -1 : a > b ? 1 : 0;
}
function checkWhichEventIsLeft(e1, e2) {
  if (e1.p.x > e2.p.x) return 1;
  if (e1.p.x < e2.p.x) return -1;
  if (e1.p.y !== e2.p.y) return e1.p.y > e2.p.y ? 1 : -1;
  return 1;
}
function checkWhichSegmentHasRightEndpointFirst(seg1, seg2) {
  if (seg1.rightSweepEvent.p.x > seg2.rightSweepEvent.p.x) return 1;
  if (seg1.rightSweepEvent.p.x < seg2.rightSweepEvent.p.x) return -1;
  if (seg1.rightSweepEvent.p.y !== seg2.rightSweepEvent.p.y) return seg1.rightSweepEvent.p.y < seg2.rightSweepEvent.p.y ? 1 : -1;
  return 1;
}
class Event {
  constructor(p, featureId2, ringId2, eventId2) {
    this.p = {
      x: p[0],
      y: p[1]
    };
    this.featureId = featureId2;
    this.ringId = ringId2;
    this.eventId = eventId2;
    this.otherEvent = null;
    this.isLeftEndpoint = null;
  }
  isSamePoint(eventToCheck) {
    return this.p.x === eventToCheck.p.x && this.p.y === eventToCheck.p.y;
  }
}
function fillEventQueue(geojson, eventQueue) {
  if (geojson.type === "FeatureCollection") {
    const features = geojson.features;
    for (let i = 0; i < features.length; i++) {
      processFeature(features[i], eventQueue);
    }
  } else {
    processFeature(geojson, eventQueue);
  }
}
let featureId = 0;
let ringId = 0;
let eventId = 0;
function processFeature(featureOrGeometry, eventQueue) {
  const geom = featureOrGeometry.type === "Feature" ? featureOrGeometry.geometry : featureOrGeometry;
  let coords = geom.coordinates;
  if (geom.type === "Polygon" || geom.type === "MultiLineString") coords = [coords];
  if (geom.type === "LineString") coords = [[coords]];
  for (let i = 0; i < coords.length; i++) {
    for (let ii = 0; ii < coords[i].length; ii++) {
      let currentP = coords[i][ii][0];
      let nextP = null;
      ringId = ringId + 1;
      for (let iii = 0; iii < coords[i][ii].length - 1; iii++) {
        nextP = coords[i][ii][iii + 1];
        const e1 = new Event(currentP, featureId, ringId, eventId);
        const e2 = new Event(nextP, featureId, ringId, eventId + 1);
        e1.otherEvent = e2;
        e2.otherEvent = e1;
        if (checkWhichEventIsLeft(e1, e2) > 0) {
          e2.isLeftEndpoint = true;
          e1.isLeftEndpoint = false;
        } else {
          e1.isLeftEndpoint = true;
          e2.isLeftEndpoint = false;
        }
        eventQueue.push(e1);
        eventQueue.push(e2);
        currentP = nextP;
        eventId = eventId + 1;
      }
    }
  }
  featureId = featureId + 1;
}
let Segment$1 = class Segment {
  constructor(event) {
    this.leftSweepEvent = event;
    this.rightSweepEvent = event.otherEvent;
  }
};
function testSegmentIntersect(seg1, seg2) {
  if (seg1 === null || seg2 === null) return false;
  if (seg1.leftSweepEvent.ringId === seg2.leftSweepEvent.ringId && (seg1.rightSweepEvent.isSamePoint(seg2.leftSweepEvent) || seg1.rightSweepEvent.isSamePoint(seg2.leftSweepEvent) || seg1.rightSweepEvent.isSamePoint(seg2.rightSweepEvent) || seg1.leftSweepEvent.isSamePoint(seg2.leftSweepEvent) || seg1.leftSweepEvent.isSamePoint(seg2.rightSweepEvent))) return false;
  const x12 = seg1.leftSweepEvent.p.x;
  const y12 = seg1.leftSweepEvent.p.y;
  const x2 = seg1.rightSweepEvent.p.x;
  const y2 = seg1.rightSweepEvent.p.y;
  const x3 = seg2.leftSweepEvent.p.x;
  const y3 = seg2.leftSweepEvent.p.y;
  const x4 = seg2.rightSweepEvent.p.x;
  const y4 = seg2.rightSweepEvent.p.y;
  const denom = (y4 - y3) * (x2 - x12) - (x4 - x3) * (y2 - y12);
  const numeA = (x4 - x3) * (y12 - y3) - (y4 - y3) * (x12 - x3);
  const numeB = (x2 - x12) * (y12 - y3) - (y2 - y12) * (x12 - x3);
  if (denom === 0) {
    if (numeA === 0 && numeB === 0) return false;
    return false;
  }
  const uA = numeA / denom;
  const uB = numeB / denom;
  if (uA >= 0 && uA <= 1 && uB >= 0 && uB <= 1) {
    const x = x12 + uA * (x2 - x12);
    const y = y12 + uA * (y2 - y12);
    return [x, y];
  }
  return false;
}
function runCheck(eventQueue, ignoreSelfIntersections) {
  ignoreSelfIntersections = ignoreSelfIntersections ? ignoreSelfIntersections : false;
  const intersectionPoints = [];
  const outQueue = new TinyQueue([], checkWhichSegmentHasRightEndpointFirst);
  while (eventQueue.length) {
    const event = eventQueue.pop();
    if (event.isLeftEndpoint) {
      const segment = new Segment$1(event);
      for (let i = 0; i < outQueue.data.length; i++) {
        const otherSeg = outQueue.data[i];
        if (ignoreSelfIntersections) {
          if (otherSeg.leftSweepEvent.featureId === event.featureId) continue;
        }
        const intersection2 = testSegmentIntersect(segment, otherSeg);
        if (intersection2 !== false) intersectionPoints.push(intersection2);
      }
      outQueue.push(segment);
    } else if (event.isLeftEndpoint === false) {
      outQueue.pop();
    }
  }
  return intersectionPoints;
}
function sweeplineIntersections$1(geojson, ignoreSelfIntersections) {
  const eventQueue = new TinyQueue([], checkWhichEventIsLeft);
  fillEventQueue(geojson, eventQueue);
  return runCheck(eventQueue, ignoreSelfIntersections);
}
var sweeplineIntersections = sweeplineIntersections$1;
function lineIntersect(line1, line2, options = {}) {
  const { removeDuplicates = true, ignoreSelfIntersections = true } = options;
  let features = [];
  if (line1.type === "FeatureCollection")
    features = features.concat(line1.features);
  else if (line1.type === "Feature") features.push(line1);
  else if (line1.type === "LineString" || line1.type === "Polygon" || line1.type === "MultiLineString" || line1.type === "MultiPolygon") {
    features.push(feature(line1));
  }
  if (line2.type === "FeatureCollection")
    features = features.concat(line2.features);
  else if (line2.type === "Feature") features.push(line2);
  else if (line2.type === "LineString" || line2.type === "Polygon" || line2.type === "MultiLineString" || line2.type === "MultiPolygon") {
    features.push(feature(line2));
  }
  const intersections = sweeplineIntersections(
    featureCollection(features),
    ignoreSelfIntersections
  );
  let results = [];
  if (removeDuplicates) {
    const unique = {};
    intersections.forEach((intersection2) => {
      const key = intersection2.join(",");
      if (!unique[key]) {
        unique[key] = true;
        results.push(intersection2);
      }
    });
  } else {
    results = intersections;
  }
  return featureCollection(results.map((r2) => point(r2)));
}
var index_default$5 = lineIntersect;
function booleanClockwise(line2) {
  const ring = getCoords(line2);
  let sum2 = 0;
  let i = 1;
  let prev;
  let cur;
  while (i < ring.length) {
    prev = cur || ring[0];
    cur = ring[i];
    sum2 += (cur[0] - prev[0]) * (cur[1] + prev[1]);
    i++;
  }
  return sum2 > 0;
}
function rewind(geojson, options = {}) {
  var _a, _b;
  options = options || {};
  if (!isObject(options)) throw new Error("options is invalid");
  const mutate = (_a = options.mutate) != null ? _a : false;
  const reverse = (_b = options.reverse) != null ? _b : false;
  if (!geojson) throw new Error("<geojson> is required");
  if (typeof reverse !== "boolean")
    throw new Error("<reverse> must be a boolean");
  if (typeof mutate !== "boolean")
    throw new Error("<mutate> must be a boolean");
  if (!mutate && geojson.type !== "Point" && geojson.type !== "MultiPoint") {
    geojson = clone$1(geojson);
  }
  const results = [];
  switch (geojson.type) {
    case "GeometryCollection":
      geomEach(geojson, function(geometry) {
        rewindFeature(geometry, reverse);
      });
      return geojson;
    case "FeatureCollection":
      featureEach(geojson, function(feature2) {
        const rewoundFeature = rewindFeature(feature2, reverse);
        featureEach(rewoundFeature, function(result) {
          results.push(result);
        });
      });
      return featureCollection(results);
  }
  return rewindFeature(geojson, reverse);
}
function rewindFeature(geojson, reverse) {
  const type = geojson.type === "Feature" ? geojson.geometry.type : geojson.type;
  switch (type) {
    case "GeometryCollection":
      geomEach(geojson, function(geometry) {
        rewindFeature(geometry, reverse);
      });
      return geojson;
    case "LineString":
      rewindLineString(getCoords(geojson), reverse);
      return geojson;
    case "Polygon":
      rewindPolygon(getCoords(geojson), reverse);
      return geojson;
    case "MultiLineString":
      getCoords(geojson).forEach(function(lineCoords) {
        rewindLineString(lineCoords, reverse);
      });
      return geojson;
    case "MultiPolygon":
      getCoords(geojson).forEach(function(lineCoords) {
        rewindPolygon(lineCoords, reverse);
      });
      return geojson;
    case "Point":
    case "MultiPoint":
      return geojson;
  }
}
function rewindLineString(coords, reverse) {
  if (booleanClockwise(coords) === reverse) coords.reverse();
}
function rewindPolygon(coords, reverse) {
  if (booleanClockwise(coords[0]) !== reverse) {
    coords[0].reverse();
  }
  for (let i = 1; i < coords.length; i++) {
    if (booleanClockwise(coords[i]) === reverse) {
      coords[i].reverse();
    }
  }
}
var index_default$4 = rewind;
const pointBasedGeometryType = ["Point", "MultiPoint"];
const lineBasedGeometryType = [
  "LineString",
  "MultiLineString",
  "Polygon",
  "MultiPolygon"
];
const isPointBasedGeoJsonFeature = (feature2) => {
  return pointBasedGeometryType.includes(feature2.geometry.type);
};
const isLineBasedGeoJsonFeature = (feature2) => {
  return lineBasedGeometryType.includes(feature2.geometry.type);
};
const moveGeoJson = (geoJson, lngLatDiff) => {
  eachCoordinateWithPath(geoJson, (position) => {
    const lngLat = position.coordinate;
    lngLat[0] += lngLatDiff.lng;
    lngLat[1] += lngLatDiff.lat;
  });
  return geoJson;
};
const getBufferedOuterPolygon = (mapAdapter, polygonGeoJson) => {
  const FRACTION = 1e-4;
  const bounds = mapAdapter.getBounds();
  const widthMeters = index_default$a(bounds[0], bounds[1], { units: "meters" });
  return index_default$6(polygonGeoJson, widthMeters * FRACTION, { units: "meters" }) || null;
};
const getMovedGeoJson = (featureData, lngLatDiff) => {
  const featureGeoJson = cloneDeep(featureData.getGeoJson());
  moveGeoJson(featureGeoJson, lngLatDiff);
  return featureGeoJson;
};
const moveFeatureData = (featureData, lngLatDiff) => {
  const featureGeoJson = getMovedGeoJson(featureData, lngLatDiff);
  const shapeCenter = featureData.getShapeProperty("center");
  if (shapeCenter) {
    featureData.setShapeProperty("center", shapeCenter[0] + lngLatDiff.lng);
    featureData.setShapeProperty("center", shapeCenter[1] + lngLatDiff.lat);
  }
  featureData.updateGeoJsonGeometry(featureGeoJson.geometry);
};
const allFeaturePointsInPolygon = (featureGeoJson, containerGeoJson) => {
  try {
    eachCoordinateWithPath(featureGeoJson, (position) => {
      if (!booleanPointInPolygon(position.coordinate, containerGeoJson)) {
        throw new Error("stop");
      }
    });
  } catch {
    return false;
  }
  return true;
};
const isGeoJsonFeatureInPolygon = (featureGeoJson, containerGeoJson) => {
  const allPointsInPolygon = allFeaturePointsInPolygon(featureGeoJson, containerGeoJson);
  if (isPointBasedGeoJsonFeature(featureGeoJson)) {
    return allPointsInPolygon;
  }
  if (allPointsInPolygon && isLineBasedGeoJsonFeature(featureGeoJson)) {
    return !index_default$5(featureGeoJson, containerGeoJson, { ignoreSelfIntersections: true }).features.length;
  }
  return false;
};
const getFeatureFirstPoint = (featureData) => {
  const shapeGeoJson = featureData.getGeoJson();
  if (typeof shapeGeoJson !== "object") {
    return null;
  }
  return getGeoJsonFirstPoint(shapeGeoJson);
};
const fixGeoJsonFeature = (feature2) => {
  if (isLineBasedGeoJsonFeature(feature2)) {
    const resultFeature = index_default$4(feature2, { mutate: false });
    if (resultFeature.type === "Feature" && isLineBasedGeoJsonFeature(resultFeature)) {
      return {
        ...resultFeature,
        properties: feature2.properties || {}
      };
    }
  }
  if (isPointBasedGeoJsonFeature(feature2)) {
    return feature2;
  }
  return null;
};
const getCustomFeatureId = (featureGeoJson, idPropertyName) => {
  const featureId2 = featureGeoJson.properties?.[idPropertyName];
  if (typeof featureId2 === "string" || typeof featureId2 === "number") {
    return featureId2;
  }
  return null;
};
class BaseDrag extends BaseEdit {
  mode = "drag";
  previousLngLat = null;
  linkedFeatures = [];
  pointBasedShapes = ["marker", "circle_marker", "text_marker"];
  throttledMethods = convertToThrottled(
    {
      onMouseMove: this.onMouseMove
    },
    this,
    this.gm.options.settings.throttlingDelay
  );
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:edit`]: this.handleGmEdit.bind(this),
    mousedown: this.onMouseDown.bind(this),
    touchstart: this.onMouseDown.bind(this),
    mousemove: this.throttledMethods.onMouseMove.bind(this),
    touchmove: this.throttledMethods.onMouseMove.bind(this),
    mouseup: this.onMouseUp.bind(this),
    touchend: this.onMouseUp.bind(this)
  };
  getUpdatedGeoJsonHandlers = {
    marker: this.moveSource.bind(this),
    ellipse: this.moveEllipse.bind(this),
    circle: this.moveCircle.bind(this),
    circle_marker: this.moveSource.bind(this),
    text_marker: this.moveSource.bind(this),
    line: this.moveSource.bind(this),
    rectangle: this.moveSource.bind(this),
    polygon: this.moveSource.bind(this)
  };
  onMouseDown(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    const featureData = this.getFeatureByMouseEvent({ event, sourceNames: [SOURCES.main] });
    if (featureData && this.getUpdatedGeoJsonHandlers[featureData.shape]) {
      if (!this.gm.features.selection.has(featureData.id)) {
        this.gm.features.setSelection([featureData.id], true);
      }
      const linkedFeatures = this.gm.features.getLinkedFeatures(featureData);
      if (linkedFeatures.some((f) => f.getShapeProperty("disableEdit") === true)) {
        return { next: true };
      }
      this.featureData = featureData;
      this.linkedFeatures = linkedFeatures;
      this.gm.mapAdapter.setDragPan(false);
      this.gm.features.updateManager.beginTransaction("transactional-update");
      [this.featureData, ...this.linkedFeatures].map((featureData2) => {
        featureData2.changeSource({ sourceName: SOURCES.temporary, atomic: true });
        this.snappingHelper?.addExcludedFeature(featureData2);
        this.fireFeatureEditStartEvent({ feature: featureData2, forceMode: "drag" });
      });
      this.gm.features.updateManager.commitTransaction();
      this.flags.actionInProgress = true;
      if (this.isPointBasedShape()) {
        this.alignShapeCenterWithControlMarker(this.featureData, event);
      }
      return { next: false };
    }
    return { next: true };
  }
  onMouseUp(event) {
    if (!this.featureData || !isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    this.snappingHelper?.clearExcludedFeatures();
    this.gm.mapAdapter.setDragPan(true);
    this.flags.actionInProgress = false;
    this.gm.features.updateManager.beginTransaction("transactional-update");
    [this.featureData, ...this.linkedFeatures].map((featureData) => {
      featureData.changeSource({ sourceName: SOURCES.main, atomic: true });
      this.fireFeatureEditEndEvent({ feature: featureData, forceMode: "drag" });
    });
    this.gm.features.updateManager.commitTransaction();
    this.previousLngLat = null;
    this.featureData = null;
    this.linkedFeatures = [];
    return { next: true };
  }
  onMouseMove(event) {
    if (!this.flags.actionInProgress || !isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    const lngLatEnd = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    const lngLatStart = this.previousLngLat ?? lngLatEnd;
    const lngLatDiff = getLngLatDiff(lngLatStart, lngLatEnd);
    if (this.featureData) {
      this.gm.features.updateManager.beginTransaction("transactional-update", SOURCES.temporary);
      this.linkedFeatures.map((featureData) => {
        this.moveFeature(featureData, lngLatDiff);
      });
      const isUpdated = this.moveFeature(this.featureData, lngLatDiff);
      this.gm.features.updateManager.commitTransaction(SOURCES.temporary);
      if (isUpdated) {
        this.previousLngLat = lngLatEnd;
      }
    }
    return { next: false };
  }
  isPointBasedShape() {
    return !!this.featureData && this.pointBasedShapes.includes(this.featureData.shape);
  }
  alignShapeCenterWithControlMarker(featureData, event) {
    const shapeLngLat = getFeatureFirstPoint(featureData);
    if (shapeLngLat) {
      this.gm.markerPointer.marker?.setLngLat(shapeLngLat);
      this.onMouseMove(event);
    }
  }
  moveFeature(featureData, lngLatDiff) {
    let isUpdated = false;
    if (!this.flags.actionInProgress) {
      return;
    }
    const shapeUpdateMethod = this.getUpdatedGeoJsonHandlers[featureData.shape];
    if (shapeUpdateMethod) {
      const updatedGeoJson = shapeUpdateMethod(featureData, lngLatDiff);
      if (!updatedGeoJson) {
        log.error("BaseDrag.moveFeature: invalid updatedGeoJson", featureData);
        return;
      }
      this.fireBeforeFeatureUpdate({
        features: [featureData],
        geoJsonFeatures: [updatedGeoJson],
        forceMode: "drag"
      });
      isUpdated = this.updateFeatureGeoJson({
        featureData,
        featureGeoJson: updatedGeoJson,
        forceMode: "drag"
      });
      if (!isEqual(featureData.getGeoJson().properties, updatedGeoJson.properties)) {
        featureData._updateAllProperties(updatedGeoJson.properties);
      }
    }
    return isUpdated;
  }
  moveSource(featureData, lngLatDiff) {
    return getMovedGeoJson(featureData, lngLatDiff);
  }
  moveEllipse(featureData, lngLatDiff) {
    if (featureData.shape !== "ellipse") {
      log.error("BaseDrag.moveCircle: invalid shape type", featureData);
      return null;
    }
    const oldCenter = featureData.getShapeProperty("center");
    const xSemiAxis = featureData.getShapeProperty("xSemiAxis");
    const ySemiAxis = featureData.getShapeProperty("ySemiAxis");
    const angle = featureData.getShapeProperty("angle");
    if (!Array.isArray(oldCenter) || typeof xSemiAxis !== "number" || typeof ySemiAxis !== "number" || typeof angle !== "number") {
      log.error(
        "BaseDrag.moveEllipse: missing center, xSemiAxis, ySemiAxis or angle in the featureData",
        featureData
      );
      return null;
    }
    const newCenterCoords = [
      oldCenter[0] + lngLatDiff.lng,
      oldCenter[1] + lngLatDiff.lat
    ];
    return getGeoJsonEllipse({
      center: newCenterCoords,
      xSemiAxis,
      ySemiAxis,
      angle
    });
  }
  moveCircle(featureData, lngLatDiff) {
    if (featureData.shape !== "circle") {
      log.error("BaseDrag.moveCircle: invalid shape type", featureData);
      return null;
    }
    const shapeCenter = featureData.getShapeProperty("center");
    if (!Array.isArray(shapeCenter)) {
      log.error("BaseDrag.moveCircle: missing center in the featureData", featureData);
      return null;
    }
    const geoJson = featureData.getGeoJson();
    const circleRimLngLat = getGeoJsonFirstPoint(geoJson);
    if (!circleRimLngLat) {
      log.error("BaseDrag.moveCircle: missing center circleRimLngLat");
      return null;
    }
    const newCenterCoords = [
      shapeCenter[0] + lngLatDiff.lng,
      shapeCenter[1] + lngLatDiff.lat
    ];
    featureData.setShapeProperty("center", newCenterCoords);
    const circlePolygon = getGeoJsonCircle({
      center: newCenterCoords,
      radius: this.gm.mapAdapter.getDistance(shapeCenter, circleRimLngLat)
    });
    return circlePolygon;
  }
}
const UNIT_SYSTEMS = {
  imperial: "en-US",
  metric: "nb-NO"
};
const UNITS = {
  distance: {
    metric: [
      { range: [0, 1], unit: "cm", factor: 100 },
      { range: [1, 1e4], unit: "m", factor: 1 },
      { range: [1e4, Infinity], unit: "km", factor: 1e-3 }
    ],
    imperial: [
      { range: [0, 0.3048], factor: 39.3701, unit: "in" },
      { range: [0.3048, 1609.344], factor: 3.28084, unit: "ft" },
      { range: [1609.344, Infinity], factor: 621371e-9, unit: "mi" }
    ]
  },
  area: {
    metric: [
      { range: [0, 1], unit: "cm²", factor: 1e4 },
      { range: [1, 1e4], unit: "m²", factor: 1 },
      { range: [1e5, Infinity], unit: "km²", factor: 1e-6 }
    ],
    imperial: [
      { range: [0, 0.092903], factor: 1550.0031, unit: "in²" },
      { range: [0.092903, 4046.856], factor: 10.7639, unit: "ft²" },
      { range: [4046.856, 2589988], factor: 247105e-9, unit: "ac" },
      { range: [2589988, Infinity], factor: 3861e-10, unit: "mi²" }
    ]
  }
};
const toMod = (num, mod) => (num % mod + mod) % mod;
const formatNumber = (num, options) => {
  return new Intl.NumberFormat(UNIT_SYSTEMS[options.units], {
    minimumFractionDigits: options.minimumFractionDigits ?? 0,
    maximumFractionDigits: options.maximumFractionDigits ?? 2
  }).format(num);
};
const formatDistance = (num, options) => {
  const ranges = UNITS.distance[options.units];
  const range = ranges.find((item) => num >= item.range[0] && num < item.range[1]);
  if (range) {
    return `${formatNumber(num * range.factor, options)} ${range.unit}`;
  }
  return formatNumber(num, options);
};
const formatArea = (num, options) => {
  const ranges = UNITS.area[options.units];
  const range = ranges.find((item) => num >= item.range[0] && num < item.range[1]);
  if (range) {
    return `${formatNumber(num * range.factor, options)} ${range.unit}`;
  }
  return formatNumber(num, options);
};
class EditChange extends BaseDrag {
  mode = "change";
  cutVertexShapeTypes = ["line", "polygon", "rectangle"];
  markerData = null;
  shapeUpdateHandlers = {
    marker: this.updateSingleVertex.bind(this),
    circle: this.updateCircle.bind(this),
    circle_marker: this.updateSingleVertex.bind(this),
    ellipse: this.updateEllipse.bind(this),
    text_marker: this.updateSingleVertex.bind(this),
    line: this.updateSingleVertex.bind(this),
    rectangle: this.updateRectangle.bind(this),
    polygon: this.updateSingleVertex.bind(this)
  };
  get snapGuidesInstance() {
    const instance = this.gm.actionInstances.helper__snap_guides;
    return isSnapGuidesHelper(instance) ? instance : null;
  }
  onStartAction() {
  }
  onEndAction() {
    this.snapGuidesInstance?.removeSnapGuides();
  }
  handleGmEdit(event) {
    if (!isGmEditEvent(event)) {
      return { next: true };
    }
    if (event.action === "marker_move" && event.lngLatStart && event.markerData) {
      if (event.markerData.type === "vertex") {
        this.moveVertex(event);
        return { next: false };
      } else if (event.lngLatEnd) {
        const lngLatDiff = getLngLatDiff(event.lngLatStart, event.lngLatEnd);
        this.moveSource(event.featureData, lngLatDiff);
        return { next: false };
      }
    }
    if (event.action === "marker_right_click") {
      this.cutVertex(event);
      this.fireFeatureEditEndEvent({ feature: event.featureData });
    } else if (event.action === "edge_marker_click") {
      this.insertVertex(event);
    } else if (event.action === "marker_captured") {
      this.setCursorToPointer();
      event.featureData.changeSource({ sourceName: SOURCES.temporary, atomic: true });
      this.flags.actionInProgress = true;
      this.fireFeatureEditStartEvent({ feature: event.featureData });
    } else if (event.action === "marker_released") {
      this.markerData = null;
      this.snapGuidesInstance?.removeSnapGuides();
      event.featureData.changeSource({ sourceName: SOURCES.main, atomic: true });
      this.fireFeatureEditEndEvent({ feature: event.featureData });
      this.flags.actionInProgress = false;
    }
    return { next: true };
  }
  moveVertex(event) {
    if (!this.markerData) {
      this.markerData = event.markerData || null;
      this.snapGuidesInstance?.updateSnapGuides(event.featureData.getGeoJson(), event.lngLatStart);
    }
    const featureData = event.featureData;
    const shape = featureData.shape;
    const updatedGeoJson = this.shapeUpdateHandlers[shape]?.(event) || null;
    if (updatedGeoJson) {
      this.fireBeforeFeatureUpdate({
        features: [featureData],
        geoJsonFeatures: [updatedGeoJson]
      });
      this.updateFeatureGeoJson({ featureData, featureGeoJson: updatedGeoJson });
    } else {
      log.error("EditChange.moveVertex: invalid geojson", updatedGeoJson, event);
    }
  }
  cutVertex(event) {
    const featureData = event.featureData;
    if (event.markerData.type !== "vertex") {
      return;
    }
    if (!this.cutVertexShapeTypes.includes(featureData.shape)) {
      return;
    }
    let featureUpdated = false;
    const geoJson = featureData.getGeoJson();
    const marker2 = event.markerData.instance;
    if (isLineStringFeature(geoJson)) {
      if (getGeoJsonCoordinatesCount(geoJson) <= 2) {
        this.gm.features.delete(featureData);
        return;
      }
    } else if (isMultiPolygonFeature(geoJson)) {
      if (getGeoJsonCoordinatesCount(geoJson) <= 3) {
        this.gm.features.delete(featureData);
        return;
      }
    } else if (isPolygonFeature(geoJson)) {
      if (getGeoJsonCoordinatesCount(geoJson) <= 3) {
        this.gm.features.delete(featureData);
        return;
      }
    }
    const markerPosition = getFeatureFirstPoint(marker2);
    if (markerPosition) {
      featureUpdated = removeVertexFromGeoJsonFeature(geoJson, markerPosition);
    }
    if (featureUpdated) {
      featureData.convertToPolygon();
      featureData.updateGeoJsonGeometry(geoJson.geometry);
      this.fireFeatureUpdatedEvent({
        sourceFeatures: [featureData],
        targetFeatures: [featureData],
        markerData: event.markerData
      });
    } else {
      log.error("EditChange.cutVertex: feature not updated", event);
    }
  }
  insertVertex(event) {
    if (event.markerData.type !== "edge") {
      return;
    }
    const geoJson = event.featureData.getGeoJson();
    const coordinatesPath = event.markerData.segment.end.path;
    const insertIndex = coordinatesPath.pop();
    const coordinates = get$2(geoJson, coordinatesPath);
    if (typeof insertIndex === "number") {
      coordinates.splice(insertIndex, 0, [...event.markerData.position.coordinate]);
      event.featureData.updateGeoJsonGeometry(geoJson.geometry);
      event.featureData.convertToPolygon();
      this.fireFeatureUpdatedEvent({
        sourceFeatures: [event.featureData],
        targetFeatures: [event.featureData],
        markerData: event.markerData
      });
    }
  }
  updateSingleVertex({ featureData, lngLatEnd, markerData }) {
    const geoJson = cloneDeep(featureData.getGeoJson());
    const coordPath = cloneDeep(markerData.position.path);
    const coordIndex = coordPath.pop();
    const coordinates = get$2(geoJson, coordPath);
    if (Array.isArray(coordinates) && typeof coordIndex === "number") {
      coordinates[coordIndex] = [...lngLatEnd];
      if (coordIndex === 0 && featureData.shape === "polygon") {
        coordinates[coordinates.length - 1] = [...lngLatEnd];
      }
    } else {
      log.error("BaseDrag.moveSingleVertex: invalid coordinates", geoJson, coordPath);
    }
    return geoJson;
  }
  updateCircle({ featureData, lngLatEnd }) {
    const shapeCenter = featureData.getShapeProperty("center");
    if (featureData.shape !== "circle" || !shapeCenter) {
      log.error("BaseDrag.moveCircle: invalid shape type / missing center", featureData);
      return null;
    }
    const circlePolygon = getGeoJsonCircle({
      center: shapeCenter,
      radius: this.gm.mapAdapter.getDistance(shapeCenter, lngLatEnd)
    });
    return circlePolygon;
  }
  updateEllipse(args) {
    const { featureData, lngLatEnd, markerData } = args;
    if (featureData.shape !== "ellipse") {
      log.error("EditChange.updateEllipse: invalid shape type", featureData);
      return null;
    }
    const center2 = featureData.getShapeProperty("center");
    let xSemiAxis = featureData.getShapeProperty("xSemiAxis");
    let ySemiAxis = featureData.getShapeProperty("ySemiAxis");
    const angle = featureData.getShapeProperty("angle");
    if (!Array.isArray(center2) || typeof xSemiAxis !== "number" || typeof ySemiAxis !== "number" || typeof angle !== "number") {
      log.error(
        "updateEllipse: missing center, xSemiAxis, ySemiAxis or angle in the featureData",
        featureData
      );
      return null;
    }
    const distance2 = this.gm.mapAdapter.getDistance(center2, lngLatEnd);
    const vertexIdx = markerData.position.path[3];
    const vertexRatio = Math.floor(vertexIdx / ellipseSteps * 4);
    const axe = vertexRatio === 0 || vertexRatio === 2 ? "x" : "y";
    if (axe === "x") {
      xSemiAxis = distance2;
    } else {
      ySemiAxis = distance2;
    }
    const ellipsePolygon = getGeoJsonEllipse({
      center: center2,
      xSemiAxis,
      ySemiAxis,
      angle
    });
    return ellipsePolygon;
  }
  updateRectangle({ featureData, lngLatStart, lngLatEnd }) {
    const totalCoordsCount = 4;
    const geoJson = featureData.getGeoJson();
    const shapeCoords = geoJson.geometry.coordinates[0];
    const { absCoordIndex: startIndex } = findCoordinateIndices(geoJson, lngLatStart);
    if (startIndex === -1) {
      log.error("EditChange.updateRectangle: start vertex not found", featureData);
      return null;
    }
    const oppositeVertexIndex = toMod(startIndex - 2, totalCoordsCount);
    const oppositeCoordinate = shapeCoords[oppositeVertexIndex];
    return twoCoordsToGeoJsonRectangle(lngLatEnd, oppositeCoordinate);
  }
}
function booleanDisjoint(feature1, feature2, {
  ignoreSelfIntersections = true
} = { ignoreSelfIntersections: true }) {
  let bool = true;
  flattenEach(feature1, (flatten1) => {
    flattenEach(feature2, (flatten2) => {
      if (bool === false) {
        return false;
      }
      bool = disjoint(
        flatten1.geometry,
        flatten2.geometry,
        ignoreSelfIntersections
      );
    });
  });
  return bool;
}
function disjoint(geom1, geom2, ignoreSelfIntersections) {
  switch (geom1.type) {
    case "Point":
      switch (geom2.type) {
        case "Point":
          return !compareCoords(geom1.coordinates, geom2.coordinates);
        case "LineString":
          return !isPointOnLine(geom2, geom1);
        case "Polygon":
          return !booleanPointInPolygon(geom1, geom2);
      }
      break;
    case "LineString":
      switch (geom2.type) {
        case "Point":
          return !isPointOnLine(geom1, geom2);
        case "LineString":
          return !isLineOnLine(geom1, geom2, ignoreSelfIntersections);
        case "Polygon":
          return !isLineInPoly(geom2, geom1, ignoreSelfIntersections);
      }
      break;
    case "Polygon":
      switch (geom2.type) {
        case "Point":
          return !booleanPointInPolygon(geom2, geom1);
        case "LineString":
          return !isLineInPoly(geom1, geom2, ignoreSelfIntersections);
        case "Polygon":
          return !isPolyInPoly(geom2, geom1, ignoreSelfIntersections);
      }
  }
  return false;
}
function isPointOnLine(lineString2, pt) {
  for (let i = 0; i < lineString2.coordinates.length - 1; i++) {
    if (isPointOnLineSegment(
      lineString2.coordinates[i],
      lineString2.coordinates[i + 1],
      pt.coordinates
    )) {
      return true;
    }
  }
  return false;
}
function isLineOnLine(lineString1, lineString2, ignoreSelfIntersections) {
  const doLinesIntersect = lineIntersect(lineString1, lineString2, {
    ignoreSelfIntersections
  });
  if (doLinesIntersect.features.length > 0) {
    return true;
  }
  return false;
}
function isLineInPoly(polygon2, lineString2, ignoreSelfIntersections) {
  for (const coord of lineString2.coordinates) {
    if (booleanPointInPolygon(coord, polygon2)) {
      return true;
    }
  }
  const doLinesIntersect = lineIntersect(lineString2, polygonToLine(polygon2), {
    ignoreSelfIntersections
  });
  if (doLinesIntersect.features.length > 0) {
    return true;
  }
  return false;
}
function isPolyInPoly(feature1, feature2, ignoreSelfIntersections) {
  for (const coord1 of feature1.coordinates[0]) {
    if (booleanPointInPolygon(coord1, feature2)) {
      return true;
    }
  }
  for (const coord2 of feature2.coordinates[0]) {
    if (booleanPointInPolygon(coord2, feature1)) {
      return true;
    }
  }
  const doLinesIntersect = lineIntersect(
    polygonToLine(feature1),
    polygonToLine(feature2),
    { ignoreSelfIntersections }
  );
  if (doLinesIntersect.features.length > 0) {
    return true;
  }
  return false;
}
function isPointOnLineSegment(lineSegmentStart, lineSegmentEnd, pt) {
  const dxc = pt[0] - lineSegmentStart[0];
  const dyc = pt[1] - lineSegmentStart[1];
  const dxl = lineSegmentEnd[0] - lineSegmentStart[0];
  const dyl = lineSegmentEnd[1] - lineSegmentStart[1];
  const cross2 = dxc * dyl - dyc * dxl;
  if (cross2 !== 0) {
    return false;
  }
  if (Math.abs(dxl) >= Math.abs(dyl)) {
    if (dxl > 0) {
      return lineSegmentStart[0] <= pt[0] && pt[0] <= lineSegmentEnd[0];
    } else {
      return lineSegmentEnd[0] <= pt[0] && pt[0] <= lineSegmentStart[0];
    }
  } else if (dyl > 0) {
    return lineSegmentStart[1] <= pt[1] && pt[1] <= lineSegmentEnd[1];
  } else {
    return lineSegmentEnd[1] <= pt[1] && pt[1] <= lineSegmentStart[1];
  }
}
function compareCoords(pair1, pair2) {
  return pair1[0] === pair2[0] && pair1[1] === pair2[1];
}
function booleanIntersects(feature1, feature2, {
  ignoreSelfIntersections = true
} = {}) {
  let bool = false;
  flattenEach(feature1, (flatten1) => {
    flattenEach(feature2, (flatten2) => {
      if (bool === true) {
        return true;
      }
      bool = !booleanDisjoint(flatten1.geometry, flatten2.geometry, {
        ignoreSelfIntersections
      });
    });
  });
  return bool;
}
var index_default$3 = booleanIntersects;
var isNumeric = /^-?(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?$/i, mathceil = Math.ceil, mathfloor = Math.floor, bignumberError = "[BigNumber Error] ", tooManyDigits = bignumberError + "Number primitive has more than 15 significant digits: ", BASE = 1e14, LOG_BASE = 14, MAX_SAFE_INTEGER = 9007199254740991, POWS_TEN = [1, 10, 100, 1e3, 1e4, 1e5, 1e6, 1e7, 1e8, 1e9, 1e10, 1e11, 1e12, 1e13], SQRT_BASE = 1e7, MAX = 1e9;
function clone(configObject) {
  var div, convertBase, parseNumeric, P = BigNumber2.prototype = { constructor: BigNumber2, toString: null, valueOf: null }, ONE = new BigNumber2(1), DECIMAL_PLACES = 20, ROUNDING_MODE = 4, TO_EXP_NEG = -7, TO_EXP_POS = 21, MIN_EXP = -1e7, MAX_EXP = 1e7, CRYPTO = false, MODULO_MODE = 1, POW_PRECISION = 0, FORMAT = {
    prefix: "",
    groupSize: 3,
    secondaryGroupSize: 0,
    groupSeparator: ",",
    decimalSeparator: ".",
    fractionGroupSize: 0,
    fractionGroupSeparator: " ",
    // non-breaking space
    suffix: ""
  }, ALPHABET = "0123456789abcdefghijklmnopqrstuvwxyz", alphabetHasNormalDecimalDigits = true;
  function BigNumber2(v, b) {
    var alphabet, c, caseChanged, e, i, isNum, len, str, x = this;
    if (!(x instanceof BigNumber2)) return new BigNumber2(v, b);
    if (b == null) {
      if (v && v._isBigNumber === true) {
        x.s = v.s;
        if (!v.c || v.e > MAX_EXP) {
          x.c = x.e = null;
        } else if (v.e < MIN_EXP) {
          x.c = [x.e = 0];
        } else {
          x.e = v.e;
          x.c = v.c.slice();
        }
        return;
      }
      if ((isNum = typeof v == "number") && v * 0 == 0) {
        x.s = 1 / v < 0 ? (v = -v, -1) : 1;
        if (v === ~~v) {
          for (e = 0, i = v; i >= 10; i /= 10, e++) ;
          if (e > MAX_EXP) {
            x.c = x.e = null;
          } else {
            x.e = e;
            x.c = [v];
          }
          return;
        }
        str = String(v);
      } else {
        if (!isNumeric.test(str = String(v))) return parseNumeric(x, str, isNum);
        x.s = str.charCodeAt(0) == 45 ? (str = str.slice(1), -1) : 1;
      }
      if ((e = str.indexOf(".")) > -1) str = str.replace(".", "");
      if ((i = str.search(/e/i)) > 0) {
        if (e < 0) e = i;
        e += +str.slice(i + 1);
        str = str.substring(0, i);
      } else if (e < 0) {
        e = str.length;
      }
    } else {
      intCheck(b, 2, ALPHABET.length, "Base");
      if (b == 10 && alphabetHasNormalDecimalDigits) {
        x = new BigNumber2(v);
        return round(x, DECIMAL_PLACES + x.e + 1, ROUNDING_MODE);
      }
      str = String(v);
      if (isNum = typeof v == "number") {
        if (v * 0 != 0) return parseNumeric(x, str, isNum, b);
        x.s = 1 / v < 0 ? (str = str.slice(1), -1) : 1;
        if (BigNumber2.DEBUG && str.replace(/^0\.0*|\./, "").length > 15) {
          throw Error(tooManyDigits + v);
        }
      } else {
        x.s = str.charCodeAt(0) === 45 ? (str = str.slice(1), -1) : 1;
      }
      alphabet = ALPHABET.slice(0, b);
      e = i = 0;
      for (len = str.length; i < len; i++) {
        if (alphabet.indexOf(c = str.charAt(i)) < 0) {
          if (c == ".") {
            if (i > e) {
              e = len;
              continue;
            }
          } else if (!caseChanged) {
            if (str == str.toUpperCase() && (str = str.toLowerCase()) || str == str.toLowerCase() && (str = str.toUpperCase())) {
              caseChanged = true;
              i = -1;
              e = 0;
              continue;
            }
          }
          return parseNumeric(x, String(v), isNum, b);
        }
      }
      isNum = false;
      str = convertBase(str, b, 10, x.s);
      if ((e = str.indexOf(".")) > -1) str = str.replace(".", "");
      else e = str.length;
    }
    for (i = 0; str.charCodeAt(i) === 48; i++) ;
    for (len = str.length; str.charCodeAt(--len) === 48; ) ;
    if (str = str.slice(i, ++len)) {
      len -= i;
      if (isNum && BigNumber2.DEBUG && len > 15 && (v > MAX_SAFE_INTEGER || v !== mathfloor(v))) {
        throw Error(tooManyDigits + x.s * v);
      }
      if ((e = e - i - 1) > MAX_EXP) {
        x.c = x.e = null;
      } else if (e < MIN_EXP) {
        x.c = [x.e = 0];
      } else {
        x.e = e;
        x.c = [];
        i = (e + 1) % LOG_BASE;
        if (e < 0) i += LOG_BASE;
        if (i < len) {
          if (i) x.c.push(+str.slice(0, i));
          for (len -= LOG_BASE; i < len; ) {
            x.c.push(+str.slice(i, i += LOG_BASE));
          }
          i = LOG_BASE - (str = str.slice(i)).length;
        } else {
          i -= len;
        }
        for (; i--; str += "0") ;
        x.c.push(+str);
      }
    } else {
      x.c = [x.e = 0];
    }
  }
  BigNumber2.clone = clone;
  BigNumber2.ROUND_UP = 0;
  BigNumber2.ROUND_DOWN = 1;
  BigNumber2.ROUND_CEIL = 2;
  BigNumber2.ROUND_FLOOR = 3;
  BigNumber2.ROUND_HALF_UP = 4;
  BigNumber2.ROUND_HALF_DOWN = 5;
  BigNumber2.ROUND_HALF_EVEN = 6;
  BigNumber2.ROUND_HALF_CEIL = 7;
  BigNumber2.ROUND_HALF_FLOOR = 8;
  BigNumber2.EUCLID = 9;
  BigNumber2.config = BigNumber2.set = function(obj) {
    var p, v;
    if (obj != null) {
      if (typeof obj == "object") {
        if (obj.hasOwnProperty(p = "DECIMAL_PLACES")) {
          v = obj[p];
          intCheck(v, 0, MAX, p);
          DECIMAL_PLACES = v;
        }
        if (obj.hasOwnProperty(p = "ROUNDING_MODE")) {
          v = obj[p];
          intCheck(v, 0, 8, p);
          ROUNDING_MODE = v;
        }
        if (obj.hasOwnProperty(p = "EXPONENTIAL_AT")) {
          v = obj[p];
          if (v && v.pop) {
            intCheck(v[0], -MAX, 0, p);
            intCheck(v[1], 0, MAX, p);
            TO_EXP_NEG = v[0];
            TO_EXP_POS = v[1];
          } else {
            intCheck(v, -MAX, MAX, p);
            TO_EXP_NEG = -(TO_EXP_POS = v < 0 ? -v : v);
          }
        }
        if (obj.hasOwnProperty(p = "RANGE")) {
          v = obj[p];
          if (v && v.pop) {
            intCheck(v[0], -MAX, -1, p);
            intCheck(v[1], 1, MAX, p);
            MIN_EXP = v[0];
            MAX_EXP = v[1];
          } else {
            intCheck(v, -MAX, MAX, p);
            if (v) {
              MIN_EXP = -(MAX_EXP = v < 0 ? -v : v);
            } else {
              throw Error(bignumberError + p + " cannot be zero: " + v);
            }
          }
        }
        if (obj.hasOwnProperty(p = "CRYPTO")) {
          v = obj[p];
          if (v === !!v) {
            if (v) {
              if (typeof crypto != "undefined" && crypto && (crypto.getRandomValues || crypto.randomBytes)) {
                CRYPTO = v;
              } else {
                CRYPTO = !v;
                throw Error(bignumberError + "crypto unavailable");
              }
            } else {
              CRYPTO = v;
            }
          } else {
            throw Error(bignumberError + p + " not true or false: " + v);
          }
        }
        if (obj.hasOwnProperty(p = "MODULO_MODE")) {
          v = obj[p];
          intCheck(v, 0, 9, p);
          MODULO_MODE = v;
        }
        if (obj.hasOwnProperty(p = "POW_PRECISION")) {
          v = obj[p];
          intCheck(v, 0, MAX, p);
          POW_PRECISION = v;
        }
        if (obj.hasOwnProperty(p = "FORMAT")) {
          v = obj[p];
          if (typeof v == "object") FORMAT = v;
          else throw Error(bignumberError + p + " not an object: " + v);
        }
        if (obj.hasOwnProperty(p = "ALPHABET")) {
          v = obj[p];
          if (typeof v == "string" && !/^.?$|[+\-.\s]|(.).*\1/.test(v)) {
            alphabetHasNormalDecimalDigits = v.slice(0, 10) == "0123456789";
            ALPHABET = v;
          } else {
            throw Error(bignumberError + p + " invalid: " + v);
          }
        }
      } else {
        throw Error(bignumberError + "Object expected: " + obj);
      }
    }
    return {
      DECIMAL_PLACES,
      ROUNDING_MODE,
      EXPONENTIAL_AT: [TO_EXP_NEG, TO_EXP_POS],
      RANGE: [MIN_EXP, MAX_EXP],
      CRYPTO,
      MODULO_MODE,
      POW_PRECISION,
      FORMAT,
      ALPHABET
    };
  };
  BigNumber2.isBigNumber = function(v) {
    if (!v || v._isBigNumber !== true) return false;
    if (!BigNumber2.DEBUG) return true;
    var i, n, c = v.c, e = v.e, s = v.s;
    out: if ({}.toString.call(c) == "[object Array]") {
      if ((s === 1 || s === -1) && e >= -MAX && e <= MAX && e === mathfloor(e)) {
        if (c[0] === 0) {
          if (e === 0 && c.length === 1) return true;
          break out;
        }
        i = (e + 1) % LOG_BASE;
        if (i < 1) i += LOG_BASE;
        if (String(c[0]).length == i) {
          for (i = 0; i < c.length; i++) {
            n = c[i];
            if (n < 0 || n >= BASE || n !== mathfloor(n)) break out;
          }
          if (n !== 0) return true;
        }
      }
    } else if (c === null && e === null && (s === null || s === 1 || s === -1)) {
      return true;
    }
    throw Error(bignumberError + "Invalid BigNumber: " + v);
  };
  BigNumber2.maximum = BigNumber2.max = function() {
    return maxOrMin(arguments, -1);
  };
  BigNumber2.minimum = BigNumber2.min = function() {
    return maxOrMin(arguments, 1);
  };
  BigNumber2.random = (function() {
    var pow2_53 = 9007199254740992;
    var random53bitInt = Math.random() * pow2_53 & 2097151 ? function() {
      return mathfloor(Math.random() * pow2_53);
    } : function() {
      return (Math.random() * 1073741824 | 0) * 8388608 + (Math.random() * 8388608 | 0);
    };
    return function(dp) {
      var a, b, e, k, v, i = 0, c = [], rand = new BigNumber2(ONE);
      if (dp == null) dp = DECIMAL_PLACES;
      else intCheck(dp, 0, MAX);
      k = mathceil(dp / LOG_BASE);
      if (CRYPTO) {
        if (crypto.getRandomValues) {
          a = crypto.getRandomValues(new Uint32Array(k *= 2));
          for (; i < k; ) {
            v = a[i] * 131072 + (a[i + 1] >>> 11);
            if (v >= 9e15) {
              b = crypto.getRandomValues(new Uint32Array(2));
              a[i] = b[0];
              a[i + 1] = b[1];
            } else {
              c.push(v % 1e14);
              i += 2;
            }
          }
          i = k / 2;
        } else if (crypto.randomBytes) {
          a = crypto.randomBytes(k *= 7);
          for (; i < k; ) {
            v = (a[i] & 31) * 281474976710656 + a[i + 1] * 1099511627776 + a[i + 2] * 4294967296 + a[i + 3] * 16777216 + (a[i + 4] << 16) + (a[i + 5] << 8) + a[i + 6];
            if (v >= 9e15) {
              crypto.randomBytes(7).copy(a, i);
            } else {
              c.push(v % 1e14);
              i += 7;
            }
          }
          i = k / 7;
        } else {
          CRYPTO = false;
          throw Error(bignumberError + "crypto unavailable");
        }
      }
      if (!CRYPTO) {
        for (; i < k; ) {
          v = random53bitInt();
          if (v < 9e15) c[i++] = v % 1e14;
        }
      }
      k = c[--i];
      dp %= LOG_BASE;
      if (k && dp) {
        v = POWS_TEN[LOG_BASE - dp];
        c[i] = mathfloor(k / v) * v;
      }
      for (; c[i] === 0; c.pop(), i--) ;
      if (i < 0) {
        c = [e = 0];
      } else {
        for (e = -1; c[0] === 0; c.splice(0, 1), e -= LOG_BASE) ;
        for (i = 1, v = c[0]; v >= 10; v /= 10, i++) ;
        if (i < LOG_BASE) e -= LOG_BASE - i;
      }
      rand.e = e;
      rand.c = c;
      return rand;
    };
  })();
  BigNumber2.sum = function() {
    var i = 1, args = arguments, sum2 = new BigNumber2(args[0]);
    for (; i < args.length; ) sum2 = sum2.plus(args[i++]);
    return sum2;
  };
  convertBase = /* @__PURE__ */ (function() {
    var decimal = "0123456789";
    function toBaseOut(str, baseIn, baseOut, alphabet) {
      var j, arr = [0], arrL, i = 0, len = str.length;
      for (; i < len; ) {
        for (arrL = arr.length; arrL--; arr[arrL] *= baseIn) ;
        arr[0] += alphabet.indexOf(str.charAt(i++));
        for (j = 0; j < arr.length; j++) {
          if (arr[j] > baseOut - 1) {
            if (arr[j + 1] == null) arr[j + 1] = 0;
            arr[j + 1] += arr[j] / baseOut | 0;
            arr[j] %= baseOut;
          }
        }
      }
      return arr.reverse();
    }
    return function(str, baseIn, baseOut, sign, callerIsToString) {
      var alphabet, d, e, k, r2, x, xc, y, i = str.indexOf("."), dp = DECIMAL_PLACES, rm = ROUNDING_MODE;
      if (i >= 0) {
        k = POW_PRECISION;
        POW_PRECISION = 0;
        str = str.replace(".", "");
        y = new BigNumber2(baseIn);
        x = y.pow(str.length - i);
        POW_PRECISION = k;
        y.c = toBaseOut(
          toFixedPoint(coeffToString(x.c), x.e, "0"),
          10,
          baseOut,
          decimal
        );
        y.e = y.c.length;
      }
      xc = toBaseOut(str, baseIn, baseOut, callerIsToString ? (alphabet = ALPHABET, decimal) : (alphabet = decimal, ALPHABET));
      e = k = xc.length;
      for (; xc[--k] == 0; xc.pop()) ;
      if (!xc[0]) return alphabet.charAt(0);
      if (i < 0) {
        --e;
      } else {
        x.c = xc;
        x.e = e;
        x.s = sign;
        x = div(x, y, dp, rm, baseOut);
        xc = x.c;
        r2 = x.r;
        e = x.e;
      }
      d = e + dp + 1;
      i = xc[d];
      k = baseOut / 2;
      r2 = r2 || d < 0 || xc[d + 1] != null;
      r2 = rm < 4 ? (i != null || r2) && (rm == 0 || rm == (x.s < 0 ? 3 : 2)) : i > k || i == k && (rm == 4 || r2 || rm == 6 && xc[d - 1] & 1 || rm == (x.s < 0 ? 8 : 7));
      if (d < 1 || !xc[0]) {
        str = r2 ? toFixedPoint(alphabet.charAt(1), -dp, alphabet.charAt(0)) : alphabet.charAt(0);
      } else {
        xc.length = d;
        if (r2) {
          for (--baseOut; ++xc[--d] > baseOut; ) {
            xc[d] = 0;
            if (!d) {
              ++e;
              xc = [1].concat(xc);
            }
          }
        }
        for (k = xc.length; !xc[--k]; ) ;
        for (i = 0, str = ""; i <= k; str += alphabet.charAt(xc[i++])) ;
        str = toFixedPoint(str, e, alphabet.charAt(0));
      }
      return str;
    };
  })();
  div = /* @__PURE__ */ (function() {
    function multiply(x, k, base) {
      var m, temp2, xlo, xhi, carry = 0, i = x.length, klo = k % SQRT_BASE, khi = k / SQRT_BASE | 0;
      for (x = x.slice(); i--; ) {
        xlo = x[i] % SQRT_BASE;
        xhi = x[i] / SQRT_BASE | 0;
        m = khi * xlo + xhi * klo;
        temp2 = klo * xlo + m % SQRT_BASE * SQRT_BASE + carry;
        carry = (temp2 / base | 0) + (m / SQRT_BASE | 0) + khi * xhi;
        x[i] = temp2 % base;
      }
      if (carry) x = [carry].concat(x);
      return x;
    }
    function compare2(a, b, aL, bL) {
      var i, cmp;
      if (aL != bL) {
        cmp = aL > bL ? 1 : -1;
      } else {
        for (i = cmp = 0; i < aL; i++) {
          if (a[i] != b[i]) {
            cmp = a[i] > b[i] ? 1 : -1;
            break;
          }
        }
      }
      return cmp;
    }
    function subtract(a, b, aL, base) {
      var i = 0;
      for (; aL--; ) {
        a[aL] -= i;
        i = a[aL] < b[aL] ? 1 : 0;
        a[aL] = i * base + a[aL] - b[aL];
      }
      for (; !a[0] && a.length > 1; a.splice(0, 1)) ;
    }
    return function(x, y, dp, rm, base) {
      var cmp, e, i, more, n, prod, prodL, q, qc, rem, remL, rem0, xi, xL, yc0, yL, yz, s = x.s == y.s ? 1 : -1, xc = x.c, yc = y.c;
      if (!xc || !xc[0] || !yc || !yc[0]) {
        return new BigNumber2(
          // Return NaN if either NaN, or both Infinity or 0.
          !x.s || !y.s || (xc ? yc && xc[0] == yc[0] : !yc) ? NaN : (
            // Return ±0 if x is ±0 or y is ±Infinity, or return ±Infinity as y is ±0.
            xc && xc[0] == 0 || !yc ? s * 0 : s / 0
          )
        );
      }
      q = new BigNumber2(s);
      qc = q.c = [];
      e = x.e - y.e;
      s = dp + e + 1;
      if (!base) {
        base = BASE;
        e = bitFloor(x.e / LOG_BASE) - bitFloor(y.e / LOG_BASE);
        s = s / LOG_BASE | 0;
      }
      for (i = 0; yc[i] == (xc[i] || 0); i++) ;
      if (yc[i] > (xc[i] || 0)) e--;
      if (s < 0) {
        qc.push(1);
        more = true;
      } else {
        xL = xc.length;
        yL = yc.length;
        i = 0;
        s += 2;
        n = mathfloor(base / (yc[0] + 1));
        if (n > 1) {
          yc = multiply(yc, n, base);
          xc = multiply(xc, n, base);
          yL = yc.length;
          xL = xc.length;
        }
        xi = yL;
        rem = xc.slice(0, yL);
        remL = rem.length;
        for (; remL < yL; rem[remL++] = 0) ;
        yz = yc.slice();
        yz = [0].concat(yz);
        yc0 = yc[0];
        if (yc[1] >= base / 2) yc0++;
        do {
          n = 0;
          cmp = compare2(yc, rem, yL, remL);
          if (cmp < 0) {
            rem0 = rem[0];
            if (yL != remL) rem0 = rem0 * base + (rem[1] || 0);
            n = mathfloor(rem0 / yc0);
            if (n > 1) {
              if (n >= base) n = base - 1;
              prod = multiply(yc, n, base);
              prodL = prod.length;
              remL = rem.length;
              while (compare2(prod, rem, prodL, remL) == 1) {
                n--;
                subtract(prod, yL < prodL ? yz : yc, prodL, base);
                prodL = prod.length;
                cmp = 1;
              }
            } else {
              if (n == 0) {
                cmp = n = 1;
              }
              prod = yc.slice();
              prodL = prod.length;
            }
            if (prodL < remL) prod = [0].concat(prod);
            subtract(rem, prod, remL, base);
            remL = rem.length;
            if (cmp == -1) {
              while (compare2(yc, rem, yL, remL) < 1) {
                n++;
                subtract(rem, yL < remL ? yz : yc, remL, base);
                remL = rem.length;
              }
            }
          } else if (cmp === 0) {
            n++;
            rem = [0];
          }
          qc[i++] = n;
          if (rem[0]) {
            rem[remL++] = xc[xi] || 0;
          } else {
            rem = [xc[xi]];
            remL = 1;
          }
        } while ((xi++ < xL || rem[0] != null) && s--);
        more = rem[0] != null;
        if (!qc[0]) qc.splice(0, 1);
      }
      if (base == BASE) {
        for (i = 1, s = qc[0]; s >= 10; s /= 10, i++) ;
        round(q, dp + (q.e = i + e * LOG_BASE - 1) + 1, rm, more);
      } else {
        q.e = e;
        q.r = +more;
      }
      return q;
    };
  })();
  function format(n, i, rm, id) {
    var c0, e, ne, len, str;
    if (rm == null) rm = ROUNDING_MODE;
    else intCheck(rm, 0, 8);
    if (!n.c) return n.toString();
    c0 = n.c[0];
    ne = n.e;
    if (i == null) {
      str = coeffToString(n.c);
      str = id == 1 || id == 2 && (ne <= TO_EXP_NEG || ne >= TO_EXP_POS) ? toExponential(str, ne) : toFixedPoint(str, ne, "0");
    } else {
      n = round(new BigNumber2(n), i, rm);
      e = n.e;
      str = coeffToString(n.c);
      len = str.length;
      if (id == 1 || id == 2 && (i <= e || e <= TO_EXP_NEG)) {
        for (; len < i; str += "0", len++) ;
        str = toExponential(str, e);
      } else {
        i -= ne + (id === 2 && e > ne);
        str = toFixedPoint(str, e, "0");
        if (e + 1 > len) {
          if (--i > 0) for (str += "."; i--; str += "0") ;
        } else {
          i += e - len;
          if (i > 0) {
            if (e + 1 == len) str += ".";
            for (; i--; str += "0") ;
          }
        }
      }
    }
    return n.s < 0 && c0 ? "-" + str : str;
  }
  function maxOrMin(args, n) {
    var k, y, i = 1, x = new BigNumber2(args[0]);
    for (; i < args.length; i++) {
      y = new BigNumber2(args[i]);
      if (!y.s || (k = compare(x, y)) === n || k === 0 && x.s === n) {
        x = y;
      }
    }
    return x;
  }
  function normalise(n, c, e) {
    var i = 1, j = c.length;
    for (; !c[--j]; c.pop()) ;
    for (j = c[0]; j >= 10; j /= 10, i++) ;
    if ((e = i + e * LOG_BASE - 1) > MAX_EXP) {
      n.c = n.e = null;
    } else if (e < MIN_EXP) {
      n.c = [n.e = 0];
    } else {
      n.e = e;
      n.c = c;
    }
    return n;
  }
  parseNumeric = /* @__PURE__ */ (function() {
    var basePrefix = /^(-?)0([xbo])(?=\w[\w.]*$)/i, dotAfter = /^([^.]+)\.$/, dotBefore = /^\.([^.]+)$/, isInfinityOrNaN = /^-?(Infinity|NaN)$/, whitespaceOrPlus = /^\s*\+(?=[\w.])|^\s+|\s+$/g;
    return function(x, str, isNum, b) {
      var base, s = isNum ? str : str.replace(whitespaceOrPlus, "");
      if (isInfinityOrNaN.test(s)) {
        x.s = isNaN(s) ? null : s < 0 ? -1 : 1;
      } else {
        if (!isNum) {
          s = s.replace(basePrefix, function(m, p1, p2) {
            base = (p2 = p2.toLowerCase()) == "x" ? 16 : p2 == "b" ? 2 : 8;
            return !b || b == base ? p1 : m;
          });
          if (b) {
            base = b;
            s = s.replace(dotAfter, "$1").replace(dotBefore, "0.$1");
          }
          if (str != s) return new BigNumber2(s, base);
        }
        if (BigNumber2.DEBUG) {
          throw Error(bignumberError + "Not a" + (b ? " base " + b : "") + " number: " + str);
        }
        x.s = null;
      }
      x.c = x.e = null;
    };
  })();
  function round(x, sd, rm, r2) {
    var d, i, j, k, n, ni, rd, xc = x.c, pows10 = POWS_TEN;
    if (xc) {
      out: {
        for (d = 1, k = xc[0]; k >= 10; k /= 10, d++) ;
        i = sd - d;
        if (i < 0) {
          i += LOG_BASE;
          j = sd;
          n = xc[ni = 0];
          rd = mathfloor(n / pows10[d - j - 1] % 10);
        } else {
          ni = mathceil((i + 1) / LOG_BASE);
          if (ni >= xc.length) {
            if (r2) {
              for (; xc.length <= ni; xc.push(0)) ;
              n = rd = 0;
              d = 1;
              i %= LOG_BASE;
              j = i - LOG_BASE + 1;
            } else {
              break out;
            }
          } else {
            n = k = xc[ni];
            for (d = 1; k >= 10; k /= 10, d++) ;
            i %= LOG_BASE;
            j = i - LOG_BASE + d;
            rd = j < 0 ? 0 : mathfloor(n / pows10[d - j - 1] % 10);
          }
        }
        r2 = r2 || sd < 0 || // Are there any non-zero digits after the rounding digit?
        // The expression  n % pows10[d - j - 1]  returns all digits of n to the right
        // of the digit at j, e.g. if n is 908714 and j is 2, the expression gives 714.
        xc[ni + 1] != null || (j < 0 ? n : n % pows10[d - j - 1]);
        r2 = rm < 4 ? (rd || r2) && (rm == 0 || rm == (x.s < 0 ? 3 : 2)) : rd > 5 || rd == 5 && (rm == 4 || r2 || rm == 6 && // Check whether the digit to the left of the rounding digit is odd.
        (i > 0 ? j > 0 ? n / pows10[d - j] : 0 : xc[ni - 1]) % 10 & 1 || rm == (x.s < 0 ? 8 : 7));
        if (sd < 1 || !xc[0]) {
          xc.length = 0;
          if (r2) {
            sd -= x.e + 1;
            xc[0] = pows10[(LOG_BASE - sd % LOG_BASE) % LOG_BASE];
            x.e = -sd || 0;
          } else {
            xc[0] = x.e = 0;
          }
          return x;
        }
        if (i == 0) {
          xc.length = ni;
          k = 1;
          ni--;
        } else {
          xc.length = ni + 1;
          k = pows10[LOG_BASE - i];
          xc[ni] = j > 0 ? mathfloor(n / pows10[d - j] % pows10[j]) * k : 0;
        }
        if (r2) {
          for (; ; ) {
            if (ni == 0) {
              for (i = 1, j = xc[0]; j >= 10; j /= 10, i++) ;
              j = xc[0] += k;
              for (k = 1; j >= 10; j /= 10, k++) ;
              if (i != k) {
                x.e++;
                if (xc[0] == BASE) xc[0] = 1;
              }
              break;
            } else {
              xc[ni] += k;
              if (xc[ni] != BASE) break;
              xc[ni--] = 0;
              k = 1;
            }
          }
        }
        for (i = xc.length; xc[--i] === 0; xc.pop()) ;
      }
      if (x.e > MAX_EXP) {
        x.c = x.e = null;
      } else if (x.e < MIN_EXP) {
        x.c = [x.e = 0];
      }
    }
    return x;
  }
  function valueOf(n) {
    var str, e = n.e;
    if (e === null) return n.toString();
    str = coeffToString(n.c);
    str = e <= TO_EXP_NEG || e >= TO_EXP_POS ? toExponential(str, e) : toFixedPoint(str, e, "0");
    return n.s < 0 ? "-" + str : str;
  }
  P.absoluteValue = P.abs = function() {
    var x = new BigNumber2(this);
    if (x.s < 0) x.s = 1;
    return x;
  };
  P.comparedTo = function(y, b) {
    return compare(this, new BigNumber2(y, b));
  };
  P.decimalPlaces = P.dp = function(dp, rm) {
    var c, n, v, x = this;
    if (dp != null) {
      intCheck(dp, 0, MAX);
      if (rm == null) rm = ROUNDING_MODE;
      else intCheck(rm, 0, 8);
      return round(new BigNumber2(x), dp + x.e + 1, rm);
    }
    if (!(c = x.c)) return null;
    n = ((v = c.length - 1) - bitFloor(this.e / LOG_BASE)) * LOG_BASE;
    if (v = c[v]) for (; v % 10 == 0; v /= 10, n--) ;
    if (n < 0) n = 0;
    return n;
  };
  P.dividedBy = P.div = function(y, b) {
    return div(this, new BigNumber2(y, b), DECIMAL_PLACES, ROUNDING_MODE);
  };
  P.dividedToIntegerBy = P.idiv = function(y, b) {
    return div(this, new BigNumber2(y, b), 0, 1);
  };
  P.exponentiatedBy = P.pow = function(n, m) {
    var half, isModExp, i, k, more, nIsBig, nIsNeg, nIsOdd, y, x = this;
    n = new BigNumber2(n);
    if (n.c && !n.isInteger()) {
      throw Error(bignumberError + "Exponent not an integer: " + valueOf(n));
    }
    if (m != null) m = new BigNumber2(m);
    nIsBig = n.e > 14;
    if (!x.c || !x.c[0] || x.c[0] == 1 && !x.e && x.c.length == 1 || !n.c || !n.c[0]) {
      y = new BigNumber2(Math.pow(+valueOf(x), nIsBig ? n.s * (2 - isOdd(n)) : +valueOf(n)));
      return m ? y.mod(m) : y;
    }
    nIsNeg = n.s < 0;
    if (m) {
      if (m.c ? !m.c[0] : !m.s) return new BigNumber2(NaN);
      isModExp = !nIsNeg && x.isInteger() && m.isInteger();
      if (isModExp) x = x.mod(m);
    } else if (n.e > 9 && (x.e > 0 || x.e < -1 || (x.e == 0 ? x.c[0] > 1 || nIsBig && x.c[1] >= 24e7 : x.c[0] < 8e13 || nIsBig && x.c[0] <= 9999975e7))) {
      k = x.s < 0 && isOdd(n) ? -0 : 0;
      if (x.e > -1) k = 1 / k;
      return new BigNumber2(nIsNeg ? 1 / k : k);
    } else if (POW_PRECISION) {
      k = mathceil(POW_PRECISION / LOG_BASE + 2);
    }
    if (nIsBig) {
      half = new BigNumber2(0.5);
      if (nIsNeg) n.s = 1;
      nIsOdd = isOdd(n);
    } else {
      i = Math.abs(+valueOf(n));
      nIsOdd = i % 2;
    }
    y = new BigNumber2(ONE);
    for (; ; ) {
      if (nIsOdd) {
        y = y.times(x);
        if (!y.c) break;
        if (k) {
          if (y.c.length > k) y.c.length = k;
        } else if (isModExp) {
          y = y.mod(m);
        }
      }
      if (i) {
        i = mathfloor(i / 2);
        if (i === 0) break;
        nIsOdd = i % 2;
      } else {
        n = n.times(half);
        round(n, n.e + 1, 1);
        if (n.e > 14) {
          nIsOdd = isOdd(n);
        } else {
          i = +valueOf(n);
          if (i === 0) break;
          nIsOdd = i % 2;
        }
      }
      x = x.times(x);
      if (k) {
        if (x.c && x.c.length > k) x.c.length = k;
      } else if (isModExp) {
        x = x.mod(m);
      }
    }
    if (isModExp) return y;
    if (nIsNeg) y = ONE.div(y);
    return m ? y.mod(m) : k ? round(y, POW_PRECISION, ROUNDING_MODE, more) : y;
  };
  P.integerValue = function(rm) {
    var n = new BigNumber2(this);
    if (rm == null) rm = ROUNDING_MODE;
    else intCheck(rm, 0, 8);
    return round(n, n.e + 1, rm);
  };
  P.isEqualTo = P.eq = function(y, b) {
    return compare(this, new BigNumber2(y, b)) === 0;
  };
  P.isFinite = function() {
    return !!this.c;
  };
  P.isGreaterThan = P.gt = function(y, b) {
    return compare(this, new BigNumber2(y, b)) > 0;
  };
  P.isGreaterThanOrEqualTo = P.gte = function(y, b) {
    return (b = compare(this, new BigNumber2(y, b))) === 1 || b === 0;
  };
  P.isInteger = function() {
    return !!this.c && bitFloor(this.e / LOG_BASE) > this.c.length - 2;
  };
  P.isLessThan = P.lt = function(y, b) {
    return compare(this, new BigNumber2(y, b)) < 0;
  };
  P.isLessThanOrEqualTo = P.lte = function(y, b) {
    return (b = compare(this, new BigNumber2(y, b))) === -1 || b === 0;
  };
  P.isNaN = function() {
    return !this.s;
  };
  P.isNegative = function() {
    return this.s < 0;
  };
  P.isPositive = function() {
    return this.s > 0;
  };
  P.isZero = function() {
    return !!this.c && this.c[0] == 0;
  };
  P.minus = function(y, b) {
    var i, j, t, xLTy, x = this, a = x.s;
    y = new BigNumber2(y, b);
    b = y.s;
    if (!a || !b) return new BigNumber2(NaN);
    if (a != b) {
      y.s = -b;
      return x.plus(y);
    }
    var xe = x.e / LOG_BASE, ye = y.e / LOG_BASE, xc = x.c, yc = y.c;
    if (!xe || !ye) {
      if (!xc || !yc) return xc ? (y.s = -b, y) : new BigNumber2(yc ? x : NaN);
      if (!xc[0] || !yc[0]) {
        return yc[0] ? (y.s = -b, y) : new BigNumber2(xc[0] ? x : (
          // IEEE 754 (2008) 6.3: n - n = -0 when rounding to -Infinity
          ROUNDING_MODE == 3 ? -0 : 0
        ));
      }
    }
    xe = bitFloor(xe);
    ye = bitFloor(ye);
    xc = xc.slice();
    if (a = xe - ye) {
      if (xLTy = a < 0) {
        a = -a;
        t = xc;
      } else {
        ye = xe;
        t = yc;
      }
      t.reverse();
      for (b = a; b--; t.push(0)) ;
      t.reverse();
    } else {
      j = (xLTy = (a = xc.length) < (b = yc.length)) ? a : b;
      for (a = b = 0; b < j; b++) {
        if (xc[b] != yc[b]) {
          xLTy = xc[b] < yc[b];
          break;
        }
      }
    }
    if (xLTy) {
      t = xc;
      xc = yc;
      yc = t;
      y.s = -y.s;
    }
    b = (j = yc.length) - (i = xc.length);
    if (b > 0) for (; b--; xc[i++] = 0) ;
    b = BASE - 1;
    for (; j > a; ) {
      if (xc[--j] < yc[j]) {
        for (i = j; i && !xc[--i]; xc[i] = b) ;
        --xc[i];
        xc[j] += BASE;
      }
      xc[j] -= yc[j];
    }
    for (; xc[0] == 0; xc.splice(0, 1), --ye) ;
    if (!xc[0]) {
      y.s = ROUNDING_MODE == 3 ? -1 : 1;
      y.c = [y.e = 0];
      return y;
    }
    return normalise(y, xc, ye);
  };
  P.modulo = P.mod = function(y, b) {
    var q, s, x = this;
    y = new BigNumber2(y, b);
    if (!x.c || !y.s || y.c && !y.c[0]) {
      return new BigNumber2(NaN);
    } else if (!y.c || x.c && !x.c[0]) {
      return new BigNumber2(x);
    }
    if (MODULO_MODE == 9) {
      s = y.s;
      y.s = 1;
      q = div(x, y, 0, 3);
      y.s = s;
      q.s *= s;
    } else {
      q = div(x, y, 0, MODULO_MODE);
    }
    y = x.minus(q.times(y));
    if (!y.c[0] && MODULO_MODE == 1) y.s = x.s;
    return y;
  };
  P.multipliedBy = P.times = function(y, b) {
    var c, e, i, j, k, m, xcL, xlo, xhi, ycL, ylo, yhi, zc, base, sqrtBase, x = this, xc = x.c, yc = (y = new BigNumber2(y, b)).c;
    if (!xc || !yc || !xc[0] || !yc[0]) {
      if (!x.s || !y.s || xc && !xc[0] && !yc || yc && !yc[0] && !xc) {
        y.c = y.e = y.s = null;
      } else {
        y.s *= x.s;
        if (!xc || !yc) {
          y.c = y.e = null;
        } else {
          y.c = [0];
          y.e = 0;
        }
      }
      return y;
    }
    e = bitFloor(x.e / LOG_BASE) + bitFloor(y.e / LOG_BASE);
    y.s *= x.s;
    xcL = xc.length;
    ycL = yc.length;
    if (xcL < ycL) {
      zc = xc;
      xc = yc;
      yc = zc;
      i = xcL;
      xcL = ycL;
      ycL = i;
    }
    for (i = xcL + ycL, zc = []; i--; zc.push(0)) ;
    base = BASE;
    sqrtBase = SQRT_BASE;
    for (i = ycL; --i >= 0; ) {
      c = 0;
      ylo = yc[i] % sqrtBase;
      yhi = yc[i] / sqrtBase | 0;
      for (k = xcL, j = i + k; j > i; ) {
        xlo = xc[--k] % sqrtBase;
        xhi = xc[k] / sqrtBase | 0;
        m = yhi * xlo + xhi * ylo;
        xlo = ylo * xlo + m % sqrtBase * sqrtBase + zc[j] + c;
        c = (xlo / base | 0) + (m / sqrtBase | 0) + yhi * xhi;
        zc[j--] = xlo % base;
      }
      zc[j] = c;
    }
    if (c) {
      ++e;
    } else {
      zc.splice(0, 1);
    }
    return normalise(y, zc, e);
  };
  P.negated = function() {
    var x = new BigNumber2(this);
    x.s = -x.s || null;
    return x;
  };
  P.plus = function(y, b) {
    var t, x = this, a = x.s;
    y = new BigNumber2(y, b);
    b = y.s;
    if (!a || !b) return new BigNumber2(NaN);
    if (a != b) {
      y.s = -b;
      return x.minus(y);
    }
    var xe = x.e / LOG_BASE, ye = y.e / LOG_BASE, xc = x.c, yc = y.c;
    if (!xe || !ye) {
      if (!xc || !yc) return new BigNumber2(a / 0);
      if (!xc[0] || !yc[0]) return yc[0] ? y : new BigNumber2(xc[0] ? x : a * 0);
    }
    xe = bitFloor(xe);
    ye = bitFloor(ye);
    xc = xc.slice();
    if (a = xe - ye) {
      if (a > 0) {
        ye = xe;
        t = yc;
      } else {
        a = -a;
        t = xc;
      }
      t.reverse();
      for (; a--; t.push(0)) ;
      t.reverse();
    }
    a = xc.length;
    b = yc.length;
    if (a - b < 0) {
      t = yc;
      yc = xc;
      xc = t;
      b = a;
    }
    for (a = 0; b; ) {
      a = (xc[--b] = xc[b] + yc[b] + a) / BASE | 0;
      xc[b] = BASE === xc[b] ? 0 : xc[b] % BASE;
    }
    if (a) {
      xc = [a].concat(xc);
      ++ye;
    }
    return normalise(y, xc, ye);
  };
  P.precision = P.sd = function(sd, rm) {
    var c, n, v, x = this;
    if (sd != null && sd !== !!sd) {
      intCheck(sd, 1, MAX);
      if (rm == null) rm = ROUNDING_MODE;
      else intCheck(rm, 0, 8);
      return round(new BigNumber2(x), sd, rm);
    }
    if (!(c = x.c)) return null;
    v = c.length - 1;
    n = v * LOG_BASE + 1;
    if (v = c[v]) {
      for (; v % 10 == 0; v /= 10, n--) ;
      for (v = c[0]; v >= 10; v /= 10, n++) ;
    }
    if (sd && x.e + 1 > n) n = x.e + 1;
    return n;
  };
  P.shiftedBy = function(k) {
    intCheck(k, -MAX_SAFE_INTEGER, MAX_SAFE_INTEGER);
    return this.times("1e" + k);
  };
  P.squareRoot = P.sqrt = function() {
    var m, n, r2, rep, t, x = this, c = x.c, s = x.s, e = x.e, dp = DECIMAL_PLACES + 4, half = new BigNumber2("0.5");
    if (s !== 1 || !c || !c[0]) {
      return new BigNumber2(!s || s < 0 && (!c || c[0]) ? NaN : c ? x : 1 / 0);
    }
    s = Math.sqrt(+valueOf(x));
    if (s == 0 || s == 1 / 0) {
      n = coeffToString(c);
      if ((n.length + e) % 2 == 0) n += "0";
      s = Math.sqrt(+n);
      e = bitFloor((e + 1) / 2) - (e < 0 || e % 2);
      if (s == 1 / 0) {
        n = "5e" + e;
      } else {
        n = s.toExponential();
        n = n.slice(0, n.indexOf("e") + 1) + e;
      }
      r2 = new BigNumber2(n);
    } else {
      r2 = new BigNumber2(s + "");
    }
    if (r2.c[0]) {
      e = r2.e;
      s = e + dp;
      if (s < 3) s = 0;
      for (; ; ) {
        t = r2;
        r2 = half.times(t.plus(div(x, t, dp, 1)));
        if (coeffToString(t.c).slice(0, s) === (n = coeffToString(r2.c)).slice(0, s)) {
          if (r2.e < e) --s;
          n = n.slice(s - 3, s + 1);
          if (n == "9999" || !rep && n == "4999") {
            if (!rep) {
              round(t, t.e + DECIMAL_PLACES + 2, 0);
              if (t.times(t).eq(x)) {
                r2 = t;
                break;
              }
            }
            dp += 4;
            s += 4;
            rep = 1;
          } else {
            if (!+n || !+n.slice(1) && n.charAt(0) == "5") {
              round(r2, r2.e + DECIMAL_PLACES + 2, 1);
              m = !r2.times(r2).eq(x);
            }
            break;
          }
        }
      }
    }
    return round(r2, r2.e + DECIMAL_PLACES + 1, ROUNDING_MODE, m);
  };
  P.toExponential = function(dp, rm) {
    if (dp != null) {
      intCheck(dp, 0, MAX);
      dp++;
    }
    return format(this, dp, rm, 1);
  };
  P.toFixed = function(dp, rm) {
    if (dp != null) {
      intCheck(dp, 0, MAX);
      dp = dp + this.e + 1;
    }
    return format(this, dp, rm);
  };
  P.toFormat = function(dp, rm, format2) {
    var str, x = this;
    if (format2 == null) {
      if (dp != null && rm && typeof rm == "object") {
        format2 = rm;
        rm = null;
      } else if (dp && typeof dp == "object") {
        format2 = dp;
        dp = rm = null;
      } else {
        format2 = FORMAT;
      }
    } else if (typeof format2 != "object") {
      throw Error(bignumberError + "Argument not an object: " + format2);
    }
    str = x.toFixed(dp, rm);
    if (x.c) {
      var i, arr = str.split("."), g1 = +format2.groupSize, g2 = +format2.secondaryGroupSize, groupSeparator = format2.groupSeparator || "", intPart = arr[0], fractionPart = arr[1], isNeg = x.s < 0, intDigits = isNeg ? intPart.slice(1) : intPart, len = intDigits.length;
      if (g2) {
        i = g1;
        g1 = g2;
        g2 = i;
        len -= i;
      }
      if (g1 > 0 && len > 0) {
        i = len % g1 || g1;
        intPart = intDigits.substr(0, i);
        for (; i < len; i += g1) intPart += groupSeparator + intDigits.substr(i, g1);
        if (g2 > 0) intPart += groupSeparator + intDigits.slice(i);
        if (isNeg) intPart = "-" + intPart;
      }
      str = fractionPart ? intPart + (format2.decimalSeparator || "") + ((g2 = +format2.fractionGroupSize) ? fractionPart.replace(
        new RegExp("\\d{" + g2 + "}\\B", "g"),
        "$&" + (format2.fractionGroupSeparator || "")
      ) : fractionPart) : intPart;
    }
    return (format2.prefix || "") + str + (format2.suffix || "");
  };
  P.toFraction = function(md) {
    var d, d0, d1, d2, e, exp, n, n0, n1, q, r2, s, x = this, xc = x.c;
    if (md != null) {
      n = new BigNumber2(md);
      if (!n.isInteger() && (n.c || n.s !== 1) || n.lt(ONE)) {
        throw Error(bignumberError + "Argument " + (n.isInteger() ? "out of range: " : "not an integer: ") + valueOf(n));
      }
    }
    if (!xc) return new BigNumber2(x);
    d = new BigNumber2(ONE);
    n1 = d0 = new BigNumber2(ONE);
    d1 = n0 = new BigNumber2(ONE);
    s = coeffToString(xc);
    e = d.e = s.length - x.e - 1;
    d.c[0] = POWS_TEN[(exp = e % LOG_BASE) < 0 ? LOG_BASE + exp : exp];
    md = !md || n.comparedTo(d) > 0 ? e > 0 ? d : n1 : n;
    exp = MAX_EXP;
    MAX_EXP = 1 / 0;
    n = new BigNumber2(s);
    n0.c[0] = 0;
    for (; ; ) {
      q = div(n, d, 0, 1);
      d2 = d0.plus(q.times(d1));
      if (d2.comparedTo(md) == 1) break;
      d0 = d1;
      d1 = d2;
      n1 = n0.plus(q.times(d2 = n1));
      n0 = d2;
      d = n.minus(q.times(d2 = d));
      n = d2;
    }
    d2 = div(md.minus(d0), d1, 0, 1);
    n0 = n0.plus(d2.times(n1));
    d0 = d0.plus(d2.times(d1));
    n0.s = n1.s = x.s;
    e = e * 2;
    r2 = div(n1, d1, e, ROUNDING_MODE).minus(x).abs().comparedTo(
      div(n0, d0, e, ROUNDING_MODE).minus(x).abs()
    ) < 1 ? [n1, d1] : [n0, d0];
    MAX_EXP = exp;
    return r2;
  };
  P.toNumber = function() {
    return +valueOf(this);
  };
  P.toPrecision = function(sd, rm) {
    if (sd != null) intCheck(sd, 1, MAX);
    return format(this, sd, rm, 2);
  };
  P.toString = function(b) {
    var str, n = this, s = n.s, e = n.e;
    if (e === null) {
      if (s) {
        str = "Infinity";
        if (s < 0) str = "-" + str;
      } else {
        str = "NaN";
      }
    } else {
      if (b == null) {
        str = e <= TO_EXP_NEG || e >= TO_EXP_POS ? toExponential(coeffToString(n.c), e) : toFixedPoint(coeffToString(n.c), e, "0");
      } else if (b === 10 && alphabetHasNormalDecimalDigits) {
        n = round(new BigNumber2(n), DECIMAL_PLACES + e + 1, ROUNDING_MODE);
        str = toFixedPoint(coeffToString(n.c), n.e, "0");
      } else {
        intCheck(b, 2, ALPHABET.length, "Base");
        str = convertBase(toFixedPoint(coeffToString(n.c), e, "0"), 10, b, s, true);
      }
      if (s < 0 && n.c[0]) str = "-" + str;
    }
    return str;
  };
  P.valueOf = P.toJSON = function() {
    return valueOf(this);
  };
  P._isBigNumber = true;
  P[Symbol.toStringTag] = "BigNumber";
  P[Symbol.for("nodejs.util.inspect.custom")] = P.valueOf;
  if (configObject != null) BigNumber2.set(configObject);
  return BigNumber2;
}
function bitFloor(n) {
  var i = n | 0;
  return n > 0 || n === i ? i : i - 1;
}
function coeffToString(a) {
  var s, z, i = 1, j = a.length, r2 = a[0] + "";
  for (; i < j; ) {
    s = a[i++] + "";
    z = LOG_BASE - s.length;
    for (; z--; s = "0" + s) ;
    r2 += s;
  }
  for (j = r2.length; r2.charCodeAt(--j) === 48; ) ;
  return r2.slice(0, j + 1 || 1);
}
function compare(x, y) {
  var a, b, xc = x.c, yc = y.c, i = x.s, j = y.s, k = x.e, l = y.e;
  if (!i || !j) return null;
  a = xc && !xc[0];
  b = yc && !yc[0];
  if (a || b) return a ? b ? 0 : -j : i;
  if (i != j) return i;
  a = i < 0;
  b = k == l;
  if (!xc || !yc) return b ? 0 : !xc ^ a ? 1 : -1;
  if (!b) return k > l ^ a ? 1 : -1;
  j = (k = xc.length) < (l = yc.length) ? k : l;
  for (i = 0; i < j; i++) if (xc[i] != yc[i]) return xc[i] > yc[i] ^ a ? 1 : -1;
  return k == l ? 0 : k > l ^ a ? 1 : -1;
}
function intCheck(n, min, max, name) {
  if (n < min || n > max || n !== mathfloor(n)) {
    throw Error(bignumberError + (name || "Argument") + (typeof n == "number" ? n < min || n > max ? " out of range: " : " not an integer: " : " not a primitive number: ") + String(n));
  }
}
function isOdd(n) {
  var k = n.c.length - 1;
  return bitFloor(n.e / LOG_BASE) == k && n.c[k] % 2 != 0;
}
function toExponential(str, e) {
  return (str.length > 1 ? str.charAt(0) + "." + str.slice(1) : str) + (e < 0 ? "e" : "e+") + e;
}
function toFixedPoint(str, e, z) {
  var len, zs;
  if (e < 0) {
    for (zs = z + "."; ++e; zs += z) ;
    str = zs + str;
  } else {
    len = str.length;
    if (++e > len) {
      for (zs = z, e -= len; --e; zs += z) ;
      str += zs;
    } else if (e < len) {
      str = str.slice(0, e) + "." + str.slice(e);
    }
  }
  return str;
}
var BigNumber = clone();
var SplayTreeNode = class {
  key;
  left = null;
  right = null;
  constructor(key) {
    this.key = key;
  }
};
var SplayTreeSetNode = class extends SplayTreeNode {
  constructor(key) {
    super(key);
  }
};
var SplayTree = class {
  size = 0;
  modificationCount = 0;
  splayCount = 0;
  splay(key) {
    const root2 = this.root;
    if (root2 == null) {
      this.compare(key, key);
      return -1;
    }
    let right = null;
    let newTreeRight = null;
    let left = null;
    let newTreeLeft = null;
    let current = root2;
    const compare2 = this.compare;
    let comp;
    while (true) {
      comp = compare2(current.key, key);
      if (comp > 0) {
        let currentLeft = current.left;
        if (currentLeft == null) break;
        comp = compare2(currentLeft.key, key);
        if (comp > 0) {
          current.left = currentLeft.right;
          currentLeft.right = current;
          current = currentLeft;
          currentLeft = current.left;
          if (currentLeft == null) break;
        }
        if (right == null) {
          newTreeRight = current;
        } else {
          right.left = current;
        }
        right = current;
        current = currentLeft;
      } else if (comp < 0) {
        let currentRight = current.right;
        if (currentRight == null) break;
        comp = compare2(currentRight.key, key);
        if (comp < 0) {
          current.right = currentRight.left;
          currentRight.left = current;
          current = currentRight;
          currentRight = current.right;
          if (currentRight == null) break;
        }
        if (left == null) {
          newTreeLeft = current;
        } else {
          left.right = current;
        }
        left = current;
        current = currentRight;
      } else {
        break;
      }
    }
    if (left != null) {
      left.right = current.left;
      current.left = newTreeLeft;
    }
    if (right != null) {
      right.left = current.right;
      current.right = newTreeRight;
    }
    if (this.root !== current) {
      this.root = current;
      this.splayCount++;
    }
    return comp;
  }
  splayMin(node) {
    let current = node;
    let nextLeft = current.left;
    while (nextLeft != null) {
      const left = nextLeft;
      current.left = left.right;
      left.right = current;
      current = left;
      nextLeft = current.left;
    }
    return current;
  }
  splayMax(node) {
    let current = node;
    let nextRight = current.right;
    while (nextRight != null) {
      const right = nextRight;
      current.right = right.left;
      right.left = current;
      current = right;
      nextRight = current.right;
    }
    return current;
  }
  _delete(key) {
    if (this.root == null) return null;
    const comp = this.splay(key);
    if (comp != 0) return null;
    let root2 = this.root;
    const result = root2;
    const left = root2.left;
    this.size--;
    if (left == null) {
      this.root = root2.right;
    } else {
      const right = root2.right;
      root2 = this.splayMax(left);
      root2.right = right;
      this.root = root2;
    }
    this.modificationCount++;
    return result;
  }
  addNewRoot(node, comp) {
    this.size++;
    this.modificationCount++;
    const root2 = this.root;
    if (root2 == null) {
      this.root = node;
      return;
    }
    if (comp < 0) {
      node.left = root2;
      node.right = root2.right;
      root2.right = null;
    } else {
      node.right = root2;
      node.left = root2.left;
      root2.left = null;
    }
    this.root = node;
  }
  _first() {
    const root2 = this.root;
    if (root2 == null) return null;
    this.root = this.splayMin(root2);
    return this.root;
  }
  _last() {
    const root2 = this.root;
    if (root2 == null) return null;
    this.root = this.splayMax(root2);
    return this.root;
  }
  clear() {
    this.root = null;
    this.size = 0;
    this.modificationCount++;
  }
  has(key) {
    return this.validKey(key) && this.splay(key) == 0;
  }
  defaultCompare() {
    return (a, b) => a < b ? -1 : a > b ? 1 : 0;
  }
  wrap() {
    return {
      getRoot: () => {
        return this.root;
      },
      setRoot: (root2) => {
        this.root = root2;
      },
      getSize: () => {
        return this.size;
      },
      getModificationCount: () => {
        return this.modificationCount;
      },
      getSplayCount: () => {
        return this.splayCount;
      },
      setSplayCount: (count) => {
        this.splayCount = count;
      },
      splay: (key) => {
        return this.splay(key);
      },
      has: (key) => {
        return this.has(key);
      }
    };
  }
};
var SplayTreeSet = class _SplayTreeSet extends SplayTree {
  root = null;
  compare;
  validKey;
  constructor(compare2, isValidKey) {
    super();
    this.compare = compare2 ?? this.defaultCompare();
    this.validKey = isValidKey ?? ((v) => v != null && v != void 0);
  }
  delete(element) {
    if (!this.validKey(element)) return false;
    return this._delete(element) != null;
  }
  deleteAll(elements) {
    for (const element of elements) {
      this.delete(element);
    }
  }
  forEach(f) {
    const nodes = this[Symbol.iterator]();
    let result;
    while (result = nodes.next(), !result.done) {
      f(result.value, result.value, this);
    }
  }
  add(element) {
    const compare2 = this.splay(element);
    if (compare2 != 0) this.addNewRoot(new SplayTreeSetNode(element), compare2);
    return this;
  }
  addAndReturn(element) {
    const compare2 = this.splay(element);
    if (compare2 != 0) this.addNewRoot(new SplayTreeSetNode(element), compare2);
    return this.root.key;
  }
  addAll(elements) {
    for (const element of elements) {
      this.add(element);
    }
  }
  isEmpty() {
    return this.root == null;
  }
  isNotEmpty() {
    return this.root != null;
  }
  single() {
    if (this.size == 0) throw "Bad state: No element";
    if (this.size > 1) throw "Bad state: Too many element";
    return this.root.key;
  }
  first() {
    if (this.size == 0) throw "Bad state: No element";
    return this._first().key;
  }
  last() {
    if (this.size == 0) throw "Bad state: No element";
    return this._last().key;
  }
  lastBefore(element) {
    if (element == null) throw "Invalid arguments(s)";
    if (this.root == null) return null;
    const comp = this.splay(element);
    if (comp < 0) return this.root.key;
    let node = this.root.left;
    if (node == null) return null;
    let nodeRight = node.right;
    while (nodeRight != null) {
      node = nodeRight;
      nodeRight = node.right;
    }
    return node.key;
  }
  firstAfter(element) {
    if (element == null) throw "Invalid arguments(s)";
    if (this.root == null) return null;
    const comp = this.splay(element);
    if (comp > 0) return this.root.key;
    let node = this.root.right;
    if (node == null) return null;
    let nodeLeft = node.left;
    while (nodeLeft != null) {
      node = nodeLeft;
      nodeLeft = node.left;
    }
    return node.key;
  }
  retainAll(elements) {
    const retainSet = new _SplayTreeSet(this.compare, this.validKey);
    const modificationCount = this.modificationCount;
    for (const object of elements) {
      if (modificationCount != this.modificationCount) {
        throw "Concurrent modification during iteration.";
      }
      if (this.validKey(object) && this.splay(object) == 0) {
        retainSet.add(this.root.key);
      }
    }
    if (retainSet.size != this.size) {
      this.root = retainSet.root;
      this.size = retainSet.size;
      this.modificationCount++;
    }
  }
  lookup(object) {
    if (!this.validKey(object)) return null;
    const comp = this.splay(object);
    if (comp != 0) return null;
    return this.root.key;
  }
  intersection(other) {
    const result = new _SplayTreeSet(this.compare, this.validKey);
    for (const element of this) {
      if (other.has(element)) result.add(element);
    }
    return result;
  }
  difference(other) {
    const result = new _SplayTreeSet(this.compare, this.validKey);
    for (const element of this) {
      if (!other.has(element)) result.add(element);
    }
    return result;
  }
  union(other) {
    const u2 = this.clone();
    u2.addAll(other);
    return u2;
  }
  clone() {
    const set2 = new _SplayTreeSet(this.compare, this.validKey);
    set2.size = this.size;
    set2.root = this.copyNode(this.root);
    return set2;
  }
  copyNode(node) {
    if (node == null) return null;
    function copyChildren(node2, dest) {
      let left;
      let right;
      do {
        left = node2.left;
        right = node2.right;
        if (left != null) {
          const newLeft = new SplayTreeSetNode(left.key);
          dest.left = newLeft;
          copyChildren(left, newLeft);
        }
        if (right != null) {
          const newRight = new SplayTreeSetNode(right.key);
          dest.right = newRight;
          node2 = right;
          dest = newRight;
        }
      } while (right != null);
    }
    const result = new SplayTreeSetNode(node.key);
    copyChildren(node, result);
    return result;
  }
  toSet() {
    return this.clone();
  }
  entries() {
    return new SplayTreeSetEntryIterableIterator(this.wrap());
  }
  keys() {
    return this[Symbol.iterator]();
  }
  values() {
    return this[Symbol.iterator]();
  }
  [Symbol.iterator]() {
    return new SplayTreeKeyIterableIterator(this.wrap());
  }
  [Symbol.toStringTag] = "[object Set]";
};
var SplayTreeIterableIterator = class {
  tree;
  path = new Array();
  modificationCount = null;
  splayCount;
  constructor(tree) {
    this.tree = tree;
    this.splayCount = tree.getSplayCount();
  }
  [Symbol.iterator]() {
    return this;
  }
  next() {
    if (this.moveNext()) return { done: false, value: this.current() };
    return { done: true, value: null };
  }
  current() {
    if (!this.path.length) return null;
    const node = this.path[this.path.length - 1];
    return this.getValue(node);
  }
  rebuildPath(key) {
    this.path.splice(0, this.path.length);
    this.tree.splay(key);
    this.path.push(this.tree.getRoot());
    this.splayCount = this.tree.getSplayCount();
  }
  findLeftMostDescendent(node) {
    while (node != null) {
      this.path.push(node);
      node = node.left;
    }
  }
  moveNext() {
    if (this.modificationCount != this.tree.getModificationCount()) {
      if (this.modificationCount == null) {
        this.modificationCount = this.tree.getModificationCount();
        let node2 = this.tree.getRoot();
        while (node2 != null) {
          this.path.push(node2);
          node2 = node2.left;
        }
        return this.path.length > 0;
      }
      throw "Concurrent modification during iteration.";
    }
    if (!this.path.length) return false;
    if (this.splayCount != this.tree.getSplayCount()) {
      this.rebuildPath(this.path[this.path.length - 1].key);
    }
    let node = this.path[this.path.length - 1];
    let next = node.right;
    if (next != null) {
      while (next != null) {
        this.path.push(next);
        next = next.left;
      }
      return true;
    }
    this.path.pop();
    while (this.path.length && this.path[this.path.length - 1].right === node) {
      node = this.path.pop();
    }
    return this.path.length > 0;
  }
};
var SplayTreeKeyIterableIterator = class extends SplayTreeIterableIterator {
  getValue(node) {
    return node.key;
  }
};
var SplayTreeSetEntryIterableIterator = class extends SplayTreeIterableIterator {
  getValue(node) {
    return [node.key, node.key];
  }
};
var constant_default = (x) => {
  return () => {
    return x;
  };
};
var compare_default = (eps) => {
  const almostEqual = eps ? (a, b) => b.minus(a).abs().isLessThanOrEqualTo(eps) : constant_default(false);
  return (a, b) => {
    if (almostEqual(a, b)) return 0;
    return a.comparedTo(b);
  };
};
function orient_default(eps) {
  const almostCollinear = eps ? (area2, ax, ay, cx, cy) => area2.exponentiatedBy(2).isLessThanOrEqualTo(
    cx.minus(ax).exponentiatedBy(2).plus(cy.minus(ay).exponentiatedBy(2)).times(eps)
  ) : constant_default(false);
  return (a, b, c) => {
    const ax = a.x, ay = a.y, cx = c.x, cy = c.y;
    const area2 = ay.minus(cy).times(b.x.minus(cx)).minus(ax.minus(cx).times(b.y.minus(cy)));
    if (almostCollinear(area2, ax, ay, cx, cy)) return 0;
    return area2.comparedTo(0);
  };
}
var identity_default = (x) => {
  return x;
};
var snap_default = (eps) => {
  if (eps) {
    const xTree = new SplayTreeSet(compare_default(eps));
    const yTree = new SplayTreeSet(compare_default(eps));
    const snapCoord = (coord, tree) => {
      return tree.addAndReturn(coord);
    };
    const snap = (v) => {
      return {
        x: snapCoord(v.x, xTree),
        y: snapCoord(v.y, yTree)
      };
    };
    snap({ x: new BigNumber(0), y: new BigNumber(0) });
    return snap;
  }
  return identity_default;
};
var set = (eps) => {
  return {
    set: (eps2) => {
      precision = set(eps2);
    },
    reset: () => set(eps),
    compare: compare_default(eps),
    snap: snap_default(eps),
    orient: orient_default(eps)
  };
};
var precision = set();
var isInBbox = (bbox2, point2) => {
  return bbox2.ll.x.isLessThanOrEqualTo(point2.x) && point2.x.isLessThanOrEqualTo(bbox2.ur.x) && bbox2.ll.y.isLessThanOrEqualTo(point2.y) && point2.y.isLessThanOrEqualTo(bbox2.ur.y);
};
var getBboxOverlap = (b1, b2) => {
  if (b2.ur.x.isLessThan(b1.ll.x) || b1.ur.x.isLessThan(b2.ll.x) || b2.ur.y.isLessThan(b1.ll.y) || b1.ur.y.isLessThan(b2.ll.y))
    return null;
  const lowerX = b1.ll.x.isLessThan(b2.ll.x) ? b2.ll.x : b1.ll.x;
  const upperX = b1.ur.x.isLessThan(b2.ur.x) ? b1.ur.x : b2.ur.x;
  const lowerY = b1.ll.y.isLessThan(b2.ll.y) ? b2.ll.y : b1.ll.y;
  const upperY = b1.ur.y.isLessThan(b2.ur.y) ? b1.ur.y : b2.ur.y;
  return { ll: { x: lowerX, y: lowerY }, ur: { x: upperX, y: upperY } };
};
var crossProduct = (a, b) => a.x.times(b.y).minus(a.y.times(b.x));
var dotProduct = (a, b) => a.x.times(b.x).plus(a.y.times(b.y));
var length = (v) => dotProduct(v, v).sqrt();
var sineOfAngle = (pShared, pBase, pAngle) => {
  const vBase = { x: pBase.x.minus(pShared.x), y: pBase.y.minus(pShared.y) };
  const vAngle = { x: pAngle.x.minus(pShared.x), y: pAngle.y.minus(pShared.y) };
  return crossProduct(vAngle, vBase).div(length(vAngle)).div(length(vBase));
};
var cosineOfAngle = (pShared, pBase, pAngle) => {
  const vBase = { x: pBase.x.minus(pShared.x), y: pBase.y.minus(pShared.y) };
  const vAngle = { x: pAngle.x.minus(pShared.x), y: pAngle.y.minus(pShared.y) };
  return dotProduct(vAngle, vBase).div(length(vAngle)).div(length(vBase));
};
var horizontalIntersection = (pt, v, y) => {
  if (v.y.isZero()) return null;
  return { x: pt.x.plus(v.x.div(v.y).times(y.minus(pt.y))), y };
};
var verticalIntersection = (pt, v, x) => {
  if (v.x.isZero()) return null;
  return { x, y: pt.y.plus(v.y.div(v.x).times(x.minus(pt.x))) };
};
var intersection = (pt1, v1, pt2, v2) => {
  if (v1.x.isZero()) return verticalIntersection(pt2, v2, pt1.x);
  if (v2.x.isZero()) return verticalIntersection(pt1, v1, pt2.x);
  if (v1.y.isZero()) return horizontalIntersection(pt2, v2, pt1.y);
  if (v2.y.isZero()) return horizontalIntersection(pt1, v1, pt2.y);
  const kross = crossProduct(v1, v2);
  if (kross.isZero()) return null;
  const ve = { x: pt2.x.minus(pt1.x), y: pt2.y.minus(pt1.y) };
  const d1 = crossProduct(ve, v1).div(kross);
  const d2 = crossProduct(ve, v2).div(kross);
  const x12 = pt1.x.plus(d2.times(v1.x)), x2 = pt2.x.plus(d1.times(v2.x));
  const y12 = pt1.y.plus(d2.times(v1.y)), y2 = pt2.y.plus(d1.times(v2.y));
  const x = x12.plus(x2).div(2);
  const y = y12.plus(y2).div(2);
  return { x, y };
};
var SweepEvent = class _SweepEvent {
  point;
  isLeft;
  segment;
  otherSE;
  consumedBy;
  // for ordering sweep events in the sweep event queue
  static compare(a, b) {
    const ptCmp = _SweepEvent.comparePoints(a.point, b.point);
    if (ptCmp !== 0) return ptCmp;
    if (a.point !== b.point) a.link(b);
    if (a.isLeft !== b.isLeft) return a.isLeft ? 1 : -1;
    return Segment2.compare(a.segment, b.segment);
  }
  // for ordering points in sweep line order
  static comparePoints(aPt, bPt) {
    if (aPt.x.isLessThan(bPt.x)) return -1;
    if (aPt.x.isGreaterThan(bPt.x)) return 1;
    if (aPt.y.isLessThan(bPt.y)) return -1;
    if (aPt.y.isGreaterThan(bPt.y)) return 1;
    return 0;
  }
  // Warning: 'point' input will be modified and re-used (for performance)
  constructor(point2, isLeft) {
    if (point2.events === void 0) point2.events = [this];
    else point2.events.push(this);
    this.point = point2;
    this.isLeft = isLeft;
  }
  link(other) {
    if (other.point === this.point) {
      throw new Error("Tried to link already linked events");
    }
    const otherEvents = other.point.events;
    for (let i = 0, iMax = otherEvents.length; i < iMax; i++) {
      const evt = otherEvents[i];
      this.point.events.push(evt);
      evt.point = this.point;
    }
    this.checkForConsuming();
  }
  /* Do a pass over our linked events and check to see if any pair
   * of segments match, and should be consumed. */
  checkForConsuming() {
    const numEvents = this.point.events.length;
    for (let i = 0; i < numEvents; i++) {
      const evt1 = this.point.events[i];
      if (evt1.segment.consumedBy !== void 0) continue;
      for (let j = i + 1; j < numEvents; j++) {
        const evt2 = this.point.events[j];
        if (evt2.consumedBy !== void 0) continue;
        if (evt1.otherSE.point.events !== evt2.otherSE.point.events) continue;
        evt1.segment.consume(evt2.segment);
      }
    }
  }
  getAvailableLinkedEvents() {
    const events = [];
    for (let i = 0, iMax = this.point.events.length; i < iMax; i++) {
      const evt = this.point.events[i];
      if (evt !== this && !evt.segment.ringOut && evt.segment.isInResult()) {
        events.push(evt);
      }
    }
    return events;
  }
  /**
   * Returns a comparator function for sorting linked events that will
   * favor the event that will give us the smallest left-side angle.
   * All ring construction starts as low as possible heading to the right,
   * so by always turning left as sharp as possible we'll get polygons
   * without uncessary loops & holes.
   *
   * The comparator function has a compute cache such that it avoids
   * re-computing already-computed values.
   */
  getLeftmostComparator(baseEvent) {
    const cache = /* @__PURE__ */ new Map();
    const fillCache = (linkedEvent) => {
      const nextEvent = linkedEvent.otherSE;
      cache.set(linkedEvent, {
        sine: sineOfAngle(this.point, baseEvent.point, nextEvent.point),
        cosine: cosineOfAngle(this.point, baseEvent.point, nextEvent.point)
      });
    };
    return (a, b) => {
      if (!cache.has(a)) fillCache(a);
      if (!cache.has(b)) fillCache(b);
      const { sine: asine, cosine: acosine } = cache.get(a);
      const { sine: bsine, cosine: bcosine } = cache.get(b);
      if (asine.isGreaterThanOrEqualTo(0) && bsine.isGreaterThanOrEqualTo(0)) {
        if (acosine.isLessThan(bcosine)) return 1;
        if (acosine.isGreaterThan(bcosine)) return -1;
        return 0;
      }
      if (asine.isLessThan(0) && bsine.isLessThan(0)) {
        if (acosine.isLessThan(bcosine)) return -1;
        if (acosine.isGreaterThan(bcosine)) return 1;
        return 0;
      }
      if (bsine.isLessThan(asine)) return -1;
      if (bsine.isGreaterThan(asine)) return 1;
      return 0;
    };
  }
};
var RingOut = class _RingOut {
  events;
  poly;
  _isExteriorRing;
  _enclosingRing;
  /* Given the segments from the sweep line pass, compute & return a series
   * of closed rings from all the segments marked to be part of the result */
  static factory(allSegments) {
    const ringsOut = [];
    for (let i = 0, iMax = allSegments.length; i < iMax; i++) {
      const segment = allSegments[i];
      if (!segment.isInResult() || segment.ringOut) continue;
      let prevEvent = null;
      let event = segment.leftSE;
      let nextEvent = segment.rightSE;
      const events = [event];
      const startingPoint = event.point;
      const intersectionLEs = [];
      while (true) {
        prevEvent = event;
        event = nextEvent;
        events.push(event);
        if (event.point === startingPoint) break;
        while (true) {
          const availableLEs = event.getAvailableLinkedEvents();
          if (availableLEs.length === 0) {
            const firstPt = events[0].point;
            const lastPt = events[events.length - 1].point;
            throw new Error(
              `Unable to complete output ring starting at [${firstPt.x}, ${firstPt.y}]. Last matching segment found ends at [${lastPt.x}, ${lastPt.y}].`
            );
          }
          if (availableLEs.length === 1) {
            nextEvent = availableLEs[0].otherSE;
            break;
          }
          let indexLE = null;
          for (let j = 0, jMax = intersectionLEs.length; j < jMax; j++) {
            if (intersectionLEs[j].point === event.point) {
              indexLE = j;
              break;
            }
          }
          if (indexLE !== null) {
            const intersectionLE = intersectionLEs.splice(indexLE)[0];
            const ringEvents = events.splice(intersectionLE.index);
            ringEvents.unshift(ringEvents[0].otherSE);
            ringsOut.push(new _RingOut(ringEvents.reverse()));
            continue;
          }
          intersectionLEs.push({
            index: events.length,
            point: event.point
          });
          const comparator = event.getLeftmostComparator(prevEvent);
          nextEvent = availableLEs.sort(comparator)[0].otherSE;
          break;
        }
      }
      ringsOut.push(new _RingOut(events));
    }
    return ringsOut;
  }
  constructor(events) {
    this.events = events;
    for (let i = 0, iMax = events.length; i < iMax; i++) {
      events[i].segment.ringOut = this;
    }
    this.poly = null;
  }
  getGeom() {
    let prevPt = this.events[0].point;
    const points = [prevPt];
    for (let i = 1, iMax = this.events.length - 1; i < iMax; i++) {
      const pt2 = this.events[i].point;
      const nextPt2 = this.events[i + 1].point;
      if (precision.orient(pt2, prevPt, nextPt2) === 0) continue;
      points.push(pt2);
      prevPt = pt2;
    }
    if (points.length === 1) return null;
    const pt = points[0];
    const nextPt = points[1];
    if (precision.orient(pt, prevPt, nextPt) === 0) points.shift();
    points.push(points[0]);
    const step = this.isExteriorRing() ? 1 : -1;
    const iStart = this.isExteriorRing() ? 0 : points.length - 1;
    const iEnd = this.isExteriorRing() ? points.length : -1;
    const orderedPoints = [];
    for (let i = iStart; i != iEnd; i += step)
      orderedPoints.push([points[i].x.toNumber(), points[i].y.toNumber()]);
    return orderedPoints;
  }
  isExteriorRing() {
    if (this._isExteriorRing === void 0) {
      const enclosing = this.enclosingRing();
      this._isExteriorRing = enclosing ? !enclosing.isExteriorRing() : true;
    }
    return this._isExteriorRing;
  }
  enclosingRing() {
    if (this._enclosingRing === void 0) {
      this._enclosingRing = this._calcEnclosingRing();
    }
    return this._enclosingRing;
  }
  /* Returns the ring that encloses this one, if any */
  _calcEnclosingRing() {
    let leftMostEvt = this.events[0];
    for (let i = 1, iMax = this.events.length; i < iMax; i++) {
      const evt = this.events[i];
      if (SweepEvent.compare(leftMostEvt, evt) > 0) leftMostEvt = evt;
    }
    let prevSeg = leftMostEvt.segment.prevInResult();
    let prevPrevSeg = prevSeg ? prevSeg.prevInResult() : null;
    while (true) {
      if (!prevSeg) return null;
      if (!prevPrevSeg) return prevSeg.ringOut;
      if (prevPrevSeg.ringOut !== prevSeg.ringOut) {
        if (prevPrevSeg.ringOut?.enclosingRing() !== prevSeg.ringOut) {
          return prevSeg.ringOut;
        } else return prevSeg.ringOut?.enclosingRing();
      }
      prevSeg = prevPrevSeg.prevInResult();
      prevPrevSeg = prevSeg ? prevSeg.prevInResult() : null;
    }
  }
};
var PolyOut = class {
  exteriorRing;
  interiorRings;
  constructor(exteriorRing) {
    this.exteriorRing = exteriorRing;
    exteriorRing.poly = this;
    this.interiorRings = [];
  }
  addInterior(ring) {
    this.interiorRings.push(ring);
    ring.poly = this;
  }
  getGeom() {
    const geom0 = this.exteriorRing.getGeom();
    if (geom0 === null) return null;
    const geom = [geom0];
    for (let i = 0, iMax = this.interiorRings.length; i < iMax; i++) {
      const ringGeom = this.interiorRings[i].getGeom();
      if (ringGeom === null) continue;
      geom.push(ringGeom);
    }
    return geom;
  }
};
var MultiPolyOut = class {
  rings;
  polys;
  constructor(rings) {
    this.rings = rings;
    this.polys = this._composePolys(rings);
  }
  getGeom() {
    const geom = [];
    for (let i = 0, iMax = this.polys.length; i < iMax; i++) {
      const polyGeom = this.polys[i].getGeom();
      if (polyGeom === null) continue;
      geom.push(polyGeom);
    }
    return geom;
  }
  _composePolys(rings) {
    const polys = [];
    for (let i = 0, iMax = rings.length; i < iMax; i++) {
      const ring = rings[i];
      if (ring.poly) continue;
      if (ring.isExteriorRing()) polys.push(new PolyOut(ring));
      else {
        const enclosingRing = ring.enclosingRing();
        if (!enclosingRing?.poly) polys.push(new PolyOut(enclosingRing));
        enclosingRing?.poly?.addInterior(ring);
      }
    }
    return polys;
  }
};
var SweepLine = class {
  queue;
  tree;
  segments;
  constructor(queue, comparator = Segment2.compare) {
    this.queue = queue;
    this.tree = new SplayTreeSet(comparator);
    this.segments = [];
  }
  process(event) {
    const segment = event.segment;
    const newEvents = [];
    if (event.consumedBy) {
      if (event.isLeft) this.queue.delete(event.otherSE);
      else this.tree.delete(segment);
      return newEvents;
    }
    if (event.isLeft) this.tree.add(segment);
    let prevSeg = segment;
    let nextSeg = segment;
    do {
      prevSeg = this.tree.lastBefore(prevSeg);
    } while (prevSeg != null && prevSeg.consumedBy != void 0);
    do {
      nextSeg = this.tree.firstAfter(nextSeg);
    } while (nextSeg != null && nextSeg.consumedBy != void 0);
    if (event.isLeft) {
      let prevMySplitter = null;
      if (prevSeg) {
        const prevInter = prevSeg.getIntersection(segment);
        if (prevInter !== null) {
          if (!segment.isAnEndpoint(prevInter)) prevMySplitter = prevInter;
          if (!prevSeg.isAnEndpoint(prevInter)) {
            const newEventsFromSplit = this._splitSafely(prevSeg, prevInter);
            for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
              newEvents.push(newEventsFromSplit[i]);
            }
          }
        }
      }
      let nextMySplitter = null;
      if (nextSeg) {
        const nextInter = nextSeg.getIntersection(segment);
        if (nextInter !== null) {
          if (!segment.isAnEndpoint(nextInter)) nextMySplitter = nextInter;
          if (!nextSeg.isAnEndpoint(nextInter)) {
            const newEventsFromSplit = this._splitSafely(nextSeg, nextInter);
            for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
              newEvents.push(newEventsFromSplit[i]);
            }
          }
        }
      }
      if (prevMySplitter !== null || nextMySplitter !== null) {
        let mySplitter = null;
        if (prevMySplitter === null) mySplitter = nextMySplitter;
        else if (nextMySplitter === null) mySplitter = prevMySplitter;
        else {
          const cmpSplitters = SweepEvent.comparePoints(
            prevMySplitter,
            nextMySplitter
          );
          mySplitter = cmpSplitters <= 0 ? prevMySplitter : nextMySplitter;
        }
        this.queue.delete(segment.rightSE);
        newEvents.push(segment.rightSE);
        const newEventsFromSplit = segment.split(mySplitter);
        for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
          newEvents.push(newEventsFromSplit[i]);
        }
      }
      if (newEvents.length > 0) {
        this.tree.delete(segment);
        newEvents.push(event);
      } else {
        this.segments.push(segment);
        segment.prev = prevSeg;
      }
    } else {
      if (prevSeg && nextSeg) {
        const inter = prevSeg.getIntersection(nextSeg);
        if (inter !== null) {
          if (!prevSeg.isAnEndpoint(inter)) {
            const newEventsFromSplit = this._splitSafely(prevSeg, inter);
            for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
              newEvents.push(newEventsFromSplit[i]);
            }
          }
          if (!nextSeg.isAnEndpoint(inter)) {
            const newEventsFromSplit = this._splitSafely(nextSeg, inter);
            for (let i = 0, iMax = newEventsFromSplit.length; i < iMax; i++) {
              newEvents.push(newEventsFromSplit[i]);
            }
          }
        }
      }
      this.tree.delete(segment);
    }
    return newEvents;
  }
  /* Safely split a segment that is currently in the datastructures
   * IE - a segment other than the one that is currently being processed. */
  _splitSafely(seg, pt) {
    this.tree.delete(seg);
    const rightSE = seg.rightSE;
    this.queue.delete(rightSE);
    const newEvents = seg.split(pt);
    newEvents.push(rightSE);
    if (seg.consumedBy === void 0) this.tree.add(seg);
    return newEvents;
  }
};
var Operation = class {
  type;
  numMultiPolys;
  run(type, geom, moreGeoms) {
    operation.type = type;
    const multipolys = [new MultiPolyIn(geom, true)];
    for (let i = 0, iMax = moreGeoms.length; i < iMax; i++) {
      multipolys.push(new MultiPolyIn(moreGeoms[i], false));
    }
    operation.numMultiPolys = multipolys.length;
    if (operation.type === "difference") {
      const subject = multipolys[0];
      let i = 1;
      while (i < multipolys.length) {
        if (getBboxOverlap(multipolys[i].bbox, subject.bbox) !== null) i++;
        else multipolys.splice(i, 1);
      }
    }
    if (operation.type === "intersection") {
      for (let i = 0, iMax = multipolys.length; i < iMax; i++) {
        const mpA = multipolys[i];
        for (let j = i + 1, jMax = multipolys.length; j < jMax; j++) {
          if (getBboxOverlap(mpA.bbox, multipolys[j].bbox) === null) return [];
        }
      }
    }
    const queue = new SplayTreeSet(SweepEvent.compare);
    for (let i = 0, iMax = multipolys.length; i < iMax; i++) {
      const sweepEvents = multipolys[i].getSweepEvents();
      for (let j = 0, jMax = sweepEvents.length; j < jMax; j++) {
        queue.add(sweepEvents[j]);
      }
    }
    const sweepLine = new SweepLine(queue);
    let evt = null;
    if (queue.size != 0) {
      evt = queue.first();
      queue.delete(evt);
    }
    while (evt) {
      const newEvents = sweepLine.process(evt);
      for (let i = 0, iMax = newEvents.length; i < iMax; i++) {
        const evt2 = newEvents[i];
        if (evt2.consumedBy === void 0) queue.add(evt2);
      }
      if (queue.size != 0) {
        evt = queue.first();
        queue.delete(evt);
      } else {
        evt = null;
      }
    }
    precision.reset();
    const ringsOut = RingOut.factory(sweepLine.segments);
    const result = new MultiPolyOut(ringsOut);
    return result.getGeom();
  }
};
var operation = new Operation();
var operation_default = operation;
var segmentId = 0;
var Segment2 = class _Segment {
  id;
  leftSE;
  rightSE;
  rings;
  windings;
  ringOut;
  consumedBy;
  prev;
  _prevInResult;
  _beforeState;
  _afterState;
  _isInResult;
  /* This compare() function is for ordering segments in the sweep
   * line tree, and does so according to the following criteria:
   *
   * Consider the vertical line that lies an infinestimal step to the
   * right of the right-more of the two left endpoints of the input
   * segments. Imagine slowly moving a point up from negative infinity
   * in the increasing y direction. Which of the two segments will that
   * point intersect first? That segment comes 'before' the other one.
   *
   * If neither segment would be intersected by such a line, (if one
   * or more of the segments are vertical) then the line to be considered
   * is directly on the right-more of the two left inputs.
   */
  static compare(a, b) {
    const alx = a.leftSE.point.x;
    const blx = b.leftSE.point.x;
    const arx = a.rightSE.point.x;
    const brx = b.rightSE.point.x;
    if (brx.isLessThan(alx)) return 1;
    if (arx.isLessThan(blx)) return -1;
    const aly = a.leftSE.point.y;
    const bly = b.leftSE.point.y;
    const ary = a.rightSE.point.y;
    const bry = b.rightSE.point.y;
    if (alx.isLessThan(blx)) {
      if (bly.isLessThan(aly) && bly.isLessThan(ary)) return 1;
      if (bly.isGreaterThan(aly) && bly.isGreaterThan(ary)) return -1;
      const aCmpBLeft = a.comparePoint(b.leftSE.point);
      if (aCmpBLeft < 0) return 1;
      if (aCmpBLeft > 0) return -1;
      const bCmpARight = b.comparePoint(a.rightSE.point);
      if (bCmpARight !== 0) return bCmpARight;
      return -1;
    }
    if (alx.isGreaterThan(blx)) {
      if (aly.isLessThan(bly) && aly.isLessThan(bry)) return -1;
      if (aly.isGreaterThan(bly) && aly.isGreaterThan(bry)) return 1;
      const bCmpALeft = b.comparePoint(a.leftSE.point);
      if (bCmpALeft !== 0) return bCmpALeft;
      const aCmpBRight = a.comparePoint(b.rightSE.point);
      if (aCmpBRight < 0) return 1;
      if (aCmpBRight > 0) return -1;
      return 1;
    }
    if (aly.isLessThan(bly)) return -1;
    if (aly.isGreaterThan(bly)) return 1;
    if (arx.isLessThan(brx)) {
      const bCmpARight = b.comparePoint(a.rightSE.point);
      if (bCmpARight !== 0) return bCmpARight;
    }
    if (arx.isGreaterThan(brx)) {
      const aCmpBRight = a.comparePoint(b.rightSE.point);
      if (aCmpBRight < 0) return 1;
      if (aCmpBRight > 0) return -1;
    }
    if (!arx.eq(brx)) {
      const ay = ary.minus(aly);
      const ax = arx.minus(alx);
      const by = bry.minus(bly);
      const bx = brx.minus(blx);
      if (ay.isGreaterThan(ax) && by.isLessThan(bx)) return 1;
      if (ay.isLessThan(ax) && by.isGreaterThan(bx)) return -1;
    }
    if (arx.isGreaterThan(brx)) return 1;
    if (arx.isLessThan(brx)) return -1;
    if (ary.isLessThan(bry)) return -1;
    if (ary.isGreaterThan(bry)) return 1;
    if (a.id < b.id) return -1;
    if (a.id > b.id) return 1;
    return 0;
  }
  /* Warning: a reference to ringWindings input will be stored,
   *  and possibly will be later modified */
  constructor(leftSE, rightSE, rings, windings) {
    this.id = ++segmentId;
    this.leftSE = leftSE;
    leftSE.segment = this;
    leftSE.otherSE = rightSE;
    this.rightSE = rightSE;
    rightSE.segment = this;
    rightSE.otherSE = leftSE;
    this.rings = rings;
    this.windings = windings;
  }
  static fromRing(pt1, pt2, ring) {
    let leftPt, rightPt, winding;
    const cmpPts = SweepEvent.comparePoints(pt1, pt2);
    if (cmpPts < 0) {
      leftPt = pt1;
      rightPt = pt2;
      winding = 1;
    } else if (cmpPts > 0) {
      leftPt = pt2;
      rightPt = pt1;
      winding = -1;
    } else
      throw new Error(
        `Tried to create degenerate segment at [${pt1.x}, ${pt1.y}]`
      );
    const leftSE = new SweepEvent(leftPt, true);
    const rightSE = new SweepEvent(rightPt, false);
    return new _Segment(leftSE, rightSE, [ring], [winding]);
  }
  /* When a segment is split, the rightSE is replaced with a new sweep event */
  replaceRightSE(newRightSE) {
    this.rightSE = newRightSE;
    this.rightSE.segment = this;
    this.rightSE.otherSE = this.leftSE;
    this.leftSE.otherSE = this.rightSE;
  }
  bbox() {
    const y12 = this.leftSE.point.y;
    const y2 = this.rightSE.point.y;
    return {
      ll: { x: this.leftSE.point.x, y: y12.isLessThan(y2) ? y12 : y2 },
      ur: { x: this.rightSE.point.x, y: y12.isGreaterThan(y2) ? y12 : y2 }
    };
  }
  /* A vector from the left point to the right */
  vector() {
    return {
      x: this.rightSE.point.x.minus(this.leftSE.point.x),
      y: this.rightSE.point.y.minus(this.leftSE.point.y)
    };
  }
  isAnEndpoint(pt) {
    return pt.x.eq(this.leftSE.point.x) && pt.y.eq(this.leftSE.point.y) || pt.x.eq(this.rightSE.point.x) && pt.y.eq(this.rightSE.point.y);
  }
  /* Compare this segment with a point.
   *
   * A point P is considered to be colinear to a segment if there
   * exists a distance D such that if we travel along the segment
   * from one * endpoint towards the other a distance D, we find
   * ourselves at point P.
   *
   * Return value indicates:
   *
   *   1: point lies above the segment (to the left of vertical)
   *   0: point is colinear to segment
   *  -1: point lies below the segment (to the right of vertical)
   */
  comparePoint(point2) {
    return precision.orient(this.leftSE.point, point2, this.rightSE.point);
  }
  /**
   * Given another segment, returns the first non-trivial intersection
   * between the two segments (in terms of sweep line ordering), if it exists.
   *
   * A 'non-trivial' intersection is one that will cause one or both of the
   * segments to be split(). As such, 'trivial' vs. 'non-trivial' intersection:
   *
   *   * endpoint of segA with endpoint of segB --> trivial
   *   * endpoint of segA with point along segB --> non-trivial
   *   * endpoint of segB with point along segA --> non-trivial
   *   * point along segA with point along segB --> non-trivial
   *
   * If no non-trivial intersection exists, return null
   * Else, return null.
   */
  getIntersection(other) {
    const tBbox = this.bbox();
    const oBbox = other.bbox();
    const bboxOverlap = getBboxOverlap(tBbox, oBbox);
    if (bboxOverlap === null) return null;
    const tlp = this.leftSE.point;
    const trp = this.rightSE.point;
    const olp = other.leftSE.point;
    const orp = other.rightSE.point;
    const touchesOtherLSE = isInBbox(tBbox, olp) && this.comparePoint(olp) === 0;
    const touchesThisLSE = isInBbox(oBbox, tlp) && other.comparePoint(tlp) === 0;
    const touchesOtherRSE = isInBbox(tBbox, orp) && this.comparePoint(orp) === 0;
    const touchesThisRSE = isInBbox(oBbox, trp) && other.comparePoint(trp) === 0;
    if (touchesThisLSE && touchesOtherLSE) {
      if (touchesThisRSE && !touchesOtherRSE) return trp;
      if (!touchesThisRSE && touchesOtherRSE) return orp;
      return null;
    }
    if (touchesThisLSE) {
      if (touchesOtherRSE) {
        if (tlp.x.eq(orp.x) && tlp.y.eq(orp.y)) return null;
      }
      return tlp;
    }
    if (touchesOtherLSE) {
      if (touchesThisRSE) {
        if (trp.x.eq(olp.x) && trp.y.eq(olp.y)) return null;
      }
      return olp;
    }
    if (touchesThisRSE && touchesOtherRSE) return null;
    if (touchesThisRSE) return trp;
    if (touchesOtherRSE) return orp;
    const pt = intersection(tlp, this.vector(), olp, other.vector());
    if (pt === null) return null;
    if (!isInBbox(bboxOverlap, pt)) return null;
    return precision.snap(pt);
  }
  /**
   * Split the given segment into multiple segments on the given points.
   *  * Each existing segment will retain its leftSE and a new rightSE will be
   *    generated for it.
   *  * A new segment will be generated which will adopt the original segment's
   *    rightSE, and a new leftSE will be generated for it.
   *  * If there are more than two points given to split on, new segments
   *    in the middle will be generated with new leftSE and rightSE's.
   *  * An array of the newly generated SweepEvents will be returned.
   *
   * Warning: input array of points is modified
   */
  split(point2) {
    const newEvents = [];
    const alreadyLinked = point2.events !== void 0;
    const newLeftSE = new SweepEvent(point2, true);
    const newRightSE = new SweepEvent(point2, false);
    const oldRightSE = this.rightSE;
    this.replaceRightSE(newRightSE);
    newEvents.push(newRightSE);
    newEvents.push(newLeftSE);
    const newSeg = new _Segment(
      newLeftSE,
      oldRightSE,
      this.rings.slice(),
      this.windings.slice()
    );
    if (SweepEvent.comparePoints(newSeg.leftSE.point, newSeg.rightSE.point) > 0) {
      newSeg.swapEvents();
    }
    if (SweepEvent.comparePoints(this.leftSE.point, this.rightSE.point) > 0) {
      this.swapEvents();
    }
    if (alreadyLinked) {
      newLeftSE.checkForConsuming();
      newRightSE.checkForConsuming();
    }
    return newEvents;
  }
  /* Swap which event is left and right */
  swapEvents() {
    const tmpEvt = this.rightSE;
    this.rightSE = this.leftSE;
    this.leftSE = tmpEvt;
    this.leftSE.isLeft = true;
    this.rightSE.isLeft = false;
    for (let i = 0, iMax = this.windings.length; i < iMax; i++) {
      this.windings[i] *= -1;
    }
  }
  /* Consume another segment. We take their rings under our wing
   * and mark them as consumed. Use for perfectly overlapping segments */
  consume(other) {
    let consumer = this;
    let consumee = other;
    while (consumer.consumedBy) consumer = consumer.consumedBy;
    while (consumee.consumedBy) consumee = consumee.consumedBy;
    const cmp = _Segment.compare(consumer, consumee);
    if (cmp === 0) return;
    if (cmp > 0) {
      const tmp = consumer;
      consumer = consumee;
      consumee = tmp;
    }
    if (consumer.prev === consumee) {
      const tmp = consumer;
      consumer = consumee;
      consumee = tmp;
    }
    for (let i = 0, iMax = consumee.rings.length; i < iMax; i++) {
      const ring = consumee.rings[i];
      const winding = consumee.windings[i];
      const index2 = consumer.rings.indexOf(ring);
      if (index2 === -1) {
        consumer.rings.push(ring);
        consumer.windings.push(winding);
      } else consumer.windings[index2] += winding;
    }
    consumee.rings = null;
    consumee.windings = null;
    consumee.consumedBy = consumer;
    consumee.leftSE.consumedBy = consumer.leftSE;
    consumee.rightSE.consumedBy = consumer.rightSE;
  }
  /* The first segment previous segment chain that is in the result */
  prevInResult() {
    if (this._prevInResult !== void 0) return this._prevInResult;
    if (!this.prev) this._prevInResult = null;
    else if (this.prev.isInResult()) this._prevInResult = this.prev;
    else this._prevInResult = this.prev.prevInResult();
    return this._prevInResult;
  }
  beforeState() {
    if (this._beforeState !== void 0) return this._beforeState;
    if (!this.prev)
      this._beforeState = {
        rings: [],
        windings: [],
        multiPolys: []
      };
    else {
      const seg = this.prev.consumedBy || this.prev;
      this._beforeState = seg.afterState();
    }
    return this._beforeState;
  }
  afterState() {
    if (this._afterState !== void 0) return this._afterState;
    const beforeState = this.beforeState();
    this._afterState = {
      rings: beforeState.rings.slice(0),
      windings: beforeState.windings.slice(0),
      multiPolys: []
    };
    const ringsAfter = this._afterState.rings;
    const windingsAfter = this._afterState.windings;
    const mpsAfter = this._afterState.multiPolys;
    for (let i = 0, iMax = this.rings.length; i < iMax; i++) {
      const ring = this.rings[i];
      const winding = this.windings[i];
      const index2 = ringsAfter.indexOf(ring);
      if (index2 === -1) {
        ringsAfter.push(ring);
        windingsAfter.push(winding);
      } else windingsAfter[index2] += winding;
    }
    const polysAfter = [];
    const polysExclude = [];
    for (let i = 0, iMax = ringsAfter.length; i < iMax; i++) {
      if (windingsAfter[i] === 0) continue;
      const ring = ringsAfter[i];
      const poly = ring.poly;
      if (polysExclude.indexOf(poly) !== -1) continue;
      if (ring.isExterior) polysAfter.push(poly);
      else {
        if (polysExclude.indexOf(poly) === -1) polysExclude.push(poly);
        const index2 = polysAfter.indexOf(ring.poly);
        if (index2 !== -1) polysAfter.splice(index2, 1);
      }
    }
    for (let i = 0, iMax = polysAfter.length; i < iMax; i++) {
      const mp = polysAfter[i].multiPoly;
      if (mpsAfter.indexOf(mp) === -1) mpsAfter.push(mp);
    }
    return this._afterState;
  }
  /* Is this segment part of the final result? */
  isInResult() {
    if (this.consumedBy) return false;
    if (this._isInResult !== void 0) return this._isInResult;
    const mpsBefore = this.beforeState().multiPolys;
    const mpsAfter = this.afterState().multiPolys;
    switch (operation_default.type) {
      case "union": {
        const noBefores = mpsBefore.length === 0;
        const noAfters = mpsAfter.length === 0;
        this._isInResult = noBefores !== noAfters;
        break;
      }
      case "intersection": {
        let least;
        let most;
        if (mpsBefore.length < mpsAfter.length) {
          least = mpsBefore.length;
          most = mpsAfter.length;
        } else {
          least = mpsAfter.length;
          most = mpsBefore.length;
        }
        this._isInResult = most === operation_default.numMultiPolys && least < most;
        break;
      }
      case "xor": {
        const diff = Math.abs(mpsBefore.length - mpsAfter.length);
        this._isInResult = diff % 2 === 1;
        break;
      }
      case "difference": {
        const isJustSubject = (mps) => mps.length === 1 && mps[0].isSubject;
        this._isInResult = isJustSubject(mpsBefore) !== isJustSubject(mpsAfter);
        break;
      }
    }
    return this._isInResult;
  }
};
var RingIn = class {
  poly;
  isExterior;
  segments;
  bbox;
  constructor(geomRing, poly, isExterior) {
    if (!Array.isArray(geomRing) || geomRing.length === 0) {
      throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
    }
    this.poly = poly;
    this.isExterior = isExterior;
    this.segments = [];
    if (typeof geomRing[0][0] !== "number" || typeof geomRing[0][1] !== "number") {
      throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
    }
    const firstPoint = precision.snap({ x: new BigNumber(geomRing[0][0]), y: new BigNumber(geomRing[0][1]) });
    this.bbox = {
      ll: { x: firstPoint.x, y: firstPoint.y },
      ur: { x: firstPoint.x, y: firstPoint.y }
    };
    let prevPoint = firstPoint;
    for (let i = 1, iMax = geomRing.length; i < iMax; i++) {
      if (typeof geomRing[i][0] !== "number" || typeof geomRing[i][1] !== "number") {
        throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
      }
      const point2 = precision.snap({ x: new BigNumber(geomRing[i][0]), y: new BigNumber(geomRing[i][1]) });
      if (point2.x.eq(prevPoint.x) && point2.y.eq(prevPoint.y)) continue;
      this.segments.push(Segment2.fromRing(prevPoint, point2, this));
      if (point2.x.isLessThan(this.bbox.ll.x)) this.bbox.ll.x = point2.x;
      if (point2.y.isLessThan(this.bbox.ll.y)) this.bbox.ll.y = point2.y;
      if (point2.x.isGreaterThan(this.bbox.ur.x)) this.bbox.ur.x = point2.x;
      if (point2.y.isGreaterThan(this.bbox.ur.y)) this.bbox.ur.y = point2.y;
      prevPoint = point2;
    }
    if (!firstPoint.x.eq(prevPoint.x) || !firstPoint.y.eq(prevPoint.y)) {
      this.segments.push(Segment2.fromRing(prevPoint, firstPoint, this));
    }
  }
  getSweepEvents() {
    const sweepEvents = [];
    for (let i = 0, iMax = this.segments.length; i < iMax; i++) {
      const segment = this.segments[i];
      sweepEvents.push(segment.leftSE);
      sweepEvents.push(segment.rightSE);
    }
    return sweepEvents;
  }
};
var PolyIn = class {
  multiPoly;
  exteriorRing;
  interiorRings;
  bbox;
  constructor(geomPoly, multiPoly) {
    if (!Array.isArray(geomPoly)) {
      throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
    }
    this.exteriorRing = new RingIn(geomPoly[0], this, true);
    this.bbox = {
      ll: { x: this.exteriorRing.bbox.ll.x, y: this.exteriorRing.bbox.ll.y },
      ur: { x: this.exteriorRing.bbox.ur.x, y: this.exteriorRing.bbox.ur.y }
    };
    this.interiorRings = [];
    for (let i = 1, iMax = geomPoly.length; i < iMax; i++) {
      const ring = new RingIn(geomPoly[i], this, false);
      if (ring.bbox.ll.x.isLessThan(this.bbox.ll.x)) this.bbox.ll.x = ring.bbox.ll.x;
      if (ring.bbox.ll.y.isLessThan(this.bbox.ll.y)) this.bbox.ll.y = ring.bbox.ll.y;
      if (ring.bbox.ur.x.isGreaterThan(this.bbox.ur.x)) this.bbox.ur.x = ring.bbox.ur.x;
      if (ring.bbox.ur.y.isGreaterThan(this.bbox.ur.y)) this.bbox.ur.y = ring.bbox.ur.y;
      this.interiorRings.push(ring);
    }
    this.multiPoly = multiPoly;
  }
  getSweepEvents() {
    const sweepEvents = this.exteriorRing.getSweepEvents();
    for (let i = 0, iMax = this.interiorRings.length; i < iMax; i++) {
      const ringSweepEvents = this.interiorRings[i].getSweepEvents();
      for (let j = 0, jMax = ringSweepEvents.length; j < jMax; j++) {
        sweepEvents.push(ringSweepEvents[j]);
      }
    }
    return sweepEvents;
  }
};
var MultiPolyIn = class {
  isSubject;
  polys;
  bbox;
  constructor(geom, isSubject) {
    if (!Array.isArray(geom)) {
      throw new Error("Input geometry is not a valid Polygon or MultiPolygon");
    }
    try {
      if (typeof geom[0][0][0] === "number") geom = [geom];
    } catch (ex) {
    }
    this.polys = [];
    this.bbox = {
      ll: { x: new BigNumber(Number.POSITIVE_INFINITY), y: new BigNumber(Number.POSITIVE_INFINITY) },
      ur: { x: new BigNumber(Number.NEGATIVE_INFINITY), y: new BigNumber(Number.NEGATIVE_INFINITY) }
    };
    for (let i = 0, iMax = geom.length; i < iMax; i++) {
      const poly = new PolyIn(geom[i], this);
      if (poly.bbox.ll.x.isLessThan(this.bbox.ll.x)) this.bbox.ll.x = poly.bbox.ll.x;
      if (poly.bbox.ll.y.isLessThan(this.bbox.ll.y)) this.bbox.ll.y = poly.bbox.ll.y;
      if (poly.bbox.ur.x.isGreaterThan(this.bbox.ur.x)) this.bbox.ur.x = poly.bbox.ur.x;
      if (poly.bbox.ur.y.isGreaterThan(this.bbox.ur.y)) this.bbox.ur.y = poly.bbox.ur.y;
      this.polys.push(poly);
    }
    this.isSubject = isSubject;
  }
  getSweepEvents() {
    const sweepEvents = [];
    for (let i = 0, iMax = this.polys.length; i < iMax; i++) {
      const polySweepEvents = this.polys[i].getSweepEvents();
      for (let j = 0, jMax = polySweepEvents.length; j < jMax; j++) {
        sweepEvents.push(polySweepEvents[j]);
      }
    }
    return sweepEvents;
  }
};
var union = (geom, ...moreGeoms) => operation_default.run("union", geom, moreGeoms);
var difference = (geom, ...moreGeoms) => operation_default.run("difference", geom, moreGeoms);
precision.set;
function difference2(features) {
  const geoms = [];
  geomEach(features, (geom) => {
    geoms.push(geom.coordinates);
  });
  if (geoms.length < 2) {
    throw new Error("Must have at least two features");
  }
  const properties = features.features[0].properties || {};
  const differenced = difference(geoms[0], ...geoms.slice(1));
  if (differenced.length === 0) return null;
  if (differenced.length === 1) return polygon(differenced[0], properties);
  return multiPolygon(differenced, properties);
}
var index_default$2 = difference2;
function geojsonRbush(maxEntries) {
  var tree = new RBush(maxEntries);
  tree.insert = function(feature2) {
    if (feature2.type !== "Feature") throw new Error("invalid feature");
    feature2.bbox = feature2.bbox ? feature2.bbox : bbox$1(feature2);
    return RBush.prototype.insert.call(this, feature2);
  };
  tree.load = function(features) {
    var load = [];
    if (Array.isArray(features)) {
      features.forEach(function(feature2) {
        if (feature2.type !== "Feature") throw new Error("invalid features");
        feature2.bbox = feature2.bbox ? feature2.bbox : bbox$1(feature2);
        load.push(feature2);
      });
    } else {
      featureEach(features, function(feature2) {
        if (feature2.type !== "Feature") throw new Error("invalid features");
        feature2.bbox = feature2.bbox ? feature2.bbox : bbox$1(feature2);
        load.push(feature2);
      });
    }
    return RBush.prototype.load.call(this, load);
  };
  tree.remove = function(feature2, equals2) {
    if (feature2.type !== "Feature") throw new Error("invalid feature");
    feature2.bbox = feature2.bbox ? feature2.bbox : bbox$1(feature2);
    return RBush.prototype.remove.call(this, feature2, equals2);
  };
  tree.clear = function() {
    return RBush.prototype.clear.call(this);
  };
  tree.search = function(geojson) {
    var features = RBush.prototype.search.call(this, this.toBBox(geojson));
    return featureCollection(features);
  };
  tree.collides = function(geojson) {
    return RBush.prototype.collides.call(this, this.toBBox(geojson));
  };
  tree.all = function() {
    var features = RBush.prototype.all.call(this);
    return featureCollection(features);
  };
  tree.toJSON = function() {
    return RBush.prototype.toJSON.call(this);
  };
  tree.fromJSON = function(json) {
    return RBush.prototype.fromJSON.call(this, json);
  };
  tree.toBBox = function(geojson) {
    var bbox2;
    if (geojson.bbox) bbox2 = geojson.bbox;
    else if (Array.isArray(geojson) && geojson.length === 4) bbox2 = geojson;
    else if (Array.isArray(geojson) && geojson.length === 6)
      bbox2 = [geojson[0], geojson[1], geojson[3], geojson[4]];
    else if (geojson.type === "Feature") bbox2 = bbox$1(geojson);
    else if (geojson.type === "FeatureCollection") bbox2 = bbox$1(geojson);
    else throw new Error("invalid geojson");
    return {
      minX: bbox2[0],
      minY: bbox2[1],
      maxX: bbox2[2],
      maxY: bbox2[3]
    };
  };
  return tree;
}
function truncate(geojson, options) {
  options = options != null ? options : {};
  if (!isObject(options)) throw new Error("options is invalid");
  var precision2 = options.precision;
  var coordinates = options.coordinates;
  var mutate = options.mutate;
  precision2 = precision2 === void 0 || precision2 === null || isNaN(precision2) ? 6 : precision2;
  coordinates = coordinates === void 0 || coordinates === null || isNaN(coordinates) ? 3 : coordinates;
  if (!geojson) throw new Error("<geojson> is required");
  if (typeof precision2 !== "number")
    throw new Error("<precision> must be a number");
  if (typeof coordinates !== "number")
    throw new Error("<coordinates> must be a number");
  if (mutate === false || mutate === void 0)
    geojson = JSON.parse(JSON.stringify(geojson));
  var factor = Math.pow(10, precision2);
  coordEach(geojson, function(coords) {
    truncateCoords(coords, factor, coordinates);
  });
  return geojson;
}
function truncateCoords(coords, factor, coordinates) {
  if (coords.length > coordinates) coords.splice(coordinates, coords.length);
  for (var i = 0; i < coords.length; i++) {
    coords[i] = Math.round(coords[i] * factor) / factor;
  }
  return coords;
}
function lineSegment(geojson) {
  if (!geojson) {
    throw new Error("geojson is required");
  }
  const results = [];
  flattenEach(geojson, (feature2) => {
    lineSegmentFeature(feature2, results);
  });
  return featureCollection(results);
}
function lineSegmentFeature(geojson, results) {
  let coords = [];
  const geometry = geojson.geometry;
  if (geometry !== null) {
    switch (geometry.type) {
      case "Polygon":
        coords = getCoords(geometry);
        break;
      case "LineString":
        coords = [getCoords(geometry)];
    }
    coords.forEach((coord) => {
      const segments = createSegments(coord, geojson.properties);
      segments.forEach((segment) => {
        segment.id = results.length;
        results.push(segment);
      });
    });
  }
}
function createSegments(coords, properties) {
  const segments = [];
  coords.reduce((previousCoords, currentCoords) => {
    const segment = lineString([previousCoords, currentCoords], properties);
    segment.bbox = bbox(previousCoords, currentCoords);
    segments.push(segment);
    return currentCoords;
  });
  return segments;
}
function bbox(coords1, coords2) {
  const x12 = coords1[0];
  const y12 = coords1[1];
  const x2 = coords2[0];
  const y2 = coords2[1];
  const west = x12 < x2 ? x12 : x2;
  const south = y12 < y2 ? y12 : y2;
  const east = x12 > x2 ? x12 : x2;
  const north = y12 > y2 ? y12 : y2;
  return [west, south, east, north];
}
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
function nearestPointOnLine(lines, pt, options = {}) {
  if (!lines || !pt) {
    throw new Error("lines and pt are required arguments");
  }
  const ptPos = getCoord(pt);
  let closestPt = point([Infinity, Infinity], {
    dist: Infinity,
    index: -1,
    multiFeatureIndex: -1,
    location: -1
  });
  let length2 = 0;
  flattenEach(
    lines,
    function(line2, _featureIndex, multiFeatureIndex) {
      const coords = getCoords(line2);
      for (let i = 0; i < coords.length - 1; i++) {
        const start = point(coords[i]);
        const startPos = getCoord(start);
        const stop = point(coords[i + 1]);
        const stopPos = getCoord(stop);
        const sectionLength = distance(start, stop, options);
        let intersectPos;
        let wasEnd;
        if (stopPos[0] === ptPos[0] && stopPos[1] === ptPos[1]) {
          [intersectPos, wasEnd] = [stopPos, true];
        } else if (startPos[0] === ptPos[0] && startPos[1] === ptPos[1]) {
          [intersectPos, wasEnd] = [startPos, false];
        } else {
          [intersectPos, wasEnd] = nearestPointOnSegment(
            startPos,
            stopPos,
            ptPos
          );
        }
        const intersectPt = point(intersectPos, {
          dist: distance(pt, intersectPos, options),
          multiFeatureIndex,
          location: length2 + distance(start, intersectPos, options)
        });
        if (intersectPt.properties.dist < closestPt.properties.dist) {
          closestPt = __spreadProps(__spreadValues({}, intersectPt), {
            properties: __spreadProps(__spreadValues({}, intersectPt.properties), {
              // Legacy behaviour where index progresses to next segment # if we
              // went with the end point this iteration.
              index: wasEnd ? i + 1 : i
            })
          });
        }
        length2 += sectionLength;
      }
    }
  );
  return closestPt;
}
function dot(v1, v2) {
  const [v1x, v1y, v1z] = v1;
  const [v2x, v2y, v2z] = v2;
  return v1x * v2x + v1y * v2y + v1z * v2z;
}
function cross(v1, v2) {
  const [v1x, v1y, v1z] = v1;
  const [v2x, v2y, v2z] = v2;
  return [v1y * v2z - v1z * v2y, v1z * v2x - v1x * v2z, v1x * v2y - v1y * v2x];
}
function magnitude(v) {
  return Math.sqrt(Math.pow(v[0], 2) + Math.pow(v[1], 2) + Math.pow(v[2], 2));
}
function normalize(v) {
  const mag = magnitude(v);
  return [v[0] / mag, v[1] / mag, v[2] / mag];
}
function lngLatToVector(a) {
  const lat = degreesToRadians(a[1]);
  const lng = degreesToRadians(a[0]);
  return [
    Math.cos(lat) * Math.cos(lng),
    Math.cos(lat) * Math.sin(lng),
    Math.sin(lat)
  ];
}
function vectorToLngLat(v) {
  const [x, y, z] = v;
  const zClamp = Math.min(Math.max(z, -1), 1);
  const lat = radiansToDegrees(Math.asin(zClamp));
  const lng = radiansToDegrees(Math.atan2(y, x));
  return [lng, lat];
}
function nearestPointOnSegment(posA, posB, posC) {
  const A = lngLatToVector(posA);
  const B2 = lngLatToVector(posB);
  const C = lngLatToVector(posC);
  const segmentAxis = cross(A, B2);
  if (segmentAxis[0] === 0 && segmentAxis[1] === 0 && segmentAxis[2] === 0) {
    if (dot(A, B2) > 0) {
      return [[...posB], true];
    } else {
      return [[...posC], false];
    }
  }
  const targetAxis = cross(segmentAxis, C);
  if (targetAxis[0] === 0 && targetAxis[1] === 0 && targetAxis[2] === 0) {
    return [[...posB], true];
  }
  const intersectionAxis = cross(targetAxis, segmentAxis);
  const I1 = normalize(intersectionAxis);
  const I2 = [-I1[0], -I1[1], -I1[2]];
  const I = dot(C, I1) > dot(C, I2) ? I1 : I2;
  const segmentAxisNorm = normalize(segmentAxis);
  const cmpAI = dot(cross(A, I), segmentAxisNorm);
  const cmpIB = dot(cross(I, B2), segmentAxisNorm);
  if (cmpAI >= 0 && cmpIB >= 0) {
    return [vectorToLngLat(I), false];
  }
  if (dot(A, C) > dot(B2, C)) {
    return [[...posA], false];
  } else {
    return [[...posB], true];
  }
}
function lineSplit(line2, splitter2) {
  if (!line2) throw new Error("line is required");
  if (!splitter2) throw new Error("splitter is required");
  var lineType = getType(line2);
  var splitterType = getType(splitter2);
  if (lineType !== "LineString") throw new Error("line must be LineString");
  if (splitterType === "FeatureCollection")
    throw new Error("splitter cannot be a FeatureCollection");
  if (splitterType === "GeometryCollection")
    throw new Error("splitter cannot be a GeometryCollection");
  var truncatedSplitter = truncate(splitter2, { precision: 7 });
  switch (splitterType) {
    case "Point":
      return splitLineWithPoint(line2, truncatedSplitter);
    case "MultiPoint":
      return splitLineWithPoints(line2, truncatedSplitter);
    case "LineString":
    case "MultiLineString":
    case "Polygon":
    case "MultiPolygon":
      return splitLineWithPoints(
        line2,
        lineIntersect(line2, truncatedSplitter, {
          ignoreSelfIntersections: true
        })
      );
  }
}
function splitLineWithPoints(line2, splitter2) {
  var results = [];
  var tree = geojsonRbush();
  flattenEach(splitter2, function(point2) {
    results.forEach(function(feature2, index2) {
      feature2.id = index2;
    });
    if (!results.length) {
      results = splitLineWithPoint(line2, point2).features;
      tree.load(featureCollection(results));
    } else {
      var search = tree.search(point2);
      if (search.features.length) {
        var closestLine = findClosestFeature(point2, search);
        results = results.filter(function(feature2) {
          return feature2.id !== closestLine.id;
        });
        tree.remove(closestLine);
        featureEach(splitLineWithPoint(closestLine, point2), function(line22) {
          results.push(line22);
          tree.insert(line22);
        });
      }
    }
  });
  return featureCollection(results);
}
function splitLineWithPoint(line2, splitter2) {
  var results = [];
  var startPoint = getCoords(line2)[0];
  var endPoint = getCoords(line2)[line2.geometry.coordinates.length - 1];
  if (pointsEquals(startPoint, getCoord(splitter2)) || pointsEquals(endPoint, getCoord(splitter2)))
    return featureCollection([line2]);
  var tree = geojsonRbush();
  var segments = lineSegment(line2);
  tree.load(segments);
  var search = tree.search(splitter2);
  if (!search.features.length) return featureCollection([line2]);
  var closestSegment = findClosestFeature(splitter2, search);
  var initialValue = [startPoint];
  var lastCoords = featureReduce(
    segments,
    function(previous, current, index2) {
      var currentCoords = getCoords(current)[1];
      var splitterCoords = getCoord(splitter2);
      if (index2 === closestSegment.id) {
        previous.push(splitterCoords);
        results.push(lineString(previous));
        if (pointsEquals(splitterCoords, currentCoords))
          return [splitterCoords];
        return [splitterCoords, currentCoords];
      } else {
        previous.push(currentCoords);
        return previous;
      }
    },
    initialValue
  );
  if (lastCoords.length > 1) {
    results.push(lineString(lastCoords));
  }
  return featureCollection(results);
}
function findClosestFeature(point2, lines) {
  if (!lines.features.length) throw new Error("lines must contain features");
  if (lines.features.length === 1) return lines.features[0];
  var closestFeature;
  var closestDistance = Infinity;
  featureEach(lines, function(segment) {
    var pt = nearestPointOnLine(segment, point2);
    var dist = pt.properties.dist;
    if (dist < closestDistance) {
      closestFeature = segment;
      closestDistance = dist;
    }
  });
  return closestFeature;
}
function pointsEquals(pt1, pt2) {
  return pt1[0] === pt2[0] && pt1[1] === pt2[1];
}
var index_default$1 = lineSplit;
class EditCut extends BaseEdit {
  mode = "cut";
  lineDrawer = new LineDrawer(this.gm, { snappingMarkers: "first", targetShape: "polygon" });
  cutShapesAllowed = ["circle", "ellipse", "line", "rectangle", "polygon"];
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:draw`]: this.forwardLineDrawerEvent.bind(this),
    mousemove: this.onMouseMove.bind(this)
  };
  onStartAction() {
    this.lineDrawer.startAction();
    this.lineDrawer.on("firstMarkerClick", this.cutPolygonFinished.bind(this));
  }
  onEndAction() {
    this.lineDrawer.endAction();
  }
  onMouseMove(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    if (!this.lineDrawer.featureData) {
      this.fireMarkerPointerUpdateEvent();
    }
    return { next: true };
  }
  cutPolygonFinished(event) {
    this.lineDrawer.endShape();
    const geoJsonPolygon = index_default$9(event.geoJson);
    const bBoxfeatures = this.getBBoxFeaturesByPolygon(geoJsonPolygon);
    this.cutFeaturesByPolygon(bBoxfeatures, geoJsonPolygon);
  }
  getBBoxFeaturesByPolygon(geoJson) {
    const coordBounds = getGeoJsonBounds(geoJson);
    const polygonScreenBounds = this.gm.mapAdapter.coordBoundsToScreenBounds(coordBounds);
    return this.gm.mapAdapter.queryFeaturesByScreenCoordinates({
      queryCoordinates: polygonScreenBounds,
      sourceNames: [SOURCES.main]
    });
  }
  cutFeaturesByPolygon(bBoxfeatures, cutGeoJson) {
    bBoxfeatures.forEach((featureData) => {
      if (featureData.getShapeProperty("disableEdit") === true) {
        return;
      }
      if (isGeoJsonFeatureInPolygon(featureData.getGeoJson(), cutGeoJson)) {
        this.gm.features.delete(featureData);
        this.fireFeatureRemovedEvent(featureData);
        return;
      }
      if (!index_default$3(featureData.getGeoJson(), cutGeoJson)) {
        return;
      }
      if (!this.cutShapesAllowed.includes(featureData.shape)) {
        return;
      }
      if (featureData.shape === "line") {
        this.cutLineFeatureByPolygon(featureData, cutGeoJson);
        return;
      }
      this.cutPolygonFeatureByPolygon(featureData.id, cutGeoJson);
    });
  }
  cutLineFeatureByPolygon(featureData, cutGeoJson) {
    const shapeGeoJson = featureData.getGeoJson();
    const bufferedCutGeoJson = getBufferedOuterPolygon(this.gm.mapAdapter, cutGeoJson);
    let isCut = false;
    let coordinates = [];
    if (!bufferedCutGeoJson) {
      return;
    }
    if (shapeGeoJson.geometry.type === "MultiLineString") {
      shapeGeoJson.geometry.coordinates.forEach((lineCoordinates) => {
        if (isGeoJsonFeatureInPolygon(lineString(lineCoordinates), cutGeoJson)) {
          return;
        }
        const resultGeoJson = index_default$1(lineString(lineCoordinates), cutGeoJson);
        if (resultGeoJson.features.length === 0) {
          coordinates.push(lineCoordinates);
          return;
        }
        resultGeoJson.features.filter((feature2) => !isGeoJsonFeatureInPolygon(feature2, bufferedCutGeoJson)).forEach((feature2) => {
          isCut = true;
          coordinates.push(feature2.geometry.coordinates);
        });
      });
    } else if (shapeGeoJson.geometry.type === "LineString") {
      const resultGeoJson = index_default$1(shapeGeoJson, cutGeoJson);
      coordinates = resultGeoJson.features.filter(
        (feature2) => !isGeoJsonFeatureInPolygon(feature2, bufferedCutGeoJson) && feature2.geometry.type === "LineString"
      ).map((feature2) => feature2.geometry.coordinates);
      if (resultGeoJson.features.length > 0) {
        isCut = true;
      }
    }
    if (isCut && coordinates.length) {
      if (coordinates.length === 1) {
        featureData.updateGeoJsonGeometry({ type: "LineString", coordinates: coordinates[0] });
      } else {
        featureData.updateGeoJsonGeometry({ type: "MultiLineString", coordinates });
      }
      this.fireFeatureUpdatedEvent({
        sourceFeatures: [featureData],
        targetFeatures: [featureData]
      });
    }
  }
  cutPolygonFeatureByPolygon(featureId2, cutGeoJson) {
    const featureData = this.gm.features.get(SOURCES.main, featureId2);
    if (!featureData) {
      log.warn("cutPolygonFeatureByPolygon: featureData not found", featureId2);
      return;
    }
    featureData.convertToPolygon();
    const shapeGeoJson = featureData.getGeoJson();
    const geoJsonDifference = this.getGeoJsonDifference(shapeGeoJson, cutGeoJson);
    if (geoJsonDifference) {
      featureData.updateGeoJsonGeometry(geoJsonDifference.geometry);
      this.fireFeatureUpdatedEvent({
        sourceFeatures: [featureData],
        targetFeatures: [featureData]
      });
    }
  }
  getGeoJsonDifference(shapeGeoJson, geoJson) {
    const collection = featureCollection([shapeGeoJson, geoJson]);
    const geoJsonDiff = index_default$2(collection);
    if (!geoJsonDiff) {
      return null;
    }
    if (geoJsonDiff.type === "Feature") {
      return geoJsonDiff;
    } else if (geoJsonDiff.type === "FeatureCollection") {
      log.error("getGeoJsonDifference: FeatureCollection detected (not supported)", geoJsonDiff);
    }
    return null;
  }
}
class EditDelete extends BaseEdit {
  mode = "delete";
  allowedShapes = [...SHAPE_NAMES];
  eventHandlers = {
    click: this.onMouseClick.bind(this)
  };
  onStartAction() {
    this.gm.markerPointer.enable({ invisibleMarker: true });
    this.gm.markerPointer.pauseSnapping();
  }
  onEndAction() {
    this.gm.markerPointer.resumeSnapping();
    this.gm.markerPointer.disable();
  }
  onMouseClick(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: false };
    }
    const principalFeature = this.getFeatureByMouseEvent({ event, sourceNames: [SOURCES.main] });
    if (principalFeature && this.allowedShapes.includes(principalFeature.shape)) {
      if (!this.gm.features.selection.has(principalFeature.id)) {
        this.gm.features.setSelection([principalFeature.id], true);
      }
      const linkedFeatures = this.gm.features.getLinkedFeatures(principalFeature);
      if (linkedFeatures.some((f) => f.getShapeProperty("disableEdit") === true)) {
        return { next: false };
      }
      this.gm.features.setSelection([], true);
      [principalFeature, ...linkedFeatures].map((featureData) => {
        this.gm.features.delete(featureData);
        this.fireFeatureRemovedEvent(featureData);
      });
    }
    return { next: false };
  }
}
class EditDrag extends BaseDrag {
  mode = "drag";
  onStartAction() {
  }
  onEndAction() {
  }
  handleGmEdit(event) {
    if (!isGmEditEvent(event)) {
      log.error("EditDrag.handleGmEdit: not an edit event", event);
      return { next: true };
    }
    if (event.action === "marker_move" && event.lngLatStart && event.lngLatEnd) {
      const lngLatDiff = getLngLatDiff(event.lngLatStart, event.lngLatEnd);
      event.linkedFeatures.map((featureData) => {
        this.moveFeature(featureData, lngLatDiff);
      });
      const isUpdated = this.moveFeature(event.featureData, lngLatDiff);
      if (isUpdated) {
        this.previousLngLat = event.lngLatEnd;
      }
      return { next: false };
    } else if (event.action === "marker_captured") {
      [event.featureData, ...event.linkedFeatures ?? []].map((featureData) => {
        featureData.changeSource({ sourceName: SOURCES.temporary, atomic: true });
        this.fireFeatureEditStartEvent({ feature: featureData });
      });
      this.flags.actionInProgress = true;
      this.setCursorToPointer();
    } else if (event.action === "marker_released") {
      this.previousLngLat = null;
      [event.featureData, ...event.linkedFeatures ?? []].map((featureData) => {
        featureData.changeSource({ sourceName: SOURCES.main, atomic: true });
        this.fireFeatureEditEndEvent({ feature: featureData });
      });
      this.flags.actionInProgress = false;
    }
    return { next: true };
  }
}
class EditRotate extends BaseDrag {
  mode = "rotate";
  allowedShapes = ["line", "rectangle", "polygon", "ellipse", "circle"];
  convertFeaturesTypes = ["rectangle"];
  shapeRotateHandlers = {
    marker: this.rotateFeature.bind(this),
    circle: this.rotateCircle.bind(this),
    circle_marker: this.rotateFeature.bind(this),
    text_marker: this.rotateFeature.bind(this),
    line: this.rotateFeature.bind(this),
    rectangle: this.rotateFeature.bind(this),
    polygon: this.rotateFeature.bind(this),
    ellipse: this.rotateEllipse.bind(this)
  };
  onStartAction() {
  }
  onEndAction() {
  }
  handleGmEdit(event) {
    if (!isGmEditEvent(event)) {
      log.error("EditChange.handleGmEdit: not an edit event", event);
      return { next: false };
    }
    if (this.isFeatureAllowed(event)) {
      return { next: true };
    }
    if (event.action === "marker_move" && event.lngLatStart && event.lngLatEnd) {
      if (event.markerData?.type === "vertex") {
        this.moveVertex(event);
      } else {
        const lngLatDiff = getLngLatDiff(event.lngLatStart, event.lngLatEnd);
        this.moveSource(event.featureData, lngLatDiff);
      }
      return { next: false };
    } else if (event.action === "marker_captured") {
      [event.featureData, ...event.linkedFeatures ?? []].map((featureData) => {
        featureData.changeSource({ sourceName: SOURCES.temporary, atomic: true });
        this.fireFeatureEditStartEvent({ feature: featureData });
      });
      this.setCursorToPointer();
      this.flags.actionInProgress = true;
    } else if (event.action === "marker_released") {
      this.flags.actionInProgress = false;
      [event.featureData, ...event.linkedFeatures ?? []].map((featureData) => {
        featureData.changeSource({ sourceName: SOURCES.main, atomic: true });
        this.fireFeatureEditEndEvent({ feature: featureData });
      });
    }
    return { next: true };
  }
  isFeatureAllowed(event) {
    return "featureData" in event && !this.allowedShapes.includes(event.featureData.shape);
  }
  moveVertex(event) {
    const shapeCentroid = geoJsonPointToLngLat(
      index_default$e(
        featureCollection([
          event.featureData.getGeoJson(),
          ...(event.linkedFeatures ?? []).map((featureData) => featureData.getGeoJson())
        ])
      )
    );
    [event.featureData, ...event.linkedFeatures ?? []].map((featureData) => {
      const updatedGeoJson = this.shapeRotateHandlers[featureData.shape]?.(featureData, shapeCentroid, event) || null;
      if (updatedGeoJson) {
        this.fireBeforeFeatureUpdate({
          features: [featureData],
          geoJsonFeatures: [updatedGeoJson]
        });
        const isUpdated = this.updateFeatureGeoJson({
          featureData,
          featureGeoJson: updatedGeoJson
        });
        if (isUpdated && this.convertFeaturesTypes.includes(featureData.shape)) {
          featureData.convertToPolygon();
        }
      } else {
        log.error("EditRotate.moveVertex: invalid geojson", updatedGeoJson, event);
      }
    });
  }
  rotateCircle(featureData, shapeCentroid, event) {
    if (featureData.shape !== "circle") {
      log.error("EditRotate.rotateCircle: invalid shape type", featureData);
      return null;
    }
    const center2 = featureData.getShapeProperty("center");
    if (!Array.isArray(center2)) {
      log.error("rotateCircle: missing center in the featureData", featureData);
      return null;
    }
    if (isEqualPosition(shapeCentroid, center2)) {
      return featureData.getGeoJson();
    }
    const deltaAngle = this.calculateRotationAngle(
      shapeCentroid,
      event.lngLatStart,
      event.lngLatEnd,
      false
    );
    const geoJson = featureData.getGeoJson();
    const circleRimLngLat = getGeoJsonFirstPoint(geoJson);
    if (!circleRimLngLat) {
      log.error("rotateCircle: missing center circleRimLngLat");
      return null;
    }
    const rotatedCenter = index_default$c(point(center2), deltaAngle, { pivot: shapeCentroid }).geometry.coordinates;
    const radius = this.gm.mapAdapter.getDistance(center2, circleRimLngLat);
    return getGeoJsonCircle({ center: rotatedCenter, radius });
  }
  rotateEllipse(featureData, shapeCentroid, event) {
    if (featureData.shape !== "ellipse") {
      log.error("EditRotate.rotateEllipse: invalid shape type", featureData);
      return null;
    }
    const center2 = featureData.getShapeProperty("center");
    const xSemiAxis = featureData.getShapeProperty("xSemiAxis");
    const ySemiAxis = featureData.getShapeProperty("ySemiAxis");
    const angle = featureData.getShapeProperty("angle");
    if (!Array.isArray(center2) || typeof xSemiAxis !== "number" || typeof ySemiAxis !== "number" || typeof angle !== "number") {
      log.error(
        "rotateEllipse: missing center, xSemiAxis, ySemiAxis or angle in the featureData",
        featureData
      );
      return null;
    }
    const deltaAngle = this.calculateRotationAngle(
      shapeCentroid,
      event.lngLatStart,
      event.lngLatEnd,
      false
    );
    const rotatedCenter = index_default$c(point(center2), deltaAngle, { pivot: shapeCentroid }).geometry.coordinates;
    return getGeoJsonEllipse({
      center: rotatedCenter,
      xSemiAxis,
      ySemiAxis,
      angle: angle + deltaAngle
    });
  }
  rotateFeature(featureData, shapeCentroid, event) {
    const geoJson = cloneDeep(featureData.getGeoJson());
    const angle = this.calculateRotationAngle(shapeCentroid, event.lngLatStart, event.lngLatEnd);
    geoJson.geometry = index_default$c(geoJson, angle, { pivot: shapeCentroid }).geometry;
    return geoJson;
  }
  calculateRotationAngle(pivot, start, end, normalize2 = true) {
    const bearingStart = index_default$b(pivot, start);
    const bearingEnd = index_default$b(pivot, end);
    const rotationAngle = bearingEnd - bearingStart;
    return normalize2 ? (rotationAngle + 360) % 360 : rotationAngle;
  }
}
const editClassMap = {
  drag: EditDrag,
  change: EditChange,
  rotate: EditRotate,
  scale: null,
  copy: null,
  cut: EditCut,
  split: null,
  union: null,
  difference: null,
  line_simplification: null,
  lasso: null,
  delete: EditDelete
};
const createEditInstance = (gm, mode) => {
  if (editClassMap[mode]) {
    return new editClassMap[mode](gm);
  }
  log.error(`Edit "${mode}" is not available`);
  return null;
};
class EditEventListener extends BaseEventListener {
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:edit`]: this.handleEditEvent.bind(this)
  };
  constructor(gm, bus) {
    super(gm);
    bus.attachEvents(this.eventHandlers);
  }
  handleEditEvent(payload) {
    if (!isGmEditEvent(payload) || !payload.mode) {
      return { next: true };
    }
    const actionInstanceKey = `${payload.actionType}__${payload.mode}`;
    if (payload.action === "mode_start") {
      this.trackExclusiveModes(payload);
      this.start(actionInstanceKey, payload);
      this.trackRelatedModes(payload);
    } else if (payload.action === "mode_end") {
      this.trackRelatedModes(payload);
      this.end(actionInstanceKey);
    }
    return { next: true };
  }
  start(actionInstanceKey, payload) {
    if (payload.action !== "mode_start") {
      return;
    }
    const actionInstance = createEditInstance(this.gm, payload.mode);
    if (!actionInstance) {
      return;
    }
    if (actionInstanceKey in this.gm.actionInstances) {
      log.error(`Action instance "${actionInstanceKey}" already exists`);
    }
    this.gm.actionInstances[actionInstanceKey] = actionInstance;
    actionInstance.startAction();
  }
  end(actionInstanceKey) {
    const actionInstance = this.gm.actionInstances[actionInstanceKey];
    if (actionInstance instanceof BaseEdit) {
      actionInstance.endAction();
      delete this.gm.actionInstances[actionInstanceKey];
    } else {
      console.error(
        `Wrong action instance for edit event "${actionInstanceKey}": `,
        actionInstance
      );
    }
  }
}
class ShapeMarkersHelper extends BaseHelper {
  mode = "shape_markers";
  pinEnabled = this.gm.options.controls.helper.pin?.active || false;
  previousPosition = null;
  activeMarker = null;
  activeFeatureData = null;
  linkedFeatures = [];
  sharedMarkers = [];
  allowedShapes = ["circle", "line", "rectangle", "polygon", "ellipse"];
  edgeMarkersAllowed = false;
  edgeMarkerAllowedShapes = ["line", "rectangle", "polygon"];
  shapeMarkerAllowedModes = ["drag", "change", "cut", "split"];
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:draw`]: this.handleGmDraw.bind(this),
    [`${GM_SYSTEM_PREFIX}:edit`]: this.handleGmEdit.bind(this),
    mousedown: this.onMouseDown.bind(this),
    touchstart: this.onMouseDown.bind(this),
    mouseup: this.onMouseUp.bind(this),
    touchend: this.onMouseUp.bind(this),
    mousemove: this.onMouseMove.bind(this),
    touchmove: this.onMouseMove.bind(this),
    contextmenu: this.onMouseRightButtonClick.bind(this)
  };
  throttledMethods = convertToThrottled(
    {
      sendMarkerMoveEvent: this.sendMarkerMoveEvent,
      sendMarkerRightClickEvent: this.sendMarkerRightClickEvent
    },
    this,
    this.gm.options.settings.throttlingDelay
  );
  debouncedMethods = convertToDebounced(
    {
      refreshMarkers: this.refreshMarkers
    },
    this,
    this.gm.options.settings.throttlingDelay * 10
  );
  get pinHelperInstance() {
    if (!this.pinEnabled) {
      return null;
    }
    return Object.values(this.gm.actionInstances).find(isPinHelper) || null;
  }
  onStartAction() {
    if (this.isShapeMarkerAllowed()) {
      this.gm.markerPointer.enable({ invisibleMarker: true });
    }
    this.edgeMarkersAllowed = this.gm.getActiveEditModes().includes("change");
    this.gm.features.updateManager.beginTransaction("transactional-set", SOURCES.internal);
    this.addMarkers();
    this.gm.features.updateManager.commitTransaction(SOURCES.internal);
  }
  onEndAction() {
    this.gm.markerPointer.disable();
    this.gm.features.updateManager.beginTransaction("transactional-set", SOURCES.internal);
    this.removeMarkers();
    this.gm.features.updateManager.commitTransaction(SOURCES.internal);
  }
  setPin(enabled) {
    this.pinEnabled = enabled;
  }
  onMouseDown(event) {
    const allowedEventNames = ["mousedown", "touchstart"];
    if (!isMapPointerEvent(event, { warning: true }) || !allowedEventNames.includes(event.type) || isPointerEventWithModifiers(event)) {
      return { next: true };
    }
    if (event.type === "mousedown" && event.originalEvent.button !== 0) {
      return { next: true };
    }
    const featureMarkerData = this.getFeatureMarkerByMouseEvent(event);
    this.activeMarker = featureMarkerData || null;
    this.activeFeatureData = featureMarkerData?.instance.parent || null;
    if (this.activeFeatureData) {
      if (!this.gm.features.selection.has(this.activeFeatureData.id)) {
        this.gm.features.setSelection([this.activeFeatureData.id], true);
      }
      const linkedFeatures = this.gm.features.getLinkedFeatures(this.activeFeatureData);
      if (linkedFeatures.some((f) => f.getShapeProperty("disableEdit") === true)) {
        return { next: true };
      }
      this.linkedFeatures = linkedFeatures;
    }
    if (!(this.activeMarker && this.activeFeatureData)) {
      return { next: true };
    }
    this.previousPosition = getFeatureFirstPoint(this.activeMarker.instance);
    this.gm.mapAdapter.setDragPan(false);
    if (this.activeMarker.type === "edge") {
      this.sendMarkerEvent("edge_marker_click", this.activeFeatureData, this.activeMarker);
    }
    if (this.pinEnabled && this.pinHelperInstance) {
      this.sharedMarkers = this.pinHelperInstance.getSharedMarkers(
        this.activeMarker.position.coordinate
      );
      this.sharedMarkers.forEach(
        (sharedMarker) => this.snappingHelper?.addExcludedFeature(sharedMarker.featureData)
      );
    } else {
      [this.activeFeatureData, ...this.linkedFeatures].map((featureData) => {
        this.snappingHelper?.addExcludedFeature(featureData);
      });
    }
    this.sendMarkerEvent(
      "marker_captured",
      this.activeFeatureData,
      this.activeMarker,
      this.linkedFeatures
    );
    return { next: false };
  }
  onMouseUp() {
    if (!this.activeMarker) {
      return { next: true };
    }
    const eventData = {
      featureData: this.activeFeatureData,
      markerData: this.activeMarker,
      linkedFeatures: this.linkedFeatures
    };
    this.activeMarker = null;
    this.activeFeatureData = null;
    this.sharedMarkers = [];
    this.linkedFeatures = [];
    this.snappingHelper?.clearExcludedFeatures();
    this.previousPosition = null;
    this.gm.mapAdapter.setDragPan(true);
    if (eventData.featureData && eventData.markerData) {
      this.sendMarkerEvent(
        "marker_released",
        eventData.featureData,
        eventData.markerData,
        eventData.linkedFeatures
      );
      return { next: false };
    } else {
      log.debug("ShapeMarkersHelper.onMouseUp: no active marker or featureData", eventData);
    }
    return { next: true };
  }
  onMouseMove(event) {
    if (!this.activeMarker || !isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    this.throttledMethods.sendMarkerMoveEvent(event);
    return { next: false };
  }
  onMouseRightButtonClick(event) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return { next: true };
    }
    const featureMarkerData = this.getFeatureMarkerByMouseEvent(event);
    if (featureMarkerData && featureMarkerData.instance.parent) {
      this.throttledMethods.sendMarkerRightClickEvent(
        featureMarkerData.instance.parent,
        featureMarkerData
      );
      return { next: false };
    }
    return { next: true };
  }
  isShapeMarkerAllowed() {
    return intersection$1(this.shapeMarkerAllowedModes, this.gm.getActiveEditModes()).length > 0;
  }
  convertToVertexMarker(markerData) {
    if (markerData.type === "edge" && markerData.instance.parent) {
      const oldPosition = markerData.position;
      const featureData = markerData.instance.parent;
      this.removeMarker(markerData);
      const newMarkerData = this.createMarker({
        type: "vertex",
        positionData: oldPosition,
        parentFeature: featureData
      });
      const shapeGeoJson = featureData.getGeoJson();
      const position = findCoordinateWithPath(shapeGeoJson, oldPosition.coordinate);
      if (position) {
        const markerKey = position.path.join(".");
        const existingMarker = featureData.markers.get(markerKey);
        if (existingMarker) {
          this.removeMarker(existingMarker);
        }
        featureData.markers.set(markerKey, newMarkerData);
        return newMarkerData;
      }
    }
    log.error("ShapeMarkersHelper.convertToVertexMarker: invalid marker type", markerData);
    return markerData;
  }
  getFeatureMarkerByMouseEvent(event) {
    const markerFeatureData = this.gm.features.getFeatureByMouseEvent({
      event,
      sourceNames: [SOURCES.internal]
    });
    if (markerFeatureData?.parent?.markers) {
      const markerData = findInCollection(
        markerFeatureData.parent.markers,
        (item) => item.instance === markerFeatureData
      );
      if (markerData?.type !== "dom") {
        return markerData;
      }
    }
    return null;
  }
  addMarkers() {
    this.gm.features.forEach((featureData) => {
      if (!featureData || !this.allowedShapes.includes(featureData.shape) || featureData.getShapeProperty("disableEdit") === true) {
        return;
      }
      this.addCenterMarker(featureData);
      const shapeSegments = this.getAllShapeSegments(featureData);
      const endMarkerIndexes = this.getEndMarkerIndexes(featureData);
      shapeSegments.forEach((segmentData, index2) => {
        const isVertexMarkerAllowed = this.isMarkerIndexAllowed(
          featureData.shape,
          index2,
          shapeSegments.length
        );
        if (isVertexMarkerAllowed) {
          const marker2 = this.createOrUpdateVertexMarker(segmentData.segment.start, featureData);
          featureData.markers.set(marker2.markerKey, marker2.markerData);
          if (endMarkerIndexes.has(index2)) {
            const lineEndMarker = this.createOrUpdateVertexMarker(
              segmentData.segment.end,
              featureData
            );
            featureData.markers.set(lineEndMarker.markerKey, lineEndMarker.markerData);
          }
        }
        if (this.isEdgeMarkerAllowed(featureData)) {
          const marker2 = this.createOrUpdateEdgeMarker(segmentData, featureData);
          featureData.markers.set(marker2.markerKey, marker2.markerData);
        }
      });
    });
  }
  addCenterMarker(featureData) {
    const shapeCenter = featureData.getShapeProperty("center");
    if (shapeCenter) {
      const markerData = this.createMarker({
        type: "center",
        positionData: {
          path: [],
          coordinate: shapeCenter
        },
        parentFeature: featureData
      });
      featureData.markers.set("center", markerData);
    }
  }
  getAllShapeSegments(featureData) {
    const shapeGeoJson = featureData.getGeoJson();
    const segmentsData = [];
    eachSegmentWithPath(shapeGeoJson, (segment, index2) => {
      segmentsData.push({
        segment,
        middle: this.getSegmentMiddlePosition(segment),
        edgeMarkerKey: this.getEdgeMarkerKey(index2)
      });
    });
    return segmentsData;
  }
  isEdgeMarkerAllowed(featureData) {
    return this.edgeMarkersAllowed && this.edgeMarkerAllowedShapes.includes(featureData.shape);
  }
  isMarkerIndexAllowed(shape, markerIndex, verticesCount) {
    const divider = Math.floor(verticesCount / 4);
    if (shape === "circle") {
      return (markerIndex + divider / 2) % divider === 0;
    } else if (shape === "ellipse") {
      return markerIndex % divider === 0;
    } else {
      return true;
    }
  }
  getEdgeMarkerKey(index2) {
    return `edge.${index2}`;
  }
  getEndMarkerIndexes(featureData) {
    const geometry = featureData.getGeoJson().geometry;
    if (featureData.shape !== "line" || !["LineString", "MultiLineString"].includes(geometry.type)) {
      return /* @__PURE__ */ new Set();
    }
    if (geometry.type === "MultiLineString") {
      const infos = geometry.coordinates.reduce(
        (acc, line2) => {
          acc.indexes.add(acc.sum + line2.length - 2);
          acc.sum += line2.length - 1;
          return acc;
        },
        { sum: 0, indexes: /* @__PURE__ */ new Set() }
      );
      return infos.indexes;
    }
    return /* @__PURE__ */ new Set([geometry.coordinates.length - 2]);
  }
  getSegmentMiddlePosition(segment) {
    const startPoint = this.gm.mapAdapter.project(segment.start.coordinate);
    const endPoint = this.gm.mapAdapter.project(segment.end.coordinate);
    const middlePoint = [
      (startPoint[0] + endPoint[0]) / 2,
      (startPoint[1] + endPoint[1]) / 2
    ];
    const middlePath = segment.start.path.slice(0, segment.start.path.length - 1).concat([-1]);
    return {
      coordinate: this.gm.mapAdapter.unproject(middlePoint),
      path: middlePath
    };
  }
  removeMarkers() {
    this.gm.features.forEach((_, featureId2) => {
      const featureData = this.gm.features.get(SOURCES.main, featureId2);
      if (featureData) {
        featureData.markers.forEach((marker2) => {
          if (marker2.type !== "dom") {
            this.gm.features.delete(marker2.instance);
          } else {
            log.error("Non a FeatureData marker", marker2);
          }
        });
        featureData.markers = /* @__PURE__ */ new Map();
      }
    });
  }
  removeMarker(markerData) {
    if (markerData.type === "dom") {
      log.error("Wrong marker type", markerData);
      return;
    }
    const featureData = markerData.instance.parent;
    if (!featureData) {
      log.error("Missing parent feature data", markerData);
      return;
    }
    try {
      featureData.markers.forEach((markerItem, key) => {
        if (markerItem === markerData) {
          this.gm.features.delete(markerItem.instance);
          featureData.markers.delete(key);
          throw new Error("break");
        }
      });
    } catch {
    }
  }
  handleGmDraw(event) {
    if (!isGmDrawEvent(event)) {
      log.error("ShapeMarkersHelper.handleGmDraw: not a draw event", event);
      return { next: true };
    }
    if (["feature_created", "mode_start"].includes(event.action)) {
      this.debouncedMethods.refreshMarkers();
    }
    return { next: true };
  }
  refreshMarkers() {
    const isEnabled = this.gm.options.isModeEnabled("helper", "shape_markers");
    if (isEnabled) {
      this.gm.features.updateManager.beginTransaction("transactional-set", SOURCES.internal);
      this.removeMarkers();
      this.addMarkers();
      this.gm.features.updateManager.commitTransaction(SOURCES.internal);
    }
  }
  handleGmEdit(event) {
    if (!isGmEditEvent(event)) {
      log.error("ShapeMarkersHelper.handleGmEdit: not an edit event", event);
      return { next: true };
    }
    if (event.action === "feature_updated") {
      this.handleShapeUpdate(event);
    }
    return { next: true };
  }
  handleShapeUpdate(event) {
    const featureData = event.targetFeatures[0];
    if (!featureData) {
      log.error("ShapeMarkersHelper.handleShapeUpdate: no featureData", event);
      return;
    }
    if (this.activeMarker?.type === "edge") {
      this.activeMarker = this.convertToVertexMarker(this.activeMarker);
    }
    const shapeSegments = this.getAllShapeSegments(featureData);
    const currentMarkerKeys = new Set(featureData.markers.keys());
    const endMarkerIndexes = this.getEndMarkerIndexes(featureData);
    shapeSegments.forEach((segmentData, index2) => {
      const isVertexMarkerAllowed = this.isMarkerIndexAllowed(
        featureData.shape,
        index2,
        shapeSegments.length
      );
      if (isVertexMarkerAllowed) {
        const marker2 = this.createOrUpdateVertexMarker(segmentData.segment.start, featureData);
        currentMarkerKeys.delete(marker2.markerKey);
        if (endMarkerIndexes.has(index2)) {
          const lineEndMarker = this.createOrUpdateVertexMarker(
            segmentData.segment.end,
            featureData
          );
          currentMarkerKeys.delete(lineEndMarker.markerKey);
        }
      }
      if (this.isEdgeMarkerAllowed(featureData)) {
        const marker2 = this.createOrUpdateEdgeMarker(segmentData, featureData);
        currentMarkerKeys.delete(marker2.markerKey);
      }
    });
    this.updateCenterMarkerPosition(featureData);
    currentMarkerKeys.delete("center");
    currentMarkerKeys.forEach((markerKey) => {
      const markerData = featureData.markers.get(markerKey);
      if (markerData && markerData.type !== "dom") {
        this.gm.features.delete(markerData.instance);
      } else {
        log.error("Non a FeatureData marker");
      }
      featureData.markers.delete(markerKey);
    });
  }
  createOrUpdateVertexMarker(position, featureData) {
    const markerKey = position.path.join(".");
    let markerData = featureData.markers.get(markerKey) || null;
    if (markerData && markerData?.type !== "vertex") {
      throw new Error(`Invalid marker type "${markerData?.type}" for edge marker`);
    }
    if (markerData) {
      if (!isEqualPosition(markerData.position.coordinate, position.coordinate)) {
        this.gm.features.updateMarkerFeaturePosition(markerData.instance, position.coordinate);
      }
      markerData.position = position;
    } else {
      markerData = this.createMarker({
        type: "vertex",
        positionData: position,
        parentFeature: featureData
      });
      featureData.markers.set(markerKey, markerData);
    }
    return { markerKey, markerData };
  }
  createOrUpdateEdgeMarker(segmentData, featureData) {
    let markerData = featureData.markers.get(segmentData.edgeMarkerKey) || null;
    if (markerData && markerData?.type !== "edge") {
      throw new Error(`Invalid marker type "${markerData?.type}" for edge marker`);
    }
    if (markerData) {
      if (!isEqualPosition(markerData.position.coordinate, segmentData.middle.coordinate)) {
        markerData.instance.updateGeoJsonGeometry({
          type: "Point",
          coordinates: segmentData.middle.coordinate
        });
      }
      markerData.position = segmentData.middle;
      markerData.segment = segmentData.segment;
    } else {
      markerData = this.createMarker({
        type: "edge",
        positionData: segmentData.middle,
        segment: segmentData.segment,
        parentFeature: featureData
      });
      featureData.markers.set(segmentData.edgeMarkerKey, markerData);
    }
    return { markerKey: segmentData.edgeMarkerKey, markerData };
  }
  updateCenterMarkerPosition(featureData) {
    const markerData = featureData.markers.get("center") || null;
    const shapeCenter = featureData.getShapeProperty("center");
    if (markerData && markerData.type !== "dom" && shapeCenter) {
      markerData.instance.updateGeoJsonGeometry({
        type: "Point",
        coordinates: shapeCenter
      });
      markerData.position.coordinate = shapeCenter;
    }
  }
  sendMarkerEvent(action, featureData, markerData, linkedFeatures = []) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:marker`,
      level: "system",
      actionType: "edit",
      mode: "change",
      action,
      featureData,
      markerData,
      linkedFeatures
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
  sendMarkerRightClickEvent(featureData, markerData) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:marker`,
      level: "system",
      actionType: "edit",
      mode: "change",
      action: "marker_right_click",
      featureData,
      markerData
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
  sendMarkerMoveEvent(event) {
    const markerLngLat = this.gm.markerPointer.marker?.getLngLat() || event.lngLat.toArray();
    if (this.activeMarker && this.activeFeatureData) {
      const targetMarkers = this.pinEnabled ? this.sharedMarkers : [
        {
          markerData: this.activeMarker,
          featureData: this.activeFeatureData
        }
      ];
      targetMarkers.forEach((item) => {
        if (this.previousPosition) {
          const payload = {
            name: `${GM_SYSTEM_PREFIX}:edit:marker_move`,
            level: "system",
            actionType: "edit",
            mode: "drag",
            action: "marker_move",
            featureData: item.featureData,
            markerData: item.markerData,
            lngLatStart: this.previousPosition,
            lngLatEnd: markerLngLat,
            linkedFeatures: item.featureData === this.activeFeatureData ? this.linkedFeatures : []
          };
          this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
        }
      });
    }
    this.previousPosition = markerLngLat;
  }
  createMarker({
    type,
    segment,
    positionData,
    parentFeature
  }) {
    const coordinate = positionData.coordinate;
    const featureData = this.gm.features.createMarkerFeature({
      sourceName: SOURCES.internal,
      parentFeature,
      type,
      coordinate
    });
    if (!featureData) {
      throw new Error(`Missine feature data for the "${type}" marker`);
    }
    if (type === "edge" && segment) {
      return {
        type,
        instance: featureData,
        position: cloneDeep(positionData),
        segment
      };
    } else if (type === "vertex" || type === "center") {
      return {
        type,
        instance: featureData,
        position: cloneDeep(positionData)
      };
    } else {
      throw new Error(`Invalid marker type "${type}" with segment: ${segment}`);
    }
  }
}
class SnappingHelper extends BaseHelper {
  mode = "snapping";
  tolerance = 18;
  lineSnappingShapes = [
    "circle",
    "line",
    "rectangle",
    "polygon",
    "snap_guide"
  ];
  eventHandlers = {};
  shapeSnappingHandlers = {
    marker: this.getPointsSnapping.bind(this),
    circle: this.getLineSnapping.bind(this),
    circle_marker: this.getPointsSnapping.bind(this),
    text_marker: this.getPointsSnapping.bind(this),
    line: this.getLineSnapping.bind(this),
    rectangle: this.getLineSnapping.bind(this),
    polygon: this.getLineSnapping.bind(this),
    snap_guide: this.getLineSnapping.bind(this)
  };
  excludedFeature = /* @__PURE__ */ new Set();
  customSnappingLngLats = /* @__PURE__ */ new Map();
  customSnappingFeatures = /* @__PURE__ */ new Set();
  onStartAction() {
    this.gm.markerPointer.setSnapping(true);
  }
  onEndAction() {
    this.gm.markerPointer.setSnapping(false);
  }
  addExcludedFeature(featureData) {
    this.excludedFeature.add(featureData);
  }
  clearExcludedFeatures() {
    this.excludedFeature.clear();
  }
  addCustomSnappingFeature(featureData) {
    this.customSnappingFeatures.add(featureData);
  }
  removeCustomSnappingFeature(featureData) {
    this.customSnappingFeatures.delete(featureData);
  }
  clearCustomSnappingFeature() {
    this.customSnappingFeatures.clear();
  }
  setCustomSnappingCoordinates(sectionKey, lngLats) {
    this.customSnappingLngLats.set(sectionKey, lngLats);
  }
  clearCustomSnappingCoordinates(sectionKey) {
    this.customSnappingLngLats.delete(sectionKey);
  }
  getSnappedLngLat(lngLat, point2) {
    let snappingLngLat = this.getCustomLngLatsSnapping(point2);
    if (snappingLngLat) {
      return snappingLngLat;
    }
    const features = this.getFeaturesInPointBounds(point2).filter(
      (featureData) => !this.excludedFeature.has(featureData)
    );
    snappingLngLat = this.getFeaturePointsSnapping(features, lngLat, point2);
    if (snappingLngLat) {
      return snappingLngLat;
    }
    snappingLngLat = this.getFeatureLinesSnapping(features, lngLat, point2);
    if (snappingLngLat) {
      return snappingLngLat;
    }
    return lngLat;
  }
  getCustomLngLatsSnapping(point2) {
    const closestPointData = {
      distance: Infinity,
      lngLat: null
    };
    this.customSnappingLngLats.forEach((coordinates) => {
      coordinates.forEach((customLngLat) => {
        const coordPoint = this.gm.mapAdapter.project(customLngLat);
        const distance2 = getEuclideanDistance(point2, coordPoint);
        if (distance2 < this.tolerance && distance2 < closestPointData.distance) {
          closestPointData.distance = distance2;
          closestPointData.lngLat = customLngLat;
        }
      });
    });
    return closestPointData.lngLat;
  }
  getFeaturePointsSnapping(features, lngLat, point2) {
    let featuresPointsSnapping = features.map((featureData) => ({
      shape: featureData.shape,
      ...this.getPointsSnapping(featureData, lngLat, point2)
    })).filter((item) => item.distance < this.tolerance);
    if (featuresPointsSnapping.length) {
      featuresPointsSnapping = sortBy(featuresPointsSnapping, ["distance"]);
      return featuresPointsSnapping[0].lngLat;
    }
    return null;
  }
  getFeatureLinesSnapping(features, lngLat, point2) {
    let featuresLineSnapping = features.filter((featureData) => this.lineSnappingShapes.includes(featureData.shape)).map((featureData) => {
      const snappingHandler = this.shapeSnappingHandlers[featureData.shape];
      if (snappingHandler) {
        return {
          shape: featureData.shape,
          ...snappingHandler(featureData, lngLat, point2)
        };
      }
      return null;
    }).filter((item) => item !== null && item.distance < this.tolerance);
    if (featuresLineSnapping.length) {
      featuresLineSnapping = sortBy(featuresLineSnapping, ["distance"]);
      return featuresLineSnapping[0].lngLat;
    }
    return null;
  }
  getFeaturesInPointBounds(point2) {
    const bounds = [
      [point2[0] - this.tolerance, point2[1] - this.tolerance],
      [point2[0] + this.tolerance, point2[1] + this.tolerance]
    ];
    return this.gm.features.getFeaturesByScreenBounds({ bounds, sourceNames: [SOURCES.main, SOURCES.temporary] }).filter((featureData) => {
      return featureData.temporary ? this.customSnappingFeatures.has(featureData) : true;
    }) || [];
  }
  getPointsSnapping(featureData, lngLat, point2) {
    const shapeGeoJson = featureData.getGeoJson();
    const closestPointData = {
      distance: Infinity,
      coord: null
      // lngLat coords
    };
    eachCoordinateWithPath(
      shapeGeoJson,
      (position) => {
        const coordPoint = this.gm.mapAdapter.project(position.coordinate);
        const distance2 = getEuclideanDistance(point2, coordPoint);
        if (distance2 < this.tolerance && distance2 < closestPointData.distance) {
          closestPointData.distance = distance2;
          closestPointData.coord = position.coordinate;
        }
      },
      true
    );
    return {
      lngLat: closestPointData.coord ? closestPointData.coord : lngLat,
      distance: closestPointData.distance
    };
  }
  getLineSnapping(featureData, lngLat, point2) {
    const shapeGeoJson = featureData.getGeoJson();
    return this.getNearestLinePointData(shapeGeoJson, lngLat, point2);
  }
  getNearestLinePointData(lineGeoJson, lngLat, point2) {
    const result = {
      lngLat,
      distance: Infinity
    };
    const nearestPointLngLat = this.gm.mapAdapter.getEuclideanNearestLngLat(lineGeoJson, lngLat);
    const nearestPointCoord = this.gm.mapAdapter.project(nearestPointLngLat);
    result.distance = getEuclideanDistance(nearestPointCoord, point2);
    if (result.distance < this.tolerance) {
      result.lngLat = nearestPointLngLat;
    }
    return result;
  }
}
class ZoomToFeaturesHelper extends BaseHelper {
  mode = "zoom_to_features";
  eventHandlers = {};
  onStartAction() {
    this.fitMapToFeatures();
    setTimeout(() => {
      this.gm.options.disableMode("helper", "zoom_to_features");
    });
  }
  onEndAction() {
  }
  fitMapToFeatures() {
    const featureCollection2 = this.gm.features.asGeoJsonFeatureCollection({
      sourceNames: [SOURCES.main, ...IS_PRO ? [SOURCES.standby] : []]
    });
    const bboxArray = index_default$g(featureCollection2);
    const bounds = [
      [bboxArray[0], bboxArray[1]],
      [bboxArray[2], bboxArray[3]]
    ];
    try {
      this.gm.mapAdapter.fitBounds(bounds, { padding: 20 });
    } catch {
      log.warn("Wrong bounds for zooming to features", bounds, featureCollection2);
    }
  }
}
const helperClassMap = {
  shape_markers: ShapeMarkersHelper,
  pin: null,
  snapping: SnappingHelper,
  snap_guides: null,
  measurements: null,
  auto_trace: null,
  geofencing: null,
  zoom_to_features: ZoomToFeaturesHelper,
  click_to_edit: null
};
const createHelperInstance = (gm, mode) => {
  if (helperClassMap[mode]) {
    return new helperClassMap[mode](gm);
  }
  log.error(`Helper "${mode}" is not available`);
  return null;
};
class HelperEventListener extends BaseEventListener {
  eventHandlers = {
    [`${GM_SYSTEM_PREFIX}:helper`]: this.handleHelperEvent.bind(this)
  };
  constructor(gm, bus) {
    super(gm);
    bus.attachEvents(this.eventHandlers);
  }
  handleHelperEvent(payload) {
    if (!isGmHelperEvent(payload)) {
      return { next: true };
    }
    const actionInstanceKey = `${payload.actionType}__${payload.mode}`;
    if (payload.action === "mode_start") {
      this.trackExclusiveModes(payload);
      this.start(actionInstanceKey, payload);
      this.trackRelatedModes(payload);
    } else if (payload.action === "mode_end") {
      this.trackRelatedModes(payload);
      this.end(actionInstanceKey);
    }
    return { next: true };
  }
  start(actionInstanceKey, payload) {
    const actionInstance = createHelperInstance(this.gm, payload.mode);
    if (!actionInstance) {
      return;
    }
    if (actionInstanceKey in this.gm.actionInstances) {
      log.error(`Action instance "${actionInstanceKey}" already exists`);
    }
    this.gm.actionInstances[actionInstanceKey] = actionInstance;
    actionInstance.startAction();
  }
  end(actionInstanceKey) {
    const actionInstance = this.gm.actionInstances[actionInstanceKey];
    if (actionInstance instanceof BaseHelper) {
      actionInstance.endAction();
      delete this.gm.actionInstances[actionInstanceKey];
    } else {
      console.error(`Wrong action instance for edit event "${actionInstanceKey}":`, actionInstance);
    }
  }
}
class GmEvents {
  gm;
  bus;
  listeners = {};
  constructor(gm) {
    this.gm = gm;
    this.bus = new EventBus(this.gm);
    this.listeners = {
      draw: new DrawEventListener(this.gm, this.bus),
      edit: new EditEventListener(this.gm, this.bus),
      helper: new HelperEventListener(this.gm, this.bus),
      control: new ControlEventListener(this.gm, this.bus)
    };
  }
  fire(eventName, payload) {
    if (!this.listeners[payload.actionType]) {
      log.error(`Can't find event listener for "${payload.actionType}" event type`);
    }
    this.bus.fireEvent(eventName, payload);
  }
}
class SourceUpdateManager {
  gm;
  updateStorage;
  delayedSourceUpdateMethods;
  // Track pending update promises per source to allow waiting for MapLibre to commit data
  // Using an array to track multiple concurrent promises (prevents overwriting if rapid updates occur)
  pendingUpdatePromises;
  constructor(gm) {
    this.gm = gm;
    this.pendingUpdatePromises = {};
    this.updateStorage = Object.fromEntries(
      typedValues(SOURCES).map((name) => [name, { diff: null, method: "automatic" }])
    );
    this.delayedSourceUpdateMethods = Object.fromEntries(
      typedValues(SOURCES).map((sourceName) => [
        sourceName,
        throttle(
          () => this.updateSourceActual(sourceName),
          this.gm.options.settings.throttlingDelay
        )
      ])
    );
  }
  beginTransaction(method, sourceName) {
    const sourceNames = sourceName ? [sourceName] : Object.values(SOURCES);
    sourceNames.forEach((sourceName2) => {
      this.updateStorage[sourceName2].method = method;
    });
  }
  updatesPending(sourceName) {
    return this.updateStorage[sourceName].diff !== null || !!(this.pendingUpdatePromises[sourceName]?.length ?? 0);
  }
  getFeatureId(feature2) {
    const id = feature2.properties?.[FEATURE_ID_PROPERTY] ?? feature2.id;
    if (id === null || id === void 0) {
      console.warn("Feature id is null or undefined", feature2);
    }
    return id;
  }
  updateSource({
    sourceName,
    diff
  }) {
    if (diff) {
      if (this.updateStorage[sourceName].method === "transactional-set") {
        if (!this.updateStorage[sourceName].diff) {
          this.updateStorage[sourceName].diff = {};
        }
        if (diff.update) {
          console.error("In transactional-set updates are not allowed");
        }
        this.mergeGeoJsonDiff(this.updateStorage[sourceName].diff, diff);
      } else {
        if (!this.updateStorage[sourceName].diff) {
          this.updateStorage[sourceName].diff = {};
        }
        this.mergeGeoJsonDiff(this.updateStorage[sourceName].diff, diff);
      }
    }
    if (this.updateStorage[sourceName].method === "automatic") {
      this.delayedSourceUpdateMethods[sourceName]();
    }
  }
  updateSourceActual(sourceName) {
    const source2 = this.gm.features.sources[sourceName];
    if (this.updateStorage[sourceName].method === "automatic" && source2) {
      if (!source2.loaded) {
        setTimeout(() => {
          this.updateSourceActual(sourceName);
        }, this.gm.options.settings.throttlingDelay);
        return;
      }
      if (this.updateStorage[sourceName].diff) {
        const updatePromise = source2.updateData(this.updateStorage[sourceName].diff);
        this.addPendingPromise(sourceName, updatePromise);
        this.updateStorage[sourceName].diff = null;
      }
    }
  }
  /**
   * Add a pending promise to the tracking array for a source.
   * Automatically removes the promise from the array when it resolves.
   */
  addPendingPromise(sourceName, promise) {
    if (!this.pendingUpdatePromises[sourceName]) {
      this.pendingUpdatePromises[sourceName] = [];
    }
    this.pendingUpdatePromises[sourceName].push(promise);
    promise.finally(() => {
      const promises = this.pendingUpdatePromises[sourceName];
      if (promises) {
        const idx = promises.indexOf(promise);
        if (idx !== -1) {
          promises.splice(idx, 1);
        }
        if (promises.length === 0) {
          delete this.pendingUpdatePromises[sourceName];
        }
      }
    });
  }
  /**
   * Wait for any pending MapLibre source updates to complete.
   * This ensures data is committed before events are fired.
   *
   * When there are queued updates in updateStorage that haven't been processed yet
   * (due to throttling), this method flushes them immediately and waits for completion.
   *
   * Note: We call updateData() directly here rather than going through updateSourceActual()
   * because updateSourceActual() checks `!source.loaded` and may delay processing.
   * When waiting for pending updates (e.g., for event handlers), we need immediate processing.
   *
   * This is safe and won't cause duplicates because getCombinedDiff() atomically drains
   * the storage - whoever calls it first gets the diffs, subsequent calls get null.
   *
   * IMPORTANT: MapLibre's _updateWorkerData() has a guard that returns early if already
   * updating (`if (this._isUpdatingWorker) return`). This means updateData() can return
   * a promise that resolves before the data is actually committed to serialize().
   * To handle this, we loop until both storage and pending promises are empty, with
   * a microtask yield between iterations to allow MapLibre's recursive updates to run.
   */
  async waitForPendingUpdates(sourceName) {
    const source2 = this.gm.features.sources[sourceName];
    if (!source2) return;
    while (this.updateStorage[sourceName] || this.pendingUpdatePromises[sourceName]?.length) {
      if (this.updateStorage[sourceName].diff) {
        const updatePromise = source2.updateData(this.updateStorage[sourceName].diff);
        this.addPendingPromise(sourceName, updatePromise);
      }
      const pendingPromises = this.pendingUpdatePromises[sourceName];
      if (pendingPromises?.length) {
        await Promise.all(pendingPromises);
      }
      await new Promise((resolve) => setTimeout(resolve, 0));
    }
    await new Promise((resolve) => requestAnimationFrame(resolve));
  }
  commitTransaction(sourceName) {
    const sourceNames = sourceName ? [sourceName] : Object.values(SOURCES);
    sourceNames.forEach((sourceName2) => {
      const source2 = this.gm.features.sources[sourceName2];
      if (!source2 || !this.updateStorage[sourceName2].diff) {
        return;
      }
      if (this.updateStorage[sourceName2].method === "transactional-set") {
        const features = this.updateStorage[sourceName2].diff.add?.values() ?? [];
        source2.setData({
          type: "FeatureCollection",
          features: Array.from(features)
        });
      } else if (this.updateStorage[sourceName2].method === "transactional-update") {
        source2.updateData(this.updateStorage[sourceName2].diff);
      }
      this.updateStorage[sourceName2].diff = null;
      this.updateStorage[sourceName2].method = "automatic";
    });
  }
  mergeGeoJsonDiff(prev, next) {
    SourceUpdateManager.resolveMergeConflicts(prev, next);
    if (prev.removeAll || next.removeAll) prev.removeAll = true;
    if (next.remove && !prev.remove) {
      prev.remove = /* @__PURE__ */ new Set();
    }
    next.remove?.forEach((value) => {
      prev.remove?.add(value);
    });
    if (next.add && !prev.add) {
      prev.add = /* @__PURE__ */ new Map();
    }
    next.add?.forEach((value, key) => {
      prev.add?.set(key, value);
    });
    if (next.update && !prev.update) {
      prev.update = /* @__PURE__ */ new Map();
    }
    next.update?.forEach((value, key) => {
      prev.update?.set(key, value);
    });
    if (prev.remove?.size && prev.add?.size) {
      for (const id of prev.add.keys()) {
        prev.remove.delete(id);
      }
    }
  }
  /**
   * Resolve merge conflicts between two GeoJSONSourceDiffs considering the ordering above (remove/add/update).
   *
   * - If you `removeAll` and then `add` features in the same diff, the added features will be kept.
   * - Updates only apply to features that exist after removes and adds have been processed.
   */
  static resolveMergeConflicts(prev, next) {
    if (next.removeAll) {
      prev.add?.clear();
      prev.update?.clear();
      prev.remove?.clear();
      next.remove?.clear();
    }
    if (next.remove) {
      for (const id of next.remove) {
        prev.add?.delete(id);
        prev.update?.delete(id);
      }
    }
    if (next.update) {
      for (const [id, nextUpdate] of next.update) {
        const prevUpdate = prev.update?.get(id);
        if (!prevUpdate) continue;
        next.update?.set(id, SourceUpdateManager.mergeFeatureDiffs(prevUpdate, nextUpdate));
        prev.update?.delete(id);
      }
    }
  }
  /**
   * Merge two feature diffs for the same feature id, considering the order of operations as specified above (remove, add/update).
   */
  static mergeFeatureDiffs(prev, next) {
    const merged = { id: prev.id };
    if (next.removeAllProperties) {
      delete prev.removeProperties;
      delete prev.addOrUpdateProperties;
      delete next.removeProperties;
    }
    if (next.removeProperties) {
      for (const key of next.removeProperties) {
        const index2 = prev.addOrUpdateProperties?.findIndex((prop) => prop.key === key) ?? -1;
        if (index2 > -1) prev.addOrUpdateProperties?.splice(index2, 1);
      }
    }
    if (prev.removeAllProperties || next.removeAllProperties) {
      merged.removeAllProperties = true;
    }
    if (prev.removeProperties || next.removeProperties) {
      merged.removeProperties = [
        ...prev.removeProperties || [],
        ...next.removeProperties || []
      ];
    }
    if (prev.addOrUpdateProperties || next.addOrUpdateProperties) {
      merged.addOrUpdateProperties = [
        ...prev.addOrUpdateProperties || [],
        ...next.addOrUpdateProperties || []
      ];
    }
    if (prev.newGeometry || next.newGeometry) {
      merged.newGeometry = next.newGeometry || prev.newGeometry;
    }
    return merged;
  }
}
class BaseSource {
  isInstanceAvailable() {
    if (this.sourceInstance) {
      return true;
    }
    log.error("Source instance is not available");
    return false;
  }
}
class Features {
  gm;
  featureCounter = 0;
  featureStore = /* @__PURE__ */ new Map();
  featureStoreAllowedSources = [
    SOURCES.main,
    SOURCES.temporary,
    SOURCES.internal
  ];
  selection = /* @__PURE__ */ new Set();
  sources;
  defaultSourceName = SOURCES.main;
  updateManager;
  layers;
  constructor(gm) {
    this.gm = gm;
    this.updateManager = new SourceUpdateManager(gm);
    this.sources = Object.fromEntries(
      typedValues(SOURCES).map((name) => [name, null])
    );
    this.layers = [];
  }
  get forEach() {
    return this.filteredForEach((featureData) => !featureData.temporary);
  }
  get tmpForEach() {
    return this.filteredForEach((featureData) => featureData.temporary);
  }
  init() {
    if (Object.values(this.sources).some((source2) => source2 !== null)) {
      log.warn("features.init(): features are already initialized");
      return;
    }
    typedKeys(this.sources).forEach((sourceName) => {
      this.sources[sourceName] = this.createSource(sourceName);
    });
    this.hydrateFromExistingSources();
    if (this.gm.options.settings.useDefaultLayers) {
      this.layers = this.createLayers();
    }
  }
  /**
   * Hydrates the feature store from existing sources and syncs the ID counter.
   * This is called during init to restore state when remounting on preserved sources.
   */
  hydrateFromExistingSources() {
    let maxCounter = 0;
    typedKeys(this.sources).forEach((sourceName) => {
      const source2 = this.sources[sourceName];
      if (!source2) return;
      try {
        const geoJson = source2.getGeoJson();
        if (geoJson && "features" in geoJson) {
          for (const feature2 of geoJson.features) {
            const featureId2 = feature2.properties?.[FEATURE_ID_PROPERTY];
            if (!featureId2) continue;
            if (typeof featureId2 === "string" && featureId2.startsWith("feature-")) {
              const num = parseInt(featureId2.replace("feature-", ""), 10);
              if (!isNaN(num) && num > maxCounter) {
                maxCounter = num;
              }
            }
            if (this.featureStore.has(featureId2)) continue;
            const shapeGeoJson = feature2;
            const featureData = new FeatureData({
              gm: this.gm,
              id: featureId2,
              parent: null,
              source: source2,
              geoJsonShapeFeature: cloneDeep(shapeGeoJson),
              skipSourceUpdate: true
            });
            this.featureStore.set(featureId2, featureData);
          }
        }
      } catch {
      }
    });
    if (maxCounter > this.featureCounter) {
      this.featureCounter = maxCounter;
    }
  }
  getNewFeatureId(shapeGeoJson) {
    this.featureCounter += 1;
    if (this.gm.options.settings.idGenerator) {
      return this.gm.options.settings.idGenerator(shapeGeoJson);
    }
    let newFeatureId = `feature-${this.featureCounter}`;
    while (this.featureStore.has(newFeatureId)) {
      this.featureCounter += 1;
      newFeatureId = `feature-${this.featureCounter}`;
    }
    return newFeatureId;
  }
  filteredForEach(filterFn) {
    return (callbackfn) => {
      this.featureStore.forEach((featureData, featureId2, featureStore) => {
        if (filterFn(featureData)) {
          callbackfn(featureData, featureId2, featureStore);
        }
      });
    };
  }
  has(sourceName, featureId2) {
    const featureData = this.featureStore.get(featureId2);
    return !!featureData && featureData?.source === this.sources[sourceName];
  }
  get(sourceName, featureId2) {
    const featureData = this.featureStore.get(featureId2) || null;
    if (featureData?.source === this.sources[sourceName]) {
      return featureData;
    }
    return null;
  }
  add(featureData) {
    if (this.featureStore.has(featureData.id)) {
      log.error(`features.add: feature with the id "${featureData.id}" already exists`);
      return;
    }
    if (this.featureStoreAllowedSources.includes(featureData.source.id)) {
      this.featureStore.set(featureData.id, featureData);
    }
  }
  setDefaultSourceName(sourceName) {
    this.defaultSourceName = sourceName;
  }
  setSelection(featureIds, fireEvent = false) {
    this.selection = new Set(featureIds);
    if (fireEvent) {
      this.fireSelectionChangeEvent(featureIds);
    }
  }
  clearSelection() {
    this.selection.clear();
  }
  createSource(sourceName) {
    const source2 = this.gm.mapAdapter.addSource(sourceName, {
      type: "FeatureCollection",
      features: []
    });
    if (source2) {
      return source2;
    }
    throw new Error(`Features: failed to create the source: "${sourceName}"`);
  }
  delete(featureIdOrFeatureData) {
    let featureData;
    if (featureIdOrFeatureData instanceof FeatureData) {
      featureData = featureIdOrFeatureData;
    } else {
      featureData = this.featureStore.get(featureIdOrFeatureData) || null;
    }
    if (featureData) {
      this.featureStore.delete(featureData.id);
      featureData.delete();
    } else {
      log.error(`features.delete: feature "${featureIdOrFeatureData}" not found`);
    }
  }
  deleteAll() {
    this.featureStore.forEach((featureData) => {
      featureData.delete();
    });
    this.featureStore.clear();
  }
  getLinkedFeatures(featureData) {
    const selectionFeatures = Array.from(this.selection).map((featureId2) => this.featureStore.get(featureId2)).filter(isDefined);
    const groupNames = new Set(
      selectionFeatures.map((feature2) => feature2.getShapeProperty("group")).filter(isDefined)
    );
    const groupFeatures = Array.from(this.featureStore.values()).filter((featureData2) => {
      const group = featureData2.getShapeProperty("group");
      return SHAPE_NAMES.includes(featureData2.shape) && group && groupNames.has(group);
    });
    return dedupeById([...selectionFeatures, ...groupFeatures]).filter(
      (f) => f.id !== featureData.id
    );
  }
  getFeatureByMouseEvent({
    event,
    sourceNames
  }) {
    if (!isMapPointerEvent(event, { warning: true })) {
      return null;
    }
    const point2 = [event.point.x, event.point.y];
    const features = this.gm.mapAdapter.queryFeaturesByScreenCoordinates({
      queryCoordinates: point2,
      sourceNames
    });
    return features.length ? features[0] : null;
  }
  getFeaturesByGeoJsonBounds({
    geoJson,
    sourceNames
  }) {
    const coordBounds = getGeoJsonBounds(geoJson);
    const polygonScreenBounds = this.gm.mapAdapter.coordBoundsToScreenBounds(coordBounds);
    return this.getFeaturesByScreenBounds({ bounds: polygonScreenBounds, sourceNames });
  }
  getFeaturesByScreenBounds({
    bounds,
    sourceNames
  }) {
    return this.gm.mapAdapter.queryFeaturesByScreenCoordinates({
      queryCoordinates: bounds,
      sourceNames
    });
  }
  createFeature({
    featureId: featureId2,
    shapeGeoJson,
    parent,
    sourceName,
    imported
  }) {
    const source2 = this.sources[sourceName];
    if (!source2) {
      log.error("Features.createFeature Missing source for feature creation");
      return null;
    }
    const id = featureId2 ?? shapeGeoJson.properties[FEATURE_ID_PROPERTY] ?? this.getNewFeatureId(shapeGeoJson);
    if (this.featureStore.get(id)) {
      log.error(
        `Features.createFeature: feature with the id "${id}" already exists`,
        this.featureStore.get(id)
      );
      return null;
    }
    const featureData = new FeatureData({
      gm: this.gm,
      id,
      parent: parent || null,
      source: source2,
      geoJsonShapeFeature: cloneDeep(shapeGeoJson)
    });
    this.add(featureData);
    if (!featureData.temporary && !imported) {
      this.fireFeatureCreatedEvent(featureData);
    }
    this.featureCounter += 1;
    return featureData;
  }
  importGeoJson(geoJson, options) {
    const opts = options ?? {};
    const features = "features" in geoJson ? geoJson.features : [geoJson];
    const result = {
      stats: {
        total: 0,
        success: 0,
        failed: 0,
        overwritten: 0
      },
      addedFeatures: []
    };
    this.updateManager.beginTransaction("transactional-update", SOURCES.main);
    features.forEach((feature2) => {
      let featureData = null;
      result.stats.total += 1;
      const featureGeoJson = fixGeoJsonFeature(feature2);
      if (featureGeoJson) {
        if (opts.idPropertyName) {
          const customId = getCustomFeatureId(featureGeoJson, opts.idPropertyName);
          if (customId) {
            featureGeoJson.id = customId;
          }
        }
        if (opts.overwrite) {
          const featureId2 = featureGeoJson.id ?? featureGeoJson.properties?.[FEATURE_ID_PROPERTY];
          if (featureId2 && this.featureStore.has(featureId2)) {
            this.delete(featureId2);
            result.stats.overwritten += 1;
          }
        }
        featureData = this.importGeoJsonFeature(featureGeoJson);
      }
      if (featureData) {
        result.addedFeatures.push(featureData);
        result.stats.success += 1;
      } else {
        result.stats.failed += 1;
      }
    });
    this.updateManager.commitTransaction("gm_main");
    return result;
  }
  importGeoJsonFeature(shapeGeoJson) {
    const sourceName = this.defaultSourceName;
    const shape = this.getFeatureShapeByGeoJson(shapeGeoJson);
    if (!shape) {
      log.error("features.addGeoJsonFeature: unknown shape", shape);
      return null;
    }
    return this.createFeature({
      featureId: shapeGeoJson.id,
      shapeGeoJson,
      sourceName,
      imported: true
    });
  }
  getAll() {
    return this.exportGeoJson();
  }
  /**
   * Exports GeoJSON from Geoman's internal state.
   *
   * This is the recommended method for most use cases as it always returns the latest
   * feature data, even during event handlers before MapLibre has committed changes.
   *
   * @param options - Export options
   * @param options.allowedShapes - Filter to only include specific shape types
   * @param options.idPropertyName - Property name to use for feature IDs (default: 'gm_id')
   * @returns GeoJSON FeatureCollection with all features
   *
   * @example
   * // Export all features
   * const geoJson = geoman.features.exportGeoJson();
   *
   * // Export only polygons and circles
   * const shapes = geoman.features.exportGeoJson({ allowedShapes: ['polygon', 'circle'] });
   */
  exportGeoJson({
    allowedShapes,
    idPropertyName
  } = { allowedShapes: void 0 }) {
    return this.asGeoJsonFeatureCollection({
      sourceNames: [SOURCES.main, ...IS_PRO ? [SOURCES.standby] : []],
      shapeTypes: allowedShapes ? allowedShapes : [...SHAPE_NAMES],
      idPropertyName,
      useMapLibreSource: false
    });
  }
  /**
   * Exports GeoJSON directly from MapLibre's underlying source data.
   *
   * This method reads from MapLibre's serialized source state, which may lag slightly
   * behind Geoman's internal state during rapid updates or in event handlers.
   *
   * Use this method when you specifically need to verify what MapLibre has committed
   * to its source, for debugging, or for synchronization with external systems that
   * read directly from MapLibre sources.
   *
   * For most use cases, prefer `exportGeoJson()` which uses Geoman's internal state
   * and is always up-to-date.
   *
   * @param options - Export options
   * @param options.allowedShapes - Filter to only include specific shape types
   * @param options.idPropertyName - Property name to use for feature IDs (default: 'gm_id')
   * @returns GeoJSON FeatureCollection from MapLibre's source
   *
   * @example
   * // Export features as stored in MapLibre source
   * const geoJson = geoman.features.exportGeoJsonFromSource();
   *
   * // Verify MapLibre has committed the data
   * await geoman.features.waitForPendingUpdates();
   * const committed = geoman.features.exportGeoJsonFromSource();
   */
  exportGeoJsonFromSource({
    allowedShapes,
    idPropertyName
  } = { allowedShapes: void 0 }) {
    return this.asGeoJsonFeatureCollection({
      sourceNames: [SOURCES.main, ...IS_PRO ? [SOURCES.standby] : []],
      shapeTypes: allowedShapes ? allowedShapes : [...SHAPE_NAMES],
      idPropertyName,
      useMapLibreSource: true
    });
  }
  asGeoJsonFeatureCollection({
    shapeTypes,
    sourceNames,
    idPropertyName,
    useMapLibreSource = false
  }) {
    const resultFeatureCollection = {
      type: "FeatureCollection",
      features: []
    };
    idPropertyName ??= FEATURE_ID_PROPERTY;
    sourceNames.forEach((sourceName) => {
      const source2 = this.sources[sourceName];
      if (source2) {
        const sourceFeatureCollection = useMapLibreSource ? source2.getGeoJson() : source2.getGmGeoJson();
        sourceFeatureCollection.features.filter((feature2) => !!feature2).forEach((feature2) => {
          const featureData = this.get(sourceName, feature2.id);
          if (!featureData) {
            return;
          }
          const id = feature2.properties[FEATURE_ID_PROPERTY];
          if (idPropertyName !== FEATURE_ID_PROPERTY) {
            feature2.properties[idPropertyName] = id;
            delete feature2.properties[FEATURE_ID_PROPERTY];
          }
          if (shapeTypes === void 0 || shapeTypes.includes(featureData.shape)) {
            resultFeatureCollection.features.push({ ...feature2, id });
          }
        });
      }
    });
    return resultFeatureCollection;
  }
  convertSourceToGm(inputSource) {
    const features = [];
    const shapeGeoJson = inputSource.getGeoJson();
    const sourceGeoJsonFeatures = "features" in shapeGeoJson ? shapeGeoJson.features : [shapeGeoJson];
    const baseSource = this.gm.mapAdapter.getSource(inputSource.id);
    baseSource.remove();
    sourceGeoJsonFeatures.forEach((sourceFeature) => {
      const featureData = this.addGeoJsonFeature({
        shapeGeoJson: sourceFeature,
        defaultSource: true
      });
      if (featureData) {
        features.push(featureData);
      }
    });
    return features;
  }
  addGeoJsonFeature({
    shapeGeoJson,
    sourceName,
    defaultSource
  }) {
    let targetSourceName;
    if (defaultSource) {
      targetSourceName = this.defaultSourceName;
      if (sourceName) {
        log.warn("features.addGeoJsonFeature: default source is set, sourceName is ignored");
      }
    } else {
      targetSourceName = sourceName || null;
    }
    if (!targetSourceName) {
      log.error("features.addGeoJsonFeature: missing sourceName");
      return null;
    }
    const shape = this.getFeatureShapeByGeoJson(shapeGeoJson);
    if (!shape) {
      log.error("features.addGeoJsonFeature: unknown shape", shape);
      return null;
    }
    return this.createFeature({
      featureId: shapeGeoJson.id,
      shapeGeoJson: {
        ...shapeGeoJson,
        properties: { ...shapeGeoJson.properties, shape }
      },
      sourceName: targetSourceName
    });
  }
  createLayers() {
    const layers = [];
    typedKeys(this.sources).forEach((sourceName) => {
      typedKeys(this.gm.options.layerStyles).forEach((shapeName) => {
        const styles2 = this.gm.options.layerStyles[shapeName][sourceName];
        styles2?.forEach((partialStyle) => {
          const layer = this.createGenericLayer({
            sourceName,
            shapeNames: [shapeName],
            partialStyle
          });
          if (layer) {
            layers.push(layer);
          }
        });
      });
    });
    return layers;
  }
  createGenericLayer({
    sourceName,
    shapeNames,
    partialStyle
  }) {
    const layerId = this.getGenericLayerName({ sourceName, shapeNames, partialStyle });
    if (!layerId) {
      throw new Error(`Can't create a layer, for ${{ sourceName, shapeNames, partialStyle }}`);
    }
    const layerOptions = {
      ...partialStyle,
      id: layerId,
      source: sourceName,
      filter: ["in", ["get", `${FEATURE_PROPERTY_PREFIX}shape`], ["literal", shapeNames]]
    };
    return this.gm.mapAdapter.addLayer(layerOptions);
  }
  getGenericLayerName({
    sourceName,
    shapeNames,
    partialStyle
  }) {
    const MAX_LAYERS = 100;
    const shapeName = shapeNames.length === 1 ? shapeNames[0] : "mixed";
    const getLayerId = (index2) => `${sourceName}-${shapeName}__${partialStyle.type}-layer-${index2}`;
    let layerId = null;
    for (let i = 0; i < MAX_LAYERS; i += 1) {
      const tmpLayerId = getLayerId(i);
      if (!this.gm.mapAdapter.getLayer(tmpLayerId)) {
        layerId = tmpLayerId;
        return layerId;
      }
    }
    return null;
  }
  getFeatureShapeByGeoJson(shapeGeoJson) {
    const SHAPE_MAP = {
      Point: "marker",
      LineString: "line",
      MultiLineString: "line",
      Polygon: "polygon",
      MultiPolygon: "polygon"
    };
    const properties = shapeGeoJson.properties;
    if (properties?.shape && SHAPE_NAMES.includes(properties?.shape)) {
      return properties?.shape;
    }
    return SHAPE_MAP[shapeGeoJson.geometry.type] || null;
  }
  createMarkerFeature({
    parentFeature,
    coordinate,
    type,
    sourceName
  }) {
    return this.createFeature({
      sourceName,
      parent: parentFeature,
      shapeGeoJson: {
        type: "Feature",
        geometry: {
          type: "Point",
          coordinates: coordinate
        },
        properties: {
          [`${FEATURE_PROPERTY_PREFIX}shape`]: `${type}_marker`
        }
      }
    });
  }
  updateMarkerFeaturePosition(markerFeatureData, coordinates) {
    markerFeatureData.updateGeoJsonGeometry({
      type: "Point",
      coordinates
    });
  }
  fireFeatureCreatedEvent(featureData) {
    if (includesWithType(featureData.shape, SHAPE_NAMES)) {
      const payload = {
        name: `${GM_SYSTEM_PREFIX}:draw:feature_created`,
        level: "system",
        actionType: "draw",
        mode: featureData.shape,
        action: "feature_created",
        featureData
      };
      this.gm.events.fire(`${GM_SYSTEM_PREFIX}:draw`, payload);
    }
  }
  fireSelectionChangeEvent(selection) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:edit:selection_change`,
      level: "system",
      actionType: "edit",
      action: "selection_change",
      selection,
      mode: null
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:edit`, payload);
  }
}
class BaseMapAdapter {
  getEuclideanNearestLngLat(shapeGeoJson, lngLat) {
    const targetPoint = this.project(lngLat);
    let closestPoint = [0, 0];
    let minDistance = Infinity;
    eachSegmentWithPath(shapeGeoJson, (segment) => {
      const linePoint1 = this.project(segment.start.coordinate);
      const linePoint2 = this.project(segment.end.coordinate);
      const nearestCoordinates = getEuclideanSegmentNearestPoint(
        linePoint1,
        linePoint2,
        targetPoint
      );
      const distance2 = getEuclideanDistance(targetPoint, nearestCoordinates);
      if (distance2 < minDistance) {
        minDistance = distance2;
        closestPoint = [nearestCoordinates[0], nearestCoordinates[1]];
      }
    });
    return this.unproject(closestPoint);
  }
  getDistance(lngLat1, lngLat2) {
    return index_default$a(lngLat1, lngLat2, { units: "meters" });
  }
}
class BaseLayer {
  isInstanceAvailable() {
    if (this.layerInstance) {
      return true;
    }
    log.error("layerInstance is not available");
    return false;
  }
}
class MaplibreLayer extends BaseLayer {
  gm;
  layerInstance = null;
  mapInstance;
  constructor({
    gm,
    layerId,
    options
  }) {
    super();
    this.gm = gm;
    this.mapInstance = this.gm.mapAdapter.mapInstance;
    if (options) {
      this.layerInstance = this.createLayer(options);
    } else {
      this.layerInstance = this.mapInstance.getLayer(layerId) || null;
    }
  }
  get id() {
    if (!this.isInstanceAvailable()) {
      throw new Error("Layer instance is not available");
    }
    return this.layerInstance.id;
  }
  get source() {
    if (!this.isInstanceAvailable()) {
      throw new Error("Layer instance is not available");
    }
    return this.layerInstance.source;
  }
  createLayer(options) {
    let layer = this.mapInstance.getLayer(options.id);
    if (layer) {
      log.warn(`Layer "${options.id}" already exists, skipping`);
    } else {
      this.mapInstance.addLayer(options);
      layer = this.mapInstance.getLayer(options.id);
    }
    return layer ?? null;
  }
  remove() {
    if (this.isInstanceAvailable()) {
      this.mapInstance.removeLayer(this.id);
    }
    this.layerInstance = null;
  }
}
class MaplibreDomMarker extends BaseDomMarker {
  markerInstance;
  constructor({
    mapInstance,
    options,
    lngLat
  }) {
    super();
    this.markerInstance = new ml.Marker(options).setLngLat(lngLat).addTo(mapInstance);
  }
  getElement() {
    if (!this.isMarkerInstanceAvailable()) {
      return null;
    }
    return this.markerInstance?.getElement() || null;
  }
  setLngLat(lngLat) {
    if (!this.isMarkerInstanceAvailable()) {
      return;
    }
    this.markerInstance?.setLngLat(lngLat);
  }
  getLngLat() {
    if (!this.isMarkerInstanceAvailable()) {
      return [0, 0];
    }
    return this.markerInstance?.getLngLat().toArray() || [0, 0];
  }
  remove() {
    this.markerInstance?.remove();
  }
}
class BasePopup {
  isInstanceAvailable() {
    if (this.popupInstance) {
      return true;
    }
    log.error("Popup instance is not available");
    return false;
  }
}
class MaplibrePopup extends BasePopup {
  popupInstance;
  constructor({
    mapInstance,
    options,
    lngLat
  }) {
    super();
    this.popupInstance = new ml.Popup(options).addTo(mapInstance);
    if (lngLat) {
      this.setLngLat(lngLat);
    }
  }
  getLngLat() {
    if (!this.isInstanceAvailable()) {
      return [0, 0];
    }
    return this.popupInstance.getLngLat().toArray() || [0, 0];
  }
  setLngLat(lngLat) {
    if (!this.isInstanceAvailable()) {
      return;
    }
    this.popupInstance.setLngLat(lngLat);
  }
  setHtml(htmlContent) {
    if (this.isInstanceAvailable()) {
      this.popupInstance.setHTML(htmlContent);
    }
  }
  remove() {
    if (this.isInstanceAvailable()) {
      this.popupInstance.remove();
    }
  }
}
class MaplibreSource extends BaseSource {
  gm;
  mapInstance;
  sourceInstance;
  constructor({ gm, geoJson, sourceId }) {
    super();
    this.gm = gm;
    this.mapInstance = this.gm.mapAdapter.mapInstance;
    if (geoJson) {
      this.sourceInstance = this.createSource({ geoJson, sourceId });
    } else {
      this.sourceInstance = this.mapInstance.getSource(sourceId) || null;
    }
  }
  get id() {
    if (!this.isInstanceAvailable()) {
      throw new Error("Source instance is not available");
    }
    return this.sourceInstance.id;
  }
  get loaded() {
    return this.mapInstance.isSourceLoaded(this.id);
  }
  createSource({ geoJson, sourceId }) {
    let source2 = this.mapInstance.getSource(sourceId);
    if (source2) {
      log.warn(`Source "${source2.id}" already exists, skipping`);
    } else {
      this.mapInstance.addSource(sourceId, {
        type: "geojson",
        data: geoJson,
        promoteId: FEATURE_ID_PROPERTY
      });
      source2 = this.mapInstance.getSource(sourceId);
    }
    return source2 ?? null;
  }
  getGeoJson() {
    if (!this.isInstanceAvailable()) {
      throw new Error("Source instance is not available");
    }
    return this.sourceInstance.serialize().data;
  }
  getGmGeoJson() {
    if (!this.isInstanceAvailable()) {
      throw new Error("Source instance is not available");
    }
    const resultFeatureCollection = {
      type: "FeatureCollection",
      features: []
    };
    const sourceForEach = this.gm.features.filteredForEach(
      (featureData) => featureData.source.id === this.sourceInstance?.id
    );
    sourceForEach((featureData) => {
      if (SHAPE_NAMES.includes(featureData.shape)) {
        resultFeatureCollection.features.push(featureData.getGeoJson());
      }
    });
    return resultFeatureCollection;
  }
  async setData(geoJson) {
    if (!this.isInstanceAvailable()) {
      throw new Error("Source instance is not available");
    }
    await this.sourceInstance.setData(geoJson, true);
  }
  async updateData(hashedDiff) {
    if (!this.isInstanceAvailable()) {
      return;
    }
    const mlDiff = MaplibreSource.hashedToDiff(hashedDiff);
    await this.sourceInstance.updateData(mlDiff, true);
  }
  /**
   * @internal
   * Convert a hashed GeoJSONSourceDiff back to the array-based representation
   */
  static hashedToDiff(hashed) {
    const diff = {};
    if (hashed.removeAll) {
      diff.removeAll = hashed.removeAll;
    }
    if (hashed.remove) {
      diff.remove = Array.from(hashed.remove);
    }
    if (hashed.add) {
      diff.add = Array.from(hashed.add.values());
    }
    if (hashed.update) {
      diff.update = Array.from(hashed.update.values());
    }
    return diff;
  }
  remove() {
    if (!this.isInstanceAvailable()) {
      return;
    }
    this.gm.mapAdapter.eachLayer((layer) => {
      if (layer.source === this.sourceInstance.id) {
        this.gm.mapAdapter.removeLayer(layer.id);
      }
    });
    this.mapInstance.removeSource(this.sourceInstance.id);
  }
}
const maplibrePointerEvents = [
  "click",
  "dblclick",
  "mousedown",
  "mouseup",
  "mousemove",
  "mouseenter",
  "mouseleave",
  "mouseover",
  "mouseout",
  "contextmenu",
  "touchstart",
  "touchend",
  "touchcancel"
];
const isMaplibreSupportedPointerEventName = (name) => {
  return maplibrePointerEvents.includes(name);
};
class MaplibreAdapter extends BaseMapAdapter {
  gm;
  mapType = "maplibre";
  mapInstance;
  constructor(map, gm) {
    super();
    this.gm = gm;
    this.mapInstance = map;
  }
  getMapInstance() {
    return this.mapInstance;
  }
  isLoaded() {
    return this.mapInstance._loaded;
  }
  getContainer() {
    return this.mapInstance.getContainer();
  }
  getCanvas() {
    return this.mapInstance.getCanvas();
  }
  addControl(control) {
    this.mapInstance.addControl(control);
  }
  removeControl(control) {
    this.mapInstance.removeControl(control);
  }
  async loadImage({ id, image }) {
    if (!this.mapInstance.hasImage(id)) {
      const loadedImage = await this.mapInstance.loadImage(image);
      this.mapInstance.addImage(id, loadedImage.data);
    }
  }
  removeImage(id) {
    try {
      if (this.mapInstance.hasImage(id)) {
        this.mapInstance.removeImage(id);
      }
    } catch {
    }
  }
  getBounds() {
    const mapBounds = this.mapInstance.getBounds();
    return mapBounds.toArray();
  }
  fitBounds(bounds, options) {
    this.mapInstance.fitBounds(bounds, options);
  }
  setCursor(cursor) {
    this.mapInstance.getCanvas().style.cursor = cursor;
  }
  disableMapInteractions(interactionTypes) {
    interactionTypes.forEach((interactionType) => {
      this.mapInstance[interactionType].disable();
    });
  }
  enableMapInteractions(interactionTypes) {
    interactionTypes.forEach((interactionType) => {
      this.mapInstance[interactionType].enable();
    });
  }
  setDragPan(value) {
    if (value) {
      this.mapInstance.dragPan.enable();
    } else {
      this.mapInstance.dragPan.disable();
    }
  }
  queryFeaturesByScreenCoordinates({
    queryCoordinates = void 0,
    sourceNames
  }) {
    const features = uniqWith(
      this.mapInstance.queryRenderedFeatures(queryCoordinates).map((feature2) => ({
        featureId: feature2.properties[FEATURE_ID_PROPERTY],
        featureSourceName: feature2.source
      })),
      isEqual
    );
    return features.map(({ featureId: featureId2, featureSourceName }) => {
      if (featureId2 === void 0 || !sourceNames.includes(featureSourceName)) {
        return null;
      }
      return this.gm.features.get(featureSourceName, featureId2) || null;
    }).filter((featureData) => !!featureData);
  }
  queryGeoJsonFeatures({
    queryCoordinates = void 0,
    sourceNames
  }) {
    const comparator = (item1, item2) => {
      return item1?.id === item2?.id;
    };
    const features = uniqWith(
      this.mapInstance.queryRenderedFeatures(queryCoordinates).map((feature2) => {
        const geoJson = this.convertToGeoJsonImportFeature(feature2);
        if (!geoJson) {
          return null;
        }
        return {
          id: feature2.properties[FEATURE_ID_PROPERTY],
          sourceName: feature2.source,
          geoJson
        };
      }),
      comparator
    );
    return features.filter((item) => {
      return !!item && item.id !== void 0 && item.geoJson && sourceNames.includes(item.sourceName);
    });
  }
  convertToGeoJsonImportFeature(feature2) {
    const featureId2 = feature2.properties[FEATURE_ID_PROPERTY];
    if (featureId2 === void 0 || feature2.geometry.type === "GeometryCollection") {
      return null;
    }
    return {
      id: featureId2,
      type: "Feature",
      properties: feature2.properties,
      geometry: feature2.geometry
    };
  }
  addSource(sourceId, geoJson) {
    return new MaplibreSource({ gm: this.gm, sourceId, geoJson });
  }
  getSource(sourceId) {
    return new MaplibreSource({ gm: this.gm, sourceId });
  }
  addLayer(options) {
    const layerId = options.id;
    return new MaplibreLayer({ gm: this.gm, layerId, options });
  }
  getLayer(layerId) {
    if (this.mapInstance.getLayer(layerId)) {
      return new MaplibreLayer({ gm: this.gm, layerId });
    }
    return null;
  }
  removeLayer(layerId) {
    const layer = this.getLayer(layerId);
    if (layer) {
      layer.remove();
    }
  }
  eachLayer(callback) {
    this.mapInstance.getStyle().layers.forEach((layer) => {
      callback(new MaplibreLayer({ gm: this.gm, layerId: layer.id }));
    });
  }
  createDomMarker(options, lngLat) {
    return new MaplibreDomMarker({
      mapInstance: this.mapInstance,
      options,
      lngLat
    });
  }
  createPopup(options, lngLat) {
    return new MaplibrePopup({
      mapInstance: this.mapInstance,
      options,
      lngLat
    });
  }
  project(position) {
    const point2 = this.mapInstance.project(position);
    return [point2.x, point2.y];
  }
  unproject(point2) {
    const lngLat = this.mapInstance.unproject(point2);
    return [lngLat.lng, lngLat.lat];
  }
  coordBoundsToScreenBounds(bounds) {
    const mlBounds = new ml.LngLatBounds(bounds);
    const sw = this.project(mlBounds.getSouthWest().toArray());
    const ne = this.project(mlBounds.getNorthEast().toArray());
    return [sw, ne];
  }
  fire(type, data) {
    this.mapInstance.fire(type, data);
  }
  on(type, arg2, listener) {
    if (typeof arg2 === "string" && listener && isMaplibreSupportedPointerEventName(type)) {
      this.mapInstance.on(type, arg2, listener);
    } else if (typeof arg2 === "function") {
      this.mapInstance.on(type, arg2);
    } else {
      throw new Error("Invalid arguments passed to 'on' method");
    }
  }
  once(type, arg2, listener) {
    if (typeof arg2 === "string" && listener && isMaplibreSupportedPointerEventName(type)) {
      this.mapInstance.once(type, arg2, listener);
    } else if (typeof arg2 === "function") {
      this.mapInstance.once(type, arg2);
    } else {
      throw new Error("Invalid arguments passed to 'once' method.");
    }
  }
  off(type, arg2, listener) {
    if (typeof arg2 === "string" && listener && isMaplibreSupportedPointerEventName(type)) {
      this.mapInstance.off(type, arg2, listener);
    } else if (typeof arg2 === "function") {
      this.mapInstance.off(type, arg2);
    } else {
      throw new Error("Invalid arguments passed to 'off' method");
    }
  }
}
const getMapAdapter = (gm, map) => {
  return new MaplibreAdapter(map, gm);
};
const mergeByTypeCustomizer = (objValue, srcValue) => {
  if (!Array.isArray(objValue) || !Array.isArray(srcValue)) {
    return void 0;
  }
  if (srcValue.some((item) => !isPartialLayer(item))) {
    log.warn("Wrong partial layer detected for layer styles");
    return void 0;
  }
  const baseMap2 = keyBy(objValue, "type");
  const countMap = countBy(srcValue, "type");
  if (Object.values(countMap).some((count) => count > 1)) {
    throw new Error(
      'Multiple layers for the same shape are detected. Use "useDefaultLayers: false" and define layers manually.'
    );
  }
  srcValue.forEach((item) => {
    if (baseMap2[item.type]) {
      merge$1(baseMap2[item.type], item);
    } else {
      baseMap2[item.type] = cloneDeep(item);
    }
  });
  return values(baseMap2);
};
class GmOptions {
  gm;
  settings;
  controls;
  layerStyles;
  constructor(gm, inputOptions) {
    this.gm = gm;
    const options = this.getMergedOptions(inputOptions);
    this.settings = options.settings;
    this.controls = options.controls;
    this.layerStyles = options.layerStyles;
  }
  getMergedOptions(options = {}) {
    const defaultOptions2 = getDefaultOptions();
    if (typeof options.settings?.controlsUiEnabledByDefault === "boolean") {
      defaultOptions2.settings.controlsUiEnabledByDefault = options.settings.controlsUiEnabledByDefault;
    }
    trackDefaultUiEnabledState(defaultOptions2);
    return mergeWith(defaultOptions2, options, mergeByTypeCustomizer);
  }
  enableMode(modeType, modeName) {
    const isModeEnabled = this.isModeEnabled(modeType, modeName);
    const isModeAvailable = this.isModeAvailable(modeType, modeName);
    if (!isModeAvailable) {
      log.warn(`Unable to enable mode, "${modeType}:${modeName}" is not available`);
    }
    if (isModeEnabled || !isModeAvailable) {
      return;
    }
    const sectionOptions = this.controls[modeType];
    const controlOptions = sectionOptions[modeName];
    if (controlOptions) {
      controlOptions.active = true;
      this.fireModeEvent(modeType, modeName, "mode_start");
      this.fireControlEvent(modeType, modeName, "on");
      this.fireModeEvent(modeType, modeName, "mode_started");
    } else {
      log.error("Can't find control section for", modeType, modeName);
    }
  }
  disableMode(modeType, modeName) {
    const isModeEnabled = this.isModeEnabled(modeType, modeName);
    const isModeAvailable = this.isModeAvailable(modeType, modeName);
    if (!isModeEnabled || !isModeAvailable) {
      return;
    }
    const sectionOptions = this.controls[modeType];
    const controlOptions = sectionOptions[modeName];
    if (controlOptions) {
      controlOptions.active = false;
      this.fireModeEvent(modeType, modeName, "mode_end");
      this.fireControlEvent(modeType, modeName, "off");
      this.fireModeEvent(modeType, modeName, "mode_ended");
    } else {
      log.error("Can't find control section for", modeType, modeName);
    }
  }
  syncModeState(modeType, modeName) {
    const sectionOptions = this.controls[modeType];
    const controlOptions = sectionOptions[modeName];
    const isModeAvailable = this.isModeAvailable(modeType, modeName);
    if (controlOptions) {
      if (isModeAvailable) {
        if (controlOptions.active) {
          this.enableMode(modeType, modeName);
        } else {
          this.disableMode(modeType, modeName);
        }
      } else {
        console.log(`Not available mode: ${modeType}:${modeName}`);
        controlOptions.active = false;
        controlOptions.uiEnabled = false;
      }
    }
  }
  toggleMode(modeType, modeName) {
    if (this.isModeEnabled(modeType, modeName)) {
      this.disableMode(modeType, modeName);
    } else {
      this.enableMode(modeType, modeName);
    }
  }
  isModeEnabled(actionType, modeName) {
    return !!Object.entries(this.gm.actionInstances).find(([actionName, instance]) => {
      return actionName === `${actionType}__${modeName}` && instance;
    });
  }
  isModeAvailable(actionType, modeName) {
    if (actionType === "draw" && includesWithType(modeName, DRAW_MODES)) {
      return !!this.gm.drawClassMap[modeName];
    } else if (actionType === "edit" && includesWithType(modeName, EDIT_MODES)) {
      return !!this.gm.editClassMap[modeName];
    } else if (actionType === "helper" && includesWithType(modeName, HELPER_MODES)) {
      return !!this.gm.helperClassMap[modeName];
    }
    return false;
  }
  getControlOptions({
    modeType,
    modeName
  }) {
    if (modeType && modeName) {
      const sectionOptions = this.controls[modeType];
      return sectionOptions[modeName] || null;
    }
    return null;
  }
  fireModeEvent(sectionName, modeName, action) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:${modeName}:mode`,
      level: "system",
      actionType: sectionName,
      mode: modeName,
      action
    };
    if (isGmModeEvent(payload)) {
      if (isGmDrawEvent(payload)) {
        this.gm.events.fire(`${GM_SYSTEM_PREFIX}:${sectionName}`, payload);
      } else if (isGmEditEvent(payload)) {
        this.gm.events.fire(`${GM_SYSTEM_PREFIX}:${sectionName}`, payload);
      } else if (isGmHelperEvent(payload)) {
        this.gm.events.fire(`${GM_SYSTEM_PREFIX}:${sectionName}`, payload);
      } else {
        log.warn("Unknown mode event: ", payload);
      }
    }
  }
  fireControlEvent(sectionName, modeName, action) {
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:control:switch`,
      level: "system",
      actionType: "control",
      section: sectionName,
      mode: modeName,
      action
    };
    this.gm.events.fire(`${GM_SYSTEM_PREFIX}:control`, payload);
  }
}
class MarkerPointer {
  gm;
  marker = null;
  tmpMarker = null;
  snapping = false;
  oldSnapping = void 0;
  constructor(gm) {
    this.gm = gm;
    this.initEventHandlers();
  }
  get snappingHelper() {
    return this.gm.actionInstances.helper__snapping || null;
  }
  initEventHandlers() {
    this.throttledMethods = convertToThrottled(
      {
        onMouseMove: this.onMouseMove
      },
      this,
      this.gm.options.settings.throttlingDelay
    );
    this.eventHandlers = {
      mousemove: this.throttledMethods.onMouseMove.bind(this)
    };
  }
  setSnapping(snapping2) {
    if (snapping2 && !this.snappingHelper) {
      log.error("MarkerPointer: snapping is not available");
      return;
    }
    this.snapping = snapping2;
  }
  pauseSnapping() {
    if (this.oldSnapping !== void 0) {
      log.error("MarkerPointer: snapping is already paused");
    }
    this.oldSnapping = this.snapping;
    this.setSnapping(false);
  }
  resumeSnapping() {
    if (this.oldSnapping === void 0) {
      log.error("MarkerPointer: resumeSnapping is called before pauseSnapping");
      this.setSnapping(true);
    } else {
      this.setSnapping(this.oldSnapping);
      this.oldSnapping = void 0;
    }
  }
  enable({ lngLat, customMarker, invisibleMarker } = {
    lngLat: [0, 0],
    customMarker: void 0,
    invisibleMarker: false
  }) {
    if (isTouchScreen()) {
      return;
    }
    if (customMarker && invisibleMarker) {
      throw new Error("MarkerPointer: customMarker and invisibleMarker can't be used together");
    }
    if (this.marker) {
      throw new Error("MarkerPointer: marker is already enabled");
    }
    this.gm.events.bus.attachEvents(this.eventHandlers);
    if (invisibleMarker) {
      this.marker = this.createInvisibleMarker(lngLat || [0, 0]);
    } else {
      this.marker = customMarker || this.createMarker(lngLat || [0, 0]);
    }
    if (this.gm.getActiveDrawModes().length) {
      this.gm.mapAdapter.setCursor("crosshair");
    }
  }
  disable() {
    if (this.marker) {
      this.gm.events.bus.detachEvents(this.eventHandlers);
      this.marker.remove();
      this.marker = null;
    }
    this.gm.mapAdapter.setCursor("");
  }
  createMarker(lngLat = [0, 0]) {
    return this.gm.mapAdapter.createDomMarker(
      {
        anchor: "center",
        element: this.gm.createSvgMarkerElement("control", { pointerEvents: "none" })
      },
      lngLat
    );
  }
  createInvisibleMarker(lngLat = [0, 0]) {
    const htmlElement = document.createElement("div");
    htmlElement.style.width = "0px";
    htmlElement.style.height = "0px";
    return this.gm.mapAdapter.createDomMarker(
      {
        anchor: "center",
        element: htmlElement
      },
      lngLat
    );
  }
  onMouseMove(event) {
    if (isMapPointerEvent(event, { warning: true }) && this.marker) {
      if (this.snapping && this.snappingHelper) {
        const point2 = [event.point.x, event.point.y];
        const snappedLngLat = this.snappingHelper.getSnappedLngLat(event.lngLat.toArray(), point2);
        this.marker.setLngLat(snappedLngLat);
      } else {
        this.marker.setLngLat(event.lngLat.toArray());
      }
    }
    return { next: true };
  }
  syncTmpMarker(lngLat) {
    if (!this.tmpMarker) {
      this.tmpMarker = this.createMarker(lngLat);
    }
    this.tmpMarker.setLngLat(lngLat);
  }
}
const customShapeTriangle = {
  type: "Feature",
  properties: {
    shape: "polygon"
  },
  geometry: {
    type: "MultiPolygon",
    coordinates: [
      [
        [
          [4, 51.2],
          [5.4, 52.4],
          [6.8, 51.2],
          [4, 51.2]
        ]
      ]
    ]
  }
};
const customShapeRectangle = {
  type: "Feature",
  properties: {
    shape: "rectangle"
  },
  geometry: {
    type: "Polygon",
    coordinates: [
      [
        [-0.47, 51.67],
        [1.43, 51.67],
        [1.43, 53.32],
        [-0.47, 53.32],
        [-0.47, 51.67]
      ]
    ]
  }
};
const isGmFeatureBeforeCreateEvent = (payload) => {
  return isGmEvent(payload) && payload.name === `${GM_SYSTEM_PREFIX}:feature:before_create`;
};
const isGmFeatureBeforeUpdateEvent = (payload) => {
  return isGmEvent(payload) && payload.name === `${GM_SYSTEM_PREFIX}:feature:before_update`;
};
const modeActions = ["mode_start", "mode_started", "mode_end", "mode_ended"];
const isGmModeEvent = (payload) => {
  return isGmEvent(payload) && includesWithType(payload.action, modeActions);
};
const isNonEmptyArray = (arr) => {
  return arr.length > 0;
};
function isDefined(value) {
  return value !== null && value !== void 0;
}
function union2(features, options = {}) {
  const geoms = [];
  geomEach(features, (geom) => {
    geoms.push(geom.coordinates);
  });
  if (geoms.length < 2) {
    throw new Error("Must have at least 2 geometries");
  }
  const unioned = union(geoms[0], ...geoms.slice(1));
  if (unioned.length === 0) return null;
  if (unioned.length === 1) return polygon(unioned[0], options.properties);
  else return multiPolygon(unioned, options.properties);
}
var index_default = union2;
class BaseGroupEdit extends BaseEdit {
  features = [];
  featureData = null;
  eventHandlers = {
    click: this.onMouseClick.bind(this)
  };
  onStartAction() {
  }
  onEndAction() {
    this.features.forEach((featureData) => {
      featureData.changeSource({ sourceName: SOURCES.main, atomic: true });
    });
    this.features = [];
  }
  onMouseClick(event) {
    if (!isMapPointerEvent(event)) {
      return { next: true };
    }
    const featureUnselected = this.unselectFeature(event);
    if (featureUnselected) {
      return { next: true };
    }
    const featureData = this.getAllowedFeatureByMouseEvent({ event, sourceNames: [SOURCES.main] });
    if (featureData && this.isFeatureAllowedToGroup(featureData)) {
      featureData.changeSource({ sourceName: SOURCES.temporary, atomic: true });
      this.features.push(featureData);
    }
    if (this.features.length > 1) {
      this.groupFeatures();
      return { next: true };
    }
    return { next: true };
  }
  unselectFeature(event) {
    const tmpFeatureData = this.getAllowedFeatureByMouseEvent({
      event,
      sourceNames: [SOURCES.temporary]
    });
    if (tmpFeatureData) {
      const featureIndex = this.features.findIndex((featureData) => featureData === tmpFeatureData);
      if (featureIndex > -1) {
        this.features.splice(featureIndex, 1);
      }
      tmpFeatureData.changeSource({ sourceName: SOURCES.main, atomic: true });
      return true;
    }
    return false;
  }
  getAllowedFeatureByMouseEvent({
    event,
    sourceNames
  }) {
    const featureData = this.getFeatureByMouseEvent({ event, sourceNames });
    if (featureData && this.allowedShapeTypes.includes(featureData.shape)) {
      return featureData;
    }
    return null;
  }
  isFeatureAllowedToGroup(featureData) {
    if (!this.allowedShapeTypes.includes(featureData.shape)) {
      return false;
    }
    if (this.features.length === 0) {
      return true;
    }
    const newFeatureGeoJson = featureData.getGeoJson();
    return this.features.every(
      (feature2) => index_default$3(feature2.getGeoJson(), newFeatureGeoJson)
    );
  }
  groupFeatures() {
    if (!this.features.length) {
      log.error("BaseGroupEdit.groupFeatures: no features to group");
      return;
    }
    const featureCollection2 = {
      type: "FeatureCollection",
      features: this.features.map((featureData) => {
        const geoJson = featureData.getGeoJson();
        if (["Polygon", "MultiPolygon"].includes(geoJson.geometry.type)) {
          return geoJson;
        }
        return null;
      }).filter((feature2) => !!feature2)
    };
    let resultGeoJson = null;
    if (this.mode === "union") {
      resultGeoJson = index_default(featureCollection2);
    } else if (this.mode === "difference") {
      resultGeoJson = index_default$2(featureCollection2);
    }
    if (resultGeoJson) {
      const featureData = this.gm.features.createFeature({
        shapeGeoJson: {
          ...resultGeoJson,
          properties: {
            ...resultGeoJson.properties,
            shape: "polygon"
          }
        },
        sourceName: SOURCES.main
      });
      this.features.forEach((feature2) => {
        this.gm.features.delete(feature2);
      });
      if (featureData && isNonEmptyArray(this.features)) {
        this.fireFeatureUpdatedEvent({
          sourceFeatures: this.features,
          targetFeatures: [featureData]
        });
      }
      this.features = [];
    }
  }
}
class Geoman {
  mapAdapterInstance = null;
  globalLngLatBounds = this.getGlobalLngLatBounds();
  features;
  loaded = false;
  destroyed = false;
  options;
  events;
  control;
  actionInstances = {};
  markerPointer;
  constructor(map, options = {}) {
    this.options = this.initCoreOptions(options);
    this.events = this.initCoreEvents();
    this.features = this.initCoreFeatures();
    this.control = this.initCoreControls();
    this.markerPointer = this.initMarkerPointer();
    const mapWithGeoman = Object.assign(map, { gm: this });
    this.mapAdapterInstance = getMapAdapter(this, mapWithGeoman);
    this.waitForBaseMap().then(this.init.bind(this)).catch((error) => {
      log.error("Geoman initialization failed:", error);
      this.destroy();
    });
  }
  get drawClassMap() {
    return drawClassMap;
  }
  get editClassMap() {
    return editClassMap;
  }
  get helperClassMap() {
    return helperClassMap;
  }
  get mapAdapter() {
    if (this.mapAdapterInstance) {
      return this.mapAdapterInstance;
    }
    log.trace("Map adapter is not initialized");
    throw new Error("Map adapter is not initialized");
  }
  initCoreOptions(options = {}) {
    return new GmOptions(this, options);
  }
  initCoreEvents() {
    return new GmEvents(this);
  }
  initCoreFeatures() {
    return new Features(this);
  }
  initCoreControls() {
    return new GMControl(this);
  }
  initMarkerPointer() {
    return new MarkerPointer(this);
  }
  addControls(controlsElement = void 0) {
    return new Promise((resolve) => {
      const handleCreateControls = async () => {
        if (controlsElement) {
          this.control.createControls(controlsElement);
        } else {
          this.mapAdapter.addControl(this.control);
        }
        await this.onMapLoad();
        resolve();
      };
      handleCreateControls().then();
    });
  }
  async waitForBaseMap() {
    const map = this.mapAdapter.mapInstance;
    if (!isMapWithOnceMethod(map)) {
      log.error('Map instance does not have a "once" method', map);
      return;
    }
    if (this.mapAdapter.isLoaded()) {
      return map;
    }
    await withPromiseTimeoutRace(
      new Promise((resolve) => {
        const onLoad = () => resolve(map);
        map.once("load", onLoad);
        if (this.mapAdapter.isLoaded()) {
          map.off("load", onLoad);
          resolve(map);
        }
      }),
      "waitForBaseMap failed"
    );
    return map;
  }
  async waitForGeomanLoaded() {
    if (this.loaded) {
      return this;
    }
    if (this.destroyed) {
      return;
    }
    const map = await this.waitForBaseMap();
    if (!map) {
      log.error("Map instance is not available", map);
      return;
    }
    const eventName = `${GM_PREFIX}:loaded`;
    await withPromiseTimeoutRace(
      new Promise((resolve) => {
        const onLoaded = () => resolve(this);
        map.once(eventName, onLoaded);
        if (this.loaded) {
          map.off(eventName, onLoaded);
          resolve(this);
        }
      }),
      "waitForGeomanLoaded failed"
    );
    return this;
  }
  async init() {
    if (this.destroyed) {
      return;
    }
    this.features.init();
    if (this.destroyed) {
      return;
    }
    await this.addControls();
  }
  /**
   * Destroys the Geoman instance and cleans up resources.
   *
   * This method can be called at any point in the lifecycle:
   * - Before initialization completes: cancels pending init and cleans up synchronously
   * - After initialization completes: performs full cleanup including controls
   *
   * For React StrictMode compatibility, this method performs synchronous cleanup
   * of the `gm` reference on the map instance, allowing immediate re-initialization.
   */
  async destroy({ removeSources } = { removeSources: false }) {
    this.destroyed = true;
    if (this.mapAdapterInstance && "gm" in this.mapAdapterInstance.mapInstance) {
      delete this.mapAdapterInstance.mapInstance.gm;
    }
    if (this.loaded) {
      this.removeControls();
      this.mapAdapter.removeImage("default-marker");
    } else {
      this.events.bus.detachAllEvents();
    }
    if (removeSources) {
      for (const source2 of Object.values(this.features.sources)) {
        if (source2) {
          source2.remove();
        }
      }
    }
  }
  removeControls() {
    this.disableAllModes();
    this.mapAdapter.removeControl(this.control);
  }
  async onMapLoad() {
    if (this.loaded || this.destroyed) {
      return;
    }
    await this.mapAdapter.loadImage({
      id: "default-marker",
      image: defaultMarker$1
    });
    if (this.destroyed) {
      return;
    }
    const payload = {
      name: `${GM_SYSTEM_PREFIX}:control:load`,
      level: "system",
      actionType: "control",
      action: "loaded"
    };
    this.events.fire(`${GM_SYSTEM_PREFIX}:control`, payload);
    this.loaded = true;
  }
  disableAllModes() {
    typedKeys(this.actionInstances).forEach((key) => {
      const [actionType, mode] = key.split("__");
      if (isModeType(actionType) && isModeName(mode)) {
        this.options.disableMode(actionType, mode);
      }
    });
  }
  getActiveDrawModes() {
    return typedKeys(this.actionInstances).map((key) => {
      const instance = this.actionInstances[key];
      return instance instanceof BaseDraw ? instance.mode : null;
    }).filter((mode) => mode !== null);
  }
  getActiveEditModes() {
    return typedKeys(this.actionInstances).map((key) => {
      const instance = this.actionInstances[key];
      return instance instanceof BaseEdit ? instance.mode : null;
    }).filter((mode) => mode !== null);
  }
  getActiveHelperModes() {
    return typedKeys(this.actionInstances).map((key) => {
      const instance = this.actionInstances[key];
      return instance instanceof BaseHelper ? instance.mode : null;
    }).filter((mode) => mode !== null);
  }
  getGlobalLngLatBounds() {
    const MAX_VALID_LATITUDE = 85.051129;
    return [
      [-179.99999, -MAX_VALID_LATITUDE],
      [179.99999, MAX_VALID_LATITUDE]
    ];
  }
  setGlobalEventsListener(callback = null) {
    this.events.bus.forwarder.globalEventsListener = callback;
  }
  createSvgMarkerElement(type, style = void 0) {
    const markerIcons = this.options.settings.markerIcons;
    if (!markerIcons[type]) {
      log.error(`createMarkerElement: marker type "${type}" not found`);
    }
    const element = document.createElement("div");
    element.classList.add("marker-wrapper");
    element.style.lineHeight = "0";
    element.innerHTML = markerIcons[type] || "NO_ICON";
    const svgElement = element.firstChild;
    if (typeof svgElement !== "object") {
      log.error(`createMarkerElement: no icon "${type}" found`);
      throw new Error(`No icon "${type}" found`);
    }
    if (style) {
      Object.assign(svgElement.style, style);
    }
    return element;
  }
  enableMode(actionType, modeName) {
    this.options.enableMode(actionType, modeName);
  }
  disableMode(actionType, modeName) {
    this.options.disableMode(actionType, modeName);
  }
  toggleMode(actionType, modeName) {
    this.options.toggleMode(actionType, modeName);
  }
  isModeEnabled(actionType, modeName) {
    return this.options.isModeEnabled(actionType, modeName);
  }
  // helper methods for compatibility with the old API
  // draw (draw:*)
  enableDraw(shape) {
    this.options.enableMode("draw", shape);
  }
  disableDraw() {
    this.getActiveDrawModes().forEach((shape) => this.options.disableMode("draw", shape));
  }
  toggleDraw(shape) {
    this.options.toggleMode("draw", shape);
  }
  drawEnabled(shape) {
    return this.options.isModeEnabled("draw", shape);
  }
  // drag(edit:drag)
  enableGlobalDragMode() {
    this.options.enableMode("edit", "drag");
  }
  disableGlobalDragMode() {
    this.options.disableMode("edit", "drag");
  }
  toggleGlobalDragMode() {
    this.options.toggleMode("edit", "drag");
  }
  globalDragModeEnabled() {
    return this.options.isModeEnabled("edit", "drag");
  }
  // edit (edit:change)
  enableGlobalEditMode() {
    this.options.enableMode("edit", "change");
  }
  disableGlobalEditMode() {
    this.options.disableMode("edit", "change");
  }
  toggleGlobalEditMode() {
    this.options.toggleMode("edit", "change");
  }
  globalEditModeEnabled() {
    return this.options.isModeEnabled("edit", "change");
  }
  // rotate (edit:rotate)
  enableGlobalRotateMode() {
    this.options.enableMode("edit", "rotate");
  }
  disableGlobalRotateMode() {
    this.options.disableMode("edit", "rotate");
  }
  toggleGlobalRotateMode() {
    this.options.toggleMode("edit", "rotate");
  }
  globalRotateModeEnabled() {
    return this.options.isModeEnabled("edit", "rotate");
  }
  // cut (edit:cut)
  enableGlobalCutMode() {
    this.options.enableMode("edit", "cut");
  }
  disableGlobalCutMode() {
    this.options.disableMode("edit", "cut");
  }
  toggleGlobalCutMode() {
    this.options.toggleMode("edit", "cut");
  }
  globalCutModeEnabled() {
    return this.options.isModeEnabled("edit", "cut");
  }
  // remove (edit:delete)
  enableGlobalRemovalMode() {
    this.options.enableMode("edit", "delete");
  }
  disableGlobalRemovalMode() {
    this.options.disableMode("edit", "delete");
  }
  toggleGlobalRemovalMode() {
    this.options.toggleMode("edit", "delete");
  }
  globalRemovalModeEnabled() {
    return this.options.isModeEnabled("edit", "delete");
  }
}
const createGeomanInstance = async (map, options) => {
  const geoman = new Geoman(map, options);
  const result = await geoman.waitForGeomanLoaded();
  if (!result || geoman.destroyed) {
    throw new Error("Geoman initialization failed");
  }
  return geoman;
};
export {
  BaseAction,
  BaseDomMarker,
  BaseDrag,
  BaseDraw,
  BaseEdit,
  BaseGroupEdit,
  BaseHelper,
  BaseLayer,
  BaseMapAdapter,
  BasePopup,
  BaseSource,
  DRAW_MODES,
  EDIT_MODES,
  EXTRA_DRAW_MODES,
  FEATURE_ID_PROPERTY,
  FEATURE_PROPERTY_PREFIX,
  FeatureData,
  GM_PREFIX,
  Geoman,
  GmOptions,
  HELPER_MODES,
  IS_PRO,
  LineDrawer,
  MarkerPointer,
  SHAPE_NAMES,
  SOURCES,
  ShapeMarkersHelper,
  boundsContains,
  boundsToBBox,
  calculatePerimeter,
  controlIcons,
  convertToLineStringFeatureCollection,
  convertToThrottled,
  createGeomanInstance,
  customShapeRectangle,
  customShapeTriangle,
  styles as defaultLayerStyles,
  drawClassMap,
  eachCoordinateWithPath,
  eachSegmentWithPath,
  editClassMap,
  findCoordinateWithPath,
  formatArea,
  formatDistance,
  geoJsonPointToLngLat,
  getEuclideanDistance,
  getEuclideanSegmentNearestPoint,
  getGeoJsonCoordinatesCount,
  getGeoJsonFirstPoint,
  getLngLatDiff,
  helperClassMap,
  includesWithType,
  isActionType,
  isBaseMapEventName,
  isDefined,
  isDrawModeName,
  isEditModeName,
  isEqualPosition,
  isGeoJsonFeatureInPolygon,
  isGmControlEvent,
  isGmDrawEvent,
  isGmDrawFreehandDrawerEvent,
  isGmDrawLineDrawerEvent,
  isGmDrawShapeEvent,
  isGmEditEvent,
  isGmEvent,
  isGmFeatureBeforeCreateEvent,
  isGmFeatureBeforeUpdateEvent,
  isGmHelperEvent,
  isGmModeEvent,
  isHelperModeName,
  isMapPointerEvent,
  isMapWithOnceMethod,
  isModeName,
  isMultiPolygonFeature,
  isNonEmptyArray,
  isPointerEventName,
  isPolygonFeature,
  lngLatToGeoJsonPoint,
  mergeByTypeCustomizer,
  moveFeatureData,
  moveGeoJson,
  toMod,
  twoCoordsToLineString,
  typedKeys
};
//# sourceMappingURL=maplibre-geoman.es.js.map
